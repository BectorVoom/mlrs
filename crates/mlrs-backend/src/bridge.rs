//! TODO(plan 03): Arrow zero-copy bridge with hard-reject validation —
//! reject non-zero offset / nulls / misalignment before any unsafe transmute
//! (FOUND-06 / D-06), returning a typed `BridgeError` (D-07). Stub in Wave 0.
