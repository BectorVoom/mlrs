//! `mlrs-algos` — sklearn-compatible ML estimators.
//!
//! Intentionally empty in Phase 1; estimators (LinearRegression, KMeans, PCA,
//! KNN, ...) are built in Phase 4+. This skeleton reserves the crate's place in
//! the workspace DAG so downstream phases edit only this crate.
