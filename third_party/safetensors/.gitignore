# Excluded from the workspace (root Cargo.toml `workspace.exclude`), so running
# this crate's own suite builds into a target/ dir of its own.
/target
# Artifacts safetensors' own tests write into the crate root
# (test_serialize_to_file, test_serialize_to_file_same_path_with_active_mmap).
/model.safetensors
/out.safetensors
