---
phase: 08-kernel-family
plan: 02
type: execute
wave: 1
depends_on: ["08-01"]
files_modified:
  - crates/mlrs-kernels/src/elementwise.rs
  - crates/mlrs-kernels/src/lib.rs
  - crates/mlrs-backend/src/prims/kernel_matrix.rs
  - crates/mlrs-backend/tests/kernel_matrix_test.rs
autonomous: true
requirements: [PRIM-08]
must_haves:
  truths:
    - "kernel_matrix(X, Y, Kernel::Linear/Rbf/Poly/Sigmoid) returns the correct K(X,Y) within tolerance for f32 and f64 vs a host reference"
    - "RBF dispatches the v1 distance prim (sqrt=false); linear/poly/sigmoid dispatch the v1 gemm prim (transb=true)"
    - "The map kernel is SharedMemory-free, atomics-free, F/u32 accumulators only, never F::INFINITY"
    - "Driving kernel_matrix N times at fixed shape does not grow live_bytes or peak_bytes after warmup (PoolStats gate, build-failing)"
  artifacts:
    - path: "crates/mlrs-backend/src/prims/kernel_matrix.rs"
      provides: "kernel_matrix host orchestration (base-op dispatch + in-place map)"
      contains: "fn kernel_matrix"
    - path: "crates/mlrs-kernels/src/elementwise.rs"
      provides: "rbf/poly/sigmoid #[cube(launch)] map kernels"
      contains: "rbf_map"
    - path: "crates/mlrs-backend/tests/kernel_matrix_test.rs"
      provides: "PRIM-08 value tests (4 kernels, f32+f64) + PoolStats memory gate"
      contains: "kernel_matrix_memory_gate"
  key_links:
    - from: "crates/mlrs-backend/src/prims/kernel_matrix.rs"
      to: "prims::distance / prims::gemm"
      via: "base-op dispatch on the Kernel enum"
      pattern: "distance::|gemm::"
    - from: "crates/mlrs-backend/src/prims/kernel_matrix.rs"
      to: "mlrs_kernels::rbf_map / poly_map / sigmoid_map"
      via: "in-place map launch over the base buffer"
      pattern: "_map::launch"
---

<objective>
Land + standalone-validate the keystone `prims/kernel_matrix.rs` primitive (PRIM-08): one
SharedMemory-free elementwise map over the v1 distance (RBF) / gemm (linear/poly/sigmoid)
base, producing the typed `Kernel<F>` matrix `K(X, Y)` (D-01/D-02/D-03). This is the
seam KernelRidge (Plan 03) and Phase 9 spectral affinity reuse. It must validate vs a host
reference for f32 + f64 AND pass its build-failing PoolStats memory gate BEFORE any estimator
wires to it (primitive-first discipline, mirroring Phase-7 rng/incremental_svd gating).

Purpose: Establish the validated kernel-matrix base op so KernelRidge is a thin dual solve on
top of it. This is the wave dependency gate — Plan 03 does NOT start until this is green.
Output: kernel_matrix host fn + rbf/poly/sigmoid map kernels + green value/memory tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-kernel-family/08-CONTEXT.md
@.planning/phases/08-kernel-family/08-RESEARCH.md
@.planning/phases/08-kernel-family/08-PATTERNS.md
@.planning/phases/08-kernel-family/08-VALIDATION.md
@crates/mlrs-backend/src/prims/covariance.rs
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: rbf/poly/sigmoid map kernels (SharedMemory-free, F::exp/tanh/powf)</name>
  <files>crates/mlrs-kernels/src/elementwise.rs, crates/mlrs-kernels/src/lib.rs</files>
  <behavior>
    - rbf_map(input=sqdist, output, gamma): output[tid] = F::exp(-gamma * input[tid]) for tid < len.
    - poly_map(input=G, output, gamma, coef0, degree): output[tid] = F::powf(gamma*G + coef0, degree).
    - sigmoid_map(input=G, output, gamma, coef0): output[tid] = F::tanh(gamma*G + coef0).
    - linear: identity — NO kernel (kernel_matrix returns the gemm buffer directly).
    - All bounds-checked `if tid < input.len()`; scalar F params by value; never F::INFINITY; no SharedMemory; no atomics.
  </behavior>
  <read_first>
    - crates/mlrs-kernels/src/elementwise.rs:18-23 (CubeElement-bound doc for by-value scalar F), :64-70 (`scale` shape to copy), :107-127 (`dist_combine_clamp`), :38-48 (`clamp_nonneg` statement-form clamp), :1-27 (module doc / cpu-MLIR-safety precedent). 08-PATTERNS.md §elementwise.rs lines 85-116.
    - crates/mlrs-kernels/src/lib.rs — existing `pub use` of `scale`/`center_columns`/`dist_combine_clamp` to extend.
    - 08-RESEARCH.md §"Kernel formulas" (linear/poly/sigmoid/rbf bodies) + §Pattern 2 (transcendental static-fn form) + Pitfall 7 (`F::exp(x)` NOT `x.exp()`).
  </read_first>
  <action>
    Add three `#[cube(launch)] pub fn` map kernels to elementwise.rs, copying the exact shape of
    `scale` (`elementwise.rs:64-70`): `rbf_map<F>(input, output, gamma)` = `F::exp(-gamma *
    input[tid])` (input is squared-euclidean distance); `poly_map<F>(input, output, gamma, coef0,
    degree)` = `F::powf(gamma * input[tid] + coef0, degree)` (powf = sklearn-faithful real degree,
    A3); `sigmoid_map<F>(input, output, gamma, coef0)` = `F::tanh(gamma * input[tid] + coef0)`.
    `linear` needs NO kernel (identity). Use static associated fns ONLY (`F::exp`/`F::powf`/
    `F::tanh`) — never instance methods (Pitfall 7). Bounds-check `if tid < input.len()`. No
    SharedMemory, no atomics, no `F::INFINITY`. Add the three fns to lib.rs `pub use`. Ignore any
    rust-analyzer macro-expansion noise inside `#[cube]` ([[rust-analyzer-no-feature-false-positives]]);
    verify with `cargo build --features cpu`. On any build error, consult
    /home/user/Documents/workspace/cintx/docs/cubecl_error_guideline.md (AGENTS.md rule).
  </action>
  <verify>
    <automated>cargo build -p mlrs-kernels --features cpu 2>&1 | tail -5; grep -Eq "F::exp|F::powf|F::tanh" crates/mlrs-kernels/src/elementwise.rs && ! grep -q "F::INFINITY" crates/mlrs-kernels/src/elementwise.rs && grep -c "SharedMemory" crates/mlrs-kernels/src/elementwise.rs | grep -qx 0 && echo MAPKERNELS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p mlrs-kernels --features cpu` succeeds.
    - elementwise.rs defines `rbf_map`, `poly_map`, `sigmoid_map` using `F::exp`/`F::powf`/`F::tanh`.
    - No `F::INFINITY` and no `SharedMemory` introduced by the new kernels (grep gates).
    - lib.rs re-exports the three new map fns.
  </acceptance_criteria>
  <done>Three SharedMemory-free map kernels compile under `--features cpu` and are re-exported; cpu-MLIR landmines absent.</done>
</task>

<task type="auto">
  <name>Task 2: kernel_matrix host orchestration (base-op dispatch + in-place map) + value test</name>
  <files>crates/mlrs-backend/src/prims/kernel_matrix.rs, crates/mlrs-backend/tests/kernel_matrix_test.rs</files>
  <read_first>
    - crates/mlrs-backend/src/prims/covariance.rs:42-52 (imports), :151-204 (base-op → in-place map idiom — the load-bearing pattern), :212-262 (geometry validation BEFORE launch), :266-273 (`launch_dims_1d`). 08-PATTERNS.md §kernel_matrix.rs lines 34-82.
    - crates/mlrs-backend/src/prims/distance.rs:79 — `distance::<F>(pool, x, (rows_x,cols), y, (rows_y,cols), false, out)` (RBF base, sqrt=false → squared, D-03/Pitfall 4).
    - crates/mlrs-backend/src/prims/gemm.rs:54 — `gemm::<F>(pool, x, (rows_x,cols), y, (cols,rows_y), false, true, out)` (XYᵀ Gram, transb=true, linear/poly/sigmoid base).
    - crates/mlrs-algos/tests/../mlrs-backend/tests/incremental_svd_test.rs:45 (`F32_MERGE_TOL`), :48-64 (fixture loader/`from_f64`), :170-181 (`skip_f64_with_log`). 08-PATTERNS.md §kernel_matrix_test.rs lines 280-301.
    - The Plan-01 stub (Kernel<F> enum + kernel_matrix signature already in scope).
  </read_first>
  <action>
    Flesh out the Plan-01 `kernel_matrix` stub body, copying the covariance.rs base-op→in-place-map
    idiom verbatim (covariance.rs:151-204). Match on `kernel: Kernel<F>` (D-01):
    - `Linear`: `gemm(x, (rows_x,cols), y, (cols,rows_y), transa=false, transb=true, out)` → return
      the GEMM buffer directly (identity, no map launch).
    - `Rbf { gamma }`: `distance(x, (rows_x,cols), y, (rows_y,cols), sqrt=false, out)` → squared
      base; launch `rbf_map` IN PLACE (in_arg handle == out_arg handle) with `gamma` by value.
    - `Poly { gamma, degree, coef0 }`: gemm base → `poly_map` in place with gamma/coef0/degree.
    - `Sigmoid { gamma, coef0 }`: gemm base → `sigmoid_map` in place with gamma/coef0.
    Always compute the full general `rows_x × rows_y` K(X,Y) (D-02 — no symmetry special-case).
    Geometry validation BEFORE any `unsafe ArrayArg::from_raw_parts` (mirror covariance.rs:212-262,
    ASVS V5 / T-08-02-01): `rows_x*cols == x.len()`, `rows_y*cols == y.len()`, reject empty,
    validate `out` len == `rows_x*rows_y` → `PrimError::ShapeMismatch`. Use `launch_dims_1d` over
    `n = rows_x*rows_y` (copy covariance.rs:266-273). The map runs over the FULL n×n buffer in
    global memory — large n×n operands stay in global (gfx1100 LDS ≤ 65536 B); no SharedMemory tile.

    Wire the value test: remove `#[ignore]` from the kernel_matrix value-test stubs; for each of
    the 4 kernels load the committed `kernel_matrix_*` fixture, compute K, `assert_slice_close` —
    f64 strict (`F64_TOL`), f32 documented band `Tolerance::new(1e-4, 1e-4)` (mirror
    `F32_MERGE_TOL`). Keep the `skip_f64_with_log` gate verbatim (incremental_svd_test.rs:170-181):
    cpu runs f64, rocm skips-with-log.
  </action>
  <verify>
    <automated>cargo test --features cpu -p mlrs-backend kernel_matrix 2>&1 | tail -15; grep -Eq "distance::|gemm::" crates/mlrs-backend/src/prims/kernel_matrix.rs && grep -q "_map::launch" crates/mlrs-backend/src/prims/kernel_matrix.rs && echo KM_VALUE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --features cpu -p mlrs-backend kernel_matrix` passes the 4-kernel f32+f64 value cases (f64 behind `skip_f64_with_log`).
    - kernel_matrix.rs dispatches `distance::` (RBF) and `gemm::` (linear/poly/sigmoid) and launches `*_map`.
    - Geometry validation returns `PrimError::ShapeMismatch` before any `unsafe` launch.
  </acceptance_criteria>
  <done>kernel_matrix produces correct K(X,Y) for all 4 kernels within tolerance (f32 + f64), validated vs the host-reference fixtures.</done>
</task>

<task type="auto">
  <name>Task 3: kernel_matrix PoolStats memory gate (build-failing)</name>
  <files>crates/mlrs-backend/tests/kernel_matrix_test.rs</files>
  <read_first>
    - crates/mlrs-backend/tests/incremental_svd_test.rs:201-269 — the PoolStats memory-gate structure to copy (drive the prim N times at fixed shape; assert no live/peak growth after warmup). 08-PATTERNS.md §kernel_matrix_test.rs lines 297-301.
    - .planning/STATE.md [02-05] + [05-11] decisions — the PoolStats free-list-probe / `live_after[w] <= live_after[1]` gate convention (counters backend-agnostic).
  </read_first>
  <action>
    Remove `#[ignore]` from the `kernel_matrix_memory_gate` stub and implement it copying the
    incremental_svd_test.rs:201-269 structure: drive `kernel_matrix` N=5 times at a fixed n×n shape
    (use the Rbf branch so both the distance base AND the in-place map exercise the pool), collect
    `pool.stats().live_bytes` / `peak_bytes` after each iteration, and assert HARD (build-failing):
    `live_after[w] <= live_after[1]` for w in 2..N (no allocation growth after warmup — the
    in-place map reuses the base buffer, no parallel allocation) AND `peak_after` plateaus. This is
    the per-prim PoolStats gate (D-03 / the keystone's mandatory memory contract). Keep it under
    `--features cpu` (counters are backend-agnostic per [02-05]).
  </action>
  <verify>
    <automated>cargo test --features cpu -p mlrs-backend kernel_matrix_memory_gate 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --features cpu -p mlrs-backend kernel_matrix_memory_gate` passes.
    - The gate asserts `live_after[w] <= live_after[1]` and a plateauing `peak_after` over N≥5 iterations (build-failing on regression).
  </acceptance_criteria>
  <done>The PoolStats memory gate is green: kernel_matrix's live/peak bytes do not grow after warmup over repeated fixed-shape calls.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host caller → kernel_matrix prim | Caller-supplied X/Y arrays + (rows,cols) geometry + Kernel<F> params cross here. Pure numeric; no network/auth/secrets/filesystem. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02-01 | Tampering | kernel_matrix geometry args | mitigate | Validate `rows_x*cols==x.len()`, `rows_y*cols==y.len()`, non-empty, `out` length BEFORE any `unsafe ArrayArg::from_raw_parts` → `PrimError::ShapeMismatch` (mirror covariance.rs:212-262, ASVS V5). |
| T-08-02-02 | Denial of Service | n×n device memory for large inputs | mitigate | Large n×n operands stay in GLOBAL memory (no SharedMemory tile; gfx1100 LDS ≤ 65536 B); per-prim PoolStats gate bounds repeated-call allocation (Task 3). |
| T-08-02-03 | Tampering/DoS | NaN/inf in K from F::INFINITY in the map | mitigate | Map kernels are F::INFINITY-free (grep gate, Task 1); RBF/poly/sigmoid are finite transcendentals over finite base. |
| T-08-02-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase (workspace deps unchanged; sklearn/scipy/numpy build-time-only in /tmp venv). Package Legitimacy Gate not applicable. |
</threat_model>

<verification>
- `cargo test --features cpu -p mlrs-backend kernel_matrix` green (4-kernel value cases, f32 + f64 with f64 behind `skip_f64_with_log`).
- `cargo test --features cpu -p mlrs-backend kernel_matrix_memory_gate` green (PoolStats gate).
- grep gates: map kernels contain `F::exp`/`F::powf`/`F::tanh`, contain no `F::INFINITY`, introduce no `SharedMemory`.
- Opportunistic `cargo test --features rocm kernel_matrix` (f32 only) within the documented band (manual, gfx1100 host).
</verification>

<success_criteria>
- kernel_matrix(X, Y, Kernel) returns correct K within tolerance for all 4 kernels, f32 + f64.
- RBF → distance(sqrt=false) base; linear/poly/sigmoid → gemm(transb=true) base; map applied in place.
- Map kernels SharedMemory-free, atomics-free, never F::INFINITY.
- PoolStats memory gate green (no live/peak growth after warmup).
- Wave gate satisfied: Plan 03 may now wire KernelRidge to this prim.
</success_criteria>

<output>
Create `.planning/phases/08-kernel-family/08-02-SUMMARY.md` when done.
</output>
