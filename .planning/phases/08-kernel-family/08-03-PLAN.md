---
phase: 08-kernel-family
plan: 03
type: execute
wave: 2
depends_on: ["08-02"]
files_modified:
  - crates/mlrs-algos/src/kernel_ridge/mod.rs
  - crates/mlrs-algos/src/kernel_ridge/kernel_ridge.rs
  - crates/mlrs-algos/tests/kernel_ridge_test.rs
autonomous: true
requirements: [KERNEL-01]
must_haves:
  truths:
    - "A user can fit KernelRidge (4 kernels: linear/rbf/polynomial/sigmoid with gamma/degree/coef0) and predict, matching scikit-learn within 1e-5 (f64)"
    - "Multi-target y (n_samples × n_targets) is solved in one multi-RHS cholesky_solve call (D-04)"
    - "gamma=None resolves to 1/n_features at fit; explicit gamma is used as-is (D-05); both oracle-pinned"
    - "alpha is added to the K diagonal only; NO centering, NO intercept (D-06)"
  artifacts:
    - path: "crates/mlrs-algos/src/kernel_ridge/kernel_ridge.rs"
      provides: "KernelRidge struct (dual_coef_, X_fit_, resolved Kernel<F>) + fit + predict"
      contains: "struct KernelRidge"
    - path: "crates/mlrs-algos/tests/kernel_ridge_test.rs"
      provides: "KERNEL-01 oracle tests (4 kernels, multi-target, gamma None+explicit)"
      contains: "kernel_ridge"
  key_links:
    - from: "crates/mlrs-algos/src/kernel_ridge/kernel_ridge.rs"
      to: "mlrs_backend::prims::kernel_matrix"
      via: "K = kernel_matrix(X, X, kernel) at fit; K_test = kernel_matrix(X_test, X_fit_, kernel) at predict"
      pattern: "kernel_matrix"
    - from: "crates/mlrs-algos/src/kernel_ridge/kernel_ridge.rs"
      to: "mlrs_backend::prims::cholesky_solve"
      via: "(K+αI) multi-RHS dual solve"
      pattern: "cholesky_solve"
---

<objective>
Build `KernelRidge` (KERNEL-01): the dual-coefficient solve of `(K + αI)` via the v1 Cholesky
prim over the Plan-02 `kernel_matrix` keystone. Mirrors `linear/ridge.rs` MINUS centering — the
normal matrix is `K` (not `XᵀX`), `α` goes on the `K` diagonal, multi-target `y` is solved in one
multi-RHS `cholesky_solve` (D-04 near-free), and predict is `K(X_test, X_fit_) · dual_coef_`.
Predict matches scikit-learn ≤ 1e-5 (f64).

Purpose: First estimator on the validated keystone prim, exercising the kernel_matrix seam end to
end. File-disjoint from Plan 04 (KernelDensity) — both depend only on Plan 02, run in the same wave.
Output: KernelRidge fit/predict + green ≤1e-5 oracle (4 kernels, multi-target, gamma both paths).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-kernel-family/08-CONTEXT.md
@.planning/phases/08-kernel-family/08-RESEARCH.md
@.planning/phases/08-kernel-family/08-PATTERNS.md
@.planning/phases/08-kernel-family/08-VALIDATION.md
@crates/mlrs-algos/src/linear/ridge.rs
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: KernelRidge struct + fit (kernel_matrix → diagonal-α → multi-RHS Cholesky)</name>
  <files>crates/mlrs-algos/src/kernel_ridge/mod.rs, crates/mlrs-algos/src/kernel_ridge/kernel_ridge.rs</files>
  <behavior>
    - new(kernel, alpha, gamma_opt, degree, coef0) constructs unfit; validate alpha>=0 (InvalidAlpha), degree>=1 (InvalidDegree), kernel name (InvalidKernel) BEFORE any launch.
    - fit(X (n×d), y (n×t)): gamma=None → 1/n_features (D-05); build the typed Kernel<F>; K = kernel_matrix(X, X, kernel) (n×n, D-02); add alpha to K diagonal only (D-06); dual_coef_ = cholesky_solve(K+αI, y, n, rhs=t) (n×t, D-04).
    - NO centering, NO x_mean/y_mean/intercept_ (D-06 / Pitfall 1).
    - Stores dual_coef_ (n×t), X_fit_ (n×d), resolved Kernel<F>.
  </behavior>
  <read_first>
    - crates/mlrs-algos/src/linear/ridge.rs:68-122 (struct + accessors + NotFitted), :128-166 (fit validate), :248-254 (diagonal-α host pass — copy VERBATIM), :276-280 (`cholesky_solve(pool, a, b, n, rhs, out)` call), :378-393 (`host_to_f64`/`f64_to_host` helpers). 08-PATTERNS.md §kernel_ridge lines 151-182.
    - **DELETE analog:** ridge.rs:168-223 + :282-295 (centering/x_mean/y_mean/intercept_) — do NOT copy (D-06 / Pitfall 1).
    - crates/mlrs-backend/src/prims/cholesky.rs:191 (`n ≤ MAX_DIM=64` cap — Pitfall 6 / A2; oracle n_samples must be ≤ 64).
    - crates/mlrs-backend/src/prims/kernel_matrix.rs (Plan-02 `kernel_matrix` + `Kernel<F>` enum).
    - 08-RESEARCH.md §"KernelRidge — exact sklearn math" (dual solve, `K.flat[::n+1]+=alpha`, defaults alpha=1/kernel=linear/gamma=None/degree=3/coef0=1).
  </read_first>
  <action>
    Create the `kernel_ridge.rs` estimator under the Plan-01 module home (edit `kernel_ridge/mod.rs`
    to `pub mod kernel_ridge; pub use kernel_ridge::KernelRidge;` — do NOT touch lib.rs, the Wave-0
    scaffold owns it). Mirror `ridge.rs` struct/accessors/NotFitted (ridge.rs:68-122) but store
    `dual_coef_` (n×t), the fitted `X_fit_` (n×d), and the resolved `Kernel<F>`.

    `fit(X, y)`: validate hyperparameters BEFORE launch (alpha>=0 via existing `InvalidAlpha`;
    degree>=1 via `InvalidDegree`; kernel name via `InvalidKernel`; geometry). Resolve `gamma`:
    `None → 1/n_features` computed from `X.shape[1]`, explicit gamma as-is (D-05); store it in the
    typed `Kernel<F>` so `predict` reuses the identical value (Pitfall 5). Build `K =
    kernel_matrix(X, X, kernel)` (Y=X, D-02, n×n). Add `α` to the K DIAGONAL only — copy the
    ridge.rs:248-254 host pass verbatim (`k_host[i*n+i] += alpha`; cubecl 0.10 has no in-place
    device scalar write, so materialize→mutate→re-upload, recycling the released n² buffer).
    Solve `dual_coef_ = cholesky_solve(K+αI, y, n, rhs=n_targets)` (D-04 multi-RHS, near-free).
    Do NOT center X or y; do NOT compute an intercept (D-06 / Pitfall 1 — the normal matrix is K,
    not XᵀX). Copy `host_to_f64`/`f64_to_host` verbatim (ridge.rs:378-393). Keep oracle
    `n_samples ≤ MAX_DIM=64` (Pitfall 6 / A2). On any cubecl build error consult
    /home/user/Documents/workspace/cintx/docs/cubecl_error_guideline.md.
  </action>
  <verify>
    <automated>cargo build -p mlrs-algos --features cpu 2>&1 | tail -5; grep -q "struct KernelRidge" crates/mlrs-algos/src/kernel_ridge/kernel_ridge.rs && grep -Eq "kernel_matrix|cholesky_solve" crates/mlrs-algos/src/kernel_ridge/kernel_ridge.rs && ! grep -Eq "x_mean|y_mean|intercept_" crates/mlrs-algos/src/kernel_ridge/kernel_ridge.rs && echo KR_FIT_OK</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p mlrs-algos --features cpu` succeeds.
    - kernel_ridge.rs defines `struct KernelRidge`, calls `kernel_matrix` and `cholesky_solve`.
    - NO `x_mean`/`y_mean`/`intercept_` present (grep gate — D-06 / Pitfall 1).
    - Hyperparameter guards (`InvalidAlpha`/`InvalidDegree`/`InvalidKernel`) raised at fit before launch.
  </acceptance_criteria>
  <done>KernelRidge.fit builds K via kernel_matrix, adds α to the diagonal, and solves multi-RHS Cholesky with no centering/intercept; compiles under `--features cpu`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: KernelRidge predict + ≤1e-5 oracle (4 kernels, multi-target, gamma both paths)</name>
  <files>crates/mlrs-algos/src/kernel_ridge/kernel_ridge.rs, crates/mlrs-algos/tests/kernel_ridge_test.rs</files>
  <behavior>
    - predict(X_test (m×d)): K_test = kernel_matrix(X_test, X_fit_, kernel) (m×n); y_pred = K_test · dual_coef_ (gemm, m×t). No intercept broadcast.
    - Predict matches sklearn ≤ 1e-5 (f64 strict) for linear/rbf/poly/sigmoid; multi-target (t=2); gamma=None and explicit; degree=3/coef0=1 defaults exercised.
    - f32 within the documented per-family band; f64 cases behind skip_f64_with_log.
  </behavior>
  <read_first>
    - crates/mlrs-algos/src/linear/ridge.rs:313-373 (predict skeleton — but kernel-based, NO intercept broadcast). 08-PATTERNS.md §kernel_ridge lines 179-180.
    - crates/mlrs-backend/src/prims/gemm.rs:54 (K_test · dual_coef_ matmul).
    - crates/mlrs-algos/tests/ridge_test.rs:23-126 (harness: `fixture`, `host_to_f64`/`f64_to`, `assert_close` numpy-allclose), :171-179 (`skip_f64_with_log`). 08-PATTERNS.md §kernel_ridge_test lines 304-322.
    - 08-RESEARCH.md §"Oracle cases the planner must pin" (the exact case matrix) + §"Claude's Discretion" (f32-on-rocm band; f64 strict ≤1e-5).
  </read_first>
  <action>
    Implement `predict(X_test)`: `K_test = kernel_matrix(X_test, X_fit_, kernel)` (m×n, the stored
    resolved kernel — reuse the fit-time gamma), then `y_pred = K_test · dual_coef_` via gemm
    (m×t). No intercept broadcast (D-06). Mirror ridge.rs:313-373 minus the intercept step.

    Wire the oracle test (remove `#[ignore]` from the Plan-01 kernel_ridge stubs): copy the
    ridge_test.rs:23-126 harness (`fixture`, `host_to_f64`/`f64_to`, `assert_close` numpy-allclose).
    Pin the cases from RESEARCH §Oracle cases: one per kernel (linear/rbf/poly/sigmoid, single
    target), one 2-target case (multi-RHS, D-04), one explicit-gamma + one gamma=None (D-05), at
    least one `degree=3, coef0=1` default poly/sigmoid case. f64 strict `F64_TOL` (≤1e-5); f32 a
    documented per-family band (follow the v1 documented-band precedent — record the exact band in
    the SUMMARY). Gate every f64 case behind `skip_f64_with_log` (ridge_test.rs:171-179) — cpu runs
    f64, rocm skips-with-log.
  </action>
  <verify>
    <automated>cargo test --features cpu -p mlrs-algos kernel_ridge 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --features cpu -p mlrs-algos kernel_ridge` passes all oracle cases.
    - Cases cover all 4 kernels, a 2-target multi-RHS case, gamma=None AND explicit, and a degree=3/coef0=1 default case.
    - f64 predict within 1e-5 of sklearn (behind `skip_f64_with_log`); f32 within the documented band.
    - predict computes `K_test · dual_coef_` via gemm with no intercept broadcast.
  </acceptance_criteria>
  <done>KernelRidge.predict matches scikit-learn ≤ 1e-5 (f64) across all 4 kernels, multi-target, and both gamma paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host caller → KernelRidge.fit/predict | Caller-supplied X/y/X_test arrays + hyperparameters (alpha, gamma, degree, coef0, kernel) cross here. Pure numeric; no network/auth/secrets/filesystem. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03-01 | Tampering | KernelRidge hyperparameters | mitigate | Validate `alpha>=0` (InvalidAlpha), `degree>=1` (InvalidDegree), kernel name (InvalidKernel), geometry — typed AlgoError at fit BEFORE any device launch (ASVS V5, mirror ridge.rs:137-166). |
| T-08-03-02 | Tampering/DoS | non-SPD `K+αI` (NaN poisoning) | mitigate | `cholesky_solve` returns typed `NotPositiveDefinite`, never NaN; α≥0 on an SPD kernel diagonal keeps it well-conditioned. |
| T-08-03-03 | Denial of Service | n_samples > MAX_DIM Cholesky cap | mitigate | Cholesky rejects `n>MAX_DIM=64` with typed `PrimError::NotSquare`; oracle fixtures sized ≤64, the cap documented (Pitfall 6 / A2). |
| T-08-03-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase. Package Legitimacy Gate not applicable. |
</threat_model>

<verification>
- `cargo test --features cpu -p mlrs-algos kernel_ridge` green (4 kernels, multi-target, gamma both paths; f64 ≤1e-5 behind `skip_f64_with_log`, f32 documented band).
- grep gates: kernel_ridge.rs references `kernel_matrix` + `cholesky_solve`, contains no `x_mean`/`y_mean`/`intercept_`.
- Opportunistic `cargo test --features rocm kernel_ridge` (f32) within the documented band (manual, gfx1100 host).
</verification>

<success_criteria>
- KernelRidge fit/predict matches sklearn ≤ 1e-5 (f64) for linear/rbf/poly/sigmoid.
- Multi-target solved via one multi-RHS cholesky_solve (D-04).
- gamma=None → 1/n_features and explicit gamma both oracle-pinned (D-05).
- NO centering, NO intercept (D-06).
- f64 cases behind skip_f64_with_log; f32-on-rocm band documented.
</success_criteria>

<output>
Create `.planning/phases/08-kernel-family/08-03-SUMMARY.md` when done.
</output>
