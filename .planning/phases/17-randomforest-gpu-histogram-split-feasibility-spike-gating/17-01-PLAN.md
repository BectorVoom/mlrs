---
phase: 17-randomforest-gpu-histogram-split-feasibility-spike-gating
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/gen_oracle.py
  - tests/fixtures/  # committed .npz blobs (tree_dt_clf/reg, standard + adversarial, f32+f64)
autonomous: true
requirements: [TREE-01]
user_setup: []

must_haves:
  truths:
    - "Committed sklearn .npz fixtures exist for DecisionTreeClassifier(gini) AND DecisionTreeRegressor(squared_error) built on injected fixed bootstrap/feature indices, f32 + f64 (D-07, D-09)"
    - "At least one adversarial fixture exists carrying a forced-pure-leaf node AND a gain tie (the histogram analogue of Phase 13's duplicate-point row — the 002-B silent-miscompile backstop)"
    - "Each fixture is reproducible from the committed gen_oracle.py generator with the tie-break encoded as an INDEPENDENT rule (never hand-patched into the blob — Phase 13 CR-01/CR-02 lesson)"
  artifacts:
    - path: "scripts/gen_oracle.py"
      provides: "gen_decision_tree_clf + gen_decision_tree_reg generators (injected-index, sklearn tree_ attrs emitted) wired into main()"
      contains: "gen_decision_tree"
    - path: "tests/fixtures/tree_dt_clf_f64_seed42.npz"
      provides: "DecisionTreeClassifier(gini) reference: X, y, bootstrap_idx, feature_idx, children_left/right, feature, threshold, value"
    - path: "tests/fixtures/tree_dt_reg_f64_seed42.npz"
      provides: "DecisionTreeRegressor(squared_error) reference (same array set; continuous y/value)"
  key_links:
    - from: "scripts/gen_oracle.py"
      to: "tests/fixtures/tree_dt_clf_f64_seed42.npz"
      via: "np.savez emitting the sklearn tree_ attributes for the witness structure+leaf compare"
      pattern: "children_left"
---

<objective>
Lay the Nyquist Wave-0 oracle foundation for TREE-01: the committed sklearn reference fixtures
the Tier-1 witness (Plan 03) value-asserts against. Two generators (`gen_decision_tree_clf`
gini + `gen_decision_tree_reg` squared_error, per D-09) emit injected-fixed-index trees plus the
sklearn `tree_` attributes (split structure + leaf values), in f32 and f64, including at least
one ADVERSARIAL fixture (forced-pure-leaf + gain tie) as the 002-B silent-miscompile backstop.

Purpose: the only catch for a cpu-MLIR SILENT histogram/argmax miscompile is a VALUE-asserting
oracle on an adversarial fixture. The fixtures must exist — and be reproducible from the committed
generator with an INDEPENDENT tie-break rule (Phase 13 CR-01/CR-02) — before any kernel composition
is value-gated in Plan 03.
Output: committed `tree_dt_{clf,reg}_{f32,f64}_seed42.npz` + adversarial variant(s); `gen_oracle.py`
extended with the two generators wired into `main()`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-randomforest-gpu-histogram-split-feasibility-spike-gating/17-CONTEXT.md
@.planning/phases/17-randomforest-gpu-histogram-split-feasibility-spike-gating/17-RESEARCH.md
@.planning/phases/17-randomforest-gpu-histogram-split-feasibility-spike-gating/17-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add gen_decision_tree_clf + gen_decision_tree_reg to gen_oracle.py</name>
  <files>scripts/gen_oracle.py</files>
  <read_first>
    - scripts/gen_oracle.py lines 728-813 (existing `gen_knn` — the `c()` dtype-cast closure, `dtype_tag` naming, `os.makedirs(_FIXTURE_DIR)`, `np.savez(out_path, ...)` shape) and line 814 (`gen_knn_metric`)
    - scripts/gen_oracle.py line 41 (`_FIXTURE_DIR = .../tests/fixtures`) and lines 627-727 (`gen_kmeans`/`gen_dbscan` — sklearn fit + injected-init precedent, `metric=`/param plumbing)
    - 17-PATTERNS.md lines 180-202 ("scripts/gen_oracle.py — gen_decision_tree_clf/reg" — exact extend pattern, the `np.savez(... children_left=, children_right=, feature=, threshold=, value=)` array set, dispatch-via-main() note, tie-break independence)
    - 17-RESEARCH.md lines 301-314 (Tier-1 determinism recipe: inject fixed bootstrap+feature indices; emit sklearn `tree_` attrs; adversarial/degenerate fixture) and lines 305-308 (independent tie-break rule)
    - MEMORY.md note oracle-fixture-regen-needs-venv (regen needs /tmp numpy+sklearn venv; PEP 668; fixtures are committed blobs)
  </read_first>
  <action>
    Add two generators to scripts/gen_oracle.py modeled on the existing `gen_knn` shape (reuse the
    `c()` dtype-cast closure, `dtype_tag` map, `_FIXTURE_DIR`, `np.savez` idiom). Per D-09 implement
    BOTH `gen_decision_tree_clf(seed, dtype, structure="standard")` using
    `DecisionTreeClassifier(criterion="gini", random_state=seed, max_depth=...)` and
    `gen_decision_tree_reg(seed, dtype, structure="standard")` using
    `DecisionTreeRegressor(criterion="squared_error", random_state=seed, max_depth=...)`. Per D-07
    construct a small synthetic dataset (≈60 samples × ≈8 features), a FIXED `bootstrap_idx` array and
    a FIXED `feature_idx` subset (plain integer index arrays, NOT RNG-drawn), fit each tree on
    `X[bootstrap_idx]` / `y[bootstrap_idx]` restricting the considered columns to `feature_idx` (set
    `max_features` / column-subset so the injected subset is what sklearn actually considers). Emit
    named arrays: `X`, `y`, `bootstrap_idx`, `feature_idx`, and the sklearn `tree_` attributes
    `children_left`, `children_right`, `feature`, `threshold`, `value` so Plan 03 can compare split
    STRUCTURE and leaf VALUES. Filenames `tree_dt_clf_{dtype_tag}_seed{seed}.npz` and
    `tree_dt_reg_{dtype_tag}_seed{seed}.npz`. For `structure="adversarial"` engineer the dataset so
    one node must become a PURE leaf (all one class / zero-variance target) AND two candidate splits
    have EQUAL gain (a tie); write `tree_dt_clf_adv_{dtype_tag}_seed{seed}.npz` (and a reg adversarial
    if a zero-variance-region tie is constructible). Encode sklearn's tie-break (lowest feature index,
    then lowest threshold) as an INDEPENDENT documented rule in the generator — do NOT hand-patch the
    blob to match any kernel's pick (Phase 13 CR-01/CR-02). Generate BOTH f32 and f64. Do NOT modify
    any existing generator or fixture.
  </action>
  <verify>
    <automated>python3 -c "import ast; ast.parse(open('scripts/gen_oracle.py').read()); print('parse-ok')" && grep -v '^#' scripts/gen_oracle.py | grep -c 'def gen_decision_tree_clf\|def gen_decision_tree_reg' | awk '$1>=2{print "gens-ok"; exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/gen_oracle.py` parses (`python3 -c "import ast; ast.parse(...)"` exits 0).
    - Both `gen_decision_tree_clf` and `gen_decision_tree_reg` are defined; each fits sklearn on
      `X[bootstrap_idx]` (grep `bootstrap_idx` inside the generator bodies) and emits the sklearn
      `tree_` attributes via `np.savez` (grep `children_left`, `threshold`, `value`).
    - A `structure="adversarial"` path exists producing a forced-pure-leaf + gain-tie fixture, with
      the tie-break documented as an independent rule (comment in the generator).
    - No existing generator/fixture is modified.
  </acceptance_criteria>
  <done>gen_oracle.py defines the gini-classifier + squared-error-regressor injected-index generators (standard + adversarial), emitting sklearn tree_ structure and leaf values, parseable and unwired-but-present.</done>
</task>

<task type="auto">
  <name>Task 2: Wire generators into main() and regenerate the committed .npz fixtures</name>
  <files>scripts/gen_oracle.py, tests/fixtures/</files>
  <read_first>
    - scripts/gen_oracle.py lines 3024-3081 (the `main()` dispatch blocks — every `gen_*` is called UNCONDITIONALLY inside `for dtype in (np.float32, np.float64)` loops; this is where to append, NOT a CLI arg dispatch)
    - scripts/gen_oracle.py lines 3033-3040 (the Phase-13 multi-metric KNN append block — the exact `for dtype ...: print(f"wrote {gen_...(dtype=dtype)}")` pattern to mirror)
    - 17-RESEARCH.md lines 96-100 (regen recipe: `/tmp` venv with numpy+scikit-learn, PEP 668) and line 200 of 17-PATTERNS.md (append call blocks to main())
    - MEMORY.md note oracle-fixture-regen-needs-venv
  </read_first>
  <action>
    Append a Phase-17 block to the END of `main()` mirroring the Phase-13 KNN append style: a
    commented header then `for dtype in (np.float32, np.float64): print(f"wrote {gen_decision_tree_clf(dtype=dtype)}")`,
    the same for `gen_decision_tree_reg`, and a `structure="adversarial"` line for the adversarial
    fixture(s). Then regenerate the committed blobs into `tests/fixtures/` using a one-time `/tmp` venv
    (`python3 -m venv /tmp/oracle-venv && /tmp/oracle-venv/bin/pip install numpy scikit-learn`, then
    `/tmp/oracle-venv/bin/python scripts/gen_oracle.py`) per the PEP-668 workaround. Commit the new
    `tree_dt_*` `.npz` blobs. Confirm no pre-existing fixture changed (only `tree_dt_*` files are
    added). If the full `main()` run is heavy, it is acceptable to run only the appended generators by
    temporarily invoking them — but the COMMITTED generator must call them unconditionally in `main()`
    so the blobs stay reproducible from source.
  </action>
  <verify>
    <automated>python3 -c "import ast; ast.parse(open('scripts/gen_oracle.py').read())" && grep -v '^#' scripts/gen_oracle.py | grep -c 'gen_decision_tree_clf(dtype\|gen_decision_tree_reg(dtype' | awk '$1>=2{exit 0} {exit 1}' && ls tests/fixtures/ | grep -c -E 'tree_dt_(clf|reg)(_adv)?_(f32|f64)_seed42\.npz' | awk '$1>=6{print "fixtures-ok"; exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `main()` calls both `gen_decision_tree_clf(dtype=...)` and `gen_decision_tree_reg(dtype=...)` for
      f32 AND f64 plus the adversarial line (grep the call sites, comment lines excluded).
    - At least 6 committed `.npz` files matching `tree_dt_{clf,reg}(_adv)?_{f32,f64}_seed42.npz` exist
      in `tests/fixtures/` (clf std f32/f64, reg std f32/f64, clf adv f32/f64 minimum).
    - The committed blobs are reproducible from the committed generator (no hand-patching).
    - No non-`tree_dt_*` fixture is added or modified (`git status tests/fixtures/` shows only new
      `tree_dt_*` files).
  </acceptance_criteria>
  <done>The two decision-tree generators are wired into main() and the committed sklearn reference .npz blobs (standard + adversarial, f32+f64) are present and reproducible from source.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| oracle generator (offline) → repo | `gen_oracle.py` runs offline in a `/tmp` venv; output is the trusted committed blob |
| committed fixtures → test harness | `.npz` blobs are trusted build inputs (committed, regen via offline venv); no untrusted runtime/network input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-01 | Repudiation (data integrity) | adversarial oracle fixture | high | mitigate | The forced-pure-leaf + gain-tie adversarial fixture is the VALUE-assert backstop for the cpu-MLIR SILENT histogram/argmax miscompile (002-B), consumed by Plan 03. Tie-break encoded as an INDEPENDENT generator rule (Phase 13 CR-01/CR-02), keeping the gate non-circular. |
| T-17-02 | Tampering | fixture .npz authenticity | low | accept | Fixtures are committed, regenerated offline from sklearn; no runtime/network ingress. Low-value internal test asset. |
| T-17-SC | Tampering | npm/pip/cargo installs | n/a | accept | Zero NEW workspace packages (RESEARCH Package Legitimacy Audit: none additive). The `/tmp` venv numpy+scikit-learn are pre-existing test-only oracle deps. No install task touches the workspace lockfile. |
</threat_model>

<verification>
- `python3 -c "import ast; ast.parse(open('scripts/gen_oracle.py').read())"` exits 0.
- `gen_decision_tree_clf` + `gen_decision_tree_reg` defined and called in `main()` for f32+f64 + adversarial.
- ≥6 `tree_dt_*` `.npz` fixtures committed under `tests/fixtures/`; no other fixture changed.
</verification>

<success_criteria>
The Tier-1 oracle foundation for TREE-01 is committed: gini-classifier + squared-error-regressor
injected-index reference fixtures (standard + adversarial, f32+f64), reproducible from the committed
generator with an independent tie-break — ready for the Plan 03 witness to value-assert against.
</success_criteria>

<output>
Create `.planning/phases/17-randomforest-gpu-histogram-split-feasibility-spike-gating/17-01-SUMMARY.md` when done.
Record the exact fixture names + array keys emitted so Plan 03 binds to the correct `npyz` `by_name` keys.
</output>
