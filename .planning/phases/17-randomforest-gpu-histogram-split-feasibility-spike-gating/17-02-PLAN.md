---
phase: 17-randomforest-gpu-histogram-split-feasibility-spike-gating
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/mlrs-backend/tests/tree_spike/mod.rs
  - crates/mlrs-backend/tests/tree_spike_probes.rs
autonomous: true
requirements: [TREE-01]
user_setup: []

must_haves:
  truths:
    - "GATHER-histogram, seed-from-first split-find, and relabel-partition kernels each STANDALONE-LAUNCH on cpu(f64) and return a non-zero, VALUE-correct read-back vs an in-test host oracle (SC-1, A1, A4)"
    - "Each kernel is authored strictly inside the proven cpu-MLIR op-set — no SharedMemory, no atomics, no F::INFINITY init, no cross-sibling-loop accumulator (the spike's whole reason to exist)"
    - "The SparseTreeNode { colid:i32, threshold:F, left_child:i32, value:i32 } struct is defined with the D-02/D-03/D-04 semantics, and a host per-level build loop composing the three kernels emits a flat node array + shared leaf-value buffer"
    - "f64 probes run on cpu; f64-on-rocm SKIPS-with-log; f32 probes always run (gate = cpu f64 + rocm f32)"
  artifacts:
    - path: "crates/mlrs-backend/tests/tree_spike/mod.rs"
      provides: "the three inline #[cube(launch)] kernels + launch wrappers + SparseTreeNode + host per-level tree build loop (shared support module, NOT a test binary)"
      min_lines: 150
    - path: "crates/mlrs-backend/tests/tree_spike_probes.rs"
      provides: "standalone-launch VALUE-asserting probe for EACH of the three kernels (SC-1 / A1 / A4 evidence) with the 002-A all-zeros guard"
      min_lines: 90
  key_links:
    - from: "crates/mlrs-backend/tests/tree_spike_probes.rs"
      to: "crates/mlrs-backend/tests/tree_spike/mod.rs"
      via: "mod tree_spike; — calls the launch wrappers"
      pattern: "mod tree_spike"
    - from: "crates/mlrs-backend/tests/tree_spike_probes.rs"
      to: "crates/mlrs-backend/src/capability.rs"
      via: "capability::skip_f64_with_log() early-return gate on every f64 probe"
      pattern: "skip_f64_with_log"
---

<objective>
Prove the three tree kernels lower and value-correctly compute under cpu-MLIR (the f64 correctness
gate) — the core feasibility question of TREE-01. Author the GATHER histogram (single-owner, one unit
per (node,feature,bin), modeled on `distance.rs::manhattan_dist`), the seed-from-first split-find
argmax (modeled on `topk.rs::select_k`, NO F::INFINITY), and the relabel-partition (per-sample GATHER,
modeled on `distance.rs::self_drop_gather`, NO scan/compaction) as inline `#[cube(launch)]` kernels in
a shared test-support module, plus the SparseTreeNode contract struct and the host per-level build loop
that composes them. Each kernel gets a standalone-launch VALUE-asserting probe (SC-1 / A1 / A4).

Purpose: SC-1 and abort signals A1 (lowering) + A4 (argmax safety) are answered here. The build loop +
SparseTreeNode are the shared artifacts Plans 03 (witness) and 04 (benchmark) both reuse.
Output: `tree_spike/mod.rs` (3 kernels + wrappers + SparseTreeNode + build loop) and
`tree_spike_probes.rs` (per-kernel standalone VALUE-assert probes, 002-A guard, f64-skip-with-log).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-randomforest-gpu-histogram-split-feasibility-spike-gating/17-CONTEXT.md
@.planning/phases/17-randomforest-gpu-histogram-split-feasibility-spike-gating/17-RESEARCH.md
@.planning/phases/17-randomforest-gpu-histogram-split-feasibility-spike-gating/17-PATTERNS.md
@.claude/skills/spike-findings-mlrs/references/cpu-mlir-kernel-authoring.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the three inline #[cube] kernels + launch wrappers + SparseTreeNode in tree_spike/mod.rs</name>
  <files>crates/mlrs-backend/tests/tree_spike/mod.rs</files>
  <read_first>
    - crates/mlrs-kernels/src/distance.rs lines 12-45 (module cpu-MLIR authoring contract doc) and lines 66-91 (`manhattan_dist` same-iteration feature-loop accumulator — the histogram inner-loop model)
    - crates/mlrs-kernels/src/distance.rs lines 200-242 (`self_drop_gather` per-row `CUBE_POS_X`/`UNIT_POS_X==0` shape + statement-form bound-clamp + the nested same-iteration count idiom — the relabel model and the 002-B-safe recompute pattern)
    - crates/mlrs-kernels/src/topk.rs lines 84-90 (banned-list authoring note) and lines 99-165 (`select_k` seed-from-candidate-0 running-best argmax + the `u32` admit/better flag discipline — the split-find model)
    - .claude/skills/spike-findings-mlrs/references/cpu-mlir-kernel-authoring.md (proven op-set; 002-A loud launch failure; 002-B silent cross-loop miscompile; banned-entirely list)
    - 17-PATTERNS.md lines 29-122 (Pattern Assignments for all three kernels — the exact transform notes) and 17-RESEARCH.md lines 177-249 (Patterns 1-3) and lines 282-299 (SparseTreeNode contract D-02/D-03/D-04)
    - cuml-main/cpp/include/cuml/tree/flatnode.h lines 25-45 (cuML SparseTreeNode divergence reference: leaf=left_child==-1 vs mlrs leaf=colid==-1; right=left+1 shared)
  </read_first>
  <action>
    Create `crates/mlrs-backend/tests/tree_spike/mod.rs` as a shared test-support module (a subdirectory
    `mod.rs` is NOT compiled as its own test binary). Open with a module doc stating the cpu-MLIR
    authoring contract (describe the constraint set by concept — single-owner GATHER, statement-form
    running-best seeded from candidate 0, per-sample `CUBE_POS_X`/`UNIT_POS_X==0` relabel, same-iteration
    accumulators only) WITHOUT inlining banned-construct identifiers as code samples.

    Define three inline `#[cube(launch)]` kernels generic over `F: Float + CubeElement`:
    (1) `tree_gather_histogram` — one unit per output cell `(node,feature,bin)`; runtime `while` over the
    sample range; in the SAME iteration read both `node_id[s]` and `binned[s*n_feat + my_feature]` and
    accumulate (count `+= 1`, and a parallel value-sum buffer `+= y[s]`) only when both the node filter
    and bin filter hold. Model directly on the `manhattan_dist` accumulator; index the cell with the 2D
    guarded `ABSOLUTE_POS_X/Y` (probe this first per Open Question 2) or a linearized per-cell
    `CUBE_POS_X` fallback. (2) `tree_split_find` — running-best gain argmax seeded from candidate 0,
    statement-form `if g > best_gain`, carrying `best_col`+`best_bin`; model on `select_k`; use `u32`
    flags (never mutable `bool`) for any composite tie-rule (lowest feature index then lowest threshold,
    D-09/A4). (3) `tree_relabel_partition` — `row = CUBE_POS_X` per sample, `UNIT_POS_X == 0` guard; read
    own `node_id`, read that node's `split_col`/`split_bin` from frontier arrays, statement-form branch
    writing `left_child[nid]` (go-left) or `left_child[nid] + 1` (go-right, D-02). Pure self-overwrite
    GATHER — never a scan/compaction.

    Add host launch wrappers for each kernel (clone `spike_test.rs`/`self_drop_gather_test.rs` boilerplate:
    `runtime::active_client()`, `Bytes::from_elems` upload, `client.empty(n*size_of::<F>())`, clone the
    output handle BEFORE `ArrayArg::from_raw_parts` consumes it, `client.read_one` + `bytemuck::cast_slice`,
    scalars by value — cubecl 0.10, no `ScalarArg`). Validate launch geometry (`n_samples`/`n_feat`/`n_bins`/
    `depth` for `u32` overflow) before each `unsafe` launch (the `topk::validate_geometry` precedent).

    Define `pub struct SparseTreeNode<F> { colid: i32, threshold: F, left_child: i32, value: i32 }` with
    a doc recording D-02 (right child = left_child+1), D-03 (leaf sentinel colid == -1; signed i32), D-04
    (`value` is an OFFSET into a shared leaf-value buffer, NOT a scalar). Add a host per-level tree build
    loop (`build_tree(...) -> (Vec<SparseTreeNode<F>>, Vec<f64> leaf_buffer)`): host `while` over levels
    bounded by `max_depth` (kernels cannot recurse), per level launch histogram → split-find → relabel,
    read back chosen splits, append internal nodes, mark a node a leaf (colid = -1, value = leaf-buffer
    offset) when pure / max_depth / min_samples reached. Accept host-precomputed quantile bin edges
    (D-10 — NO on-device sort) and `n_bins` as a parameter so Plan 04 can drive 64 vs 128.

    Author strictly inside the proven op-set; do NOT introduce a cross-sibling-loop accumulator (recompute
    per-cell positional values with a self-contained nested accumulate in the same outer iteration —
    the `self_drop_gather` `src = s + bump` idiom).
  </action>
  <verify>
    <automated>cargo build -p mlrs-backend --features cpu --tests 2>&1 | tail -3 && grep -v '^//' crates/mlrs-backend/tests/tree_spike/mod.rs | grep -c -E 'tree_gather_histogram|tree_split_find|tree_relabel_partition' | awk '$1>=3{print "kernels-ok"; exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `crates/mlrs-backend/tests/tree_spike/mod.rs` compiles under `--features cpu --tests`.
    - All three kernels (`tree_gather_histogram`, `tree_split_find`, `tree_relabel_partition`) are defined
      with launch wrappers (grep the three fn names, comment lines excluded).
    - `SparseTreeNode<F>` is defined with fields `colid: i32`, `threshold: F`, `left_child: i32`, `value: i32`
      and the D-02/D-03/D-04 semantics documented.
    - A host `build_tree` per-level loop exists, parameterized by host bin edges + `n_bins`, emitting a
      flat node Vec + a leaf-value buffer.
    - Launch geometry is validated before every `unsafe` launch (grep the validate/`u32`-overflow guard).
  </acceptance_criteria>
  <done>The three cpu-MLIR-safe tree kernels, their launch wrappers, the SparseTreeNode contract struct, and the host per-level build loop compile as a shared test-support module.</done>
</task>

<task type="auto">
  <name>Task 2: Standalone-launch VALUE-asserting probe for each kernel (SC-1 / A1 / A4)</name>
  <files>crates/mlrs-backend/tests/tree_spike_probes.rs</files>
  <read_first>
    - crates/mlrs-backend/tests/self_drop_gather_test.rs lines 25-40 (`host_to_f64`/`from_f64` byte-cast helpers — never call `#[cube]` fns on the host), lines 45-50 (`launch_dims_rows`), lines 110-137 (VALUE asserts + the 002-A all-zeros non-zero guard), lines 186-192 (skip_f64_with_log usage)
    - crates/mlrs-backend/tests/spike_test.rs lines 30-81 (live-launch boilerplate: `active_client`, `Bytes::from_elems`, `client.empty`, clone-before-`from_raw_parts`, `read_one`+`cast_slice`, by-value scalars)
    - crates/mlrs-backend/src/capability.rs lines 147-154 (`skip_f64_with_log`, `active_backend_name`, `log_oracle_dtype`)
    - 17-PATTERNS.md lines 61-68 (002-A guard), lines 243-245 (VALUE-assert backstop), lines 251-253 (launch-geometry validation)
  </read_first>
  <action>
    Create `crates/mlrs-backend/tests/tree_spike_probes.rs` declaring `mod tree_spike;` to reuse the
    Task-1 kernels/wrappers. Write one standalone VALUE-asserting probe per kernel, each a generic
    `fn ...<F>()` instantiated for f32 (always runs) and f64 (cpu gate), each opening with the
    `capability::skip_f64_with_log()` early-return + a `println!` skip line and a backend/dtype log line:
    (1) histogram probe — feed a tiny hand-built `node_id`/`binned`/`y` input whose exact per-cell counts
    and value-sums are computable by an in-test host oracle; launch `tree_gather_histogram`; assert the
    read-back EQUALS the host counts/sums AND assert a non-trivial non-zero value is present (the 002-A
    all-zeros guard — a kernel that never launched reads back zeros). (2) split-find probe — feed a host
    gain array with a KNOWN argmax INCLUDING a tie case; launch `tree_split_find`; assert it returns the
    correct `(best_col, best_bin, best_gain)` and that the tie resolves to the lowest feature index then
    lowest threshold (A4 VALUE assertion, not non-panic). (3) relabel probe — feed a `node_id`/`binned`
    input with known left/right routing; launch `tree_relabel_partition`; assert each sample's overwritten
    `node_id` equals `left_child[nid]` or `left_child[nid]+1` exactly per the split (D-02). Use
    `host_to_f64`/`from_f64` for all host↔device casts. Run targeted; do NOT add fenced kernel code here —
    only host-side launch calls + asserts.
  </action>
  <verify>
    <automated>cargo test -p mlrs-backend --features cpu --test tree_spike_probes -- --nocapture 2>&1 | tail -15 | grep -E 'test result: ok|0 failed'</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p mlrs-backend --features cpu --test tree_spike_probes` passes (all three kernel probes GREEN on cpu f64).
    - Each probe asserts returned VALUES against an in-test host oracle (grep `assert` on read-back) — never a bare non-panic.
    - The histogram probe includes a non-zero read-back guard (002-A); the split-find probe asserts the tie resolution; the relabel probe asserts exact left/right child labels.
    - Every f64 probe early-returns via `capability::skip_f64_with_log()` and emits a backend/dtype log line; f32 probes always run.
  </acceptance_criteria>
  <done>All three tree kernels standalone-launch and VALUE-match an in-test host oracle on cpu(f64) (f64-on-rocm skips-with-log) — SC-1 satisfied, A1 (lowering) and A4 (argmax) answered with VALUE evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host test harness → device kernel launch | launch geometry (rows/cols/bins/depth) crosses into `unsafe { ArrayArg::from_raw_parts }`; must be validated host-side first |
| in-test host oracle → VALUE assert | developer-controlled host inputs; no untrusted/network input (local compute spike) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-03 | Tampering / DoS | tree kernel launch geometry | medium | mitigate | Validate `n_samples`/`n_feat`/`n_bins`/`depth` for `u32` overflow and pass only validated element counts to `from_raw_parts` BEFORE launch (the `topk::validate_geometry` precedent); kernels bounds-check every index (`if cell < n_cells`, `if s < n_samples`). |
| T-17-04 | Repudiation (data integrity) | silent kernel miscompile (002-B) | high | mitigate | Every probe VALUE-asserts read-back against an in-test host oracle + a 002-A all-zeros non-zero guard; a green non-panic is insufficient. |
| T-17-SC | Tampering | npm/pip/cargo installs | n/a | accept | Zero new packages — only pre-existing workspace deps (`cubecl` 0.10, `bytemuck`). No install task. |
</threat_model>

<verification>
- `cargo build -p mlrs-backend --features cpu --tests` exits 0 (kernels + build loop + SparseTreeNode compile).
- `cargo test -p mlrs-backend --features cpu --test tree_spike_probes -- --nocapture` passes (3 kernels VALUE-correct on cpu f64).
- f64 probes skip-with-log on rocm; f32 probes run on rocm (opportunistic GPU host).
</verification>

<success_criteria>
SC-1 is met: the GATHER-histogram, seed-from-first split-find, and relabel-partition kernels each
standalone-launch under cpu-MLIR with no SharedMemory/atomics/F::INFINITY, returning VALUE-correct
read-back vs a host oracle. A1 and A4 abort signals are answered with VALUE evidence. The SparseTreeNode
contract struct and the composing host build loop exist for Plans 03/04 to reuse.
</success_criteria>

<output>
Create `.planning/phases/17-randomforest-gpu-histogram-split-feasibility-spike-gating/17-02-SUMMARY.md` when done.
Record which histogram cube-indexing shape (2D guarded vs linearized per-cell) lowered cleanly (Open Question 2)
and any 002-A/002-B near-misses, plus the exact wrapper signatures Plans 03/04 call.
</output>
