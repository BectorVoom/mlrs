---
phase: 17-randomforest-gpu-histogram-split-feasibility-spike-gating
plan: 05
type: execute
wave: 3
depends_on: ["17-01", "17-02", "17-03", "17-04"]
files_modified:
  - .planning/phases/17-randomforest-gpu-histogram-split-feasibility-spike-gating/VERDICT.md
  - .planning/spikes/MANIFEST.md
  - .planning/spikes/003-gather-histogram-lower/
  - .planning/spikes/004-seed-from-first-split-find/
  - .planning/spikes/005-relabel-partition/
  - .planning/spikes/006-tier1-decisiontree-witness/
autonomous: false

must_haves:
  truths:
    - "VERDICT.md explicitly evaluates ALL FIVE abort signals A1–A5 with the concrete evidence each produced (kernel launch read-back, host-pre-pass binning, 64-vs-128 benchmark, split-find VALUE assert, sklearn witness) and renders an explicit GO / ADJUST / ABORT decision (SC-4, SC-5)"
    - "The two-tier stochastic-gate convention is documented as the milestone-wide standard: Tier-1 deterministic injected-fixed-index core (D-07) + Tier-2 ensemble/predictive band (D-08)"
    - "The SparseTreeNode contract is recorded as FINALIZED with the deliberate cuML divergence noted (mlrs leaf = colid==-1, not cuML's left_child==-1) so Phase 20 FIL binds to the mlrs convention (D-02/D-03/D-04)"
    - "If ADJUST is the verdict, the D-06 ladder rung chosen is named with its data backing; the proven probe+witness source is copied verbatim into .planning/spikes/NNN-*/ and MANIFEST.md is appended (D-01, mirrors Phase 13)"
  artifacts:
    - path: ".planning/phases/17-randomforest-gpu-histogram-split-feasibility-spike-gating/VERDICT.md"
      provides: "the GATING artifact: A1–A5 evaluation + GO/ADJUST/ABORT + two-tier convention + finalized SparseTreeNode contract + cuML divergence note"
      contains: "GO"
    - path: ".planning/spikes/MANIFEST.md"
      provides: "appended tree-spike rows (idea, requirements, spike table with verdicts)"
      contains: "003"
  key_links:
    - from: ".planning/spikes/003-gather-histogram-lower/"
      to: "crates/mlrs-backend/tests/tree_spike/mod.rs"
      via: "verbatim copy of the proven kernel+harness source (durable evidence artifact)"
      pattern: "tree_gather_histogram"
---

<objective>
Deliver the GATING artifact for TREE-01 and the entire tree chain (Phases 18→21): `VERDICT.md` with all
five abort signals A1–A5 explicitly evaluated against the evidence Plans 02/03/04 produced, an explicit
GO / ADJUST / ABORT decision, the two-tier stochastic-gate convention documented as the milestone-wide
standard (D-07/D-08), and the SparseTreeNode contract recorded as finalized (D-02/D-03/D-04, with the
cuML divergence noted). Wrap up per D-01: copy the proven probe+witness source verbatim into
`.planning/spikes/NNN-*/` and append `.planning/spikes/MANIFEST.md` (mirrors Phase 13).

Purpose: this is the spike's reason for existing — a defensible feasibility decision that gates whether
Phases 18–21 proceed, with the convention + contract downstream phases bind to. The decision is
human-gated because it commits four downstream phases.
Output: `VERDICT.md`, appended `MANIFEST.md`, and the durable `.planning/spikes/003..006-*/` source artifacts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-randomforest-gpu-histogram-split-feasibility-spike-gating/17-CONTEXT.md
@.planning/phases/17-randomforest-gpu-histogram-split-feasibility-spike-gating/17-RESEARCH.md
@.planning/phases/17-randomforest-gpu-histogram-split-feasibility-spike-gating/17-02-SUMMARY.md
@.planning/phases/17-randomforest-gpu-histogram-split-feasibility-spike-gating/17-03-SUMMARY.md
@.planning/phases/17-randomforest-gpu-histogram-split-feasibility-spike-gating/17-04-SUMMARY.md
@.planning/spikes/MANIFEST.md
@.planning/spikes/CONVENTIONS.md
@.planning/spikes/WRAP-UP-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author VERDICT.md — A1–A5 evaluation + GO/ADJUST/ABORT + two-tier convention + finalized contract</name>
  <files>.planning/phases/17-randomforest-gpu-histogram-split-feasibility-spike-gating/VERDICT.md</files>
  <read_first>
    - 17-02-SUMMARY.md (A1/A4 kernel evidence + indexing-shape findings), 17-03-SUMMARY.md (A5 witness evidence: clf+reg+adversarial), 17-04-SUMMARY.md (A3 benchmark numbers + scaling shape + 64-vs-128 delta + frontier-memory observation)
    - 17-RESEARCH.md lines 316-326 (the A1–A5 evaluation table + verdict logic GO/ADJUST/ABORT) and lines 34-39 (two-tier convention D-07/D-08) and lines 282-299 (SparseTreeNode contract + cuML divergence note for Phase 20 FIL)
    - .planning/milestones/v3.0-phases/13-knn-graph-primitive-feasibility-keystone/13-VERIFICATION.md (the Phase-13 verdict structure to mirror)
    - 17-CONTEXT.md lines 28-42 (D-05 A3 judgment basis, D-06 ADJUST ladder order, D-07/D-08 two-tier convention)
  </read_first>
  <action>
    Author `VERDICT.md` mirroring the Phase-13 verification structure. Include: (1) an A1–A5 table —
    for EACH signal state the question, the concrete evidence produced (A1: histogram kernel non-zero
    correct read-back; A2: host quantile pre-pass consumed as binned u32, NO device sort exists; A3: the
    64-vs-128 wall-clock numbers + scaling shape from Plan 04; A4: split-find VALUE-assert incl. tie; A5:
    clf+reg+adversarial witness from Plan 03), and the disposition pass/borderline/fail. (2) The explicit
    GO / ADJUST / ABORT decision using the D-05/D-06 logic: GO if A1/A2/A4/A5 pass and A3 is
    tractable+sub-quadratic; ADJUST (name the D-06 rung — fewer bins 128→64 backed by the benchmark →
    frontier-only histogramming → shallower default max_depth → defer as last resort) if A3 is borderline;
    ABORT only if A1/A5 unresolvable or A3 pathological at every rung. (3) The two-tier stochastic-gate
    convention as the milestone-wide standard: Tier-1 deterministic injected-fixed-index core (D-07,
    exact structure + ≤1e-5 leaf values, abort signal A5) and Tier-2 ensemble/predictive band (D-08,
    accuracy / R² within ~0.02–0.05 — documented now, governs Phase 19, not run this phase). (4) The
    FINALIZED SparseTreeNode contract (D-02 right=left+1, D-03 leaf sentinel colid==-1 signed i32, D-04
    value=offset into shared leaf buffer) with the explicit cuML divergence note (cuML marks leaf via
    left_child==-1; mlrs uses colid==-1 so FIL stops on colid<0) so Phase 20 FIL binds to the mlrs
    convention. (5) Any caveats for Phase 18 (threshold decision-equivalence vs binned cut; frontier-memory
    observation feeding the D-06 lever).
  </action>
  <verify>
    <automated>VD=.planning/phases/17-randomforest-gpu-histogram-split-feasibility-spike-gating/VERDICT.md; test -f "$VD" && grep -c -E 'A[1-5]' "$VD" | awk '$1>=5{exit 0}{exit 1}' && grep -E -q 'GO|ADJUST|ABORT' "$VD" && grep -q 'colid' "$VD" && grep -q -i 'tier' "$VD" && echo verdict-ok</automated>
  </verify>
  <acceptance_criteria>
    - `VERDICT.md` exists and explicitly evaluates all five abort signals A1–A5 (grep `A[1-5]` >= 5 matches), each with the concrete evidence from Plans 02/03/04.
    - An explicit GO / ADJUST / ABORT decision is rendered with the D-05/D-06 rationale (and the named D-06 rung if ADJUST).
    - The two-tier stochastic-gate convention (Tier-1 D-07 + Tier-2 D-08) is documented as the milestone-wide standard.
    - The finalized SparseTreeNode contract (D-02/D-03/D-04) is recorded with the cuML divergence note (mlrs leaf = colid==-1).
    - Phase-18 caveats (threshold decision-equivalence; frontier-memory observation) are recorded.
  </acceptance_criteria>
  <done>VERDICT.md delivers the A1–A5 evaluation, the explicit GO/ADJUST/ABORT decision, the two-tier convention, and the finalized SparseTreeNode contract — the gating artifact for the tree chain. SC-4 (A1–A5) and SC-5 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Spike wrap-up — copy proven source verbatim into .planning/spikes/NNN-*/ and append MANIFEST.md</name>
  <files>.planning/spikes/MANIFEST.md, .planning/spikes/003-gather-histogram-lower/, .planning/spikes/004-seed-from-first-split-find/, .planning/spikes/005-relabel-partition/, .planning/spikes/006-tier1-decisiontree-witness/</files>
  <read_first>
    - .planning/spikes/MANIFEST.md (the existing 001/002 entries: Idea, Requirements, Spikes table with verdicts — the append format)
    - .planning/spikes/CONVENTIONS.md (the raw-spike layout convention — durable verbatim source artifact)
    - .planning/spikes/WRAP-UP-SUMMARY.md (the wrap-up convention)
    - .planning/spikes/001-direct-feature-loop-distance-kernels/ (an example durable spike dir to mirror)
    - 17-CONTEXT.md line 21 (D-01: proven findings copied verbatim; spike code is evidence not promoted code)
  </read_first>
  <action>
    Create the four durable spike directories `.planning/spikes/003-gather-histogram-lower/`,
    `004-seed-from-first-split-find/`, `005-relabel-partition/`, and `006-tier1-decisiontree-witness/`,
    each holding a VERBATIM copy of the proven probe/witness source from `crates/mlrs-backend/tests/`
    (the relevant kernel from `tree_spike/mod.rs` + its probe from `tree_spike_probes.rs` for 003/004/005;
    `tree_witness.rs` + the build loop for 006) plus a short README noting the spike's idea, verdict, and
    the cpu-MLIR findings it produced (e.g. which histogram indexing shape lowered; any 002-A/002-B
    near-miss). Mirror the existing `001-*/`/`002-*/` layout and the CONVENTIONS.md format. Append a
    Phase-17 block to `.planning/spikes/MANIFEST.md`: an Idea paragraph, the non-negotiable design
    decisions (the SparseTreeNode contract D-02/D-03/D-04, the two-tier convention D-07/D-08, the cpu-MLIR
    op-set), and a Spikes table appending rows 003–006 with their VALIDATED/verdict status and tags.
    Do NOT delete the live `crates/mlrs-backend/tests/tree_*` files (they remain the runnable gate);
    the `.planning/spikes/` copies are the durable evidence artifact (D-01). If the verdict is GO,
    note that Phase 18 re-authors the production prims from these findings + the spike-findings skill.
  </action>
  <verify>
    <automated>test -d .planning/spikes/003-gather-histogram-lower && test -d .planning/spikes/004-seed-from-first-split-find && test -d .planning/spikes/005-relabel-partition && test -d .planning/spikes/006-tier1-decisiontree-witness && grep -c -E '00[3-6]' .planning/spikes/MANIFEST.md | awk '$1>=4{print "manifest-ok"; exit 0}{exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - All four `.planning/spikes/00[3-6]-*/` directories exist, each with a verbatim source copy + a short README (idea, verdict, cpu-MLIR findings).
    - `.planning/spikes/MANIFEST.md` has an appended Phase-17 block (Idea + design decisions + Spikes table rows 003–006 with verdicts/tags).
    - The live `crates/mlrs-backend/tests/tree_*` files are NOT deleted (they remain the runnable gate).
  </acceptance_criteria>
  <done>The proven tree-spike source is preserved verbatim in .planning/spikes/003–006-*/ and MANIFEST.md is appended — the durable feasibility evidence (D-01, mirrors Phase 13) is in place.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Lock the GO/ADJUST/ABORT verdict — human gate before the tree chain proceeds</name>
  <action>Checkpoint: pause for human confirmation of the verdict before it gates Phases 18–21. See <how-to-verify> for the exact evidence the human reviews and the commands to re-run.</action>
  <what-built>The complete TREE-01 feasibility spike: three cpu-MLIR-safe tree kernels (standalone-launch VALUE-asserted), a Tier-1 sklearn witness (clf gini + reg squared_error + adversarial, exact structure + ≤1e-5 leaf values), the 64-vs-128 per-tree cost benchmark, the finalized SparseTreeNode contract, the two-tier stochastic-gate convention, and VERDICT.md with A1–A5 evaluated and an explicit GO/ADJUST/ABORT decision.</what-built>
  <how-to-verify>
    1. Read `VERDICT.md` — confirm A1–A5 are each evaluated with concrete evidence and the GO/ADJUST/ABORT decision follows the D-05/D-06 logic. This decision GATES Phases 18–21.
    2. Run `cargo test -p mlrs-backend --features cpu --test tree_spike_probes --test tree_witness -- --nocapture` and confirm all probes + the clf/reg/adversarial witness are GREEN on cpu f64.
    3. Run `cargo test -p mlrs-backend --features cpu --test tree_bench -- --nocapture` and confirm the 64-bin + 128-bin wall-clock numbers and the scaling sweep print, and that they support the A3 disposition in VERDICT.md.
    4. Confirm the SparseTreeNode contract + cuML divergence note + two-tier convention are recorded for downstream phases, and the `.planning/spikes/003–006-*/` evidence dirs exist.
  </how-to-verify>
  <resume-signal>Type "approved" to lock the verdict (GO → Phase 18 proceeds; ADJUST → apply the named D-06 rung; ABORT → re-scope v4.0 tree chain), or describe the gap to address.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spike evidence → downstream phases | VERDICT.md + spike source are trusted planning artifacts that gate Phases 18–21; human-confirmed before lock |
| live test source → durable spike copy | verbatim copy into `.planning/spikes/`; no transformation, no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-09 | Repudiation (decision integrity) | GO/ADJUST/ABORT verdict | high | mitigate | The verdict cites the concrete A1–A5 evidence (kernel read-back, host binning, benchmark numbers, VALUE witness) and is human-gated by a blocking checkpoint before it commits four downstream phases. |
| T-17-10 | Tampering | durable spike evidence | low | accept | Verbatim copy of committed test source into `.planning/spikes/`; no runtime/network ingress; live tests remain the authoritative gate. |
| T-17-SC | Tampering | npm/pip/cargo installs | n/a | accept | Documentation + file-copy only; zero packages. No install task. |
</threat_model>

<verification>
- `VERDICT.md` exists with A1–A5 each evaluated, an explicit GO/ADJUST/ABORT decision, the two-tier convention, and the finalized SparseTreeNode contract + cuML divergence note.
- `.planning/spikes/003–006-*/` durable evidence dirs exist; `MANIFEST.md` appended with rows 003–006.
- Blocking human-verify checkpoint confirms the verdict before it gates Phases 18–21.
</verification>

<success_criteria>
SC-5 (and the A1–A5 component of SC-4) is met: an explicit, evidence-backed GO/ADJUST/ABORT verdict is
delivered, the two-tier stochastic-gate convention is documented as the milestone-wide standard, the
SparseTreeNode contract is finalized, and the durable spike evidence is preserved — the tree chain is
gated on a defensible, human-confirmed decision.
</success_criteria>

<output>
Create `.planning/phases/17-randomforest-gpu-histogram-split-feasibility-spike-gating/17-05-SUMMARY.md` when done.
Record the locked verdict (GO/ADJUST/ABORT), any D-06 rung applied, and the explicit hand-off note for
Phase 18 (re-author production prims from findings) or the re-scope path if ABORT.
</output>
