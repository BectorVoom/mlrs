---
phase: 17-randomforest-gpu-histogram-split-feasibility-spike-gating
plan: 03
type: execute
wave: 2
depends_on: ["17-01", "17-02"]
files_modified:
  - crates/mlrs-backend/tests/tree_witness.rs
autonomous: true
requirements: [TREE-01]

must_haves:
  truths:
    - "A single tree built by the host build loop on INJECTED FIXED bootstrap/feature indices VALUE-matches sklearn.tree.DecisionTreeClassifier(gini) — exact split structure + ≤1e-5 (f64) dereferenced leaf values (SC-2, A5)"
    - "The SAME witness path matches DecisionTreeRegressor(squared_error) — both leaf-value shapes (class-probability leaf AND regression-mean leaf) carried through the ONE value offset field, proving the multiclass-uniform claim (D-09, SC-3)"
    - "The SparseTreeNode contract is validated: right child == left_child+1 (D-02), every leaf has colid==-1 and every internal node colid>=0 (D-03), value dereferences into the shared leaf-value buffer NOT a scalar (D-04)"
    - "The adversarial fixture (forced-pure-leaf + gain tie) passes — the 002-B silent-miscompile backstop catches a histogram/argmax boundary error"
  artifacts:
    - path: "crates/mlrs-backend/tests/tree_witness.rs"
      provides: "Tier-1 VALUE-assert witness vs sklearn (clf gini + reg squared_error + adversarial), SparseTreeNode contract assertions, decision-equivalence threshold gate"
      min_lines: 140
  key_links:
    - from: "crates/mlrs-backend/tests/tree_witness.rs"
      to: "tests/fixtures/tree_dt_clf_f64_seed42.npz"
      via: "mlrs_core::oracle::load_npz — by_name X/y/bootstrap_idx/feature_idx/children_left/threshold/value"
      pattern: "load_npz"
    - from: "crates/mlrs-backend/tests/tree_witness.rs"
      to: "crates/mlrs-backend/tests/tree_spike/mod.rs"
      via: "mod tree_spike; — calls build_tree composing the three kernels"
      pattern: "build_tree"
---

<objective>
Deliver the Tier-1 correctness witness — the real correctness gate for TREE-01 and abort signal A5.
Compose the Plan-02 kernels via the host `build_tree` loop on the Plan-01 injected-fixed-index fixtures
and prove a single tree reproduces sklearn EXACTLY: exact split structure + ≤1e-5 (f64) leaf values vs
BOTH DecisionTreeClassifier(gini) AND DecisionTreeRegressor(squared_error) (D-09), validate the full
SparseTreeNode contract (D-02/D-03/D-04), and pass the adversarial fixture (the 002-B backstop).

Purpose: this is where "the kernels launch" becomes "the histogram/gain/partition MATH is correct."
If it fails, the math is wrong (not the RNG — indices are injected, removing stochasticity, D-07).
Output: `tree_witness.rs` — the VALUE-asserting oracle witness, RED until run, GREEN proving SC-2/SC-3/A5.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-randomforest-gpu-histogram-split-feasibility-spike-gating/17-CONTEXT.md
@.planning/phases/17-randomforest-gpu-histogram-split-feasibility-spike-gating/17-RESEARCH.md
@.planning/phases/17-randomforest-gpu-histogram-split-feasibility-spike-gating/17-PATTERNS.md
@.planning/phases/17-randomforest-gpu-histogram-split-feasibility-spike-gating/17-01-SUMMARY.md
@.planning/phases/17-randomforest-gpu-histogram-split-feasibility-spike-gating/17-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tier-1 clf + reg VALUE witness with SparseTreeNode contract assertions</name>
  <files>crates/mlrs-backend/tests/tree_witness.rs</files>
  <read_first>
    - 17-01-SUMMARY.md (exact fixture names + emitted array keys) and 17-02-SUMMARY.md (build_tree signature + histogram indexing shape that lowered)
    - crates/mlrs-core/src/oracle.rs lines 60-91 (`load_npz`, `expect_f64`/`expect_f32`, `shape` — by_name loader, no Python at test time)
    - crates/mlrs-core/tests/compare_test.rs line 8 (`F64_TOL` abs 1e-5/rel 1e-5) and the `assert_slice_close` usage
    - crates/mlrs-backend/tests/self_drop_gather_test.rs lines 110-174 (VALUE-assert + adversarial-row discipline) and lines 186-192 (skip_f64_with_log)
    - 17-RESEARCH.md lines 278-299 (SparseTreeNode validation method D-02/D-03/D-04), lines 301-314 (determinism recipe + decision-equivalence threshold gate), lines 393-398 (Open Question 1 — binned cut vs sklearn midpoint)
    - 17-PATTERNS.md lines 126-156 (witness analog mapping: structural EXACT assert + leaf-value ≤1e-5 + contract validation + skip-with-log)
  </read_first>
  <action>
    Create `crates/mlrs-backend/tests/tree_witness.rs` declaring `mod tree_spike;` to reuse `build_tree`,
    the kernels, and `SparseTreeNode`. Implement a generic `check_tree_witness<F>(fixture_name, kind)`
    that: loads the `.npz` via `mlrs_core::oracle::load_npz` (by_name `X`, `y`, `bootstrap_idx`,
    `feature_idx`, `children_left`, `children_right`, `feature`, `threshold`, `value`); computes host
    quantile bin edges (D-10, 128 bins default — NO on-device sort); calls `build_tree` with the injected
    `bootstrap_idx`/`feature_idx`; then VALUE-asserts:
    (a) STRUCTURE EXACT (`assert_eq!`) — per internal node the chosen split feature (`colid`) maps to
    sklearn's `feature`, `left_child` matches sklearn's `children_left` layout, the chosen bin is correct;
    every leaf has `colid == -1` and every internal node `colid >= 0` (D-03); right child == left_child+1
    (D-02) — assert the node array lays a parent's children adjacent.
    (b) LEAF VALUES ≤1e-5 — dereference each leaf's `value` as an OFFSET into the shared leaf-value buffer
    (D-04, NOT a scalar) and `assert_slice_close(..., &F64_TOL)` against sklearn's `value` (classifier:
    class-probability entry; regressor: regression-mean entry).
    (c) THRESHOLD = DECISION-EQUIVALENCE — because host quantile binning rounds sklearn's exact midpoint
    (Open Question 1), assert the chosen split classifies the same samples left/right rather than
    byte-equality on the raw float; document this in the test.
    Add the f64 (cpu gate) clf witness on the gini fixture AND the f64 reg witness on the squared_error
    fixture (D-09) — exercising BOTH leaf-value paths through the ONE `value` field (the multiclass-uniform
    proof). Add an f32 companion that runs on cpu and rocm. Every f64 test opens with
    `capability::skip_f64_with_log()` + a backend/dtype log line.
  </action>
  <verify>
    <automated>cargo test -p mlrs-backend --features cpu --test tree_witness -- --nocapture 2>&1 | tail -20 | grep -E 'test result: ok|0 failed'</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p mlrs-backend --features cpu --test tree_witness` passes for BOTH clf(gini) and reg(squared_error) on cpu f64.
    - Structure is asserted EXACT (`assert_eq!` on colid/feature, left_child, chosen bin, leaf sentinel colid==-1, right==left+1).
    - Leaf values are asserted ≤1e-5 via `assert_slice_close`/`F64_TOL` after dereferencing `value` as a leaf-buffer offset (grep the dereference + tol compare).
    - Thresholds gated by decision-equivalence (same-sample routing), documented in the test (Open Question 1).
    - f64 tests skip-with-log on rocm; f32 companion runs.
  </acceptance_criteria>
  <done>A single injected-fixed-index tree VALUE-matches sklearn DecisionTreeClassifier(gini) AND DecisionTreeRegressor(squared_error) — exact structure + ≤1e-5 leaf values — and the SparseTreeNode contract (D-02/D-03/D-04) is validated. SC-2, SC-3, A5 answered.</done>
</task>

<task type="auto">
  <name>Task 2: Adversarial fixture witness (forced-pure-leaf + gain tie — 002-B backstop)</name>
  <files>crates/mlrs-backend/tests/tree_witness.rs</files>
  <read_first>
    - 17-01-SUMMARY.md (the adversarial fixture name + the independent tie-break rule encoded in the generator)
    - 17-RESEARCH.md lines 314 (adversarial/degenerate fixture as the histogram analogue of the Phase-13 duplicate-point row), lines 355-359 (Pitfall 3 silent miscompile + Pitfall 4 circular oracle)
    - crates/mlrs-backend/tests/self_drop_gather_test.rs lines 161-174 (the duplicate-point boundary VALUE assert model)
    - .claude/skills/spike-findings-mlrs/references/cpu-mlir-kernel-authoring.md (002-B silent cross-loop miscompile — why VALUE-assert on a boundary fixture is the only catch)
  </read_first>
  <action>
    Add an adversarial witness test to `tree_witness.rs` running `check_tree_witness` (or a focused
    variant) on the Plan-01 adversarial fixture. Assert explicitly: (1) the forced-pure-leaf node becomes
    a leaf with `colid == -1` and its dereferenced `value` matches sklearn's pure-leaf output ≤1e-5; (2)
    the gain-TIE node resolves to the SAME split sklearn picked — i.e. the kernel's argmax tie-break
    (lowest feature index, then lowest threshold) matches the INDEPENDENT generator-encoded rule (Phase-13
    CR-01/CR-02 — the gate must not be circular; the fixture is reproducible from the committed generator,
    not hand-patched to the kernel's pick). This is the explicit 002-B silent-miscompile backstop: a
    boundary miscompile that the happy-path clf/reg witness would miss must fail HERE. f64 cpu gate +
    skip-with-log on rocm.
  </action>
  <verify>
    <automated>cargo test -p mlrs-backend --features cpu --test tree_witness adversarial -- --nocapture 2>&1 | tail -15 | grep -E 'test result: ok|0 failed'</automated>
  </verify>
  <acceptance_criteria>
    - The adversarial witness test passes on cpu f64.
    - The forced-pure-leaf node is asserted as a leaf (`colid == -1`) with leaf value ≤1e-5 vs sklearn.
    - The gain-tie node is asserted to resolve to sklearn's pick via the independent tie-break rule (not the kernel's own pick conformed by hand).
    - The test documents that it is the 002-B silent-miscompile backstop.
  </acceptance_criteria>
  <done>The adversarial (forced-pure-leaf + gain-tie) fixture VALUE-matches sklearn through the kernel composition — the cpu-MLIR silent-miscompile boundary backstop is GREEN and non-circular.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed sklearn fixtures → witness | `.npz` blobs trusted committed inputs; loaded via `npyz` by_name, no Python at test time |
| host build loop → device kernels | validated launch geometry only (inherited from Plan 02 wrappers); no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-05 | Repudiation (data integrity) | Tier-1 witness vs sklearn | high | mitigate | VALUE-assert (exact structure + ≤1e-5 leaf values) on clf+reg+adversarial fixtures; the adversarial forced-pure-leaf+tie fixture catches the 002-B silent miscompile a happy-path check ships. |
| T-17-06 | Repudiation (circular oracle) | tie-break gate | high | mitigate | Tie-break is an INDEPENDENT generator-encoded rule (Plan 01); the witness asserts the boundary case against the reproducible blob, never conforming the fixture to the kernel's pick (Phase-13 CR-01/CR-02). |
| T-17-SC | Tampering | npm/pip/cargo installs | n/a | accept | Zero new packages — pre-existing `npyz`/`bytemuck`/`mlrs-core` only. No install task. |
</threat_model>

<verification>
- `cargo test -p mlrs-backend --features cpu --test tree_witness` passes for clf(gini), reg(squared_error), and adversarial on cpu f64.
- Structure EXACT, leaf values ≤1e-5, thresholds decision-equivalent; SparseTreeNode contract (D-02/D-03/D-04) validated.
- f64 skips-with-log on rocm; f32 companion runs.
</verification>

<success_criteria>
SC-2/SC-3 and abort signal A5 are answered: a single injected-fixed-index tree VALUE-matches both
sklearn DecisionTreeClassifier(gini) and DecisionTreeRegressor(squared_error) with exact split structure
and ≤1e-5 leaf values, the SparseTreeNode contract is validated, and the adversarial fixture proves the
composition is free of a silent cpu-MLIR miscompile.
</success_criteria>

<output>
Create `.planning/phases/17-randomforest-gpu-histogram-split-feasibility-spike-gating/17-03-SUMMARY.md` when done.
Record the A5 verdict evidence (clf + reg + adversarial all GREEN, or the exact divergence) and any
threshold decision-equivalence caveat for VERDICT.md (Plan 05).
</output>
