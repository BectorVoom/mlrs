---
phase: 17-randomforest-gpu-histogram-split-feasibility-spike-gating
plan: 04
type: execute
wave: 2
depends_on: ["17-02"]
files_modified:
  - crates/mlrs-backend/tests/tree_bench.rs
autonomous: true
requirements: [TREE-01]

must_haves:
  truths:
    - "A per-tree build cost benchmark on the representative fixture (≈1000 samples × 20 features × depth 8) is recorded at BOTH 64 AND 128 bins (D-05, D-10), printing wall-clock per build (SC-4, A3)"
    - "The benchmark reports the scaling SHAPE as samples and bins grow (a small sweep), so A3 can be judged sub-quadratic in samples/bins — not against an absolute wall-clock ceiling (D-05)"
    - "The 64-vs-128 bin delta is reported so the D-06 'fewer bins' ADJUST lever is data-backed"
    - "Scratch-memory observation noted: whether the GATHER layout keeps scratch bounded by the active frontier (feeds the D-06 'frontier-only' lever)"
  artifacts:
    - path: "crates/mlrs-backend/tests/tree_bench.rs"
      provides: "wall-clock per-tree cost probe at 64 vs 128 bins + a samples/bins scaling sweep (std::time::Instant, --nocapture; NOT Criterion)"
      min_lines: 80
  key_links:
    - from: "crates/mlrs-backend/tests/tree_bench.rs"
      to: "crates/mlrs-backend/tests/tree_spike/mod.rs"
      via: "mod tree_spike; — drives build_tree at n_bins ∈ {64,128}"
      pattern: "build_tree"
---

<objective>
Measure the one genuine unknown of TREE-01: abort signal A3 — is the O(samples × bins) single-owner
GATHER tractable on a realistic load? Benchmark the full per-tree build (host loop driving the three
Plan-02 kernels) on ≈1000×20×depth-8 at 64 AND 128 bins (D-05/D-10), report wall-clock + the scaling
shape as samples/bins grow, and report the 64-vs-128 delta so the D-06 "fewer bins" lever is data-backed.

Purpose: A3 cannot be reasoned away — only measured (RESEARCH: the single MEDIUM-confidence unknown).
The verdict (Plan 05) cites THESE numbers, not "it compiled."
Output: `tree_bench.rs` — a simple wall-clock probe (NOT Criterion), run targeted with `--nocapture`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-randomforest-gpu-histogram-split-feasibility-spike-gating/17-CONTEXT.md
@.planning/phases/17-randomforest-gpu-histogram-split-feasibility-spike-gating/17-RESEARCH.md
@.planning/phases/17-randomforest-gpu-histogram-split-feasibility-spike-gating/17-PATTERNS.md
@.planning/phases/17-randomforest-gpu-histogram-split-feasibility-spike-gating/17-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Per-tree cost benchmark at 64 vs 128 bins + scaling sweep</name>
  <files>crates/mlrs-backend/tests/tree_bench.rs</files>
  <read_first>
    - 17-02-SUMMARY.md (the `build_tree` signature + `n_bins` parameter + the histogram indexing shape that lowered)
    - crates/mlrs-backend/tests/spike_test.rs lines 30-81 (live-launch boilerplate to clone for the build driver)
    - crates/mlrs-backend/src/capability.rs lines 147-154 (skip_f64_with_log + backend/dtype log)
    - 17-RESEARCH.md lines 328-332 (benchmark harness: simple wall-clock `std::time::Instant`, `--nocapture`, run targeted — full cpu suite is slow / disk-heavy), lines 316-326 (A3 row + verdict logic), lines 361-365 (Pitfall 5 "A3 declared fine without a benchmark" + Pitfall 6 frontier-only memory)
    - 17-PATTERNS.md lines 160-176 (tree_bench analog; report the 64-vs-128 delta; run targeted only)
    - MEMORY.md notes backend-test-suite-slow + full-cargo-test-exhausts-disk (run targeted; never the full suite here)
  </read_first>
  <action>
    Create `crates/mlrs-backend/tests/tree_bench.rs` declaring `mod tree_spike;` to reuse `build_tree`.
    Generate a representative host dataset ≈1000 samples × 20 features (deterministic host RNG seed),
    precompute host quantile bin edges (D-10). Time a full depth-8 per-tree build with
    `std::time::Instant` at `n_bins = 64` and `n_bins = 128`, printing the wall-clock per build and the
    64-vs-128 delta with `--nocapture`. Add a small scaling sweep — a few sample counts (e.g. 250 / 500 /
    1000) at fixed 128 bins, and the two bin counts at fixed n — and print the per-step timings so the
    SHAPE (sub-quadratic vs pathological) is observable per D-05 (judge shape, not an absolute ceiling).
    Note in the printed output whether scratch (histogram + node arrays) stayed bounded by the active
    frontier rather than materializing `nodes × features × bins × classes` resident at once (Pitfall 6 —
    feeds the D-06 "frontier-only" lever). Keep it a wall-clock probe, NOT a Criterion micro-benchmark.
    Open with `capability::skip_f64_with_log()` for the f64 path; run f32 always. Mark the bench test
    `#[ignore]` is NOT required, but it MUST be runnable targeted (`--test tree_bench`) and complete in
    reasonable time; do NOT let it pull the full mlrs-backend suite. Add a correctness sanity assert
    (the built tree has a plausible non-zero node count) so the timing is of a real build, not a no-op.
  </action>
  <verify>
    <automated>cargo test -p mlrs-backend --features cpu --test tree_bench -- --nocapture 2>&1 | tail -25 | grep -E 'bins|test result: ok|0 failed'</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p mlrs-backend --features cpu --test tree_bench -- --nocapture` runs to completion and PRINTS wall-clock timings for the ≈1000×20×depth-8 build at BOTH 64 and 128 bins (grep `bins` in output).
    - A samples/bins scaling sweep is printed so the cost shape is observable (multiple timed steps).
    - The 64-vs-128 delta is printed (the D-06 lever data).
    - A correctness sanity assert confirms the timed build produced a non-trivial tree (non-zero node count) — not a no-op.
    - The probe runs targeted only and completes in reasonable time (does not invoke the full cpu suite).
  </acceptance_criteria>
  <done>The per-tree cost benchmark records wall-clock at 64 and 128 bins on ≈1000×20×depth-8 plus a scaling sweep and the 64-vs-128 delta — the A3 evidence the verdict cites.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host benchmark driver → device kernels | validated launch geometry only (Plan-02 wrappers); deterministic host-generated dataset, no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-07 | Repudiation (measurement honesty) | A3 cost benchmark | medium | mitigate | Benchmark the representative ≈1000×20×depth-8 load at 64 AND 128 bins with a scaling sweep + a non-no-op correctness sanity assert; the verdict cites the printed numbers, never "it compiled" (Pitfall 5). |
| T-17-08 | DoS (environment) | full-suite/disk exhaustion | low | mitigate | Run targeted (`--test tree_bench`) only; never the full cpu suite (project memory: backend-test-suite-slow, full-cargo-test-exhausts-disk). |
| T-17-SC | Tampering | npm/pip/cargo installs | n/a | accept | Zero new packages — `std::time::Instant` + existing deps. No install task. |
</threat_model>

<verification>
- `cargo test -p mlrs-backend --features cpu --test tree_bench -- --nocapture` completes and prints 64-bin + 128-bin wall-clock + scaling sweep + delta.
- A non-no-op correctness sanity assert confirms a real tree was timed.
- Runs targeted only (no full-suite/disk hazard).
</verification>

<success_criteria>
SC-4's cost component is met: a per-tree build benchmark at 64 vs 128 bins on the representative fixture,
with a scaling sweep showing the cost shape, gives the verdict the A3 evidence and the D-06 "fewer bins"
lever its data.
</success_criteria>

<output>
Create `.planning/phases/17-randomforest-gpu-histogram-split-feasibility-spike-gating/17-04-SUMMARY.md` when done.
Record the 64-bin and 128-bin wall-clock numbers, the observed scaling shape (sub-quadratic? pathological?),
the 64-vs-128 delta, and the frontier-memory observation — the raw A3 inputs to VERDICT.md (Plan 05).
</output>
