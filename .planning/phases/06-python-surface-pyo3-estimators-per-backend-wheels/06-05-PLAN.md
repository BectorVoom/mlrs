---
phase: 06-python-surface-pyo3-estimators-per-backend-wheels
plan: 05
type: execute
wave: 5
depends_on: ["06-04"]
files_modified:
  - crates/mlrs-py/python/tests/conftest.py
  - crates/mlrs-py/python/tests/test_oracle_linear.py
  - crates/mlrs-py/python/tests/test_oracle_cluster.py
  - crates/mlrs-py/python/tests/test_oracle_decomposition.py
  - crates/mlrs-py/python/tests/test_oracle_neighbors.py
  - crates/mlrs-py/python/tests/test_dtype.py
autonomous: true
requirements: [PY-01, PY-03, PY-05]

must_haves:
  truths:
    - "The 1e-5 numerical contract is re-validated through the FULL numpy->pyarrow->PyCapsule->Rust FFI->validate->device->host->numpy path (criterion 1), not just the Rust layer"
    - "Each estimator family's oracle test loads the committed .npz fixtures and compares the shim's fitted attribute to the sklearn reference within 1e-5, using sign-flip (PCA/SVD) and label-permutation (KMeans/DBSCAN) invariance helpers"
    - "LogisticRegression compares gauge-fixed predict_proba (the primary gate, Phase-5 D-12), not raw coef_"
    - "f32 and f64 both dispatch correctly on cpu; f64 on a rocm wheel is skipped-with-log via mlrs.backend_supports_f64(); f64-on-rocm fit raises a clear error (D-04/D-05/PY-05)"
    - "A threaded test demonstrates the GIL is released during compute (PY-03)"
  artifacts:
    - path: "crates/mlrs-py/python/tests/test_oracle_linear.py"
      provides: "oracle 1e-5 check for LinearRegression/Ridge/Lasso/ElasticNet/LogisticRegression"
      contains: "1e-5"
    - path: "crates/mlrs-py/python/tests/test_dtype.py"
      provides: "f32/f64 dispatch + f64-on-rocm raise/skip"
      contains: "backend_supports_f64"
  key_links:
    - from: "crates/mlrs-py/python/tests/conftest.py"
      to: "tests/fixtures/*.npz"
      via: "numpy.load committed fixtures"
      pattern: "load|fixtures"
    - from: "crates/mlrs-py/python/tests/test_dtype.py"
      to: "mlrs.backend_supports_f64"
      via: "skipif / raises"
      pattern: "backend_supports_f64"
---

<objective>
Stand up the Python oracle harness (criterion 1): re-validate the 1e-5 contract for
all 12 estimators through the full numpy->pyarrow->PyCapsule->device->host->numpy path,
reusing the committed `.npz` fixtures as a second consumer (no fixture regeneration).
Convert the Plan-01 oracle/dtype stubs into real assertions with sign-flip /
label-permutation / gauge-fixed-proba comparison helpers, and prove f32/f64 dispatch
plus the f64-on-rocm skip/raise behavior (D-04/D-05) and GIL release (PY-03).

Purpose: This is the numerical gate that proves the binding path preserves correctness
end-to-end — the single most important phase criterion. It consumes the Plan-04 shim.
Output: a filled `conftest.py` and five test modules (oracle per family + dtype).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@AGENTS.md
@.planning/phases/06-python-surface-pyo3-estimators-per-backend-wheels/06-RESEARCH.md
@.planning/phases/06-python-surface-pyo3-estimators-per-backend-wheels/06-PATTERNS.md
@.planning/phases/06-python-surface-pyo3-estimators-per-backend-wheels/06-VALIDATION.md
@scripts/gen_oracle.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fill conftest helpers + the four per-family oracle tests (criterion 1)</name>
  <read_first>
    - .planning/phases/06-python-surface-pyo3-estimators-per-backend-wheels/06-RESEARCH.md (## pytest Oracle Harness — second-consumer pattern, sign-flip/label-perm/gauge-fixed-proba, dtype gating)
    - .planning/phases/06-python-surface-pyo3-estimators-per-backend-wheels/06-PATTERNS.md (conftest/test_oracle analogs, lines 223-242 — gen_oracle fixtures + kmeans_test/pca_test compare structure)
    - scripts/gen_oracle.py (the .npz key names per estimator: coef_, labels_, components_, predict_proba, distances/indices)
    - crates/mlrs-algos/tests/kmeans_test.rs (label-permutation compare), pca_test.rs/truncated_svd_test.rs (sign-flip compare) — re-express in Python
    - crates/mlrs-py/python/tests/conftest.py (the Plan 01 stub)
  </read_first>
  <action>Fill `conftest.py`'s `sign_flip_allclose`, `label_perm_allclose`, and the fixture loader (`numpy.load` of repo-root `tests/fixtures/<name>_<dtype>_seed42.npz`) with real implementations mirroring `mlrs_core::oracle` (column-sign invariance for components_; best label permutation for labels_). Add a `proba_allclose` helper for gauge-fixed `predict_proba`. Convert the four `test_oracle_{linear,cluster,decomposition,neighbors}.py` stubs into real tests: each `@pytest.mark.parametrize`s over the relevant committed fixtures (both dtypes), runs the input through the matching `mlrs.<Estimator>(...).fit(X[, y])`, and asserts the fitted attribute matches the fixture reference within `1e-5` using the right comparison helper — linear: `coef_`/`intercept_` direct; LogisticRegression: gauge-fixed `predict_proba` (Phase-5 D-12), NOT raw coef_; cluster: KMeans `labels_`+`cluster_centers_` (label-perm), DBSCAN `labels_` (label-perm); decomposition: PCA/TruncatedSVD `components_` (sign-flip) + `mean_`; neighbors: NearestNeighbors `kneighbors` distances/indices, KNeighborsClassifier/Regressor predictions. Gate f64 fixtures behind `@pytest.mark.skipif(not mlrs.backend_supports_f64())` (mirrors `skip_f64_with_log`). Remove the Plan-01 xfail guards. Tests stay in the pytest tree (AGENTS.md §2).</action>
  <verify>
    <automated>maturin develop -m crates/mlrs-py/pyproject/cpu.pyproject.toml >/dev/null 2>&1; python3 -m pytest crates/mlrs-py/python/tests/test_oracle_linear.py crates/mlrs-py/python/tests/test_oracle_cluster.py crates/mlrs-py/python/tests/test_oracle_decomposition.py crates/mlrs-py/python/tests/test_oracle_neighbors.py -q 2>&1 | grep -Eq '[0-9]+ passed' && ! python3 -m pytest crates/mlrs-py/python/tests/test_oracle_linear.py -q 2>&1 | grep -q 'failed'</automated>
  </verify>
  <acceptance_criteria>
    - On the cpu wheel (f64), all four oracle modules pass for all 12 estimators within 1e-5 (full binding path).
    - `conftest.py` defines real `sign_flip_allclose`, `label_perm_allclose`, `proba_allclose`, and the fixture loader (no stub `pass`).
    - LogisticRegression oracle compares gauge-fixed `predict_proba`, not raw coef_ (grep the test).
    - f64 fixtures are skipped-with-reason when `backend_supports_f64()` is False (grep the skipif).
  </acceptance_criteria>
  <done>The full numpy->...->numpy path is proven correct to 1e-5 for all 12 estimators (criterion 1), reusing the committed fixtures with the right invariance helpers.</done>
</task>

<task type="auto">
  <name>Task 2: test_dtype.py — f32/f64 dispatch, f64-on-rocm raise, GIL-release (PY-05/PY-03)</name>
  <read_first>
    - .planning/phases/06-python-surface-pyo3-estimators-per-backend-wheels/06-RESEARCH.md (## Common Pitfalls Pitfall 5 + Pitfall 6; ## sklearn estimator_checks dtype tag note; D-04/D-05)
    - crates/mlrs-py/python/tests/test_dtype.py (the Plan 01 stub)
    - crates/mlrs-backend/src/capability.rs (feature_enabled/skip_f64_with_log semantics — the f64-incapable behavior being asserted)
  </read_first>
  <action>Convert `test_dtype.py` into real assertions: (1) f32-in -> f32-out and f64-in -> f64-out dtype preservation through a representative estimator (e.g. Ridge) on cpu (D-05); (2) integer input defaults to f64 on an f64-capable backend, f32 otherwise, via `mlrs.backend_supports_f64()` (D-05/Pitfall 5); (3) `test_f64_on_incapable_backend_raises`: `@pytest.mark.skipif(mlrs.backend_supports_f64())` (only runs where f64 is unsupported, e.g. rocm) asserting `pytest.raises((ValueError,))` with a message naming the backend and float64 (D-04) — never a silent downcast; (4) a GIL-release smoke (PY-03/Pitfall 6): spawn two `threading.Thread`s each running a `.fit` on independent estimators and assert both complete (proves `py.detach` lets the threads progress; serialization on the device mutex is acceptable — assert completion, not parallel speedup). Remove the Plan-01 xfail guard.</action>
  <verify>
    <automated>maturin develop -m crates/mlrs-py/pyproject/cpu.pyproject.toml >/dev/null 2>&1; python3 -m pytest crates/mlrs-py/python/tests/test_dtype.py -q 2>&1 | grep -Eq '[0-9]+ passed'</automated>
  </verify>
  <acceptance_criteria>
    - On cpu: f32/f64 dtype preservation passes; the f64-on-incapable-backend raise test is SKIPPED (f64 supported) with a reason.
    - `grep 'backend_supports_f64' crates/mlrs-py/python/tests/test_dtype.py` matches (capability-gated).
    - `grep -E 'pytest.raises' crates/mlrs-py/python/tests/test_dtype.py` matches the f64-on-rocm raise test (asserts a clear ValueError, not a downcast).
    - `grep -E 'threading|Thread' crates/mlrs-py/python/tests/test_dtype.py` matches the GIL-release smoke.
    - (rocm gate, opportunistic) `maturin develop -m crates/mlrs-py/pyproject/rocm.pyproject.toml && pytest test_dtype.py` runs the f64-raise test green and skips f64 oracle cases.
  </acceptance_criteria>
  <done>PY-05 dtype dispatch + the D-04 f64-on-incapable-backend clear error + the PY-03 GIL-release behavior are all proven by green pytest (f64-raise gated to the rocm wheel).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed .npz fixture -> test input | fixtures are trusted committed blobs (not regenerated at test time); the path under test is the binding, not the fixture |
| f64 input -> f64-incapable device | the test asserts the guard raises, proving no silent downcast |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-14 | Tampering | Numerical drift introduced by the binding path (wrong dtype, wrong shape, buffer mishandling) silently passing | mitigate | The oracle harness re-asserts 1e-5 through the FULL path with the correct invariance helpers; drift fails the gate (criterion 1). |
| T-06-15 | Tampering | f64 silently downcast on rocm slipping past review | mitigate | `test_f64_on_incapable_backend_raises` asserts a clear ValueError on the rocm wheel; a downcast would fail the test (D-04). |

No unmitigated `high` threats. This plan is the validation gate; it adds no new
attack surface.
</threat_model>

<verification>
- cpu(f64): all four oracle modules + test_dtype pass within 1e-5 (full binding path).
- rocm(f32, opportunistic): f64 oracle cases skip-with-log; the f64-raise test passes.
- conftest has real sign-flip/label-perm/proba helpers; GIL-release smoke present.
</verification>

<success_criteria>
- The 1e-5 contract is re-validated end-to-end for all 12 estimators (criterion 1 / PY-01).
- f32/f64 dispatch + f64-on-incapable-backend clear error proven (D-04/D-05/PY-05).
- GIL released during compute, demonstrated by the threaded smoke (PY-03).
</success_criteria>

<output>
Create `.planning/phases/06-python-surface-pyo3-estimators-per-backend-wheels/06-05-SUMMARY.md` when done.
</output>
