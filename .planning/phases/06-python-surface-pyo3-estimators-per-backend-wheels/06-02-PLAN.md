---
phase: 06-python-surface-pyo3-estimators-per-backend-wheels
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - crates/mlrs-py/src/lib.rs
  - crates/mlrs-py/src/ingress.rs
  - crates/mlrs-py/src/egress.rs
  - crates/mlrs-py/src/capability.rs
  - crates/mlrs-py/src/dispatch.rs
  - crates/mlrs-py/src/errors.rs
  - crates/mlrs-py/tests/ingress_test.rs
  - crates/mlrs-py/tests/probe_test.rs
autonomous: true
requirements: [PY-03, PY-04, PY-05]

must_haves:
  truths:
    - "An Arrow PyCapsule (__arrow_c_array__) is consumed via an OWNED arrow-rs import — arrow-rs owns the release callback; no bare &[u8] borrow into a Python-owned buffer (PY-03/D-02)"
    - "The existing bridge::validate_f32/validate_f64 is reused UNCHANGED at the PyCapsule boundary; sliced/offset arrays are hard-rejected (D-02)"
    - "Device compute runs inside Python::detach — the GIL is released around the trait call (PY-03)"
    - "A single process-global Mutex<BufferPool<ActiveRuntime>> owns the pool+client (Claude's-discretion concurrency model recorded here)"
    - "import _mlrs probes the driver via std::panic::catch_unwind(active_client) and raises PyImportError on a missing driver, never aborting the process (D-08/PY-04)"
    - "Passing f64 to an f64-incapable backend raises a clear Python exception via capability.feature_enabled(FloatKind::F64); never a silent downcast (D-04/PY-05)"
    - "mlrs.backend_supports_f64() is exposed so the shim and pytest can pick the default dtype / skip f64 (D-05)"
  artifacts:
    - path: "crates/mlrs-py/src/lib.rs"
      provides: "#[pymodule] _mlrs with catch_unwind import probe + OnceLock<Mutex<BufferPool>> global"
      contains: "pymodule"
    - path: "crates/mlrs-py/src/ingress.rs"
      provides: "PyCapsule → owned ArrayData → validate_f32/f64 → DeviceArray::from_host"
      contains: "validate_f"
    - path: "crates/mlrs-py/src/dispatch.rs"
      provides: "macro_rules! any_estimator! producing the per-estimator F32/F64 enum (D-06)"
      contains: "macro_rules"
    - path: "crates/mlrs-py/src/capability.rs"
      provides: "f64-on-incapable-backend guard → PyValueError; backend_supports_f64 flag"
      contains: "feature_enabled"
    - path: "crates/mlrs-py/src/errors.rs"
      provides: "boundary anyhow/AlgoError → PyErr mapping"
      contains: "PyErr"
  key_links:
    - from: "crates/mlrs-py/src/ingress.rs"
      to: "mlrs_backend::bridge::validate_f32"
      via: "reuse unchanged"
      pattern: "validate_f32|validate_f64"
    - from: "crates/mlrs-py/src/lib.rs"
      to: "mlrs_backend::runtime::active_client"
      via: "catch_unwind import probe"
      pattern: "catch_unwind"
    - from: "crates/mlrs-py/src/dispatch.rs"
      to: "Python::detach"
      via: "GIL release around trait call"
      pattern: "detach"
---

<objective>
Build the core binding plumbing inside the `mlrs-py` cdylib: the `#[pymodule] _mlrs`
with the import-time driver probe (D-08), the process-global pool/client, the Arrow
PyCapsule ingress (reusing `bridge::validate_f32/f64` unchanged), the device→host
egress helpers, the capability guard (D-04) + `backend_supports_f64()` flag, the
boundary error→PyErr mapping, and the `macro_rules! any_estimator!` dtype-dispatch
generator (D-06). No `#[pyclass]` wrappers yet — those are Plan 03; this plan delivers
every shared primitive they consume.

Purpose: Plan 03's 12 wrappers must receive the ingress/egress/dispatch/capability
contracts in-hand (interface-first), not rediscover them. Getting the PyCapsule
ownership, GIL release, and panic-safe probe right here is the load-bearing FFI work.
Output: `lib.rs` (module-init), `ingress.rs`, `egress.rs`, `capability.rs`,
`dispatch.rs`, `errors.rs`, plus Rust integration tests for ingress + probe.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@AGENTS.md
@.planning/phases/06-python-surface-pyo3-estimators-per-backend-wheels/06-RESEARCH.md
@.planning/phases/06-python-surface-pyo3-estimators-per-backend-wheels/06-PATTERNS.md
@crates/mlrs-py/src/lib.rs
@crates/mlrs-py/src/arrow_symbol_probe.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PyCapsule ingress + capability guard + boundary errors (PY-03/D-02/D-04)</name>
  <read_first>
    - crates/mlrs-backend/src/bridge.rs:40-104 (validate_f32/validate_f64 + validate_no_offset hard-reject — REUSE UNCHANGED)
    - crates/mlrs-backend/src/device_array.rs:59-83 (DeviceArray::from_host — the upload target)
    - crates/mlrs-backend/src/capability.rs:50-54 (feature_enabled), :107 (active_backend_name)
    - crates/mlrs-py/src/arrow_symbol_probe.rs (the confirmed arrow-59 FromPyArrow method name from Plan 01)
    - .planning/phases/06-python-surface-pyo3-estimators-per-backend-wheels/06-RESEARCH.md (### Pattern 2 Arrow PyCapsule Ingest; ## Error Mapping notes; Pitfall 3 sliced-array reject)
    - .planning/phases/06-python-surface-pyo3-estimators-per-backend-wheels/06-PATTERNS.md (ingress.rs / capability.rs sections, lines 69-123)
  </read_first>
  <behavior>
    - Consuming a freshly-contiguous Float32 pyarrow array yields a validated &[f32] of the expected length.
    - Consuming a Float64 array yields a validated &[f64].
    - A SLICED/offset pyarrow array is hard-rejected (bridge BridgeError::Offset → mapped PyValueError), never uploaded.
    - feature_enabled(F64) == false on a backend → fit_f64 path raises a clear PyValueError naming the backend and suggesting mlrs-cpu (D-04), never downcasts.
    - A non-float Arrow dtype raises an "unsupported dtype" PyTypeError.
  </behavior>
  <action>Create `crates/mlrs-py/src/errors.rs`: a boundary error type (or `From` impls) mapping `mlrs_backend::bridge::BridgeError`, `mlrs_algos::error::AlgoError`, and `anyhow::Error` to the right `pyo3::exceptions::Py*::new_err` (BridgeError::Offset/Null/Align → `PyValueError`; unsupported dtype → `PyTypeError`; capability failure → `PyValueError`). Use `anyhow` at this boundary (CLAUDE.md), `thiserror` stays in the libs. Create `crates/mlrs-py/src/ingress.rs`: a `fn capsule_to_array(x: &Bound<PyAny>) -> PyResult<ArrayRef>` that consumes the `__arrow_c_array__` capsule via the arrow-rs `FromPyArrow` method confirmed in `arrow_symbol_probe.rs` (owned `ArrayData` → `make_array`; arrow-rs owns the release callback — NO `&[u8]` borrow), and a `fn validated_f32(arr)`/`validated_f64(arr)` that downcasts to `Float32Array`/`Float64Array` and calls `mlrs_backend::bridge::validate_f32`/`validate_f64` UNCHANGED, then `DeviceArray::from_host(pool, validated)`. Create `crates/mlrs-py/src/capability.rs`: re-export/wrap `mlrs_backend::capability::feature_enabled(FloatKind::F64)` as `pub fn supports_f64() -> bool`, and a `fn guard_f64(backend: &str) -> PyResult<()>` that returns a clear `PyValueError` ("backend '{backend}' does not support float64 — pass float32 or install mlrs-cpu") when `!supports_f64()`. Tests go in `crates/mlrs-py/tests/ingress_test.rs` (AGENTS.md §2 — never in-source).</action>
  <verify>
    <automated>cargo test -p mlrs-py --features cpu --test ingress_test 2>&1 | grep -Eq 'test result: ok|0 failed'</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p mlrs-py --features cpu --test ingress_test` passes (contiguous f32/f64 validate; sliced array rejected; non-float rejected).
    - `grep -E 'validate_f32|validate_f64' crates/mlrs-py/src/ingress.rs` matches (bridge reused unchanged — no re-implemented validation).
    - `grep 'does not support float64' crates/mlrs-py/src/capability.rs` matches (D-04 clear error).
    - No `&[u8]` borrow of the capsule buffer survives past `capsule_to_array` (owned ArrayData only).
  </acceptance_criteria>
  <done>Arrow PyCapsule ingress is owned + validated via the unchanged bridge; the f64-on-incapable-backend guard raises a clear error; boundary errors map to the right PyErr types.</done>
</task>

<task type="auto">
  <name>Task 2: #[pymodule] _mlrs — catch_unwind import probe + global pool + egress helpers (D-08/PY-04)</name>
  <read_first>
    - crates/mlrs-py/src/lib.rs (current: mod allocator + BoundaryResult alias, lines 1-16 — grow into #[pymodule])
    - crates/mlrs-backend/src/runtime.rs:41-46 (active_client — .unwrap()s internally → PANICS on missing driver)
    - crates/mlrs-backend/src/pool.rs:73 (BufferPool::new)
    - crates/mlrs-backend/src/device_array.rs:108-122 (to_host → Vec<F>)
    - crates/mlrs-backend/src/capability.rs:107 (active_backend_name for the error message)
    - .planning/phases/06-python-surface-pyo3-estimators-per-backend-wheels/06-RESEARCH.md (### Pattern 4 Import-Time Driver Probe; ### Pattern 5 Python::detach; ## BufferPool + Client Lifecycle recommendation; Pitfall 2)
    - .planning/phases/06-python-surface-pyo3-estimators-per-backend-wheels/06-PATTERNS.md (lib.rs section, lines 43-66)
  </read_first>
  <action>Grow `crates/mlrs-py/src/lib.rs` into `#[pymodule] fn _mlrs(...)`. Keep `mod allocator;` untouched (FOUND-09 single allocator site). Declare `mod ingress; mod egress; mod capability; mod dispatch; mod errors;` and (in Plan 03) `mod estimators;`. At module init, run the D-08 driver probe: `std::panic::catch_unwind(|| { let c = mlrs_backend::runtime::active_client(); let _ = c.properties(); })`; on `Err`, return `PyImportError::new_err(format!("mlrs-{0} requires the {0} runtime/driver; none was detected. Install the {0} driver or use a different mlrs backend wheel.", mlrs_backend::capability::active_backend_name()))`. Define the process-global pool as `static GLOBAL_POOL: OnceLock<Mutex<BufferPool<ActiveRuntime>>>` initialized from `active_client()` (only reached after the probe succeeds), exposing a `pub(crate) fn global_pool() -> &'static Mutex<BufferPool<ActiveRuntime>>`. Record the concurrency model in a module doc comment: a single device mutex serializes joblib `n_jobs>1` (accepted v1 single-device semantics, RESEARCH §BufferPool Lifecycle). Add `#[pyfunction] fn backend_supports_f64() -> bool` delegating to `capability::supports_f64()`, registered on the module so Python sees `mlrs._mlrs.backend_supports_f64()`. Create `crates/mlrs-py/src/egress.rs`: `fn vec_f_to_py(...)` / `fn vec_i32_to_py(...)` helpers that return `(Vec<F>, shape)` / `(Vec<i32>, shape)` to Python as plain lists/tuples (the numpy/pyarrow wrap is shim-side per D-03) using `device_array::to_host`. Create `crates/mlrs-py/src/dispatch.rs` with a `macro_rules! any_estimator!` skeleton + a usage doc comment showing the `{ Unfit{..}, F32(_), F64(_) }` shape and where `py.detach { global_pool().lock()... }` wraps the trait call (Plan 03 invokes the macro per estimator). The probe test lives in `crates/mlrs-py/tests/probe_test.rs`.</action>
  <verify>
    <automated>cargo build -p mlrs-py --features cpu 2>&1 | grep -Eq 'Compiling|Finished' && cargo test -p mlrs-py --features cpu --test probe_test 2>&1 | grep -Eq 'test result: ok|0 failed'</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p mlrs-py --features cpu` compiles the `#[pymodule] _mlrs`.
    - `grep 'catch_unwind' crates/mlrs-py/src/lib.rs` and `grep 'PyImportError' crates/mlrs-py/src/lib.rs` match (D-08 probe).
    - `grep -E 'OnceLock.*Mutex.*BufferPool' crates/mlrs-py/src/lib.rs` matches (single global pool).
    - `grep 'macro_rules! any_estimator' crates/mlrs-py/src/dispatch.rs` matches; the macro doc shows `py.detach` around the trait call (D-06/PY-03 contract for Plan 03).
    - `grep 'backend_supports_f64' crates/mlrs-py/src/lib.rs` matches (registered #[pyfunction]).
    - `cargo test -p mlrs-py --features cpu --test probe_test` passes (probe succeeds on cpu, where the driver is present).
  </acceptance_criteria>
  <done>The _mlrs module initializes with a panic-safe driver probe, a single global Mutex<BufferPool>, the backend_supports_f64 flag, egress helpers, and the any_estimator! macro contract — Plan 03's wrappers have every primitive they need.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python-owned Arrow buffer → Rust FFI | The C ArrowArray + release callback cross here; mis-ownership = use-after-free/double-free |
| absent driver → CPython process | cubecl `client()` `.unwrap()`s; an uncaught panic unwinding into CPython is UB / process abort |
| f64 input → f64-incapable device | silent downcast would break the 1e-5 contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Tampering | Use-after-free / double-free of the Arrow C array across FFI | mitigate | `capsule_to_array` takes an OWNED arrow-rs `ArrayData` (arrow-rs owns the release callback); no `&[u8]` borrow into a Python buffer (PY-03 literal). Verified by `ingress_test`. |
| T-06-04 | Tampering / Info-disclosure | Aliased parent-buffer data from a sliced array | mitigate | `bridge::validate_no_offset` (reused unchanged) hard-rejects offset/sliced arrays → `PyValueError`; the shim hands a fresh contiguous array (Plan 04). `ingress_test` asserts the reject. |
| T-06-05 | DoS | Driver-absent panic unwinding into CPython → process abort | mitigate (HIGH→mitigated) | `std::panic::catch_unwind(active_client)` at module init → clean `PyImportError`; the wheel profile keeps `panic = "unwind"` (no `panic = "abort"` was added in Plan 01). RESEARCH Pitfall 2. |
| T-06-06 | Tampering | Silent f64→f32 downcast on an f64-incapable backend | mitigate | `guard_f64` raises `PyValueError` before compute via `feature_enabled(F64)`; never downcasts (D-04). |
| T-06-07 | Tampering | Misaligned transmute (UB) on the validated slice | mitigate | `bridge::cast_validated` (bytemuck `try_cast_slice`) returns a recoverable error, never panics — reused unchanged. |

The HIGH-severity threat (T-06-05, driver-absent process abort) is mitigated by the
`catch_unwind` probe + `panic = "unwind"`; no unmitigated `high` remains.
</threat_model>

<verification>
- `cargo build -p mlrs-py --features cpu` compiles the `_mlrs` module.
- `cargo test -p mlrs-py --features cpu --test ingress_test --test probe_test` passes.
- Owned-capsule ingress (no `&[u8]` borrow), reused validate bridge, catch_unwind probe, global Mutex pool, f64 guard, backend_supports_f64 flag, and any_estimator! macro all present (greps above).
</verification>

<success_criteria>
- Arrow PyCapsule ingress is ownership-correct and reuses the validate bridge unchanged (PY-03/D-02).
- GIL-release contract (`py.detach`) and the global Mutex pool are established for Plan 03.
- Import probe raises `PyImportError` (not abort) on a missing driver (D-08/PY-04).
- f64-on-incapable-backend raises a clear error; `backend_supports_f64()` is exposed (D-04/D-05/PY-05).
</success_criteria>

<output>
Create `.planning/phases/06-python-surface-pyo3-estimators-per-backend-wheels/06-02-SUMMARY.md` when done.
</output>
