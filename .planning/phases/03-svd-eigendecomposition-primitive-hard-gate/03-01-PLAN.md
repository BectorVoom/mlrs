---
phase: 03-svd-eigendecomposition-primitive-hard-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/mlrs-backend/src/runtime.rs
  - crates/mlrs-backend/Cargo.toml
  - .planning/ROADMAP.md
  - .planning/PROJECT.md
  - .planning/phases/03-svd-eigendecomposition-primitive-hard-gate/03-CONTEXT.md
autonomous: true
requirements: [PRIM-05]
must_haves:
  truths:
    - "A real #[cube] kernel compiles and runs end-to-end on gfx1100 via --features rocm (spike_saxpy_runs_on_active_backend passes)."
    - "capability::active_backend_name() == rocm and supports_type(F32) == true under --features rocm."
    - "supports_type(F64) == false on rocm is documented as expected (f64 validates on cpu), not a defect."
    - "ROADMAP/PROJECT/CONTEXT document the cpu+rocm gate with the f64-on-cpu / f32-on-rocm split (no stale cpu+wgpu or f64-runs-on-rocm wording for Phase 3+)."
  artifacts:
    - path: "crates/mlrs-backend/src/runtime.rs"
      provides: "rocm ActiveRuntime/ActiveDevice resolved via cubecl::hip"
      contains: "cubecl::hip"
    - path: "crates/mlrs-backend/Cargo.toml"
      provides: "rocm feature that compiles cubecl-hip"
      contains: "cubecl/std"
  key_links:
    - from: "crates/mlrs-backend/src/runtime.rs"
      to: "cubecl::hip::{HipRuntime, AmdDevice}"
      via: "cfg(feature rocm) pub use"
      pattern: "cubecl::hip::\\{AmdDevice"
    - from: "crates/mlrs-backend/Cargo.toml"
      to: "cubecl/std + cubecl/default"
      via: "rocm feature list"
      pattern: "rocm = \\[.*cubecl/std"
---

<objective>
De-risk the project's first real ROCm/HIP GPU execution (D-07) BEFORE any SVD work,
and reconcile the project documentation to the corrected cpu+rocm gate with the
f64-on-cpu / f32-on-rocm split (RESEARCH CRITICAL FINDING 2). This plan gates every
GPU task in the phase: until the existing spike_saxpy_runs_on_active_backend saxpy
test runs on gfx1100 under --features rocm, no Jacobi kernel can be validated on the
GPU gate backend.

Purpose: rocm has only ever compiled (never executed) through Phase 2. RESEARCH
reproduced a working bring-up with exactly two required fixes. This plan applies them
and proves the saxpy gate, then corrects the stale cpu+wgpu / f64-runs-on-rocm
wording the docs still carry (per D-07).
Output: a compiling+running rocm feature, a passing saxpy gate on gfx1100, and
reconciled ROADMAP/PROJECT/CONTEXT wording.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-svd-eigendecomposition-primitive-hard-gate/03-RESEARCH.md
@.planning/phases/03-svd-eigendecomposition-primitive-hard-gate/03-PATTERNS.md
@crates/mlrs-backend/src/runtime.rs
@crates/mlrs-backend/Cargo.toml
@crates/mlrs-backend/tests/spike_test.rs
@AGENTS.md
</context>

<artifacts_this_phase_produces>
This plan modifies these symbols (the phase-wide symbol list also appears in plans 02-05):
- crates/mlrs-backend/src/runtime.rs: corrected cfg(feature rocm) re-export
  pub use cubecl::hip::{AmdDevice as ActiveDevice, HipRuntime as ActiveRuntime}.
- crates/mlrs-backend/Cargo.toml: rocm = ["cubecl/rocm", "cubecl/std", "cubecl/default"].
- Reconciled doc wording in ROADMAP.md (Phase 3 success criteria 1 & 3 + Overview),
  PROJECT.md (gate backend), and 03-CONTEXT.md (D-07 f64-on-rocm clause).
No new code symbols (kernels/prims/tests) — those are plans 02-05.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Apply the two verified ROCm bring-up fixes (runtime.rs + Cargo.toml) and prove the saxpy gate on gfx1100</name>
  <files>crates/mlrs-backend/src/runtime.rs, crates/mlrs-backend/Cargo.toml</files>
  <read_first>
    - crates/mlrs-backend/src/runtime.rs (the stale cfg(feature rocm) line ~20 importing the non-existent cubecl::rocm::{RocmDevice, RocmRuntime})
    - crates/mlrs-backend/Cargo.toml (the [features] table; current rocm = ["cubecl/rocm"])
    - crates/mlrs-backend/tests/spike_test.rs (spike_saxpy_runs_on_active_backend — the bring-up acceptance gate)
    - 03-RESEARCH.md Pattern 1 (ROCm/HIP runtime bring-up) + Pitfall 2 (cubecl-hip needs multi_threading = all(feature std, not wasm); the symbols MultiStream/ResolvedStreams/EventStreamBackend are otherwise absent)
    - 03-PATTERNS.md sections "crates/mlrs-backend/src/runtime.rs" + "crates/mlrs-backend/Cargo.toml"
    - AGENTS.md §4 (CubeCL build-error protocol — if the rocm build fails, read /home/user/Documents/workspace/cubecl_manual/manual/Cubecl/cubecl_error_guideline.md before any blind fix)
  </read_first>
  <action>
    In runtime.rs replace the cfg(feature rocm) re-export line so it reads
    pub use cubecl::hip::{AmdDevice as ActiveDevice, HipRuntime as ActiveRuntime}; .
    cubecl 0.10 re-exports cubecl_hip as hip under rocm = ["hip"]; there is NO cubecl::rocm
    module and NO HipDevice alias — the device struct is AmdDevice, which derives Default so
    ActiveDevice::default() in active_client() still works. Leave the cpu/wgpu/cuda lines and
    the any(...) cfg on Client/active_client untouched. In Cargo.toml change the rocm feature to
    rocm = ["cubecl/rocm", "cubecl/std", "cubecl/default"] — cubecl/std propagates the
    multi_threading cfg into cubecl-hip (else use cubecl_runtime::stream::MultiStream is
    unresolved, three E0432 errors). Do NOT weaken default-features = false on the workspace
    cubecl / cubek-matmul / cubek-std pins — ONLY the rocm feature line changes (per D-07 /
    RESEARCH Security V12). Per AGENTS.md §4, if the rocm build emits any cubecl
    compile/feature/toolchain error, consult cubecl_error_guideline.md before adjusting features
    further; ensure /opt/rocm/bin is on PATH (cubecl-hip-sys build.rs falls back to hipconfig).
  </action>
  <verify>
    <automated>cargo build -p mlrs-backend --no-default-features --features rocm 2>&1 | tail -5</automated>
    <automated>cargo test -p mlrs-backend --no-default-features --features rocm --test spike_test spike_saxpy_runs_on_active_backend -- --nocapture 2>&1 | tail -15</automated>
    <automated>cargo build -p mlrs-backend --no-default-features --features cpu 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - cargo build -p mlrs-backend --no-default-features --features rocm exits 0 (cubecl-hip compiles; no MultiStream/ResolvedStreams/EventStreamBackend E0432).
    - cargo test ... --features rocm --test spike_test spike_saxpy_runs_on_active_backend reports "test result: ok. 1 passed" (a real HIP saxpy kernel ran on gfx1100 with correct read-back).
    - runtime.rs contains the literal cubecl::hip::{AmdDevice as ActiveDevice, HipRuntime as ActiveRuntime} and NO cubecl::rocm.
    - Cargo.toml rocm feature line contains both cubecl/std and cubecl/default; cpu build still exits 0 (no cross-feature regression).
  </acceptance_criteria>
  <done>The rocm feature compiles, the saxpy gate passes on gfx1100, and the cpu build is unbroken — GPU work in plans 03-05 is unblocked.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm the rocm capability split (f32 supported, f64 unsupported) and reconcile ROADMAP/PROJECT/CONTEXT wording to the cpu+rocm gate</name>
  <files>.planning/ROADMAP.md, .planning/PROJECT.md, .planning/phases/03-svd-eigendecomposition-primitive-hard-gate/03-CONTEXT.md</files>
  <read_first>
    - crates/mlrs-backend/src/capability.rs (active_backend_name, supports_type, feature_enabled, skip_f64_with_log — the gate that already produces the correct f64 skip on rocm)
    - crates/mlrs-backend/tests/capability_test.rs (existing capability assertions to mirror)
    - 03-RESEARCH.md CRITICAL FINDING 2 / Pitfall 1 (cubecl-cpp 0.10 comments F64 out of register_supported_types; f64 validates on cpu)
    - .planning/ROADMAP.md Phase 3 success criteria 1 & 3 (still say wgpu) and the Overview sentence (cpu and wgpu)
    - .planning/PROJECT.md (gate-backend / constraints wording)
    - .planning/phases/.../03-CONTEXT.md D-07 (the "f64 runs natively on rocm" clause RESEARCH corrects)
  </read_first>
  <action>
    Run the capability probe under rocm and cpu and record the result in the SUMMARY: confirm
    active_backend_name()==rocm, supports_type(F32)==true, supports_type(F64)==false on rocm, and
    supports_type(F64)==true on cpu. Then edit the three docs to the corrected gate (per D-07 +
    RESEARCH CRITICAL FINDING 2), changing ONLY the gate-backend wording (do not alter unrelated
    phase content): (1) ROADMAP.md Phase 3 success criteria 1 & 3 — replace wgpu / "f64
    capability-gated on wgpu" with the cpu+rocm gate and the explicit split "f64 validates on cpu;
    rocm validates f32; f64-on-rocm SKIPS-with-log because cubecl-cpp 0.10 does not register F64
    for the HIP backend"; (2) the ROADMAP.md Overview sentence "on both the cpu and wgpu backends"
    — note the gate moved to cpu+rocm from Phase 3 (wgpu opportunistic); (3) PROJECT.md
    gate/constraints wording to cpu+rocm with the same f64/f32 split; (4) 03-CONTEXT.md D-07 —
    append a one-line RESEARCH correction that f64-on-rocm is empirically unsupported at the CubeCL
    layer and is validated on cpu (cite D-07 + the RESEARCH finding). Keep skip_f64_with_log as the
    portable mechanism — do NOT remove or change capability.rs. This is documentation reconciliation
    only; no source/test code changes in this task.
  </action>
  <verify>
    <automated>cargo test -p mlrs-backend --no-default-features --features rocm --test capability_test -- --nocapture 2>&1 | tail -15</automated>
    <automated>cargo test -p mlrs-backend --no-default-features --features cpu --test capability_test -- --nocapture 2>&1 | tail -15</automated>
    <automated>grep -rcn "cpu+rocm" .planning/ROADMAP.md .planning/PROJECT.md .planning/phases/03-svd-eigendecomposition-primitive-hard-gate/03-CONTEXT.md</automated>
  </verify>
  <acceptance_criteria>
    - capability_test passes under --features rocm (the run logs f32_supported=true, f64_supported=false) AND under --features cpu (f64_supported=true).
    - ROADMAP.md Phase 3 success criteria 1 & 3 no longer contain the word wgpu; they state the cpu+rocm gate with the f64-on-cpu / f32-on-rocm split.
    - PROJECT.md and 03-CONTEXT.md D-07 carry the corrected split (grep finds cpu+rocm in all three edited docs).
    - capability.rs and skip_f64_with_log are unchanged (reconciliation is docs-only; the gate mechanism is reused).
  </acceptance_criteria>
  <done>The capability split is confirmed by a passing test on both backends and the three docs no longer carry the stale cpu+wgpu / f64-on-rocm wording.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cargo build config → linked runtime | The rocm feature selects which CubeCL backend runtime is linked; a wrong feature set silently links the wrong backend or fails to compile. |
| host → HIP device (saxpy launch) | The bring-up smoke test crosses host→gfx1100; lengths come from validated host buffers (existing spike_test idiom). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | rocm Cargo feature change | mitigate | Only add cubecl/std+cubecl/default to the rocm feature line; do NOT weaken default-features = false on cubecl/cubek-matmul/cubek-std (RESEARCH Security V12). cpu build re-verified green to prove no cross-feature leakage. |
| T-03-01-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed; cubecl-hip/cubecl-hip-sys are transitive via the existing cubecl pin and were built locally in research (Package Legitimacy Audit: approved, no SLOP/SUS). |
| T-03-01-02 | DoS | HIP device access | accept | gfx1100 + /dev/kfd + /dev/dri/renderD128 + render group verified present (RESEARCH Environment Availability); no untrusted input crosses to the device in this plan. |
</threat_model>

<verification>
- Phase gate prerequisite: cargo test ... --features rocm --test spike_test spike_saxpy_runs_on_active_backend is green (real HIP kernel on gfx1100).
- cargo build -p mlrs-backend --no-default-features --features cpu and --features rocm both exit 0.
- The three docs carry the cpu+rocm gate with the f64-on-cpu / f32-on-rocm split; no wgpu remains in Phase 3 success criteria 1 & 3.
</verification>

<success_criteria>
- --features rocm compiles and the saxpy gate runs on gfx1100.
- supports_type(F32)==true, supports_type(F64)==false on rocm; supports_type(F64)==true on cpu — confirmed by a passing capability_test on both backends.
- ROADMAP/PROJECT/CONTEXT reconciled to cpu+rocm with the f64 split.
- cpu build unbroken.
</success_criteria>

<output>
Create `.planning/phases/03-svd-eigendecomposition-primitive-hard-gate/03-01-SUMMARY.md` when done.
</output>
