---
phase: 02-core-compute-primitives
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/mlrs-backend/src/pool.rs
  - crates/mlrs-backend/src/device_array.rs
  - crates/mlrs-backend/tests/spike_test.rs
  - scripts/gen_oracle.py
  - tests/fixtures/gemm_f32_seed42.npz
  - tests/fixtures/gemm_f64_seed42.npz
  - crates/mlrs-kernels/src/gemm.rs
  - crates/mlrs-kernels/src/lib.rs
  - crates/mlrs-backend/src/prims/mod.rs
  - crates/mlrs-backend/src/prims/gemm.rs
  - crates/mlrs-backend/src/lib.rs
  - crates/mlrs-core/src/error.rs
  - crates/mlrs-backend/tests/gemm_test.rs
autonomous: false
requirements: [PRIM-01]
user_setup: []

must_haves:
  truths:
    - "GEMM matches a host triple-loop reference within 1e-5 for f32 on cpu and wgpu"
    - "GEMM matches the host reference within 1e-5 for f64 (capability-gated via skip_f64_with_log) on cpu and wgpu"
    - "GEMM with transa/transb flags computes the transposed product without materializing a transpose buffer"
    - "PoolStats exposes a read_backs counter bumped on every device read-back (enables D-10 gate 2)"
    - "A subgroup-capability probe reports whether the active adapter supports the plane/subgroup path"
  artifacts:
    - path: "crates/mlrs-kernels/src/gemm.rs"
      provides: "Hand-written tiled GEMM #[cube(launch)] kernel generic over <F: Float + CubeElement> with comptime transpose flags (DEFAULT path; omitted only if checkpoint selects cubecl-matmul wrap)"
      contains: "pub fn gemm_kernel"
    - path: "crates/mlrs-backend/src/prims/gemm.rs"
      provides: "GEMM host API: gemm(pool, a, (m,k), b, (k,n), transa, transb, out) -> DeviceArray with rows*cols==len asserts and pool-routed scratch/out-buffer"
      contains: "pub fn gemm"
    - path: "crates/mlrs-backend/src/pool.rs"
      provides: "PoolStats.read_backs counter + record_read_back() bump method"
      contains: "read_backs"
    - path: "crates/mlrs-backend/tests/gemm_test.rs"
      provides: "host-ref sweep + npz GEMM fixture tests, f32 always + f64 gated, transpose cases, cpu/wgpu"
      contains: "gemm_f64_matches_host_ref"
  key_links:
    - from: "crates/mlrs-backend/src/prims/gemm.rs"
      to: "crates/mlrs-kernels/src/gemm.rs"
      via: "gemm_kernel::launch::<F, ActiveRuntime>"
      pattern: "gemm_kernel::launch"
    - from: "crates/mlrs-backend/src/prims/gemm.rs"
      to: "crates/mlrs-backend/src/pool.rs"
      via: "pool.acquire / pool.release for scratch + out-buffer"
      pattern: "pool\\.(acquire|release)"
---

<objective>
Land the Wave-0 infrastructure the whole phase depends on, resolve the GEMM
substrate blocker via a decision checkpoint, then build and validate the GEMM
primitive (PRIM-01) — the substrate distance (Plan 03) and covariance (Plan 04)
compose on.

This plan front-loads the forced first step: GEMM is the shared substrate, so a
correctness/perf issue here propagates. It also adds the two small runtime hooks
(`PoolStats.read_backs`, subgroup probe) the later memory gate and reduction
plane-path skip need, so they are not discovered mid-implementation.

Purpose: PRIM-01 — a backend-portable GEMM, oracle-validated, with BLAS-style
transpose flags so XᵀX (covariance) reuses it without a transpose buffer (D-06).
Output: `gemm_kernel` (kernels), `gemm` host API (backend), `PoolStats.read_backs`,
a subgroup-capability probe, GEMM npz fixtures, and `gemm_test.rs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-compute-primitives/02-CONTEXT.md
@.planning/phases/02-core-compute-primitives/02-RESEARCH.md
@.planning/phases/02-core-compute-primitives/02-PATTERNS.md
@AGENTS.md
</context>

<artifacts_this_phase_produces>
This plan (Plan 01) produces the following symbols/files (the rest of the phase
extends this list — see Plans 02–05):

- **Kernel fns** (`crates/mlrs-kernels/src/gemm.rs`): `gemm_kernel` (tiled,
  `#[comptime] trans_a/trans_b`) — DEFAULT hand-written path. Omitted only if the
  decision checkpoint selects a cubecl-matmul wrap.
- **Host API fns** (`crates/mlrs-backend/src/prims/gemm.rs`): `gemm(...)`.
- **Pool field/method** (`crates/mlrs-backend/src/pool.rs`): `PoolStats.read_backs`
  field + `BufferPool::record_read_back()`.
- **Capability probe** (`crates/mlrs-backend/src/capability.rs` is NOT modified;
  the probe lands in `spike_test.rs` as a Wave-0 probe + a thin facade fn if a
  symbol is confirmed): `supports_plane`/subgroup query (symbol pinned in Wave 0).
- **Error variants** (`crates/mlrs-core/src/error.rs`): a `PrimError` (or extended
  enum) with a shape-mismatch variant for `rows*cols != len` and incompatible GEMM
  dims (D-04). Naming is Claude's discretion.
- **New files**: `crates/mlrs-kernels/src/gemm.rs`,
  `crates/mlrs-backend/src/prims/mod.rs`, `crates/mlrs-backend/src/prims/gemm.rs`,
  `crates/mlrs-backend/tests/gemm_test.rs`, GEMM `.npz` fixtures.
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: GEMM substrate decision (Open Question 1)</name>
  <decision>Resolve the GEMM substrate: hand-write a tiled GEMM in mlrs-kernels (researcher DEFAULT) vs wrap a cubecl-0.10-compatible cubecl-matmul source.</decision>
  <context>
RESEARCH §"Summary" + Open Question 1: the workspace is firmly on `cubecl 0.10.0`
(builds today). `cubecl-matmul` has NO 0.10 release — latest is `0.9.0-pre.5`,
which pins `cubecl-core =0.9.0-pre.5` and cannot link against 0.10. The matmul
crate is absent from the `cubecl` repo at the `v0.10.0` tag and from `main`
(now 0.11.0-pre.1). ROADMAP Criterion 1 says GEMM "wraps cubecl-matmul" — that is
NOT satisfiable at cubecl 0.10 today. Re-verify the matmul version situation
before deciding (research valid-until ~2026-06-26).
  </context>
  <read_first>
    - .planning/phases/02-core-compute-primitives/02-RESEARCH.md (§Summary, §Standard Stack, §"Alternatives Considered", Open Question 1, Pitfall 1)
    - .planning/phases/02-core-compute-primitives/02-PATTERNS.md (§"No Analog Found" — gemm.rs existence is gated by this decision; Pattern 3 hand-written sketch)
  </read_first>
  <action>
Re-verify (`cargo search cubecl-matmul` / crates.io / tracel-ai git) whether any
cubecl-0.10-compatible matmul source now exists. Present the finding and these
two options to the developer.
  </action>
  <options>
    <option id="hand-write">
      <name>Hand-write tiled GEMM in mlrs-kernels/src/gemm.rs (researcher DEFAULT)</name>
      <pros>Zero new dependencies; no version coupling; naturally `<F: Float>`-generic + feature-free (D-13); transpose becomes comptime index arithmetic (cleaner D-06); F-typed accumulator sidesteps the matmul f64 f32-accumulation precision caveat (Pitfall 2/A4).</pros>
      <cons>More kernel code; must hit 1e-5 vs the host reference on its own (the host-ref oracle is the gate either way).</cons>
    </option>
    <option id="wrap-matmul">
      <name>Wrap a cubecl-0.10-compatible cubecl-matmul (only if a source is confirmed)</name>
      <pros>Matches ROADMAP wording literally; tiling/perf handled by the library; `swap_dims` zero-copy transpose.</pros>
      <cons>Requires locating a real 0.10-line source (uncertain — absent at v0.10.0 tag); f64 uses f32 accumulation registers (precision caveat on large K); adds an external dep + version-coupling risk.</cons>
    </option>
  </options>
  <verify>
    <automated>MISSING — decision task; no automated check. Downstream GEMM code tasks (Task 5/6) depend on the selected option being recorded in the SUMMARY.</automated>
  </verify>
  <acceptance_criteria>
    - The selected option (hand-write | wrap-matmul) is recorded in the plan SUMMARY.
    - If hand-write is chosen, the SUMMARY notes that ROADMAP/REQUIREMENTS wording ("wraps cubecl-matmul") needs an update (surface to orchestrator — do not silently diverge).
    - If wrap-matmul is chosen, the confirmed source (git org/repo/rev) is recorded for Task 5.
  </acceptance_criteria>
  <resume-signal>Select: hand-write or wrap-matmul (with confirmed source)</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Add PoolStats.read_backs counter + subgroup-capability probe (Wave-0 infra)</name>
  <files>crates/mlrs-backend/src/pool.rs, crates/mlrs-backend/src/device_array.rs, crates/mlrs-backend/tests/spike_test.rs</files>
  <read_first>
    - crates/mlrs-backend/src/pool.rs (the existing PoolStats struct + acquire/release counter-bump pattern; lines defining PoolStats and acquire)
    - crates/mlrs-backend/src/device_array.rs (to_host read-back path — instrument it to bump the counter)
    - crates/mlrs-backend/tests/spike_test.rs (spike_capability_query_reports_f64 — the analog to mirror for the subgroup probe)
    - crates/mlrs-backend/src/capability.rs (supports_type/supports_f64 facade shape to mirror)
    - .planning/phases/02-core-compute-primitives/02-RESEARCH.md (§"D-10 Memory-Gate Assertion Strategy" assertion 2; Open Question 3; A3 subgroup query)
    - .planning/phases/02-core-compute-primitives/02-PATTERNS.md (§pool.rs MODIFY section)
  </read_first>
  <action>
Add a `pub read_backs: u64` field to `PoolStats` (it derives `Copy + Default + Eq`
so no other change is needed). Add `BufferPool::record_read_back(&mut self)` that
does `self.stats.read_backs += 1`, mirroring the `acquire` counter-bump idiom.
Route `DeviceArray::to_host` through the pool so each read-back bumps the counter:
since `to_host` currently takes `&BufferPool` (immutable), change the memory-gate
read path so the counter is bumped — the cleanest hook (Claude's discretion) is
either (a) add a `DeviceArray::to_host_metered(&mut BufferPool)` that bumps then
delegates, or (b) make the gate test call `pool.record_read_back()` at each
terminal read. Prefer (a) so the counter is a real runtime assertion, not a
code-review claim (RESEARCH line 360). Keep `to_host` itself available for the
existing immutable call sites.

Then pin the subgroup-capability-query symbol for cubecl 0.10. Add a Wave-0 probe
test `spike_subgroup_query_reports_support` in `spike_test.rs` mirroring
`spike_capability_query_reports_f64`: query the active client's properties for
subgroup/plane support (the exact symbol is unverified — consult the CubeCL manual
and, on ANY build error, the cubecl_error_guideline.md per AGENTS.md §4). Log
`subgroup_supported={bool}`. If a stable property query exists, also add a thin
`capability::supports_plane()` facade fn mirroring `supports_f64`; if no symbol is
reachable, record in the SUMMARY that the plane-path skip (Plan 02) must instead
attempt-the-launch-and-skip-on-error, and have the probe assert only that the
query returns a value.
  </action>
  <verify>
    <automated>cargo test -p mlrs-backend --features cpu spike_subgroup_query_reports_support</automated>
  </verify>
  <acceptance_criteria>
    - `crates/mlrs-backend/src/pool.rs` contains `pub read_backs: u64` in PoolStats and a `record_read_back` method.
    - `grep -n 'read_backs' crates/mlrs-backend/src/pool.rs` shows the field and the bump.
    - `cargo test -p mlrs-backend --features cpu spike_subgroup_query_reports_support` exits 0.
    - The SUMMARY records the resolved subgroup-query symbol (or that the attempt-launch-and-skip fallback is required for Plan 02's plane path).
    - `cargo build -p mlrs-backend --features cpu` and `--features wgpu` both succeed (read_backs addition compiles on both).
  </acceptance_criteria>
  <done>PoolStats carries read_backs with a bump method wired to a metered read-back path; the subgroup-query symbol is pinned (or its fallback recorded); workspace compiles on cpu and wgpu.</done>
</task>

<task type="auto">
  <name>Task 3: Generate GEMM convention npz fixtures (D-12)</name>
  <files>scripts/gen_oracle.py, tests/fixtures/gemm_f32_seed42.npz, tests/fixtures/gemm_f64_seed42.npz</files>
  <read_first>
    - scripts/gen_oracle.py (the existing fixture-generation script — naming convention case_dtype_seed.npz, np.random.default_rng(seed), named arrays)
    - crates/mlrs-core/src/oracle.rs (load_npz → OracleCase; .expect_f32/.expect_f64/.shape — the loader the fixtures must satisfy; 4-/8-byte float arrays only)
    - .planning/phases/02-core-compute-primitives/02-PATTERNS.md (§fixtures/gen_oracle.py section — naming + contract)
    - .planning/phases/02-core-compute-primitives/02-RESEARCH.md (D-12; Environment Availability — regen needs /tmp venv, PEP 668)
    - $HOME/.claude/projects/-home-user-Documents-workspace-mlrs/memory/MEMORY.md (oracle-fixture-regen-needs-venv note)
  </read_first>
  <action>
Extend `scripts/gen_oracle.py` with a GEMM case: seeded `rng = np.random.default_rng(42)`,
random `A` (m×k) and `B` (k×n), store `A`, `B`, and the numpy reference product
`C = A @ B` under named arrays in `gemm_f32_seed42.npz` (float32) and
`gemm_f64_seed42.npz` (float64). Pick a small non-square shape (e.g. m=5, k=4, n=3)
so the fixture also exercises rectangular geometry. Regenerate the `.npz` blobs:
numpy is not importable system-wide (PEP 668), so create a /tmp venv
(`python3 -m venv /tmp/oraclevenv && /tmp/oraclevenv/bin/pip install numpy`) and
run the script with that interpreter. Commit the resulting `.npz` blobs into
`tests/fixtures/` (they are read with NO Python at test time).
Confirm the arrays load via the existing `load_npz` loader contract.
  </action>
  <verify>
    <automated>test -f tests/fixtures/gemm_f32_seed42.npz && test -f tests/fixtures/gemm_f64_seed42.npz</automated>
  </verify>
  <acceptance_criteria>
    - Both `gemm_f32_seed42.npz` and `gemm_f64_seed42.npz` exist under `tests/fixtures/`.
    - Each contains named arrays `A`, `B`, `C` with `C == A @ B` (numpy reference).
    - The f32 fixture holds 4-byte floats and the f64 fixture holds 8-byte floats (loader requirement).
    - `scripts/gen_oracle.py` contains the GEMM case and is re-runnable from a numpy venv.
  </acceptance_criteria>
  <done>Committed GEMM npz convention fixtures (f32+f64) loadable by mlrs-core's load_npz, regenerable via the documented /tmp venv path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Scaffold GEMM test + prims module + error variant (Wave-0 test scaffold)</name>
  <files>crates/mlrs-backend/tests/gemm_test.rs, crates/mlrs-backend/src/prims/mod.rs, crates/mlrs-backend/src/lib.rs, crates/mlrs-core/src/error.rs</files>
  <behavior>
    - gemm_test.rs declares (initially #[ignore] or compiling-against-stub) the test set: gemm_f32_matches_host_ref, gemm_f64_matches_host_ref (skip_f64_with_log gated), gemm_transpose_matches_host_ref, gemm_npz_fixture_matches.
    - A shape-mismatch error variant exists so `rows*cols != len` and incompatible GEMM dims are reportable (D-04).
  </behavior>
  <read_first>
    - crates/mlrs-backend/tests/pipeline_test.rs (the canonical oracle test header/imports + assert_slice_close usage + f64 gate)
    - crates/mlrs-backend/tests/spike_test.rs (launch_dims helper + launch idiom)
    - crates/mlrs-core/src/error.rs (BridgeError #[derive(Debug, Error)] + #[error("...")] one-variant-per-violation shape — the extension point)
    - crates/mlrs-backend/src/lib.rs (module list to add `pub mod prims;`)
    - .planning/phases/02-core-compute-primitives/02-PATTERNS.md (§gemm_test.rs section; §thiserror error variants; §prims/mod.rs)
  </read_first>
  <action>
Add `pub mod prims;` to `crates/mlrs-backend/src/lib.rs` and create
`crates/mlrs-backend/src/prims/mod.rs` as a barrel (`pub mod gemm;` — gemm.rs body
lands in Task 5). Add a primitive error type to `crates/mlrs-core/src/error.rs`
(naming Claude's discretion, e.g. `PrimError`) with a `#[derive(Debug, Error)]`
shape-mismatch variant carrying the offending `(rows, cols, len)` or
incompatible-dims context, following the existing `BridgeError` one-variant shape.
Create `crates/mlrs-backend/tests/gemm_test.rs` with the test header/imports from
`pipeline_test.rs` (`env_logger` init, `log_oracle_dtype`, `assert_slice_close`,
`F32_TOL`/`F64_TOL`, `skip_f64_with_log`) and the `launch_dims` helper from
`spike_test.rs`. Declare the four test fns named above; until Task 5 lands the
`gemm` host API they may be `#[ignore]`d or assert against a host-only reference,
but they MUST compile. The f64 test MUST start with
`if mlrs_backend::capability::skip_f64_with_log() { return; }`. NO in-source
`mod tests` anywhere (AGENTS.md §2).
  </action>
  <verify>
    <automated>cargo test -p mlrs-backend --features cpu gemm -- --list</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p mlrs-backend --features cpu gemm -- --list` lists gemm_f32_matches_host_ref, gemm_f64_matches_host_ref, gemm_transpose_matches_host_ref, gemm_npz_fixture_matches.
    - `crates/mlrs-core/src/error.rs` contains a primitive shape-mismatch error variant (`grep -n 'rows' crates/mlrs-core/src/error.rs` or the chosen variant name matches).
    - `crates/mlrs-backend/src/lib.rs` contains `pub mod prims;` and `crates/mlrs-backend/src/prims/mod.rs` exists.
    - `cargo build -p mlrs-backend --features cpu` exits 0.
  </acceptance_criteria>
  <done>Compiling GEMM test scaffold (4 named tests), prims module wired, and a primitive shape-mismatch error variant — the Wave-0 surface for PRIM-01.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Implement GEMM kernel + host API (transpose flags, pool-routed out/scratch)</name>
  <files>crates/mlrs-kernels/src/gemm.rs, crates/mlrs-kernels/src/lib.rs, crates/mlrs-backend/src/prims/gemm.rs</files>
  <behavior>
    - gemm_kernel computes C[i,j] = Σ_k A·B with comptime trans_a/trans_b choosing the index expression (A[i*k+kk] vs A[kk*m+i]; B[kk*n+j] vs B[j*k+kk]) — no transpose buffer.
    - gemm host API asserts rows*cols==len per operand (D-04), allocates/reuses scratch+out via the pool (D-11), launches over a 2D grid, returns a device-resident DeviceArray.
  </behavior>
  <read_first>
    - crates/mlrs-kernels/src/smoke.rs (the #[cube(launch)] generic-over-F shell + bounds-check idiom)
    - crates/mlrs-kernels/src/lib.rs (barrel: add `pub mod gemm; pub use gemm::gemm_kernel;`)
    - crates/mlrs-backend/tests/spike_test.rs (launch::<F, ActiveRuntime> idiom, ArrayArg::from_raw_parts(handle, n), handle.clone() before launch/read)
    - crates/mlrs-backend/src/device_array.rs (from_host/to_host/handle/len — the device-resident I/O + pool routing)
    - crates/mlrs-backend/src/pool.rs (acquire/release for scratch + caller out-buffer)
    - .planning/phases/02-core-compute-primitives/02-RESEARCH.md (Pattern 3 hand-written GEMM sketch lines ~224-251; §Per-Primitive §1 GEMM host API signature; Pitfall 6 scratch handling)
    - .planning/phases/02-core-compute-primitives/02-PATTERNS.md (§gemm.rs "No Analog Found"; §prims host orchestration — launch_dims 2D variant, host-API signature, shape-assert)
    - /home/user/Documents/workspace/cubecl_manual/manual/Cubecl/INDEX.md (read before writing the kernel; shared-memory + transpose manuals)
    - AGENTS.md (§3 read manual first; §4 cubecl_error_guideline.md on ANY build error)
  </read_first>
  <action>
DEFAULT (if Task 1 selected hand-write): create
`crates/mlrs-kernels/src/gemm.rs` with `gemm_kernel<F: Float + CubeElement>(a, b,
c, m, k, n, #[comptime] trans_a: bool, #[comptime] trans_b: bool)` per RESEARCH
Pattern 3 — map `(row, col)` onto `ABSOLUTE_POS_X/ABSOLUTE_POS_Y`, bounds-check
`row < m && col < n`, accumulate into an `F::from_int(0i64)` accumulator, and pick
the A/B index expression from the comptime flags (no transpose buffer, D-06).
Start with the simple (non-tiled) form; add a `SharedMemory`-tiled inner loop with
`sync_cube()` between stages only if the large-shape host-ref case breaches 1e-5
(numerical stability). Register it in `lib.rs` (`pub mod gemm; pub use
gemm::gemm_kernel;`).

If Task 1 selected wrap-matmul: omit `gemm.rs` and put the matmul wrap in
`prims/gemm.rs` against the confirmed 0.10-compatible source instead (re-pin the
real launch/`swap_dims`/`MatmulElems` signature — the cached 0.9-pre.5 surface is
indicative only, A1).

Either way, implement `crates/mlrs-backend/src/prims/gemm.rs`:
`gemm(pool: &mut BufferPool, a: &DeviceArray, (m,k), b: &DeviceArray, (k,n),
transa: bool, transb: bool, out: Option<DeviceArray>) -> DeviceArray`. Assert
`m*k == a.len()` and `k*n == b.len()` (the new shape-mismatch error variant, D-04).
Acquire the `m*n` output from `pool` when `out` is None, else reuse the supplied
out-buffer (D-11); draw any scratch from the pool. Launch `gemm_kernel` over a 2D
launch config (extend `launch_dims` to a 2D `(rows, cols)` variant), cloning
handles before launch (handles are consumed). Return a device-resident
`DeviceArray` — NO `to_host` inside the host API (D-05). NO fenced code in the
SUMMARY; the kernel body shape is in RESEARCH Pattern 3.
  </action>
  <verify>
    <automated>cargo build -p mlrs-kernels && cargo build -p mlrs-backend --features cpu</automated>
  </verify>
  <acceptance_criteria>
    - `crates/mlrs-kernels/src/gemm.rs` contains `pub fn gemm_kernel` (DEFAULT path) and `crates/mlrs-kernels/src/lib.rs` re-exports it; OR (wrap path) `prims/gemm.rs` wraps the confirmed matmul source.
    - `crates/mlrs-backend/src/prims/gemm.rs` contains `pub fn gemm` with the `(rows,cols)` shape asserts and `pool.acquire`/`pool.release` for out/scratch.
    - `mlrs-kernels` carries NO backend feature flag (grep its Cargo.toml — feature-free, D-13).
    - `cargo build -p mlrs-kernels` and `cargo build -p mlrs-backend --features cpu` both exit 0.
    - On any CubeCL build error, the resolution followed cubecl_error_guideline.md (noted in SUMMARY).
  </acceptance_criteria>
  <done>A backend-portable GEMM with comptime transpose flags and pool-routed out/scratch compiles on cpu; device-resident in/out (no host round-trip in the API).</done>
</task>

<task type="auto">
  <name>Task 6: Validate GEMM against host reference + npz fixture (f32/f64, cpu/wgpu, transpose)</name>
  <files>crates/mlrs-backend/tests/gemm_test.rs</files>
  <read_first>
    - crates/mlrs-backend/tests/gemm_test.rs (the scaffold from Task 4 — fill the four test bodies)
    - crates/mlrs-backend/tests/pipeline_test.rs (host-ref → device → read-back → assert_slice_close idiom; f32 near-zero floor precedent)
    - crates/mlrs-core/src/compare.rs (assert_slice_close abs+rel + near-zero guard)
    - crates/mlrs-core/src/oracle.rs (load_npz + expect_f32/expect_f64 for the GEMM fixture)
    - crates/mlrs-backend/src/prims/gemm.rs (the gemm API under test)
    - .planning/phases/02-core-compute-primitives/02-RESEARCH.md (§Per-Primitive §1 Validate; Pitfall 2 f64; Pitfall 3 large-N stability — compare f32 device output against an f64 host ref)
  </read_first>
  <action>
Fill the four `gemm_test.rs` bodies. `gemm_f32_matches_host_ref`: seeded host RNG
builds random A/B over several random shapes (incl. one large-K case for stability),
compute a triple-loop f64 host reference, run `gemm`, `to_host`, and
`assert_slice_close(&got, &expected, &F32_TOL)`. `gemm_f64_matches_host_ref`: same
but f64 device path, guarded by `if skip_f64_with_log() { return; }` and
`assert_slice_close(..&F64_TOL)`. `gemm_transpose_matches_host_ref`: assert
`gemm(transa=true)` and `gemm(transb=true)` equal the transposed-operand host
reference (the D-06 contract — no transpose buffer). `gemm_npz_fixture_matches`:
load `gemm_f32_seed42.npz` / `gemm_f64_seed42.npz`, run `gemm`, compare device `C`
to the fixture `C`. Each test logs the dtype/backend line via `log_oracle_dtype`.
Run on cpu and wgpu. If the f32 large-K case breaches the strict relative bound
near zero, reuse the `F32_ORACLE_NEAR_ZERO_FLOOR` local-helper precedent from
`pipeline_test.rs` rather than loosening `F32_TOL`.
  </action>
  <verify>
    <automated>cargo test -p mlrs-backend --features cpu gemm</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p mlrs-backend --features cpu gemm` exits 0 (all four tests pass or f64 is logged-skipped).
    - `cargo test -p mlrs-backend --features wgpu gemm` exits 0.
    - The transpose test proves transa/transb match the transposed host reference within tolerance.
    - The npz-fixture test passes for both f32 and f64 (f64 gated).
    - GEMM output matches the host reference within F32_TOL/F64_TOL (abs AND rel) including the large-K stability case.
  </acceptance_criteria>
  <done>GEMM (PRIM-01) validated: host-ref sweep + npz fixture + transpose flags, f32 always and f64 capability-gated, green on cpu and wgpu.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host slice → device buffer | host `&[F]` uploaded via `DeviceArray::from_host`; length is the validated source of truth (T-04-01) |
| caller `(rows, cols)` → kernel index math | caller-supplied geometry must satisfy `rows*cols == len` before any launch (D-04 input validation, ASVS V5) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-0201-01 | Tampering | `ArrayArg::from_raw_parts(handle, len)` in `gemm` launch | mitigate | `len` derives from validated `DeviceArray.len`; kernel bounds-checks `row<m && col<n` (carries the Phase 1 T-04-01 mitigation forward) |
| T-0201-02 | Tampering | caller `(rows, cols)` vs operand `len` (D-04) | mitigate | assert `m*k==a.len()` and `k*n==b.len()` at the `gemm` entry; reject via the new shape-mismatch error variant before any unsafe launch (ASVS V5) |
| T-0201-SC | Tampering | cargo/git dependency (cubecl-matmul wrap path only) | mitigate | The wrap path is gated behind the Task-1 `checkpoint:human-verify` decision; the DEFAULT hand-written path adds ZERO new dependencies. No `[ASSUMED]`/`[SUS]` package is installed without the checkpoint. |

No high-severity threats: numerical compute-kernel phase, no auth/session/network/PII surface (RESEARCH §Security Domain).
</threat_model>

<verification>
- `cargo test -p mlrs-backend --features cpu gemm` green; `--features wgpu gemm` green.
- `cargo build -p mlrs-kernels` (feature-free) and `cargo build -p mlrs-backend --features cpu` succeed.
- `PoolStats.read_backs` exists and compiles on cpu+wgpu; subgroup probe test green.
- GEMM npz fixtures committed and loadable.
</verification>

<success_criteria>
- PRIM-01: GEMM matches the host reference within tolerance for f32 and f64 on cpu and wgpu (ROADMAP Criterion 1).
- Transpose flags (D-06) proven without a materialized transpose buffer.
- Wave-0 hooks (`read_backs` counter, subgroup probe, GEMM fixtures) in place for Plans 02–05.
</success_criteria>

<output>
Create `.planning/phases/02-core-compute-primitives/02-01-SUMMARY.md` when done.
Record: the Task-1 decision (and whether ROADMAP/REQUIREMENTS wording needs the
"wraps cubecl-matmul" update), the resolved subgroup-query symbol or its fallback,
and any CubeCL build-error resolutions per cubecl_error_guideline.md.
</output>
