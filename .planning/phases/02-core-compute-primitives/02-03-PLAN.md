---
phase: 02-core-compute-primitives
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - crates/mlrs-kernels/src/elementwise.rs
  - crates/mlrs-kernels/src/lib.rs
  - crates/mlrs-backend/src/prims/mod.rs
  - crates/mlrs-backend/src/prims/distance.rs
  - tests/fixtures/dist_sq_f32_seed42.npz
  - tests/fixtures/dist_sq_f64_seed42.npz
  - tests/fixtures/dist_sqrt_f64_seed42.npz
  - crates/mlrs-backend/tests/distance_test.rs
autonomous: true
requirements: [PRIM-03]
user_setup: []

must_haves:
  truths:
    - "Pairwise squared-Euclidean distance via GEMM-expansion ‖x‖²+‖y‖²−2XYᵀ reuses GEMM (Plan 01) and the row-L2-norm reduction (Plan 02)"
    - "An unconditional max(d²,0) clamp produces NO negative distances under f32 (property test asserts min >= 0)"
    - "Squared distance is the core output; an optional sqrt at the boundary serves KNN's Euclidean distances"
    - "Distance matches the host reference within tolerance for f32 and f64 on cpu and wgpu"
    - "The whole distance pipeline stays device-resident (no host round-trip between GEMM, reduction, clamp, sqrt)"
  artifacts:
    - path: "crates/mlrs-kernels/src/elementwise.rs"
      provides: "clamp_nonneg, sqrt_elem, and a scale kernel (per-element map kernels)"
      contains: "clamp_nonneg"
    - path: "crates/mlrs-backend/src/prims/distance.rs"
      provides: "distance host API: GEMM-expansion + row-L2-norm + dist_combine_clamp + optional sqrt, device-resident"
      contains: "pub fn distance"
    - path: "crates/mlrs-backend/tests/distance_test.rs"
      provides: "host-ref sweep + min>=0 property + squared/sqrt npz fixtures, f32/f64, cpu/wgpu"
      contains: "distance_min_nonnegative"
  key_links:
    - from: "crates/mlrs-backend/src/prims/distance.rs"
      to: "crates/mlrs-backend/src/prims/gemm.rs"
      via: "gemm(transb=true) for XYᵀ"
      pattern: "gemm\\("
    - from: "crates/mlrs-backend/src/prims/distance.rs"
      to: "crates/mlrs-backend/src/prims/reduce.rs"
      via: "row-L2-norm reduction for ‖x_i‖²/‖y_j‖²"
      pattern: "reduce|l2|norm"
    - from: "crates/mlrs-backend/src/prims/distance.rs"
      to: "crates/mlrs-kernels/src/elementwise.rs"
      via: "dist_combine_clamp / clamp_nonneg launch"
      pattern: "clamp"
---

<objective>
Build the pairwise squared-Euclidean distance primitive (PRIM-03) via
GEMM-expansion `‖x‖²+‖y‖²−2XYᵀ`, composing the GEMM (Plan 01) and row-L2-norm
reduction (Plan 02), with an unconditional `max(d²,0)` clamp and an optional sqrt
at the boundary. One validated kernel serves KMeans, DBSCAN, and KNN.

Purpose: PRIM-03 — a distance primitive that produces no negative distances under
f32 cancellation (the reason the clamp exists, D-07) and matches the host
reference within tolerance. Output: `clamp_nonneg`/`sqrt_elem`/`scale` kernels,
the `distance` host API, distance npz fixtures, and `distance_test.rs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-compute-primitives/02-CONTEXT.md
@.planning/phases/02-core-compute-primitives/02-RESEARCH.md
@.planning/phases/02-core-compute-primitives/02-PATTERNS.md
@AGENTS.md
@.planning/phases/02-core-compute-primitives/02-01-SUMMARY.md
@.planning/phases/02-core-compute-primitives/02-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan (Plan 03) adds:
- **Kernel fns** (`crates/mlrs-kernels/src/elementwise.rs`): `clamp_nonneg`,
  `sqrt_elem`, `scale`, and `dist_combine_clamp` (the per-element combine that
  applies `‖x‖²+‖y‖²−2XYᵀ` then `max(d²,0)`). This is the shared elementwise file
  that Plan 04 (covariance) also consumes (the `scale` kernel).
- **Host API fn** (`crates/mlrs-backend/src/prims/distance.rs`): `distance(...)`
  returning squared distance, with an optional sqrt flag (D-08).
- **New file**: `crates/mlrs-backend/tests/distance_test.rs`, distance `.npz` fixtures.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Elementwise kernels (clamp_nonneg, sqrt_elem, scale, dist_combine_clamp)</name>
  <files>crates/mlrs-kernels/src/elementwise.rs, crates/mlrs-kernels/src/lib.rs</files>
  <behavior>
    - clamp_nonneg: out[i] = max(in[i], 0) using the statement form (if d < zero { d = zero }).
    - sqrt_elem: out[i] = sqrt(in[i]).
    - scale: out[i] = in[i] * factor (factor a scalar arg by value).
    - dist_combine_clamp: out[i,j] = max(xnorm[i] + ynorm[j] - 2*xy[i,j], 0).
  </behavior>
  <read_first>
    - crates/mlrs-kernels/src/smoke.rs (the per-element #[cube(launch)] ABSOLUTE_POS-indexed bounds-checked shell — clamp/sqrt/scale copy this exactly)
    - crates/mlrs-kernels/src/lib.rs (barrel — add `pub mod elementwise;` + re-exports)
    - .planning/phases/02-core-compute-primitives/02-RESEARCH.md (§Code Examples dist_combine_clamp lines ~395-411 — F::new(2.0), F::from_int(0i64), statement-form clamp)
    - .planning/phases/02-core-compute-primitives/02-PATTERNS.md (§elementwise.rs section — clamp statement form, NOT expression)
    - /home/user/Documents/workspace/cubecl_manual/manual/Cubecl/INDEX.md (Cubecl_conditionals.md for the clamp statement form)
    - AGENTS.md (§4 cubecl_error_guideline.md on ANY build error)
  </read_first>
  <action>
Create `crates/mlrs-kernels/src/elementwise.rs`. Implement four `#[cube(launch)]
pub fn ...<F: Float + CubeElement>` kernels following the `smoke.rs` per-element
shell with `ABSOLUTE_POS` bounds-checks: `clamp_nonneg(input, output)` using the
STATEMENT form (`let zero = F::from_int(0i64); if d < zero { d = zero; }` — NOT an
expression, per PATTERNS §elementwise.rs); `sqrt_elem(input, output)`;
`scale(input, output, factor)` (factor a scalar `F` arg passed by value, A6); and
`dist_combine_clamp(xy, xnorm, ynorm, out, rows, cols)` mapping `(i,j)` onto
`ABSOLUTE_POS_X/Y`, computing `xnorm[i] + ynorm[j] - F::new(2.0) * xy[idx]` then
the `max(d²,0)` statement clamp (RESEARCH §Code Examples). Register all in `lib.rs`.
  </action>
  <verify>
    <automated>cargo build -p mlrs-kernels</automated>
  </verify>
  <acceptance_criteria>
    - `crates/mlrs-kernels/src/elementwise.rs` contains `pub fn clamp_nonneg`, `pub fn sqrt_elem`, `pub fn scale`, and `pub fn dist_combine_clamp`.
    - The clamp uses the statement form (grep shows an `if` guard, not a `max` expression call that could mis-lower).
    - `crates/mlrs-kernels/src/lib.rs` re-exports the four kernels.
    - `cargo build -p mlrs-kernels` exits 0; crate stays feature-free (D-13).
  </acceptance_criteria>
  <done>Per-element clamp/sqrt/scale plus the dist_combine_clamp kernel compile feature-free, clamp in statement form.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Distance host API (GEMM-expansion + row-L2-norm + clamp + optional sqrt)</name>
  <files>crates/mlrs-backend/src/prims/distance.rs, crates/mlrs-backend/src/prims/mod.rs</files>
  <behavior>
    - distance computes XYᵀ via gemm(transb=true), ‖x_i‖²/‖y_j‖² via the row-L2-norm reduction, combines + clamps via dist_combine_clamp, all device-resident.
    - Squared distance is the core return; an optional sqrt flag applies sqrt_elem at the boundary (D-08).
    - No to_host between stages (D-05/D-10 gate 2).
  </behavior>
  <read_first>
    - crates/mlrs-backend/src/prims/gemm.rs (the gemm API — call with transb=true for XYᵀ)
    - crates/mlrs-backend/src/prims/reduce.rs (the row-L2-norm reduction fn — Plan 02's SUMMARY records its exact name)
    - crates/mlrs-kernels/src/elementwise.rs (dist_combine_clamp + sqrt_elem to launch)
    - crates/mlrs-backend/src/device_array.rs (DeviceArray in/out)
    - crates/mlrs-backend/src/pool.rs (acquire/release for scratch + out-buffer, D-11)
    - crates/mlrs-backend/src/prims/mod.rs (add `pub mod distance;`)
    - .planning/phases/02-core-compute-primitives/02-RESEARCH.md (§Per-Primitive §3 Distance; Pitfall 5 negatives; D-07/D-08)
    - .planning/phases/02-core-compute-primitives/02-PATTERNS.md (§distance composition — DeviceArray→DeviceArray no to_host between stages)
    - .planning/phases/02-core-compute-primitives/02-02-SUMMARY.md (row-L2-norm reduction fn name)
  </read_first>
  <action>
Add `pub mod distance;` to `prims/mod.rs` and create
`crates/mlrs-backend/src/prims/distance.rs`. Implement `distance(pool, x:
&DeviceArray, (rows_x, cols), y: &DeviceArray, (rows_y, cols), sqrt: bool, out:
Option<DeviceArray>) -> DeviceArray`: assert shapes (D-04); compute `XYᵀ` via
`gemm(..., transb=true)` (rows_x × rows_y); compute per-row `‖x_i‖²` and `‖y_j‖²`
via the Plan-02 row-L2-norm reduction; launch `dist_combine_clamp` to produce the
clamped squared distance into the pool-acquired (or caller-supplied, D-11) output;
if `sqrt`, launch `sqrt_elem` on that buffer in place. Keep every stage
`DeviceArray`→`DeviceArray` — NO `to_host` between stages (D-05/D-10 gate 2). Draw
all scratch from the pool so it is metered/recycled.
  </action>
  <verify>
    <automated>cargo build -p mlrs-backend --features cpu && cargo build -p mlrs-backend --features wgpu</automated>
  </verify>
  <acceptance_criteria>
    - `crates/mlrs-backend/src/prims/distance.rs` contains `pub fn distance` calling `gemm(...)` with a transpose flag and the row-L2-norm reduction.
    - The clamp is applied unconditionally; the sqrt is behind the `sqrt` flag (D-08).
    - No `to_host` appears in `distance.rs` (`grep -c to_host crates/mlrs-backend/src/prims/distance.rs` is 0).
    - `cargo build -p mlrs-backend --features cpu` and `--features wgpu` exit 0.
  </acceptance_criteria>
  <done>Device-resident distance composing GEMM + row-L2-norm + clamp + optional sqrt, no mid-pipeline host round-trip.</done>
</task>

<task type="auto">
  <name>Task 3: Validate distance (host-ref, min>=0 property, squared/sqrt fixtures)</name>
  <files>crates/mlrs-backend/tests/distance_test.rs, tests/fixtures/dist_sq_f32_seed42.npz, tests/fixtures/dist_sq_f64_seed42.npz, tests/fixtures/dist_sqrt_f64_seed42.npz, scripts/gen_oracle.py</files>
  <read_first>
    - crates/mlrs-backend/tests/pipeline_test.rs (oracle test header/imports, assert_slice_close, f64 gate, f32 near-zero floor precedent)
    - crates/mlrs-backend/src/prims/distance.rs (the API under test)
    - crates/mlrs-core/src/compare.rs (assert_slice_close)
    - crates/mlrs-core/src/oracle.rs (load_npz for the distance fixtures)
    - scripts/gen_oracle.py (extend for dist squared + sqrt cases; /tmp numpy venv, PEP 668)
    - .planning/phases/02-core-compute-primitives/02-RESEARCH.md (§Per-Primitive §3 Validate; Pitfall 5 min>=0; D-12 distance squared-vs-sqrt fixtures)
    - .planning/phases/02-core-compute-primitives/02-PATTERNS.md (§distance_test.rs property test)
  </read_first>
  <action>
Create `crates/mlrs-backend/tests/distance_test.rs`. Extend `gen_oracle.py` to emit
`dist_sq_f32_seed42.npz`/`dist_sq_f64_seed42.npz` (squared pairwise distance
reference) and `dist_sqrt_f64_seed42.npz` (the sqrt variant), regenerate via the
/tmp numpy venv, commit the blobs. Write `distance_matches_host_ref`: random
X/Y over several shapes, direct host distance loop (f64), run `distance(sqrt=false)`,
`assert_slice_close(&F32_TOL/&F64_TOL)`. Write `distance_min_nonnegative`: include a
deliberate near-identical-rows f32 case (catastrophic cancellation) and assert
`min(result) >= 0` (Criterion 3 / Pitfall 5). Write `distance_sqrt_matches_host_ref`:
`distance(sqrt=true)` vs the sqrt host reference + the sqrt fixture. Write
`distance_npz_fixture_matches` against the squared fixtures. f64 arms guarded by
`skip_f64_with_log`; each test logs the dtype/backend line; run on cpu and wgpu. If
the f32 near-zero case spuriously fails the strict relative bound, reuse the
`F32_ORACLE_NEAR_ZERO_FLOOR` precedent from `pipeline_test.rs` (do not loosen F32_TOL).
  </action>
  <verify>
    <automated>cargo test -p mlrs-backend --features cpu distance</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p mlrs-backend --features cpu distance` and `--features wgpu distance` exit 0.
    - `distance_min_nonnegative` asserts `min(result) >= 0` over a deliberate f32 cancellation case (no negative distances).
    - Squared and sqrt outputs both match the host reference + npz fixtures within tolerance.
    - `dist_sq_f32_seed42.npz`, `dist_sq_f64_seed42.npz`, `dist_sqrt_f64_seed42.npz` exist under `tests/fixtures/`.
  </acceptance_criteria>
  <done>PRIM-03 validated: GEMM-expansion distance with no negative results, squared + sqrt outputs matching host ref + fixtures, green on cpu and wgpu.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host slice → device buffer | X/Y uploaded via `DeviceArray::from_host`; `len` is the validated read-back source of truth (T-04-01) |
| caller `(rows, cols)` → GEMM/clamp index math | distance derives geometry from caller shapes; must satisfy `rows*cols == len` (D-04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-0203-01 | Tampering | `ArrayArg::from_raw_parts(handle, len)` in the distance launches | mitigate | `len` from validated `DeviceArray.len`; kernels bounds-check `i<rows && j<cols` (T-04-01 carried forward) |
| T-0203-02 | Tampering | caller `(rows, cols)` vs operand `len` | mitigate | assert shapes at the `distance` entry via the shape-mismatch error variant before launch (D-04, ASVS V5) |
| T-0203-03 | Information Disclosure | sqrt-of-negative → NaN leaking to KNN (Pitfall 5) | mitigate | Unconditional `max(d²,0)` clamp before the optional sqrt; property test asserts `min >= 0` |
| T-0203-SC | Tampering | cargo dependency | mitigate | Zero new dependencies — composition of in-tree primitives + small `#[cube]` kernels |

No high-severity threats: numerical compute-kernel phase, no auth/session/network/PII surface.
</threat_model>

<verification>
- `cargo test -p mlrs-backend --features cpu distance` and `--features wgpu distance` green.
- `distance_min_nonnegative` proves the clamp; squared + sqrt fixtures pass.
- No `to_host` in `distance.rs` (device-resident composition).
</verification>

<success_criteria>
- PRIM-03 / ROADMAP Criterion 3: pairwise squared-Euclidean distance with `max(d²,0)` clamp produces no negative distances under f32 and matches the host reference within tolerance.
- Optional sqrt boundary serves KNN (D-08); reuses GEMM + row-L2-norm (D-07).
</success_criteria>

<output>
Create `.planning/phases/02-core-compute-primitives/02-03-SUMMARY.md` when done.
Record: the `scale` kernel signature (Plan 04 covariance consumes it from
elementwise.rs), and any CubeCL build-error resolutions.
</output>
