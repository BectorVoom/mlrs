---
phase: 16-builder-retrofit-sweep-shim-coverage
plan: 05
type: execute
wave: 5
depends_on: ["16-04"]
files_modified:
  - crates/mlrs-algos/src/cluster/dbscan.rs
  - crates/mlrs-algos/src/cluster/spectral_clustering.rs
  - crates/mlrs-algos/src/cluster/spectral_embedding.rs
  - crates/mlrs-algos/tests/dbscan_test.rs
  - crates/mlrs-algos/tests/spectral_clustering_test.rs
  - crates/mlrs-algos/tests/spectral_embedding_test.rs
  - crates/mlrs-py/src/estimators/cluster.rs
  - crates/mlrs-py/src/estimators/spectral.rs
autonomous: true
requirements: [BLDR-03]
must_haves:
  truths:
    - "DBSCAN, SpectralClustering construct only via zero-arg new() + builder; arg-new removed"
    - "SpectralEmbedding (shape A', inherent fit, never on a trait) ADOPTS typestate Fit/Transform"
    - "SpectralClustering's 6-arg new (incl. affinity: String, Option<usize>) is fully folded into the builder"
    - "Each estimator's oracle suite passes unchanged (exact-label / 1e-5, D-03)"
    - "cargo build -p mlrs-py --features cpu succeeds after each PyO3 arm migrates"
  artifacts:
    - path: "crates/mlrs-algos/src/cluster/spectral_clustering.rs"
      provides: "SpectralClustering<F, S=Unfit> + builder subsuming the 6-arg new"
      contains: "SpectralClustering<F, S"
    - path: "crates/mlrs-algos/src/cluster/spectral_embedding.rs"
      provides: "SpectralEmbedding<F, S=Unfit> adopting typestate Fit/Transform (was inherent)"
      contains: "SpectralEmbedding<F, S"
  key_links:
    - from: "crates/mlrs-algos/src/cluster/spectral_embedding.rs"
      to: "crates/mlrs-algos/src/typestate.rs"
      via: "newly ADOPTS impl typestate::Fit for SpectralEmbedding<F, Unfit> + Transform on Fitted (was inherent fit, no trait)"
      pattern: "typestate::(Fit|Transform)"
---

<objective>
Bulk sweep — cluster/ (D-06), the non-KMeans members. DBSCAN is shape A (Fit + PredictLabels). SpectralClustering is the wide 6-arg `new` case (affinity: String, Option<usize>, gamma, n_neighbors, seed) → builder must absorb String/Option/enum setters. SpectralEmbedding is shape A' — it has an INHERENT `fit`/`transform` and was NEVER on `crate::traits`; the retrofit ADOPTS `typestate::Fit`/`Transform` so the single-surface end-state holds (RESEARCH Open Q3, D-01).

KMeans is deliberately NOT here — it is the late multi-constructor stress case (Plan 06) once the wide-builder recipe is proven on SpectralClustering.

Purpose: Migrate cluster/ minus KMeans; prove the wide-builder (String/Option/enum setters) and the shape-A' trait-adoption paths.
Output: DBSCAN, SpectralClustering, SpectralEmbedding on the typestate surface.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-builder-retrofit-sweep-shim-coverage/16-CONTEXT.md
@.planning/phases/16-builder-retrofit-sweep-shim-coverage/16-RESEARCH.md
@.planning/phases/16-builder-retrofit-sweep-shim-coverage/16-PATTERNS.md
@.planning/phases/16-builder-retrofit-sweep-shim-coverage/16-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
- `Dbscan<F, S=Unfit>` + `DbscanBuilder` (`.eps(f64).min_samples(usize)`) + Fitted Fit/PredictLabels
- `SpectralClustering<F, S=Unfit>` + builder (`.n_clusters(usize).n_components(Option<usize>).affinity(String/enum).gamma(f64).n_neighbors(usize).seed(u64)`) + Fitted Fit
- `SpectralEmbedding<F, S=Unfit>` + builder + newly-ADOPTED typestate Fit + Transform on Fitted
- `AnyDbscan`/`AnySpectralClustering`/`AnySpectralEmbedding` → `any_estimator_typestate!`
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate DBSCAN (shape A; Fit + PredictLabels)</name>
  <files>crates/mlrs-algos/src/cluster/dbscan.rs, crates/mlrs-algos/tests/dbscan_test.rs, crates/mlrs-py/src/estimators/cluster.rs</files>
  <read_first>
    - crates/mlrs-algos/src/cluster/dbscan.rs (full — struct, new(eps: f64, min_samples: usize), Fit + PredictLabels, fit body + geometry guard)
    - crates/mlrs-algos/src/linear/ridge.rs (migrated Shape-A reference)
    - crates/mlrs-algos/src/typestate.rs (Fit/PredictLabels/Fitted/Unfit/validate_geometry)
    - crates/mlrs-algos/tests/dbscan_test.rs (~8 call sites — the most; exact-label oracle)
    - crates/mlrs-py/src/estimators/cluster.rs (PyDBSCAN — note: DBSCAN has no predict; AnyDbscan + import)
    - .planning/phases/16-builder-retrofit-sweep-shim-coverage/16-PATTERNS.md (§1, §4; §6 note "DBSCAN no predict")
  </read_first>
  <behavior>
    - Dbscan::<F, Unfit>::new() yields sklearn defaults (eps=0.5, min_samples=5 — match current new()); builder `.eps(f64).min_samples(usize)`; new() == builder().build()?.
    - On Fitted: predict_labels (labels_, noise=-1); exact-label oracle passes unchanged. DBSCAN exposes no predict (fit-predict-only).
  </behavior>
  <action>
    Apply Shape-A recipe. DbscanBuilder `.eps(f64).min_samples(usize)`, defaults from current new(). Swap `use crate::traits::{Fit, PredictLabels}` → typestate. Consuming-self `Fit::fit` (validate_geometry, BYTE-IDENTICAL clustering math — exact-label gate is sensitive, copy every compute line). Move `impl PredictLabels` + labels_ accessor onto `impl<F> Dbscan<F, Fitted>`. Migrate the ~8 dbscan_test call sites + PyDBSCAN arm; `AnyDbscan` → `any_estimator_typestate!`. hyperparams_eq case. Commit.
  </action>
  <verify>
    <automated>cargo test --features cpu --test dbscan_test && cargo build -p mlrs-py --features cpu</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --features cpu --test dbscan_test` passes (EXACT-label, zero fit-body deltas).
    - `cargo build -p mlrs-py --features cpu` succeeds.
    - `! grep -q 'crate::traits' crates/mlrs-algos/src/cluster/dbscan.rs`.
    - `AnyDbscan` uses `any_estimator_typestate!`.
  </acceptance_criteria>
  <done>DBSCAN migrated, exact-label oracle green, mlrs-py builds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate SpectralClustering (shape A, wide 6-arg new → builder)</name>
  <files>crates/mlrs-algos/src/cluster/spectral_clustering.rs, crates/mlrs-algos/tests/spectral_clustering_test.rs, crates/mlrs-py/src/estimators/spectral.rs</files>
  <read_first>
    - crates/mlrs-algos/src/cluster/spectral_clustering.rs (full — struct, new(n_clusters, n_components: Option<usize>, affinity: String, gamma: F, n_neighbors, seed) + the 1 internal self-construction call site, Fit, fit body)
    - crates/mlrs-algos/src/linear/lasso.rs (multi-setter builder reference)
    - crates/mlrs-algos/src/typestate.rs (Fit/PredictLabels/Fitted/Unfit)
    - crates/mlrs-algos/tests/spectral_clustering_test.rs (~5 call sites)
    - crates/mlrs-py/src/estimators/spectral.rs (PySpectralClustering + Any* + import; note the embedding wrap is here too)
    - .planning/phases/16-builder-retrofit-sweep-shim-coverage/16-PATTERNS.md (§1 + the "SpectralClustering 6-arg, String/Option setter" note in RESEARCH §Call-Site)
  </read_first>
  <behavior>
    - SpectralClustering::<F, Unfit>::new() yields sklearn defaults (read current new() defaults: n_clusters=8, affinity="rbf", gamma=1.0, n_neighbors=10, seed default — match current exactly); builder exposes all 6 with String/Option/numeric setters; new() == builder().build()?.
    - Oracle (exact-label / 1e-5) passes unchanged.
  </behavior>
  <action>
    Apply Shape-A recipe with a WIDE builder (the affinity: String and n_components: Option<usize> setters are the new shapes — the builder setter takes `String`/`Option<usize>` directly; gamma is `f64` per A5). SpectralClusteringBuilder subsumes the full 6-arg new; defaults from current new(). Swap `use crate::traits::{Fit}` → typestate; consuming-self Fit (validate_geometry, BYTE-IDENTICAL math); move accessors + PredictLabels (if present) onto Fitted. Update the 1 internal self-construction site (spectral_clustering.rs) to the builder. Migrate ~5 test call sites + PySpectralClustering arm; `AnySpectralClustering` → `any_estimator_typestate!`. hyperparams_eq case. Commit.
  </action>
  <verify>
    <automated>cargo test --features cpu --test spectral_clustering_test && cargo build -p mlrs-py --features cpu</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --features cpu --test spectral_clustering_test` passes (zero fit-body deltas).
    - `cargo build -p mlrs-py --features cpu` succeeds.
    - `! grep -q 'crate::traits' crates/mlrs-algos/src/cluster/spectral_clustering.rs`.
    - The builder exposes affinity (String) and n_components (Option) setters; the internal self-construction site uses the builder.
  </acceptance_criteria>
  <done>SpectralClustering migrated with the wide builder, oracle green, mlrs-py builds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Migrate SpectralEmbedding (shape A'; ADOPT typestate Fit/Transform)</name>
  <files>crates/mlrs-algos/src/cluster/spectral_embedding.rs, crates/mlrs-algos/tests/spectral_embedding_test.rs, crates/mlrs-py/src/estimators/spectral.rs</files>
  <read_first>
    - crates/mlrs-algos/src/cluster/spectral_embedding.rs (full — struct SpectralEmbedding<F>, arg-taking new, INHERENT fit/transform with NO crate::traits import; accessors)
    - crates/mlrs-algos/src/cluster/spectral_clustering.rs (the JUST-migrated sibling) + crates/mlrs-algos/src/decomposition/pca.rs (Transform-on-Fitted reference)
    - crates/mlrs-algos/src/typestate.rs (Fit/Transform/Fitted/Unfit/validate_geometry)
    - crates/mlrs-algos/tests/spectral_embedding_test.rs
    - crates/mlrs-py/src/estimators/spectral.rs (PySpectralEmbedding at :65 + Any* + import)
    - .planning/phases/16-builder-retrofit-sweep-shim-coverage/16-PATTERNS.md (§1 "Shape A' subset" note at :111; RESEARCH Open Q3 — adopt typestate::Fit for the single surface)
  </read_first>
  <behavior>
    - SpectralEmbedding::<F, Unfit>::new() yields sklearn defaults (match current new()); builder exposes its params; new() == builder().build()?.
    - It NEWLY adopts typestate Fit (consuming-self) + Transform on Fitted (its inherent fit/transform become trait impls). Oracle passes unchanged.
  </behavior>
  <action>
    Apply the Shape-A' recipe (full build-out PLUS trait adoption): add `<F, S = Unfit>` + PhantomData + builder + zero-arg new(); ADD `use crate::typestate::{validate_geometry, Fit, Transform, Fitted, Unfit}` (it had no trait import before). Convert the INHERENT `fit` into `impl typestate::Fit for SpectralEmbedding<F, Unfit>` (consuming-self, type Fitted = Fitted variant, validate_geometry, BYTE-IDENTICAL math) and the inherent `transform`/accessors into `impl typestate::Transform`/accessors on `impl<F> SpectralEmbedding<F, Fitted>`. This brings it onto the single trait surface so the traits.rs-gone grep (Plan 11) stays clean and the convention is uniform. Migrate test call sites + PySpectralEmbedding arm; `AnySpectralEmbedding` → `any_estimator_typestate!`. hyperparams_eq case. Commit.
  </action>
  <verify>
    <automated>cargo test --features cpu --test spectral_embedding_test && cargo build -p mlrs-py --features cpu</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --features cpu --test spectral_embedding_test` passes (1e-5, zero fit-body deltas).
    - `cargo build -p mlrs-py --features cpu` succeeds.
    - `grep -cE 'typestate::(Fit|Transform)|impl.*Fit.*SpectralEmbedding|impl.*Transform.*Fitted' crates/mlrs-algos/src/cluster/spectral_embedding.rs` > 0 (now ON the trait surface, was inherent).
    - `AnySpectralEmbedding` uses `any_estimator_typestate!`.
  </acceptance_criteria>
  <done>SpectralEmbedding migrated and newly adopting typestate Fit/Transform, oracle green, mlrs-py builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → build() | eps/min_samples/affinity/gamma hyperparameter (data-independent) |
| host x geometry → fit | shapes reach validate_geometry before device launch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-V5 | Tampering / DoS | cluster fit geometry guard | mitigate | `validate_geometry` stays atop each ported fit; data-independent validation relocates to build() → BuildError, not dropped |
| T-16-GUARDF64 | DoS | PyDBSCAN/PySpectralClustering/PySpectralEmbedding F64 arms | mitigate | `guard_f64()` + `lock_pool()` preserved in migrated PyO3 fits |
| T-16-ARM | Tampering | Any* Fitted arms | mitigate | `any_estimator_typestate!` arms are `T<f*, Fitted>` |
</threat_model>

<verification>
- All three oracle suites green (exact-label / 1e-5).
- `cargo build -p mlrs-py --features cpu` green.
- No cluster/ (non-KMeans) file references `crate::traits`; SpectralEmbedding now on the trait surface.
</verification>

<success_criteria>
- DBSCAN, SpectralClustering, SpectralEmbedding migrated; wide-builder + shape-A' adoption proven (clears the path for KMeans in Plan 06).
</success_criteria>

<output>
Create `.planning/phases/16-builder-retrofit-sweep-shim-coverage/16-05-SUMMARY.md` when done. Record the String/Option setter pattern for KMeans's init() setter in Plan 06.
</output>
