---
phase: 16-builder-retrofit-sweep-shim-coverage
plan: 11
type: execute
wave: 11
depends_on: ["16-10"]
files_modified:
  - crates/mlrs-py/python/mlrs/linear.py
  - crates/mlrs-py/python/mlrs/naive_bayes.py
  - crates/mlrs-py/python/mlrs/kernel_ridge.py
  - crates/mlrs-py/python/mlrs/density.py
  - crates/mlrs-py/python/mlrs/cluster.py
  - crates/mlrs-py/python/mlrs/manifold.py
  - crates/mlrs-py/python/mlrs/__init__.py
  - crates/mlrs-py/python/tests/test_shims.py
  - crates/mlrs-py/python/tests/test_params.py
  - crates/mlrs-py/python/tests/test_estimator_checks.py
autonomous: true
requirements: [SHIM-01, SHIM-03]
must_haves:
  truths:
    - "Every PyO3-wrapped estimator has a faithful pure-Python MlrsBase shim class (the 15 missing ones added, incl. mlrs.UMAP / mlrs.HDBSCAN)"
    - "Each new shim __init__ stores every ctor arg verbatim under the same sklearn-named name with NO validation/computation, defaulting ALL args (zero-arg constructible)"
    - "get_params(deep=True)/set_params(**kw) round-trip exactly and clone() is equivalent for every new class"
    - "The static test matrix (test_shims/test_params/test_estimator_checks) covers the full shim set, and the AST-purity test passes for all of them"
    - "The fit-free estimator_checks subset (check_no_attributes_set_in_init, check_parameters_default_constructible, check_get_params_invariance) runs green for every new class"
  artifacts:
    - path: "crates/mlrs-py/python/mlrs/manifold.py"
      provides: "mlrs.UMAP shim (TransformerMixin; sklearn-named params; transform/fit_transform forwarders)"
      contains: "class UMAP"
    - path: "crates/mlrs-py/python/mlrs/cluster.py"
      provides: "mlrs.HDBSCAN shim (ClusterMixin; sklearn-named params; labels_/fit_predict)"
      contains: "class HDBSCAN"
  key_links:
    - from: "crates/mlrs-py/python/tests/test_params.py"
      to: "crates/mlrs-py/python/mlrs/__init__.py"
      via: "the test matrix iterates the full exported shim set (no longer hard-coded ALL_12)"
      pattern: "EXPECTED_PARAMS|ALL_"
---

<objective>
SHIM-01 + SHIM-03 — complete the pure-Python sklearn shim. Add a faithful `MlrsBase` subclass for every PyO3-wrapped estimator that lacks one (RESEARCH Open Q1 resolution: shim every wrapped estimator — the 15 missing classes: LinearSVC, LinearSVR, MBSGDClassifier, MBSGDRegressor, the 5 NB, KernelRidge, KernelDensity, SpectralClustering, SpectralEmbedding, UMAP, HDBSCAN). Then expand the static test matrix to the full set and exercise the (Plan-00-created) AST-purity test over all of them.

`get_params`/`set_params`/`clone` come FREE from `BaseEstimator` given a faithful (pure, zero-validation) `__init__` — the machinery in `base.py` (`_normalize`, `_ext`, `_suffixed`, `_post_fit`, `__sklearn_tags__`) is already complete.

Purpose: Bring the Python API to full estimator coverage with a static gate that proves __init__ purity, param round-trip, and the fit-free sklearn estimator_checks subset — the maximum verifiable without the FFI host (SHIM-03; the live check_estimator run stays deferred to UAT).
Output: 15 new shim classes + full test-matrix expansion.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-builder-retrofit-sweep-shim-coverage/16-CONTEXT.md
@.planning/phases/16-builder-retrofit-sweep-shim-coverage/16-RESEARCH.md
@.planning/phases/16-builder-retrofit-sweep-shim-coverage/16-PATTERNS.md
@.planning/phases/16-builder-retrofit-sweep-shim-coverage/16-00-SUMMARY.md
@.planning/phases/16-builder-retrofit-sweep-shim-coverage/16-10-SUMMARY.md
</context>

<artifacts_this_phase_produces>
- New pure-Python shim classes (15): `LinearSVC`, `LinearSVR`, `MBSGDClassifier`, `MBSGDRegressor` (linear.py); `GaussianNB`, `MultinomialNB`, `BernoulliNB`, `ComplementNB`, `CategoricalNB` (naive_bayes.py); `KernelRidge` (kernel_ridge.py); `KernelDensity` (density.py); `SpectralEmbedding` + `SpectralClustering` + `HDBSCAN` (cluster.py); `UMAP` (manifold.py)
- New shim modules where needed (naive_bayes.py, kernel_ridge.py, density.py, manifold.py) registered in `mlrs/__init__.py`
- Expanded `EXPECTED_PARAMS`/`SET_PARAM`/`ALL_*` matrices in test_shims.py/test_params.py/test_estimator_checks.py covering the full set incl. UMAP/HDBSCAN
- AST-purity test (from Plan 00) now exercised over the full matrix
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add the 13 family shim classes (SVM/MBSGD, NB, KernelRidge, KernelDensity, SpectralClustering, SpectralEmbedding)</name>
  <files>crates/mlrs-py/python/mlrs/linear.py, crates/mlrs-py/python/mlrs/naive_bayes.py, crates/mlrs-py/python/mlrs/kernel_ridge.py, crates/mlrs-py/python/mlrs/density.py, crates/mlrs-py/python/mlrs/cluster.py, crates/mlrs-py/python/mlrs/__init__.py</files>
  <read_first>
    - crates/mlrs-py/python/mlrs/linear.py (Ridge shim at :57-88 — the faithful __init__/fit/_suffixed template; LogisticRegression C-mapping at :202)
    - crates/mlrs-py/python/mlrs/cluster.py (DBSCAN at :72-98, KMeans random_state to seed at :36 — labels-only / ClusterMixin reference)
    - crates/mlrs-py/python/mlrs/base.py (MlrsBase machinery — _normalize, _ext, _suffix/_suffixed at :119-143, _post_fit at :147-158, __sklearn_tags__ at :171-182; mixin composition)
    - crates/mlrs-py/python/mlrs/__init__.py (the export list — new classes must be registered)
    - crates/mlrs-py/src/estimators/naive_bayes.rs, kernel.rs, spectral.rs (the PyO3 new() signatures + fitted-attr getters the shims forward to — read each estimator's exact param names/defaults)
    - .planning/phases/16-builder-retrofit-sweep-shim-coverage/16-PATTERNS.md (§6 — the shim template + the per-class mixin/param mapping; Pitfall 6 zero-arg constructibility)
  </read_first>
  <action>
    Create faithful `MlrsBase` shim classes for the 13 non-UMAP/HDBSCAN missing estimators, each in its family module: LinearSVC/LinearSVR/MBSGDClassifier/MBSGDRegressor in linear.py; the 5 NB in a new naive_bayes.py; KernelRidge in a new kernel_ridge.py; KernelDensity in a new density.py; SpectralEmbedding + SpectralClustering in cluster.py. For each: subclass the correct sklearn mixin (RegressorMixin for LinearSVR/MBSGDRegressor/KernelRidge; ClassifierMixin for LinearSVC/MBSGDClassifier/the 5 NB; ClusterMixin for SpectralClustering; TransformerMixin for SpectralEmbedding; KernelDensity per its score_samples surface) + `MlrsBase`. Write a PURE `__init__(self, <sklearn-named args with sklearn defaults>, output_type=...)` storing each arg verbatim under the same name (NO validation/computation — the AST test from Plan 00 enforces this), with ALL args defaulted (Pitfall 6 — zero-arg constructible). Read the exact param names + defaults from each PyO3 new() signature (do not invent; honor sklearn-named boundaries like capital `C`, `random_state` mapped to Rust seed). Write a `fit` that normalizes input, calls `self._ext().<Name>(...)`, stores `self._mlrs_obj`, calls `self._post_fit(cols)`, returns `self`; add the dtype-suffixed fitted-attr accessors via `self._suffixed(...)` and the family-appropriate predict/transform/score_samples forwarders. Register all 13 in `mlrs/__init__.py`. Do NOT add UMAP/HDBSCAN here (Task 2). Do NOT yet touch the test matrices (Task 3).
  </action>
  <verify>
    <automated>cd crates/mlrs-py/python && python -m pytest tests/test_shims.py -q -k "LinearSVC or KernelRidge or GaussianNB"</automated>
  </verify>
  <acceptance_criteria>
    - All 13 classes import from `mlrs` without the compiled `_mlrs` extension and construct with ZERO required args (`Cls()` succeeds — Pitfall 6).
    - `grep -cE 'class (LinearSVC|LinearSVR|MBSGDClassifier|MBSGDRegressor|GaussianNB|MultinomialNB|BernoulliNB|ComplementNB|CategoricalNB|KernelRidge|KernelDensity|SpectralClustering|SpectralEmbedding)' crates/mlrs-py/python/mlrs/*.py` returns 13.
    - Each new `__init__` body is pure (`self.x = x` only) — confirmed by the Plan-00 AST test once it covers them (Task 3); no validation/compute node present.
    - All 13 are exported from `mlrs/__init__.py`.
  </acceptance_criteria>
  <done>13 family shim classes added, zero-arg constructible, exported, importable without the compiled extension.</done>
</task>

<task type="auto">
  <name>Task 2: Add mlrs.UMAP and mlrs.HDBSCAN shim classes (SHIM-01 pair)</name>
  <files>crates/mlrs-py/python/mlrs/manifold.py, crates/mlrs-py/python/mlrs/cluster.py, crates/mlrs-py/python/mlrs/__init__.py</files>
  <read_first>
    - crates/mlrs-py/python/mlrs/linear.py (Ridge template) + crates/mlrs-py/python/mlrs/cluster.py (DBSCAN/KMeans labels-only template)
    - crates/mlrs-py/python/mlrs/base.py (MlrsBase machinery)
    - crates/mlrs-py/src/estimators/manifold.rs (PyUMAP new() signature at :142-148 — the exact umap-learn-named params + defaults; the transform/fit_transform methods added in Plan 10)
    - crates/mlrs-py/src/estimators/cluster.rs (PyHDBSCAN new() at :375-379 — sklearn-named params + defaults; the fit_predict/probabilities_/outlier_scores_ methods added in Plan 10)
    - .planning/phases/16-builder-retrofit-sweep-shim-coverage/16-PATTERNS.md (§6 — the explicit UMAP/HDBSCAN param lists at :276-277)
    - .planning/phases/16-builder-retrofit-sweep-shim-coverage/16-10-SUMMARY.md (the exact Plan-10 method names to forward to)
  </read_first>
  <action>
    Add `class UMAP(TransformerMixin, MlrsBase)` to a new manifold.py and `class HDBSCAN(ClusterMixin, MlrsBase)` to cluster.py. UMAP `__init__` defaults (match PyUMAP new() at manifold.rs:142-148, sklearn/umap-learn-named): `n_neighbors=15, n_components=2, min_dist=0.1, spread=1.0, metric="euclidean", n_epochs=None, init="spectral", random_state=None, learning_rate=1.0, set_op_mix_ratio=1.0, local_connectivity=1.0, repulsion_strength=1.0, negative_sample_rate=5, a=None, b=None` (+ output_type) — store each verbatim, pure body. HDBSCAN `__init__` defaults (match PyHDBSCAN new() at cluster.rs:375-379): `min_cluster_size=5, min_samples=None, cluster_selection_epsilon=0.0, cluster_selection_method="eom", metric="euclidean", alpha=1.0, max_cluster_size=0` (+ output_type) — pure body. UMAP: `fit` + `transform` + `fit_transform` forwarders (forward to the Plan-10 PyUMAP methods); `embedding_` accessor. HDBSCAN: `fit` (ClusterMixin gives fit_predict; or forward to PyHDBSCAN.fit_predict) + `labels_` property (like cluster.py:62-64), plus `probabilities_`/`outlier_scores_` accessors forwarding to the Plan-10 PyHDBSCAN getters. Both default ALL args (zero-arg constructible — Pitfall 6). Register both in `mlrs/__init__.py`. Honor the random_state→seed and any other sklearn-name boundary mapping at the shim layer (the Rust field name stays as-is; the shim exposes the sklearn name).
  </action>
  <verify>
    <automated>cd crates/mlrs-py/python && python -m pytest tests/test_shims.py -q -k "UMAP or HDBSCAN"</automated>
  </verify>
  <acceptance_criteria>
    - `mlrs.UMAP()` and `mlrs.HDBSCAN()` construct with ZERO required args (Pitfall 6 — needed for check_parameters_default_constructible).
    - `grep -c 'class UMAP' crates/mlrs-py/python/mlrs/manifold.py` returns 1 and `grep -c 'class HDBSCAN' crates/mlrs-py/python/mlrs/cluster.py` returns 1.
    - Both `__init__` bodies are pure (verified by the Task-3 AST test).
    - Both exported from `mlrs/__init__.py`; UMAP exposes transform/fit_transform, HDBSCAN exposes labels_/probabilities_/outlier_scores_.
  </acceptance_criteria>
  <done>mlrs.UMAP and mlrs.HDBSCAN shim classes added, zero-arg constructible, exported, forwarding to the Plan-10 PyO3 methods.</done>
</task>

<task type="auto">
  <name>Task 3: Expand the static test matrix + run the AST-purity + estimator_checks subset over the full set</name>
  <files>crates/mlrs-py/python/tests/test_shims.py, crates/mlrs-py/python/tests/test_params.py, crates/mlrs-py/python/tests/test_estimator_checks.py</files>
  <read_first>
    - crates/mlrs-py/python/tests/test_params.py (EXPECTED_PARAMS/SET_PARAM/ALL_12 at :25-83; runtime purity :117-127; the AST test added in Plan 00; the PCA required-arg special-case at :86-91)
    - crates/mlrs-py/python/tests/test_shims.py (ALL_12 at :26 + the parametrized importability/round-trip/family-surface tests)
    - crates/mlrs-py/python/tests/test_estimator_checks.py (parametrize_with_checks + the _expected_failed_checks xfail map)
    - crates/mlrs-py/python/mlrs/__init__.py (the now-full export list — derive the matrix from it)
    - .planning/phases/16-builder-retrofit-sweep-shim-coverage/16-PATTERNS.md (§7 — matrix expansion template rows incl. UMAP/HDBSCAN; the estimator_checks-subset confirmation at §7c)
  </read_first>
  <action>
    Expand the three test matrices to the full shim set. In test_params.py: add `EXPECTED_PARAMS` + `SET_PARAM` rows for every new class (15) matching their shim `__init__` defaults exactly (incl. UMAP/HDBSCAN per §7 template rows); replace the hard-coded `ALL_12` with the full key list (prefer deriving from the exported set so it cannot drift). In test_shims.py: replace `ALL_12` with the full set and ensure the family-specific surface assertions (transformer has transform, cluster labels-only, NN has kneighbors) cover the new classes. In test_estimator_checks.py: add the new classes to the parametrized estimator list; for any by-design failures add an xfail entry to `_expected_failed_checks`, but CONFIRM the three fit-free checks `check_no_attributes_set_in_init`, `check_parameters_default_constructible`, `check_get_params_invariance` are NOT xfailed for any new class (they must run green — D-07 step 4 / A7). Ensure the Plan-00 AST-purity test now iterates the full matrix (it should automatically if it reads the shared list). Run all three test files green WITHOUT the compiled extension. Keep the PCA required-arg special-case as-is (PCA is the documented exception; new classes must NOT require args).
  </action>
  <verify>
    <automated>cd crates/mlrs-py/python && python -m pytest tests/test_shims.py tests/test_params.py tests/test_estimator_checks.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_shims.py tests/test_params.py tests/test_estimator_checks.py` is fully green WITHOUT the compiled `_mlrs` extension.
    - The AST-purity test (`-k init_purity`) passes for the FULL set incl. UMAP/HDBSCAN.
    - The matrix covers all 32 estimator shims (17 pre-existing + 15 new) — `EXPECTED_PARAMS` length matches the exported estimator-shim count.
    - `check_no_attributes_set_in_init`, `check_parameters_default_constructible`, `check_get_params_invariance` are NOT in the xfail map for any new class and run green (grep the xfail map to confirm absence).
  </acceptance_criteria>
  <done>Full static shim gate green over all 32 shims; AST-purity + the three fit-free estimator_checks confirmed green; SHIM-03 static path complete (live run deferred to UAT).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python user kwargs → shim __init__ | ctor args must be stored verbatim, no validation/computation in __init__ (SHIM-01 invariant) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-00-PURITY | Tampering | every new shim `__init__` | mitigate | The Plan-00 AST test makes any computation/validation in `__init__` a hard failure across the full matrix (SHIM-01 enforced statically) |
| T-16-DEFCTOR | Tampering | zero-arg constructibility | mitigate | All new shims default every arg; check_parameters_default_constructible runs green (Pitfall 6) — a missing default is a test failure |
| T-16-FFI-DEFER | — | live check_estimator | accept | No maturin+pyarrow host in this environment (project memory: Python wheel untestable); the static subset is the maximum verifiable gate; live run routes to UAT (SHIM-03 by-design) |
</threat_model>

<verification>
- All 15 new shims import without the compiled extension and are zero-arg constructible.
- `pytest tests/test_shims.py tests/test_params.py tests/test_estimator_checks.py` green.
- AST-purity green over the full matrix; the three fit-free estimator_checks green (not xfailed).
</verification>

<success_criteria>
- Full pure-Python estimator coverage (32 shims) with faithful __init__s; static gate (round-trip + clone + AST purity + fit-free estimator_checks subset) green; live check_estimator deferred to UAT.
</success_criteria>

<output>
Create `.planning/phases/16-builder-retrofit-sweep-shim-coverage/16-11-SUMMARY.md` when done. Record the full shim count and the UAT note for the deferred live check_estimator run.
</output>
