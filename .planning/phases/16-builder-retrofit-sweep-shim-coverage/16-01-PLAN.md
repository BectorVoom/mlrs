---
phase: 16-builder-retrofit-sweep-shim-coverage
plan: 01
type: execute
wave: 1
depends_on: ["16-00"]
files_modified:
  - crates/mlrs-algos/src/linear/ridge.rs
  - crates/mlrs-algos/src/linear/mbsgd_regressor.rs
  - crates/mlrs-algos/tests/ridge_test.rs
  - crates/mlrs-algos/tests/mbsgd_regressor_test.rs
  - crates/mlrs-py/src/estimators/linear.rs
autonomous: true
requirements: [BLDR-03]
must_haves:
  truths:
    - "Ridge constructs only via zero-arg new() + builder; the arg-taking new(alpha, fit_intercept) is gone"
    - "MBSGDRegressor gains the <F, S=Unfit> state param and migrates from crate::traits to typestate"
    - "Ridge and MBSGDRegressor oracle suites still pass at 1e-5 unchanged (fit-body math byte-identical, D-03)"
    - "predict-before-fit on Ridge/MBSGDRegressor is a compile error (Fitted-gated Predict), not a runtime NotFitted"
    - "cargo build -p mlrs-py --features cpu succeeds after both pilot PyO3 arms migrate (no cross-crate break)"
  artifacts:
    - path: "crates/mlrs-algos/src/linear/ridge.rs"
      provides: "Ridge<F, S=Unfit> + RidgeBuilder + zero-arg new() + Fitted-gated Predict/accessors"
      contains: "struct Ridge<F, S"
    - path: "crates/mlrs-algos/src/linear/mbsgd_regressor.rs"
      provides: "MBSGDRegressor<F, S=Unfit> on typestate surface (builder pre-existing, trait-swap only)"
      contains: "MBSGDRegressor<F, S"
  key_links:
    - from: "crates/mlrs-py/src/estimators/linear.rs"
      to: "crates/mlrs-algos/src/linear/ridge.rs"
      via: "PyRidge fit builds via Ridge::builder()...build()?.fit(...); AnyRidge arms are Ridge<f*, Fitted>"
      pattern: "Ridge::<f(32|64)>::builder"
    - from: "crates/mlrs-algos/src/linear/ridge.rs"
      to: "crates/mlrs-algos/src/typestate.rs"
      via: "impl typestate::Fit for Ridge<F, Unfit>, impl typestate::Predict for Ridge<F, Fitted>"
      pattern: "typestate::(Fit|Predict)"
---

<objective>
Pilots — prove the two structurally-distinct retrofit shapes under their green suites BEFORE the bulk sweep (D-05). Ridge = shape A (no builder, arg-taking `new`) → the FULL build-out. MBSGDRegressor = shape B (builder already exists) → typestate-param + trait-swap only.

Each pilot is migrated in its OWN commit, gated by its oracle suite AND `cargo build -p mlrs-py` (catches the hidden PyO3 cross-crate break). This plan establishes the recipe every subsequent estimator follows.

Purpose: De-risk the broad sweep. If a pilot's oracle suite stays green and the fit-body diff shows zero compute deltas, the mechanical recipe is proven for all 27 remaining estimators.
Output: Ridge + MBSGDRegressor on the typestate surface, their call sites migrated, AnyRidge/AnyMBSGDRegressor arms updated to Fitted.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-builder-retrofit-sweep-shim-coverage/16-CONTEXT.md
@.planning/phases/16-builder-retrofit-sweep-shim-coverage/16-RESEARCH.md
@.planning/phases/16-builder-retrofit-sweep-shim-coverage/16-PATTERNS.md
</context>

<artifacts_this_phase_produces>
- `Ridge<F, S=Unfit>` struct param + `PhantomData<S>` field
- `RidgeBuilder` (f64 setters: `.alpha(f64)`, `.fit_intercept(bool)`, `build::<F>()` → `BuildError`)
- `Ridge::<F, Unfit>::new()` zero-arg (sklearn defaults), `into_builder()`, `hyperparams_eq()`, `Default`
- `impl typestate::Fit for Ridge<F, Unfit>` (type Fitted = Ridge<F, Fitted>); `impl typestate::Predict for Ridge<F, Fitted>`; `coef`/`intercept` accessors on `Ridge<F, Fitted>`
- `MBSGDRegressor<F, S=Unfit>` param; `build::<F>()` returns `MBSGDRegressor<F, Unfit>`; typestate Fit/Predict on the right state arms
- `AnyRidge`/`AnyMBSGDRegressor` switch to `any_estimator_typestate!` (Fitted arms)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pilot A — Ridge full build-out (shape A)</name>
  <files>crates/mlrs-algos/src/linear/ridge.rs, crates/mlrs-algos/tests/ridge_test.rs, crates/mlrs-py/src/estimators/linear.rs</files>
  <read_first>
    - crates/mlrs-algos/src/linear/ridge.rs (full — current struct :68, arg-new :90-97, coef/intercept accessors :101-109, Fit::fit :124-311 incl. alpha<0 guard :140-146 and geometry guard :147-154, Predict :317-373)
    - crates/mlrs-algos/src/manifold/umap.rs (the target exemplar — struct+PhantomData :124-170, new/builder/into_builder/hyperparams_eq :172-279, Default :272-279, builder+Default-from-new :285-446, Fit consuming-self :448-526, Fitted accessors :528-592)
    - crates/mlrs-algos/src/linear/mbsgd_regressor.rs:217-265 (BuildError::InvalidAlpha shape for the data-independent alpha>=0 check moving to build())
    - crates/mlrs-algos/src/typestate.rs (Fit/Predict/Fitted/Unfit/validate_geometry — surface after Plan 00)
    - crates/mlrs-algos/tests/ridge_test.rs (oracle fixture Ridge(solver='cholesky', fit_intercept=True); 2 call sites incl. Ridge::<f32>::new(1.0,true) at :276)
    - crates/mlrs-py/src/estimators/linear.rs (PyRidge fit f32/f64 arms :243-256 with `alpha as f32`; AnyRidge enum :189-193; import line :23)
    - .planning/phases/16-builder-retrofit-sweep-shim-coverage/16-PATTERNS.md (§1 — the full (a)-(f) Shape-A recipe with exact umap.rs line refs; §4 — PyO3 migration recipe with linear.rs:243-256 → manifold.rs:220-242 target; §5 not needed here)
  </read_first>
  <behavior>
    - Ridge::<F, Unfit>::new() yields alpha=1.0 (sklearn Ridge default), fit_intercept=true; equals Ridge::builder().build::<F>()? (hyperparams_eq true).
    - RidgeBuilder::default() is derived from new().into_builder() (NOT re-listed literals).
    - build::<F>() with alpha < 0 returns Err(BuildError::InvalidAlpha); the geometry guard stays in fit().
    - Fitted Ridge predicts; Unfit Ridge has no predict method (compile-time gate).
    - ridge_test oracle passes unchanged at 1e-5 (f64 + f32).
  </behavior>
  <action>
    Apply the §1 Shape-A recipe to ridge.rs exactly: (a) `struct Ridge<F, S = Unfit>` with `_state: PhantomData<S>` added as the ONLY new field — alpha, fit_intercept, coef_, intercept_ fields UNCHANGED (D-03). (b) Replace arg-taking `new(alpha, fit_intercept)` with zero-arg `new()` on `impl<F> Ridge<F, Unfit>` setting sklearn defaults (alpha = 1.0 via `F::from_int(1)`, fit_intercept = true); add `builder()`, `into_builder()`, `hyperparams_eq()`, and `impl Default for Ridge<F, Unfit>`. (c) Add `RidgeBuilder` with `f64` setters `.alpha(v: f64)` and `.fit_intercept(v: bool)`, `impl Default` = `Ridge::<f64, Unfit>::new().into_builder()` (single source — Pitfall 1), and `build<F>() -> Result<Ridge<F, Unfit>, BuildError>` that performs the data-INDEPENDENT `alpha >= 0` check → `BuildError::InvalidAlpha` (relocated from the current fit-body check at ridge.rs:140-146 — Pitfall 7) and casts the `f64` setters to `F` internally. (d) Swap imports: drop `use crate::traits::{Fit, Predict}`, add `use crate::error::BuildError` and `use crate::typestate::{validate_geometry, Fit, Fitted, Predict, Unfit}`. (e) Convert `Fit::fit(&mut self) -> &mut Self` to consuming-self `fit(self) -> Result<Ridge<F, Fitted>, AlgoError>` — replace the inline geometry guard with `validate_geometry(x, shape)?` at the top, keep EVERY compute line from the fit body BYTE-IDENTICAL (Pitfall 2 — zero compute deltas), and reconstruct the struct field-by-field into the Fitted value with `_state: PhantomData`. (f) Move `coef`/`intercept` accessors + `impl Predict` onto `impl<F> Ridge<F, Fitted>`, dropping the `ok_or(NotFitted)` in favor of `.expect("Some by construction on Ridge<F, Fitted>")`. Then migrate the 2 ridge_test.rs call sites (`Ridge::<F>::new(args)` → `Ridge::<F>::builder().alpha(..).fit_intercept(..).build()?`; `let mut est; est.fit(..)?` → `let est = est.fit(..)?`) and the PyRidge f32/f64 arms in linear.rs (§4 target shape: `Ridge::<f32>::builder().alpha(alpha).fit_intercept(fit_intercept).build::<f32>().map_err(build_err_to_py)?` then `.fit(...).map_err(algo_err_to_py)?`; DROP the `as f32` cast — builder setter is f64). Switch `AnyRidge` from `any_estimator!` to `any_estimator_typestate!` so the F32/F64 arms are `Ridge<f32, Fitted>`/`Ridge<f64, Fitted>` (Pitfall 4). Keep `py.detach`, `lock_pool()`, `validated_f32/f64`, `build_err_to_py`/`algo_err_to_py` UNCHANGED. Add a `hyperparams_eq` defaults-equality unit assertion to ridge_test.rs (BLDR-01). Commit Ridge + its tests + its PyO3 arm together.
  </action>
  <verify>
    <automated>cargo test --features cpu --test ridge_test && cargo build -p mlrs-py --features cpu</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --features cpu --test ridge_test` passes (1e-5 oracle, f64+f32) — D-03 invariant: the fit-body diff shows ZERO compute-line changes (signature/return/guard-call only).
    - `cargo build -p mlrs-py --features cpu` succeeds (PyO3 arm migrated; no cross-crate break).
    - `grep -c 'fn new(' crates/mlrs-algos/src/linear/ridge.rs` shows the zero-arg new only (no arg-taking new remains — verifiable that `new(alpha` no longer appears).
    - `! grep -q 'crate::traits' crates/mlrs-algos/src/linear/ridge.rs` (Ridge is off the old surface).
    - ridge_test contains a `hyperparams_eq` defaults-equality case that passes.
    - `AnyRidge` uses `any_estimator_typestate!` and its fit arms store `Ridge<f*, Fitted>`.
  </acceptance_criteria>
  <done>Ridge fully migrated (state param + builder + zero-arg new + typestate Fit/Predict + Fitted accessors), oracle green, mlrs-py builds, PyO3 arm on Fitted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pilot B — MBSGDRegressor trait-swap (shape B)</name>
  <files>crates/mlrs-algos/src/linear/mbsgd_regressor.rs, crates/mlrs-algos/tests/mbsgd_regressor_test.rs, crates/mlrs-py/src/estimators/linear.rs</files>
  <read_first>
    - crates/mlrs-algos/src/linear/mbsgd_regressor.rs (full — struct :36, existing MBSGDRegressorBuilder :89-288 incl. build<F> :213 and returned literal :282-286, coef/intercept accessors :62-82, import :30, Fit/Predict impls, fit body from :294)
    - crates/mlrs-algos/src/manifold/umap.rs:448-547 (Fit/Fitted consuming-self target)
    - crates/mlrs-algos/src/typestate.rs (Fit/Predict/Fitted/Unfit after Plan 00)
    - crates/mlrs-algos/tests/mbsgd_regressor_test.rs (call sites)
    - crates/mlrs-py/src/estimators/linear.rs (PyMBSGDRegressor — uses MBSGDRegressor::<f32>::builder()... at :1271; AnyMBSGDRegressor enum; the est.fit(&mut) shape)
    - .planning/phases/16-builder-retrofit-sweep-shim-coverage/16-PATTERNS.md (§2 — the minimal (a)-(d) Shape-B delta; builder already correct, ONLY build<F> return type + struct param + trait swap change)
  </read_first>
  <behavior>
    - MBSGDRegressor::<F, Unfit> constructs via the existing builder (unchanged setters/validation), now returning the Unfit-tagged type.
    - fit consumes self → MBSGDRegressor<F, Fitted>; predict only on Fitted.
    - mbsgd_regressor oracle passes unchanged.
  </behavior>
  <action>
    Apply the §2 Shape-B delta to mbsgd_regressor.rs (this is trait-swap-only — the builder already exists and is correct, so do NOT touch the builder body, its validation at :217-265, or the SgdConfig lowering at :266-281): (a) add `<F, S = Unfit>` + `_state: PhantomData<S>` to `struct MBSGDRegressor<F, S>` (config/coef_/intercept_ fields UNCHANGED). (b) The ONLY builder edit: change `build<F>() -> Result<MBSGDRegressor<F>, BuildError>` (:213) to `-> Result<MBSGDRegressor<F, Unfit>, BuildError>` and add `_state: PhantomData` to the returned struct literal (:282-286). (c) Swap import (:30) `use crate::traits::{Fit, Predict}` → `use crate::typestate::{validate_geometry, Fit, Fitted, Predict, Unfit}`. (d) Convert `Fit::fit(&mut self) -> &mut Self` to consuming-self returning `MBSGDRegressor<F, Fitted>` — keep the `sgd_solve` drive body BYTE-IDENTICAL (only signature + final reconstruct change; replace any inline geometry guard with `validate_geometry`); move `coef`/`intercept` accessors (:62-82) + `impl Predict` onto `impl<F> MBSGDRegressor<F, Fitted>`, dropping `ok_or(NotFitted)` → `.expect(...)`. Note: MBSGDRegressor is NOT a PartialFit consumer (IncrementalPCA is the only one — verified), so no PartialFit impl here. Migrate mbsgd_regressor_test.rs call sites (consuming-self + already builder-shaped) and the PyMBSGDRegressor arm in linear.rs (already builder-shaped at :1271 — only the `est.fit(&mut)` → consuming-self + `AnyMBSGDRegressor` switch to `any_estimator_typestate!` with `MBSGDRegressor<f*, Fitted>` arms). Add the `hyperparams_eq` defaults-equality assertion. Commit MBSGDRegressor + tests + PyO3 arm together.
  </action>
  <verify>
    <automated>cargo test --features cpu --test mbsgd_regressor_test && cargo build -p mlrs-py --features cpu</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --features cpu --test mbsgd_regressor_test` passes — fit-body diff shows ZERO compute deltas; the builder body is untouched (only build<F> return type changed).
    - `cargo build -p mlrs-py --features cpu` succeeds.
    - `! grep -q 'crate::traits' crates/mlrs-algos/src/linear/mbsgd_regressor.rs`.
    - `AnyMBSGDRegressor` uses `any_estimator_typestate!`; fit arm stores `MBSGDRegressor<f*, Fitted>`.
    - `hyperparams_eq` defaults-equality case passes.
  </acceptance_criteria>
  <done>MBSGDRegressor on the typestate surface (state param + trait swap only; builder untouched), oracle green, mlrs-py builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python/test caller → estimator construction | hyperparameters cross into build() (data-independent validation) |
| host x/y geometry → fit | shapes reach validate_geometry before any device launch |
| PyO3 F64 path → device | f64 upload must be guarded on f64-incapable backends |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-V5 | Tampering / DoS | Ridge/MBSGDRegressor fit geometry guard | mitigate | `validate_geometry(x, shape)?` stays at the TOP of every ported fit, before any device launch (ASVS V5); the data-independent alpha>=0 check relocates to build() → BuildError, NOT dropped |
| T-16-GUARDF64 | DoS | PyRidge/PyMBSGDRegressor F64 arm | mitigate | `guard_f64()` before the F64 upload preserved verbatim in the migrated PyO3 fit; `lock_pool()` (poison-recovering) kept, not `.lock().expect()` |
| T-16-ARM | Tampering | AnyRidge/AnyMBSGDRegressor Fitted arm type | mitigate | Arms switched to `any_estimator_typestate!` so a fitted value is typed `T<f*, Fitted>` (Pitfall 4 — no Unfit value stored as fitted) |
</threat_model>

<verification>
- Both pilot oracle suites green (1e-5, f64+f32).
- `cargo build -p mlrs-py --features cpu` green after both arms migrate.
- Neither pilot file references `crate::traits`.
- D-03: fit-body diffs reviewed for zero compute-line changes.
</verification>

<success_criteria>
- Ridge (full build-out) and MBSGDRegressor (trait-swap) both migrated under their green suites.
- The mechanical recipe is proven for the bulk sweep.
- PyO3 arms on Fitted; mlrs-py compiles.
</success_criteria>

<output>
Create `.planning/phases/16-builder-retrofit-sweep-shim-coverage/16-01-SUMMARY.md` when done. Record the exact RidgeBuilder setter signatures and the consuming-self fit pattern so the bulk-sweep plans copy them.
</output>
