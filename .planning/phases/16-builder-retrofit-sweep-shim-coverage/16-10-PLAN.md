---
phase: 16-builder-retrofit-sweep-shim-coverage
plan: 10
type: execute
wave: 10
depends_on: ["16-09"]
files_modified:
  - crates/mlrs-py/src/estimators/manifold.rs
  - crates/mlrs-py/src/estimators/cluster.rs
autonomous: true
requirements: [SHIM-02]
must_haves:
  truths:
    - "PyUMAP exposes transform and fit_transform #[pymethods] forwarding to the Rust Umap<F, Fitted>::transform / Umap<F, Unfit>::fit_transform"
    - "PyHDBSCAN exposes fit_predict, probabilities_, and outlier_scores_ #[pymethods] forwarding to the Rust Hdbscan<F, Fitted>"
    - "Both wraps keep py.detach GIL release, lock_pool (poison-recovering), guard_f64 before F64, n_features_in_ enforced"
    - "cargo build -p mlrs-py --features cpu succeeds with the new methods"
  artifacts:
    - path: "crates/mlrs-py/src/estimators/manifold.rs"
      provides: "PyUMAP transform + fit_transform #[pymethods] (dtype-split f32/f64)"
      contains: "fn transform"
    - path: "crates/mlrs-py/src/estimators/cluster.rs"
      provides: "PyHDBSCAN fit_predict + probabilities_ + outlier_scores_ #[pymethods]"
      contains: "fn fit_predict"
  key_links:
    - from: "crates/mlrs-py/src/estimators/manifold.rs"
      to: "crates/mlrs-algos/src/manifold/umap.rs"
      via: "PyUMAP.transform matches AnyUmap::{F32,F64} arm and calls typestate::Transform::transform"
      pattern: "Transform::transform|fit_transform"
---

<objective>
SHIM-02 — fill the VERIFIED method gaps on the EXISTING PyUMAP/PyHDBSCAN wraps. The wraps are already shipped and registered (manifold.rs:74, cluster.rs:324, lib.rs:265-266) — this is NOT creating new wraps (the CONTEXT "two new PyO3 wraps" framing is wrong). RESEARCH verified the gaps: PyUMAP lacks `transform`/`fit_transform`; PyHDBSCAN lacks `fit_predict`/`probabilities_`/`outlier_scores_`. The Rust `Umap<F, Fitted>` and `Hdbscan<F, Fitted>` already HAVE these methods (umap.rs:215/568, hdbscan.rs:284 + GLOSH); this is pure forwarding work.

Done after the sweep (Plan 09) because PyHDBSCAN lives in cluster.rs, which the sweep also edits — sequential avoids a file conflict.

Purpose: Complete the UMAP/HDBSCAN Python boundary surface (D-08) so the Plan 11 shim classes (mlrs.UMAP/mlrs.HDBSCAN) can forward to a complete wrap.
Output: PyUMAP.transform/fit_transform; PyHDBSCAN.fit_predict/probabilities_/outlier_scores_.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-builder-retrofit-sweep-shim-coverage/16-CONTEXT.md
@.planning/phases/16-builder-retrofit-sweep-shim-coverage/16-RESEARCH.md
@.planning/phases/16-builder-retrofit-sweep-shim-coverage/16-PATTERNS.md
</context>

<artifacts_this_phase_produces>
- `PyUMAP::transform` (#[pymethods], dtype-split f32/f64) — forwards to `typestate::Transform::transform` on `AnyUmap::{F32,F64}`
- `PyUMAP::fit_transform` (#[pymethods]) — forwards to `Umap<F, Unfit>::fit_transform`
- `PyHDBSCAN::fit_predict` (#[pymethods]) — forwards to `Hdbscan<F, Fitted>::fit_predict`
- `PyHDBSCAN::probabilities_` (#[pymethods] getter) + `PyHDBSCAN::outlier_scores_` (#[pymethods] getter)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add PyUMAP transform + fit_transform #[pymethods]</name>
  <files>crates/mlrs-py/src/estimators/manifold.rs</files>
  <read_first>
    - crates/mlrs-py/src/estimators/manifold.rs (full — PyUMAP at :74, #[new] at :142-148, fit at :194-271 with py.detach + guard_f64 at :244, embedding_f32/f64 getters at :118-132/275-281, the AnyUmap enum at :36-46)
    - crates/mlrs-algos/src/manifold/umap.rs (Umap<F, Fitted>::transform via typestate::Transform at :568; Umap<F, Unfit>::fit_transform at :215)
    - crates/mlrs-py/src/estimators/linear.rs (the migrated PyRidge predict_f32/predict_f64 dtype-split pattern — a #[pyclass] method can't be generic over F, so split by dtype suffix and arm-match)
    - .planning/phases/16-builder-retrofit-sweep-shim-coverage/16-PATTERNS.md (§5 — the dtype-arm-match forwarder pattern)
  </read_first>
  <action>
    Add `transform` and `fit_transform` `#[pymethods]` to PyUMAP in manifold.rs. `transform`: take an X array, `py.detach` for GIL release, `lock_pool()`, match the `AnyUmap::{F32,F64}` fitted arm (call `not_fitted("umap", "transform")` on the `Unfit` arm), `guard_f64()` before the F64 upload, call the Rust `typestate::Transform::transform` on the fitted Umap, return host `Vec<f32>`/`Vec<f64>` (dtype-split as PyRidge's predict does — the method can't be generic over F, so resolve dtype by the matched arm). Add `use mlrs_algos::typestate::Transform` to manifold.rs (needed to call transform). `fit_transform`: take X (+ y if the signature needs it), build via the umap builder + call `Umap::<F, Unfit>::fit_transform` (umap.rs:215), same GIL/guard/lock pattern, return the embedding as host Vec. Keep `validated_f32/f64`, `algo_err_to_py`, `not_fitted` mappers unchanged. Do NOT modify the existing `fit`/`embedding_` methods or the wrap registration (already in lib.rs).
  </action>
  <verify>
    <automated>cargo build -p mlrs-py --features cpu</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p mlrs-py --features cpu` succeeds.
    - `grep -cE 'fn transform|fn fit_transform' crates/mlrs-py/src/estimators/manifold.rs` returns at least 2 (both methods added; counting their dtype-split helpers is acceptable as >=2).
    - `grep -q 'guard_f64' crates/mlrs-py/src/estimators/manifold.rs` (the F64 guard is present on the transform path).
    - The transform path calls `not_fitted` on the `AnyUmap::Unfit` arm (predict-before-fit at the Python boundary still errors).
  </acceptance_criteria>
  <done>PyUMAP exposes transform + fit_transform forwarding to the Rust Fitted/Unfit methods; mlrs-py builds.</done>
</task>

<task type="auto">
  <name>Task 2: Add PyHDBSCAN fit_predict + probabilities_ + outlier_scores_ #[pymethods]</name>
  <files>crates/mlrs-py/src/estimators/cluster.rs</files>
  <read_first>
    - crates/mlrs-py/src/estimators/cluster.rs (full — PyHDBSCAN at :324, #[new] at :375-379, fit at :408-466 with guard_f64 + TypestateFit::fit at :443, labels_inner at :359-366, the AnyHdbscan enum)
    - crates/mlrs-algos/src/cluster/hdbscan.rs (Hdbscan<F, Fitted>::fit_predict at :284; GLOSH outlier_scores_; probabilities_ — confirm exact method names/signatures)
    - crates/mlrs-py/src/estimators/manifold.rs (PyUMAP getter pattern for dtype-suffixed F-valued outputs)
    - .planning/phases/16-builder-retrofit-sweep-shim-coverage/16-PATTERNS.md (§5 — labels_inner forwarder at cluster.rs:359-366; i32 labels are dtype-agnostic, F-valued outputs are dtype-suffixed)
  </read_first>
  <action>
    Add `fit_predict`, `probabilities_`, and `outlier_scores_` `#[pymethods]` to PyHDBSCAN in cluster.rs. `fit_predict`: take X, build + fit via the existing HDBSCAN path (mirror the existing `fit` at :408-466 — py.detach, lock_pool, guard_f64, TypestateFit::fit), then return the i32 labels (dtype-agnostic, like labels_inner at :359-366) — sklearn semantics: fit then return labels_. `probabilities_` and `outlier_scores_`: getters returning the `F`-valued vectors — dtype-suffixed split (match `AnyHdbscan::{F32,F64}`, call `not_fitted("hdbscan", ...)` on the `Unfit` arm), forwarding to the Rust `Hdbscan<F, Fitted>` accessors (confirm exact method names from hdbscan.rs). Keep all existing methods (`labels_`, `fit`, `is_fitted`, `dtype`) + the wrap registration unchanged.
  </action>
  <verify>
    <automated>cargo build -p mlrs-py --features cpu</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p mlrs-py --features cpu` succeeds.
    - `grep -cE 'fn fit_predict|fn probabilities|fn outlier_scores' crates/mlrs-py/src/estimators/cluster.rs` returns at least 3 (all three added; dtype-split helpers acceptable).
    - The probabilities_/outlier_scores_ paths call `not_fitted` on the `AnyHdbscan::Unfit` arm.
    - `guard_f64` is present on the fit_predict path.
  </acceptance_criteria>
  <done>PyHDBSCAN exposes fit_predict + probabilities_ + outlier_scores_; mlrs-py builds; D-08 wrap surface complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python transform/fit_predict caller → device | host X crosses into a device transform/cluster read |
| F64 path → device | f64 upload on f64-incapable backend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-GUARDF64 | DoS | PyUMAP.transform / PyHDBSCAN.fit_predict F64 path | mitigate | `guard_f64()` BEFORE the F64 upload on every new method (manifold.rs:244 pattern) |
| T-16-V5 | Tampering / DoS | new transform/fit_predict device reads | mitigate | reuse `validated_f32/f64` ingress; the Rust Fitted method runs its own geometry guard (n_features_in_ enforced) |
| T-16-POISON | DoS | pool mutex on a panicked transform | mitigate | use `lock_pool()` (poison-recovering), NOT `.lock().expect()`, on the new methods |
| T-16-NOTFIT | Tampering | predict-before-fit at the Python boundary | mitigate | the Unfit arm returns `not_fitted(...)` on transform/probabilities_/outlier_scores_ |
</threat_model>

<verification>
- `cargo build -p mlrs-py --features cpu` green with the new methods.
- guard_f64 + lock_pool + not_fitted present on the new paths.
</verification>

<success_criteria>
- PyUMAP.transform/fit_transform + PyHDBSCAN.fit_predict/probabilities_/outlier_scores_ exist and forward to the Rust Fitted methods; D-08 wrap surface complete.
</success_criteria>

<output>
Create `.planning/phases/16-builder-retrofit-sweep-shim-coverage/16-10-SUMMARY.md` when done. Record the exact new method names so the Plan 11 mlrs.UMAP/mlrs.HDBSCAN shim classes forward correctly.
</output>
