---
phase: 10-sgd-linear-svm
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - crates/mlrs-algos/src/error.rs
  - crates/mlrs-algos/src/linear/mod.rs
  - crates/mlrs-algos/src/linear/sgd_config.rs
  - crates/mlrs-algos/src/linear/mbsgd_classifier.rs
  - crates/mlrs-algos/src/linear/mbsgd_regressor.rs
  - crates/mlrs-algos/src/linear/linear_svc.rs
  - crates/mlrs-algos/src/linear/linear_svr.rs
  - crates/mlrs-kernels/src/lib.rs
  - crates/mlrs-kernels/src/sgd.rs
  - crates/mlrs-backend/src/prims/mod.rs
  - crates/mlrs-backend/src/prims/sgd.rs
  - crates/mlrs-py/src/estimators/linear.rs
  - crates/mlrs-py/src/errors.rs
  - crates/mlrs-backend/tests/sgd_test.rs
  - crates/mlrs-backend/tests/memory_gate_test.rs
  - crates/mlrs-algos/tests/sgd_config_test.rs
  - crates/mlrs-algos/tests/mbsgd_classifier_test.rs
  - crates/mlrs-algos/tests/mbsgd_regressor_test.rs
  - crates/mlrs-algos/tests/linear_svc_test.rs
  - crates/mlrs-algos/tests/linear_svr_test.rs
  - crates/mlrs-py/tests/sgd_smoke_test.rs
  - scripts/gen_oracle.py
autonomous: true
requirements: [PRIM-10, SGDSVM-01, SGDSVM-02, SGDSVM-03, SGDSVM-04]
must_haves:
  truths:
    - "Workspace compiles with --features cpu after the scaffold (all stubs in place)"
    - "Loss/Penalty/LearningRate enums exist with TryFrom<&str> accepting sklearn spellings + legacy aliases (D-04/D-05)"
    - "BuildError exists with data-independent + UnknownLoss/Penalty/LearningRate + InvalidLossForEstimator variants (D-08/D-09)"
    - "Four *Builder types exist with build() -> Result<Estimator, BuildError> stubs whose field initializers encode sklearn defaults (D-01/D-03)"
    - "SgdConfig struct exists as the shared lowering target for all four builders (D-06)"
    - "sgd_margin + sgd_weight_update #[cube(launch)] kernel stubs exist, SharedMemory-free by construction"
    - "prims/sgd.rs has a real geometry-validating sgd_solve signature with a todo!() compute body"
    - "build_err_to_py exists in mlrs-py/src/errors.rs mapping BuildError -> PyValueError (D-09)"
    - "Six #[ignore] Nyquist test files compile and assert fixture-load+shape only"
    - "gen_oracle.py emits committed pinned-deterministic .npz fixtures for the four estimators (shuffle=False, tol=0, fixed max_iter/eta0)"
  artifacts:
    - path: "crates/mlrs-algos/src/linear/sgd_config.rs"
      provides: "Loss/Penalty/LearningRate enums + TryFrom<&str> + SgdConfig + four builders"
      contains: "pub struct SgdConfig"
    - path: "crates/mlrs-algos/src/error.rs"
      provides: "BuildError enum (D-08/D-09)"
      contains: "enum BuildError"
    - path: "crates/mlrs-kernels/src/sgd.rs"
      provides: "sgd_margin + sgd_weight_update #[cube(launch)] stubs"
      contains: "pub fn sgd_margin"
    - path: "crates/mlrs-backend/src/prims/sgd.rs"
      provides: "sgd_solve host signature with geometry guard + todo!() body"
      contains: "pub fn sgd_solve"
    - path: "crates/mlrs-algos/src/linear/mbsgd_classifier.rs"
      provides: "MBSGDClassifier struct + MBSGDClassifierBuilder + build() stub"
      contains: "pub struct MBSGDClassifier"
    - path: "crates/mlrs-algos/src/linear/mbsgd_regressor.rs"
      provides: "MBSGDRegressor struct + builder stub"
      contains: "pub struct MBSGDRegressor"
    - path: "crates/mlrs-algos/src/linear/linear_svc.rs"
      provides: "LinearSVC struct + builder stub"
      contains: "pub struct LinearSVC"
    - path: "crates/mlrs-algos/src/linear/linear_svr.rs"
      provides: "LinearSVR struct + builder stub"
      contains: "pub struct LinearSVR"
    - path: "crates/mlrs-py/src/errors.rs"
      provides: "build_err_to_py mapper (D-09)"
      contains: "build_err_to_py"
    - path: "crates/mlrs-backend/tests/sgd_test.rs"
      provides: "PRIM-10 #[ignore] scaffolds (convex/cpu_launch)"
      contains: "sgd"
    - path: "crates/mlrs-algos/tests/sgd_config_test.rs"
      provides: "TryFrom + build() validation scaffolds (D-05/D-08)"
      contains: "try_from"
  key_links:
    - from: "crates/mlrs-algos/src/linear/mod.rs"
      to: "sgd_config + four estimator modules"
      via: "pub mod registrations"
      pattern: "pub mod sgd_config"
    - from: "crates/mlrs-py/src/errors.rs"
      to: "mlrs_algos::error::BuildError"
      via: "build_err_to_py import + map"
      pattern: "build_err_to_py"
---

<objective>
Wave-0 scaffold for Phase 10 (SGD / Linear-SVM), mirroring the shipped 07-01/08-01/09-01 precedent: front-load ALL shared-file edits and every compiling stub into one wave so Waves 1/2/3 are file-disjoint and parallel-safe. This plan lands the builder-pattern construction surface (D-01…D-09), the SGD prim/kernel stubs, the four estimator homes, the PyO3 integration points, the six #[ignore] Nyquist test scaffolds, and the four pinned-deterministic oracle generators.

Purpose: De-risk the highest-risk phase by resolving the construction API + oracle pins BEFORE any compute is wired. The Wave-0 oracle generators (run against live sklearn in a /tmp venv) are the AUTHORITATIVE pin that later resolves RESEARCH Open Questions A1 (optimal/Bottou-t0), A2 (minibatch sum-vs-average), A3 (intercept_decay), A6/Q3 (binary scope).
Output: A compiling workspace with all Phase-10 symbols stubbed, BuildError + build_err_to_py wired, and committed .npz oracle fixtures for the four estimators.

Scope (D-02): the builder is the going-forward project standard, but Phase 10 INTRODUCES it via ONLY the four new estimators — retrofitting the existing low-arity estimators (Ridge/Lasso/ElasticNet/LogisticRegression/KMeans/PCA/spectral) is deferred and OUT OF SCOPE here. This plan touches no existing estimator's constructor.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-sgd-linear-svm/10-CONTEXT.md
@.planning/phases/10-sgd-linear-svm/10-RESEARCH.md
@.planning/phases/10-sgd-linear-svm/10-PATTERNS.md
@.planning/phases/10-sgd-linear-svm/10-VALIDATION.md
@.planning/phases/09-spectral-family/09-01-wave0-scaffold-PLAN.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Wave-0 surface; later waves fill bodies)

**Enums (sgd_config.rs, D-04):**
- `Loss::{Hinge, Log, SquaredHinge, SquaredLoss, EpsilonInsensitive, SquaredEpsilonInsensitive}`
- `Penalty::{L1, L2, ElasticNet}`
- `LearningRate::{Optimal, InvScaling, Constant, Adaptive}`
- `impl TryFrom<&str>` for each (sklearn spellings + aliases `log`/`log_loss`, `squared_error`/`squared_loss`); `name(self) -> &'static str` on each.

**Shared config (sgd_config.rs, D-06):**
- `pub struct SgdConfig { loss, penalty, alpha:f64, l1_ratio:f64, fit_intercept:bool, max_iter:usize, tol:f64, learning_rate, eta0:f64, power_t:f64, epsilon:f64, batch_size:usize, shuffle:bool, seed:u64 }`

**BuildError (error.rs, D-08/D-09):**
- `pub enum BuildError { InvalidAlpha, InvalidL1Ratio, InvalidEta0, InvalidEpsilon, UnknownLoss, UnknownPenalty, UnknownLearningRate, InvalidLossForEstimator }` (thiserror, `#[error("…")]` per variant).

**Builders (per estimator module, D-01/D-03):**
- `MBSGDClassifierBuilder`, `MBSGDRegressorBuilder`, `LinearSVCBuilder`, `LinearSVRBuilder`, each `Estimator::builder() -> Builder`, setters returning `Self`, `build() -> Result<Estimator<F>, BuildError>`. Default field initializers encode sklearn defaults (RESEARCH §Defaults table).

**Estimator structs:**
- `MBSGDClassifier<F>` (config:SgdConfig, classes_:Vec<i64>, n_features, coef_, intercept_ Options)
- `MBSGDRegressor<F>` (config:SgdConfig, coef_, intercept_ Options)
- `LinearSVC<F>` (config:SgdConfig + c:f64 + intercept_scaling:f64, classes_, coef_, intercept_)
- `LinearSVR<F>` (config:SgdConfig + c:f64 + intercept_scaling:f64, coef_, intercept_)

**Kernel fns (mlrs-kernels/src/sgd.rs):**
- `#[cube(launch)] pub fn sgd_margin<F>(x, w, bias:F, p:&mut, b:u32, d:u32)` — pass 1 per-sample
- `#[cube(launch)] pub fn sgd_weight_update<F>(x, g, w:&mut, eta:F, inv_b:F, d:u32, b:u32)` — pass 2 per-coordinate

**Prim (mlrs-backend/src/prims/sgd.rs):**
- `pub fn sgd_solve<F>(pool, x, y, shape, config:&SgdConfig) -> Result<(coef, intercept), PrimError>` host signature (geometry guard real, compute todo!()).
- `pub const SGD_DEFAULT_MAX_ITER: usize = 1000; pub const SGD_DEFAULT_TOL: f64 = 1e-3;`

**PyO3 (mlrs-py):**
- `build_err_to_py(BuildError) -> PyErr` (errors.rs).
- Four `any_estimator!` Unfit{} stub blocks in estimators/linear.rs storing sklearn strings + scalars (bodies filled Wave 3).

**New file paths:** the four estimator modules, sgd_config.rs, kernels/sgd.rs, prims/sgd.rs, six test files, four oracle generators.

**New oracle fixtures (committed .npz):** `mbsgd_classifier_{f32,f64}_seed42.npz` (+ a log-loss variant), `mbsgd_regressor_{f32,f64}_seed42.npz`, `linear_svc_{f32,f64}_seed42.npz`, `linear_svr_{f32,f64}_seed42.npz`.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Enums, SgdConfig, BuildError, four builders + module index</name>
  <files>crates/mlrs-algos/src/linear/sgd_config.rs, crates/mlrs-algos/src/error.rs, crates/mlrs-algos/src/linear/mod.rs, crates/mlrs-algos/src/linear/mbsgd_classifier.rs, crates/mlrs-algos/src/linear/mbsgd_regressor.rs, crates/mlrs-algos/src/linear/linear_svc.rs, crates/mlrs-algos/src/linear/linear_svr.rs</files>
  <read_first>
    - crates/mlrs-algos/src/kernel_ridge/kernel_ridge.rs §`enum KernelKind` lines 67-89 (enum + name() shape, D-04 precedent)
    - crates/mlrs-algos/src/cluster/spectral_clustering.rs — NOTE the `affinity: String` ANTI-PATTERN D-04 rejects (do not copy)
    - crates/mlrs-algos/src/error.rs lines 54-60 (InvalidAlpha shape), 137-146 (InvalidL1Ratio), 152-158 (InvalidC) — thiserror `#[error("…")]` variant shape for BuildError
    - crates/mlrs-algos/src/linear/elastic_net.rs lines 57-74 (struct shape), 117-137 (accessors) — regressor struct precedent
    - crates/mlrs-algos/src/linear/logistic.rs lines 99-125 (classifier struct w/ classes_), 135-153 (new()/with_opts that builder supersedes)
    - 10-PATTERNS.md §sgd_config.rs (lines 166-219) + §error.rs (lines 350-371) + §mbsgd_classifier/regressor/svc/svr
    - 10-RESEARCH.md §Pattern-3 (SgdConfig lines 279-294), §SGD-Math §Defaults table (lines 387-394), §TryFrom (lines 517-533)
    - 10-CONTEXT.md D-01..D-09
  </read_first>
  <action>
    Create sgd_config.rs with the three enums per D-04 (exact variant names in artifacts section), each deriving Debug/Clone/Copy/PartialEq/Eq, each with `name(self) -> &'static str` and `impl TryFrom<&str>` (Error = BuildError) accepting sklearn spellings AND legacy aliases: `log`|`log_loss`->Log, `squared_error`|`squared_loss`->SquaredLoss; unknown -> `BuildError::UnknownLoss{value}` (and parallel UnknownPenalty/UnknownLearningRate). Add `pub struct SgdConfig` with the exact 14 fields in the artifacts section. Add `BuildError` to error.rs as a SEPARATE thiserror enum (sibling to AlgoError) with variants InvalidAlpha/InvalidL1Ratio/InvalidEta0/InvalidEpsilon/UnknownLoss/UnknownPenalty/UnknownLearningRate/InvalidLossForEstimator, each `#[error("…")]`. Create the four estimator modules with their struct (artifacts section) + a `*Builder` whose DEFAULT field initializers encode each estimator's sklearn defaults (D-03: classifier loss=hinge/penalty=l2/alpha=1e-4/learning_rate=optimal/max_iter=1000/tol=1e-3/eta0=0.01/l1_ratio=0.15/power_t=0.5; regressor loss=squared_error/learning_rate=invscaling/power_t=0.25; svc loss=squared_hinge/c=1.0/intercept_scaling=1.0; svr loss=squared_epsilon_insensitive/epsilon=0.0/c=1.0). Each builder exposes ONLY the knobs valid for that estimator (D-06: svc/svr have no eta0/learning_rate setter; classifier/regressor have them; regressor+svr have an epsilon setter). `build()` body is a STUB returning Ok(Estimator{config: lowered SgdConfig, fitted state None}) — the validation predicates are filled in Wave 1+ but the function SIGNATURE `build(self) -> Result<Estimator<F>, BuildError>` is final now. Register all five new modules in linear/mod.rs with `pub mod` lines (Wave-0 owns the index; estimator plans do NOT edit mod.rs). Each estimator is generic `<F: Float + CubeElement + Pod>` matching the analog bounds.
  </action>
  <verify>
    <automated>cargo build -p mlrs-algos --features cpu 2>&1 | tail -5; grep -c "affinity: String" crates/mlrs-algos/src/linear/sgd_config.rs</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p mlrs-algos --features cpu` exits 0
    - `sgd_config.rs` contains `pub enum Loss`, `pub enum Penalty`, `pub enum LearningRate`, `pub struct SgdConfig`, and three `impl TryFrom<&str>` blocks
    - `grep -c "affinity: String" crates/mlrs-algos/src/linear/sgd_config.rs` == 0 (D-04 anti-pattern absent)
    - `error.rs` contains `pub enum BuildError` with the 8 named variants
    - Each of the four estimator modules contains `pub struct <Estimator>` and a `*Builder` with `build(self) -> Result<<Estimator><F>, BuildError>`
    - `linear/mod.rs` contains `pub mod sgd_config; pub mod mbsgd_classifier; pub mod mbsgd_regressor; pub mod linear_svc; pub mod linear_svr;`
  </acceptance_criteria>
  <done>mlrs-algos compiles on cpu with all enums, SgdConfig, BuildError, and four builder-fronted estimator stubs registered; D-04 String anti-pattern absent.</done>
</task>

<task type="auto">
  <name>Task 2: SGD kernel + prim stubs, PyO3 errors + Unfit stubs</name>
  <files>crates/mlrs-kernels/src/sgd.rs, crates/mlrs-kernels/src/lib.rs, crates/mlrs-backend/src/prims/sgd.rs, crates/mlrs-backend/src/prims/mod.rs, crates/mlrs-py/src/errors.rs, crates/mlrs-py/src/estimators/linear.rs</files>
  <read_first>
    - crates/mlrs-kernels/src/coordinate.rs lines 19-34 (cpu-MLIR safety contract doc), 36-40 (imports/re-export), 57-78 (col_dot GATHER shape), 93-107 (residual_axpy over-provisioned bounds-check)
    - crates/mlrs-backend/src/prims/coordinate_descent.rs lines 40/52-54 (const tol/max_iter), 94-120 (validate-before-launch), 137-222 (host loop), map_cd_error (NotConverged at cap)
    - crates/mlrs-backend/src/prims/laplacian.rs — geometry-guard + todo!() compute precedent (09-01 Wave-0 shape)
    - crates/mlrs-py/src/errors.rs lines 11-19 (error→exception doc table), 55-57 (algo_err_to_py)
    - crates/mlrs-py/src/estimators/kernel.rs line 103 (Unfit stores `kernel: String`)
    - crates/mlrs-py/src/estimators/linear.rs lines 632-636 (any_estimator! invocation), 632-806 (PyLogisticRegression)
    - 10-PATTERNS.md §sgd.rs kernel (lines 42-111), §prims/sgd.rs (lines 114-163), §errors.rs (lines 412-428), §linear.rs (lines 374-409), §Module-index edits (lines 467-474)
    - 10-RESEARCH.md §Pattern-1 (kernel bodies lines 199-256), §Pitfall-1 cpu-MLIR rules (lines 259-267)
  </read_first>
  <action>
    Create kernels/sgd.rs with the module-doc cpu-MLIR safety contract copied from coordinate.rs (the prose IS the grep gate). Define two `#[cube(launch)]` fns SharedMemory-free by construction: `sgd_margin<F: Float + CubeElement>(x, w, bias: F, p:&mut Array<F>, b:u32, d:u32)` (pass 1, `let i=ABSOLUTE_POS; if i < b as usize { forward while-scan dot + bias }`) and `sgd_weight_update<F>(x, g, w:&mut Array<F>, eta:F, inv_b:F, d:u32, b:u32)` (pass 2, `let j=ABSOLUTE_POS; if j < d as usize { GATHER grad over batch; w[j]=w[j]-eta*grad*inv_b }`). Scalars BY VALUE, F/u32 accumulators seeded `F::from_int(0i64)`/`0u32`, NO SharedMemory/atomics/F::INFINITY/mutable-bool/descending-shift. For Wave 0 the bodies MAY be the real GATHER shape OR a minimal placeholder — but MUST be SharedMemory-free and launchable (Wave 1 fills the real math + dloss host helper). Register `pub mod sgd;` in kernels/lib.rs (file-disjoint; sgd.rs adds its own `pub use self::sgd_margin as sgd_margin;` etc). Create prims/sgd.rs with `pub const SGD_DEFAULT_MAX_ITER/SGD_DEFAULT_TOL`, a `map_sgd_error` mirroring map_cd_error (NotConverged at cap), and `pub fn sgd_solve<F>(pool, x:&DeviceArray, y:&DeviceArray, shape:(usize,usize), config:&SgdConfig) -> Result<(DeviceArray<...>, DeviceArray<...>), PrimError>` with a REAL geometry guard (reject when x.len() != n*d) but a `todo!()` compute body. Register `pub mod sgd;` in prims/mod.rs. In mlrs-py errors.rs add `pub fn build_err_to_py(err: BuildError) -> PyErr { PyValueError::new_err(err.to_string()) }` (import BuildError from mlrs_algos::error) + a doc-table row. In estimators/linear.rs add four `any_estimator!` Unfit{} stub blocks (AnyMBSGDClassifier/Regressor/LinearSVC/LinearSVR) storing sklearn STRINGS + scalars per the artifacts section — the hand-written `#[pymethods]` fit bodies are STUBS (todo!()/NotImplemented) filled in Wave 3; the four pyclasses are NOT yet registered on the `_mlrs` module (Wave 3 owns that registration so this plan compiles without the estimator bodies). Keep mlrs-kernels feature-free (build with bare `cargo build -p mlrs-kernels`, NOT --features cpu, per [09-02] memory).
  </action>
  <verify>
    <automated>cargo build -p mlrs-kernels 2>&1 | tail -3; cargo build -p mlrs-backend --features cpu 2>&1 | tail -3; grep -v '^//' crates/mlrs-kernels/src/sgd.rs | grep -c SharedMemory; grep -v '^//' crates/mlrs-kernels/src/sgd.rs | grep -c INFINITY</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p mlrs-kernels` exits 0 (bare, no feature)
    - `cargo build -p mlrs-backend --features cpu` exits 0
    - `crates/mlrs-kernels/src/sgd.rs` contains `pub fn sgd_margin` and `pub fn sgd_weight_update`
    - `grep -v '^//' crates/mlrs-kernels/src/sgd.rs | grep -c SharedMemory` == 0 (code, not comments)
    - `grep -v '^//' crates/mlrs-kernels/src/sgd.rs | grep -c INFINITY` == 0
    - `crates/mlrs-backend/src/prims/sgd.rs` contains `pub fn sgd_solve` and `SGD_DEFAULT_MAX_ITER`
    - `crates/mlrs-py/src/errors.rs` contains `build_err_to_py`
    - `crates/mlrs-py/src/estimators/linear.rs` contains four `any_estimator!` blocks for the new estimators
  </acceptance_criteria>
  <done>Kernel + prim stubs compile (kernels bare, backend on cpu), sgd.rs is SharedMemory/INFINITY-free, build_err_to_py + four Unfit stub blocks exist.</done>
</task>

<task type="auto">
  <name>Task 3: Six #[ignore] Nyquist test scaffolds + four oracle generators</name>
  <files>crates/mlrs-backend/tests/sgd_test.rs, crates/mlrs-backend/tests/memory_gate_test.rs, crates/mlrs-algos/tests/sgd_config_test.rs, crates/mlrs-algos/tests/mbsgd_classifier_test.rs, crates/mlrs-algos/tests/mbsgd_regressor_test.rs, crates/mlrs-algos/tests/linear_svc_test.rs, crates/mlrs-algos/tests/linear_svr_test.rs, crates/mlrs-py/tests/sgd_smoke_test.rs, scripts/gen_oracle.py</files>
  <read_first>
    - scripts/gen_oracle.py lines 782-886 (gen_logistic skeleton), 667-700 (gen_elastic_net), line 44 (SEED=42), _FIXTURE_DIR
    - crates/mlrs-backend/tests/laplacian_test.rs + crates/mlrs-algos/tests/spectral_embedding_test.rs (09-01 #[ignore] scaffold shape: fixture-load + shape assert only)
    - crates/mlrs-backend/tests/memory_gate_test.rs (existing PoolStats gate shape, e.g. laplacian/kernel_matrix memory_gate fns)
    - crates/mlrs-algos/tests/logistic_test.rs + elastic_net_test.rs (oracle test shape: load .npz, fit, compare coef/intercept/predict)
    - 10-VALIDATION.md §Wave 0 Requirements + §Per-Task Verification Map + §Phase Requirements→Test Map
    - 10-RESEARCH.md §Validation Architecture (lines 592-638), §gen_oracle (lines 432-463), §Environment Availability oracle note (line 590)
    - 10-PATTERNS.md §gen_oracle.py (lines 432-464)
    - MEMORY [[oracle-fixture-regen-needs-venv]] — /tmp venv (numpy/scipy/sklearn), PEP 668, regen in ISOLATION
  </read_first>
  <action>
    Add four generators to gen_oracle.py following gen_logistic shape: `gen_mbsgd_classifier` (sklearn SGDClassifier, default loss=hinge but PINNED shuffle=False/tol=0/fixed max_iter K/explicit eta0; emit one CONSTANT-schedule fixture AND one optimal-schedule fixture to isolate the t0 math per A1/Pitfall 3; emit a SECOND log-loss variant for the predict_proba gate; store X/Xq/y/coef/intercept/predict/predict_proba), `gen_mbsgd_regressor` (SGDRegressor squared_error + invscaling pinned; store X/Xq/y/coef/intercept/predict), `gen_linear_svc` (LinearSVC squared_hinge default, dual='auto', intercept_scaling=1.0; store predict labels + coef/intercept), `gen_linear_svr` (LinearSVR squared_epsilon_insensitive + epsilon; store predict + coef/intercept). Each emits both f32 and f64 .npz at seed=42 via the `c(arr)` dtype-cast helper; n_samples small (>= n_features so LinearSVC dual='auto' resolves to primal per RESEARCH §dual='auto'). Per Pitfall 2/7 the fixtures pin shuffle=False/tol=0/fixed max_iter/explicit schedule so neither side early-stops. Run the generators in a /tmp venv (numpy 2.4.6/scipy 1.18.0/sklearn 1.9.0, PEP 668) in ISOLATION so other phases' blobs do not churn; commit the resulting .npz. Create the six test files with Nyquist scaffolds that ONLY load the fixture + assert shape (compile today): sgd_test.rs (`sgd_convex_objective`, `sgd_cpu_launch` #[ignore]; cpu-launch is a Wave-1 SUCCESS CRITERION not a compile-only assert), memory_gate_test.rs (add `memory_gate_sgd_bounded` #[ignore]), sgd_config_test.rs (`try_from_accepts_sklearn_spellings` + `try_from_rejects_unknown` — these MAY be non-ignored if they exercise only the Wave-0 enum surface; `build_rejects_bad_alpha` #[ignore] until Wave-1 fills build() validation), mbsgd_classifier_test.rs (`oracle`/`exact_labels`/`proba`/`default_matches_sklearn` #[ignore]), mbsgd_regressor_test.rs (`oracle`/`default_matches_sklearn` #[ignore]), linear_svc_test.rs (`oracle` #[ignore]), linear_svr_test.rs (`oracle` #[ignore]), and mlrs-py/tests/sgd_smoke_test.rs (construct+fit smoke #[ignore]). Every f64 oracle scaffold carries the `skip_f64_with_log` gate comment verbatim. Update 10-VALIDATION.md Per-Task Verification Map rows for the new tasks (mark ❌ W0 pending) and set wave_0_complete tracking note.
  </action>
  <verify>
    <automated>cargo test -p mlrs-algos -p mlrs-backend --features cpu --no-run 2>&1 | tail -5; ls crates/mlrs-py/tests/sgd_smoke_test.rs; find . -name 'mbsgd_classifier_f64_seed42.npz' 2>/dev/null | head -1; find . -name 'linear_svr_f64_seed42.npz' 2>/dev/null | head -1</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p mlrs-algos -p mlrs-backend --features cpu --no-run` exits 0 (all six test files compile)
    - The four `gen_*` functions exist in `scripts/gen_oracle.py`
    - Committed fixtures exist: `mbsgd_classifier_{f32,f64}_seed42.npz`, `mbsgd_regressor_{f32,f64}_seed42.npz`, `linear_svc_{f32,f64}_seed42.npz`, `linear_svr_{f32,f64}_seed42.npz` (find returns a path)
    - `sgd_config_test.rs` contains `try_from` test(s); `sgd_test.rs` contains `sgd_convex_objective` + `sgd_cpu_launch`; `memory_gate_test.rs` contains `memory_gate_sgd_bounded`
    - Every f64 oracle scaffold references `skip_f64_with_log`
    - 10-VALIDATION.md Per-Task Verification Map updated with the new task rows
  </acceptance_criteria>
  <done>Six test files compile with #[ignore] Nyquist scaffolds asserting fixture-load+shape; four oracle generators added; eight+ pinned .npz fixtures committed; VALIDATION map updated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python → mlrs-py (#[pyclass]) | Untrusted hyperparameter strings/scalars cross here (loss/penalty/lr strings, alpha/eta0/epsilon) — Wave-0 stubs store them verbatim; validation lands in Waves 1/3 |
| mlrs-algos build() → SgdConfig | Data-independent hyperparameters lowered into the prim contract |
| host prim → device launch | Geometry/shape reaches the (future) GATHER kernels |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01-01 | Tampering | BuildError/TryFrom surface | mitigate | Unknown enum string -> typed BuildError::UnknownLoss/Penalty/LearningRate (not a panic); build_err_to_py maps to PyValueError (D-09). Scaffolded this wave; bodies enforced Waves 1/3. |
| T-10-01-02 | Denial of Service | sgd_solve geometry guard | mitigate | sgd_solve stub carries a REAL geometry guard (reject x.len() != n*d) before the todo!() body, so a malformed shape cannot reach a device launch even at scaffold stage. |
| T-10-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (v2 "Cargo unchanged" contract; RESEARCH §Package Legitimacy Audit: no new packages). Oracle gen uses an isolated /tmp venv (numpy/scipy/sklearn) for fixture generation only, not a runtime/build dependency. |

No high-severity threats exist for this scaffold plan: it adds no new dependency, no auth/network surface, and every untrusted-input path is funneled into a typed error rather than a panic. The compute paths that touch device memory are todo!() stubs gated behind real geometry validation.
</threat_model>

<verification>
- `cargo build -p mlrs-algos --features cpu`, `cargo build -p mlrs-kernels`, `cargo build -p mlrs-backend --features cpu` all exit 0.
- `cargo test -p mlrs-algos -p mlrs-backend --features cpu --no-run` exits 0 (all scaffolds compile).
- SharedMemory/INFINITY grep gates clean on `crates/mlrs-kernels/src/sgd.rs` (comment-filtered).
- Eight+ committed pinned-deterministic `.npz` fixtures present.
- D-04 String anti-pattern absent in sgd_config.rs.
</verification>

<success_criteria>
- Workspace compiles on cpu with all Phase-10 stubs (enums, SgdConfig, BuildError, four builders, kernels, prim, PyO3 errors + Unfit blocks).
- TryFrom<&str> accepts sklearn spellings + legacy aliases; unknown maps to BuildError.
- build_err_to_py wired (D-09).
- Six #[ignore] Nyquist test files compile and assert fixture-load+shape only.
- Pinned-deterministic oracle fixtures committed for all four estimators (incl. log-loss + optimal/constant classifier variants).
- All five REQ-IDs and all nine D-01..D-09 decisions are structurally represented (enums/builders D-01..D-06; split-validation signature D-08; BuildError->ValueError D-09).
</success_criteria>

<output>
Create `.planning/phases/10-sgd-linear-svm/10-01-SUMMARY.md` when done.
</output>
