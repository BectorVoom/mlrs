---
phase: 10-sgd-linear-svm
plan: 04
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - crates/mlrs-algos/src/linear/linear_svc.rs
  - crates/mlrs-algos/src/linear/linear_svr.rs
  - crates/mlrs-algos/tests/linear_svc_test.rs
  - crates/mlrs-algos/tests/linear_svr_test.rs
autonomous: true
requirements: [SGDSVM-03, SGDSVM-04]
must_haves:
  truths:
    - "LinearSVC.fit + predict reproduce the pinned sklearn LinearSVC oracle: predict labels EXACT (hard gate), coef_/intercept_ within tolerance"
    - "LinearSVR.fit + predict reproduce the pinned sklearn LinearSVR oracle within tolerance"
    - "dual='auto' is resolved INTERNALLY at fit (never a builder knob, D-07); for the fixtures (n_samples>=n_features) it resolves to primal"
    - "intercept_scaling is handled by the synthetic-feature mechanism (intercept_ = intercept_scaling * synthetic_weight), NOT cd_fit center-then-solve (Pitfall 5)"
    - "builder().build() with no setters reproduces sklearn LinearSVC/SVR defaults (D-03: squared_hinge/squared_epsilon_insensitive, C=1.0, dual='auto')"
    - "Open Question Q1 resolved: the SVM squared-hinge / squared-epsilon-insensitive objective is reached (cd_fit extension or thin SVM-CD), documented in the SUMMARY"
  artifacts:
    - path: "crates/mlrs-algos/src/linear/linear_svc.rs"
      provides: "LinearSVC builder.build() + Fit + PredictLabels (CD-reuse, intercept_scaling, dual='auto')"
      contains: "intercept_scaling"
    - path: "crates/mlrs-algos/src/linear/linear_svr.rs"
      provides: "LinearSVR builder.build() + Fit + Predict (CD-reuse, epsilon)"
      contains: "impl"
    - path: "crates/mlrs-algos/tests/linear_svc_test.rs"
      provides: "oracle / exact_labels / default_matches_sklearn"
      contains: "exact_labels"
    - path: "crates/mlrs-algos/tests/linear_svr_test.rs"
      provides: "oracle / default_matches_sklearn"
      contains: "oracle"
  key_links:
    - from: "crates/mlrs-algos/src/linear/linear_svc.rs"
      to: "coordinate_descent::cd_fit"
      via: "CD-reuse on the synthetic-feature-augmented design (fit_intercept=false)"
      pattern: "cd_fit"
    - from: "crates/mlrs-algos/src/linear/linear_svr.rs"
      to: "elastic_net::predict_linear"
      via: "shared X·coef_+intercept_ predict path"
      pattern: "predict_linear"
---

<objective>
Wire the two linear-SVM estimators: `LinearSVC` (SGDSVM-03) and `LinearSVR` (SGDSVM-04). Per D-07 these reuse the v1 coordinate-descent solver (sklearn LinearSVC/SVR are liblinear CD — a converged optimum), NOT the SGD prim. Resolve `dual='auto'` internally and handle `intercept_scaling` via the synthetic-feature mechanism (not cd_fit centering).

Purpose: Deliver the converged-optimum SVM estimators matching their pinned sklearn oracle, and resolve RESEARCH Open Question Q1 (whether cd_fit expresses the SVM squared-hinge/squared-epsilon-insensitive objective or a thin SVM-CD path is needed). Exact predicted labels are the HARD classifier gate.
Output: Fitted LinearSVC/SVR matching their fixtures, with intercept_scaling recovery, dual='auto' internal resolution, and the D-03 default litmus proven.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-sgd-linear-svm/10-RESEARCH.md
@.planning/phases/10-sgd-linear-svm/10-PATTERNS.md
@.planning/phases/10-sgd-linear-svm/10-VALIDATION.md
@.planning/phases/10-sgd-linear-svm/10-01-PLAN.md
</context>

<artifacts_produced>
## Artifacts this plan fills

**LinearSVC (linear_svc.rs):**
- `LinearSVCBuilder::build()` validation (C>0, valid loss-for-svc {Hinge,SquaredHinge}, valid penalty {L1,L2}); lowers C/intercept_scaling/penalty/loss.
- `impl Fit` — classes_ ±1 remap (copy logistic.rs), dual='auto' resolution (n_samples<n_features & supported → dual else primal, D-07), C↔alpha mapping (l2_reg=1/(C·n), logistic precedent), synthetic-feature augmentation for intercept_scaling, CD-reuse (cd_fit on augmented design with fit_intercept=false) OR thin SVM-CD if Q1 requires, recover intercept_ = intercept_scaling·w_last.
- `impl PredictLabels`.

**LinearSVR (linear_svr.rs):**
- `LinearSVRBuilder::build()` validation (C>0, epsilon>=0, valid loss-for-svr {EpsilonInsensitive,SquaredEpsilonInsensitive}).
- `impl Fit` — same synthetic-feature/CD-reuse path, epsilon tube.
- `impl Predict` — reuse elastic_net::predict_linear.

**Tests:** linear_svc_test (oracle/exact_labels/default_matches_sklearn), linear_svr_test (oracle/default_matches_sklearn).
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: LinearSVC — dual='auto', intercept_scaling, CD-reuse + Q1 spike + oracle</name>
  <files>crates/mlrs-algos/src/linear/linear_svc.rs, crates/mlrs-algos/tests/linear_svc_test.rs</files>
  <behavior>
    - LinearSVC::builder().loss(SquaredHinge).c(1.0).intercept_scaling(1.0)... pinned to the fixture.fit then predict reproduces the fixture's predict labels EXACTLY (hard gate) for f32 + f64.
    - coef_/intercept_ match the fixture within tolerance (f64 strict 1e-5; f32 band).
    - dual='auto' resolves to primal for the fixture (n_samples>=n_features) — internal, never a builder knob.
    - intercept_ == intercept_scaling * synthetic_feature_weight (NOT ȳ - x̄·coef centering).
    - builder().build() with no setters yields sklearn LinearSVC defaults (D-03: loss=SquaredHinge, penalty=L2, C=1.0, dual='auto', intercept_scaling=1.0, max_iter=1000, tol=1e-4).
    - build() rejects C<=0 -> BuildError; rejects EpsilonInsensitive -> InvalidLossForEstimator.
  </behavior>
  <read_first>
    - crates/mlrs-algos/src/linear/coordinate_descent.rs lines 72-214 (cd_fit), 122-205 (center-then-solve intercept that LinearSVC must NOT use), CD_DEFAULT_TOL/MAX_ITER
    - crates/mlrs-algos/src/linear/logistic.rs lines 239-282 (classes_ ±1 remap), 284-286 (C↔l2_reg=1/(C·n) mapping), 586-601 (predict_labels roundtrip)
    - crates/mlrs-algos/src/linear/elastic_net.rs (struct + predict precedent)
    - crates/mlrs-algos/tests/logistic_test.rs (classifier oracle test shape)
    - 10-PATTERNS.md §linear_svc.rs+linear_svr.rs (lines 319-347)
    - 10-RESEARCH.md §LinearSVC/SVR+dual='auto'+CD-reuse (lines 396-408), §Pitfall-5 (intercept_scaling lines 469-472), §Open Question Q1 (lines 562-565), §Defaults table
    - 10-CONTEXT.md D-03/D-07/D-08
  </read_first>
  <action>
    SPIKE Q1 FIRST: fit cd_fit against the LinearSVC fixture early; determine whether the existing Lasso/ElasticNet soft-threshold CD reaches the squared-hinge SVM optimum, or whether a thin SVM-CD per-coordinate update is needed. Document the resolution in the SUMMARY (option a: extend cd_fit with the SVM loss; option b: add a small SVM-CD path host-orchestrated over the device dot/axpy). Then fill LinearSVCBuilder::build() validation (C>0 → InvalidC-equivalent BuildError; valid loss-for-svc {Hinge,SquaredHinge}, EpsilonInsensitive/SquaredEpsilonInsensitive → InvalidLossForEstimator; valid penalty). Implement Fit: copy logistic.rs classes_ distinct-sorted ±1 remap; resolve dual='auto' internally (`if n_samples < n_features && supported { dual } else { primal }`, D-07 — never exposed as a builder knob); map C↔alpha via l2_reg=1/(C·n) (logistic precedent); when fit_intercept, APPEND a synthetic feature column of value intercept_scaling to the design, solve via the chosen CD path with fit_intercept=false on the augmented matrix, and recover intercept_ = intercept_scaling · w_last (Pitfall 5 — do NOT use cd_fit's center-then-solve intercept path); store coef_ (drop the synthetic column)/intercept_ device-resident. Implement PredictLabels (margin sign → classes_[·] as i32). Un-ignore + fill linear_svc_test::oracle (coef/intercept band), exact_labels (HARD gate), default_matches_sklearn (D-03 litmus). f64 skip_f64_with_log; f32 band.
  </action>
  <verify>
    <automated>cargo test -p mlrs-algos --features cpu linear_svc_test -- --nocapture 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p mlrs-algos --features cpu linear_svc_test::exact_labels` exits 0 (predict labels EXACT, hard gate)
    - `cargo test -p mlrs-algos --features cpu linear_svc_test::oracle` exits 0 (coef/intercept within band)
    - `cargo test -p mlrs-algos --features cpu linear_svc_test::default_matches_sklearn` exits 0 (D-03 litmus)
    - `linear_svc.rs` contains `intercept_scaling` and the synthetic-feature recovery (intercept_scaling * w_last), and resolves dual='auto' internally (no dual builder setter)
    - Q1 resolution (cd_fit extension vs thin SVM-CD) documented in the SUMMARY
    - f64 cases reference skip_f64_with_log; f32 documented band
  </acceptance_criteria>
  <done>LinearSVC matches the pinned oracle (exact labels hard gate), handles intercept_scaling via the synthetic-feature mechanism, resolves dual='auto' internally, and Q1 is resolved + documented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: LinearSVR — epsilon tube, CD-reuse, Predict + oracle</name>
  <files>crates/mlrs-algos/src/linear/linear_svr.rs, crates/mlrs-algos/tests/linear_svr_test.rs</files>
  <behavior>
    - LinearSVR::builder().loss(SquaredEpsilonInsensitive).c(1.0).epsilon(e)... pinned to the fixture.fit then predict matches the fixture's predict within tolerance (f64 strict 1e-5; f32 band).
    - coef_/intercept_ match the fixture within tolerance.
    - intercept_ via the same synthetic-feature mechanism as LinearSVC (intercept_scaling * w_last).
    - builder().build() with no setters yields sklearn LinearSVR defaults (D-03: loss=SquaredEpsilonInsensitive, C=1.0, epsilon=0.0, max_iter=1000, tol=1e-4).
    - build() rejects epsilon<0 / C<=0 -> BuildError; rejects Hinge -> InvalidLossForEstimator.
  </behavior>
  <read_first>
    - crates/mlrs-algos/src/linear/linear_svc.rs (the synthetic-feature/CD path from Task 1 — reuse the resolution)
    - crates/mlrs-algos/src/linear/elastic_net.rs lines 180-268 (Predict via predict_linear pub(crate))
    - crates/mlrs-algos/tests/elastic_net_test.rs (regressor oracle test shape)
    - 10-PATTERNS.md §linear_svc.rs+linear_svr.rs (lines 319-347, LinearSVR = regressor: Fit + Predict)
    - 10-RESEARCH.md §LinearSVC/SVR (CD-reuse), §SGD-Math (squared_epsilon_insensitive subgradient), §Defaults table
    - 10-CONTEXT.md D-03/D-07
  </read_first>
  <action>
    Fill LinearSVRBuilder::build() validation (C>0, epsilon>=0, valid loss-for-svr {EpsilonInsensitive,SquaredEpsilonInsensitive}; Hinge/Log/SquaredHinge → InvalidLossForEstimator). Implement Fit using the SAME synthetic-feature + CD path resolved in Task 1 (epsilon-tube objective; intercept_ = intercept_scaling · w_last; do NOT use cd_fit centering). Implement Predict via `predict_linear(self.coef_.as_ref(), self.intercept_.as_ref(), "linear_svr", pool, x, shape)`. Un-ignore + fill linear_svr_test::oracle (coef/intercept/predict within tol) + default_matches_sklearn (D-03 litmus). f64 skip_f64_with_log; f32 band.
  </action>
  <verify>
    <automated>cargo test -p mlrs-algos --features cpu linear_svr_test -- --nocapture 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p mlrs-algos --features cpu linear_svr_test::oracle` exits 0 (predict + coef/intercept within tolerance)
    - `cargo test -p mlrs-algos --features cpu linear_svr_test::default_matches_sklearn` exits 0 (D-03 litmus)
    - `linear_svr.rs` Predict delegates to `predict_linear`; intercept via synthetic-feature mechanism
    - f64 cases reference skip_f64_with_log; f32 documented band
  </acceptance_criteria>
  <done>LinearSVR matches the pinned oracle (epsilon tube), reuses the synthetic-feature CD path + predict_linear, and the D-03 default litmus holds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| builder().build() | C/epsilon/penalty/loss validated here (D-08) |
| fit() | geometry/labels + synthetic-feature augmentation (untrusted shape) validated before CD launch |
| estimator → cd_fit/SVM-CD | augmented design + C↔alpha mapping reach the converged solver |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04-01 | Tampering | build() C/epsilon/loss validation | mitigate | C<=0 / epsilon<0 / invalid loss-for-estimator → typed BuildError (D-08) before any solve. |
| T-10-04-02 | Tampering / DoS | fit() geometry + synthetic-feature augmentation | mitigate | x.len()!=n*d and <2 classes → typed AlgoError before CD launch; the synthetic column append is a bounded host op on validated dimensions. |
| T-10-04-03 | Denial of Service | unbounded CD iteration | mitigate | cd_fit/SVM-CD honors CD_DEFAULT_MAX_ITER cap; NotConverged at the cap (reuse map_cd_error). |
| T-10-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs (v2 Cargo-unchanged; RESEARCH §Package Legitimacy Audit: none). |

No high-severity threats: untrusted hyperparameters and shapes are funneled into typed BuildError/AlgoError before any unsafe solve; the SVMs reuse the already-validated CD bookkeeping (capped iteration).
</threat_model>

<verification>
- `cargo test -p mlrs-algos --features cpu linear_svc_test linear_svr_test` green.
- LinearSVC exact-labels HARD gate passes; coef/intercept within band.
- intercept_scaling synthetic-feature recovery verified (not centering).
- D-03 default litmus passes for both.
- Q1 resolution documented in the SUMMARY.
- f64 cases skip_f64_with_log; f32 band; rocm(f32) target builds at wave merge.
</verification>

<success_criteria>
- SGDSVM-03 met: LinearSVC predict matches the pinned oracle (exact labels hard gate), intercept_scaling correct, dual='auto' internal.
- SGDSVM-04 met: LinearSVR predict matches the pinned oracle (epsilon tube).
- D-03 (default==sklearn) and D-07 (implicit solver + dual='auto') proven; Q1 resolved.
</success_criteria>

<output>
Create `.planning/phases/10-sgd-linear-svm/10-04-SUMMARY.md` when done (document the Q1 resolution: cd_fit extension vs thin SVM-CD).
</output>
