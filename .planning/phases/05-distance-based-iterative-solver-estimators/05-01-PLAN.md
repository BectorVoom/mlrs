---
phase: 05-distance-based-iterative-solver-estimators
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/mlrs-algos/src/traits.rs
  - crates/mlrs-algos/src/error.rs
  - crates/mlrs-algos/src/lib.rs
  - crates/mlrs-algos/src/cluster/mod.rs
  - crates/mlrs-algos/src/neighbors/mod.rs
  - crates/mlrs-kernels/src/lib.rs
  - crates/mlrs-kernels/src/topk.rs
  - crates/mlrs-kernels/src/kmeans.rs
  - crates/mlrs-kernels/src/dbscan.rs
  - crates/mlrs-kernels/src/coordinate.rs
  - crates/mlrs-kernels/src/lbfgs.rs
  - crates/mlrs-backend/src/prims/mod.rs
  - crates/mlrs-backend/src/prims/topk.rs
  - crates/mlrs-backend/src/prims/kmeans.rs
  - crates/mlrs-backend/src/prims/dbscan.rs
  - crates/mlrs-backend/src/prims/coordinate_descent.rs
  - crates/mlrs-backend/src/prims/lbfgs.rs
  - scripts/gen_oracle.py
  - tests/fixtures/kmeans_f32_seed42.npz
  - tests/fixtures/kmeans_f64_seed42.npz
  - tests/fixtures/dbscan_f32_seed42.npz
  - tests/fixtures/dbscan_f64_seed42.npz
  - tests/fixtures/knn_f32_seed42.npz
  - tests/fixtures/knn_f64_seed42.npz
  - tests/fixtures/lasso_f32_seed42.npz
  - tests/fixtures/lasso_f64_seed42.npz
  - tests/fixtures/elastic_net_f32_seed42.npz
  - tests/fixtures/elastic_net_f64_seed42.npz
  - tests/fixtures/logistic_binary_f32_seed42.npz
  - tests/fixtures/logistic_binary_f64_seed42.npz
  - tests/fixtures/logistic_multi_f32_seed42.npz
  - tests/fixtures/logistic_multi_f64_seed42.npz
  - crates/mlrs-backend/tests/topk_test.rs
  - crates/mlrs-backend/tests/kmeanspp_test.rs
  - crates/mlrs-backend/tests/lloyd_test.rs
  - crates/mlrs-backend/tests/dbscan_mask_test.rs
  - crates/mlrs-backend/tests/cd_test.rs
  - crates/mlrs-backend/tests/lbfgs_test.rs
  - crates/mlrs-algos/tests/kmeans_test.rs
  - crates/mlrs-algos/tests/dbscan_test.rs
  - crates/mlrs-algos/tests/nearest_neighbors_test.rs
  - crates/mlrs-algos/tests/knn_classifier_test.rs
  - crates/mlrs-algos/tests/knn_regressor_test.rs
  - crates/mlrs-algos/tests/lasso_test.rs
  - crates/mlrs-algos/tests/elastic_net_test.rs
  - crates/mlrs-algos/tests/logistic_test.rs
autonomous: true
requirements: [LINEAR-03, LINEAR-04, LINEAR-05, CLUSTER-01, CLUSTER-02, NEIGH-01, NEIGH-02, NEIGH-03]
nyquist_compliant: true

must_haves:
  truths:
    - "The mlrs-algos and mlrs-backend crates compile with the new trait/error surface and all Wave-0 test stubs on --features cpu and --features rocm"
    - "Every Phase-5 oracle fixture is a committed .npz blob loadable via mlrs_core::load_npz (not regenerated at test time)"
    - "An i32 DeviceArray round-trips through the pool, confirming D-06 needs zero pool/bridge changes"
    - "The new kernel + prim module files exist as compiling stubs with their pub mod declarations, so the five primitive plans (02-06) stay file-disjoint and parallel-safe"
  artifacts:
    - path: "crates/mlrs-algos/src/traits.rs"
      provides: "PredictLabels, KNeighbors, PredictProba traits (D-05/D-07)"
      contains: "trait PredictLabels"
    - path: "crates/mlrs-algos/src/error.rs"
      provides: "InvalidK, InvalidEps, InvalidMinSamples, InvalidL1Ratio, InvalidC, NotConverged variants"
      contains: "InvalidK"
    - path: "scripts/gen_oracle.py"
      provides: "gen_kmeans (injected init), gen_dbscan, gen_knn, gen_lasso, gen_elastic_net, gen_logistic generators"
      contains: "def gen_kmeans"
    - path: "crates/mlrs-kernels/src/lib.rs"
      provides: "pub mod topk/kmeans/dbscan/coordinate/lbfgs declarations + their stub module files"
      contains: "pub mod topk"
  key_links:
    - from: "crates/mlrs-algos/src/lib.rs"
      to: "cluster, neighbors modules"
      via: "pub mod"
      pattern: "pub mod cluster"
    - from: "tests/fixtures/kmeans_f32_seed42.npz"
      to: "gen_oracle.py gen_kmeans"
      via: "committed blob carrying injected init"
      pattern: "init"
---

<objective>
Land the Phase-5 Wave-0 scaffold: extend the estimator trait surface (D-05 label-returning trait, D-07 KNeighbors + PredictProba traits), extend `AlgoError` with the new hyperparameter-guard variants (D-06 — InvalidK/InvalidEps/InvalidMinSamples/InvalidL1Ratio/InvalidC/NotConverged), add the new `cluster`/`neighbors` module index stubs, CREATE the five new kernel + five new prim module files as compiling stubs with their `pub mod` declarations (so the primitive plans 02-06 stay file-disjoint and never touch the shared `lib.rs`/`prims/mod.rs`), extend `scripts/gen_oracle.py` with the six new sklearn-oracle generators (KMeans with INJECTED init per D-09, DBSCAN, KNN, Lasso, ElasticNet, LogReg binary+multiclass), commit the resulting `.npz` blobs, and create every Wave-0 prim + estimator oracle test as a compiling `#[ignore]` stub that asserts fixture-load + shape only.

Purpose: unblock all five primitive plans (02-06) and all four estimator plans (07-10) so they land file-disjoint and parallel-safe — the Phase-3/4 Wave-0 scaffold precedent (04-01 owned lib.rs + module stubs). Confirms D-06 i32 DeviceArray support so no later plan is surprised.
Output: extended `traits.rs`/`error.rs`/`lib.rs` + new `cluster/mod.rs`/`neighbors/mod.rs`, 5 stub kernel files + 5 stub prim files with registrations, 6 gen_oracle generators, 14 committed fixtures, 14 ignored test stubs (6 prim + 8 estimator).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-distance-based-iterative-solver-estimators/05-CONTEXT.md
@.planning/phases/05-distance-based-iterative-solver-estimators/05-RESEARCH.md
@.planning/phases/05-distance-based-iterative-solver-estimators/05-PATTERNS.md
@AGENTS.md
</context>

<artifacts_this_phase_produces>
This plan creates these new symbols/files (downstream drift-verification MUST exclude them as newly-created):
- Traits: `PredictLabels<F>`, `KNeighbors<F>`, `PredictProba<F>` in `mlrs-algos/src/traits.rs`
- `AlgoError` variants: `InvalidK`, `InvalidEps`, `InvalidMinSamples`, `InvalidL1Ratio`, `InvalidC`, `NotConverged`
- Modules: `mlrs_algos::cluster`, `mlrs_algos::neighbors` (index stubs only)
- Kernel module stubs: `mlrs_kernels::{topk, kmeans, dbscan, coordinate, lbfgs}` (declared + empty-body files)
- Prim module stubs: `mlrs_backend::prims::{topk, kmeans, dbscan, coordinate_descent, lbfgs}` (declared + empty-body files)
- gen_oracle.py functions: `gen_kmeans`, `gen_dbscan`, `gen_knn`, `gen_lasso`, `gen_elastic_net`, `gen_logistic`
- Fixtures: `kmeans_{f32,f64}`, `dbscan_{f32,f64}`, `knn_{f32,f64}`, `lasso_{f32,f64}`, `elastic_net_{f32,f64}`, `logistic_binary_{f32,f64}`, `logistic_multi_{f32,f64}` (seed42 .npz)
- Test files: `topk_test.rs`, `kmeanspp_test.rs`, `lloyd_test.rs`, `dbscan_mask_test.rs`, `cd_test.rs`, `lbfgs_test.rs` (mlrs-backend); `kmeans_test.rs`, `dbscan_test.rs`, `nearest_neighbors_test.rs`, `knn_classifier_test.rs`, `knn_regressor_test.rs`, `lasso_test.rs`, `elastic_net_test.rs`, `logistic_test.rs` (mlrs-algos)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Extend traits.rs (D-05/D-07) + error.rs (D-06) + lib.rs/cluster/neighbors module stubs</name>
  <files>crates/mlrs-algos/src/traits.rs, crates/mlrs-algos/src/error.rs, crates/mlrs-algos/src/lib.rs, crates/mlrs-algos/src/cluster/mod.rs, crates/mlrs-algos/src/neighbors/mod.rs</files>
  <read_first>
    - crates/mlrs-algos/src/traits.rs (the existing Fit/Predict/Transform surface to mirror — bound `<F: Float + CubeElement + Pod>`, `&mut self` fit, device-resident return)
    - crates/mlrs-algos/src/error.rs (existing AlgoError variants — copy the `#[error("...")]` struct-variant shape of InvalidAlpha/InvalidNComponents exactly)
    - crates/mlrs-algos/src/lib.rs (the module index + `pub use` block — this file IS edited here, Wave-0 owns it like 04-01 did)
    - crates/mlrs-algos/src/linear/mod.rs (module-index doc-comment style for the new cluster/mod.rs, neighbors/mod.rs)
    - 05-PATTERNS.md §"traits.rs (MODIFY)" + §"error.rs (MODIFY)" (the exact trait signatures and variant shapes)
  </read_first>
  <action>
    In traits.rs add three traits mirroring the existing `Predict<F>` bound and signature shape: `PredictLabels<F>` with `fn predict_labels(&self, pool, x, shape) -> Result<DeviceArray<ActiveRuntime, i32>, AlgoError>` (D-05/D-06 — integer labels, used by KMeans.predict and KNeighborsClassifier.predict); `KNeighbors<F>` with `fn kneighbors(&self, pool, x, shape, k) -> Result<(DeviceArray<ActiveRuntime, F>, DeviceArray<ActiveRuntime, i32>), AlgoError>` returning (distances, indices) (D-07); `PredictProba<F>` with `fn predict_proba(&self, pool, x, shape) -> Result<DeviceArray<ActiveRuntime, F>, AlgoError>` (D-07, per-class fractions). Each carries the same `where F: Float + CubeElement + Pod` bound and the same doc-comment density as the existing traits. Import nothing new beyond what traits.rs already imports.
    In error.rs add six struct variants in the existing InvalidAlpha style: `InvalidK { estimator: &'static str, k: usize, n_samples: usize }`, `InvalidEps { estimator: &'static str, eps: f64 }`, `InvalidMinSamples { estimator: &'static str, min_samples: usize }`, `InvalidL1Ratio { estimator: &'static str, l1_ratio: f64 }`, `InvalidC { estimator: &'static str, c: f64 }`, `NotConverged { estimator: &'static str, max_iter: usize }`. Each with a concrete `#[error("...")]` message naming the valid range (k ≤ n_samples, eps ≥ 0, min_samples ≥ 1, 0 ≤ l1_ratio ≤ 1, C > 0, max_iter reached).
    In lib.rs add `pub mod cluster; pub mod neighbors;` to the module index and `pub use traits::{PredictLabels, KNeighbors, PredictProba};` to the re-export block (keep the doc note that estimator plans edit only their own file + their module-index mod.rs, never lib.rs). Create `cluster/mod.rs` and `neighbors/mod.rs` as doc-commented index stubs mirroring `linear/mod.rs` (no `pub mod <estimator>;` lines yet — the estimator plans add those), documenting that KMeans/DBSCAN (cluster) and NearestNeighbors/KNeighborsClassifier/KNeighborsRegressor (neighbors) land here.
  </action>
  <verify>
    <automated>cargo build -p mlrs-algos --features cpu 2>&1 | tail -5; grep -q "trait PredictLabels" crates/mlrs-algos/src/traits.rs && grep -q "trait KNeighbors" crates/mlrs-algos/src/traits.rs && grep -q "trait PredictProba" crates/mlrs-algos/src/traits.rs && grep -q "InvalidK" crates/mlrs-algos/src/error.rs && grep -q "NotConverged" crates/mlrs-algos/src/error.rs && grep -q "pub mod cluster" crates/mlrs-algos/src/lib.rs && echo SCAFFOLD_OK</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p mlrs-algos --features cpu` exits 0
    - `cargo build -p mlrs-algos --features rocm` exits 0
    - traits.rs contains `trait PredictLabels`, `trait KNeighbors`, `trait PredictProba`, each bound `F: Float + CubeElement + Pod`
    - error.rs contains the six new variants InvalidK/InvalidEps/InvalidMinSamples/InvalidL1Ratio/InvalidC/NotConverged
    - lib.rs contains `pub mod cluster`, `pub mod neighbors`, and re-exports the three new traits
    - cluster/mod.rs and neighbors/mod.rs exist with no `pub mod <estimator>` lines (estimator plans add them)
  </acceptance_criteria>
  <done>traits.rs/error.rs/lib.rs extended; cluster/neighbors module stubs created; both feature builds green.</done>
</task>

<task type="auto">
  <name>Task 2: Create 5 kernel + 5 prim stub module files with pub mod registrations (file-disjoint enabler for 02-06)</name>
  <files>crates/mlrs-kernels/src/lib.rs, crates/mlrs-kernels/src/topk.rs, crates/mlrs-kernels/src/kmeans.rs, crates/mlrs-kernels/src/dbscan.rs, crates/mlrs-kernels/src/coordinate.rs, crates/mlrs-kernels/src/lbfgs.rs, crates/mlrs-backend/src/prims/mod.rs, crates/mlrs-backend/src/prims/topk.rs, crates/mlrs-backend/src/prims/kmeans.rs, crates/mlrs-backend/src/prims/dbscan.rs, crates/mlrs-backend/src/prims/coordinate_descent.rs, crates/mlrs-backend/src/prims/lbfgs.rs</files>
  <read_first>
    - crates/mlrs-kernels/src/lib.rs (the `pub mod` + `pub use` re-export block — this plan adds the five `pub mod` lines; the prim plans add their own `pub use` of the symbols they create OR callers use fully-qualified paths like the existing `prims::cholesky::cholesky_solve`)
    - crates/mlrs-backend/src/prims/mod.rs (the existing `pub mod cholesky; pub mod distance; ...` index to extend)
    - crates/mlrs-kernels/src/smoke.rs (the minimal module-file shape — doc comment + content; an empty-body module compiles)
    - 05-PATTERNS.md §"Recommended Project Structure" (the kernel/prim file names) + the shared-kernel/wrapper contracts
  </read_first>
  <action>
    Create the five kernel files (`topk.rs`, `kmeans.rs`, `dbscan.rs`, `coordinate.rs`, `lbfgs.rs` in mlrs-kernels/src/) and five prim files (`topk.rs`, `kmeans.rs`, `dbscan.rs`, `coordinate_descent.rs`, `lbfgs.rs` in mlrs-backend/src/prims/) as COMPILING STUBS: each carries only a doc-comment header naming which plan fills it (02→topk, 03→kmeans, 04→dbscan, 05→coordinate/CD, 06→lbfgs) and the artifact it will provide — NO `#[cube]` kernel and NO function yet (an empty module body is valid Rust). Add the five `pub mod topk; pub mod kmeans; pub mod dbscan; pub mod coordinate; pub mod lbfgs;` lines to `mlrs-kernels/src/lib.rs` and the five `pub mod topk; pub mod kmeans; pub mod dbscan; pub mod coordinate_descent; pub mod lbfgs;` lines to `mlrs-backend/src/prims/mod.rs`. Do NOT add `pub use` re-exports for symbols that don't exist yet — the prim plans add their own `pub use` inside extending these files (the plans 02-06 only touch their own stub file body, never lib.rs/mod.rs, so they stay file-disjoint). Both crates must still build.
  </action>
  <verify>
    <automated>cargo build -p mlrs-kernels 2>&1 | tail -3 && cargo build -p mlrs-backend --features cpu 2>&1 | tail -3 && grep -q "pub mod topk" crates/mlrs-kernels/src/lib.rs && grep -q "pub mod coordinate_descent" crates/mlrs-backend/src/prims/mod.rs && echo STUB_MODS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p mlrs-kernels` exits 0; `cargo build -p mlrs-backend --features cpu` exits 0
    - mlrs-kernels/src/lib.rs declares `pub mod topk/kmeans/dbscan/coordinate/lbfgs`
    - mlrs-backend/src/prims/mod.rs declares `pub mod topk/kmeans/dbscan/coordinate_descent/lbfgs`
    - The ten stub files exist, each a compiling doc-commented empty module (no `#[cube]`/fn yet — grep: no `#[cube]` in the stubs)
    - `cargo build -p mlrs-backend --features rocm` exits 0
  </acceptance_criteria>
  <done>Ten stub module files created + registered; both crates build; plans 02-06 can now fill their own file without touching lib.rs/prims/mod.rs.</done>
</task>

<task type="auto">
  <name>Task 3: Extend gen_oracle.py with six sklearn generators + commit 14 fixtures (D-09 injected init)</name>
  <files>scripts/gen_oracle.py, tests/fixtures/kmeans_f32_seed42.npz, tests/fixtures/kmeans_f64_seed42.npz, tests/fixtures/dbscan_f32_seed42.npz, tests/fixtures/dbscan_f64_seed42.npz, tests/fixtures/knn_f32_seed42.npz, tests/fixtures/knn_f64_seed42.npz, tests/fixtures/lasso_f32_seed42.npz, tests/fixtures/lasso_f64_seed42.npz, tests/fixtures/elastic_net_f32_seed42.npz, tests/fixtures/elastic_net_f64_seed42.npz, tests/fixtures/logistic_binary_f32_seed42.npz, tests/fixtures/logistic_binary_f64_seed42.npz, tests/fixtures/logistic_multi_f32_seed42.npz, tests/fixtures/logistic_multi_f64_seed42.npz</files>
  <read_first>
    - scripts/gen_oracle.py (the existing gen_ridge / gen_cholesky / gen_pca generators + the `c()` C-contiguous wrapper + `_FIXTURE_DIR` + the `main()` registration block at the tail — import sklearn INSIDE each fn, use `np.random.default_rng(SEED)`)
    - 05-RESEARCH.md §"Supporting (the numerical contract)" table (the EXACT sklearn entry points + constants each generator must reproduce) and §"Code Examples"
    - 05-PATTERNS.md §"scripts/gen_oracle.py (MODIFY)" (the gen_kmeans template with injected init)
    - MEMORY.md "oracle fixture regen needs venv" (regen uses /tmp/oracle-venv, PEP 668 — committed blobs not test-time)
  </read_first>
  <action>
    Add to gen_oracle.py, each importing its sklearn estimator inside the function and wrapping every stored array with the existing `c()` C-contiguous helper:
    `gen_kmeans` — fit `sklearn.cluster.KMeans(n_clusters=K, init=<fixed array>, n_init=1, max_iter=300, tol=1e-4)` with the SAME fixed `init` centers supplied to mlrs later (D-09); store `X`, `init`, `centers`(cluster_centers_), `labels`(labels_, int), `inertia`. Choose a small well-separated blob (e.g. 30×4, K=3) so Lloyd converges identically up to permutation.
    `gen_dbscan` — fit `sklearn.cluster.DBSCAN(eps=<val>, min_samples=<val>, metric='euclidean', algorithm='brute')`; store `X`, `eps`, `min_samples`, `labels`(noise=-1, int), `core_sample_indices`(int). Pick eps/min_samples giving ≥1 cluster + ≥1 noise point + ≥1 border point (Pitfall 7 determinism).
    `gen_knn` — fit `sklearn.neighbors.NearestNeighbors(n_neighbors=k, algorithm='brute', metric='euclidean')` on train X, query test Xq; store `X`, `Xq`, `k`, `distances`(sqrt Euclidean), `indices`(int), plus `y_class`(int targets) and `y_reg`(float targets) + classifier `predict`/`predict_proba` and regressor `predict` outputs (one fixture serves NEIGH-01/02/03). Use DISTINCT distances (Pitfall 8 — avoid tie ambiguity).
    `gen_lasso` — fit `sklearn.linear_model.Lasso(alpha=<val>, fit_intercept=True, tol=1e-4, max_iter=1000)`; store `X`, `y`, `alpha`, `coef`(coef_, including exact zeros), `intercept`. Pick a design that yields a genuinely sparse coef_ (some exact zeros, Pitfall 1).
    `gen_elastic_net` — fit `sklearn.linear_model.ElasticNet(alpha=<val>, l1_ratio=<val>, fit_intercept=True, tol=1e-4, max_iter=1000)`; store `X`, `y`, `alpha`, `l1_ratio`, `coef`, `intercept`.
    `gen_logistic` — TWO fixtures: binary (2-class) and multiclass (3-class). Fit `sklearn.linear_model.LogisticRegression(solver='lbfgs', C=<val>, max_iter=100, tol=1e-4, fit_intercept=True)` (do NOT pass deprecated multi_class); store `X`, `Xq`, `y`, `C`, `coef`(coef_), `intercept`(intercept_), `predict`(int), `predict_proba` for Xq. predict_proba/predict are the PRIMARY gate (gauge-invariant, Pitfall 5); coef_ is the looser secondary.
    Register both dtype calls for each in `main()` after the Phase-4 block (f32 = rocm gate, f64 = cpu gate). Run the generators against /tmp/oracle-venv and commit the 14 resulting .npz blobs.
  </action>
  <verify>
    <automated>test -f /tmp/oracle-venv/bin/python && /tmp/oracle-venv/bin/python scripts/gen_oracle.py 2>&1 | grep -E "kmeans|dbscan|knn|lasso|elastic|logistic" | tail -20; ls tests/fixtures/kmeans_f32_seed42.npz tests/fixtures/dbscan_f64_seed42.npz tests/fixtures/knn_f32_seed42.npz tests/fixtures/lasso_f64_seed42.npz tests/fixtures/elastic_net_f32_seed42.npz tests/fixtures/logistic_binary_f32_seed42.npz tests/fixtures/logistic_multi_f64_seed42.npz && echo FIXTURES_OK</automated>
  </verify>
  <acceptance_criteria>
    - gen_oracle.py contains `def gen_kmeans`, `def gen_dbscan`, `def gen_knn`, `def gen_lasso`, `def gen_elastic_net`, `def gen_logistic`
    - gen_kmeans stores an `init` array (D-09 injected init) and labels/centers/inertia
    - All 14 fixture .npz files exist under tests/fixtures/ (kmeans/dbscan/knn/lasso/elastic_net f32+f64; logistic_binary + logistic_multi f32+f64)
    - Every stored array passes through the `c()` C-contiguous wrapper (grep confirms `c(` usage in each new generator)
    - `main()` registers both f32 and f64 calls for each generator
  </acceptance_criteria>
  <done>Six generators added; 14 fixtures regenerated in /tmp/oracle-venv and committed; KMeans fixture carries injected init.</done>
</task>

<task type="auto">
  <name>Task 4: Create 14 Wave-0 oracle test stubs (#[ignore], fixture-load + shape only) + i32 DeviceArray confirmation</name>
  <files>crates/mlrs-backend/tests/topk_test.rs, crates/mlrs-backend/tests/kmeanspp_test.rs, crates/mlrs-backend/tests/lloyd_test.rs, crates/mlrs-backend/tests/dbscan_mask_test.rs, crates/mlrs-backend/tests/cd_test.rs, crates/mlrs-backend/tests/lbfgs_test.rs, crates/mlrs-algos/tests/kmeans_test.rs, crates/mlrs-algos/tests/dbscan_test.rs, crates/mlrs-algos/tests/nearest_neighbors_test.rs, crates/mlrs-algos/tests/knn_classifier_test.rs, crates/mlrs-algos/tests/knn_regressor_test.rs, crates/mlrs-algos/tests/lasso_test.rs, crates/mlrs-algos/tests/elastic_net_test.rs, crates/mlrs-algos/tests/logistic_test.rs</files>
  <read_first>
    - crates/mlrs-backend/tests/cholesky_test.rs (the Phase-4 Wave-0 scaffold precedent — `fixture(name)` workspace-root resolver, `#[ignore]` stubs asserting load + shape only, NO reference to non-existent prim symbols, f64 capability gate)
    - crates/mlrs-algos/tests/ridge_test.rs (the estimator test harness shape: load_npz → fixture resolver → assert_close import)
    - crates/mlrs-core/src/oracle.rs (load_npz signature) and crates/mlrs-core/src/label_perm.rs (best_match_accuracy for the clustering stubs)
    - 05-VALIDATION.md §"Per-Task Verification Map" (the exact test file names + the command each maps to) and §"Wave 0 Requirements"
    - 05-PATTERNS.md §"Oracle tests" (the prim-oracle and estimator-oracle replication notes)
  </read_first>
  <action>
    Create each test file with the cholesky_test.rs/ridge_test.rs scaffold shape: a `fixture(name)` workspace-root path resolver, the `host_to_f64`/`from_f64` size-dispatch helpers, and one `#[ignore]`-marked `fixture_loads` test per file that calls `mlrs_core::load_npz(fixture("<blob>"))` and asserts the expected keys + array shapes are well-formed — and NOTHING that references a not-yet-existing prim (`topk`, `lloyd`, `cd`, `lbfgs`, …) or estimator (`KMeans`, `DBSCAN`, …) symbol, so the test crates compile today. Map each file to its fixture: topk_test/nearest_neighbors_test/knn_classifier_test/knn_regressor_test → knn fixtures; kmeanspp_test/lloyd_test/kmeans_test → kmeans fixtures (assert the `init` key exists, D-09); dbscan_mask_test/dbscan_test → dbscan fixtures; cd_test/lasso_test → lasso fixtures; cd_test also asserts elastic_net; elastic_net_test → elastic_net; lbfgs_test/logistic_test → logistic_binary + logistic_multi fixtures. Add the full set of additional VALIDATION.md test-function NAMES as further `#[ignore]` stubs in each file (e.g. topk_test gets the lowest-index-tie and k-distances stub names) that likewise assert only fixture/shape, so plans 02-10 simply remove `#[ignore]` and wire the real assertion. Include the `if capability::skip_f64_with_log() { return; }` gate in every f64 stub.
    Add ONE non-ignored test in topk_test.rs as `i32_device_array_roundtrips` that builds a `DeviceArray::<ActiveRuntime, i32>::from_host(&mut pool, &[-1i32, 0, 5, 42])`, reads it back with `to_host`, and `assert_eq!`s the round-trip — confirming D-06 i32 support with zero pool/bridge changes (including the DBSCAN noise = -1 value).
  </action>
  <verify>
    <automated>cargo test --features cpu -p mlrs-backend --no-run 2>&1 | tail -3 && cargo test --features cpu -p mlrs-algos --no-run 2>&1 | tail -3 && cargo test --features cpu -p mlrs-backend topk_test i32_device_array_roundtrips 2>&1 | tail -5 && echo STUBS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --features cpu -p mlrs-backend --no-run` exits 0 (all 6 prim test stubs compile)
    - `cargo test --features cpu -p mlrs-algos --no-run` exits 0 (all 8 estimator test stubs compile)
    - `cargo test --features cpu -p mlrs-backend i32_device_array_roundtrips` passes (D-06 confirmed, incl. -1)
    - Every test file's `fixture_loads` stub is `#[ignore]` and references only `load_npz` + shape asserts (grep: no `KMeans`/`DBSCAN`/`topk::`/`lbfgs::` symbol refs in the stub bodies)
    - kmeans_test.rs/kmeanspp_test.rs/lloyd_test.rs assert the `init` key is present (D-09)
    - `cargo build -p mlrs-backend --features rocm --tests` and `-p mlrs-algos --features rocm --tests` exit 0
  </acceptance_criteria>
  <done>14 compiling ignored test stubs created (6 prim + 8 estimator); i32 DeviceArray round-trip passes; both crates' test targets build on cpu+rocm.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → estimator constructor / fit | Untrusted hyperparameters (k, eps, min_samples, alpha, l1_ratio, C) and geometry enter here; this plan defines the typed AlgoError variants that reject them |
| gen_oracle.py → committed .npz | Build-time-only fixture generation in /tmp/oracle-venv; never runs in CI or links into the Rust crate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Tampering | AlgoError variants for untrusted hyperparameters | mitigate | Define InvalidK/InvalidEps/InvalidMinSamples/InvalidL1Ratio/InvalidC/NotConverged so estimator plans (07-10) reject out-of-range values BEFORE any unsafe launch (ASVS V5); this plan only defines the variants, consumers enforce |
| T-05-01-02 | Tampering | i32 DeviceArray element type (D-06) | mitigate | Confirm via round-trip test that the byte-keyed pool admits i32 with no new unsafe path; -1 noise value validated |
| T-05-01-SC | Tampering | npm/pip/cargo installs | accept | This plan adds ZERO external packages (RESEARCH Package Legitimacy Audit: none); fixtures generated against already-installed /tmp/oracle-venv, never linked |
</threat_model>

<verification>
- `cargo build -p mlrs-algos --features cpu` and `--features rocm` green (trait/error/module scaffold compiles)
- `cargo build -p mlrs-kernels` + `-p mlrs-backend --features cpu` green (stub modules + registrations compile)
- All 14 fixtures committed and loadable via load_npz
- `cargo test --features cpu -p mlrs-backend --no-run` + `-p mlrs-algos --no-run` green (all stubs compile)
- i32 DeviceArray round-trip test passes (D-06)
</verification>

<success_criteria>
- Trait surface extended with PredictLabels/KNeighbors/PredictProba; AlgoError extended with 6 hyperparameter-guard variants
- cluster/neighbors module index stubs exist; lib.rs wires them
- 10 stub kernel/prim module files created + registered so plans 02-06 stay file-disjoint
- 6 gen_oracle generators added; 14 fixtures committed (KMeans carries injected init per D-09)
- 14 ignored oracle test stubs compile on cpu+rocm; i32 round-trip confirmed
</success_criteria>

<output>
Create `.planning/phases/05-distance-based-iterative-solver-estimators/05-01-SUMMARY.md` when done
</output>
