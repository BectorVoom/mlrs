---
phase: 05-distance-based-iterative-solver-estimators
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - crates/mlrs-kernels/src/topk.rs
  - crates/mlrs-backend/src/prims/topk.rs
  - crates/mlrs-backend/tests/topk_test.rs
autonomous: true
requirements: [NEIGH-01, NEIGH-02, NEIGH-03]
nyquist_compliant: true

must_haves:
  truths:
    - "A top-k primitive returns the k smallest distances + their i32 indices per query row with a lowest-index tie-break, validated standalone against numpy within 1e-5"
    - "The top-k prim validates geometry before any unsafe launch and returns a typed PrimError on bad k/shape"
  artifacts:
    - path: "crates/mlrs-kernels/src/topk.rs"
      provides: "feature-free #[cube] partial-select-k kernel, lowest-index tie-break"
      contains: "#[cube"
    - path: "crates/mlrs-backend/src/prims/topk.rs"
      provides: "top_k launch wrapper (validate-before-launch, device-resident out)"
      contains: "pub fn"
  key_links:
    - from: "crates/mlrs-backend/src/prims/topk.rs"
      to: "crates/mlrs-kernels/src/topk.rs"
      via: "kernel::launch::<F, ActiveRuntime>"
      pattern: "launch::"
    - from: "crates/mlrs-backend/tests/topk_test.rs"
      to: "tests/fixtures/knn_f32_seed42.npz"
      via: "load_npz oracle compare"
      pattern: "load_npz"
---

<objective>
Land the NEW top-k selection primitive (D-02): a partial-select-k `#[cube]` kernel over the pairwise squared-Euclidean distance rows (Phase-2 PRIM-03), returning the k smallest distances + their k indices per query row with a LOWEST-INDEX tie-break (the Phase-2 `argmin_rows` convention). The launch wrapper validates geometry before any unsafe launch, threads an optional reused out buffer, and returns device-resident outputs. Shared by all three KNN estimators (08) — so it lands and passes its standalone oracle BEFORE any estimator consumes it (D-01 primitive-first).

The `topk.rs` (kernel) and `prims/topk.rs` (wrapper) stub files + their `pub mod` registrations were created by 05-01; this plan FILLS those stub files (and adds any `pub use` re-export inside them), so it never touches the shared `lib.rs`/`prims/mod.rs` — staying file-disjoint from the sibling Wave-2 prim plans.

Purpose: NEIGH-01/02/03 brute-force k-nearest selection. The genuinely-new device work for the neighbors family.
Output: `topk.rs` kernel + `prims/topk.rs` wrapper + the de-`#[ignore]`-d `topk_test.rs` oracle.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-distance-based-iterative-solver-estimators/05-RESEARCH.md
@.planning/phases/05-distance-based-iterative-solver-estimators/05-PATTERNS.md
@AGENTS.md
@crates/mlrs-backend/src/prims/distance.rs
@crates/mlrs-backend/src/prims/reduce.rs
</context>

<artifacts_this_phase_produces>
- Kernel: `mlrs_kernels::topk` `#[cube]` partial-select-k function (e.g. `select_k`), `pub use`-re-exported from within topk.rs
- Prim: `mlrs_backend::prims::topk::top_k` launch wrapper
- Test: real assertions in `topk_test.rs` (k-distances, lowest-index tie, sqrt-at-boundary)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: fill the top-k #[cube] kernel (partial-select-k, lowest-index tie-break)</name>
  <files>crates/mlrs-kernels/src/topk.rs</files>
  <read_first>
    - crates/mlrs-kernels/src/topk.rs (the 05-01 stub to fill; already declared `pub mod topk;` in lib.rs)
    - crates/mlrs-kernels/src/reduce.rs (argmin_shared lines ~344-390 — the (value, index) pair carry with the EXACT lowest-index tie rule `if ov < cv {..} else if ov == cv { if oi < ci {..} }`; top-k generalizes k=1 → k)
    - 05-PATTERNS.md §"topk.rs — partial-select-k (D-02)" (the value+index pair-carry excerpt + the shared-kernel contract: SharedMemory comptime size bounded by runtime n, no `continue` in #[cube], scalar args by value, no hardcoded plane width)
    - AGENTS.md §3 + /home/user/Documents/workspace/cubecl_manual/manual/Cubecl/INDEX.md (read before writing #[cube])
  </read_first>
  <behavior>
    - Given a rows×cols distance matrix and k, output rows×k smallest values + rows×k indices per row
    - Tie-break: on equal distance the LOWER column index is selected (D-02)
    - Within a row the k outputs are ordered ascending by distance
    - k=1 reduces to argmin_rows behavior (lowest-index tie)
  </behavior>
  <action>
    Fill the `topk.rs` stub with a feature-free `#[cube]` partial-select-k kernel generic over `<F: Float + CubeElement>`, launched `::launch::<F, R>`. One cube per query row; maintain a k-length running best (value+index) in `SharedMemory::<F>` / a parallel `SharedMemory::<u32>` index buffer with a comptime cap (the reduce.rs 256-cap idiom) bounded at runtime by `k`. Scan the row's `cols` distances; insert each candidate into the running top-k preserving ascending order, applying the EXACT lowest-index tie rule from `argmin_shared` (strictly-smaller wins; on equal value the lower index wins). Write `out_val: &mut Array<F>` (rows×k) and `out_idx: &mut Array<u32>` (rows×k). Scalar args (`rows: u32`, `cols: u32`, `k: u32`) passed by value (no ScalarArg). Use `if`-wrapped branches (no `continue`). Add a `pub use` of the kernel symbol INSIDE topk.rs if needed (do NOT edit lib.rs — 05-01 already declared the module).
  </action>
  <verify>
    <automated>cargo build -p mlrs-kernels 2>&1 | tail -3 && grep -q "#\[cube\]" crates/mlrs-kernels/src/topk.rs && echo TOPK_KERNEL_OK</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p mlrs-kernels` exits 0
    - topk.rs contains a `#[cube]` kernel generic over `<F: Float + CubeElement>` with no backend feature
    - The lowest-index tie rule (`else if ov == cv { if oi < ci ... }`) is present in topk.rs (grep)
    - No hardcoded plane width; SharedMemory size is comptime, bounded by runtime k
    - lib.rs is NOT modified by this plan (05-01 owns the `pub mod` line)
  </acceptance_criteria>
  <done>top-k kernel compiles feature-free; tie-break rule matches argmin_shared; no shared-file edit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: fill the top_k launch wrapper + standalone oracle (k distances + i32 indices, sqrt at boundary)</name>
  <files>crates/mlrs-backend/src/prims/topk.rs, crates/mlrs-backend/tests/topk_test.rs</files>
  <read_first>
    - crates/mlrs-backend/src/prims/topk.rs (the 05-01 stub to fill; already declared `pub mod topk;` in prims/mod.rs)
    - crates/mlrs-backend/src/prims/distance.rs (the launch-wrapper precedent: validate_geometry before unsafe, launch_dims helpers, ArrayArg::from_raw_parts at validated counts, optional `out`, device-resident return, the optional-sqrt boundary)
    - crates/mlrs-backend/src/prims/reduce.rs (argmin_rows — the host index-readback idiom; top_k returns indices similarly)
    - crates/mlrs-backend/tests/cholesky_test.rs (standalone prim oracle scaffold: fixture resolver, f64 skip_f64_with_log gate, assert_close)
    - crates/mlrs-backend/tests/topk_test.rs (the Wave-0 stub from 05-01 to de-#[ignore])
    - 05-RESEARCH.md Pitfall 8 (top-k on SQUARED distance via argpartition; sqrt only on the returned k; tie-break lowest index) + §"Standard Stack" distance prim row
    - 05-PATTERNS.md §"prims/topk.rs" (exact wrapper shape match to distance.rs)
  </read_first>
  <action>
    Fill `prims/topk.rs` with `pub fn top_k<F>(pool, dist: &DeviceArray<..,F>, rows, cols, k, sqrt: bool, out_val: Option<..>, out_idx: Option<..>) -> Result<(DeviceArray<..,F>, DeviceArray<..,u32>), PrimError>` mirroring `distance.rs`: validate `rows*cols == dist.len()`, `1 <= k <= cols` (else `PrimError::ShapeMismatch`) BEFORE any unsafe launch (ASVS V5); acquire out buffers from pool only when None; launch `topk::<kernel>::launch::<F, ActiveRuntime>` with `ArrayArg::from_raw_parts` at validated counts; select on SQUARED distance, then apply the existing optional sqrt over the returned k values only (Pitfall 8). The returned u32 indices become i32 host-side in the KNN consumers (plan 08, D-06). Do NOT edit prims/mod.rs (05-01 owns the `pub mod` line).
    De-`#[ignore]` `topk_test.rs`: load `knn_{f32,f64}_seed42.npz`, run `distance(X_train, Xq, sqrt=false)` then `top_k(.., k, sqrt=true)`, and `assert_close` the returned distances vs the fixture `distances` (sqrt Euclidean) within 1e-5 AND assert the returned indices match the fixture `indices` exactly. Add a constructed-tie invariant case (two equal distances) asserting the LOWER index is returned (Pitfall 8 / A5). f64 cases guarded by `skip_f64_with_log`.
  </action>
  <verify>
    <automated>cargo test --features cpu -p mlrs-backend topk_test 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --features cpu -p mlrs-backend topk_test` exits 0 (no longer ignored)
    - prims/topk.rs validates `1 <= k <= cols` and `rows*cols == dist.len()` before launch (grep PrimError before unsafe)
    - Oracle asserts distances within 1e-5 of fixture `distances` AND indices equal fixture `indices`
    - The constructed-tie case asserts lowest-index selection
    - f64 test bodies guard on `capability::skip_f64_with_log()`
    - prims/mod.rs is NOT modified by this plan (05-01 owns the `pub mod` line)
    - `cargo build -p mlrs-backend --features rocm --tests` exits 0
  </acceptance_criteria>
  <done>top_k wrapper validates-before-launch and returns (distances, indices); topk_test oracle green on cpu(f64), incl. tie invariant; rocm test target builds; no shared-file edit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| estimator → top_k(rows, cols, k) | k and geometry are untrusted; a bad k or rows*cols mismatch must not become an out-of-bounds device read |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Tampering / Info-disclosure | top_k geometry & k | mitigate | validate `1 <= k <= cols` and `rows*cols == dist.len()` → PrimError before `ArrayArg::from_raw_parts` (distance.rs precedent, ASVS V5) |
| T-05-02-02 | Tampering | lowest-index tie determinism | mitigate | replicate argmin_shared's exact `else if ov == cv { if oi < ci }` rule; constructed-tie oracle case pins it |
| T-05-02-SC | Tampering | dependencies | accept | zero new packages (RESEARCH audit: none) |
</threat_model>

<verification>
- `cargo test --features cpu -p mlrs-backend topk_test` green (distances 1e-5, indices exact, tie lowest-index)
- `cargo build -p mlrs-kernels` + `-p mlrs-backend --features rocm --tests` green
</verification>

<success_criteria>
- top-k kernel feature-free, lowest-index tie-break; wrapper validates-before-launch, returns (distances, indices)
- Standalone oracle passes within 1e-5 on cpu(f64) before any KNN estimator consumes it (D-01)
</success_criteria>

<output>
Create `.planning/phases/05-distance-based-iterative-solver-estimators/05-02-SUMMARY.md` when done
</output>
