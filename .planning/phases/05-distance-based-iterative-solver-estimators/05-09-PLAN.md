---
phase: 05-distance-based-iterative-solver-estimators
plan: 09
type: execute
wave: 3
depends_on: ["05-01", "05-05"]
files_modified:
  - crates/mlrs-algos/src/linear/mod.rs
  - crates/mlrs-algos/src/linear/coordinate_descent.rs
  - crates/mlrs-algos/src/linear/lasso.rs
  - crates/mlrs-algos/src/linear/elastic_net.rs
  - crates/mlrs-algos/tests/lasso_test.rs
  - crates/mlrs-algos/tests/elastic_net_test.rs
autonomous: true
requirements: [LINEAR-03, LINEAR-04]
nyquist_compliant: true

must_haves:
  truths:
    - "ElasticNet fits via the shared CD solver (l1_reg=α·l1_ratio·n, l2_reg=α·(1−l1_ratio)·n), center-then-solve intercept, exposing coef_/intercept_ matching sklearn within 1e-5 incl. sparsity"
    - "Lasso is ElasticNet with l1_ratio=1 (l2_reg=0), reproducing sklearn's sparse coef_ incl. exact zeros within 1e-5"
  artifacts:
    - path: "crates/mlrs-algos/src/linear/coordinate_descent.rs"
      provides: "shared CD host helper (penalty mapping + centering + cd_solve)"
      contains: "fn"
    - path: "crates/mlrs-algos/src/linear/elastic_net.rs"
      provides: "ElasticNet<F>: Fit + Predict<F>"
      contains: "impl"
    - path: "crates/mlrs-algos/src/linear/lasso.rs"
      provides: "Lasso<F>: thin wrapper over ElasticNet(l1_ratio=1)"
      contains: "impl"
  key_links:
    - from: "crates/mlrs-algos/src/linear/coordinate_descent.rs"
      to: "mlrs_backend::prims::coordinate_descent::cd_solve"
      via: "penalty-mapped CD solve"
      pattern: "cd_solve"
    - from: "crates/mlrs-algos/tests/lasso_test.rs"
      to: "tests/fixtures/lasso_f32_seed42.npz"
      via: "load_npz oracle compare"
      pattern: "load_npz"
---

<objective>
Land Lasso (LINEAR-03) + ElasticNet (LINEAR-04) on the validated CD primitive (05-05). They SHARE one coordinate-descent helper (D-03 — Lasso = ElasticNet `l1_ratio==1`, do NOT unify with the L-BFGS LogReg solver). The shared `coordinate_descent.rs` helper maps the user-facing `(alpha, l1_ratio)` to sklearn's un-normalized `(l1_reg=α·l1_ratio·n, l2_reg=α·(1−l1_ratio)·n)` (Pitfall 1), centers X/y for the unpenalized intercept (D-13, reusing the Phase-4 center-then-solve), calls `cd_solve`, and recovers `intercept_ = ȳ − x̄·coef_`. `ElasticNet<F>` and `Lasso<F>` both implement `Fit` + `Predict<F>`, exposing `coef_`/`intercept_` matching sklearn within 1e-5 INCLUDING the exact sparsity pattern.

Purpose: LINEAR-03 + LINEAR-04 — the CD iterative-solver linear models.
Output: `linear/coordinate_descent.rs`, `linear/elastic_net.rs`, `linear/lasso.rs`, `linear/mod.rs` (mod lines), 2 oracle tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-distance-based-iterative-solver-estimators/05-RESEARCH.md
@.planning/phases/05-distance-based-iterative-solver-estimators/05-PATTERNS.md
@AGENTS.md
@crates/mlrs-algos/src/linear/ridge.rs
@crates/mlrs-algos/src/linear/mod.rs
@crates/mlrs-algos/tests/ridge_test.rs
</context>

<artifacts_this_phase_produces>
- Helper: `mlrs_algos::linear::coordinate_descent` shared CD fit helper
- Estimators: `mlrs_algos::linear::elastic_net::ElasticNet<F>`, `::lasso::Lasso<F>`
- `linear/mod.rs` adds `pub mod coordinate_descent; pub mod lasso; pub mod elastic_net;`
- Tests: real assertions in `lasso_test.rs`, `elastic_net_test.rs`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: shared coordinate_descent helper (penalty map + center-then-solve) + ElasticNet<F> Fit/Predict + oracle</name>
  <files>crates/mlrs-algos/src/linear/coordinate_descent.rs, crates/mlrs-algos/src/linear/elastic_net.rs, crates/mlrs-algos/src/linear/mod.rs, crates/mlrs-algos/tests/elastic_net_test.rs</files>
  <read_first>
    - crates/mlrs-algos/src/linear/ridge.rs (center-then-solve intercept: host two-pass means + `intercept_ = ȳ − x̄·coef_` + the host_to_f64/f64_to_host helpers — copy the centering verbatim, D-13; device-resident coef_/intercept_; validate-before-launch; Predict<F> GEMM X·coef + intercept broadcast)
    - mlrs_backend::prims::coordinate_descent::cd_solve (from 05-05) + prims::reduce (column means for centering)
    - 05-RESEARCH.md §"_coordinate_descent.py:781-782" (l1_reg=α·l1_ratio·n, l2_reg=α·(1−l1_ratio)·n) + Pitfall 1 (n-scaling) + D-13 (intercept via centering, unpenalized) + §"phase_requirements" LINEAR-04
    - crates/mlrs-algos/src/linear/mod.rs (the "deliberately different solvers, do not unify" doc note to extend) + the Wave-0 elastic_net_test stub
    - 05-PATTERNS.md §"linear/elastic_net.rs" + §"coordinate_descent.rs"
  </read_first>
  <action>
    Add `linear/coordinate_descent.rs` as a SHARED host helper `fn cd_fit<F>(pool, x, y, n, d, alpha, l1_ratio, tol, max_iter) -> Result<(coef DeviceArray, intercept DeviceArray), AlgoError>`: validate `alpha >= 0` (InvalidAlpha), `0 <= l1_ratio <= 1` (InvalidL1Ratio) BEFORE launch (ASVS V5); center X and y host-side (ridge.rs two-pass means, D-13); map `(l1_reg = alpha*l1_ratio*n, l2_reg = alpha*(1-l1_ratio)*n)` (Pitfall 1); call `cd_solve(centered_x, centered_y, .., l1_reg, l2_reg, tol=1e-4, max_iter=1000)`; recover `intercept_ = ȳ − x̄·coef_` (unpenalized, never penalize the intercept). Surface `cd_solve`'s NotConverged as `AlgoError::NotConverged`.
    Add `linear/elastic_net.rs` with `ElasticNet<F>` (fields `alpha`, `l1_ratio`, `fit_intercept`, `max_iter`, `tol`, device-resident `coef_`/`intercept_`) implementing `Fit` (delegates to `cd_fit`) + `Predict<F>` (ridge.rs GEMM X·coef + intercept). Add `pub mod coordinate_descent; pub mod elastic_net;` to linear/mod.rs.
    De-`#[ignore]` `elastic_net_test.rs`: load `elastic_net_{f32,f64}_seed42.npz`, fit with the fixture alpha/l1_ratio, assert `coef_` within 1e-5 (incl. exact zeros) and `intercept_` within 1e-5. f64 guarded by `skip_f64_with_log`.
  </action>
  <verify>
    <automated>cargo test --features cpu -p mlrs-algos elastic_net_test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --features cpu -p mlrs-algos elastic_net_test` exits 0 (no longer ignored)
    - cd_fit validates `alpha>=0` (InvalidAlpha) + `0<=l1_ratio<=1` (InvalidL1Ratio) before launch
    - penalty mapping is `l1_reg=α·l1_ratio·n`, `l2_reg=α·(1−l1_ratio)·n` (grep: `* n_samples` / `* l1_ratio`)
    - intercept via centering (unpenalized); coef_ + intercept_ within 1e-5 incl. exact zeros
    - `cargo build -p mlrs-algos --features rocm --tests` exits 0
  </acceptance_criteria>
  <done>ElasticNet fits via the shared CD helper matching sklearn coef_/intercept_ within 1e-5 incl. sparsity (LINEAR-04).</done>
</task>

<task type="auto">
  <name>Task 2: Lasso<F> = ElasticNet(l1_ratio=1) thin wrapper + lasso oracle</name>
  <files>crates/mlrs-algos/src/linear/lasso.rs, crates/mlrs-algos/src/linear/mod.rs, crates/mlrs-algos/tests/lasso_test.rs</files>
  <read_first>
    - crates/mlrs-algos/src/linear/elastic_net.rs (Task 1 — Lasso delegates to the same cd_fit with l1_ratio=1.0 → l2_reg=0)
    - crates/mlrs-algos/src/linear/mod.rs (the thin-wrapper "do not unify" note style)
    - 05-RESEARCH.md §"phase_requirements" LINEAR-03 (Lasso = l1_ratio=1 → l2_reg=0; reproduce the sparse coef_ incl. exact zeros) + Pitfall 1
    - 05-PATTERNS.md §"linear/lasso.rs" (thin wrapper shape) + the Wave-0 lasso_test stub
  </read_first>
  <action>
    Add `linear/lasso.rs` with `Lasso<F>` (fields `alpha`, `fit_intercept`, `max_iter`, `tol`, device-resident `coef_`/`intercept_`) implementing `Fit` by delegating to the shared `cd_fit` with `l1_ratio = 1.0` (→ `l2_reg = 0`, pure L1) + `Predict<F>` (delegate to the ElasticNet/ridge GEMM path). Document it as the `l1_ratio==1` case of ElasticNet (D-03, do NOT re-implement CD). Add `pub mod lasso;` to linear/mod.rs.
    De-`#[ignore]` `lasso_test.rs`: load `lasso_{f32,f64}_seed42.npz`, fit with the fixture alpha, assert `coef_` within 1e-5 INCLUDING the exact zero/sparsity pattern (Pitfall 1) and `intercept_` within 1e-5. f64 guarded by `skip_f64_with_log`.
  </action>
  <verify>
    <automated>cargo test --features cpu -p mlrs-algos lasso_test 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --features cpu -p mlrs-algos lasso_test` exits 0 (no longer ignored)
    - Lasso delegates to cd_fit with l1_ratio=1.0 (grep: no duplicate CD loop in lasso.rs)
    - coef_ within 1e-5 INCLUDING the exact sparsity (zero) pattern; intercept_ within 1e-5
    - `cargo build -p mlrs-algos --features rocm --tests` exits 0
  </acceptance_criteria>
  <done>Lasso (= ElasticNet l1_ratio=1) reproduces sklearn's sparse coef_ within 1e-5 incl. exact zeros (LINEAR-03).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → Lasso/ElasticNet fit | alpha/l1_ratio + geometry untrusted at the estimator boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09-01 | Tampering | alpha / l1_ratio validation | mitigate | validate `alpha>=0` (InvalidAlpha) + `0<=l1_ratio<=1` (InvalidL1Ratio) before launch (ASVS V5) |
| T-05-09-02 | DoS | non-convergent CD | mitigate | cd_solve max_iter=1000 cap → AlgoError::NotConverged surfaced, never silent NaN (05-05 guard) |
| T-05-09-SC | Tampering | dependencies | accept | zero new packages (RESEARCH audit: none) |
</threat_model>

<verification>
- `cargo test --features cpu -p mlrs-algos lasso_test elastic_net_test` green (coef_ + sparsity + intercept_ 1e-5)
- `cargo build -p mlrs-algos --features rocm --tests` green
</verification>

<success_criteria>
- ElasticNet: shared CD helper, penalty mapping + center-then-solve intercept, coef_/intercept_ within 1e-5 incl. sparsity (LINEAR-04)
- Lasso: l1_ratio=1 thin wrapper, sparse coef_ within 1e-5 incl. exact zeros (LINEAR-03); solvers not unified with LogReg (D-03)
</success_criteria>

<output>
Create `.planning/phases/05-distance-based-iterative-solver-estimators/05-09-SUMMARY.md` when done
</output>
