---
phase: 05-distance-based-iterative-solver-estimators
plan: 05
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - crates/mlrs-kernels/src/coordinate.rs
  - crates/mlrs-backend/src/prims/coordinate_descent.rs
  - crates/mlrs-backend/tests/cd_test.rs
autonomous: true
requirements: [LINEAR-03, LINEAR-04]
nyquist_compliant: true

must_haves:
  truths:
    - "A coordinate-descent step primitive computes sklearn's exact soft-threshold update t = X_j·R + w_j·‖X_j‖² → w_j = sign(t)·max(|t|−l1_reg,0)/(‖X_j‖²+l2_reg) and the residual axpy update, validated against numpy within 1e-5"
    - "The host CD loop reads back exactly one scalar (duality gap) per outer convergence check and reuses solver buffers across iterations (D-10 bounded-allocation)"
  artifacts:
    - path: "crates/mlrs-kernels/src/coordinate.rs"
      provides: "CD soft-threshold + residual axpy #[cube] kernel"
      contains: "#[cube"
    - path: "crates/mlrs-backend/src/prims/coordinate_descent.rs"
      provides: "cd_solve host loop (cyclic CD, 1 scalar gap/iter, tol·‖y‖² stop)"
      contains: "pub fn"
  key_links:
    - from: "crates/mlrs-backend/src/prims/coordinate_descent.rs"
      to: "crates/mlrs-kernels/src/coordinate.rs"
      via: "kernel::launch per-coordinate update"
      pattern: "launch::"
    - from: "crates/mlrs-backend/tests/cd_test.rs"
      to: "tests/fixtures/lasso_f32_seed42.npz"
      via: "load_npz oracle compare"
      pattern: "load_npz"
---

<objective>
Land the NEW coordinate-descent step primitive (D-03 shared CD kernel for Lasso + ElasticNet) plus the host-driven CD loop (D-10). The kernel computes sklearn's EXACT un-normalized soft-threshold update and the residual axpy; the host owns the cyclic loop and reads back exactly ONE scalar (the duality gap) per outer convergence check, reusing solver buffers across iterations. Constants pinned from `_cd_fast.pyx`: `l1_reg = α·l1_ratio·n_samples`, `l2_reg = α·(1−l1_ratio)·n_samples`, `w_j = sign(t)·max(|t|−l1_reg,0)/(‖X_j‖²+l2_reg)`, stop on `gap ≤ tol·‖y‖²` (`tol=1e-4`, `max_iter=1000`). Lands with its standalone oracle BEFORE Lasso/ElasticNet (09) consume it.

Purpose: LINEAR-03 + LINEAR-04 solver core. Reproduces sklearn's exact `coef_` INCLUDING the sparsity pattern (Pitfall 1) and exact gap-stopping (Pitfall 2).
Output: `coordinate.rs` kernel + `prims/coordinate_descent.rs` (cd_solve) + `cd_test.rs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-distance-based-iterative-solver-estimators/05-RESEARCH.md
@.planning/phases/05-distance-based-iterative-solver-estimators/05-PATTERNS.md
@AGENTS.md
@crates/mlrs-backend/src/prims/reduce.rs
@crates/mlrs-backend/src/prims/cholesky.rs
</context>

<artifacts_this_phase_produces>
- Kernel: `mlrs_kernels::coordinate` module — soft-threshold + residual axpy `#[cube]` fn(s)
- Prim: `mlrs_backend::prims::coordinate_descent::cd_solve` (host cyclic loop, 1 scalar gap/iter)
- Test: real assertions in `cd_test.rs`
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: coordinate #[cube] kernel (soft-threshold + residual axpy)</name>
  <files>crates/mlrs-kernels/src/coordinate.rs</files>
  <read_first>
    - crates/mlrs-kernels/src/elementwise.rs (scale ~64-79 — the scalar-`F` per-element map; the residual axpy `R += (w_old − w_new)·X[:,j]` reuses this shape) + center_columns
    - crates/mlrs-kernels/src/reduce.rs (the column-dot accumulation for `t = X_j·R`)
    - 05-RESEARCH.md §"CD soft-threshold update" + §"Code Examples" (the EXACT un-normalized form; l1_reg/l2_reg = α·{l1_ratio,1−l1_ratio}·n) + Pitfall 1
    - 05-PATTERNS.md §"coordinate.rs" + the shared-kernel contract (scalar args by value, no `continue`)
    - AGENTS.md §3 + cubecl manual INDEX.md
  </read_first>
  <behavior>
    - Given X column j, residual R, w_j_old, ‖X_j‖², l1_reg, l2_reg: compute t = X_j·R + w_j_old·‖X_j‖²
    - w_j_new = sign(t)·max(|t|−l1_reg, 0)/(‖X_j‖²+l2_reg); exact zero when |t| ≤ l1_reg (sparsity)
    - If w_j changed: R += (w_j_old − w_j_new)·X[:,j]
    - All within 1e-5 of a numpy soft-threshold reference; exact-zero entries are exactly zero
  </behavior>
  <action>
    Add `coordinate.rs` with feature-free `#[cube]` kernels generic over `<F: Float + CubeElement>`: a residual axpy update (`R[i] += factor * X[i*cols + j]`, the scale per-element shape) and, where it stays device-side, the column dot `t = Σ_i X[i*cols+j]·R[i]` (or reuse the reduce prim host-side). Scalar args `j: u32`, `rows: u32`, `cols: u32`, `factor: F`, `l1_reg: F`, `l2_reg: F` by value. The soft-threshold scalar math (`sign(t)·max(|t|−l1_reg,0)/(norm2_j+l2_reg)`) may be host-side per coordinate (it's one scalar) — keep the n-heavy dot + axpy device-side. Use `if`-wrapped branches (no `continue` — the `norm2_j==0 → skip` guard is an `if`). Fill the 05-01 `coordinate.rs` stub (module already declared); add any `pub use` INSIDE coordinate.rs — do NOT edit lib.rs.
  </action>
  <verify>
    <automated>cargo build -p mlrs-kernels 2>&1 | tail -3 && grep -q "#\[cube\]" crates/mlrs-kernels/src/coordinate.rs && echo CD_KERNEL_OK</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p mlrs-kernels` exits 0; coordinate.rs is feature-free `<F: Float + CubeElement>`
    - lib.rs is NOT modified (05-01 owns the `pub mod` line)
    - The residual axpy and column-dot device math are present; scalar args passed by value
    - No `continue` (the zero-column guard is `if`-wrapped); no hardcoded plane width
  </acceptance_criteria>
  <done>CD soft-threshold + residual axpy kernel compiles feature-free.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: cd_solve host loop (cyclic CD, 1 scalar gap/iter, tol·‖y‖² stop) + cd_test oracle</name>
  <files>crates/mlrs-backend/src/prims/coordinate_descent.rs, crates/mlrs-backend/tests/cd_test.rs</files>
  <read_first>
    - crates/mlrs-backend/src/prims/cholesky.rs (validate→launch→scalar-readback wrapper shape; the `to_host` + `release_into` scalar idiom)
    - crates/mlrs-backend/src/prims/reduce.rs (column SumSq for ‖X_j‖²; the gap dots ‖XᵀR−βw‖_∞, R·R, R·y)
    - 05-RESEARCH.md §"Pattern 2 (host-driven loop)" pseudocode (acquire R/norm2_cols/w ONCE, reuse every iter; cheap host gate then 1 scalar gap readback) + §"CD duality gap" + Pitfall 1/2 + §"_coordinate_descent.py:781-782" mapping
    - 05-PATTERNS.md §"prims/coordinate_descent.rs" + the Wave-0 cd_test stub
  </read_first>
  <action>
    Add `prims/coordinate_descent.rs` with `cd_solve(pool, x, y, n, d, l1_reg, l2_reg, tol, max_iter) -> Result<DeviceArray<..,F> coef, PrimError>` implementing RESEARCH §"Pattern 2": validate `n*d == x.len()`, `y.len() == n` → PrimError before any unsafe launch; acquire `R` (=y at w=0), `norm2_cols` (column SumSq, once), and `w` ONCE before the loop and reuse every iteration (D-10 bounded allocation); run the CYCLIC pass over columns (skip `norm2_cols[j]==0`), computing `t`, the soft-threshold update, and the device residual axpy per coordinate; after each outer pass apply sklearn's cheap host gate (`d_w_max/w_max <= tol` OR last iter), then compute the duality gap (`gap_enet` formulation A) reading back EXACTLY ONE scalar and stop when `gap <= tol*dot(y,y)` (scale tol by ‖y‖² ONCE, Pitfall 2). Cap at `max_iter=1000` → `PrimError::NotConverged` analogue (or return the last iterate; the estimator surfaces AlgoError::NotConverged). Do NOT reproduce sklearn's Gap-Safe screening (Anti-Pattern) — plain cyclic CD reaches the same `coef_`. Fill the 05-01 `prims/coordinate_descent.rs` stub; do NOT edit prims/mod.rs (05-01 owns the `pub mod` line).
    De-`#[ignore]` `cd_test.rs`: this is a prim oracle, so drive it with the Lasso (`l2_reg=0`) and ElasticNet fixtures' (X, y) and the fixture α/l1_ratio mapped to (l1_reg, l2_reg) = (α·l1_ratio·n, α·(1−l1_ratio)·n); assert `cd_solve` coef matches the fixture `coef` within 1e-5 INCLUDING exact-zero entries (sparsity pattern, Pitfall 1). Cover both lasso_{f32,f64} and elastic_net_{f32,f64} fixtures. f64 guarded by `skip_f64_with_log`.
  </action>
  <verify>
    <automated>cargo test --features cpu -p mlrs-backend cd_test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --features cpu -p mlrs-backend cd_test` exits 0 (no longer ignored)
    - cd_solve validates `n*d == x.len()` and `y.len() == n` before unsafe launch
    - coef matches the lasso + elastic_net fixtures within 1e-5 INCLUDING the exact sparsity (zero) pattern
    - tol is scaled by ‖y‖² once (grep: `dot(y` / `* y_norm2` before the loop)
    - Exactly one scalar gap readback per outer convergence check; R/norm2_cols/w acquired once before the loop
    - No Gap-Safe screening reproduced (plain cyclic CD)
    - `cargo build -p mlrs-backend --features rocm --tests` exits 0
  </acceptance_criteria>
  <done>cd_solve reproduces sklearn's coef_ + sparsity within 1e-5 on both lasso + elastic_net fixtures; 1 scalar gap/iter, buffers reused — before the estimators (D-01).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| estimator → cd_solve(n, d, l1_reg, l2_reg, max_iter) | geometry untrusted; a zero column or non-convergent run is a NaN/DoS hazard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-01 | Tampering / Info-disclosure | cd_solve geometry | mitigate | validate `n*d == x.len()`, `y.len() == n` → PrimError before unsafe launch (ASVS V5) |
| T-05-05-02 | DoS | zero-column / non-convergent CD | mitigate | `norm2_cols[j]==0 → skip` guard avoids divide-by-zero; `max_iter=1000` cap → NotConverged, never silent NaN (Pitfall guard) |
| T-05-05-03 | Tampering | per-iteration array readback breaking the memory gate | mitigate | exactly ONE scalar gap readback per outer check; R/w/norm2 reused (D-10); plan 11 gates it |
| T-05-05-SC | Tampering | dependencies | accept | zero new packages (RESEARCH audit: none) |
</threat_model>

<verification>
- `cargo test --features cpu -p mlrs-backend cd_test` green (coef + sparsity 1e-5 on lasso + elastic_net fixtures)
- `cargo build -p mlrs-kernels` + `-p mlrs-backend --features rocm --tests` green
</verification>

<success_criteria>
- CD kernel feature-free; soft-threshold + axpy
- cd_solve reproduces sklearn's coef_ + exact sparsity within 1e-5, 1 scalar gap/iter, buffers reused — before Lasso/ElasticNet (D-01)
</success_criteria>

<output>
Create `.planning/phases/05-distance-based-iterative-solver-estimators/05-05-SUMMARY.md` when done
</output>
