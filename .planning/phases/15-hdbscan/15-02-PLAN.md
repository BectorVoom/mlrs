---
phase: 15-hdbscan
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - scripts/gen_oracle.py
  - crates/mlrs-algos/tests/fixtures/
  - crates/mlrs-algos/tests/hdbscan_test.rs
autonomous: true
requirements: [HDBS-01, HDBS-02, HDBS-03, HDBS-04]
must_haves:
  truths:
    - "Committed .npz fixtures exist for every {euclidean,manhattan,cosine,chebyshev,minkowski,precomputed} x {f32,f64} carrying sklearn labels/probabilities/centroids/medoids + hdbscan 0.8.44 hdb_labels/outlier_scores"
    - "A distinct-MST-edge-weight gate fixture AND a separate tie-heavy + duplicate-point fixture exist per the D-04 exactness design"
    - "Nested-density fixtures exist whose eom/leaf/epsilon/max_cluster_size/alpha non-default outputs genuinely differ from defaults"
    - "hdbscan_test.rs declares every oracle gate fn (labels_match_sklearn, tie_break_exact, probabilities_match, outlier_scores_match, centers_match, selection_knobs, build_validation, memory_gate, edge_cases) wired to fixtures + the pinned-noise matcher"
  artifacts:
    - path: "scripts/gen_oracle.py"
      provides: "gen_hdbscan_* generators (sklearn + hdbscan 0.8.44)"
      contains: "def gen_hdbscan"
    - path: "crates/mlrs-algos/tests/hdbscan_test.rs"
      provides: "the full oracle gate suite replacing the shell convention tests"
      contains: "fn labels_match_sklearn"
  key_links:
    - from: "crates/mlrs-algos/tests/hdbscan_test.rs"
      to: "crates/mlrs-core/src/label_perm.rs"
      via: "best_match_accuracy_pinned_noise for the exact-label gate"
      pattern: "best_match_accuracy_pinned_noise"
    - from: "crates/mlrs-algos/tests/hdbscan_test.rs"
      to: "crates/mlrs-algos/tests/fixtures/hdbscan_*.npz"
      via: "load_npz fixture loader"
      pattern: "load_npz"
---

<objective>
Build all Wave-0 test scaffolding for HDBSCAN: the `gen_hdbscan_*` fixture
generators in `scripts/gen_oracle.py`, the committed `.npz` oracle fixtures, and
the REPLACEMENT of the Phase-12 shell convention tests with the full oracle gate
suite in `hdbscan_test.rs`. No algorithm runs yet — the gate functions reference
the still-trivial `fit` and therefore FAIL (or are `#[ignore]`-pending),
establishing the Nyquist gate every later wave turns green.

Purpose: per VALIDATION Wave 0, the fixtures + generators + gate functions must
exist before the back-end port so each subsequent task has an automated
`cargo test --features cpu --test hdbscan_test <fn>` gate to drive it.
Output: generators, committed fixtures, and the gate suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-hdbscan/15-RESEARCH.md
@.planning/phases/15-hdbscan/15-PATTERNS.md
@.planning/phases/15-hdbscan/15-VALIDATION.md
@crates/mlrs-algos/tests/hdbscan_test.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add gen_hdbscan_* generators and regenerate committed fixtures</name>
  <files>scripts/gen_oracle.py, crates/mlrs-algos/tests/fixtures/</files>
  <read_first>
    - scripts/gen_oracle.py (the file being modified — `gen_dbscan` lines ~684-725 for the dtype-cast `c()` + `np.savez` labels-only convention; `gen_knn_metric` lines ~814-960 for the per-metric loop, metric-in-FILENAME-only rule, and the duplicate-point R-9 design `x[DUP_B]=x[DUP_A]` line ~862; the `main()` per-metric dispatch)
    - .planning/phases/15-hdbscan/15-RESEARCH.md Code Examples ("Fixture generator skeleton" — exact kwargs incl. `copy=True`, `metric_params={'p':3.0}` for minkowski, precomputed via `pairwise_distances`, hdbscan 0.8.44 for `outlier_scores_`) and Common Pitfalls 1/5/6
    - .planning/phases/15-hdbscan/15-PATTERNS.md scripts/gen_oracle.py section (distinct-weight vs tie-heavy vs nested-density designs)
    - crates/mlrs-core/src/oracle.rs (load_npz — decodes only 4/8-byte float arrays, so labels are stored float-valued)
  </read_first>
  <action>
    Add a `gen_hdbscan(seed, dtype, metric, mcs, ms, csm, eps, mxc, alpha, store, structure)`
    generator to `scripts/gen_oracle.py` per the RESEARCH skeleton: fit
    `sklearn.cluster.HDBSCAN` (PRIMARY oracle; set `copy=True` explicitly to pin the
    sklearn-1.10 FutureWarning) and `hdbscan.HDBSCAN` 0.8.44 (for `outlier_scores_` and the
    cross-check), then `np.savez` the arrays `X, labels, probabilities, centroids, medoids`
    (sklearn) plus `hdb_labels, outlier_scores` (hdbscan 0.8.44). For `metric=='minkowski'`
    pass `metric_params={'p':3.0}`; for `metric=='precomputed'` set
    `Xin = pairwise_distances(X, metric='euclidean')` and store that square matrix as `X`.
    The metric + dtype tag rides the FILENAME ONLY
    (`hdbscan_{metric}_{dtype}_seed{seed}.npz`), never an in-blob string. Generate, per metric
    in {euclidean, manhattan, cosine, chebyshev, minkowski, precomputed} x {f32, f64}: a
    DISTINCT-MST-edge-weight blob gate fixture (spread points so MST weights are distinct —
    Pitfall 1 option 2). Additionally generate metric-agnostic specials: one tie-heavy +
    duplicate-point fixture (`x[DUP_B]=x[DUP_A]`, R-9) named `hdbscan_tieheavy_{dtype}_seed*`;
    nested-density fixtures whose `eom` vs `leaf`, `cluster_selection_epsilon>0`,
    `max_cluster_size>0`, and `alpha!=1.0` outputs DEMONSTRABLY differ from defaults (assert
    the divergence in the generator before writing — Pitfall 5); and small edge-case fixtures
    (all-noise, single cluster, n less than min_cluster_size). Wire each into the `main()`
    dispatch. Regenerate the `.npz` blobs in a throwaway `/tmp` venv
    (`python3 -m venv /tmp/hdbscan-oracle-venv`; install `numpy>=1.26`, `scikit-learn==1.9.0`,
    `hdbscan==0.8.44`) per project memory (PEP-668); commit the resulting
    `crates/mlrs-algos/tests/fixtures/hdbscan_*.npz` blobs.
  </action>
  <verify>
    <automated>ls crates/mlrs-algos/tests/fixtures/hdbscan_euclidean_f64_seed*.npz crates/mlrs-algos/tests/fixtures/hdbscan_precomputed_f64_seed*.npz crates/mlrs-algos/tests/fixtures/hdbscan_tieheavy_f64_seed*.npz</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'def gen_hdbscan' scripts/gen_oracle.py` shows the generator
    - All six metrics x {f32,f64} blob fixtures plus the tie-heavy, nested-density, and edge-case fixtures exist under `crates/mlrs-algos/tests/fixtures/`
    - The generator asserts (before writing) that each non-default-knob fixture's labels differ from the default-knob labels (Pitfall 5)
  </acceptance_criteria>
  <done>The gen_hdbscan generator exists and the committed .npz fixtures cover every metric/dtype plus the tie-heavy, nested-density, and edge-case designs with both sklearn and hdbscan-0.8.44 oracle arrays.</done>
</task>

<task type="auto">
  <name>Task 2: Replace shell convention tests with the full oracle gate suite</name>
  <files>crates/mlrs-algos/tests/hdbscan_test.rs</files>
  <read_first>
    - crates/mlrs-algos/tests/hdbscan_test.rs (the file being replaced — KEEP `defaults_equal` and `build_rejects_bad_min_cluster_size`; REMOVE the `fit_roundtrip` all-`-1` test since fit no longer returns all-`-1`; keep the `fit_no_leak` PoolStats idiom as the basis for `memory_gate`)
    - crates/mlrs-algos/tests/kmeans_test.rs (the exact-label + same-permutation value-gate precedent: `fixture()` loader lines ~39-46, `f64_to`/`host_to_f64` casts lines ~48-62, `assert_close` abs-OR-rel ≤1e-5 lines ~66-84, `best_match_accuracy==1.0` shape lines ~140-144, `best_mapping` per-cluster value gate lines ~148-156, `skip_f64_with_log` gate lines ~183-189)
    - .planning/phases/15-hdbscan/15-VALIDATION.md Per-Task Verification Map (the exact gate fn names + automated commands)
    - .planning/phases/15-hdbscan/15-PATTERNS.md hdbscan_test.rs section (which precedent lines to copy verbatim)
  </read_first>
  <action>
    Replace `crates/mlrs-algos/tests/hdbscan_test.rs` with the oracle gate suite, copying the
    `fixture()` loader, `f64_to`/`host_to_f64` casts, and the `assert_close` (abs-OR-rel
    ≤1e-5) helper verbatim from `kmeans_test.rs`. Declare these gate functions, each loading
    its fixture via `mlrs_core::oracle::load_npz` and reading float arrays cast `as i64` for
    labels: `labels_match_sklearn` (per metric x {f32,f64}, asserts
    `mlrs_core::best_match_accuracy_pinned_noise(&labels, &labels_ref) == 1.0`);
    `tie_break_exact` (the tie-heavy + duplicate-point fixture, same pinned-noise gate — the
    D-04 TRUE GATE); `probabilities_match` (per-point `assert_close` vs `probabilities`,
    ≤1e-5); `outlier_scores_match` (per-point `assert_close` vs hdbscan `outlier_scores`,
    ≤1e-5); `centers_match` (`best_mapping` each fitted cluster id to its sklearn id, then
    per-cluster `assert_close` of `centroids`/`medoids` rows, ≤1e-5 — Pitfall 6);
    `selection_knobs` (the nested-density non-default eom/leaf/epsilon/max_cluster_size/alpha
    fixtures, pinned-noise exact gate); `build_validation` (min_samples<1 when Some and
    bad max_cluster_size are rejected at build — extend the kept
    `build_rejects_bad_min_cluster_size`); `memory_gate` (PoolStats re-fit-no-growth +
    sub-quadratic `peak_bytes`, from the kept `fit_no_leak` idiom); `edge_cases` (all-noise,
    single-cluster, n less than min_cluster_size fixtures vs sklearn). Apply the
    `skip_f64_with_log` capability gate verbatim at the top of every f64 test. These functions
    reference symbols the back-end has not built yet (extended `Metric`, score/center
    accessors); mark any that will not COMPILE against the current shell with `#[ignore]` and
    a `// un-ignore in 15-NN` comment so the file compiles green in Wave 0, OR keep them
    compiling-but-failing where the symbols already exist — the suite must compile.
    Invoke `Skill("spike-findings-mlrs")` before writing any test that asserts kernel VALUES
    so the duplicate-point R-9 row assertion is honored.
  </action>
  <verify>
    <automated>cargo test --features cpu --test hdbscan_test -- --list</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --features cpu --test hdbscan_test -- --list` compiles and lists `labels_match_sklearn`, `tie_break_exact`, `probabilities_match`, `outlier_scores_match`, `centers_match`, `selection_knobs`, `build_validation`, `memory_gate`, `edge_cases`
    - `grep -c 'fit_roundtrip' crates/mlrs-algos/tests/hdbscan_test.rs` returns 0 (the all-`-1` shell test is removed)
    - Source assertion: `labels_match_sklearn` and `tie_break_exact` call `best_match_accuracy_pinned_noise`, not the unpinned matcher
  </acceptance_criteria>
  <done>The test file compiles and lists all nine oracle gate functions; the all-`-1` shell test is gone; the exact-label gates use the pinned-noise matcher.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture-gen venv -> committed .npz | Fixtures are generated host-side in a disposable `/tmp` venv and committed as blobs; the test crate only READS them via `load_npz` (no Python at test time). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-02-V5 | Tampering | `.npz` fixture decode at test time | accept | `mlrs_core::oracle::load_npz` decodes only 4/8-byte float arrays from committed, in-repo blobs; no untrusted/network input. Established pattern. |
| T-15-02-SC | Tampering | pip installs (numpy, scikit-learn, hdbscan) in fixture-gen venv | mitigate | All three audited OK in RESEARCH Package Legitimacy Audit (decade-old scikit-learn-ecosystem packages); run only in a throwaway `/tmp` venv, never enter the shipped artifact; pin `hdbscan==0.8.44`, `scikit-learn==1.9.0`. |
</threat_model>

<verification>
- `cargo test --features cpu --test hdbscan_test -- --list` lists all nine gate fns and compiles.
- Fixtures present for all six metrics x {f32,f64} + tie-heavy + nested-density + edge cases.
</verification>

<success_criteria>
The generators, committed fixtures, and the compiling oracle gate suite all exist;
the gates are wired to the pinned-noise matcher and the fixtures and are ready for
each later wave to turn green.
</success_criteria>

## Artifacts this phase produces (cumulative — this plan's slice)

- **New generators:** `gen_hdbscan` (+ per-metric/structure dispatch) in `scripts/gen_oracle.py`.
- **New fixtures:** `crates/mlrs-algos/tests/fixtures/hdbscan_{metric}_{dtype}_seed*.npz` (6 metrics x 2 dtypes), `hdbscan_tieheavy_{dtype}_seed*.npz`, nested-density knob fixtures, edge-case fixtures.
- **New test fns:** `labels_match_sklearn`, `tie_break_exact`, `probabilities_match`, `outlier_scores_match`, `centers_match`, `selection_knobs`, `build_validation`, `memory_gate`, `edge_cases` (replacing the shell `fit_roundtrip`).

<output>
Create `.planning/phases/15-hdbscan/15-02-SUMMARY.md` when done.
</output>
