---
phase: 15-hdbscan
plan: 05
type: execute
wave: 5
depends_on: ["15-04"]
files_modified:
  - crates/mlrs-kernels/src/mutual_reachability.rs
  - crates/mlrs-kernels/src/lib.rs
  - crates/mlrs-algos/src/cluster/hdbscan.rs
autonomous: true
requirements: [HDBS-01, HDBS-02]
must_haves:
  truths:
    - "A SharedMemory-free 2D-GATHER mutual_reachability kernel computes out[i*n+j]=max(core_i,core_j,d_ij/alpha) and is oracle-asserted on VALUES incl. a duplicate-point row (R-9)"
    - "The feature-metric fit calls knn_graph(include_self=true) for core distances, builds the dense distance + mutual-reachability, and runs the host back-end to produce labels"
    - "labels_ are a -1-pinned permutation of sklearn for all 5 feature metrics (euclidean/manhattan/cosine/chebyshev/minkowski) x {f32,f64}"
    - "The device front-end leaves no full n x n device-resident leak (PoolStats memory gate); n*n and n*k allocations are checked_mul-guarded"
  artifacts:
    - path: "crates/mlrs-kernels/src/mutual_reachability.rs"
      provides: "the SharedMemory-free MR GATHER kernel"
      contains: "pub fn mutual_reachability"
    - path: "crates/mlrs-algos/src/cluster/hdbscan.rs"
      provides: "feature-metric device front-end wiring in fit"
      contains: "knn_graph"
  key_links:
    - from: "crates/mlrs-algos/src/cluster/hdbscan.rs"
      to: "crates/mlrs-backend/src/prims/knn_graph.rs"
      via: "knn_graph(include_self=true) for core distances"
      pattern: "include_self"
    - from: "crates/mlrs-algos/src/cluster/hdbscan.rs"
      to: "crates/mlrs-kernels/src/mutual_reachability.rs"
      via: "MR kernel launch on the dense distance block"
      pattern: "mutual_reachability"
---

<objective>
Land the device front-end (HDBS-01): a SharedMemory-free 2D-GATHER
`mutual_reachability` kernel and the feature-metric `fit` wiring that reuses the
Phase-13 KNN prim (`include_self=true`) for core distances, builds the dense distance +
mutual-reachability, and feeds the Wave-3 host back-end — extending the exact-label gate
from precomputed to all 5 feature metrics x {f32,f64} (D-03), with a PoolStats memory gate.

Purpose: this closes D-01/D-03 (all 5 feature metrics under the exact gate). The kernel
MUST obey every cpu-MLIR landmine — it is the only device code in this phase.
Output: mutual_reachability.rs kernel, its re-export + backend launch wrapper, and the
feature-metric `fit` path.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-hdbscan/15-RESEARCH.md
@.planning/phases/15-hdbscan/15-PATTERNS.md
@crates/mlrs-algos/src/cluster/hdbscan.rs
@crates/mlrs-backend/src/prims/knn_graph.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author the SharedMemory-free mutual_reachability GATHER kernel</name>
  <files>crates/mlrs-kernels/src/mutual_reachability.rs, crates/mlrs-kernels/src/lib.rs</files>
  <read_first>
    - crates/mlrs-kernels/src/distance.rs (the proven cpu-MLIR-safe 2D GATHER shape — `manhattan_dist`/`chebyshev_dist` lines ~66-126, the statement-form running-max idiom lines ~117-123, the doc-comment contract lines ~12-45, the `pub use` re-export line)
    - .claude/skills/spike-findings-mlrs/references/cpu-mlir-kernel-authoring.md (the BANNED list: SharedMemory/Atomic/F::INFINITY/mutable-bool/shift-loop; statement-form `if`; scalars by value in cubecl 0.10; never a bare 1D ABSOLUTE_POS launch — FINDING 002-A)
    - .planning/phases/15-hdbscan/15-PATTERNS.md mutual_reachability.rs section (the exact kernel signature, launch_dims_2d shape, host-launch wrapper idiom from knn_graph.rs::compute_tile_distance lines ~286-323)
    - crates/mlrs-backend/src/prims/knn_graph.rs (launch_dims_2d lines ~481-490, the `unsafe ArrayArg::from_raw_parts` 2-arg-by-value idiom + SAFETY comment)
  </read_first>
  <behavior>
    - mutual_reachability(d, core, out, rows_x, rows_y, alpha): out[i*rows_y+j] = max(core[i], core[j], d[i*rows_y+j]/alpha) for guarded i<rows_x, j<rows_y
    - On a duplicate-point input row (R-9), the MR values match the host reference exactly (not just non-panic)
    - Runs on cpu-MLIR f64 without panic and on rocm f32; f64-on-rocm skips-with-log
  </behavior>
  <action>
    Invoke `Skill("spike-findings-mlrs")` FIRST (mandatory — kernel authoring). Create
    `crates/mlrs-kernels/src/mutual_reachability.rs` with a `#[cube(launch)] pub fn
    mutual_reachability<F: Float + CubeElement>(d, core, out, rows_x: u32, rows_y: u32, alpha: F)`
    that computes `out[(i*rows_y+j)] = max(core[i], core[j], d[(i*rows_y+j)]/alpha)` using the
    STATEMENT-form running-max (`let mut acc = d.../alpha; if ci>acc {acc=ci;} if cj>acc
    {acc=cj;}`) — NEVER an if-expression in value position. Use 2D `ABSOLUTE_POS_X/Y`,
    `CubeDim {x:16,y:16}`, ceiling-div counts, guarded `if i<rows_x { if j<rows_y {...} }` via
    `knn_graph.rs::launch_dims_2d` verbatim. BANNED entirely: SharedMemory, Atomic,
    `F::INFINITY`, mutable-bool scans, shift-loops, bare 1D `ABSOLUTE_POS` launch. `alpha`
    passes BY VALUE (cubecl 0.10, no `ScalarArg`). Re-export via the existing
    `pub use distance::{...}`-style line in `crates/mlrs-kernels/src/lib.rs`.
  </action>
  <verify>
    <automated>cargo test --features cpu -p mlrs-kernels mutual_reachability</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --features cpu -p mlrs-kernels mutual_reachability` passes (kernel value test incl. a duplicate-point row, R-9)
    - `grep -Ec 'SharedMemory|Atomic|INFINITY' crates/mlrs-kernels/src/mutual_reachability.rs` (filtering comments) == 0
    - `grep -n 'pub fn mutual_reachability' crates/mlrs-kernels/src/lib.rs` shows the re-export
  </acceptance_criteria>
  <done>The MR kernel exists, is SharedMemory-free, runs on cpu-MLIR f64, and matches the host reference on a duplicate-point row.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the feature-metric device front-end into fit; memory gate</name>
  <files>crates/mlrs-algos/src/cluster/hdbscan.rs</files>
  <read_first>
    - .planning/phases/15-hdbscan/15-RESEARCH.md Pattern 1 (core distance = column `min_samples-1` of `knn_graph(include_self=true)`), Pitfall 3 (cosine dense matrix vs the kNN-only set — the dense Prim needs all pairs), Pitfall 2 (Variant A for cosine, Variant B for the other four), Open Question 3 (memory gate shape: Variant B keeps n x d resident, Variant A needs n x n)
    - .planning/phases/15-hdbscan/15-PATTERNS.md hdbscan.rs fit section (the device-stage->to_host->host-walk->from_host shape) + the Host-side validation before unsafe launch shared pattern (checked_mul overflow guards)
    - crates/mlrs-backend/src/prims/knn_graph.rs (the `knn_graph(..., include_self=true)` entry point returning ascending `(n,k)` distances, and validate_geometry lines ~411-476)
    - crates/mlrs-algos/src/cluster/hdbscan.rs (the precomputed `fit` branch from 15-03/15-04 — add the feature-metric branch alongside it, routing Variant A for Cosine vs Variant B for the other four)
    - crates/mlrs-algos/tests/hdbscan_test.rs (the memory_gate PoolStats expectation)
  </read_first>
  <behavior>
    - For euclidean/manhattan/chebyshev/minkowski: core dist from knn_graph(include_self=true) column min_samples-1; MST via Variant B (source-tracking, recompute pairwise) — labels exact vs sklearn
    - For cosine: build the dense n x n cosine distance matrix, MR via the kernel, MST via Variant A — labels exact vs sklearn (Pitfall 3)
    - memory_gate: the device front-end peak_bytes stays sub-quadratic for Variant B (no n x n device-resident); re-fit does not grow live_bytes
  </behavior>
  <action>
    Add the feature-metric branch to `Hdbscan::fit` (the non-`Precomputed` metrics), mirroring
    DBSCAN's device-stage -> `to_host` -> host walk -> `from_host` shape. Validate geometry +
    `checked_mul` on `n*k` and (Variant A only) `n*n` before any launch. Call
    `knn_graph(x, k=min_samples, include_self=true, metric)` and read `core_dist[i] =
    distances[i*min_samples + (min_samples-1)]`. Route by metric: Cosine -> build the dense
    n x n cosine distance matrix (`1-cos` via L2-normalized GEMM, matching sklearn
    `pairwise_distances('cosine')` to ≤1e-5 — Pitfall 3), launch the `mutual_reachability`
    kernel, and run Variant A (dense) MST; the other four metrics -> Variant B
    (`mst_from_data_matrix`, recompute pairwise on the host from the n x d data, no n x n
    resident). Feed the resulting hierarchy through the Wave-3 host back-end (condense ->
    stability -> select -> labelling -> probabilities) and `from_host` the fitted state. Set the
    PoolStats memory gate assertion in `memory_gate` to the established build-failing convention
    (Variant-B device front-end peak sub-quadratic; re-fit no live_bytes growth). Invoke
    `Skill("spike-findings-mlrs")` before any kernel launch and to honor R-9 in the cosine/MR
    value path.
  </action>
  <verify>
    <automated>cargo test --features cpu --test hdbscan_test labels_match_sklearn memory_gate</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --features cpu --test hdbscan_test labels_match_sklearn` passes for all 5 feature metrics x {f32,f64} (precomputed already green from 15-04) under the pinned-noise gate
    - `cargo test --features cpu --test hdbscan_test memory_gate` passes (sub-quadratic device peak for Variant B; no re-fit growth)
    - `grep -c 'include_self' crates/mlrs-algos/src/cluster/hdbscan.rs` >= 1 (the prim is called with self-counted core distances)
  </acceptance_criteria>
  <done>All 5 feature metrics hold the exact-label gate, the device front-end has no n x n leak, and the MR kernel is wired for the cosine/dense path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host fit -> device launch | Geometry/k validated host-side before any `unsafe` kernel launch (ASVS V5, knn_graph::validate_geometry precedent). |
| MR kernel device read | Out-of-bounds device read on bad shape/k is the primary device hazard. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-05-V5 | Tampering/DoS | mutual_reachability kernel device read | mitigate | Validate geometry + bound u32 launch dims host-side before the `unsafe` launch (T-13-06 precedent); guarded `if i<rows_x { if j<rows_y {...} }` in-kernel. |
| T-15-05-OVF | DoS | n*n (Variant A) / n*k allocations | mitigate | `checked_mul` before `pool.acquire` (existing prim pattern); Variant B avoids the n x n entirely (n x d resident). |
| T-15-05-MIS | Tampering | cpu-MLIR silent miscompile (cross-loop accumulator) | mitigate | MR kernel is a per-element GATHER with NO cross-sibling accumulator (FINDING 002-B); SharedMemory-free; the oracle asserts VALUES incl. a duplicate-point row (R-9), not just non-panic. |
| T-15-05-SC | Tampering | npm/pip/cargo installs | mitigate | No new crate dependency added (kernel uses existing cubecl pin); nothing to audit. |
</threat_model>

<verification>
- `cargo test --features cpu --test hdbscan_test labels_match_sklearn memory_gate` green for all 5 feature metrics.
- MR kernel SharedMemory-free, value-gated on a duplicate-point row (R-9).
- rocm(f32) spot-check; f64-on-rocm skips-with-log (manual per VALIDATION).
</verification>

<success_criteria>
All 5 feature metrics hold the exact-label gate, the MR kernel is cpu-MLIR-safe and
value-correct, and the device front-end leaves no n x n leak.
</success_criteria>

## Artifacts this phase produces (cumulative — this plan's slice)

- **New kernel:** `mlrs_kernels::mutual_reachability` (`#[cube(launch)]`, SharedMemory-free 2D GATHER) + its `pub use`.
- **New behavior:** feature-metric `Hdbscan::fit` device front-end (knn_graph `include_self=true` core distances -> Variant A/B MST routing -> host back-end), all 5 metrics exact, PoolStats memory gate.

<output>
Create `.planning/phases/15-hdbscan/15-05-SUMMARY.md` when done.
</output>
