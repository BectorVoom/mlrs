---
phase: 15-hdbscan
plan: 06
type: execute
wave: 6
depends_on: ["15-05"]
files_modified:
  - crates/mlrs-algos/src/cluster/hdbscan/glosh.rs
  - crates/mlrs-algos/src/cluster/hdbscan/centers.rs
  - crates/mlrs-algos/src/cluster/hdbscan.rs
autonomous: true
requirements: [HDBS-03, HDBS-04]
must_haves:
  truths:
    - "GLOSH outlier_scores_ are computed via the condensed tree with upward death-propagation and (lambda_max - lambda)/lambda_max, matching hdbscan 0.8.44 within 1e-5"
    - "store_centers='centroid' produces centroids_ (probability-weighted mean per cluster) and 'medoid' produces medoids_ (strength-weighted min-total-distance member), both within 1e-5 vs sklearn under the SAME label permutation"
    - "store_centers errors on Precomputed (feature-array only, sklearn parity); centers iterate ascending cluster id 0..n_clusters"
  artifacts:
    - path: "crates/mlrs-algos/src/cluster/hdbscan/glosh.rs"
      provides: "GLOSH outlier_scores"
      contains: "outlier_scores"
    - path: "crates/mlrs-algos/src/cluster/hdbscan/centers.rs"
      provides: "centroid + medoid"
      contains: "weighted_cluster_center"
  key_links:
    - from: "crates/mlrs-algos/src/cluster/hdbscan.rs"
      to: "crates/mlrs-algos/src/cluster/hdbscan/glosh.rs"
      via: "fit computes outlier_scores_ from the condensed tree"
      pattern: "outlier_scores"
    - from: "crates/mlrs-algos/src/cluster/hdbscan.rs"
      to: "crates/mlrs-algos/src/cluster/hdbscan/centers.rs"
      via: "fit computes centroids_/medoids_ when store_centers is set"
      pattern: "weighted_cluster_center"
---

<objective>
Land the two HDBSCAN differentiators/parity features: GLOSH `outlier_scores_`
(HDBS-03, gated vs hdbscan 0.8.44 — sklearn lacks it) and `store_centers` →
`centroids_`/`medoids_` (HDBS-04, gated vs sklearn). Both are pure host passes over the
already-built condensed tree and fitted labels/probabilities.

Purpose: completes the full HDBSCAN surface under the ≤1e-5 value gates (D-06/D-07/D-08).
Output: glosh.rs, centers.rs, the fitted fields/accessors, and the `fit` wiring.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-hdbscan/15-RESEARCH.md
@.planning/phases/15-hdbscan/15-PATTERNS.md
@crates/mlrs-algos/src/cluster/hdbscan.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port GLOSH outlier_scores_ (HDBS-03)</name>
  <files>crates/mlrs-algos/src/cluster/hdbscan/glosh.rs, crates/mlrs-algos/src/cluster/hdbscan.rs</files>
  <read_first>
    - .planning/phases/15-hdbscan/15-RESEARCH.md Pattern 7 (GLOSH outlier_scores — `max_lambdas`, root=`parent_array.min()`, UPWARD death-propagation reverse pass, `(lambda_max - lambda)/lambda_max`, 0.0 when lambda_max==0 or lambda non-finite; the key difference from get_probabilities) and Assumptions A1 (the committed 0.8.44 fixture is the real gate)
    - .planning/phases/15-hdbscan/15-PATTERNS.md glosh.rs section (sibling to stability.rs — same condensed-tree pass)
    - crates/mlrs-algos/src/cluster/hdbscan/stability.rs (the max_lambdas + condensed-tree iteration from 15-04)
    - crates/mlrs-algos/src/cluster/hdbscan.rs (the `labels_`/`probabilities_` fitted-field pattern to mirror for `outlier_scores_`)
  </read_first>
  <behavior>
    - outlier_scores propagate deaths upward (a child's death floods its parent if larger), then result[point] = (deaths[parent] - lambda)/deaths[parent], 0.0 when deaths[parent]==0 or lambda non-finite
    - outlier_scores_match: per-point ≤1e-5 vs the hdbscan-0.8.44 `outlier_scores` fixture array
  </behavior>
  <action>
    Create `crates/mlrs-algos/src/cluster/hdbscan/glosh.rs` porting the hdbscan-library
    `outlier_scores` line-for-line from RESEARCH Pattern 7: `deaths = max_lambdas(tree)`,
    `root = parent_array.min()`, the UPWARD death-propagation reverse pass
    (`if deaths[cluster] > deaths[parent] { deaths[parent] = deaths[cluster] }` iterating the
    condensed tree in reverse), then for each child point below `root`:
    `result[point] = (deaths[parent] - lambda)/deaths[parent]`, with `0.0` when
    `deaths[parent]==0` or `lambda` is non-finite. Index deaths by the point's PARENT (not its
    assigned cluster) — the GLOSH/probabilities difference. Add an
    `outlier_scores_: Option<DeviceArray<ActiveRuntime, F>>` fitted field + a `Fitted`-only
    `outlier_scores()` accessor to `hdbscan.rs`, and compute+`from_host` it in `fit` (both
    precomputed and feature paths) from the condensed tree.
  </action>
  <verify>
    <automated>cargo test --features cpu --test hdbscan_test outlier_scores_match</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --features cpu --test hdbscan_test outlier_scores_match` passes (≤1e-5 vs hdbscan 0.8.44)
    - `grep -c 'outlier_scores' crates/mlrs-algos/src/cluster/hdbscan/glosh.rs` >= 1
    - Source assertion: deaths indexed by parent; `(lambda_max - lambda)/lambda_max` formula (NOT probabilities' `lambda/lambda_max`)
  </acceptance_criteria>
  <done>GLOSH outlier_scores_ are computed and match hdbscan 0.8.44 within 1e-5.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Port store_centers centroid + medoid (HDBS-04)</name>
  <files>crates/mlrs-algos/src/cluster/hdbscan/centers.rs, crates/mlrs-algos/src/cluster/hdbscan.rs</files>
  <read_first>
    - .planning/phases/15-hdbscan/15-RESEARCH.md Pattern 8 (`_weighted_cluster_center`: centroid = `np.average(data, weights=probabilities)`; medoid = `argmin(sum(pairwise_distances*strength, axis=1))`; ascending cluster id 0..n_clusters; feature-array only, errors on precomputed) and Pitfall 6 (compare via the SAME label permutation)
    - .planning/phases/15-hdbscan/15-PATTERNS.md centers.rs section (host reduce analog spectral_embedding.rs lines ~241-284) + the Don't-Hand-Roll medoid pairwise (host pairwise over the small per-cluster data)
    - crates/mlrs-algos/src/cluster/hdbscan.rs (the `store_centers: Option<StoreCenters>` field added in 15-03; the labels_/probabilities_ fitted state)
    - crates/mlrs-algos/tests/kmeans_test.rs (the best_mapping per-cluster value gate the centers_match test uses)
  </read_first>
  <behavior>
    - centroid[c] = probability-weighted mean of cluster c's rows; medoid[c] = the cluster member minimizing the strength-weighted total distance
    - store_centers errors (typed) on Metric::Precomputed (feature-array only)
    - centers_match: per-cluster ≤1e-5 vs sklearn centroids_/medoids_ under the SAME permutation as labels (best_mapping)
  </behavior>
  <action>
    Create `crates/mlrs-algos/src/cluster/hdbscan/centers.rs` porting `_weighted_cluster_center`
    from RESEARCH Pattern 8: for each ascending cluster id `0..n_clusters` (clusters =
    `set(labels)-{-1,-2}`), `centroid = np.average(data, weights=strength)` (probability-weighted
    mean) and `medoid = data[argmin(sum(pairwise_distances(data, metric)*strength, axis=1))]`
    (weight the per-cluster pairwise distance matrix by strength, sum rows, argmin). Compute the
    small per-cluster pairwise distances host-side (Don't-Hand-Roll: host pairwise over the
    per-cluster `data`, matching `pairwise_distances` for the estimator's metric). Add
    `centroids_`/`medoids_` (`Option<DeviceArray<ActiveRuntime, F>>`) fitted fields + `Fitted`-only
    `centroids()`/`medoids()` accessors to `hdbscan.rs`; compute them in `fit` when `store_centers`
    is `Some(Centroid|Both)` / `Some(Medoid|Both)`. Return a typed error when `store_centers` is
    set with `Metric::Precomputed` (feature-array only, sklearn parity). Use f64 host bridging
    (`mlrs_core::{host_to_f64,f64_to_host}`).
  </action>
  <verify>
    <automated>cargo test --features cpu --test hdbscan_test centers_match</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --features cpu --test hdbscan_test centers_match` passes (≤1e-5 vs sklearn centroids_/medoids_ under the same permutation)
    - `grep -c 'weighted_cluster_center' crates/mlrs-algos/src/cluster/hdbscan/centers.rs` >= 1
    - Source assertion: medoid weights the distance matrix by strength then argmins row sums; store_centers errors on Precomputed
  </acceptance_criteria>
  <done>store_centers centroid+medoid are computed and match sklearn within 1e-5 under the same permutation; precomputed correctly errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| store_centers + precomputed | `store_centers` is feature-array only; requesting it with `Metric::Precomputed` must error (no feature rows to average), matching sklearn. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-06-V5 | Tampering | store_centers with Precomputed | mitigate | Typed error when `store_centers` is set with `Metric::Precomputed` (feature-array only) — do not silently emit garbage centers. |
| T-15-06-INF | Tampering | NaN in GLOSH/centers on degenerate clusters | mitigate | GLOSH returns 0.0 on `deaths==0`/non-finite lambda; the ≤1e-5 fixture gates (hdbscan 0.8.44 for GLOSH, sklearn for centers) catch divergence. Per D-06, escalate rather than widen if a genuine divergence appears. |
| T-15-06-SC | Tampering | npm/pip/cargo installs | mitigate | No new crate dependency added (pure host port); nothing to audit. |
</threat_model>

<verification>
- `cargo test --features cpu --test hdbscan_test outlier_scores_match centers_match` green.
- Per-wave merge: `cargo test --features cpu --test hdbscan_test` (whole file) green on cpu(f64).
- D-06: any score divergence beyond 1e-5 for genuine reasons is escalated, not widened.
</verification>

<success_criteria>
GLOSH outlier_scores_ match hdbscan 0.8.44 and store_centers centroid+medoid match sklearn,
both within 1e-5; the full HDBSCAN surface is complete under every gate.
</success_criteria>

## Artifacts this phase produces (cumulative — this plan's slice)

- **New modules:** `crates/mlrs-algos/src/cluster/hdbscan/glosh.rs` (`outlier_scores`), `.../centers.rs` (`weighted_cluster_center` — centroid + medoid).
- **New fitted fields/accessors:** `outlier_scores_` + `outlier_scores()`, `centroids_` + `centroids()`, `medoids_` + `medoids()`.
- **New behavior:** GLOSH gate vs hdbscan 0.8.44; store_centers centroid/medoid gate vs sklearn; precomputed-rejects-store_centers parity.

<output>
Create `.planning/phases/15-hdbscan/15-06-SUMMARY.md` when done.
</output>
