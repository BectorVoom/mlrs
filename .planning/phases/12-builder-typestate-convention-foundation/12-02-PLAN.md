---
phase: 12-builder-typestate-convention-foundation
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - crates/mlrs-algos/src/error.rs
  - crates/mlrs-algos/src/manifold/mod.rs
  - crates/mlrs-algos/src/manifold/umap.rs
  - crates/mlrs-algos/src/cluster/hdbscan.rs
  - crates/mlrs-algos/src/cluster/mod.rs
  - crates/mlrs-algos/src/lib.rs
  - crates/mlrs-algos/tests/umap_test.rs
  - crates/mlrs-algos/tests/hdbscan_test.rs
autonomous: true
requirements: [BLDR-01, BLDR-02]
must_haves:
  truths:
    - "`Umap<F, S=Unfit>` exists in a NEW `manifold/` module with the full UMAP-01 param surface, an owned-setter builder, `new()` single-source defaults, and a non-algorithmic trivial fit producing a zeros `embedding_` of shape (n, n_components)"
    - "`Hdbscan<F, S=Unfit>` exists in the existing `cluster/` module with the full HDBS-01 param surface, an owned-setter builder, `new()` defaults, and a trivial fit producing all-`-1` `labels_` of length n"
    - "`T::builder().build::<F>()? == T::new()` holds for both shells (single-source defaults, BLDR-01)"
    - "Bad hyperparameters (`min_dist > spread`; `min_cluster_size < 2`) are rejected at `build()` with a typed `BuildError` variant (data-independent validation, the D-08 split)"
    - "`embedding()`/`labels()` accessors exist ONLY on `impl T<F, Fitted>`; the trivial fit round-trips (zeros / all-`-1`); `live_bytes` does not climb across re-construct+fit (memory gate)"
  artifacts:
    - path: "crates/mlrs-algos/src/manifold/umap.rs"
      provides: "Umap<F,S> struct + UmapBuilder + new()/builder()/into_builder() + impl Fit + impl Transform + fitted accessors"
      contains: "pub struct Umap"
      min_lines: 120
    - path: "crates/mlrs-algos/src/cluster/hdbscan.rs"
      provides: "Hdbscan<F,S> struct + HdbscanBuilder + new()/builder()/into_builder() + impl Fit + fitted labels() accessor"
      contains: "pub struct Hdbscan"
      min_lines: 110
    - path: "crates/mlrs-algos/src/error.rs"
      provides: "New BuildError variants InvalidMinDist + InvalidMinClusterSize (data-independent)"
      contains: "InvalidMinDist"
    - path: "crates/mlrs-algos/tests/umap_test.rs"
      provides: "defaults-eq, build-rejects, fit round-trip, PoolStats no-leak"
    - path: "crates/mlrs-algos/tests/hdbscan_test.rs"
      provides: "defaults-eq, build-rejects, fit round-trip, PoolStats no-leak"
  key_links:
    - from: "crates/mlrs-algos/src/manifold/umap.rs"
      to: "mlrs_algos::typestate::Fit"
      via: "impl Fit<F> for Umap<F, Unfit> { type Fitted = Umap<F, Fitted>; }"
      pattern: "impl<F[^>]*> Fit<F> for Umap<F, *Unfit>"
    - from: "crates/mlrs-algos/src/cluster/hdbscan.rs"
      to: "mlrs_algos::typestate::Fit"
      via: "impl Fit<F> for Hdbscan<F, Unfit>"
      pattern: "impl<F[^>]*> Fit<F> for Hdbscan<F, *Unfit>"
    - from: "crates/mlrs-algos/src/manifold/umap.rs"
      to: "crate::error::BuildError::InvalidMinDist"
      via: "build() validation"
      pattern: "BuildError::InvalidMinDist"
---

<objective>
Author the two new-estimator SHELLS that demonstrate the Phase-12 convention end-to-end: `Umap<F, S=Unfit>` (new `manifold/` module) and `Hdbscan<F, S=Unfit>` (existing `cluster/` module). Each gets the full sklearn param surface (UMAP-01 / HDBS-01 subsets), an owned chained-setter builder mirroring `mbsgd_regressor.rs`, `new()` as the single defaults source with the builder `Default` re-deriving via `new().into_builder()`, a NON-algorithmic trivial fit (zeros embedding / all-`-1` labels — no kernel, no compute), and fitted-attr accessors gated on `impl T<F, Fitted>` only. Add the two new `BuildError` variants for the shells' data-independent validation, and wire both modules.

Both shells edit the SHARED `crates/mlrs-algos/src/error.rs` and `lib.rs`, so they live in ONE plan to preserve file-disjoint discipline (the alternative — two parallel plans both touching `error.rs` — would conflict).

Implements D-01 (per-estimator `<F, S = Unfit>` state type-param — the `PhantomData<S>` struct shape), D-02 (`fit` consumes `self`; accessors only on `T<Fitted>`), D-08 (single-source defaults), D-09 (reuse the shipped builder shape), D-10 (full shape + trivial non-algorithmic fit + real fitted accessors), D-12 (module homes: new `manifold/`, existing `cluster/`). Closes BLDR-01 (builder) and the Rust-side half of BLDR-02 (typestate; the compile-fail PROOF is Plan 03).

Purpose: prove the convention runs end-to-end (runtime round-trip + defaults equality + memory gate), giving Phases 14–15 a born-builder-fronted shell to fill with real algorithm.
Output: `manifold/umap.rs`, `cluster/hdbscan.rs`, two `BuildError` variants, module wiring, and the two algos test files (the Wave-0 test gaps for both shells).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-builder-typestate-convention-foundation/12-CONTEXT.md
@.planning/phases/12-builder-typestate-convention-foundation/12-RESEARCH.md
@.planning/phases/12-builder-typestate-convention-foundation/12-PATTERNS.md
@.planning/phases/12-builder-typestate-convention-foundation/12-01-SUMMARY.md
@AGENTS.md
</context>

<artifacts_this_phase_produces>
This plan introduces (consumed by Plan 03 compile-fail + Plan 04 PyO3 this phase):
- NEW module `mlrs_algos::manifold` (`manifold/mod.rs` re-exporting `umap::Umap`)
- `pub struct Umap<F, S = Unfit>` + `pub struct UmapBuilder` (non-generic)
- `Umap::new()` / `Umap::builder()` / `Umap::into_builder()` ; `impl Default for UmapBuilder` re-deriving from `new()`
- `impl Fit<F> for Umap<F, Unfit> { type Fitted = Umap<F, Fitted>; }` ; `impl Transform<F> for Umap<F, Fitted>`
- `Umap<F, Fitted>::embedding(&self, pool) -> Vec<F>` + `n_features_in()` accessors (Fitted-only)
- `pub struct Hdbscan<F, S = Unfit>` + `pub struct HdbscanBuilder` in `cluster/hdbscan.rs`
- `Hdbscan::new()`/`builder()`/`into_builder()`; `impl Fit<F> for Hdbscan<F, Unfit>`; `Hdbscan<F, Fitted>::labels(&self, pool) -> Vec<i32>` (Fitted-only)
- NEW `BuildError::InvalidMinDist { estimator, min_dist }` and `BuildError::InvalidMinClusterSize { estimator, min_cluster_size }` (data-independent, mapped by the existing single-site `build_err_to_py`)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add the two BuildError variants (data-independent validation surface)</name>
  <read_first>
    - crates/mlrs-algos/src/error.rs (lines 384-547 — the `BuildError` enum; the `InvalidVarSmoothing` variant at :507-520 is the EXACT shape to copy: `#[error("…")]` + `estimator: &'static str` + the offending value. ADD the two new variants at the end of the enum.)
    - .planning/phases/12-builder-typestate-convention-foundation/12-PATTERNS.md § "crates/mlrs-algos/src/error.rs" (lines 165-179 — the worked variant text)
    - .planning/phases/12-builder-typestate-convention-foundation/12-RESEARCH.md § "Pitfall 3" (lines 425-429 — these are data-INDEPENDENT → BuildError at build(), NEVER AlgoError at fit())
  </read_first>
  <action>
    Edit `crates/mlrs-algos/src/error.rs`. Add two variants to the `BuildError` enum (end of the enum, following the `InvalidVarSmoothing` shape at :507-520):
    - `InvalidMinDist { estimator: &'static str, min_dist: f64 }` with `#[error("estimator '{estimator}': min_dist = {min_dist} is invalid (must be finite and <= spread)")]`
    - `InvalidMinClusterSize { estimator: &'static str, min_cluster_size: usize }` with `#[error("estimator '{estimator}': min_cluster_size = {min_cluster_size} is invalid (must be >= 2)")]`
    These are data-INDEPENDENT (validated at `build()` before any data) per the D-08 split (Pitfall 3) — they go on `BuildError`, NOT `AlgoError`. The single-site `build_err_to_py` (`crates/mlrs-py/src/errors.rs:71-73`) already maps the whole `BuildError` enum, so NO PyO3 mapper change is needed (Plan 04 reuses it unchanged). Do NOT touch the existing `AlgoError::InvalidMinSamples` (`error.rs:126`) — that is the OLD fit-time surface and stays as-is.
  </action>
  <verify>
    <automated>cargo build -p mlrs-algos --features cpu 2>&1 | tail -5 && grep -c "InvalidMinDist\|InvalidMinClusterSize" crates/mlrs-algos/src/error.rs</automated>
  </verify>
  <done>`cargo build -p mlrs-algos --features cpu` succeeds; `error.rs` contains both `InvalidMinDist` and `InvalidMinClusterSize` variants on `BuildError`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author the UMAP shell (manifold/umap.rs) + module wiring + umap_test.rs</name>
  <read_first>
    - crates/mlrs-algos/src/linear/mbsgd_regressor.rs (lines 36-363 — the CANONICAL builder template: struct with `Option` fitted fields :36-43, owned setters + non-generic builder :89-198, `build<F>() -> Result<_, BuildError>` with up-front validation :213-287, geometry guard at fit :303-312, `coef()` accessor :62-82. Copy the builder shape 1:1; the ONLY deviation is the `Default` re-derive per D-08.)
    - crates/mlrs-algos/src/typestate.rs (the markers + `Fit`/`Transform` traits from Plan 01 — `use mlrs_algos::typestate::{Unfit, Fitted, Fit, Transform};`, NOT `traits::`)
    - crates/mlrs-algos/src/cluster/mod.rs (lines 21-43 — the `pub mod` + `pub use` module-wiring shape to mirror in the NEW `manifold/mod.rs`)
    - crates/mlrs-algos/tests/gaussian_nb_test.rs (lines 280-340 — build-rejects assertion :280-292, pool setup + f64-skip-with-log :294-313, PoolStats `live_bytes` monotone gate :323-335)
    - .planning/phases/12-builder-typestate-convention-foundation/12-PATTERNS.md § "manifold/umap.rs" (lines 82-151) and § "Tests" (lines 287-308)
    - .planning/REQUIREMENTS.md (UMAP-01, line 23 — the authoritative param subset + defaults; `min_dist <= spread` validation)
  </read_first>
  <behavior>
    - `Umap::<f64>::new()` returns a struct with sklearn defaults (`n_neighbors=15`, `n_components=2`, `min_dist=0.1`, `spread=1.0`, `metric=Euclidean`, plus the rest of the UMAP-01 subset) and `embedding_: None`, `n_features_in_: 0`, `_state: PhantomData<Unfit>`.
    - `Umap::builder().build::<f64>().unwrap()` equals `Umap::<f64>::new()` on the hyperparameter subset (defaults equality, BLDR-01).
    - `Umap::<f64>::builder().min_dist(2.0).spread(1.0).build::<f64>()` returns `Err(BuildError::InvalidMinDist { min_dist: 2.0, .. })`.
    - `Umap::<f64>::new().fit(&mut pool, &x_dev, None, (n, p))?` returns `Umap<f64, Fitted>`; `.embedding(&pool)` returns a `Vec<f64>` of length `n * n_components` that is all zeros; `.n_features_in()` returns `p`.
    - Re-constructing + re-fitting at the same shape several times does not grow `pool.stats().live_bytes` (memory gate).
    - `predict`/`transform` are NOT callable on `Umap<f64, Unfit>` (proven structurally by Plan 03's trybuild gate — do NOT attempt to assert non-compilation in this `.rs` test file).
  </behavior>
  <action>
    Create `crates/mlrs-algos/src/manifold/mod.rs` mirroring `cluster/mod.rs:21-43`: `pub mod umap;` + `pub use umap::Umap;`.

    Create `crates/mlrs-algos/src/manifold/umap.rs`:
    - Define a `Metric` enum (at minimum `Euclidean`; include the UMAP-01 string-metric set the requirement names — euclidean default) if one is not already shareable; keep it local unless an existing shared `Metric` exists (grep `enum Metric` first — reuse if present).
    - `pub struct Umap<F, S = Unfit>` holding the UMAP-01 hyperparameter subset (`n_neighbors: usize`, `n_components: usize`, `min_dist: f64`, `spread: f64`, `metric`, `n_epochs: Option<usize>`, `init`, `random_state`, `learning_rate: f64`, `set_op_mix_ratio: f64`, `local_connectivity: f64`, `repulsion_strength: f64`, `negative_sample_rate: usize`, `a: Option<f64>`, `b: Option<f64>`) + `embedding_: Option<DeviceArray<ActiveRuntime, F>>` + `n_features_in_: usize` + `_state: PhantomData<S>`. Derive `PartialEq`/`Debug` on the hyperparameter subset OR provide a test-visible comparison (the fitted `Option<DeviceArray>` fields are `None` in both `new()` and `build()?`, so a `#[derive(PartialEq)]` works only if `DeviceArray` is `PartialEq` — if not, write a small `fn hyperparams_eq(&self, other) -> bool` comparing only the scalar/enum fields, and use it in the defaults-eq test).
    - `impl<F: Float + CubeElement + Pod> Umap<F, Unfit>`: `new()` (struct literal with sklearn defaults, `_state: PhantomData`, trusts defaults valid — bypasses build validation per D-08), `builder() -> UmapBuilder { UmapBuilder::default() }`, `into_builder(self) -> UmapBuilder` (copy hyperparams into the builder).
    - `#[derive(Debug, Clone, Copy)] pub struct UmapBuilder { … }` with one `fn name(mut self, v) -> Self` owned setter per hyperparameter (mirror `mbsgd_regressor.rs:130-198`), and `pub fn build<F>(self) -> Result<Umap<F, Unfit>, BuildError> where F: Float + CubeElement + Pod` doing data-INDEPENDENT validation up front: reject `!min_dist.is_finite() || min_dist > spread` → `BuildError::InvalidMinDist { estimator: "umap", min_dist }`; then construct the struct with `embedding_: None`, `n_features_in_: 0`, `_state: PhantomData`.
    - `impl Default for UmapBuilder { fn default() -> Self { Umap::<f64, Unfit>::new().into_builder() } }` — D-08 single source (resolve Open Question 1: pin `f64` to read the F-independent scalar defaults; do NOT hand-write literal defaults here — Pitfall 4).
    - `impl<F: Float + CubeElement + Pod> Fit<F> for Umap<F, Unfit> { type Fitted = Umap<F, Fitted>; fn fit(self, pool, x, _y, shape) -> Result<Umap<F, Fitted>, AlgoError> { … } }`: geometry guard first (`let (n, p) = shape; if n == 0 || p == 0 || x.len() != n*p { return Err(AlgoError::Prim(PrimError::ShapeMismatch { … })) }` mirroring `mbsgd_regressor.rs:303-312`); then allocate `let zeros = vec![F::from_int(0); n * self.n_components];` and `DeviceArray::from_host(pool, &zeros)` — NO kernel, NO compute (document the body explicitly as "non-algorithmic shell — real UMAP lands in Phase 14"); move every hyperparam field, set `embedding_: Some(embedding)`, `n_features_in_: p`, `_state: PhantomData`.
    - `impl<F: Float + CubeElement + Pod> Umap<F, Fitted>`: `embedding(&self, pool: &BufferPool<ActiveRuntime>) -> Vec<F>` (`self.embedding_.as_ref().unwrap().to_host(pool)` — `Some` by construction on `Fitted`) and `n_features_in(&self) -> usize`. Implement `impl Transform<F> for Umap<F, Fitted>` (trivial: re-emit a zeros buffer of `(rows, n_components)` from the passed shape, non-algorithmic, documented). These accessors/impls exist ONLY on `Fitted` — that is what makes predict/transform-before-fit a compile error (D-02).

    Edit `crates/mlrs-algos/src/lib.rs`: add `pub mod manifold;` to the `pub mod` block (per Plan 01's non-glob note; `manifold::Umap` is reachable via `pub use umap::Umap` in `manifold/mod.rs`).

    Create `crates/mlrs-algos/tests/umap_test.rs` covering the `<behavior>` block: `defaults_equal` (new() == builder().build()? on hyperparams), `build_rejects_bad_min_dist` (min_dist=2.0,spread=1.0 → `BuildError::InvalidMinDist`), `fit_roundtrip` (fit then `embedding()` returns `n*n_components` zeros + `n_features_in()==p`), `fit_no_leak` (re-construct+fit loop, `live_bytes` monotone-non-increasing, copy pool setup + `skip_f64_with_log()` guard from `gaussian_nb_test.rs:294-340` — note the consuming `fit(self)` means RE-CONSTRUCT each loop iteration, not re-fit the same instance). Use `DeviceArray::from_host` for the input `x`.
  </action>
  <verify>
    <automated>cargo test -p mlrs-algos --features cpu --test umap_test 2>&1 | tail -20</automated>
  </verify>
  <done>`cargo test -p mlrs-algos --features cpu --test umap_test` passes all four tests. `manifold/umap.rs` contains `pub struct Umap`, `impl Fit<F> for Umap<F, Unfit>` with `type Fitted = Umap<F, Fitted>`, and the `embedding`/`n_features_in`/`Transform` impls ONLY on `Umap<F, Fitted>`. `lib.rs` contains `pub mod manifold;`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Author the HDBSCAN shell (cluster/hdbscan.rs) + module wiring + hdbscan_test.rs</name>
  <read_first>
    - crates/mlrs-algos/src/linear/mbsgd_regressor.rs (lines 36-363 — same builder template as Task 2)
    - crates/mlrs-algos/src/cluster/dbscan.rs (the labels-only surface analog — HDBSCAN, like DBSCAN, exposes `labels_` and NO standalone predict; `-1` noise sentinel)
    - crates/mlrs-algos/src/cluster/mod.rs (lines 21-43 — add `pub mod hdbscan;` + `pub use hdbscan::Hdbscan;` following the `spectral_*` precedent at :39-43)
    - crates/mlrs-algos/src/manifold/umap.rs (the UMAP shell from Task 2 — the structural template; HDBSCAN's fit body is identical EXCEPT it allocates all-`-1` i32 labels)
    - crates/mlrs-algos/tests/gaussian_nb_test.rs (lines 280-340 — same test patterns as Task 2)
    - .planning/phases/12-builder-typestate-convention-foundation/12-PATTERNS.md § "cluster/hdbscan.rs" (lines 155-162)
    - .planning/REQUIREMENTS.md (HDBS-01, line 30 — authoritative param subset + defaults)
  </read_first>
  <behavior>
    - `Hdbscan::<f64>::new()` returns sklearn defaults (`min_cluster_size=5`, `min_samples=None→min_cluster_size`, `cluster_selection_epsilon=0.0`, `cluster_selection_method='eom'`, `metric=Euclidean`, `alpha=1.0`, `max_cluster_size=0`) with `labels_: None`, `n_features_in_: 0`, `_state: PhantomData<Unfit>`.
    - `Hdbscan::builder().build::<f64>().unwrap()` equals `Hdbscan::<f64>::new()` on hyperparameters (defaults equality, BLDR-01).
    - `Hdbscan::<f64>::builder().min_cluster_size(1).build::<f64>()` returns `Err(BuildError::InvalidMinClusterSize { min_cluster_size: 1, .. })`.
    - `Hdbscan::<f64>::new().fit(&mut pool, &x_dev, None, (n, p))?` returns `Hdbscan<f64, Fitted>`; `.labels(&pool)` returns a `Vec<i32>` of length `n` that is all `-1`; `.n_features_in()` returns `p`.
    - Re-construct+re-fit loop does not grow `live_bytes` (memory gate).
  </behavior>
  <action>
    Create `crates/mlrs-algos/src/cluster/hdbscan.rs` mirroring the UMAP shell from Task 2, with these differences:
    - Param subset HDBS-01: `min_cluster_size: usize`, `min_samples: Option<usize>`, `cluster_selection_epsilon: f64`, `cluster_selection_method` (enum: `Eom`/`Leaf`), `metric`, `alpha: f64`, `max_cluster_size: usize`.
    - `build<F>()` validation (data-independent): the shell validates ONLY `min_cluster_size >= 2` → `BuildError::InvalidMinClusterSize { estimator: "hdbscan", min_cluster_size }` (reject `min_cluster_size < 2`). `min_samples` is stored verbatim and is NOT validated in Phase 12 — its semantic validation is deferred to Phase 15 (HDBSCAN algorithm). Do NOT add a `min_samples == Some(0)` guard and do NOT introduce any third `BuildError` variant for `min_samples`. (This deferral is intentional: it matches the no-algorithm boundary of the pure-convention shell — `min_samples` only acquires meaning once the real HDBSCAN compute lands in Phase 15.) The `min_samples=None→min_cluster_size` default resolution happens in `new()`/`build()` (store the resolved value or keep `Option` and resolve at read — store resolved is simpler).
    - Fitted field `labels_: Option<DeviceArray<ActiveRuntime, i32>>` (i32, the `-1` noise sentinel — `cluster/mod.rs:12` documents `-1`).
    - `impl Fit<F> for Hdbscan<F, Unfit> { type Fitted = Hdbscan<F, Fitted>; … }`: same geometry guard as UMAP; allocate `let labels = vec![-1_i32; n];` and `DeviceArray::from_host(pool, &labels)` — NO kernel, NO compute (document "non-algorithmic shell — real HDBSCAN lands in Phase 15"); set `labels_: Some(..)`, `n_features_in_: p`.
    - `impl Hdbscan<F, Fitted>`: `labels(&self, pool) -> Vec<i32>` and `n_features_in(&self) -> usize`, Fitted-only. HDBSCAN has NO standalone predict (labels-only, like DBSCAN) — do NOT impl `Predict`/`Transform`.
    - `new()`/`builder()`/`into_builder()`/`impl Default for HdbscanBuilder { Hdbscan::<f64, Unfit>::new().into_builder() }` exactly as UMAP (D-08 single source).

    Edit `crates/mlrs-algos/src/cluster/mod.rs`: add `pub mod hdbscan;` + `pub use hdbscan::Hdbscan;`.

    Create `crates/mlrs-algos/tests/hdbscan_test.rs` covering the `<behavior>` block: `defaults_equal`, `build_rejects` (min_cluster_size=1 → `BuildError::InvalidMinClusterSize`), `fit_roundtrip` (labels all `-1`, length `n`, `n_features_in()==p`), `fit_no_leak` (re-construct+fit loop, `live_bytes` monotone, `skip_f64_with_log()` guard). Same pool/test conventions as `umap_test.rs`.
  </action>
  <verify>
    <automated>cargo test -p mlrs-algos --features cpu --test hdbscan_test 2>&1 | tail -20</automated>
  </verify>
  <done>`cargo test -p mlrs-algos --features cpu --test hdbscan_test` passes all four tests. `cluster/hdbscan.rs` contains `pub struct Hdbscan`, `impl Fit<F> for Hdbscan<F, Unfit>`, and the `labels`/`n_features_in` accessors ONLY on `Hdbscan<F, Fitted>` (no `Predict`/`Transform`). `cluster/mod.rs` contains `pub mod hdbscan;` and `pub use hdbscan::Hdbscan;`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hyperparameter → `build()` | Untrusted hyperparameter values cross into validation at `build()` (data-independent, before any data). |
| (n, p) shape + `x` buffer → `fit()` | The data-dependent geometry crosses into the fit's single bounded allocation (`n * n_components` zeros / `n` labels). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02 | Tampering / DoS | Out-of-range hyperparameter (`min_dist` NaN/huge, `min_cluster_size` absurd) reaching the fit allocation | mitigate | Validate at `build()` BEFORE data → typed `BuildError::InvalidMinDist`/`InvalidMinClusterSize` (the D-08 split). The only fit allocation is `n * n_components` zeros / `n` labels — both bounded by validated/`shape` values. Add the finite/`<= spread` check on `min_dist` (UMAP) and `min_cluster_size >= 2` (HDBSCAN). |
| T-12-03 | Tampering | Malformed geometry (`x.len() != n*p`, `n==0`, `p==0`) at fit | mitigate | Geometry guard at fit → `AlgoError::Prim(PrimError::ShapeMismatch)` mirroring `mbsgd_regressor.rs:303-312`, BEFORE the allocation. |
| T-12-04 | Tampering | A hyperparameter check leaking into `fit` instead of `build` (wrong layer) | mitigate | Pitfall 3: all hyperparameter validation lives in `build()` → `BuildError`; only the data-dependent geometry guard lives in `fit()` → `AlgoError`. Verified by the `build_rejects_*` tests asserting the error surfaces at `build()`, not after data upload. |

No `high`-severity threats. f64-on-rocm and pool-mutex-poisoning are PyO3-boundary concerns handled in Plan 04; the algos tests here invoke `skip_f64_with_log()` for the rocm f64 gate.
</threat_model>

<verification>
- `cargo test -p mlrs-algos --features cpu --test umap_test --test hdbscan_test` — all eight tests pass.
- Per-wave regression (Success Criterion 3): `cargo test -p mlrs-algos --features cpu` — full algos suite green (background per MEMORY: ~6min, reduce_test/svd_test dominate). Confirms the new `BuildError` variants + module wiring did not break any existing estimator.
- `git diff --stat crates/mlrs-algos/src/traits.rs` empty (frozen-trait invariant held).
- Opportunistic GPU gate (manual-only, end-of-phase): `cargo test -p mlrs-algos --features rocm --test umap_test fit_roundtrip` (f64 SKIPS-with-log; f32 round-trip runs).
</verification>

<success_criteria>
- Both shells construct via `T::builder().param(..).build::<F>()? -> Result<T<Unfit>, BuildError>` with owned chained setters and typed validation; `builder().build()? == new()` (single-source defaults, BLDR-01).
- `predict`/`transform`/fitted accessors exist only on `T<Fitted>` (BLDR-02 Rust-side; compile proof in Plan 03).
- Trivial fits round-trip at runtime (zeros embedding / all-`-1` labels); both memory gates hold.
- Bad hyperparameters rejected at `build()` with the new `BuildError` variants.
</success_criteria>

<output>
Create `.planning/phases/12-builder-typestate-convention-foundation/12-02-SUMMARY.md` when done. Record: the exact public paths (`mlrs_algos::manifold::umap::Umap`, `mlrs_algos::cluster::hdbscan::Hdbscan`), the builder setter names + the `new()` default values per shell (so Plan 04's PyO3 `Unfit { … }` arm matches), the two `BuildError` variant names, and the resolution of Open Question 1 (builder `Default` form).
</output>
