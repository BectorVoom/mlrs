---
phase: 13-knn-graph-primitive-feasibility-keystone
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - crates/mlrs-kernels/src/distance.rs
  - crates/mlrs-kernels/src/lib.rs
autonomous: true
requirements: [PRIM-11]
user_setup: []

must_haves:
  truths:
    - "Three new direct pairwise distance kernels (manhattan_dist, chebyshev_dist, minkowski_dist) launch under --features cpu (cpu-MLIR), not just compile, including in-kernel STATIC F::powf for Minkowski-p"
    - "self_drop_gather kernel launches under --features cpu using the CUBE_POS_X/UNIT_POS_X==0 per-row shape and a self-contained nested-count shift (no cross-sibling-loop accumulator)"
    - "All four kernels are generic over F (f32/f64) and free of SharedMemory/Atomic/F::INFINITY/mutable-bool/shift-loop; f64 runs on cpu, f64-on-rocm skips-with-log"
  artifacts:
    - path: "crates/mlrs-kernels/src/distance.rs"
      provides: "manhattan_dist / chebyshev_dist / minkowski_dist (2D feature-loop) + self_drop_gather (per-row GATHER) #[cube(launch)] kernels"
      contains: "minkowski_dist"
    - path: "crates/mlrs-kernels/src/lib.rs"
      provides: "pub use distance::{chebyshev_dist, manhattan_dist, minkowski_dist, self_drop_gather}"
      contains: "self_drop_gather"
  key_links:
    - from: "crates/mlrs-kernels/src/lib.rs"
      to: "crates/mlrs-kernels/src/distance.rs"
      via: "pub use distance::{...}"
      pattern: "pub use distance::"
---

<objective>
Land the four new device kernels PRIM-11 needs — the three direct pairwise distance kernels
(Manhattan/Chebyshev/Minkowski-p) that GEMM-expansion cannot back, plus the index-identity
`self_drop_gather` kernel — copying the VALIDATED verbatim shapes from the Phase 13 spikes.

Purpose: These are the ONLY genuinely new kernels in the phase; both feasibility unknowns
(in-kernel `F::powf` for Minkowski-p; the per-row index-identity self-drop) were already VALIDATED
under cpu-MLIR in spikes 001/002. This plan transcribes the proven shapes and proves they
launch (not just compile) via a thin launch smoke test.
Output: `crates/mlrs-kernels/src/distance.rs` with the four `#[cube(launch)]` kernels + the
`pub use` re-export in lib.rs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-knn-graph-primitive-feasibility-keystone/13-RESEARCH.md
@.planning/phases/13-knn-graph-primitive-feasibility-keystone/13-PATTERNS.md
@.claude/skills/spike-findings-mlrs/SKILL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the three direct pairwise distance kernels (manhattan/chebyshev/minkowski) — verbatim spike 001 shapes</name>
  <files>crates/mlrs-kernels/src/distance.rs</files>
  <behavior>
    - manhattan_dist(x,y,out,rows_x,rows_y,cols): out[i*rows_y+j] = sum_k |x_ik - y_jk| (acc += diff; no root)
    - chebyshev_dist(...): out[..] = max_k |x_ik - y_jk| via STATEMENT-form `if diff > acc { acc = diff; }`, seed acc=0 (diffs >= 0)
    - minkowski_dist(..., p:F): acc += F::powf(|delta|, p) (STATIC F::powf); out[..] = F::powf(acc, 1/p)
    - Each kernel reproduces the matching sklearn metric to <=1e-9 on the spike depth-probe data; verified end-to-end against the oracle in Plan 03.
  </behavior>
  <read_first>
    - .claude/skills/spike-findings-mlrs sources/001-direct-feature-loop-distance-kernels/kernels_and_harness.rs lines 23-114 (VALIDATED verbatim manhattan/chebyshev/minkowski shapes; the `.abs()` instance-form-OK comment line 23; STATIC F::powf comment)
    - 13-PATTERNS.md lines 64-102 (kernel structural pattern, Minkowski verbatim copy block, Manhattan/Chebyshev variants, `p: F` by value, `.abs()` instance-form exception, STATIC F::powf rule)
    - crates/mlrs-kernels/src/elementwise.rs lines 437-457 (dist_combine_clamp: 2D launch idiom `let i=ABSOLUTE_POS_X; let j=ABSOLUTE_POS_Y; if i<rows && j<cols`, STATEMENT-form clamp, scalar dims u32 by value) and lines 18-25, 190 (cpu-MLIR STATEMENT-form rules)
    - 13-RESEARCH.md lines 177-205 (Pattern 2 cpu-MLIR-safe direct kernel), 232-237 (Anti-Patterns: instance powf, if-expression value position, SharedMemory/Atomic/F::INFINITY)
  </read_first>
  <action>
    In crates/mlrs-kernels/src/distance.rs add three `#[cube(launch)] pub fn`s generic over `F: Float + CubeElement`, one unit per output element `(i,j)` with `let i = ABSOLUTE_POS_X; let j = ABSOLUTE_POS_Y; if i < rows_x { if j < rows_y { ... } }`, a runtime `while kk < cols` feature loop, `F`/`u32` accumulators, scalar dims (`rows_x,rows_y,cols: u32`, and `p: F` for minkowski) passed BY VALUE (no ScalarArg in cubecl 0.10). Transcribe the VALIDATED spike 001 shapes exactly: manhattan `acc += diff`; chebyshev STATEMENT-form running max `if diff > acc { acc = diff; }` seeded at 0; minkowski `acc += F::powf(diff, p)` then `out[..] = F::powf(acc, F::new(1.0)/p)`. Use the instance `.abs()` form for the per-term absolute difference (the one allowed instance form, jacobi-proven) but STATIC `F::powf` for the power (instance `x.powf()` mis-lowers). NO SharedMemory/Atomic/F::INFINITY/mutable-bool/if-expression-in-value-position. Add per-kernel doc comments noting the cpu-MLIR constraints, keeping doc prose free of bare instance-powf tokens that would self-invalidate grep gates.
  </action>
  <verify>
    <automated>cargo build -p mlrs-kernels --features cpu 2>&1 | tail -3 && grep -v '^//' crates/mlrs-kernels/src/distance.rs | grep -c 'F::powf' | awk '$1>=2{print "static-powf-ok"; exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p mlrs-kernels --features cpu` exits 0.
    - `crates/mlrs-kernels/src/distance.rs` defines `manhattan_dist`, `chebyshev_dist`, `minkowski_dist` (grep each name).
    - `grep -v '^//' crates/mlrs-kernels/src/distance.rs | grep -c 'F::powf'` >= 2 (minkowski uses static powf for both the per-term power and the final root).
    - No instance `.powf(`, `SharedMemory`, `Atomic`, or `F::INFINITY` in source-code lines: `grep -nE '\.powf\(|SharedMemory|Atomic|F::INFINITY' crates/mlrs-kernels/src/distance.rs` returns no code-line matches (doc comments excluded).
  </acceptance_criteria>
  <done>The three direct distance kernels exist, compile under --features cpu, use STATIC F::powf for Minkowski-p, and contain no cpu-MLIR-forbidden constructs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add self_drop_gather kernel (verbatim spike 002 shape) + lib.rs re-export + launch smoke test</name>
  <files>crates/mlrs-kernels/src/distance.rs, crates/mlrs-kernels/src/lib.rs</files>
  <behavior>
    - self_drop_gather(in_val,in_idx,out_val,out_idx,rows,k,k1): for each row, copies the (k+1) top-k slots minus the one whose index == row (index identity), into the k output slots.
    - Shift is computed per output slot via a SELF-CONTAINED nested count (no cross-sibling-loop accumulator).
    - Fallback: if no slot has idx==row (self absent), bump stays 0 for all s -> drops the last (k-th) column.
    - Launches under --features cpu via the CUBE_POS_X / UNIT_POS_X==0 per-row shape (NOT bare ABSOLUTE_POS 1D) and reads back non-zero correct data (the loud 002-A failure would read back zeros).
  </behavior>
  <read_first>
    - .claude/skills/spike-findings-mlrs sources/002-directed-knn-compose-and-self-drop/composition_and_self_drop.rs lines 28-85 (VALIDATED verbatim self_drop_gather + its CubeCount::Static(n,1,1)/CubeDim{1,1,1} launch)
    - 13-PATTERNS.md lines 105-143 (self_drop_gather verbatim copy block, launch shape, the TWO landmines 002-A loud / 002-B silent, the documented fallback)
    - 13-RESEARCH.md lines 207-230 (Pattern 3 self-drop), 232-237 (Anti-Patterns incl. 002-A/002-B)
    - crates/mlrs-kernels/src/topk.rs lines 53-180 (select_k: the CUBE_POS_X/UNIT_POS_X==0 per-row launch shape + u32/F accumulators) and lines 215-220 (launch_dims_rows)
    - crates/mlrs-kernels/src/lib.rs lines 20-40 (the `pub use elementwise::{...}` / `pub use topk` re-export idiom; add `pub use distance::{...}`)
  </read_first>
  <action>
    In crates/mlrs-kernels/src/distance.rs add `#[cube(launch)] pub fn self_drop_gather<F: Float + CubeElement>(in_val, in_idx, out_val, out_idx, rows:u32, k:u32, k1:u32)` transcribing the VALIDATED spike 002 shape EXACTLY: `let row = CUBE_POS_X;` (native u32, NOT ABSOLUTE_POS usize), `if row < rows { if UNIT_POS_X == 0u32 { ... } }`, outer `while s < k` consuming loop, with `bump`/`c` INITIALIZED INSIDE the consuming loop and an inner `while c < s + 1u32` nested count that increments `bump` when `in_idx[ibase+c] == row` — `src = s + bump` (no carry across sibling loops). Copy `in_val[ibase+src]`/`in_idx[ibase+src]` to `out_*[obase+s]`. Do NOT introduce a cross-sibling-loop accumulator (002-B SILENT miscompile) and do NOT use a bare 1D ABSOLUTE_POS launch (002-A loud failure). Then in crates/mlrs-kernels/src/lib.rs add `pub use distance::{chebyshev_dist, manhattan_dist, minkowski_dist, self_drop_gather};`. Add a `#[test]` launch smoke test (in the kernels crate's existing test convention or as a `tests/` unit per AGENTS.md tests-separated rule) that launches `self_drop_gather` on a tiny hand-built (k+1)-column input containing a self-index column and asserts the output drops exactly the self column by index identity AND reads back non-zero correct data (catches the 002-A loud zero-readback). Keep this smoke test backend-driven under --features cpu.
  </action>
  <verify>
    <automated>cargo build -p mlrs-kernels --features cpu 2>&1 | tail -3 && grep -v '^//' crates/mlrs-kernels/src/lib.rs | grep -c 'self_drop_gather' | awk '$1>=1{exit 0} {exit 1}' && grep -v '^//' crates/mlrs-kernels/src/distance.rs | grep -c 'CUBE_POS_X' | awk '$1>=1{print "per-row-shape-ok"; exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p mlrs-kernels --features cpu` exits 0.
    - `self_drop_gather` is defined in distance.rs and re-exported from lib.rs (`grep -v '^//' crates/mlrs-kernels/src/lib.rs | grep -c 'self_drop_gather'` >= 1).
    - distance.rs uses `CUBE_POS_X` and `UNIT_POS_X == 0` (per-row selecting shape), NOT bare `ABSOLUTE_POS` 1D for self_drop_gather; the shift uses a nested count inside the consuming loop (no second sibling `while` reading a flag written in a prior sibling `while`).
    - The launch smoke test passes under --features cpu (`cargo test -p mlrs-kernels --features cpu self_drop` exits 0) and asserts non-zero correct readback (proving the kernel actually launched, not the 002-A zero-readback failure).
  </acceptance_criteria>
  <done>self_drop_gather launches under cpu-MLIR via the proven per-row shape, drops by index identity, is re-exported from mlrs-kernels, and a launch smoke test proves real (non-zero) readback.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host launch wrapper -> device kernel | element counts passed to `unsafe ArrayArg::from_raw_parts`; kernels bounds-check `i<rows && j<cols` / `row<rows` |
| compiler (cubecl-cpu MLIR) -> kernel correctness | the cpu-MLIR backend can SILENTLY miscompile forbidden patterns (FINDING 002-B) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-03 | Repudiation (data integrity) | self_drop_gather correctness | high | mitigate | Transcribe the VALIDATED 002 shape exactly (self-contained nested count, no cross-sibling accumulator); the duplicate-point VALUE oracle in Plan 01/03 is the catch; the Task 2 launch smoke test catches the 002-A loud zero-readback. |
| T-13-04 | Tampering / DoS | out-of-bounds device read in distance kernels | medium | mitigate | Kernels bounds-check `i<rows_x && j<rows_y` and feature loop `kk<cols`; host (Plan 03) validates geometry before launch. |
| T-13-05 | Tampering | numeric integrity of Minkowski-p (instance vs static powf) | medium | mitigate | STATIC `F::powf` only (instance form mis-lowers); enforced by acceptance grep + the per-metric oracle (Plan 03). |
| T-13-SC | Tampering | npm/pip/cargo installs | n/a | accept | Zero new packages this phase (RESEARCH Package Legitimacy Audit: N/A). No install tasks. |
</threat_model>

<verification>
- `cargo build -p mlrs-kernels --features cpu` exits 0.
- distance.rs defines manhattan_dist/chebyshev_dist/minkowski_dist/self_drop_gather; all four re-exported from lib.rs.
- No instance `.powf(` / `SharedMemory` / `Atomic` / `F::INFINITY` in code lines.
- self_drop_gather uses CUBE_POS_X/UNIT_POS_X==0 (not bare ABSOLUTE_POS 1D); launch smoke test green under --features cpu with non-zero readback.
</verification>

<success_criteria>
The four new kernels exist, are generic over F, contain no cpu-MLIR-forbidden constructs, use STATIC
F::powf for Minkowski-p and the proven per-row self-drop shape, are re-exported from mlrs-kernels,
and the self_drop_gather launch smoke test proves real (non-zero) readback under --features cpu.
</success_criteria>

<output>
Create `.planning/phases/13-knn-graph-primitive-feasibility-keystone/13-02-SUMMARY.md` when done.
Record the launch-verified status of each kernel under --features cpu (and note rocm f32 if run).
</output>
