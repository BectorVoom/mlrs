---
phase: 13-knn-graph-primitive-feasibility-keystone
plan: 03
type: execute
wave: 3
depends_on: ["13-01", "13-02"]
files_modified:
  - crates/mlrs-backend/src/prims/knn_graph.rs
autonomous: true
requirements: [PRIM-11]
user_setup: []

must_haves:
  truths:
    - "knn_graph<F>(pool, x, (n,d), k, metric, include_self) returns ascending-ordered directed (indices (n,k), distances (n,k)) for every metric (Euclidean, Manhattan, Cosine, Chebyshev, Minkowski-p), composed from distance -> top_k GATHER with no new heap kernel"
    - "For each metric, returned indices are set-equal to sklearn.neighbors.NearestNeighbors up to tie-ordering and distances match <=1e-5 (f64), with lowest-index tie-break documented as the mlrs convention"
    - "include_self=false drops the self neighbor by index identity (duplicate-point VALUE assert green); include_self=true returns self at column 0"
    - "The prim validates geometry (n*d==len, k<=n-1 when self-excluded, p>=1 for Minkowski) host-side returning PrimError::ShapeMismatch BEFORE any unsafe launch, and launches under --features cpu for every metric; f64-on-rocm skips-with-log, rocm f32 launches"
    - "A build-failing PoolStats memory gate passes: the big distance operand is query-axis tiled / kept global, never the full n x n resident-and-leaking"
  artifacts:
    - path: "crates/mlrs-backend/src/prims/knn_graph.rs"
      provides: "Metric enum + knn_graph<F> prim (validate-before-launch host orchestrator: metric routing, query-axis-tiled composition, index-identity self-drop)"
      exports: ["Metric", "knn_graph"]
      min_lines: 120
  key_links:
    - from: "crates/mlrs-backend/src/prims/knn_graph.rs"
      to: "crates/mlrs-backend/src/prims/distance.rs"
      via: "distance() GEMM fast path (Euclidean / normalized-Cosine)"
      pattern: "distance"
    - from: "crates/mlrs-backend/src/prims/knn_graph.rs"
      to: "crates/mlrs-backend/src/prims/topk.rs"
      via: "top_k() k-smallest + lowest-index tie-break"
      pattern: "top_k"
    - from: "crates/mlrs-backend/src/prims/knn_graph.rs"
      to: "crates/mlrs-kernels distance kernels"
      via: "manhattan/chebyshev/minkowski_dist + self_drop_gather launch"
      pattern: "self_drop_gather"
---

<objective>
Land the `knn_graph<F>` prim — the phase keystone — as a thin host orchestrator that validates
geometry host-side, routes each metric (Euclidean/Cosine -> GEMM distance(); Manhattan/Chebyshev/
Minkowski-p -> the Plan 02 direct kernels), composes distance -> top_k -> self_drop_gather
query-axis-tiled, and emits the DIRECTED (indices, distances) (n,k) graph. Then turn the Plan 01
oracle harness GREEN for all five metrics and prove the memory gate.

Purpose: This is PRIM-11 itself and the milestone's feasibility keystone — UMAP (Phase 14) and
HDBSCAN (Phase 15) consume this prim. Both feasibility unknowns were spike-VALIDATED; this plan is
the assembly of validated parts into the standalone-gated prim.
Output: crates/mlrs-backend/src/prims/knn_graph.rs (Metric enum + prim); the per-metric oracle,
duplicate-point VALUE assert, geometry-rejection, include_self, and query-axis memory gate all GREEN.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-knn-graph-primitive-feasibility-keystone/13-CONTEXT.md
@.planning/phases/13-knn-graph-primitive-feasibility-keystone/13-RESEARCH.md
@.planning/phases/13-knn-graph-primitive-feasibility-keystone/13-PATTERNS.md
@.claude/skills/spike-findings-mlrs/SKILL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define Metric enum + knn_graph host orchestrator (validate-before-launch, metric routing, composition, self-drop)</name>
  <files>crates/mlrs-backend/src/prims/knn_graph.rs</files>
  <behavior>
    - knn_graph(pool, x, (n,d), k, Metric::Euclidean, include_self=false) returns the k true neighbors per row (self dropped by index identity); indices set-equal to sklearn euclidean up to tie-ordering; distances <=1e-5.
    - Same for Metric::{Manhattan, Cosine, Chebyshev, Minkowski{p}} vs the matching sklearn metric.
    - include_self=true returns self at column 0 (distance 0) for HDBSCAN core distances.
    - Geometry violations (n*d!=len; k>n-1 when include_self=false; p<1 for Minkowski) return Err(PrimError::ShapeMismatch{operand,..}) BEFORE any unsafe launch (operands "x","k","p").
    - Duplicate-point row with include_self=false: neighbor 0 is the GENUINE duplicate index, not self (VALUE assert).
  </behavior>
  <read_first>
    - crates/mlrs-backend/src/prims/topk.rs lines 60-72 (public signature shape to copy: pool first, geometry tuples, Option DeviceArray reuse outputs, Result PrimError, where F: Float+CubeElement+Pod, allow too_many_arguments), 148-210 (validate_geometry incl. synthetic-operand "k" idiom at 167-174 and u32-overflow guard at 178-187), 83-141 (top_k launch + sqrt boundary)
    - crates/mlrs-backend/src/prims/distance.rs lines 79-181 (distance GEMM-expansion multi-stage device-resident pipeline; release_into(pool) at 156-166; launch_dims_2d at 234-243; unconditional max(d^2,0) clamp; sqrt deferred), 115-118 (row SumSq / scale precedent reusable for Cosine L2-normalization)
    - crates/mlrs-kernels/src/lib.rs (the Plan 02 pub use of manhattan_dist/chebyshev_dist/minkowski_dist/self_drop_gather symbols to call)
    - 13-PATTERNS.md lines 23-61 (knn_graph.rs host orchestrator: signature, validate-before-launch with PrimError::ShapeMismatch — note there is NO InvalidK/InvalidArg variant; mlrs-core/src/error.rs:66-155 variants are ShapeMismatch/DimMismatch/NotSquare/NotConverged/NotPositiveDefinite — composition 5-step flow, query-axis tiling, host-launch idiom, scalars-by-value), line 19 (Metric enum lives in knn_graph.rs, mirroring kernel_matrix.rs owning Kernel<F>)
    - 13-CONTEXT.md D-01..D-06 + Claude's-discretion (include_self bool; self-drop by index identity; standalone prim; directed-only; full metric set; Metric enum shape; Minkowski special-casing; tile size; p type/validation)
    - 13-RESEARCH.md lines 158-175 (Pattern 1 prim host shape), 369-381 (Open Questions: Metric enum recommendation, p as f64 cast to F, special-case Minkowski(2)->GEMM only if profiling warrants), Pitfalls 3/4/6 (f32 cancellation clamp reuse; p>=1 host validation; k+1<=n)
  </read_first>
  <action>
    Create crates/mlrs-backend/src/prims/knn_graph.rs (fully replacing the Plan 01 empty shell). Define a public Metric enum with variants Euclidean, Manhattan, Cosine, Chebyshev, and Minkowski carrying p as f64 (per RESEARCH Open Q3; accept p as f64 at the boundary, validate p>=1, cast to F for the kernel). Define knn_graph<F> with the topk.rs signature shape (pool first; x + (n,d); k; metric: Metric; include_self: bool; Option reuse outputs; returns Result of (indices u32, distances F) (n,k); where F: Float + CubeElement + Pod; allow clippy::too_many_arguments). Add a private validate_geometry called BEFORE any unsafe launch: n*d == x.len() (operand "x"); 1 <= k and, when include_self=false, k+1 <= n i.e. k <= n-1 (operand "k"); p >= 1.0 for Minkowski (operand "p"); u32-overflow guards on n/d/k — all via PrimError::ShapeMismatch with the synthetic-operand idiom (no numeric-range variant exists). Composition: set K = if include_self { k } else { k + 1 }; route Euclidean to distance() GEMM with sqrt=false; Cosine to L2-normalize rows (reuse row SumSq + scale precedent, guard zero-norm rows per A4) then distance() giving 1 - cosine; Manhattan/Chebyshev/Minkowski to the matching Plan 02 kernel (copy the distance.rs host-launch idiom: pool.client().clone(), launch_dims_2d, unsafe ArrayArg::from_raw_parts, scalars BY VALUE, p cast f64->F). Then top_k(pool, dist, n, cols, K, sqrt = matches Euclidean|Cosine). If include_self=false launch self_drop_gather to (n,k); else the top_k(k) result IS the output. Release transient scratch (dist block, (n,k+1) intermediate) via release_into(pool) (copy distance.rs:156-166); outputs are caller-owned (never released). Compute the distance stage QUERY-AXIS TILED (loop over query-row blocks, top_k each block, release its distance scratch, advance) so the big distance operand is never full n x n resident (D-CONTEXT criterion 4 / R-6); tile size is Claude's discretion (RESEARCH Open Q2 — fixed row-block, tune if the memory gate is red). Document the lowest-index tie-break (inherited from top_k) as the mlrs convention in the prim doc-comment. Do NOT symmetrize (directed-only, D-04). No fenced kernel code in the prim — it only launches the Plan 02 kernels and the existing distance/top_k prims.
  </action>
  <verify>
    <automated>cargo build -p mlrs-backend --features cpu 2>&1 | tail -3; grep -v '^//' crates/mlrs-backend/src/prims/knn_graph.rs | grep -cE 'enum Metric|pub fn knn_graph'</automated>
  </verify>
  <acceptance_criteria>
    - cargo build -p mlrs-backend --features cpu exits 0.
    - knn_graph.rs defines a public Metric enum (with a Minkowski variant carrying p) and pub fn knn_graph<F> (grep both).
    - validate_geometry runs BEFORE any unsafe launch and returns PrimError::ShapeMismatch for n*d!=len ("x"), k>n-1 with include_self=false ("k"), and p<1 for Minkowski ("p").
    - The prim calls distance() for Euclidean/Cosine, the Plan 02 kernels for Manhattan/Chebyshev/Minkowski, top_k, and self_drop_gather for include_self=false (grep self_drop_gather and top_k present).
    - The prim does NOT symmetrize (directed-only) and does NOT materialize the full n x n distance block at once (query-axis tiled; release_into on scratch).
  </acceptance_criteria>
  <done>knn_graph<F> + Metric exist, compile under --features cpu, validate host-side before launch, route every metric through the validated parts, self-drop by index identity, tile query-axis, and emit the directed graph only.</done>
</task>

<task type="auto">
  <name>Task 2: Turn the Plan 01 oracle harness GREEN — per-metric correctness, duplicate-point VALUE, include_self, geometry-rejection, memory gate</name>
  <files>crates/mlrs-backend/src/prims/knn_graph.rs</files>
  <read_first>
    - crates/mlrs-backend/tests/knn_graph_test.rs (the Plan 01 harness this task must turn GREEN: per-metric set-equal index + <=1e-5 distance asserts, knn_self_drop_duplicate_point_value, knn_include_self_returns_self_at_col0, knn_rejects_bad_geometry, knn_memory_gate_query_axis_tiled)
    - crates/mlrs-backend/tests/memory_gate_test.rs lines 72-201 (PoolStats: live_bytes conserves, peak_bytes plateaus/sub-quadratic, reuses grow — the thresholds the gate asserts)
    - 13-RESEARCH.md lines 282-296 (Pitfalls 4/5/6: p validation; full n x n -> memory gate red, tile over query axis; k+1<=n), 411-426 (PRIM-11 test map + sampling rate), 354-365 (Assumptions A3 tile/threshold MEDIUM, A4 Cosine zero-norm MEDIUM — confirm both vs the oracle/gate this task runs)
    - MEMORY.md notes: full cargo test exhausts disk (run TARGETED knn_graph_test; cargo clean to recover); backend suite slow ~6min (background the full run)
  </read_first>
  <action>
    Run the targeted Plan 01 harness and iterate the Plan 03 prim until every gate is GREEN under --features cpu. Concretely: run cargo test -p mlrs-backend --features cpu --test knn_graph_test and drive each test green — the five per-metric set-equal-index + <=1e-5-distance oracle tests (f64 on cpu; f32 companion on cpu and, opportunistically, rocm), knn_self_drop_duplicate_point_value (the load-bearing R-9 VALUE assert that catches the 002-B silent miscompile), knn_include_self_returns_self_at_col0, knn_rejects_bad_geometry (the three host-validation operands), and knn_memory_gate_query_axis_tiled. If the memory gate is red, tune the query-axis tile size (RESEARCH Open Q2 / A3) so peak_bytes is sub-quadratic in n and live_bytes conserves after warmup with scratch reuses > 0 — adjusting ONLY knn_graph.rs (the prim owns tiling; the gate thresholds in the test file are fixed by Plan 01). If a Cosine zero-norm edge case (A4) or a Minkowski p-cast precision issue surfaces vs the oracle, fix it in the prim. Confirm rocm f32 launches (cargo test -p mlrs-backend --features rocm --test knn_graph_test) opportunistically; f64-on-rocm must skip-with-log via the existing capability gate. Use TARGETED test runs (MEMORY.md disk/slowness landmines); background any full-suite confirmation. Do NOT weaken any assert in the test file to pass — fix the prim.
  </action>
  <verify>
    <automated>cargo test -p mlrs-backend --features cpu --test knn_graph_test 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - cargo test -p mlrs-backend --features cpu --test knn_graph_test exits 0 (all per-metric oracle, duplicate-point VALUE, include_self, geometry-rejection, and memory-gate tests GREEN).
    - For every metric, indices are set-equal to sklearn up to tie-ordering and distances match <=1e-5 (f64) — the harness asserts both; no assert in the test file was weakened.
    - knn_self_drop_duplicate_point_value is GREEN: neighbor 0 of the duplicate's query row is the GENUINE duplicate index (not self), proving index-identity self-drop survived cpu-MLIR (catches 002-B).
    - knn_memory_gate_query_axis_tiled is GREEN: peak_bytes sub-quadratic in n, live_bytes conserves after warmup, scratch reuses > 0 (no full n x n resident-and-leaking).
    - rocm f32 launches (opportunistic) and f64-on-rocm skips-with-log via the capability gate.
  </acceptance_criteria>
  <done>The standalone per-metric PRIM-11 gate is GREEN under --features cpu: every metric matches sklearn (set-equal indices, <=1e-5 distances), self-drop is index-identity-correct on duplicate points, host validation rejects bad geometry before launch, and the query-axis memory gate holds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller -> knn_graph host | k / p / geometry are untrusted-shaped inputs validated before any unsafe device launch |
| host launch wrapper -> device kernels | element counts passed to unsafe ArrayArg::from_raw_parts; kernels bounds-check indices |
| cubecl-cpu MLIR -> prim correctness | the backend can SILENTLY miscompile forbidden self-drop patterns (FINDING 002-B) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-06 | Tampering / DoS | out-of-bounds device read from bad geometry/k/p | high | mitigate | validate_geometry asserts n*d==len, 1<=k<=n-1 (self-excl), k+1<=n, p>=1, and u32-overflow BEFORE any unsafe launch, returning PrimError::ShapeMismatch (topk.rs precedent). |
| T-13-07 | DoS (unbounded allocation) | full n x n distance operand resident-and-leaking | high | mitigate | Query-axis tiling keeps the big distance operand global/tiled; the build-failing PoolStats memory gate (sub-quadratic peak_bytes, conserved live_bytes) is the enforcing control. |
| T-13-08 | Repudiation (data integrity) | silent cpu-MLIR miscompile returns wrong neighbors | high | mitigate | Per-metric oracle VALUE asserts + the duplicate-point index-identity VALUE assert (R-9) — the only catch for FINDING 002-B; assert VALUES, not non-panic. |
| T-13-09 | Tampering | unsafe ArrayArg length mismatch | medium | mitigate | Pass only validated element counts; kernels bounds-check i<rows && j<cols / row<rows (Plan 02). |
| T-13-SC | Tampering | npm/pip/cargo installs | n/a | accept | Zero new packages this phase (RESEARCH Package Legitimacy Audit: N/A). No install tasks. |
</threat_model>

<verification>
- cargo build -p mlrs-backend --features cpu exits 0; knn_graph + Metric defined.
- cargo test -p mlrs-backend --features cpu --test knn_graph_test exits 0 (all gates GREEN).
- Per metric: set-equal indices vs sklearn + <=1e-5 distances (f64).
- Duplicate-point VALUE assert GREEN (index-identity self-drop); include_self=true self at col 0.
- Geometry-rejection returns PrimError::ShapeMismatch before launch for "x"/"k"/"p".
- Query-axis memory gate GREEN (sub-quadratic peak_bytes, conserved live_bytes, reuses > 0).
- rocm f32 launches opportunistically; f64-on-rocm skips-with-log.
</verification>

<success_criteria>
PRIM-11 is satisfied and standalone-gated: knn_graph<F> returns the directed (indices, distances)
(n,k) graph for all five metrics, matches sklearn (set-equal indices up to tie-ordering, <=1e-5
distances f64) with lowest-index tie-break documented, drops self by index identity (duplicate-point
VALUE-proven), validates geometry host-side before launch, launches under --features cpu for every
metric (incl. Minkowski-p pow) and rocm f32, and holds the build-failing query-axis memory gate.
</success_criteria>

<output>
Create `.planning/phases/13-knn-graph-primitive-feasibility-keystone/13-03-SUMMARY.md` when done.
Record: per-metric oracle results (<=1e-5), the chosen query-axis tile size, the memory-gate
peak_bytes profile, and rocm f32 launch status.
</output>

## Artifacts this phase produces

Newly-created symbols / files (for the plan-review-convergence source-grounding pass to exclude from drift verification):

- **Prim fn:** `knn_graph<F>` — `crates/mlrs-backend/src/prims/knn_graph.rs`
- **Enum:** `Metric { Euclidean, Manhattan, Cosine, Chebyshev, Minkowski { p: f64 } }` — same file
- **New kernels** (`crates/mlrs-kernels/src/distance.rs`): `manhattan_dist`, `chebyshev_dist`, `minkowski_dist` (direct pairwise 2D feature-loop), `self_drop_gather` (per-row index-identity GATHER)
- **New file paths:** `crates/mlrs-backend/src/prims/knn_graph.rs`, `crates/mlrs-kernels/src/distance.rs`, `crates/mlrs-backend/tests/knn_graph_test.rs`
- **Test file:** `crates/mlrs-backend/tests/knn_graph_test.rs` (per-metric oracle + duplicate-point VALUE assert + geometry-rejection + query-axis memory gate)
- **Oracle fixture generator:** `gen_knn(seed, dtype, metric, p)` in `scripts/gen_oracle.py` + committed `knn_{metric}_{dtype}_*.npz` fixtures under `crates/mlrs-backend/tests/fixtures/`
- **Memory gate:** `knn_memory_gate_query_axis_tiled` (PoolStats build-failing gate) in the test file
- **Module registrations:** `pub mod knn_graph;` in `crates/mlrs-backend/src/prims/mod.rs`; `pub mod distance;` + `pub use distance::{chebyshev_dist, manhattan_dist, minkowski_dist, self_drop_gather};` in `crates/mlrs-kernels/src/lib.rs`
