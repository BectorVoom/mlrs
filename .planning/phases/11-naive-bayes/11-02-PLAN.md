---
phase: 11-naive-bayes
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - crates/mlrs-algos/src/naive_bayes/gaussian_nb.rs
  - crates/mlrs-algos/tests/gaussian_nb_test.rs
autonomous: true
requirements: [NB-01]
must_haves:
  truths:
    - "A user can fit GaussianNB and get predict/predict_proba/predict_log_proba matching sklearn"
    - "GaussianNB exact predict labels match sklearn EXACTLY (hard gate) on cpu f64 + f32"
    - "predict_proba rows sum to 1.0 ± 1e-6 via host log-sum-exp"
    - "epsilon_ = var_smoothing · max_j(Var(X[:,j])) over the WHOLE dataset (global, ddof=0), not per-class"
    - "builder().build() reproduces sklearn GaussianNB default (var_smoothing=1e-9, priors=None)"
    - "build() rejects var_smoothing < 0 with BuildError::InvalidVarSmoothing"
    - "re-fit releases prior fitted buffers (PoolStats no-leak)"
  artifacts:
    - path: "crates/mlrs-algos/src/naive_bayes/gaussian_nb.rs"
      provides: "GaussianNB Fit + PredictLabels + PredictProba + PredictLogProba"
      contains: "fn predict_log_proba"
    - path: "crates/mlrs-algos/tests/gaussian_nb_test.rs"
      provides: "Oracle tests: exact_labels (f64+f32), proba_band, default_matches_sklearn, build_rejects, refit_releases_buffers"
      contains: "fn exact_labels"
  key_links:
    - from: "crates/mlrs-algos/src/naive_bayes/gaussian_nb.rs"
      to: "nb_common::log_sum_exp_normalize"
      via: "predict_proba/predict_log_proba normalization"
      pattern: "log_sum_exp_normalize"
    - from: "crates/mlrs-algos/src/naive_bayes/gaussian_nb.rs"
      to: "nb_common::class_grouped_sum"
      via: "per-class mean/variance sufficient statistics"
      pattern: "class_grouped_sum"
---

<objective>
Fill GaussianNB (NB-01): the continuous-feature variant. Per-class Gaussian
likelihood with global `var_smoothing` epsilon_, host log-sum-exp, exact labels.
Independent struct calling nb_common helpers (D-03). No new kernel — sufficient
statistics via the validated class_grouped_sum GATHER + host f64 math.

Purpose: Deliver the only continuous NB variant; distinct math (mean/variance
Gaussian LL, not the GEMM joint-LL the three count variants share).
Output: GaussianNB with Fit/PredictLabels/PredictProba/PredictLogProba + green oracle.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-naive-bayes/11-RESEARCH.md
@.planning/phases/11-naive-bayes/11-PATTERNS.md
@.planning/research/FEATURES.md
@.planning/phases/11-naive-bayes/11-01-SUMMARY.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GaussianNB fit (global epsilon_, per-class mean/var via GATHER) + predict surface</name>
  <read_first>
    - crates/mlrs-algos/src/naive_bayes/gaussian_nb.rs (the Wave-0 stub: builder/build/Fit-guard)
    - crates/mlrs-algos/src/naive_bayes/nb_common.rs (class_grouped_sum, log_sum_exp_normalize, empirical_class_log_prior, argmax_decode)
    - crates/mlrs-algos/src/linear/mbsgd_classifier.rs lines 289-396 (fit: classes_ sort/dedup + geometry), 399-460 (PredictLabels/PredictProba host materialization)
    - crates/mlrs-algos/src/density/kernel_density.rs lines 331-360 (host terminal-log idiom)
    - .planning/research/FEATURES.md §GaussianNB (LL = −0.5·Σ[log(2π·var)+(x−mean)²/var]; epsilon_ = var_smoothing·max_j Var(X[:,j]) GLOBAL ddof=0; priors=None→empirical)
    - 11-RESEARCH.md Pitfall 3 (global not per-class epsilon_), Pitfall 9 (log-sum-exp)
  </read_first>
  <behavior>
    - fit computes theta_[c,j] = mean of class c feature j; var_[c,j] = population variance (ddof=0) + epsilon_; epsilon_ = var_smoothing · max over features of Var(X[:,j]) computed ONCE over the whole X (not per class).
    - class_prior_ = empirical (class_count/n) when priors=None; uses supplied priors otherwise.
    - predict_proba rows sum to 1.0 ± 1e-6; predict_log_proba == joint_ll - logsumexp per row.
    - predict_labels == argmax of joint LL decoded through classes_.
  </behavior>
  <action>
    Implement GaussianNB::fit: geometry guard (already in stub) → host classes_ sort/dedup →
    build class_of_row index → per-class feature sums and sum-of-squares via
    nb_common::class_grouped_sum (sums) plus a squared pass (or ScalarOp::SumSq per the A5
    resolution recorded in 11-01-SUMMARY) → theta_ = sum/n_c, var_ = sumsq/n_c − theta_²
    (population, ddof=0). Compute epsilon_ ONCE: column variance of the FULL X (ddof=0), take
    max over features, multiply by var_smoothing; add epsilon_ to every var_ cell (Pitfall 3).
    Store class_count_, class_prior_ (empirical via nb_common::empirical_class_log_prior when
    priors=None, else the supplied priors; validate length==n_classes here → AlgoError::InvalidLabels,
    D-05 data-dependent). Keep fitted state host f64 (small n_classes×n_features tensors per the
    responsibility map) or device-resident per the stub's field types. release_into(pool) prior
    fitted buffers on re-fit (WR-07).

    Implement PredictLabels/PredictProba/PredictLogProba: compute per-query-row joint LL
    `class_log_prior_[c] − 0.5·Σ_j[log(2π·var_[c,j]) + (x_j−theta_[c,j])²/var_[c,j]]` in host f64,
    then nb_common::log_sum_exp_normalize → (proba, log_proba); argmax_decode → labels. Geometry
    guard (n_features agreement → AlgoError::InvalidLabels) before any work.
  </action>
  <verify>
    <automated>cargo test --features cpu -p mlrs-algos --test gaussian_nb_test exact_labels</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "fn predict_log_proba" crates/mlrs-algos/src/naive_bayes/gaussian_nb.rs` returns a match
    - `grep -c "SharedMemory\|F::INFINITY\|Atomic" crates/mlrs-algos/src/naive_bayes/gaussian_nb.rs` (after `grep -v '^//'`) == 0
    - GaussianNB compiles: `cargo build --features cpu -p mlrs-algos` exits 0
    - epsilon_ computed from full-X column variance (max over features), not per-class — assert in code review against FEATURES.md
  </acceptance_criteria>
  <done>GaussianNB fits and predicts with global epsilon_; predict surface complete.</done>
</task>

<task type="auto">
  <name>Task 2: GaussianNB oracle tests — exact labels (hard), proba band + rows-sum-to-1, default-matches-sklearn, build-rejects, refit no-leak</name>
  <read_first>
    - crates/mlrs-algos/tests/gaussian_nb_test.rs (the Wave-0 #[ignore] scaffolds)
    - crates/mlrs-algos/tests/mbsgd_classifier_test.rs lines 193-232 (exact_labels f64+f32 + skip_f64_with_log), 388-463 (proba band + default_matches_sklearn + build_rejects)
    - crates/mlrs-backend/src/capability.rs lines ~147 (skip_f64_with_log, log_oracle_dtype, active_backend_name)
    - tests/fixtures/gaussian_nb_{f32,f64}_seed42.npz (committed Wave-0 fixtures)
    - 11-RESEARCH.md A4 (GaussianNB log-proba gets the WIDEST f32 band — set from the actual f32-vs-f64 residual)
  </read_first>
  <action>
    Un-#[ignore] and fill: `exact_labels` (f64, skip_f64_with_log) + `exact_labels_f32` (all
    backends) asserting `labels == predict_ref` EXACTLY (hard gate, integers, no band).
    `proba_band` asserting predict_proba within the documented band AND every row sums to
    1.0±1e-6. `default_matches_sklearn` asserting `GaussianNB::builder().build()` fitted attrs
    equal the sklearn-default fixture (var_smoothing=1e-9, priors=None). `build_rejects_bad_var_smoothing`
    asserting `builder().var_smoothing(-1.0).build()` → `Some(BuildError::InvalidVarSmoothing{..})`.
    `refit_releases_buffers` asserting PoolStats live_bytes unchanged across a second fit (WR-07).
    Set the f32 proba band from the measured f32-vs-f64 residual (A4 — GaussianNB log-proba is
    the widest of the five) and DOCUMENT the chosen band value with a code comment.
  </action>
  <verify>
    <automated>cargo test --features cpu -p mlrs-algos --test gaussian_nb_test</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --features cpu -p mlrs-algos --test gaussian_nb_test` exits 0 (all cases green, none ignored)
    - exact_labels asserts integer equality with NO tolerance band
    - proba_band asserts rows sum to 1.0 ± 1e-6
    - `grep -c "skip_f64_with_log" crates/mlrs-algos/tests/gaussian_nb_test.rs` ≥ 1
    - build_rejects test matches `BuildError::InvalidVarSmoothing`
  </acceptance_criteria>
  <done>GaussianNB oracle green on cpu (f64+f32); exact labels hard gate green; proba rows sum to 1; PoolStats no-leak.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host → GaussianNB.build() | var_smoothing + priors cross here |
| host → reduce prim | X shape/values cross into the GATHER launch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02-01 | Tampering | var_smoothing intake | mitigate | build() rejects var_smoothing<0 → BuildError::InvalidVarSmoothing (D-05, before fit) |
| T-11-02-02 | Tampering | priors length / values | mitigate | build() rejects non-finite/negative entries; fit() rejects length≠n_classes → AlgoError::InvalidLabels |
| T-11-02-03 | Tampering | NaN/Inf in Gaussian LL | mitigate | var_ floored by epsilon_ (no div-by-zero); host log-sum-exp single terminal log (Pitfall 9) |
| T-11-02-04 | Denial of Service | predict geometry mismatch | mitigate | n_features-agreement guard → AlgoError before launch |
| — | Spoofing / Repudiation / Information Disclosure / Elevation | — | N/A | in-process numeric arrays; no auth/network/secret/privilege surface |

No `high`-severity threats. V5 Input Validation covered by the D-05 build/fit split.
</threat_model>

<verification>
- `cargo test --features cpu -p mlrs-algos --test gaussian_nb_test` exits 0
- exact labels match sklearn exactly (f64 + f32)
- predict_proba rows sum to 1.0 ± 1e-6
- epsilon_ global (Pitfall 3); no new kernel; grep clean of SharedMemory/INFINITY/Atomic
- PoolStats no-leak across re-fit
</verification>

<success_criteria>
GaussianNB (NB-01) fits/predicts matching sklearn within the documented band, exact
labels the hard gate, proba rows sum to 1, global epsilon_, default==sklearn, build()
rejects bad var_smoothing, re-fit leak-free.
</success_criteria>

<output>
Create `.planning/phases/11-naive-bayes/11-02-SUMMARY.md` when done.
</output>
