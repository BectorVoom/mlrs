---
phase: 11-naive-bayes
plan: 05
type: execute
wave: 3
depends_on: ["11-02", "11-03", "11-04"]
files_modified:
  - crates/mlrs-py/src/estimators/naive_bayes.rs
  - crates/mlrs-py/src/estimators/mod.rs
  - crates/mlrs-py/src/lib.rs
  - crates/mlrs-py/tests/pyclass_smoke_test.rs
  - crates/mlrs-py/tests/test_naive_bayes.py
autonomous: true
requirements: [NB-01, NB-02, NB-03, NB-04, NB-05, PY-06]
must_haves:
  truths:
    - "All five NB estimators are #[pyclass]-backed with fit/predict/predict_proba/predict_log_proba/score"
    - "Builder method + hyperparameter names mirror sklearn exactly per estimator (D-09 — zero translation)"
    - "MultinomialNB densifies sparse input at the PyO3 ingress (NB-02)"
    - "f32/f64 runtime dispatch with guard_f64 on the F64 arm before upload; GIL released during compute (py.detach)"
    - "BuildError + enum TryFrom failures → Python ValueError; fit AlgoError → ValueError via existing mappers (zero new mapper)"
    - "All five registered on the _mlrs module (add_class 25 → 30)"
    - "predict_proba rows sum to 1 across the FFI boundary; score returns accuracy via nb_common::accuracy_score"
    - "PY-06 cross-cutting sign-off: full v2 surface registered; estimator_checks re-triaged"
  artifacts:
    - path: "crates/mlrs-py/src/estimators/naive_bayes.rs"
      provides: "Five #[pyclass] wrappers (PyGaussianNB … PyCategoricalNB)"
      contains: "any_estimator!"
    - path: "crates/mlrs-py/tests/test_naive_bayes.py"
      provides: "Live FFI smoke: fit/predict/predict_proba/predict_log_proba/score + bad-hyperparam ValueError"
      contains: "def test_"
  key_links:
    - from: "crates/mlrs-py/src/lib.rs"
      to: "PyGaussianNB … PyCategoricalNB"
      via: "m.add_class registration"
      pattern: "add_class::<PyGaussianNB>"
    - from: "crates/mlrs-py/src/estimators/naive_bayes.rs"
      to: "nb_common::accuracy_score"
      via: "score() method"
      pattern: "accuracy_score"
---

<objective>
PY-06 final cross-cutting sign-off. Wrap all five NB estimators as `#[pyclass]`
(reusing the shipped `any_estimator!` macro + `py.detach` GIL-release pattern — v2
adds ZERO binding infrastructure, pyo3 stays 0.28), exposing the full sklearn surface
(`fit`/`predict`/`predict_proba`/`predict_log_proba`/`score`, `get_params`/`set_params`)
with sklearn-mirrored names (D-09 — zero translation), f32/f64 dispatch, and GIL release.
MultinomialNB densifies sparse input at ingress. Register the five on `_mlrs` (25→30),
extend the Rust smoke test, add a live-FFI Python smoke, and re-triage `estimator_checks`
across the full v2 surface.

Purpose: Complete the Python surface for Phase 11 and close the v2 PY-06 requirement.
Output: Five Py wrappers + registration + smoke green; v2 Python surface signed off.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-naive-bayes/11-RESEARCH.md
@.planning/phases/11-naive-bayes/11-PATTERNS.md
@.planning/phases/11-naive-bayes/11-02-SUMMARY.md
@.planning/phases/11-naive-bayes/11-03-SUMMARY.md
@.planning/phases/11-naive-bayes/11-04-SUMMARY.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Five #[pyclass] NB wrappers (sklearn-named, dtype dispatch, GIL release, sparse densify) + registration</name>
  <read_first>
    - crates/mlrs-py/src/estimators/linear.rs lines 831-1144 (PyMBSGDClassifier: any_estimator! Unfit arm, #[new] sklearn-default signature, fit dtype dispatch + py.detach + build_err_to_py/algo_err_to_py, predict/predict_proba/dtype-suffixed accessors, unfit_default/is_unfit)
    - crates/mlrs-py/src/dispatch.rs (any_estimator! macro — emits Unfit/F32/F64 enum only; #[pymethods] bodies are hand-written)
    - crates/mlrs-py/src/errors.rs lines 56-89 (algo_err_to_py, build_err_to_py, not_fitted → PyValueError)
    - crates/mlrs-py/src/estimators/mod.rs (add `pub mod naive_bayes;` alphabetically after `linear`)
    - crates/mlrs-py/src/lib.rs lines 199-247 (use + add_class block; add five NB imports + add_class lines)
    - 11-CONTEXT.md D-02 (sklearn defaults), D-07 (predict_log_proba + score), D-09 (sklearn-mirrored names per estimator), D-08 (CNB argmin internal — no special-case)
    - 11-RESEARCH.md §"PyO3 wrapper" code example, §"sparse densify" (MultinomialNB densify at ingress, PROJ-02 precedent)
    - 11-PATTERNS.md §naive_bayes.rs
  </read_first>
  <action>
    Create crates/mlrs-py/src/estimators/naive_bayes.rs with five `any_estimator!` blocks +
    `#[pyclass]` wrappers, copying PyMBSGDClassifier verbatim and renaming per estimator:
    - AnyGaussianNB unfit `{ var_smoothing: f64, priors: Option<Vec<f64>> }` — sklearn names, NO alpha (D-09); `#[new]` signature `(var_smoothing=1e-9, priors=None)` (D-02).
    - The four discrete unfit arms hold `{ alpha: f64, force_alpha: bool, fit_prior: bool, class_prior: Option<Vec<f64>> }` plus per-variant knobs: BernoulliNB `binarize: Option<f64>` (#[new] default Some(0.0)); ComplementNB `norm: bool` (default false); CategoricalNB `min_categories` (Option path — accept None / a uniform usize / a per-feature list, mapping to MinCategories).
    Each `fit` does float_dtype dispatch inside `py.detach(|| { let mut pool = crate::lock_pool(); match dt {…} })`
    with `guard_f64()?` on the F64 arm BEFORE upload, the builder chain using the sklearn-mirrored setters
    (D-09 — zero name translation), `.build().map_err(build_err_to_py)?`, `.fit(..).map_err(algo_err_to_py)?`.
    MultinomialNB's fit densifies sparse X at ingress (NB-02, PROJ-02 precedent — densify before upload).
    Expose `predict_labels` (i32), `predict_proba_f32`/`_f64`, `predict_log_proba_f32`/`_f64` (the new D-07
    surface, dtype-suffixed because the return is Vec<F>), `score(x, y)` calling `nb_common::accuracy_score`
    after predict_labels, `classes_`/`is_fitted`/`dtype` accessors, and `unfit_default()`/`is_unfit()` (the
    smoke seam). ComplementNB's argmin stays internal (D-08 — no PyO3 special-case). Use crate::lock_pool()
    (poison-recovering), NOT global_pool().lock().expect().

    Add `pub mod naive_bayes;` to estimators/mod.rs. In lib.rs add
    `use estimators::naive_bayes::{PyGaussianNB, PyMultinomialNB, PyBernoulliNB, PyComplementNB, PyCategoricalNB};`
    and five `m.add_class::<…>()?;` lines (registration 25 → 30).
  </action>
  <verify>
    <automated>cargo build -p mlrs-py --features cpu 2>&1 | tail -5; cargo build -p mlrs-py --features cpu</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "add_class::<PyGaussianNB>\|add_class::<PyMultinomialNB>\|add_class::<PyBernoulliNB>\|add_class::<PyComplementNB>\|add_class::<PyCategoricalNB>" crates/mlrs-py/src/lib.rs` == 5
    - `grep -n "accuracy_score" crates/mlrs-py/src/estimators/naive_bayes.rs` returns a match
    - `grep -n "predict_log_proba" crates/mlrs-py/src/estimators/naive_bayes.rs` returns a match
    - `grep -c "add_class" crates/mlrs-py/src/lib.rs` == 30
    - `cargo build -p mlrs-py --features cpu` exits 0
  </acceptance_criteria>
  <done>Five Py NB wrappers compile and register; sklearn-named surface; GIL release; sparse densify; score via accuracy_score.</done>
</task>

<task type="auto">
  <name>Task 2: Rust smoke (construct-unfit + fit/predict round-trip) + live-FFI Python smoke + estimator_checks re-triage</name>
  <read_first>
    - crates/mlrs-py/tests/pyclass_smoke_test.rs lines 32-68 (all_twelve_… / five_phase7_… construct-unfit template)
    - crates/mlrs-py/src/estimators/naive_bayes.rs (Task 1 — the five wrappers + unfit_default/is_unfit)
    - any existing crates/mlrs-py/tests/test_*.py (e.g. test_sgd.py from 10-05) — the live-FFI maturin-develop harness convention
    - 11-VALIDATION.md §"Manual-Only Verifications" (estimator_checks re-triage instructions)
    - project memory: 10-05 SUMMARY note (Rust binary cannot link PyErr::is_instance_of; the concrete ValueError assertion goes in the Python test, the typed witness in Rust)
  </read_first>
  <action>
    Extend pyclass_smoke_test.rs with `five_naive_bayes_estimators_construct_unfit` asserting each
    `Py{Gaussian,Multinomial,Bernoulli,Complement,Categorical}NB::unfit_default().is_unfit()`, and a
    Rust fit→predict round-trip per estimator on cpu (clean well-separated data → exact-or-finite
    predictions, predict_proba rows in [0,1] summing to 1, predict_log_proba == log of proba, score
    in [0,1]) for f32 (and f64 behind the f64-capable guard). The bad-hyperparameter ValueError
    assertion goes in the Python test (the Rust binary cannot link is_instance_of, 10-05 precedent).

    Create crates/mlrs-py/tests/test_naive_bayes.py (live-FFI, run via maturin develop — syntax-validate
    if maturin unavailable, the 10-05/08-05 precedent): construct each of the five with sklearn-named
    kwargs, fit/predict/predict_proba/predict_log_proba/score round-trip, assert predict_proba rows sum
    to 1.0±1e-6, and assert a bad hyperparameter (e.g. `GaussianNB(var_smoothing=-1.0)` or
    `MultinomialNB(alpha=-1.0)`) raises ValueError at fit. Also assert get_params/set_params expose the
    sklearn-named knobs.

    PY-06 cross-cutting sign-off: run the full v2 #[pyclass] registration check (30 add_class) and
    re-triage sklearn `estimator_checks` across the full v2 surface per 11-VALIDATION.md
    §Manual-Only (build the cpu wheel, run check_estimator against the five NB estimators, triage
    expected-skip vs real failures). Record the triage outcome in the SUMMARY.
  </action>
  <verify>
    <automated>cargo test -p mlrs-py --features cpu --test pyclass_smoke_test</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p mlrs-py --features cpu --test pyclass_smoke_test` exits 0 (incl. five_naive_bayes_… + fit/predict round-trips)
    - `grep -n "def test_" crates/mlrs-py/tests/test_naive_bayes.py` returns ≥5 matches
    - `python3 -c "import ast; ast.parse(open('crates/mlrs-py/tests/test_naive_bayes.py').read())"` exits 0 (syntax valid even if maturin unavailable)
    - predict_proba rows-sum-to-1 assertion present in both the Rust smoke and the Python test
    - estimator_checks re-triage outcome recorded in 11-05-SUMMARY.md
  </acceptance_criteria>
  <done>Rust smoke green (construct + fit/predict round-trip ×5); Python live-FFI smoke present + syntax-valid; estimator_checks re-triaged; PY-06 signed off.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python → #[pyclass] | untrusted sklearn-named hyperparameters + array data/shapes cross the FFI |
| #[pyclass] → estimator builder/fit | validated values cross into device compute |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05-01 | Tampering | untrusted hyperparameter (negative alpha/var_smoothing, non-finite prior) via FFI | mitigate | build().map_err(build_err_to_py) → ValueError at construction (D-05/D-09); validated before any upload |
| T-11-05-02 | Denial of Service | PyO3 panic crossing the FFI boundary | mitigate | `?` on typed errors, never panic across the boundary; py.detach body returns PyResult (mbsgd precedent) |
| T-11-05-03 | Denial of Service | f64 dispatch on an f64-incapable backend | mitigate | guard_f64()? on the F64 arm BEFORE upload → clear ValueError, no device alloc/downcast |
| T-11-05-04 | Tampering | untrusted data geometry (mismatched rows×cols / prior length) → OOB device read | mitigate | fit/predict geometry guards → AlgoError → algo_err_to_py ValueError before launch |
| T-11-05-05 | Tampering | poisoned global pool lock after a panic in another thread | mitigate | crate::lock_pool() is poison-recovering (WR-04), NOT .lock().expect() |
| — | Spoofing / Repudiation / Information Disclosure / Elevation | — | N/A | in-process library; no auth/network/secret/privilege surface |

No `high`-severity threats. The FFI boundary panic-safety (T-11-05-02) + validate-before-launch
(T-11-05-01/04) are the load-bearing controls; both follow the shipped mbsgd/KDE precedent.
</threat_model>

<verification>
- `cargo build -p mlrs-py --features cpu` exits 0
- `cargo test -p mlrs-py --features cpu --test pyclass_smoke_test` exits 0
- 30 add_class registrations; five NB wrappers with sklearn-named surface
- predict_proba rows sum to 1 across the FFI; score via accuracy_score
- test_naive_bayes.py syntax-valid; estimator_checks re-triaged
</verification>

<success_criteria>
PY-06 complete: all five NB estimators #[pyclass]-backed with the full sklearn surface
(fit/predict/predict_proba/predict_log_proba/score, get_params/set_params), sklearn-mirrored
names, f32/f64 dispatch, GIL release, sparse densify (MultinomialNB), registered on _mlrs
(25→30). Smoke green; Python live-FFI smoke present; estimator_checks re-triaged across the
full v2 surface. NB-01…NB-05 + PY-06 met.
</success_criteria>

<output>
Create `.planning/phases/11-naive-bayes/11-05-SUMMARY.md` when done.
</output>
