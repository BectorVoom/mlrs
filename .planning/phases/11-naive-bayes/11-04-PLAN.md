---
phase: 11-naive-bayes
plan: 04
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - crates/mlrs-algos/src/naive_bayes/categorical_nb.rs
  - crates/mlrs-algos/tests/categorical_nb_test.rs
autonomous: true
requirements: [NB-05]
must_haves:
  truths:
    - "A user can fit CategoricalNB on integer-encoded features with predict/predict_proba/predict_log_proba matching sklearn"
    - "Exact predict labels match sklearn EXACTLY (hard gate) on cpu f64 + f32"
    - "feature_log_prob_ is a ragged Vec<Vec<f64>> (one matrix per feature, variable category count), not a single tensor"
    - "Per-feature denominator class_count[c] + alpha·n_categories_j (Pitfall 4)"
    - "MinCategories::{Infer,Uniform,PerFeature} padding honored; n_categories_j = max(observed+1, min_categories_j)"
    - "fit() rejects negative / non-integer categorical input with AlgoError::InvalidCategoricalInput"
    - "predict guards the lookup index against n_categories_j (unseen category → smoothed prob)"
    - "predict_proba rows sum to 1; re-fit leak-free"
  artifacts:
    - path: "crates/mlrs-algos/src/naive_bayes/categorical_nb.rs"
      provides: "CategoricalNB with ragged feature_log_prob_ + MinCategories padding"
      contains: "Vec<Vec"
    - path: "crates/mlrs-algos/tests/categorical_nb_test.rs"
      provides: "Oracle tests incl. min_categories + fit_rejects_bad_input"
      contains: "fn min_categories"
  key_links:
    - from: "crates/mlrs-algos/src/naive_bayes/categorical_nb.rs"
      to: "MinCategories enum"
      via: "per-feature category padding"
      pattern: "MinCategories"
    - from: "crates/mlrs-algos/src/naive_bayes/categorical_nb.rs"
      to: "nb_common::log_sum_exp_normalize"
      via: "per-feature looked-up log-prob sum → normalize"
      pattern: "log_sum_exp_normalize"
---

<objective>
Fill CategoricalNB (NB-05): per-feature categorical likelihood over integer-encoded
features with the ragged `feature_log_prob_` host layout (one n_classes×n_categories_j
matrix per feature, variable category count — Pitfall 7) and the `MinCategories` enum
padding (D-04). Independent struct (D-03). No GEMM — the joint LL sums per-feature
looked-up log-probs in host f64.

Purpose: Deliver the structurally-distinct categorical variant (ragged tables, integer
inputs, per-feature lookup).
Output: CategoricalNB with Fit/PredictLabels/PredictProba/PredictLogProba + green oracle.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-naive-bayes/11-RESEARCH.md
@.planning/phases/11-naive-bayes/11-PATTERNS.md
@.planning/research/FEATURES.md
@.planning/phases/11-naive-bayes/11-01-SUMMARY.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CategoricalNB fit — ragged per-feature counts + MinCategories padding + integer-input validation</name>
  <read_first>
    - crates/mlrs-algos/src/naive_bayes/categorical_nb.rs (Wave-0 stub incl. MinCategories enum + min_categories field)
    - crates/mlrs-algos/src/density/kernel_density.rs lines 108-119 (BandwidthSpec enum precedent for MinCategories)
    - crates/mlrs-algos/src/naive_bayes/nb_common.rs (empirical_class_log_prior, log_sum_exp_normalize, argmax_decode)
    - .planning/research/FEATURES.md §CategoricalNB (feature_log_prob_[j][c,k]=log((count+alpha)/(class_count[c]+alpha·n_categories_j)); ragged list per feature; min_categories=None infer/pad; non-negative integer-encoded inputs)
    - 11-RESEARCH.md Pitfall 7 (ragged layout + unseen categories + min_categories padding), A3 (pin the fixture to NO unseen categories first)
    - crates/mlrs-algos/src/error.rs (AlgoError::InvalidCategoricalInput, added Wave 0)
  </read_first>
  <behavior>
    - fit validates X entries are non-negative integers (within f-rounding tolerance) → else AlgoError::InvalidCategoricalInput.
    - n_categories_j = max(observed_max+1, min_categories_j) where min_categories_j comes from MinCategories (Infer → observed_max+1; Uniform(u) → max(observed+1,u); PerFeature(v) → max(observed+1,v[j])).
    - category_count_[j][c,k] counts class-c rows with feature j == category k; feature_log_prob_[j][c,k] = log((count+alpha)/(class_count[c]+alpha·n_categories_j)).
    - feature_log_prob_ is Vec<Vec<f64>> shaped (per feature j: n_classes × n_categories_j), ragged.
  </behavior>
  <action>
    Implement CategoricalNB::fit: geometry guard → validate non-negative-integer input
    (AlgoError::InvalidCategoricalInput, D-05 data-dependent) → classes_ sort/dedup → for each
    feature j compute n_categories_j via the MinCategories enum padding → host-tabulate
    category_count_[j][c,k] (one owner per (class,feature,category) — a host count, never a device
    scatter) → feature_log_prob_[j][c,k] with denominator `class_count[c] + alpha·n_categories_j`
    (Pitfall 4). class_log_prior_ via empirical_class_log_prior when fit_prior=true & class_prior=None.
    Store feature_log_prob_ as the ragged `Vec<Vec<f64>>` (Pitfall 7 — NOT a single tensor). The
    sufficient-statistic tabulation is small host f64 work; the v1 reduce prim is not required for
    the categorical counts (planner's discretion — document if reduce is used). release_into(pool)
    any device scratch + prior fitted on re-fit (WR-07).
  </action>
  <verify>
    <automated>cargo build --features cpu -p mlrs-algos 2>&1 | tail -3; cargo test --features cpu -p mlrs-algos --test categorical_nb_test fit_rejects_bad_input</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Vec<Vec" crates/mlrs-algos/src/naive_bayes/categorical_nb.rs` returns a match (ragged feature_log_prob_)
    - `grep -n "MinCategories" crates/mlrs-algos/src/naive_bayes/categorical_nb.rs` returns a match
    - `grep -c "SharedMemory\|F::INFINITY\|Atomic" crates/mlrs-algos/src/naive_bayes/categorical_nb.rs` (after `grep -v '^//'`) == 0
    - `cargo test --features cpu -p mlrs-algos --test categorical_nb_test fit_rejects_bad_input` exits 0
  </acceptance_criteria>
  <done>CategoricalNB fits with ragged feature_log_prob_, MinCategories padding, integer-input validation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CategoricalNB predict (per-feature lookup, unseen-category guard) + oracle (exact labels, min_categories, no-leak)</name>
  <read_first>
    - crates/mlrs-algos/src/naive_bayes/categorical_nb.rs (Task 1 fit + ragged feature_log_prob_)
    - crates/mlrs-algos/src/naive_bayes/nb_common.rs (log_sum_exp_normalize, argmax_decode)
    - crates/mlrs-algos/tests/categorical_nb_test.rs (Wave-0 scaffolds)
    - .planning/research/FEATURES.md §CategoricalNB (joint LL sums per-feature looked-up log-probs; unseen category → smoothed prob)
    - 11-RESEARCH.md Pitfall 7 (guard the predict-time lookup index against n_categories_j), A3
    - crates/mlrs-algos/tests/mbsgd_classifier_test.rs lines 193-232, 388-463 (oracle harness template)
  </read_first>
  <behavior>
    - predict joint LL[i,c] = class_log_prior_[c] + Σ_j feature_log_prob_[j][c, x[i,j]] with the lookup index guarded against n_categories_j (unseen category index ≥ n_categories_j → the smoothed log(alpha/denom)).
    - exact predict labels == sklearn (argmax); predict_proba rows sum to 1.0±1e-6.
    - min_categories padding test: a MinCategories::Uniform(k) padding yields the sklearn-matching feature_log_prob_ shape and predictions.
  </behavior>
  <action>
    Implement CategoricalNB PredictLabels/PredictProba/PredictLogProba: geometry guard → per query
    row, per feature j, look up `feature_log_prob_[j][c][k]` for category k=x[i,j] (guard k against
    n_categories_j — an out-of-range/unseen category maps to the smoothed prob per A3) → sum over
    features + class_log_prior_ → nb_common::log_sum_exp_normalize + argmax_decode. All host f64
    (small per-row decision). Un-#[ignore] and fill categorical_nb_test.rs: exact_labels (f64 skip +
    f32), min_categories (MinCategories::Uniform/PerFeature padding matches sklearn), proba_band
    (rows sum to 1), default_matches_sklearn (alpha=1.0, min_categories=None), fit_rejects_bad_input
    (already validated Task 1 — keep green), refit_releases_buffers. Pin the exact-label fixture to a
    case WITHOUT unseen categories first (A3).
  </action>
  <verify>
    <automated>cargo test --features cpu -p mlrs-algos --test categorical_nb_test</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --features cpu -p mlrs-algos --test categorical_nb_test` exits 0 (all cases green, none ignored)
    - exact_labels asserts integer equality with no band
    - `grep -n "fn min_categories" crates/mlrs-algos/tests/categorical_nb_test.rs` returns a match
    - proba rows sum to 1.0±1e-6; `grep -c "skip_f64_with_log" crates/mlrs-algos/tests/categorical_nb_test.rs` ≥ 1
  </acceptance_criteria>
  <done>CategoricalNB oracle green on cpu (f64+f32); exact labels hard gate; ragged lookup + min_categories padding; unseen-category guard; PoolStats no-leak.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host → CategoricalNB.fit() | integer-encoded X + min_categories cross here |
| predict → ragged-table lookup | query category index indexes feature_log_prob_[j] |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04-01 | Tampering | negative / non-integer categorical input → OOB ragged-table index | mitigate | fit() validates non-negative-integer input → AlgoError::InvalidCategoricalInput (D-05) before any table build |
| T-11-04-02 | Tampering | unseen / out-of-range category at predict → OOB ragged lookup | mitigate | predict guards the lookup index against n_categories_j; unseen → smoothed prob (Pitfall 7, A3) |
| T-11-04-03 | Tampering | min_categories entries negative → invalid padding | mitigate | build() rejects negative min_categories entries → BuildError::InvalidMinCategories (D-05) |
| T-11-04-04 | Tampering | NaN/Inf from log(0) | mitigate | alpha smoothing keeps the argument positive; host log-sum-exp single terminal log |
| — | Spoofing / Repudiation / Information Disclosure / Elevation | — | N/A | in-process numeric arrays; no auth/network/secret/privilege surface |

No `high`-severity threats. The OOB ragged-table lookup (T-11-04-02) is the one genuine
memory-safety concern; the index guard against n_categories_j is the standard control.
</threat_model>

<verification>
- `cargo test --features cpu -p mlrs-algos --test categorical_nb_test` exits 0
- exact labels match sklearn exactly (f64 + f32)
- ragged Vec<Vec<f64>> feature_log_prob_; MinCategories padding; per-feature denominator
- fit rejects bad input; predict guards the lookup index; no new kernel; PoolStats no-leak
</verification>

<success_criteria>
CategoricalNB (NB-05) fits/predicts matching sklearn within band, exact labels the hard
gate, ragged per-feature log-prob tables, MinCategories padding, integer-input validation,
unseen-category-safe lookup, proba rows sum to 1, re-fit leak-free.
</success_criteria>

<output>
Create `.planning/phases/11-naive-bayes/11-04-SUMMARY.md` when done.
</output>
