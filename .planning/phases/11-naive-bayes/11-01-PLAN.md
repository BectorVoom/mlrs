---
phase: 11-naive-bayes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/mlrs-algos/src/traits.rs
  - crates/mlrs-algos/src/error.rs
  - crates/mlrs-algos/src/lib.rs
  - crates/mlrs-algos/src/naive_bayes/mod.rs
  - crates/mlrs-algos/src/naive_bayes/nb_common.rs
  - crates/mlrs-algos/src/naive_bayes/gaussian_nb.rs
  - crates/mlrs-algos/src/naive_bayes/multinomial_nb.rs
  - crates/mlrs-algos/src/naive_bayes/bernoulli_nb.rs
  - crates/mlrs-algos/src/naive_bayes/complement_nb.rs
  - crates/mlrs-algos/src/naive_bayes/categorical_nb.rs
  - crates/mlrs-algos/tests/nb_common_test.rs
  - crates/mlrs-algos/tests/gaussian_nb_test.rs
  - crates/mlrs-algos/tests/multinomial_nb_test.rs
  - crates/mlrs-algos/tests/bernoulli_nb_test.rs
  - crates/mlrs-algos/tests/complement_nb_test.rs
  - crates/mlrs-algos/tests/categorical_nb_test.rs
  - scripts/gen_oracle.py
autonomous: true
requirements: [NB-01, NB-02, NB-03, NB-04, NB-05]
must_haves:
  truths:
    - "The `PredictLogProba<F>` trait exists in traits.rs alongside PredictProba"
    - "nb_common free functions (log_sum_exp_normalize, empirical_class_log_prior, argmax_decode, argmin_decode, accuracy_score, class_grouped_sum) exist and are standalone-validated"
    - "class_grouped_sum composes the v1 reduce prim (GATHER), passes the --features cpu launch, no new #[cube] kernel"
    - "All five estimator structs + builders compile as stubs with sklearn-default Default impls"
    - "MinCategories enum exists with Infer/Uniform/PerFeature variants"
    - "New BuildError + AlgoError NB variants exist"
    - "Ten committed .npz fixtures (5 variants × f32/f64) regenerate from gen_oracle.py"
    - "Five #[ignore] oracle test scaffolds compile and collect"
  artifacts:
    - path: "crates/mlrs-algos/src/naive_bayes/nb_common.rs"
      provides: "Shared NB free functions incl. the class_grouped_sum GATHER helper"
      contains: "fn class_grouped_sum"
    - path: "crates/mlrs-algos/src/naive_bayes/mod.rs"
      provides: "Module index re-exporting five estimators + MinCategories"
      contains: "pub mod nb_common"
    - path: "crates/mlrs-algos/src/traits.rs"
      provides: "PredictLogProba trait"
      contains: "trait PredictLogProba"
    - path: "scripts/gen_oracle.py"
      provides: "gen_gaussian_nb/gen_multinomial_nb/gen_bernoulli_nb/gen_complement_nb/gen_categorical_nb"
      contains: "def gen_gaussian_nb"
  key_links:
    - from: "crates/mlrs-algos/src/naive_bayes/nb_common.rs"
      to: "mlrs_backend::prims::reduce"
      via: "column_reduce over per-class row-gathered buffers"
      pattern: "column_reduce|row_reduce"
    - from: "crates/mlrs-algos/src/lib.rs"
      to: "naive_bayes module"
      via: "pub mod declaration"
      pattern: "pub mod naive_bayes"
---

<objective>
Wave-0 shared seam for Phase 11 Naive Bayes. Front-load every shared-file edit
(traits.rs, error.rs, lib.rs), the `nb_common` free-function module (D-03 — NO
shared base struct), the `MinCategories` enum (D-04), all five estimator struct
homes as compiling stubs (sklearn-default builders, `todo!()` fit bodies), the
gen_oracle.py generators + ten committed `.npz` fixtures, and the five `#[ignore]`
oracle test scaffolds. This makes Waves 1/2 file-disjoint and parallel-safe and
resolves research assumptions A1 (gemm transpose_b) and A5 (sum-of-squares path).

Mirrors the 10-01 / 09-01 / 08-01 Wave-0 scaffold precedent exactly.

Purpose: Establish the parallel-buildable property (ROADMAP) so the five estimator
plans never touch the same file, and stand up the GATHER substrate + log-sum-exp
helpers once (DRY at the function level, D-03).
Output: Compiling stubs + standalone-validated nb_common + committed fixtures +
collectable #[ignore] test scaffolds.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-naive-bayes/11-CONTEXT.md
@.planning/phases/11-naive-bayes/11-RESEARCH.md
@.planning/phases/11-naive-bayes/11-PATTERNS.md
@.planning/phases/11-naive-bayes/11-VALIDATION.md
@.planning/research/FEATURES.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared-file edits — PredictLogProba trait, NB error variants, MinCategories enum, module index, lib.rs wiring</name>
  <read_first>
    - crates/mlrs-algos/src/traits.rs (the PredictProba trait at line 256 — copy its shape for PredictLogProba)
    - crates/mlrs-algos/src/error.rs (BuildError enum at line 368, InvalidAlpha at 373; AlgoError enum at line 30, InvalidLabels at 331, Prim(#[from]) at 345)
    - crates/mlrs-algos/src/lib.rs (pub mod block lines 42-51 — add naive_bayes alphabetically after `linear`)
    - crates/mlrs-algos/src/linear/mod.rs (module index analog per 11-PATTERNS.md §mod.rs)
    - crates/mlrs-algos/src/density/kernel_density.rs lines 108-119 (BandwidthSpec enum — the MinCategories precedent)
    - 11-PATTERNS.md §"Shared Patterns" → "Error handling — two-tier" (the exact new-variant list)
    - 11-CONTEXT.md D-03, D-04, D-07; 11-RESEARCH.md §"Wave 0 Gaps"
  </read_first>
  <action>
    In traits.rs add `pub trait PredictLogProba<F> where F: Float + CubeElement + Pod`
    with a single method `predict_log_proba(&self, pool, x, shape) -> Result<DeviceArray<ActiveRuntime, F>, AlgoError>`
    mirroring the PredictProba signature verbatim (returns the flat n_samples×n_classes
    row-major log-probability buffer; rustdoc notes it returns `joint_ll - logsumexp`,
    rows of `exp(.)` sum to 1). Do NOT add a default body.

    In error.rs add to BuildError (each following the existing `#[error("...")] Variant { estimator: &'static str, .. }` shape):
    `InvalidVarSmoothing { estimator, var_smoothing: f64 }`,
    `InvalidClassPrior { estimator }`,
    `InvalidMinCategories { estimator }`. REUSE `BuildError::InvalidAlpha` for the discrete variants (do not duplicate).
    Add to AlgoError: `InvalidCategoricalInput { estimator, reason: String }`. REUSE
    `AlgoError::InvalidLabels { estimator, reason }` for the prior-length-mismatch and
    n_features-disagreement data-dependent checks (do not add a new variant for those).

    Create crates/mlrs-algos/src/naive_bayes/mod.rs: module-level doc explaining D-03
    (five mutually-independent structs sharing only free functions, NO NbBase); declare
    `pub mod nb_common; pub mod gaussian_nb; pub mod multinomial_nb; pub mod bernoulli_nb;
    pub mod complement_nb; pub mod categorical_nb;`; re-export the five estimator structs
    and the `MinCategories` enum (`pub use categorical_nb::MinCategories;`).

    Define `MinCategories` in categorical_nb.rs (created in Task 2) — but DECLARE its
    existence here so mod.rs re-exports compile. Enum shape (D-04, modeled on BandwidthSpec):
    `pub enum MinCategories { Infer, Uniform(usize), PerFeature(Vec<usize>) }` —
    note `PerFeature` forces `Clone` not `Copy`.

    Add `pub mod naive_bayes;` to lib.rs alphabetically after `pub mod linear;`.
  </action>
  <verify>
    <automated>cargo build --features cpu -p mlrs-algos 2>&1 | tail -5; cargo build --features cpu -p mlrs-algos</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "pub trait PredictLogProba" crates/mlrs-algos/src/traits.rs` returns a match
    - `grep -n "InvalidVarSmoothing\|InvalidClassPrior\|InvalidMinCategories" crates/mlrs-algos/src/error.rs` returns ≥3 matches
    - `grep -n "InvalidCategoricalInput" crates/mlrs-algos/src/error.rs` returns a match
    - `grep -n "pub mod naive_bayes" crates/mlrs-algos/src/lib.rs` returns a match
    - `cargo build --features cpu -p mlrs-algos` exits 0
  </acceptance_criteria>
  <done>traits.rs/error.rs/lib.rs/mod.rs compile with the new NB surface; MinCategories enum declared.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: nb_common free functions (incl. class_grouped_sum GATHER) standalone-validated + five compiling estimator stubs</name>
  <read_first>
    - crates/mlrs-algos/src/density/kernel_density.rs lines 58 (reduce import), 331-360 (device reduce + host terminal log), 434-500 (host f64 free fns)
    - crates/mlrs-backend/src/prims/reduce.rs lines 181-260 (row_reduce/column_reduce + ScalarOp; SumSq at 260-301 resolves A5), 202-219 (host-segmentation = the GATHER substrate)
    - crates/mlrs-backend/src/prims/gemm.rs lines 54-80 (gemm signature; transa/transb flags resolve A1 — transb=true is available)
    - crates/mlrs-algos/src/linear/mbsgd_classifier.rs lines 35-46 (struct shape), 54-132 (builder/Default), 218-286 (build validation), 289-396 (fit guard + classes_ sort/dedup)
    - 11-RESEARCH.md §"Pattern 1" (class_grouped_sum signature), §"Pattern 3" (log_sum_exp_normalize), Pitfall 1/2/9
    - 11-PATTERNS.md §nb_common.rs
  </read_first>
  <behavior>
    - log_sum_exp_normalize(joint_ll: &[f64], n_classes: usize) -> (Vec<f64> proba, Vec<f64> log_proba): for a single row of n_classes LLs, proba sums to 1.0±1e-12; log_proba == joint_ll - lse; verified against a hand-computed 3-class example.
    - empirical_class_log_prior(class_count: &[f64]) -> Vec<f64>: returns log(count[c]/Σcount); a uniform [10,10] input yields [ln 0.5, ln 0.5].
    - argmax_decode(joint_ll rows, classes_: &[i64]) -> Vec<i32>: returns classes_[argmax] per row; argmin_decode returns classes_[argmin].
    - accuracy_score(pred: &[i32], y_true: &[i32]) -> f64: fraction equal; [1,1,0] vs [1,0,0] → 2/3.
    - class_grouped_sum(pool, x, shape, class_of_row, n_classes) -> Vec<Vec<f64>>: n_classes×n_features; row c col j = Σ over rows with that class; validated on a 4×2 host example with 2 classes against a host reference; runs under --features cpu (launch witness).
  </behavior>
  <action>
    Create crates/mlrs-algos/src/naive_bayes/nb_common.rs with ONLY free functions (D-03 —
    no struct, no trait): `log_sum_exp_normalize`, `empirical_class_log_prior`,
    `argmax_decode`, `argmin_decode`, `accuracy_score`, and `class_grouped_sum` (the GATHER
    helper). class_grouped_sum host-groups rows by class (one owner per class), uploads each
    class's contiguous row block via `DeviceArray::from_host`, `column_reduce(ScalarOp::Sum)`
    over it, and `release_into(pool)` each scratch buffer (WR-07). It returns an
    `n_classes × n_features` f64 host matrix — a GATHER, NEVER a scatter-add: NO new `#[cube]`
    kernel, NO SharedMemory, NO atomics, NO `F::INFINITY` (Pitfall 1/2, cubecl-cpu-no-shared-memory).
    log_sum_exp_normalize follows Pattern 3 (per-row max-shift, single terminal log, host f64).
    For GaussianNB variance, resolve A5: confirm `ScalarOp::SumSq` per-axis is usable via
    column_reduce; if a per-axis SumSq is not exposed, document a squared-host-copy then Sum
    (planner's discretion per A5 — note the choice in the SUMMARY).

    Create the five estimator stub files (gaussian_nb.rs, multinomial_nb.rs, bernoulli_nb.rs,
    complement_nb.rs, categorical_nb.rs) each with: the struct (device-resident `Option<DeviceArray>`
    fitted fields + `classes_: Vec<i64>` + `n_features: usize`), a `builder()` + `Builder` +
    `Default` seeding sklearn defaults per D-02 (GaussianNB: var_smoothing=1e-9, priors=None;
    discrete: alpha=1.0, force_alpha=true, fit_prior=true, class_prior=None; BernoulliNB:
    binarize=Some(0.0); ComplementNB: norm=false; CategoricalNB: min_categories=MinCategories::Infer),
    sklearn-mirrored setter names per D-09 (GaussianNB `.priors(..).var_smoothing(..)`, NO alpha;
    the four discrete `.class_prior(..).alpha(..).force_alpha(..).fit_prior(..)` + per-variant
    knobs), a `build() -> Result<Self, BuildError>` with the data-INDEPENDENT predicates from D-05
    (alpha>=0 / var_smoothing>=0 / min_categories entries non-negative / priors entries finite+non-negative)
    AND the D-06 force_alpha clip+warn for the four discrete variants, and a `Fit` impl whose body
    has a REAL geometry guard (n==0 / x.len()!=n*d → AlgoError::Prim(ShapeMismatch)) before a
    `todo!()` compute body. Define `MinCategories` in categorical_nb.rs (Task-1 mod.rs re-exports it).
    Do NOT implement PredictLabels/PredictProba/PredictLogProba yet — Wave 1.

    Create crates/mlrs-algos/tests/nb_common_test.rs (NOT #[ignore]) exercising the
    five non-device free functions on hand-computed examples + a class_grouped_sum
    launch witness under --features cpu (per the <behavior> block).
  </action>
  <verify>
    <automated>cargo test --features cpu -p mlrs-algos --test nb_common_test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "fn class_grouped_sum" crates/mlrs-algos/src/naive_bayes/nb_common.rs` returns a match
    - `grep -c "SharedMemory\|F::INFINITY\|Atomic" crates/mlrs-algos/src/naive_bayes/nb_common.rs` (after `grep -v '^//'`) == 0
    - `grep -n "column_reduce\|row_reduce" crates/mlrs-algos/src/naive_bayes/nb_common.rs` returns a match
    - `grep -n "pub enum MinCategories" crates/mlrs-algos/src/naive_bayes/categorical_nb.rs` returns a match
    - `cargo test --features cpu -p mlrs-algos --test nb_common_test` exits 0 (incl. the class_grouped_sum launch witness)
    - `cargo build --features cpu -p mlrs-algos` exits 0 (all five stubs compile)
  </acceptance_criteria>
  <done>nb_common standalone-validated on cpu (GATHER launches); five estimator stubs compile with sklearn-default builders + real geometry guards + todo!() bodies.</done>
</task>

<task type="auto">
  <name>Task 3: gen_oracle.py NB generators + ten committed fixtures + five #[ignore] oracle test scaffolds</name>
  <read_first>
    - scripts/gen_oracle.py lines 1753-1773 (_sgd_blobs class-blob generator — reusable for GaussianNB), 1776-1865 (gen_mbsgd_classifier per-estimator generator shape), 2007-2162 (main() dispatch + dtype loop)
    - crates/mlrs-algos/tests/mbsgd_classifier_test.rs lines 77-117 (fixture path + dtype/band helpers), 193-232 (exact_labels + skip_f64_with_log), 388-463 (proba band + default_matches_sklearn + build_rejects)
    - .planning/research/FEATURES.md §"Family 5" (the per-variant math + which sklearn attrs to store)
    - project memory: oracle-fixture-regen-needs-venv (regen needs /tmp numpy/sklearn venv, PEP 668; blobs committed, CI never runs it)
    - 11-PATTERNS.md §gen_oracle.py + §oracle test files
  </read_first>
  <action>
    Add to scripts/gen_oracle.py: `gen_gaussian_nb`, `gen_multinomial_nb`, `gen_bernoulli_nb`,
    `gen_complement_nb`, `gen_categorical_nb`, each `(seed=SEED, dtype=...)`. GaussianNB reuses
    `_sgd_blobs` (continuous, well-separated, n_classes=2 or 3). The three count-based variants
    need a new small integer-count `X` generator (Poisson/integer counts, non-negative, well-separated
    classes); CategoricalNB needs a new integer-encoded categorical-feature generator (small
    per-feature category counts, no unseen categories at predict per A3). Each fits the corresponding
    sklearn estimator with DEFAULT hyperparameters (so the default-matches-sklearn test is meaningful),
    and savez `X`, `Xq`, `y`, `predict` (exact-label hard gate), `predict_proba` (band gate) in the
    fixture dtype. Register all five in main()'s `for dtype in (np.float32, np.float64)` block →
    ten fixtures `tests/fixtures/{gaussian,multinomial,bernoulli,complement,categorical}_nb_{f32,f64}_seed42.npz`.
    Regenerate via the /tmp numpy/scipy/sklearn venv and COMMIT the ten .npz blobs.

    Create the five test files (gaussian_nb_test.rs … categorical_nb_test.rs) copying the
    mbsgd_classifier_test.rs harness (fixture()/host_to_f64/assert_band helpers). Each carries
    #[ignore]-marked scaffolds for the Wave-1 oracle tests named per 11-VALIDATION.md (exact_labels
    f64+f32, proba_band, default_matches_sklearn, build_rejects_*, refit_releases_buffers, plus the
    per-variant cases: bernoulli binarize_none, complement norm_true, categorical min_categories +
    fit_rejects_bad_input). Each scaffold asserts fixture-load + shape ONLY (fit is todo!() in Wave 0),
    so they collect but #[ignore] until Wave 1 fills the estimator. Every f64 test calls
    `capability::skip_f64_with_log()` and returns early.
  </action>
  <verify>
    <automated>ls tests/fixtures/gaussian_nb_f32_seed42.npz tests/fixtures/categorical_nb_f64_seed42.npz && cargo test --features cpu -p mlrs-algos --test gaussian_nb_test -- --list</automated>
  </verify>
  <acceptance_criteria>
    - `ls tests/fixtures/*_nb_{f32,f64}_seed42.npz | wc -l` == 10
    - `grep -c "def gen_gaussian_nb\|def gen_multinomial_nb\|def gen_bernoulli_nb\|def gen_complement_nb\|def gen_categorical_nb" scripts/gen_oracle.py` == 5
    - `cargo test --features cpu -p mlrs-algos --test gaussian_nb_test -- --list` exits 0 (scaffolds collect)
    - each test file contains `skip_f64_with_log` (grep returns a match in all five)
    - `cargo build --features cpu -p mlrs-algos --tests` exits 0
  </acceptance_criteria>
  <done>Ten committed fixtures; five oracle test files compile and collect with #[ignore] scaffolds; Wave gate satisfied (Waves 1/2 are file-disjoint).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host → estimator builder | data-independent hyperparameters (alpha, var_smoothing, priors) cross here |
| host → reduce/gemm prim | array shapes + values cross into the device launch grid |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | build() hyperparameter intake | mitigate | per D-05, build() validates alpha>=0 / var_smoothing>=0 / finite priors → typed BuildError BEFORE any data/device touch |
| T-11-02 | Denial of Service | class_grouped_sum reduce launch | mitigate | real geometry guard (x.len()!=n*d) before launch; the reduce prim already u32::try_from-guards the grid (reduce.rs precedent) |
| T-11-03 | Tampering | NaN/Inf propagation in log-sum-exp | mitigate | host f64 max-shift + single terminal log (Pattern 3); never F::INFINITY mid-pipeline |
| T-11-SC | Tampering | npm/pip/cargo installs | N/A | phase installs ZERO external packages (v2 zero-dependency constraint); gen_oracle.py uses a throwaway /tmp venv at fixture-gen time only, blobs committed, never in CI |
| — | Spoofing | — | N/A | no auth/identity surface (in-process numeric arrays) |
| — | Repudiation | — | N/A | no audit/logging-integrity surface |
| — | Information Disclosure | — | N/A | no secrets / PII / network surface |
| — | Elevation of Privilege | — | N/A | no privilege boundary (single-process library) |

No `high`-severity threats. Surface is in-process numeric arrays + hyperparameters; the validate-before-launch contract (D-05) covers V5 Input Validation (the only applicable ASVS L1 category).
</threat_model>

<verification>
- `cargo build --features cpu -p mlrs-algos` exits 0 (shared seam + five stubs compile)
- `cargo test --features cpu -p mlrs-algos --test nb_common_test` exits 0 (free functions + GATHER launch witness)
- All five oracle test files collect under `--test <variant>_nb_test -- --list`
- Ten `.npz` fixtures committed
- No new `#[cube]` kernel; nb_common grep clean of SharedMemory/F::INFINITY/Atomic
</verification>

<success_criteria>
PredictLogProba trait + NB error variants + MinCategories enum landed; nb_common
standalone-validated on cpu (GATHER launches, no scatter-add); five estimator stubs
compile with sklearn-default builders and real geometry guards; ten fixtures committed;
five #[ignore] oracle test scaffolds collect. Waves 1/2 are now file-disjoint.
</success_criteria>

<output>
Create `.planning/phases/11-naive-bayes/11-01-SUMMARY.md` when done.
</output>
