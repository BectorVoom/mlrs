---
phase: 14-umap
plan: 07
type: execute
wave: 2
depends_on: ["14-06"]
files_modified:
  - crates/mlrs-algos/src/manifold/umap.rs
  - crates/mlrs-algos/tests/umap_test.rs
  - .planning/phases/14-umap/14-VALIDATION.md
autonomous: true
gap_closure: true
requirements: [UMAP-03]
must_haves:
  truths:
    - "The fit-path layout launch passes move_other=0 — no owner-cube writes a foreign vertex's coordinates, so concurrently-scheduled cubes never race on the same embedding slots (the D-05 byte-identical contract holds on ANY parallel backend, not by accident of the sequential cpu-MLIR gate)"
    - "Each undirected fuzzy-graph pair is processed once per direction (owner-only over the already-symmetric COO), matching umap-learn's single-pass head/tail force schedule instead of the previous ~2-4× double-count"
    - "A concurrency/parallel-scheduling-simulating determinism test exercises the fit layout and asserts byte-identical output across runs, so the D-05 contract is tested for the cross-cube-write hazard rather than only on the sequential backend"
    - "PROPERTY_EPS and ARI_BAND in 14-VALIDATION.md and umap_test.rs are re-derived against the corrected (un-doubled) move_other=0 force schedule, and a per-pair sample-count assertion vs expected epochs_per_sample guards against future schedule regressions"
    - "reproducible_f64 and the layout_property_<metric> family stay green after the schedule change"
  artifacts:
    - path: "crates/mlrs-algos/src/manifold/umap.rs"
      provides: "fit-path host_epoch_driver launch with move_other=0 (owner-only over symmetric COO)"
      contains: "0u32"
    - path: "crates/mlrs-algos/tests/umap_test.rs"
      provides: "concurrency/scheduling-order determinism test + per-pair sample-count assertion"
      contains: "fit_layout_order_independent"
    - path: ".planning/phases/14-umap/14-VALIDATION.md"
      provides: "recalibrated PROPERTY_EPS / ARI_BAND measured against move_other=0 schedule"
      contains: "move_other"
  key_links:
    - from: "crates/mlrs-algos/src/manifold/umap.rs host_epoch_driver"
      to: "crates/mlrs-kernels/src/umap_layout.rs umap_layout_step"
      via: "fit launch passes move_other=0 so the kernel skips the foreign-vertex write at umap_layout.rs:155-157"
      pattern: "move_other"
---

<objective>
Close GAP 1 — collapse CR-01 (BLOCKER) and CR-03 (WARNING) into ONE fix per REVIEW CR-01 "option b".

CR-01: The fit-path `host_epoch_driver` launches one cube per owner (`CubeCount::Static(n,1,1)`, umap.rs:1265) with `move_other=1` (umap.rs:1293). Under `move_other==1` each owner-cube also writes its positive neighbour's coordinates (`embedding[other_base+d1]`, umap_layout.rs:155-157). Because the fuzzy graph is symmetric, two concurrently-scheduled cubes read-modify-write the same vertex slots with NO synchronization → an unsynchronized data race on any genuinely parallel backend (wgpu/rocm/cuda). rocm is a REAL runnable parallel GPU gate (f32) per project memory, so the race is reachable. This breaks the D-05 byte-identical contract that criterion 3 and the project core value ("any CubeCL backend") require; it passes today only because the sole gate is the sequential cpu-MLIR backend.

CR-03: The symmetric COO contains both (r,c) and (c,r); `host_epoch_driver` builds one positive edge per COO entry keyed by owner=head (umap.rs:1190-1227), so each undirected pair yields two owner-edges, and with `move_other=1` each edge moves BOTH endpoints → every undirected pair is attracted ~2-4× per due-epoch vs umap-learn's single head/tail pass. `PROPERTY_EPS=0.02` was calibrated against this doubled output, so the gate validates against mlrs's own divergence.

Fix (REVIEW CR-01 option b): set the fit launch `move_other=0` over the already-symmetric COO. Each owner-cube then writes ONLY its own vertex (no foreign-vertex write → race eliminated), AND each undirected pair is processed once per direction (the symmetric graph already carries both (r,c) and (c,r)), matching umap-learn's single-pass schedule (double-count eliminated). One change resolves both findings.

Purpose: Make the stochastic SGD layout determinism contract (D-05) hold on ANY backend and the force schedule a faithful umap-learn port (criterion 3, UMAP-03), not a gate calibrated to a bug.
Output: `move_other=0` fit launch; a concurrency/scheduling-order determinism test; recalibrated PROPERTY_EPS/ARI_BAND with a per-pair sample-count assertion; re-confirmed reproducible/property gates.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-umap/14-CONTEXT.md
@.planning/phases/14-umap/14-VERIFICATION.md
@.planning/phases/14-umap/14-REVIEW.md
@.planning/phases/14-umap/14-VALIDATION.md
@.planning/phases/14-umap/14-04-PLAN.md
@crates/mlrs-kernels/src/umap_layout.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Switch the fit-path layout launch to owner-only (move_other=0)</name>
  <files>crates/mlrs-algos/src/manifold/umap.rs</files>
  <read_first>
    - crates/mlrs-algos/src/manifold/umap.rs:1185-1304 — `host_epoch_driver`: the per-epoch CSR build (1190-1227 builds one positive edge per symmetric-COO entry keyed by `owner = head[e]`), the launch at 1277, and the trailing arg `1u32, // move_other = true (fit path)` at line 1293. THIS literal `1u32` is the only change needed in the launch.
    - crates/mlrs-kernels/src/umap_layout.rs:139-159 — the attractive update: line 153 writes the owner's own coord `embedding[(cur_base + d1)]` unconditionally; lines 155-157 write the FOREIGN vertex `embedding[(other_base + d1)]` ONLY when `move_other == 1u32`. With `move_other=0` the foreign write is skipped → no cross-cube race. The kernel ALREADY supports `move_other=0` (the transform path uses it — umap.rs:849).
    - crates/mlrs-algos/src/manifold/umap.rs:825-855 — `transform_epoch_driver`: the PROVEN `move_other=0` precedent (transform path), confirming the kernel + driver already handle owner-only correctly over a subset.
    - .claude/skills/spike-findings-mlrs/SKILL.md — cpu-MLIR landmines. NOTE: this change only flips a launch scalar from 1u32 to 0u32; it removes a write, adds no SharedMemory/atomics/F::INFINITY/mutable-bool/shift-loop, so the kernel stays cpu-MLIR-safe. Do NOT restructure the kernel.
  </read_first>
  <action>
    In `host_epoch_driver` (umap.rs), change the final `umap_layout_step::launch` argument from `1u32` (the `move_other` flag, currently commented "move_other = true (fit path)", line ~1293) to `0u32`. Update the inline comment to state that the fit path is now owner-only over the symmetric COO: each undirected pair is covered by BOTH its (r,c) and (c,r) owner-edges with NO foreign-vertex write, eliminating the CR-01 cross-cube race and the CR-03 double-count (each pair processed once per direction, matching umap-learn's single head/tail pass). Do NOT change the CSR build (umap.rs:1190-1227) — keeping the symmetric COO is exactly what makes owner-only cover both endpoints; do NOT switch to a directed edge set. Do NOT modify `umap_layout.rs` — the kernel already gates the foreign write on `move_other == 1u32` and is exercised owner-only by the transform path. The negative-sample schedule (epochs_per_negative, n_neg draws) is unchanged; only the attractive endpoint-move count changes. After this edit the fit and transform paths both drive the kernel owner-only.
  </action>
  <verify>
    <automated>cargo build -p mlrs-algos --features cpu && grep -n "move_other" crates/mlrs-algos/src/manifold/umap.rs</automated>
  </verify>
  <acceptance_criteria>
    - The `host_epoch_driver` fit launch passes `0u32` as the `move_other` argument (no longer `1u32`).
    - `crates/mlrs-kernels/src/umap_layout.rs` is unchanged (the kernel already supports owner-only).
    - `cargo build -p mlrs-algos --features cpu` succeeds.
    - No owner-cube writes a foreign vertex on the fit path: the only `move_other == 1u32`-gated write in umap_layout.rs is now unreachable from the fit launch.
  </acceptance_criteria>
  <done>The fit layout launch is owner-only (move_other=0); no foreign-vertex write occurs on the fit path; crate compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Add a scheduling-order determinism test + per-pair sample-count assertion</name>
  <files>crates/mlrs-algos/tests/umap_test.rs</files>
  <read_first>
    - crates/mlrs-algos/tests/umap_test.rs:873-899 — `run_reproducible` / `reproducible_f64`: the byte-identity pattern (`emb_a[i].to_bits() == emb_b[i].to_bits()`) and `fit_embedding(..., Some(7))` twice with the same seed. The NEW test extends this idea to expose the cross-cube race rather than re-running on the same sequential backend.
    - crates/mlrs-algos/tests/umap_test.rs:331-348 — `fit_embedding` helper (builder → fit → host embedding).
    - crates/mlrs-algos/tests/umap_test.rs:104-112 — `gate_f64` skip helper.
    - crates/mlrs-algos/src/manifold/umap.rs:1185-1241 — `host_epoch_driver`'s per-epoch CSR build: `next_sample`/`epochs_per_sample` advance once per active edge per due-epoch; the per-pair positive-sample count over a run equals the number of due-epochs for that edge (≈ `n_epochs / epochs_per_sample[e]`). This is the invariant the per-pair assertion checks.
    - .planning/phases/14-umap/14-VALIDATION.md:55-83 — the existing calibration block whose constants are re-derived in Task 3.
  </read_first>
  <action>
    Add a `#[test]` fn `fit_layout_order_independent` to umap_test.rs that asserts the D-05 byte-identical contract is robust to owner-cube SCHEDULING ORDER (the dimension the cpu-MLIR sequential gate cannot exercise). Because the test harness runs only on cpu-MLIR + rocm-f32 here, simulate parallel non-determinism at the level the race would manifest: drive two same-seed fits whose per-owner CSR launch order differs and assert byte-identical output. Concretely — since `move_other=0` makes each owner-cube write only its own contiguous `embedding[owner*dim .. ]` slots with no foreign write, the result MUST be independent of the order owners are scheduled. Implement the test by running `fit_embedding(pool, x, n, d, Metric::Euclidean, Some(7))` twice and asserting `to_bits()` equality (same as `reproducible_f64`), AND add an assertion that no two owners share a write target: derive the per-owner write set from `move_other=0` semantics and assert each `embedding` slot is written by exactly one owner-cube (owner-only ⇒ owner `o` writes only `o*dim .. (o+1)*dim`, so the write sets are disjoint by construction — assert this disjointness property holds for the n owners over `n*dim` slots). If the harness exposes a way to permute owner launch order, prefer permuting it between the two runs; otherwise document in a code comment that owner-only writes are slot-disjoint (the structural guarantee that makes scheduling order irrelevant) and keep the byte-identity assertion as the observable contract. Gate via `gate_f64("fit_layout_order_independent")`.

    Additionally add a per-pair sample-count assertion (CR-03 regression guard): in a small deterministic host-only test fn `per_pair_sample_count_matches_schedule`, reconstruct `make_epochs_per_sample` for a tiny symmetric COO and assert that over `n_epochs` the number of positive samples drawn for each undirected pair equals the expected count from `epochs_per_sample` (i.e. each pair is sampled once per direction per due-epoch, NOT doubled). This locks the corrected schedule against a future `move_other=1` regression.
  </action>
  <verify>
    <automated>cargo test -p mlrs-algos --features cpu fit_layout_order_independent per_pair_sample_count_matches_schedule -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `fit_layout_order_independent` and `per_pair_sample_count_matches_schedule` exist and are annotated `#[test]`.
    - `fit_layout_order_independent` asserts byte-identical (`to_bits()`) output across two same-seed fits AND encodes the owner-only slot-disjointness guarantee (the property that makes the D-05 contract hold regardless of cube scheduling order — the contract is no longer only-true-on-sequential-cpu-MLIR).
    - `per_pair_sample_count_matches_schedule` asserts the per-pair positive-sample count equals the expected `epochs_per_sample`-derived count (not doubled), catching a CR-03 regression.
    - Both tests pass under `cargo test -p mlrs-algos --features cpu` (or skip-with-log on f64-incapable backend for the device test).
  </acceptance_criteria>
  <done>A determinism test encodes the off-sequential-backend D-05 guarantee (owner-only slot-disjoint writes + byte-identity), and a per-pair count test guards the corrected un-doubled schedule.</done>
</task>

<task type="auto">
  <name>Task 3: Recalibrate the property gate against the corrected schedule and re-confirm gates</name>
  <files>.planning/phases/14-umap/14-VALIDATION.md, crates/mlrs-algos/tests/umap_test.rs</files>
  <read_first>
    - .planning/phases/14-umap/14-VALIDATION.md:55-83 — the "Calibrated property-gate thresholds (Plan 04)" block: the per-metric trust/overlap/ARI margin table and the `PROPERTY_EPS=0.02` / `ARI_BAND=0.05` constants that were fit to the OLD doubled (move_other=1) output. These are re-derived against the move_other=0 output.
    - crates/mlrs-algos/tests/umap_test.rs:57 — `const PROPERTY_EPS: f64 = 0.02;` and the `ARI_BAND` constant; line 835-840 — the `mlrs_trust >= umap_trust - PROPERTY_EPS` / overlap assertions in the layout_property gate.
    - crates/mlrs-algos/tests/umap_test.rs:848-865 — `layout_property_<metric>` test fns (5 metrics) that must stay green at the re-derived thresholds.
    - .planning/phases/14-umap/14-CONTEXT.md D-04 — the gate is RELATIVE to umap (`mlrs >= umap - ε`), kept TIGHT; D-05 byte-identity per (backend, dtype). The recalibration must preserve the relative-to-umap philosophy, not loosen into an absolute floor.
  </read_first>
  <action>
    Run the `layout_property_<metric>` family (all 5 metrics) and `reproducible_f64` AFTER Task 1's move_other=0 change to measure mlrs-vs-umap-learn 0.5.12 structural margins (trustworthiness, kNN-overlap, downstream-ARI) on the corrected force schedule. The full property family is slow (~28min/family) — run it in the background and read measured margins from test output. Re-derive `PROPERTY_EPS` and `ARI_BAND` from the NEW worst positive margin (keep them TIGHT and RELATIVE per D-04 — e.g. a small multiple of the worst measured margin, not an absolute floor), and update BOTH the `const PROPERTY_EPS` / `ARI_BAND` in umap_test.rs AND the calibration table + rationale in 14-VALIDATION.md. In 14-VALIDATION.md, replace the Plan-04 calibration block's framing to state the constants are now measured against the move_other=0 (owner-only, single-pass-per-direction) schedule that matches umap-learn, and record the new per-metric margin table. Add a sentence noting the per-pair sample-count assertion (`per_pair_sample_count_matches_schedule`, Task 2) now anchors the schedule so the gate validates against umap's force schedule, not mlrs's former double-count (resolving CR-03). If any `layout_property_<metric>` goes red at the re-derived TIGHT threshold, the layout itself diverges — surface that as a finding rather than inflating PROPERTY_EPS to mask it. Re-confirm `reproducible_f64` stays byte-identical (the move_other=0 change must not break same-seed reproducibility).
  </action>
  <verify>
    <automated>cargo test -p mlrs-algos --features cpu reproducible_f64 layout_property_euclidean -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `PROPERTY_EPS` and `ARI_BAND` in umap_test.rs are updated to values re-derived from the move_other=0 measured margins (recorded, not guessed), preserving the relative-to-umap (`mlrs >= umap - ε`) philosophy.
    - 14-VALIDATION.md's calibration block is updated: new per-metric margin table, explicit statement that constants are measured against the corrected move_other=0 single-pass-per-direction schedule, and a note that `per_pair_sample_count_matches_schedule` anchors the schedule (CR-03 resolved).
    - `reproducible_f64` passes (byte-identical same-seed fit preserved after the change).
    - `layout_property_euclidean` passes at the re-derived threshold (and the full 5-metric family is confirmed green via the background run, recorded in the SUMMARY).
  </acceptance_criteria>
  <done>The property-gate constants are re-derived against the corrected schedule, documented in 14-VALIDATION.md, and the reproducibility + property gates are confirmed green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host driver → device kernel | The `host_epoch_driver` launch crosses into concurrently-scheduled device cubes. The hazard is internal correctness (a data race), not external input. No network / auth / secrets surface exists in this numeric library. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-07-01 | Tampering (memory) / Repudiation (non-determinism) | `host_epoch_driver` fit launch → `umap_layout_step` foreign-vertex write | mitigate | Per CR-01: set `move_other=0` so each owner-cube writes only its own slot-disjoint coordinates. Eliminates the unsynchronized cross-cube read-modify-write race on parallel backends (wgpu/rocm/cuda), restoring the D-05 byte-identical contract. A scheduling-order determinism test exercises the property off the sequential cpu-MLIR backend. |
| T-14-07-02 | Repudiation (fidelity) | property gate calibrated to mlrs's own double-count | mitigate | Per CR-03: the move_other=0 single-pass-per-direction schedule matches umap-learn; PROPERTY_EPS/ARI_BAND are re-derived against the corrected output and a per-pair sample-count assertion anchors the schedule against regression. |

cpu-MLIR landmines: this plan only flips a launch scalar (1u32→0u32) and adds host-side tests; it removes a kernel write and introduces no SharedMemory/atomics/F::INFINITY/mutable-bool/shift-loop. The kernel stays cpu-MLIR-safe and is not restructured.
</threat_model>

<verification>
- `cargo build -p mlrs-algos --features cpu` succeeds; `move_other=0` on the fit launch; `umap_layout.rs` unchanged.
- `cargo test -p mlrs-algos --features cpu fit_layout_order_independent per_pair_sample_count_matches_schedule reproducible_f64 layout_property_euclidean` pass.
- Full `layout_property_<metric>` 5-metric family confirmed green at re-derived thresholds (background run; recorded in SUMMARY).
- 14-VALIDATION.md calibration block updated to reflect the move_other=0 schedule + new margins.
</verification>

<success_criteria>
- Fit path is owner-only (move_other=0): no foreign-vertex write, no cross-cube race — D-05 holds on any backend.
- Each undirected pair processed once per direction (umap-learn-faithful single-pass schedule); CR-03 double-count gone.
- Determinism test exercises the off-sequential-backend D-05 guarantee; per-pair count test guards the schedule.
- PROPERTY_EPS/ARI_BAND re-derived against the corrected output; reproducible_f64 + layout_property family green.
</success_criteria>

<artifacts_this_phase_produces>
NEW symbols / changes created by this plan:
- `fit_layout_order_independent` — `#[test]` fn in umap_test.rs encoding the off-sequential-backend D-05 guarantee (owner-only slot-disjoint writes + byte-identity across same-seed fits).
- `per_pair_sample_count_matches_schedule` — `#[test]` fn in umap_test.rs asserting the per-pair positive-sample count matches the expected `epochs_per_sample` (un-doubled), CR-03 regression guard.
- Recalibrated `const PROPERTY_EPS` and `ARI_BAND` values in umap_test.rs (measured against the move_other=0 schedule).
- Updated calibration block in 14-VALIDATION.md (new per-metric margin table + move_other=0 rationale).
- The `0u32` move_other fit launch in `host_epoch_driver` (umap.rs) replacing the prior `1u32`.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/14-umap/14-07-SUMMARY.md` when done.
</output>
