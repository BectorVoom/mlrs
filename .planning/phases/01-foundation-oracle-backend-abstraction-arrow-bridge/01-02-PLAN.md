---
phase: 01-foundation-oracle-backend-abstraction-arrow-bridge
plan: 02
type: execute
wave: 1
depends_on: ["01-01"]
files_modified:
  - crates/mlrs-core/src/tolerance.rs
  - crates/mlrs-core/src/compare.rs
  - crates/mlrs-core/src/sign_flip.rs
  - crates/mlrs-core/src/label_perm.rs
  - crates/mlrs-core/src/oracle.rs
  - crates/mlrs-core/src/error.rs
  - crates/mlrs-core/src/lib.rs
  - crates/mlrs-core/Cargo.toml
  - crates/mlrs-core/tests/compare_test.rs
  - crates/mlrs-core/tests/helpers_test.rs
  - docs/tolerance-policy.md
autonomous: false
requirements: [FOUND-06, FOUND-07, FOUND-08]
must_haves:
  truths:
    - "assert_close requires BOTH abs AND rel error to pass, with a near-zero guard falling back to abs-only"
    - "A sign-flip helper aligns SVD/PCA component signs before comparison"
    - "A label-permutation helper maps clustering labels via best-permutation before comparison"
    - "Named .npz fixtures load by name into a Rust case struct (no Python at test time)"
    - "A documented per-estimator-family f32 tolerance policy exists as a growable structure with a single global default"
    - "BridgeError is a typed thiserror enum with one variant per Arrow-violation class"
  artifacts:
    - path: "crates/mlrs-core/src/compare.rs"
      provides: "is_close / assert_close (abs AND rel + near-zero guard)"
      contains: "fn is_close"
    - path: "crates/mlrs-core/src/tolerance.rs"
      provides: "Tolerance struct + F32_TOL/F64_TOL (growable policy)"
      contains: "F32_TOL"
    - path: "crates/mlrs-core/src/sign_flip.rs"
      provides: "SVD/PCA sign-alignment helper"
    - path: "crates/mlrs-core/src/label_perm.rs"
      provides: "Clustering best-permutation matching helper"
    - path: "crates/mlrs-core/src/oracle.rs"
      provides: "Named .npz fixture loader"
    - path: "crates/mlrs-core/src/error.rs"
      provides: "BridgeError thiserror enum"
      contains: "enum BridgeError"
    - path: "docs/tolerance-policy.md"
      provides: "Documented per-family f32 tolerance policy"
  key_links:
    - from: "crates/mlrs-core/src/sign_flip.rs"
      to: "crates/mlrs-core/src/compare.rs"
      via: "feeds aligned values into assert_close"
      pattern: "compare|is_close"
    - from: "crates/mlrs-core/src/oracle.rs"
      to: "npz reader crate (by_name)"
      via: "load named arrays into a case struct"
      pattern: "by_name"
---

<objective>
Build the scikit-learn oracle harness in `mlrs-core`: the float-comparison core (`assert_close` with abs AND rel + near-zero guard per D-09), the growable tolerance policy (D-08), the sign-flip and label-permutation comparison helpers (FOUND-08), the named-`.npz` fixture loader (D-01/D-02/D-03), and the typed `BridgeError` enum (D-07, consumed by the Arrow bridge in Plan 03). All host-side, pure logic — no device, no backend feature — so this is the verification spine every downstream phase consumes.

Purpose: ROADMAP Criterion 5 (seeded fixtures, sign-flip + label-perm helpers, documented per-family f32 tolerance policy) and the comparison half of Criterion 2 (oracle asserts equality within 1e-5). Provides `BridgeError` so Plan 03 (bridge) has its error type ready.
Output: `mlrs-core` modules `tolerance`, `compare`, `sign_flip`, `label_perm`, `oracle`, `error`; tests in `crates/mlrs-core/tests/`; `docs/tolerance-policy.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-oracle-backend-abstraction-arrow-bridge/01-CONTEXT.md
@.planning/phases/01-foundation-oracle-backend-abstraction-arrow-bridge/01-RESEARCH.md
@.planning/phases/01-foundation-oracle-backend-abstraction-arrow-bridge/01-PATTERNS.md
@SPIKE-FINDINGS.md
@AGENTS.md
</context>

<artifacts_this_phase_produces>
Symbols introduced here and consumed by later plans/phases:
- `mlrs_core::tolerance::{Tolerance, F32_TOL, F64_TOL}` — `Tolerance { abs: f64, rel: f64 }`, both consts `{1e-5, 1e-5}` (D-08); structure is growable into a per-family table later.
- `mlrs_core::compare::{is_close, assert_close}` — abs AND rel comparison with a documented `NEAR_ZERO_FLOOR` guard (D-09).
- `mlrs_core::sign_flip::*` — SVD/PCA sign-alignment helper (FOUND-08).
- `mlrs_core::label_perm::*` — clustering best-permutation matching helper (FOUND-08).
- `mlrs_core::oracle::{load_npz, OracleCase or per-case struct}` — named-`.npz` loader (D-02/D-03).
- `mlrs_core::error::BridgeError` — thiserror enum (`Offset`, `HasNulls`, `Misaligned`, variant names Claude's discretion) consumed by Plan 03's bridge (FOUND-06/D-07).
- `docs/tolerance-policy.md` — the documented per-family f32 tolerance policy (FOUND-08).
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 0: Package legitimacy gate — verify npz reader + logger crate before install</name>
  <action>Checkpoint: blocking human verification of npz-reader and logger crate legitimacy before any install. See <how-to-verify> for the registry checks the human performs.</action>
  <what-built>Pre-install legitimacy gate for the npz reader crate (and logger crate) before its first real use.</what-built>
  <how-to-verify>
    RESEARCH.md §"Package Legitimacy Audit" marks all crates `[ASSUMED]` because slopcheck was unavailable. The npz reader (`ndarray-npy` OR `npyz`) and the logger (`log`+`env_logger` OR `tracing`) are the only non-mandated, non-obvious installs in this plan. Verify each on its registry before adding:
    1. Confirm `ndarray-npy` at https://crates.io/crates/ndarray-npy (repo github.com/jturner314/ndarray-npy) OR `npyz` at https://crates.io/crates/npyz (repo github.com/ExpHP/npyz) — pick one (npyz avoids the ndarray dependency; ndarray-npy is more ergonomic).
    2. Beware the cross-ecosystem lookalike `ndarray_npz` / `veks-io` — confirm the exact hyphenated name `ndarray-npy`.
    3. Confirm the logger crate on crates.io.
    Record the chosen crates + versions in the SUMMARY.
  </how-to-verify>
  <resume-signal>Type "approved" with the chosen npz reader + logger crate names, or flag a suspicious package.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Tolerance policy + assert_close (abs AND rel + near-zero guard) + BridgeError enum</name>
  <files>crates/mlrs-core/src/tolerance.rs, crates/mlrs-core/src/compare.rs, crates/mlrs-core/src/error.rs, crates/mlrs-core/src/lib.rs, crates/mlrs-core/Cargo.toml, crates/mlrs-core/tests/compare_test.rs, docs/tolerance-policy.md</files>
  <read_first>
    - .planning/phases/01-foundation-oracle-backend-abstraction-arrow-bridge/01-RESEARCH.md Pattern 5 (assert_close + near-zero guard), §user_constraints D-08/D-09
    - .planning/phases/01-foundation-oracle-backend-abstraction-arrow-bridge/01-PATTERNS.md `compare.rs`, `tolerance.rs`, `error.rs` sections
    - .planning/phases/01-foundation-oracle-backend-abstraction-arrow-bridge/01-CONTEXT.md (D-07, D-08, D-09 — locked; do not re-open)
    - AGENTS.md §2 (tests in tests/, never `mod tests` in src)
  </read_first>
  <behavior>
    - is_close(1.0+9e-6, 1.0, F32_TOL) == true (within 1e-5 abs and rel).
    - is_close(1.0+2e-5, 1.0, F32_TOL) == false (abs error exceeds 1e-5).
    - is_close(1e-12, 0.0, F32_TOL) == true (near-zero guard: |expected|<floor falls back to abs-only; rel term does not explode).
    - is_close(2.0, 1.0, F32_TOL) == false; a value passing rel but failing abs and vice-versa both fail (BOTH must pass, D-09).
    - assert_close panics with an informative message on mismatch (got/expected/abs/rel reported).
  </behavior>
  <action>
    Implement `mlrs_core::tolerance` with `pub struct Tolerance { pub abs: f64, pub rel: f64 }` and `pub const F32_TOL`/`F64_TOL` both `{ abs: 1e-5, rel: 1e-5 }` (D-08). Structure it so a per-family table can be added later WITHOUT building the table now (e.g. a `Tolerance::for_family(...)` stub returning the global default, or a documented extension point). Implement `mlrs_core::compare::is_close(got, expected, tol)` per D-09: compute abs_err; if `expected.abs() < NEAR_ZERO_FLOOR` return `abs_err <= tol.abs` (near-zero guard), else require `abs_err <= tol.abs && rel_err <= tol.rel` (BOTH). Choose and document `NEAR_ZERO_FLOOR` (RESEARCH suggests 1e-8) in a doc comment and in docs/tolerance-policy.md. Add `assert_close` that panics with got/expected/abs/rel on failure, plus a slice variant for arrays. Implement `mlrs_core::error::BridgeError` as a `#[derive(Debug, thiserror::Error)]` enum with one variant per Arrow-violation class (Offset, HasNulls, Misaligned — names Claude's discretion, D-07). Write `docs/tolerance-policy.md` documenting: the single global default (D-08), the near-zero floor + rationale, and the growth path to per-family rows. Put ALL tests in `crates/mlrs-core/tests/compare_test.rs` — never `mod tests` in src.
  </action>
  <verify>
    <automated>cargo test -p mlrs-core --features cpu compare</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p mlrs-core --features cpu compare` exits 0 covering all five behaviors above.
    - `crates/mlrs-core/src/error.rs` defines `enum BridgeError` deriving `thiserror::Error` with ≥3 variants.
    - `docs/tolerance-policy.md` exists and documents the global default, near-zero floor, and per-family growth path (FOUND-08).
    - `grep -rn "mod tests" crates/mlrs-core/src/` returns nothing.
  </acceptance_criteria>
  <done>assert_close enforces abs AND rel with a near-zero guard; tolerance policy is a growable structure with a single global default and documentation; BridgeError typed enum exists for Plan 03.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Sign-flip + label-permutation helpers + named-.npz fixture loader</name>
  <files>crates/mlrs-core/src/sign_flip.rs, crates/mlrs-core/src/label_perm.rs, crates/mlrs-core/src/oracle.rs, crates/mlrs-core/src/lib.rs, crates/mlrs-core/Cargo.toml, crates/mlrs-core/tests/helpers_test.rs</files>
  <read_first>
    - .planning/phases/01-foundation-oracle-backend-abstraction-arrow-bridge/01-PATTERNS.md `sign_flip.rs`, `label_perm.rs`, `oracle.rs` sections (+ "No Analog Found" for label_perm)
    - .planning/phases/01-foundation-oracle-backend-abstraction-arrow-bridge/01-RESEARCH.md §"Don't Hand-Roll" (npz via by_name; do NOT hand-roll zip/npy parsing), §Specific Ideas (sign-flip/label-perm feed assert_close)
    - cuml-main behavioral reference for svd_flip sign convention (PCA) — read-only, do NOT port C++
    - SPIKE-FINDINGS.md (resolved npz reader crate + by_name API, A4)
    - .planning/phases/01-foundation-oracle-backend-abstraction-arrow-bridge/01-CONTEXT.md (D-01/D-02/D-03)
    - AGENTS.md §2
  </read_first>
  <behavior>
    - sign_flip: a component vector and its negation normalize to the same canonical sign (e.g. largest-abs element made positive); after alignment, assert_close passes for sign-flipped-but-equal vectors and still fails for genuinely different vectors.
    - label_perm: predicted labels [1,1,0,0] vs reference [0,0,1,1] match (perfect permutation); [0,1,0,1] vs [0,0,1,1] does NOT perfectly match.
    - oracle::load_npz: loading a committed test `.npz` returns the named arrays (e.g. a/x/y/expected) with correct shapes for both f32 and f64.
  </behavior>
  <action>
    Implement `mlrs_core::sign_flip` matching sklearn's `svd_flip` convention (align each singular vector/component to a deterministic sign — e.g. sign of the largest-magnitude element — so an equivalent-but-sign-flipped result does not spuriously fail). Implement `mlrs_core::label_perm` mapping predicted cluster labels to reference labels via best-permutation matching (confusion-matrix + greedy or Hungarian; standard clustering-eval logic — no manual/cuML analog). Both feed values into `compare::assert_close`. Implement `mlrs_core::oracle::load_npz` using the npz reader chosen in the legitimacy checkpoint / SPIKE-FINDINGS.md `by_name` API, returning a case struct of named arrays for f32 and f64; do NOT hand-roll zip+npy-header parsing. Commit a small throwaway `.npz` fixture for the loader test (or reuse one produced in the spike). Tests go in `crates/mlrs-core/tests/helpers_test.rs` — never `mod tests` in src.
  </action>
  <verify>
    <automated>cargo test -p mlrs-core --features cpu helpers</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p mlrs-core --features cpu helpers` exits 0 covering sign-flip, label-perm, and npz-load behaviors above.
    - sign-flip test proves a sign-flipped vector passes assert_close after alignment AND a genuinely-different vector still fails.
    - label-perm test proves a permuted-but-equal labeling matches and a different labeling does not.
    - `load_npz` reads named arrays by name for both f32 and f64 (uses the npz crate, not a hand-rolled parser).
    - `grep -rn "mod tests" crates/mlrs-core/src/` returns nothing.
  </acceptance_criteria>
  <done>Sign-flip and label-permutation helpers feed assert_close; the named-`.npz` loader reads committed fixtures for f32/f64 without Python at test time.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed `.npz` fixture → Rust deserializer | First-party committed binary blob parsed by the npz reader; a malformed/oversized header could in principle cause a parse error. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | npz fixture as untrusted deserialization input | accept | Fixtures are first-party, committed, code-reviewed; the npz reader is memory-safe Rust (`ndarray-npy`/`npyz`) — low risk, no untrusted source. |
| T-02-02 | Tampering | Spurious near-zero comparison failures masking real correctness bugs | mitigate | Near-zero guard is documented (floor value + rationale) in docs/tolerance-policy.md and covered by an explicit test (FOUND-08). |
| T-02-SC | Tampering | npz reader / logger crate install | mitigate | Blocking-human legitimacy checkpoint (Task 0) verifies the exact crate on its registry before first use; cross-ecosystem lookalike `ndarray_npz` explicitly excluded. |
</threat_model>

<verification>
- `cargo test -p mlrs-core --features cpu` exits 0 (compare + helpers).
- `docs/tolerance-policy.md` documents the policy (FOUND-08).
- `grep -rn "mod tests" crates/mlrs-core/src/` returns nothing.
</verification>

<success_criteria>
- assert_close = abs AND rel + near-zero guard (D-09) [Criterion 5].
- Sign-flip + label-permutation helpers exist and feed assert_close [Criterion 5 / FOUND-08].
- Documented per-family f32 tolerance policy as a growable structure [Criterion 5 / FOUND-08].
- Named-`.npz` loader reads committed fixtures (no Python at test time) [FOUND-07].
- BridgeError typed enum ready for Plan 03 [FOUND-06 / D-07].
</success_criteria>

<output>
Create `.planning/phases/01-foundation-oracle-backend-abstraction-arrow-bridge/01-02-SUMMARY.md` when done.
</output>
