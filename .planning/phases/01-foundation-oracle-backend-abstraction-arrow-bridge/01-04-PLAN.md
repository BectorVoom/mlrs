---
phase: 01-foundation-oracle-backend-abstraction-arrow-bridge
plan: 04
type: execute
wave: 1
depends_on: ["01-01"]
files_modified:
  - crates/mlrs-backend/src/pool.rs
  - crates/mlrs-backend/src/device_array.rs
  - crates/mlrs-backend/tests/pool_test.rs
autonomous: true
requirements: [FOUND-05]
must_haves:
  truths:
    - "A buffer pool reuses a released buffer of matching size instead of allocating a new one"
    - "The pool exposes allocations/reuses/peak_bytes/live_bytes counters"
    - "Pool counters are logged (not asserted) in Phase 1"
    - "DeviceArray wraps a CubeCL buffer, carries length+dtype, and supports host read-back"
  artifacts:
    - path: "crates/mlrs-backend/src/pool.rs"
      provides: "BufferPool free-list + PoolStats counters (logged-only)"
      contains: "PoolStats"
    - path: "crates/mlrs-backend/src/device_array.rs"
      provides: "DeviceArray<R,F> wrapper with read-back, allocating via pool"
      contains: "DeviceArray"
    - path: "crates/mlrs-backend/tests/pool_test.rs"
      provides: "Reuse + counters + read-back tests"
  key_links:
    - from: "crates/mlrs-backend/src/device_array.rs"
      to: "crates/mlrs-backend/src/pool.rs"
      via: "routes allocation through BufferPool::acquire"
      pattern: "pool|acquire"
    - from: "crates/mlrs-backend/src/pool.rs"
      to: "cubecl client (empty/create)"
      via: "client.empty on miss, free-list on hit"
      pattern: "empty|client"
---

<objective>
Build the memory-efficiency layer (D-04 / FOUND-05): a buffer-reuse pool (free-list keyed by byte size over reclaimed CubeCL handles) with a stats/counters API, and `DeviceArray<R,F>` wrapping CubeCL buffers with length+dtype and host read-back, routing allocation through the pool. Per D-05, Phase-1 counters are LOGGED ONLY — hard reuse assertions are deferred to Phase 2 (the trivial smoke kernel does not exercise realistic allocation patterns yet). PROJECT.md treats memory efficiency as first-class and verified per-phase, so the pool ships now rather than as a thin wrapper.

Purpose: ROADMAP Criterion 5 partial (memory-efficient device-array abstraction with reuse) and FOUND-05. Provides `DeviceArray` + pool for Plan 05's end-to-end pipeline.
Output: `mlrs-backend::pool` (BufferPool + PoolStats), `mlrs-backend::device_array` (DeviceArray<R,F>), tests in `crates/mlrs-backend/tests/pool_test.rs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-oracle-backend-abstraction-arrow-bridge/01-CONTEXT.md
@.planning/phases/01-foundation-oracle-backend-abstraction-arrow-bridge/01-RESEARCH.md
@.planning/phases/01-foundation-oracle-backend-abstraction-arrow-bridge/01-PATTERNS.md
@SPIKE-FINDINGS.md
@AGENTS.md
</context>

<artifacts_this_phase_produces>
Symbols introduced here and consumed by Plan 05 (pipeline) and Phase 2 (real primitive workloads + hard reuse assertions):
- `mlrs_backend::pool::{BufferPool<R>, PoolStats}` — `PoolStats { allocations, reuses, peak_bytes, live_bytes }`; `acquire(size)` reuses from a `HashMap<usize, Vec<Handle>>` free-list (reuses += 1) or `client.empty(size)` (allocations += 1); `release(handle, size)` returns the handle to the free-list; stats logged at drop/phase boundary (D-05, logged-only).
- `mlrs_backend::device_array::DeviceArray<R, F>` — wraps a CubeCL handle, carries `len` + dtype, allocates via the pool, and reads back to host `Vec<F>` via `client.read_one` + `bytemuck::cast_slice`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BufferPool free-list with logged-only PoolStats counters</name>
  <files>crates/mlrs-backend/src/pool.rs, crates/mlrs-backend/tests/pool_test.rs</files>
  <read_first>
    - .planning/phases/01-foundation-oracle-backend-abstraction-arrow-bridge/01-RESEARCH.md Pattern 6, §Open Question 4 (mlrs-level free-list, do NOT tune CubeCL MemoryConfiguration in Phase 1)
    - /home/user/Documents/workspace/cubecl_manual/manual/Cubecl/Tuning_ExclusivePages_Allocator_and_Pool_Configurations_in_CubeCL.md (context only — relationship to CubeCL's own pools)
    - .planning/phases/01-foundation-oracle-backend-abstraction-arrow-bridge/01-PATTERNS.md `pool.rs` section
    - .planning/phases/01-foundation-oracle-backend-abstraction-arrow-bridge/01-CONTEXT.md (D-04 pool now; D-05 counters logged-only, hard reuse assertions deferred to Phase 2)
    - SPIKE-FINDINGS.md (resolved client/handle types, A6; Bytes/empty API, A3)
    - AGENTS.md §2
  </read_first>
  <behavior>
    - acquire(size) on an empty pool calls client.empty and increments allocations.
    - release then acquire(same size) reuses the freed handle and increments reuses (allocations does NOT increment on the reuse).
    - peak_bytes reflects the max live bytes observed; live_bytes decreases on release.
    - Stats are logged (log::info!) at drop/phase boundary — NOT asserted as a hard gate in Phase 1 (D-05).
  </behavior>
  <action>
    Implement `mlrs_backend::pool::PoolStats { allocations: u64, reuses: u64, peak_bytes: u64, live_bytes: u64 }` and `BufferPool<R: cubecl::Runtime>` holding the active client (use the `Client` alias from `runtime.rs`), a `HashMap<usize, Vec<Handle>>` free-list keyed by byte size, and `stats`. `acquire(size_bytes)`: pop from `free[size]` (reuses += 1, live_bytes += size) else `client.empty(size)` (allocations += 1, live_bytes += size); update peak_bytes. `release(handle, size_bytes)`: push back to `free[size]` (live_bytes -= size). Log stats at `Drop` (or an explicit `log_stats()` called at phase boundary) via `log::info!("pool stats: {:?}", stats)`. Keep it the simplest correct mlrs-level free-list — do NOT tune CubeCL `MemoryConfiguration` (Open Question 4). Tests in `crates/mlrs-backend/tests/pool_test.rs` assert reuse increments `reuses` and that a fresh acquire increments `allocations`; counters are inspected, not used as a hard phase gate. Never `mod tests` in src.
  </action>
  <verify>
    <automated>cargo test -p mlrs-backend --features cpu pool</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p mlrs-backend --features cpu pool` exits 0 covering all four behaviors above.
    - After acquire→release→acquire(same size), `stats.reuses == 1` and the second acquire did NOT increment `allocations`.
    - `PoolStats` exposes `allocations`, `reuses`, `peak_bytes`, `live_bytes`.
    - Stats are emitted via `log::info!` (logged-only, no hard reuse assertion as a phase gate, D-05).
    - `grep -rn "mod tests" crates/mlrs-backend/src/` returns nothing.
  </acceptance_criteria>
  <done>BufferPool reuses released buffers of matching size and exposes logged-only allocations/reuses/peak/live counters (FOUND-05 / D-04 / D-05).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DeviceArray<R,F> wrapper with pool-routed allocation and host read-back</name>
  <files>crates/mlrs-backend/src/device_array.rs, crates/mlrs-backend/tests/pool_test.rs</files>
  <read_first>
    - /home/user/Documents/workspace/cubecl_manual/manual/Cubecl/Backend-Agnostic_Buffer_Slicing_and_Multi-Logical_Array_Allocation.md (slice/slice_mut, read_one, CubeDim::new_1d)
    - .planning/phases/01-foundation-oracle-backend-abstraction-arrow-bridge/01-RESEARCH.md §DeviceArray + Buffer Pool, §"Don't Hand-Roll" (read_one + bytemuck::cast_slice)
    - .planning/phases/01-foundation-oracle-backend-abstraction-arrow-bridge/01-PATTERNS.md `device_array.rs` section ("No Analog Found" — wrapper type is new design)
    - SPIKE-FINDINGS.md (A3 Bytes constructor, A6 ComputeClient/Client alias, read_one API)
    - crates/mlrs-backend/src/pool.rs (the BufferPool from Task 1 — route allocation through it)
    - AGENTS.md §2
  </read_first>
  <behavior>
    - DeviceArray::from_host(&[F]) uploads a host slice to a device buffer (via pool acquire + client.create) and records len + dtype.
    - DeviceArray::to_host() reads the buffer back via client.read_one + bytemuck::cast_slice and returns a Vec<F> equal to the original host slice (round-trip) on cpu.
    - len() reflects element count.
  </behavior>
  <action>
    Implement `mlrs_backend::device_array::DeviceArray<R: cubecl::Runtime, F>` wrapping a CubeCL handle plus `len: usize` and an element-dtype marker. Constructors route allocation through the `BufferPool` from Task 1 (acquire by byte size) and upload host data via the A3-resolved `cubecl::bytes::Bytes` constructor / `client.create`. `to_host(&self) -> Vec<F>` reads back via `client.read_one(handle)` then `bytemuck::cast_slice`. Provide `len()`/`is_empty()`. Add a round-trip test in `crates/mlrs-backend/tests/pool_test.rs` (or a sibling test) proving `from_host(&[f32])` → `to_host()` returns the same data on cpu, and that allocation went through the pool (pool stats reflect it). Any `unsafe` needs a `// SAFETY:` comment. Never `mod tests` in src.
  </action>
  <verify>
    <automated>cargo test -p mlrs-backend --features cpu device_array</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p mlrs-backend --features cpu device_array` (or the `pool` test target if combined) exits 0.
    - `from_host(&[f32; n])` → `to_host()` returns a Vec equal to the input on cpu (round-trip).
    - Allocation routes through `BufferPool` (pool stats reflect the acquire).
    - `len()` returns the element count.
    - `grep -rn "mod tests" crates/mlrs-backend/src/` returns nothing.
  </acceptance_criteria>
  <done>DeviceArray<R,F> wraps a CubeCL buffer with len+dtype, allocates via the pool, and round-trips host↔device on cpu (FOUND-05).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host slice → DeviceArray → device buffer | Host data uploaded via pool/`client.create`; wrong length passed to `read_one`/`cast_slice` would read OOB. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering / DoS | DeviceArray read-back with wrong length → OOB read | mitigate | Carry `len` in the wrapper; derive read size from it; `// SAFETY:` comment on any unsafe; bytemuck::cast_slice size-checked. |
| T-04-02 | DoS | Pool free-list growing unbounded (handles never reclaimed) | accept | Phase-1 workloads are trivial; logged counters surface growth; bounded-pool policy is a Phase-2 concern (D-05 defers hard memory assertions). |
| T-04-SC | Tampering | (no new package installs) | accept | All crates already audited in Plan 01/02. |
</threat_model>

<verification>
- `cargo test -p mlrs-backend --features cpu pool device_array` exits 0.
- Pool reuse increments `reuses`, not `allocations`; counters logged.
- DeviceArray round-trips host↔device on cpu.
- `grep -rn "mod tests" crates/mlrs-backend/src/` returns nothing.
</verification>

<success_criteria>
- BufferPool reuses released buffers and exposes logged-only counters [FOUND-05 / D-04 / D-05].
- DeviceArray<R,F> wraps CubeCL buffers with reuse and minimal copies, round-tripping host↔device [FOUND-05].
</success_criteria>

<output>
Create `.planning/phases/01-foundation-oracle-backend-abstraction-arrow-bridge/01-04-SUMMARY.md` when done.
</output>
