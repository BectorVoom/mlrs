---
phase: 01-foundation-oracle-backend-abstraction-arrow-bridge
plan: 05
type: execute
wave: 2
depends_on: ["01-02", "01-03", "01-04"]
files_modified:
  - scripts/gen_oracle.py
  - tests/fixtures/saxpy_f32_seed42.npz
  - tests/fixtures/saxpy_f64_seed42.npz
  - crates/mlrs-backend/tests/pipeline_test.rs
  - crates/mlrs-py/src/lib.rs
  - crates/mlrs-py/src/allocator.rs
  - crates/mlrs-py/Cargo.toml
  - crates/mlrs-py/tests/allocator_test.rs
autonomous: false
requirements: [FOUND-02, FOUND-07, FOUND-09]
must_haves:
  truths:
    - "A trivial #[cube] kernel generic over <F: Float> runs on cpu and wgpu, ingests an Arrow Float32Array/Float64Array zero-copy through the validated bridge, reads back, and the oracle asserts equality vs a NumPy reference within 1e-5"
    - "Committed .npz fixtures are produced by scripts/gen_oracle.py and loaded by the Rust test with no Python at test time"
    - "f64 oracle tests skip-with-log on wgpu adapters lacking SHADER_F64"
    - "The mimalloc global allocator is wired in mlrs-py with source and test code in separate files"
  artifacts:
    - path: "scripts/gen_oracle.py"
      provides: "NumPy seeded reference fixture generator (build-time only)"
      contains: "default_rng"
    - path: "tests/fixtures/saxpy_f32_seed42.npz"
      provides: "Committed f32 oracle fixture"
    - path: "tests/fixtures/saxpy_f64_seed42.npz"
      provides: "Committed f64 oracle fixture"
    - path: "crates/mlrs-backend/tests/pipeline_test.rs"
      provides: "End-to-end Arrow->bridge->pool->device->kernel->readback->oracle test (f32+f64)"
    - path: "crates/mlrs-py/src/lib.rs"
      provides: "mimalloc #[global_allocator] in the cdylib"
      contains: "global_allocator"
  key_links:
    - from: "crates/mlrs-backend/tests/pipeline_test.rs"
      to: "mlrs_kernels::saxpy_kernel + mlrs_backend::bridge + mlrs_core::oracle"
      via: "Arrow->validate->upload->launch->read_one->assert_close"
      pattern: "saxpy_kernel|validate_f|assert_close"
    - from: "crates/mlrs-py/src/lib.rs"
      to: "mimalloc::MiMalloc"
      via: "#[global_allocator] static GLOBAL"
      pattern: "global_allocator"
---

<objective>
Close the phase by wiring the full pipeline end-to-end and the allocator: generate committed `.npz` oracle fixtures (`scripts/gen_oracle.py`, NumPy seeded, NOT run at test time per D-03), write the cross-crate integration test that proves Arrow → validated bridge → pool/DeviceArray → generic `#[cube]` kernel → host read-back → oracle assert within 1e-5 on cpu and wgpu for both f32 and f64 (f64 capability-gated), and wire the mimalloc global allocator in `mlrs-py` with source/test in separate files.

Purpose: ROADMAP Criterion 2 (the whole-pipeline proof) and Criterion 5 (mimalloc wired with source/test separation). Requirements FOUND-02 (generic kernel runs f32+f64 on cpu+wgpu), FOUND-07 (oracle asserts ≤1e-5 from committed fixtures), FOUND-09 (mimalloc global allocator, separate files).
Output: `scripts/gen_oracle.py`; committed `tests/fixtures/saxpy_{f32,f64}_seed42.npz`; `crates/mlrs-backend/tests/pipeline_test.rs`; mimalloc wired in `mlrs-py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-oracle-backend-abstraction-arrow-bridge/01-CONTEXT.md
@.planning/phases/01-foundation-oracle-backend-abstraction-arrow-bridge/01-RESEARCH.md
@.planning/phases/01-foundation-oracle-backend-abstraction-arrow-bridge/01-PATTERNS.md
@SPIKE-FINDINGS.md
@AGENTS.md
</context>

<artifacts_this_phase_produces>
Symbols/files introduced here and consumed by every downstream phase:
- `scripts/gen_oracle.py` — `numpy.random.default_rng(seed)`-based fixture generator; Phase 4+ extends it to `import sklearn`, fit, and `savez` `coef_`/`intercept_`/etc. NOT run in the CI test job (D-03).
- `tests/fixtures/saxpy_f32_seed42.npz`, `tests/fixtures/saxpy_f64_seed42.npz` — committed binary oracle blobs carrying named arrays `a`/`x`/`y`/`expected`.
- `crates/mlrs-backend/tests/pipeline_test.rs` — the canonical end-to-end verification vehicle reused as a smoke test in later phases.
- `mlrs-py` mimalloc wiring: `#[global_allocator] static GLOBAL: MiMalloc = MiMalloc;` in `src/lib.rs` (or `src/allocator.rs`), with the activation test in `crates/mlrs-py/tests/allocator_test.rs` (NOT `mod tests`).
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 0: Generate and commit .npz oracle fixtures (numpy, build-time only)</name>
  <action>Checkpoint: blocking human action to confirm numpy availability and that fixtures are generated + committed. See <how-to-verify> for the steps.</action>
  <what-built>Committed `.npz` oracle fixtures generated by `scripts/gen_oracle.py` using NumPy (a build-time-only step; numpy must be present in the dev/regen environment, not in CI).</what-built>
  <how-to-verify>
    `scripts/gen_oracle.py` requires `numpy` (verified available: Python 3.12.3 with numpy expected in the dev env). The executor must RUN the script once to produce the committed blobs, then those blobs are checked in and Rust tests run against them with NO Python (D-03).
    1. Confirm `python3 -c "import numpy"` succeeds in the dev/regen environment. If numpy is absent, install it (`pip install numpy`) — this is the only Python dependency for fixture generation (sklearn is not needed for the saxpy fixture; it arrives in Phase 4).
    2. After the script is written (Task 1), run `python3 scripts/gen_oracle.py` to emit `tests/fixtures/saxpy_f32_seed42.npz` and `saxpy_f64_seed42.npz`.
    3. Confirm `.gitignore` does NOT exclude `tests/fixtures/*.npz` so the blobs are committed.
  </how-to-verify>
  <resume-signal>Type "approved" once numpy is available and the fixtures are generated + committed, or report a numpy-environment blocker.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: scripts/gen_oracle.py — seeded NumPy saxpy fixtures (f32 + f64), committed</name>
  <files>scripts/gen_oracle.py, tests/fixtures/saxpy_f32_seed42.npz, tests/fixtures/saxpy_f64_seed42.npz</files>
  <read_first>
    - .planning/phases/01-foundation-oracle-backend-abstraction-arrow-bridge/01-RESEARCH.md §"scripts/gen_oracle.py shape", §Standard Stack `rand` note (Python default_rng is the authoritative seeded RNG), Pitfall 7
    - .planning/phases/01-foundation-oracle-backend-abstraction-arrow-bridge/01-PATTERNS.md `gen_oracle.py` and `tests/fixtures` sections
    - .planning/phases/01-foundation-oracle-backend-abstraction-arrow-bridge/01-CONTEXT.md (D-01/D-02/D-03 — committed fixtures, named arrays, no Python at test time; fixture naming case_dtype_seed)
    - crates/mlrs-core/src/oracle.rs (the load_npz consumer from Plan 02 — match its expected named-array keys)
  </read_first>
  <action>
    Write `scripts/gen_oracle.py` using `numpy.random.default_rng(seed=42)` to generate a saxpy case for f32 and f64: scalar `a`, vectors `x`, `y`, and `expected = a*x + y`, each cast to the target dtype, saved via `np.savez` into named arrays `a`/`x`/`y`/`expected` matching the keys `mlrs_core::oracle::load_npz` expects (Plan 02). Output `tests/fixtures/saxpy_f32_seed42.npz` and `tests/fixtures/saxpy_f64_seed42.npz` (naming encodes case_dtype_seed per the spec). Make the script idempotent/regenerable (D-03) and structure it so Phase 4+ can add sklearn-based estimator fixtures. Run it once (gated by the Task-0 checkpoint) to produce + commit the blobs. The script is NOT invoked by the CI test job.
  </action>
  <verify>
    <automated>python3 scripts/gen_oracle.py && test -f tests/fixtures/saxpy_f32_seed42.npz && test -f tests/fixtures/saxpy_f64_seed42.npz</automated>
  </verify>
  <acceptance_criteria>
    - `python3 scripts/gen_oracle.py` exits 0 and writes both `.npz` files.
    - Each `.npz` contains named arrays `a`, `x`, `y`, `expected` (verifiable: `python3 -c "import numpy as np; print(sorted(np.load('tests/fixtures/saxpy_f32_seed42.npz').files))"` lists them).
    - The f32 fixture stores float32 dtype and the f64 fixture stores float64 dtype.
    - `.gitignore` does not exclude `tests/fixtures/*.npz`; the blobs are committed.
    - The named-array keys match what `mlrs_core::oracle::load_npz` reads (Plan 02 contract).
  </acceptance_criteria>
  <done>Seeded NumPy saxpy fixtures (f32+f64) are generated and committed; the generator is regenerable and CI never runs it (D-01/D-02/D-03).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: End-to-end pipeline test — Arrow -> bridge -> pool/DeviceArray -> kernel -> read-back -> oracle (f32 + f64, capability-gated)</name>
  <files>crates/mlrs-backend/tests/pipeline_test.rs</files>
  <read_first>
    - .planning/phases/01-foundation-oracle-backend-abstraction-arrow-bridge/01-RESEARCH.md §"Code Examples" (pipeline_test, lines 462-499), §"Phase Requirements → Test Map"
    - .planning/phases/01-foundation-oracle-backend-abstraction-arrow-bridge/01-PATTERNS.md `crates/*/tests/*.rs` section (pipeline integration pattern)
    - SPIKE-FINDINGS.md (resolved Bytes constructor, launch signature, read_one, capability query)
    - crates/mlrs-kernels/src/smoke.rs (saxpy_kernel from Plan 01)
    - crates/mlrs-backend/src/bridge.rs (validate_f32/validate_f64 from Plan 03)
    - crates/mlrs-backend/src/capability.rs (supports_f64 / feature_enabled / dtype-backend log from Plan 03)
    - crates/mlrs-backend/src/device_array.rs + pool.rs (Plan 04)
    - crates/mlrs-core/src/oracle.rs + compare.rs (load_npz, assert_close, F32_TOL/F64_TOL from Plan 02)
    - .planning/phases/01-foundation-oracle-backend-abstraction-arrow-bridge/01-CONTEXT.md (Claude's discretion: smoke kernel choice; f64 skip mechanics)
    - AGENTS.md §2 and §4 (cubecl error protocol)
  </read_first>
  <behavior>
    - f32: load saxpy_f32_seed42.npz; build Float32Array from x; validate via bridge::validate_f32; upload x and y (via pool/DeviceArray); launch saxpy_kernel::launch::<f32, ActiveRuntime>; read back; assert_close(got, expected, F32_TOL) passes on cpu.
    - The same f32 test passes on wgpu.
    - f64: same flow with Float64Array + F64_TOL; gated on supports_f64(&client) — runs if supported, skips-with-log otherwise (Criterion 4).
    - dtype=… backend=… is logged at the start of each oracle case (Criterion 4).
  </behavior>
  <action>
    Write `crates/mlrs-backend/tests/pipeline_test.rs` exercising the full pipeline for f32 and f64. For each dtype: call `capability::log_oracle_dtype(...)`; `oracle::load_npz` the matching fixture; construct the Arrow array from `x`; `bridge::validate_{f32,f64}` (expect Ok); upload `x`/`y` through `DeviceArray`/pool; launch `mlrs_kernels::saxpy_kernel::launch::<F, runtime::ActiveRuntime>` with ceiling-division `CubeCount`/`CubeDim`; read back via the resolved read API; `compare::assert_close` against `expected` with `F32_TOL`/`F64_TOL`. Gate the f64 case on `capability::supports_f64(&client)` — run if supported, else `log::warn!` skip + early-return (Criterion 4). On ANY cubecl build error, consult the error guideline (AGENTS.md §4) before fixing. Never `mod tests` in src.
  </action>
  <verify>
    <automated>cargo test -p mlrs-backend --features cpu pipeline -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p mlrs-backend --features cpu pipeline -- --nocapture` exits 0; the f32 case asserts equality vs the NumPy reference within 1e-5 and the output contains `dtype=f32 backend=cpu` (or equivalent) (Criterion 2/4).
    - `cargo test -p mlrs-backend --features wgpu pipeline -- --nocapture` exits 0 (f32 on wgpu); if the adapter lacks SHADER_F64 the f64 case logs the skip reason and is skipped, not failed.
    - The f64 case runs and passes on any backend reporting f64 support (cpu, per the spike finding).
    - The test uses the validated bridge (no raw `values()` upload bypassing validation) and the pool/DeviceArray for upload.
    - `grep -rn "mod tests" crates/mlrs-backend/src/` returns nothing.
  </acceptance_criteria>
  <done>The generic `#[cube]` kernel runs on cpu and wgpu, ingests Arrow through the validated bridge, reads back, and the oracle asserts ≤1e-5 vs NumPy for f32 (always) and f64 (capability-gated) — the whole-pipeline proof (Criterion 2, FOUND-02/FOUND-07).</done>
</task>

<task type="auto">
  <name>Task 3: Wire mimalloc global allocator in mlrs-py (source/test in separate files)</name>
  <files>crates/mlrs-py/src/lib.rs, crates/mlrs-py/src/allocator.rs, crates/mlrs-py/Cargo.toml, crates/mlrs-py/tests/allocator_test.rs</files>
  <read_first>
    - /home/user/Documents/workspace/optimisor/manual/MIMALLOC_MANUAL.md (#[global_allocator] static GLOBAL: MiMalloc = MiMalloc; crate version)
    - .planning/phases/01-foundation-oracle-backend-abstraction-arrow-bridge/01-RESEARCH.md Pattern 7, Assumption A8
    - .planning/phases/01-foundation-oracle-backend-abstraction-arrow-bridge/01-PATTERNS.md `mlrs-py/src/lib.rs` section
    - .planning/phases/01-foundation-oracle-backend-abstraction-arrow-bridge/01-CONTEXT.md (FOUND-09: mimalloc; D-10: anyhow at boundary)
    - AGENTS.md §2 (allocator test in tests/, NOT mod tests in lib.rs)
  </read_first>
  <action>
    Wire `mimalloc::MiMalloc` as the `#[global_allocator]` in `mlrs-py` — define `static GLOBAL: MiMalloc = MiMalloc;` exactly once in the cdylib (in `src/lib.rs` or a dedicated `src/allocator.rs` that `lib.rs` includes; per FOUND-09 keep source/test separate — the allocator wiring is source, its check is a test file). Do NOT put `#[global_allocator]` in any library crate. Confirm `crate-type = ["cdylib"]` and that the crate uses `anyhow` at the boundary (D-10). Write `crates/mlrs-py/tests/allocator_test.rs` that performs allocations and confirms the process runs under the custom allocator (e.g. allocate/free a sizable Vec and assert no crash; mimalloc has no trivial introspection, so the test proves the allocator is active by exercising it — document the verification approach). Never `mod tests` in lib.rs.
  </action>
  <verify>
    <automated>cargo test -p mlrs-py --features cpu allocator</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p mlrs-py --features cpu allocator` exits 0.
    - `crates/mlrs-py/src/lib.rs` (or `src/allocator.rs`) contains exactly one `#[global_allocator]` defining `MiMalloc`; no library crate defines a global allocator.
    - The allocator activation test lives in `crates/mlrs-py/tests/allocator_test.rs` (separate file, FOUND-09).
    - `grep -rn "mod tests" crates/mlrs-py/src/` returns nothing.
  </acceptance_criteria>
  <done>mimalloc is the global allocator in the mlrs-py cdylib, defined once, with the activation test in a separate file per AGENTS.md (FOUND-09).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed `.npz` fixture → Rust test | First-party committed reference data parsed by the npz reader (already audited in Plan 02). |
| host → device upload in pipeline test | Validated Arrow slice uploaded via pool/DeviceArray; `unsafe { ArrayArg::from_raw_parts }` length must come from the validated slice. |
| `#[global_allocator]` definition | Exactly-once-in-cdylib invariant; a library crate setting it would break build integrity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering / OOB | `ArrayArg::from_raw_parts` with wrong length in pipeline test | mitigate | Derive length from the bridge-validated slice `.len()`; kernel bounds-check `if tid < x.len()`. |
| T-05-02 | Build integrity | `#[global_allocator]` double-definition / set in a library crate | mitigate | Define mimalloc only in the `mlrs-py` cdylib (Pattern 7); no global_allocator in any library crate. |
| T-05-03 | Tampering (silent wrong results) | Pipeline test bypassing the bridge with a raw `values()` upload | mitigate | Test goes through `bridge::validate_*` (acceptance criterion forbids raw-values bypass). |
| T-05-04 | Tampering | f64 oracle silently mis-running on wgpu without SHADER_F64 | mitigate | f64 case gated on `supports_f64`; skip-with-log; dtype×backend logged (Criterion 4). |
| T-05-SC | Tampering | numpy install for fixture generation (build-time only) | mitigate | Blocking human-action checkpoint (Task 0) confirms numpy in the dev/regen env; numpy is first-party, not in the CI test path (D-03). |
</threat_model>

<verification>
- `cargo test --workspace --features cpu` exits 0; `cargo test --workspace --features wgpu` exits 0 (f64 skip-with-log if no SHADER_F64).
- `cargo build --workspace --features cuda` compiles.
- `cargo tree -p mlrs-kernels -e features` shows no backend runtime crate.
- `grep -rn "mod tests" crates/*/src/` returns nothing.
- Pipeline test logs `dtype=… backend=…` lines (Criterion 4).
- Both `.npz` fixtures committed; `gen_oracle.py` regenerates them.
</verification>

<success_criteria>
- Generic `#[cube]` kernel runs on cpu+wgpu, ingests Arrow via the validated bridge, reads back, oracle asserts ≤1e-5 vs NumPy for f32 and f64 (capability-gated) [Criterion 2 / FOUND-02 / FOUND-07].
- Committed `.npz` fixtures from gen_oracle.py; no Python at test time [Criterion 5 / D-03].
- mimalloc global allocator wired in mlrs-py with source/test separation [Criterion 5 / FOUND-09].
</success_criteria>

<output>
Create `.planning/phases/01-foundation-oracle-backend-abstraction-arrow-bridge/01-05-SUMMARY.md` when done.
</output>
