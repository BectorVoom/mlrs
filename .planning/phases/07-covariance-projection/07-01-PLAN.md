---
phase: 07-covariance-projection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/mlrs-algos/src/traits.rs
  - crates/mlrs-algos/src/error.rs
  - crates/mlrs-algos/src/lib.rs
  - crates/mlrs-algos/src/covariance/mod.rs
  - crates/mlrs-algos/src/projection/mod.rs
  - crates/mlrs-algos/src/decomposition/mod.rs
  - crates/mlrs-backend/src/prims/mod.rs
  - scripts/gen_oracle.py
  - crates/mlrs-backend/tests/rng_test.rs
  - crates/mlrs-backend/tests/incremental_svd_test.rs
  - crates/mlrs-algos/tests/empirical_covariance_test.rs
  - crates/mlrs-algos/tests/ledoit_wolf_test.rs
  - crates/mlrs-algos/tests/incremental_pca_test.rs
  - crates/mlrs-algos/tests/random_projection_test.rs
autonomous: true
requirements: [PRIM-06, PRIM-07, COV-01, COV-02, DECOMP-03, PROJ-01, PROJ-02]
must_haves:
  truths:
    - "The `PartialFit<F>` trait exists alongside Fit/Predict/Transform and is re-exported from lib.rs"
    - "Every Phase-7 test crate compiles today with #[ignore] stubs (no references to not-yet-existent symbols)"
    - "gen_oracle.py emits empirical_covariance, ledoit_wolf, incremental_pca, jl_min_dim fixtures committed as .npz blobs"
    - "Module index registers covariance/projection module groups + rng/incremental_svd prims as empty compiling stubs"
  artifacts:
    - path: "crates/mlrs-algos/src/traits.rs"
      provides: "PartialFit<F> trait (D-01)"
      contains: "trait PartialFit"
    - path: "crates/mlrs-backend/tests/rng_test.rs"
      provides: "PRIM-06 #[ignore] scaffold (distribution + seed-repro + memory gate)"
    - path: "crates/mlrs-backend/tests/incremental_svd_test.rs"
      provides: "PRIM-07 #[ignore] scaffold (2+-batch merge + memory gate)"
    - path: "crates/mlrs-algos/tests/empirical_covariance_test.rs"
      provides: "COV-01 #[ignore] scaffold incl. rank-deficient case"
    - path: "crates/mlrs-algos/tests/ledoit_wolf_test.rs"
      provides: "COV-02 #[ignore] scaffold (two n)"
    - path: "crates/mlrs-algos/tests/incremental_pca_test.rs"
      provides: "DECOMP-03 #[ignore] scaffold (partial_fit + fit + whiten)"
    - path: "crates/mlrs-algos/tests/random_projection_test.rs"
      provides: "PROJ-01/02 #[ignore] property-gate scaffold + jl_min_dim value"
    - path: "scripts/gen_oracle.py"
      provides: "4 new oracle generators + main() wiring"
  key_links:
    - from: "crates/mlrs-algos/src/lib.rs"
      to: "PartialFit"
      via: "pub use traits::PartialFit"
      pattern: "PartialFit"
    - from: "crates/mlrs-backend/src/prims/mod.rs"
      to: "rng, incremental_svd"
      via: "pub mod registration"
      pattern: "pub mod (rng|incremental_svd)"
---

<objective>
Wave-0 scaffold for Phase 7: land the shared interface surface (the `PartialFit<F>`
trait per D-01, new `AlgoError` hyperparameter guards, the module-index
registrations for the two new prims and two new estimator module groups) and the
full Nyquist test scaffold (six `#[ignore]` test files asserting fixture-load +
shape only, never a not-yet-existent symbol) plus the four new `gen_oracle.py`
generators with committed `.npz` blobs.

This plan OWNS every shared index file (traits.rs, error.rs, lib.rs, the three
`mod.rs` files, prims/mod.rs) so that all downstream Wave-1/2 plans stay strictly
file-disjoint and parallelizable — mirrors the v1 05-01 Wave-0 scaffold precedent.

Purpose: every downstream plan receives its contracts (trait signature, error
variants, committed oracle fixtures, test stub it un-`#[ignore]`s) in the plan
itself — no scavenger hunt, no shared-file write conflicts.
Output: PartialFit<F> trait; extended AlgoError; empty compiling module stubs for
rng/incremental_svd/covariance/projection/incremental_pca; six #[ignore] test
files; gen_oracle.py generators + committed fixtures.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-covariance-projection/07-CONTEXT.md
@.planning/phases/07-covariance-projection/07-RESEARCH.md
@.planning/phases/07-covariance-projection/07-PATTERNS.md
@.planning/phases/07-covariance-projection/07-VALIDATION.md
@AGENTS.md
@crates/mlrs-algos/src/traits.rs
@crates/mlrs-algos/src/error.rs
@crates/mlrs-algos/src/lib.rs
@crates/mlrs-algos/src/decomposition/mod.rs
@crates/mlrs-backend/src/prims/mod.rs
@crates/mlrs-algos/tests/pca_test.rs
@crates/mlrs-backend/tests/gemm_test.rs
@crates/mlrs-backend/tests/memory_gate_test.rs
@scripts/gen_oracle.py
</context>

<artifacts_produced>
## Artifacts this phase produces (Wave 0 portion)

| Symbol / File | Kind | Defined here |
|---------------|------|--------------|
| `mlrs_algos::traits::PartialFit<F>` | trait | yes (D-01) |
| `mlrs_algos::error::AlgoError::InvalidDensity` | error variant | yes (V5 guard) |
| `mlrs_algos::error::AlgoError::InvalidBatchSize` | error variant | yes (V5 guard) |
| `mlrs_algos::error::AlgoError::InvalidEps` | error variant | yes (V5 guard, JL) |
| `crates/mlrs-algos/src/covariance/mod.rs` | module index | yes (empty stub) |
| `crates/mlrs-algos/src/projection/mod.rs` | module index | yes (empty stub) |
| `pub mod rng;` / `pub mod incremental_svd;` in `prims/mod.rs` | registration | yes (empty stub bodies) |
| `pub mod incremental_pca;` in `decomposition/mod.rs` | registration | yes (empty stub) |
| `gen_empirical_covariance`, `gen_ledoit_wolf`, `gen_incremental_pca`, `gen_jl_min_dim` | oracle generators | yes |
| 6 `#[ignore]` test files | test scaffold | yes |
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: PartialFit trait + AlgoError guards + module-index stubs</name>
  <read_first>
    - crates/mlrs-algos/src/traits.rs (L53-120: Fit/Predict/Transform — copy Fit's shape exactly)
    - crates/mlrs-algos/src/error.rs (L41-158: struct-variant style; L54-60 InvalidAlpha exemplar; L182 Prim #[from] PrimError)
    - crates/mlrs-algos/src/lib.rs (L32-43: re-export block + module index)
    - crates/mlrs-algos/src/decomposition/mod.rs (the `pub mod pca;` shape to copy for the new groups)
    - crates/mlrs-backend/src/prims/mod.rs (L12-27: pub mod registrations)
    - 07-PATTERNS.md §traits.rs, §error.rs, §Module-index registration
  </read_first>
  <files>crates/mlrs-algos/src/traits.rs, crates/mlrs-algos/src/error.rs, crates/mlrs-algos/src/lib.rs, crates/mlrs-algos/src/covariance/mod.rs, crates/mlrs-algos/src/projection/mod.rs, crates/mlrs-algos/src/decomposition/mod.rs, crates/mlrs-backend/src/prims/mod.rs</files>
  <action>
    Add `PartialFit<F>` to traits.rs per D-01, mirroring the existing `Fit<F>` trait
    shape EXACTLY (same `where F: Float + CubeElement + Pod` bound, same
    `pool: &mut BufferPool<ActiveRuntime>`, `x: &DeviceArray<ActiveRuntime, F>`,
    `y: Option<&DeviceArray<ActiveRuntime, F>>` slot kept for the Phase-10 MBSGD
    reuse, explicit `shape: (usize, usize)` arg, returns `Result<&mut Self, AlgoError>`).
    The method is `partial_fit`. Document that IncrementalPCA passes `y: None` but the
    slot is retained per D-01 cross-cutting contract. Re-export `PartialFit` from
    lib.rs alongside the other traits.

    Extend AlgoError (error.rs) with three struct-variants in the existing
    InvalidAlpha style (ASVS V5, RESEARCH §Security Domain): `InvalidDensity { estimator,
    density: f64 }` (density ∈ (0,1]), `InvalidBatchSize { estimator, batch_size: usize }`
    (batch_size ≥ 1), `InvalidEps { estimator, eps: f64 }` (eps ∈ (0,1) for
    johnson_lindenstrauss_min_dim). Reuse existing `InvalidNComponents`, `NotFitted`,
    `Unsupported`, `Prim(#[from] PrimError)` AS-IS — do NOT add new variants for those.

    Register the module index, copying the `decomposition/mod.rs` shape: create
    `crates/mlrs-algos/src/covariance/mod.rs` and `crates/mlrs-algos/src/projection/mod.rs`
    as empty doc-comment-only stubs (NO `pub mod empirical_covariance;` etc. yet — those
    submodule files do not exist; commented future lines only, per the 05-01 stub pattern).
    Add `pub mod covariance;` and `pub mod projection;` to lib.rs, and
    `pub mod incremental_pca;` to decomposition/mod.rs COMMENTED OUT (the file is created
    in Plan 05) — instead leave a `// pub mod incremental_pca; // Plan 05` line so the
    index stays file-disjoint. Add `pub mod rng;` and `pub mod incremental_svd;` to
    prims/mod.rs as registrations whose target files are created in Plans 02/03 — to keep
    THIS plan compiling, create `crates/mlrs-backend/src/prims/rng.rs` and
    `incremental_svd.rs` as empty doc-comment-only stub files (Plans 02/03 fill them).
    Do NOT add `pub use` for any not-yet-existent symbol.
  </action>
  <verify>
    <automated>cargo build -p mlrs-algos --features cpu && cargo build -p mlrs-backend --features cpu</automated>
  </verify>
  <done>Both crates compile with `--features cpu`; `PartialFit` is re-exported from lib.rs; the three new AlgoError variants exist; covariance/projection mod.rs + rng.rs/incremental_svd.rs stubs compile.</done>
  <acceptance_criteria>
    - `grep -c "trait PartialFit" crates/mlrs-algos/src/traits.rs` ≥ 1
    - `grep -c "PartialFit" crates/mlrs-algos/src/lib.rs` ≥ 1
    - `grep -Ec "InvalidDensity|InvalidBatchSize|InvalidEps" crates/mlrs-algos/src/error.rs` == 3
    - `cargo build -p mlrs-algos --features cpu` exits 0
    - `cargo build -p mlrs-backend --features cpu` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: gen_oracle.py — 4 new generators + main() wiring; regenerate + commit fixtures</name>
  <read_first>
    - scripts/gen_oracle.py (gen_pca L985-1041 generator skeleton; main() dual-dtype loop L1088-1163; the `np.ascontiguousarray` C-contiguous fix L1007-1015)
    - 07-PATTERNS.md §gen_oracle.py (exact generator + main() wiring)
    - 07-RESEARCH.md §Oracle harness extension + Patterns 1/3/5
    - MEMORY: oracle-fixture-regen-needs-venv (gen_oracle.py needs numpy via /tmp venv, PEP 668)
  </read_first>
  <files>scripts/gen_oracle.py</files>
  <action>
    Add four generators mirroring gen_pca's skeleton (seeded rng, fit sklearn estimator,
    `c()` C-contiguous cast helper via np.ascontiguousarray, dtype_tag, np.savez), then
    wire each into main()'s dual-dtype loop:

    1. `gen_empirical_covariance(seed,dtype,n,p,assume_centered=False)` —
       `sklearn.covariance.EmpiricalCovariance(assume_centered).fit(X)`; store `X`,
       `covariance_`, `location_`, `precision_`. Emit TWO size cases incl. a
       RANK-DEFICIENT `n ≤ p` case (e.g. n=4,p=6) so the pinvh floor is exercised. p ≤ 64
       (MAX_DIM for the eig precision_ path). VALUE-matched 1e-5.

    2. `gen_ledoit_wolf(seed,dtype,n,p)` — `sklearn.covariance.LedoitWolf().fit(X)`; store
       `X`, `covariance_`, `shrinkage_` (as a length-1 array). Emit TWO `n` per ROADMAP
       criterion 3. p ≤ 64. VALUE-matched 1e-5.

    3. `gen_incremental_pca(seed,dtype,shape,n_components,batch_size,whiten)` —
       `sklearn.decomposition.IncrementalPCA(n_components,whiten,batch_size).fit(X)`; store
       `X`, `n_components`, `batch_size`, `components_`, `explained_variance_`,
       `explained_variance_ratio_`, `singular_values_`, `mean_`, `var_`,
       `n_samples_seen_`, `transform(X)`, `inverse_transform(transform(X))`. Apply the
       np.ascontiguousarray C-contiguous fix to `components_` (Fortran-order pitfall).
       Emit whiten=False AND whiten=True cases. SIZE THE FIXTURE so the per-batch stacked
       matrix clears the SVD caps: `n_components + batch_size + 1 ≤ MAX_ROWS(=256)` and
       `n_features ≤ MAX_COLS(=64)` (RESEARCH A2 / Open Q3). VALUE-matched 1e-5 after
       align_rows.

    4. `gen_jl_min_dim()` — emit `sklearn.random_projection.johnson_lindenstrauss_min_dim(
       n_samples, eps)` over a small grid of `(n_samples, eps)` (eps ∈ (0,1)) as a value
       array (store the n_samples grid, eps grid, and the resulting integer min_dim grid).
       VALUE-matched 1e-5. This is the ONLY RandomProjection value oracle (D-12).

    Do NOT add any matrix/transform oracle for RandomProjection (D-12 — RNG ≠ MT19937).

    Then REGENERATE: create a /tmp venv with numpy+scipy+scikit-learn (PEP 668 — system
    pip is externally-managed), run `scripts/gen_oracle.py`, and COMMIT the new .npz blobs
    into the fixtures dir. The blobs are committed binaries; CI never runs the script.
  </action>
  <verify>
    <automated>python3 -c "import ast,sys; ast.parse(open('scripts/gen_oracle.py').read()); print('parse-ok')" && ls tests/fixtures/*.npz crates/*/tests/fixtures/*.npz 2>/dev/null | grep -Ec "empirical_covariance|ledoit_wolf|incremental_pca|jl_min_dim"</automated>
  </verify>
  <done>gen_oracle.py parses; the four generators are wired into main(); committed .npz blobs exist for empirical_covariance (incl. a rank-deficient n≤p case), ledoit_wolf (two n), incremental_pca (whiten on/off), and jl_min_dim — each f32+f64 where value-matched.</done>
  <acceptance_criteria>
    - gen_oracle.py parses as valid Python
    - `grep -Ec "def gen_empirical_covariance|def gen_ledoit_wolf|def gen_incremental_pca|def gen_jl_min_dim" scripts/gen_oracle.py` == 4
    - committed .npz blobs present for all four families (find the fixtures dir used by gen_pca and confirm the new blobs land there)
    - incremental_pca fixture has n_components + batch_size + 1 ≤ 256 and n_features ≤ 64
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Six #[ignore] Nyquist test scaffolds (fixture-load + shape only)</name>
  <read_first>
    - crates/mlrs-algos/tests/pca_test.rs (the canonical oracle test: fixture() resolver L45-52, host_to_f64/f64_to L54-68, assert_close L73-91, align_matrix_rows/cols L95-123, the f64 capability gate L192-233 / L216-219)
    - crates/mlrs-backend/tests/gemm_test.rs (L181-184: the f64 skip_f64_with_log gate)
    - crates/mlrs-backend/tests/memory_gate_test.rs (L88-128: PoolStats gate idiom — live_bytes/peak_bytes/reuses)
    - 07-PATTERNS.md §Tests — analogs; §Shared Patterns (f64 capability gate, svd_flip, validate-before-launch)
    - 07-VALIDATION.md §Per-Task Verification Map (exact test-name prefixes per req) + §Wave 0 Requirements
  </read_first>
  <files>crates/mlrs-backend/tests/rng_test.rs, crates/mlrs-backend/tests/incremental_svd_test.rs, crates/mlrs-algos/tests/empirical_covariance_test.rs, crates/mlrs-algos/tests/ledoit_wolf_test.rs, crates/mlrs-algos/tests/incremental_pca_test.rs, crates/mlrs-algos/tests/random_projection_test.rs</files>
  <action>
    Create six test files, each carrying every VALIDATION.md test name as an `#[ignore]`
    stub that asserts ONLY fixture-load + shape well-formedness (and, for RandomProjection,
    the jl_min_dim value-fixture load) — with ZERO references to not-yet-existent symbols
    (no `prims::rng`, no `prims::incremental_svd`, no estimator structs). This is the
    05-01 / 03-02 Wave-0 scaffold pattern: the test crate must compile TODAY; Plans 02-06
    remove `#[ignore]` and wire the real prim/estimator calls.

    Each test name MUST be prefixed so the VALIDATION.md `cargo test ... <prefix>` commands
    select them:
    - rng_test.rs → `rng_*` fns: `rng_gaussian_distribution`, `rng_seed_reproducible`,
      `rng_achlioptas_density`, `rng_permutation_bijection`, `rng_memory_gate`.
      Carry the f64 capability-gate block (copy gemm_test.rs L181-184) where an f64 case
      applies; the memory-gate stub references the PoolStats idiom shape only.
    - incremental_svd_test.rs → `incremental_svd_*` fns: `incremental_svd_two_batch_merge`
      (the 2+-batch-vs-host-reference oracle), `incremental_svd_memory_gate`. f64 gate where
      applicable.
    - empirical_covariance_test.rs → `empirical_covariance_*` fns covering covariance_,
      location_, precision_ (incl. a `*_rank_deficient` precision_ case), f32+f64 pair with
      the f64 gate.
    - ledoit_wolf_test.rs → `ledoit_wolf_*` fns covering shrinkage_ ∈ [0,1] + covariance_
      across two n, f32+f64 pair with the f64 gate.
    - incremental_pca_test.rs → `incremental_pca_*` fns covering all attrs +
      transform/inverse_transform via partial_fit AND via fit(), whiten on/off, compared
      AFTER align_rows; f32+f64 pair with the f64 gate.
    - random_projection_test.rs → `random_projection_*` fns: the PROPERTY-gate stubs
      (JL distortion averaged-over-trials, matrix moment stats, seed-reproducibility,
      `transform == X·componentsᵀ` self-consistency) AND `random_projection_jl_min_dim`
      (the ONE value-oracle). Document in comments that the gate is the property set, NOT
      1e-5 (D-12), and that D-11 mitigates strict-band flakiness with a FIXED SplitMix64
      seed + averaging over a pinned trial count (set the trial count constant here, e.g.
      `const JL_TRIALS: usize = 50;`).

    Reuse pca_test.rs scaffolding (fixture resolver, host_to_f64, assert_close,
    align_matrix_rows/cols, load_npz/OracleCase/Tolerance/F32_TOL/F64_TOL imports). f64
    stays strict F64_TOL (1e-5); leave a clearly-commented placeholder constant for the
    f32-on-rocm per-family band (LedoitWolf/IncrementalPCA — Claude's-discretion, pinned
    from the standalone-prim measurement in Plans 02/04/05).
  </action>
  <verify>
    <automated>cargo test -p mlrs-backend --features cpu --no-run 2>&1 | tail -3 && cargo test -p mlrs-algos --features cpu --no-run 2>&1 | tail -3</automated>
  </verify>
  <done>All six test files compile (test binaries build with --no-run); every test fn is `#[ignore]` and asserts fixture-load/shape only; no reference to a non-existent prim/estimator symbol; the f64 capability gate is present on f64 cases; JL_TRIALS trial-count constant is set.</done>
  <acceptance_criteria>
    - `cargo test -p mlrs-backend --features cpu --no-run` exits 0
    - `cargo test -p mlrs-algos --features cpu --no-run` exits 0
    - `grep -rc "#\[ignore\]" crates/mlrs-backend/tests/rng_test.rs crates/mlrs-backend/tests/incremental_svd_test.rs crates/mlrs-algos/tests/empirical_covariance_test.rs crates/mlrs-algos/tests/ledoit_wolf_test.rs crates/mlrs-algos/tests/incremental_pca_test.rs crates/mlrs-algos/tests/random_projection_test.rs` — every file ≥ 1
    - `grep -c "skip_f64_with_log" crates/mlrs-algos/tests/incremental_pca_test.rs` ≥ 1
    - `grep -Ec "rng_memory_gate|incremental_svd_memory_gate" crates/mlrs-backend/tests/rng_test.rs crates/mlrs-backend/tests/incremental_svd_test.rs` ≥ 1 each
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python ingress → mlrs-algos | Untrusted hyperparameters (n_components, batch_size, density, eps) and array shapes cross here; validated before any device launch. |
| Host RNG seed → matrix generation | Caller-supplied `u64` seed (documented, NOT OsRng) drives all reproducibility. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | AlgoError guards (density/eps/batch_size) | mitigate | This plan ADDS the typed `InvalidDensity`/`InvalidEps`/`InvalidBatchSize` variants (ASVS V5); the actual reject-before-launch wiring lands in the estimator plans (04/05/06). |
| T-07-02 | Repudiation | RNG source | mitigate | Scaffold the seed-reproducibility test (`rng_seed_reproducible`) that ENFORCES same-seed→identical-matrix; the prim itself (Plan 03) promotes host SplitMix64, NEVER OsRng/rand (ASVS V6). |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | NO package installs this phase (REQUIREMENTS: zero new compute deps; pyo3 stays 0.28). The only external use is numpy/scipy/sklearn in a throwaway /tmp venv to regenerate committed .npz blobs offline — never linked into the build, never in CI. No legitimacy audit table required (RESEARCH §Package Legitimacy Audit: N/A). |
| T-07-NA | S/R/I/D/E | auth, session, access control, web | accept (N/A) | In-process Rust ML library — no authn, no session, no access-control surface, no network. Marked N/A per RESEARCH §Security Domain (V2/V3/V4 do not apply). |
</threat_model>

<verification>
- `cargo build -p mlrs-algos --features cpu` and `cargo build -p mlrs-backend --features cpu` both exit 0.
- `cargo test -p mlrs-backend --features cpu --no-run` and `cargo test -p mlrs-algos --features cpu --no-run` both exit 0 (all six scaffolds compile).
- The existing `kmeanspp_test.rs` is UNAFFECTED (this plan does not touch kmeans.rs; the SplitMix64 move is Plan 03).
- gen_oracle.py parses; the four new fixture families are committed.
</verification>

<success_criteria>
- PartialFit<F> trait exists + re-exported (D-01).
- Three new AlgoError guard variants exist (ASVS V5).
- Module index registers covariance/projection groups + rng/incremental_svd prim stubs; everything compiles.
- Six #[ignore] test files compile with fixture-load/shape-only assertions and the f64 gate.
- gen_oracle.py has the four generators wired; committed .npz blobs for all four families.
</success_criteria>

<output>
Create `.planning/phases/07-covariance-projection/07-01-SUMMARY.md` when done.
</output>
