---
phase: quick-260625-8ri
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/mlrs-core/src/error.rs
  - crates/mlrs-algos/src/decomposition/pca.rs
  - crates/mlrs-algos/src/linear/linear_regression.rs
  - crates/mlrs-algos/src/linear/ridge.rs
  - crates/mlrs-algos/src/covariance/ledoit_wolf.rs
  - crates/mlrs-algos/src/covariance/empirical_covariance.rs
  - crates/mlrs-algos/src/density/kernel_density.rs
autonomous: true
requirements: [IN-04]

must_haves:
  truths:
    - "A new PrimError variant exists representing an internal-invariant / unexpected-None condition, with a thiserror message consistent with the existing variants."
    - "All 6 `.expect(\"shared path is never plane-gated to None\")` call sites in mlrs-algos are replaced with typed-error propagation via `.ok_or(...)?`."
    - "Zero occurrences of the literal `shared path is never plane-gated to None` remain inside crates/mlrs-algos/src/."
    - "mlrs-core, mlrs-algos, mlrs-py, and mlrs-backend all compile cleanly under --features cpu after the change."
  artifacts:
    - path: "crates/mlrs-core/src/error.rs"
      provides: "New PrimError variant for the internal-None invariant"
      contains: "InternalNone"
  key_links:
    - from: "crates/mlrs-algos/src/linear/linear_regression.rs"
      to: "crates/mlrs-core/src/error.rs"
      via: ".ok_or(AlgoError::Prim(PrimError::InternalNone{..}))? — #[from] PrimError converts into AlgoError::Prim"
      pattern: "PrimError::InternalNone"
---

<objective>
Implement IN-04 (deliberately skipped during the Phase 16 fix pass; the user now overrides the skip). Add a typed `PrimError` variant for the "internal invariant violated / reduce prim returned an unexpected `None`" condition, then replace the 6 panicking `.expect("shared path is never plane-gated to None")` unwraps in `mlrs-algos` with typed-error propagation so the PyO3 boundary stays panic-free even if the reduce prim's plane-gating contract ever drifts.

Purpose: The 16-REVIEW IN-04 finding flagged that `column_reduce(.., ReducePath::Shared, ..)?.expect(...)` panics across the (PyO3) boundary if the reduce prim ever returns `None` on the Shared path. The skip rationale (16-REVIEW-FIX.md) was "no semantically appropriate typed-error target exists" — this plan creates that target.

Output: One new `PrimError` variant in `mlrs-core` and 6 converted call sites in `mlrs-algos`, all compiling under `--features cpu`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16-builder-retrofit-sweep-shim-coverage/16-REVIEW.md
@.planning/phases/16-builder-retrofit-sweep-shim-coverage/16-REVIEW-FIX.md
@./CLAUDE.md
@crates/mlrs-core/src/error.rs
@crates/mlrs-algos/src/error.rs

# Verified facts established during planning (do not re-investigate):
# - The literal `shared path is never plane-gated to None` appears 6 times in
#   crates/mlrs-algos/src/ (pca.rs:278, linear_regression.rs:293, ridge.rs:315,
#   ledoit_wolf.rs:265, empirical_covariance.rs:301, kernel_density.rs:448).
#   It ALSO appears in crates/mlrs-backend/src/prims/ and in test files — those
#   are OUT OF SCOPE for IN-04 (the finding is scoped to the mlrs-algos estimator
#   call sites). Do NOT touch backend/src or any *_test.rs files.
# - `AlgoError::Prim(#[from] PrimError)` exists (error.rs:378), so `?` on a bare
#   `PrimError` auto-converts into `AlgoError::Prim`. The fit functions all return
#   a `Result<_, AlgoError>`-shaped type, so `.ok_or(PrimError::InternalNone{..})?`
#   typechecks (the `?` runs the `#[from]` conversion). Confirm by compile.
# - NO exhaustive match over PrimError variants exists in the workspace. The only
#   destructuring matches are single-pattern test arms (`Err(PrimError::ShapeMismatch
#   {..}) => ...`) and `coordinate_descent.rs:222` which has an `other =>` wildcard
#   arm. Adding a variant therefore breaks no match — but mlrs-backend MUST still be
#   compile-checked (Task 2) to confirm.
# - DeviceArray has NO Drop impl; buffers are released manually via release_into.
#   At ridge:315 / linear_regression:293 the existing later `gemm(...)?` / `svd(...)?`
#   operators ALREADY return early on error without releasing the same `x_c_dev` /
#   `y_c_dev` buffers, so converting `.expect()` to `.ok_or(...)?` introduces NO new
#   leak class — it is consistent with the function's existing error-path convention,
#   and the path is the documented-unreachable invariant anyway.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the InternalNone PrimError variant in mlrs-core</name>
  <files>crates/mlrs-core/src/error.rs</files>
  <action>
Add a new variant to the `PrimError` enum (defined starting at line 62, ending with the `Overflow` variant's closing brace near line 164). Name it `InternalNone` and give it a single `operand: &'static str` field plus a `context: &'static str` field, mirroring the `&'static str` operand-label style every existing variant uses (NEVER introduce an owned `String` field — the existing variants that carry detail use `&'static str` operand labels or numeric fields; keep allocation-free to match).

The variant represents: "a primitive that is documented to always return `Some` on the path the caller used returned `None` — an internal invariant was violated." Write a thiserror `#[error(...)]` attribute whose message is consistent in tone with the existing variants (which open with `primitive '{operand}' ...`). Use a message of the form: primitive '{operand}' returned an unexpected None ({context}); an internal invariant was violated.

Add a doc comment above the variant in the same style as the siblings: explain that this is raised when a primitive that documents a non-None guarantee on a given path nonetheless yields `None` (e.g. `column_reduce` on `ReducePath::Shared`), so the estimator surfaces a recoverable typed error across the PyO3 boundary instead of panicking. Reference IN-04 in the doc comment so the variant is traceable to the finding.

Do NOT add any `match` arm anywhere — `AlgoError::Prim` uses `#[from]` and the PyO3 boundary (`mlrs-py/src/errors.rs::algo_err_to_py`) maps via `err.to_string()` (Display), so the new variant needs no boundary wiring beyond its `#[error(...)]` Display string.
  </action>
  <verify>
    <automated>cargo check -p mlrs-core --features cpu</automated>
  </verify>
  <done>`PrimError::InternalNone { operand, context }` exists in crates/mlrs-core/src/error.rs with a thiserror `#[error(...)]` message in the sibling style and a doc comment citing IN-04; `cargo check -p mlrs-core --features cpu` compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the 6 expect() unwraps with typed-error propagation and compile-check the consumers</name>
  <files>crates/mlrs-algos/src/decomposition/pca.rs, crates/mlrs-algos/src/linear/linear_regression.rs, crates/mlrs-algos/src/linear/ridge.rs, crates/mlrs-algos/src/covariance/ledoit_wolf.rs, crates/mlrs-algos/src/covariance/empirical_covariance.rs, crates/mlrs-algos/src/density/kernel_density.rs</files>
  <action>
At each of the 6 call sites listed below, replace the `.expect("shared path is never plane-gated to None")` that immediately follows a `column_reduce(.., ReducePath::Shared, ..)?` call with typed-error propagation:

Change the `...)?\n        .expect("shared path is never plane-gated to None")` form to `...)?\n        .ok_or(AlgoError::Prim(PrimError::InternalNone { operand: "column_reduce", context: "ReducePath::Shared" }))?`.

The `?` after `.ok_or(...)` returns the `AlgoError` directly (the value is already an `AlgoError`, so no `#[from]` conversion is even needed there — `.ok_or` produces `Result<DeviceArray, AlgoError>` and `?` propagates it). Pick an `operand` / `context` that names the actual prim and path at each site; `"column_reduce"` / `"ReducePath::Shared"` is correct for all 6 since every site is a Shared-path column_reduce.

The 6 sites (re-grep to get exact current line numbers before editing — do NOT trust stale numbers):
  - crates/mlrs-algos/src/decomposition/pca.rs:278
  - crates/mlrs-algos/src/linear/linear_regression.rs:293
  - crates/mlrs-algos/src/linear/ridge.rs:315
  - crates/mlrs-algos/src/covariance/ledoit_wolf.rs:265
  - crates/mlrs-algos/src/covariance/empirical_covariance.rs:301
  - crates/mlrs-algos/src/density/kernel_density.rs:448

Ensure `PrimError` is in scope in each file. Most already import it (they reference `PrimError` per the planning grep). If a file does not yet import `PrimError`, add `use mlrs_core::error::PrimError;` (or extend the existing `mlrs_core::error::{...}` use group) — check the file's existing imports first and match the existing import style; do not duplicate an import.

Buffer-leak note (linear_regression / ridge): the early `?` return on the `None` branch happens at the same program point where the existing later `gemm(...)?` / `svd(...)?` already return early without releasing `x_c_dev` / `y_c_dev`. This is the function's existing error-path convention on a documented-unreachable path; do NOT add new release_into cleanup or restructure the buffer threading — that would diverge from every sibling `?` site in the same function. Just convert the unwrap.

After editing all 6, re-grep to confirm zero remain in mlrs-algos/src:
  Run: grep -rn "shared path is never plane-gated to None" crates/mlrs-algos/src/
  Expect: zero matches.
(Backend/src and *_test.rs occurrences are OUT OF SCOPE and MUST remain untouched.)
  </action>
  <verify>
    <automated>grep -rc "shared path is never plane-gated to None" crates/mlrs-algos/src/ | grep -v ':0$' | grep -c . | grep -qx 0 && cargo check -p mlrs-algos -p mlrs-py --features cpu --lib && cargo check -p mlrs-backend --features cpu --lib</automated>
  </verify>
  <done>All 6 `.expect("shared path is never plane-gated to None")` sites in crates/mlrs-algos/src/ are replaced with `.ok_or(AlgoError::Prim(PrimError::InternalNone { .. }))?`; `grep -rn "shared path is never plane-gated to None" crates/mlrs-algos/src/` returns zero matches; `cargo check -p mlrs-algos -p mlrs-py --features cpu --lib` and `cargo check -p mlrs-backend --features cpu --lib` all compile cleanly (no new errors; pre-existing dead-code warnings are acceptable).</done>
</task>

</tasks>

<verification>
- `cargo check -p mlrs-core --features cpu` compiles (new variant well-formed).
- `cargo check -p mlrs-algos -p mlrs-py --features cpu --lib` compiles (call-site conversions + PyO3 boundary unchanged).
- `cargo check -p mlrs-backend --features cpu --lib` compiles (confirms the new variant broke no exhaustive match in the consuming backend crate — the skip rationale's stated risk).
- `grep -rn "shared path is never plane-gated to None" crates/mlrs-algos/src/` returns zero matches.
- The same grep over `crates/mlrs-backend/src/` and the `tests/` dirs is UNCHANGED (those sites are deliberately out of scope).
</verification>

<success_criteria>
- `PrimError::InternalNone { operand: &'static str, context: &'static str }` exists with a sibling-style `#[error(...)]` message and an IN-04-citing doc comment.
- All 6 mlrs-algos `.expect(...)` panic sites are now `.ok_or(AlgoError::Prim(PrimError::InternalNone{..}))?` typed-error propagations.
- mlrs-core, mlrs-algos, mlrs-py, and mlrs-backend all compile under `--features cpu`.
- No backend/src or test-file occurrences of the literal were modified.
</success_criteria>

<output>
Create `.planning/quick/260625-8ri-implement-in-04-add-a-primerror-variant-/260625-8ri-SUMMARY.md` when done.
</output>
