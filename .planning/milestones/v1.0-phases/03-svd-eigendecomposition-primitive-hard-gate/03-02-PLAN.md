---
phase: 03-svd-eigendecomposition-primitive-hard-gate
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/mlrs-core/src/error.rs
  - scripts/gen_oracle.py
  - crates/mlrs-backend/tests/svd_test.rs
  - crates/mlrs-backend/tests/eig_test.rs
  - tests/fixtures/svd_tall_f32_seed42.npz
  - tests/fixtures/svd_tall_f64_seed42.npz
  - tests/fixtures/svd_wide_f32_seed42.npz
  - tests/fixtures/eigh_f32_seed42.npz
  - tests/fixtures/eigh_f64_seed42.npz
autonomous: true
requirements: [PRIM-05]
must_haves:
  truths:
    - "PrimError carries NotSquare and NotConverged variants for SVD/eig validation and convergence failure (D-06, D-12)."
    - "scripts/gen_oracle.py emits np.linalg.svd(full_matrices=False) and np.linalg.eigh fixtures for the D-08/D-09 cases."
    - "Committed .npz fixtures exist for svd tall (f32+f64), svd wide (f32), and eigh (f32+f64)."
    - "svd_test.rs and eig_test.rs exist as compiling scaffolds with the Nyquist test functions present (initially ignored/failing until plans 03/04 land the prims)."
  artifacts:
    - path: "crates/mlrs-core/src/error.rs"
      provides: "NotSquare + NotConverged PrimError variants"
      contains: "NotConverged"
    - path: "scripts/gen_oracle.py"
      provides: "gen_svd + gen_eigh fixture generators"
      contains: "np.linalg.svd"
    - path: "crates/mlrs-backend/tests/svd_test.rs"
      provides: "SVD oracle + invariant test scaffold + fixture() resolver"
      min_lines: 30
    - path: "crates/mlrs-backend/tests/eig_test.rs"
      provides: "eig oracle + residual-invariant test scaffold"
      min_lines: 20
  key_links:
    - from: "crates/mlrs-backend/tests/svd_test.rs"
      to: "tests/fixtures/svd_tall_f32_seed42.npz"
      via: "load_npz(fixture(...))"
      pattern: "svd_tall_f32_seed42"
    - from: "scripts/gen_oracle.py"
      to: "tests/fixtures/eigh_f32_seed42.npz"
      via: "np.savez"
      pattern: "eigh_.*seed"
---

<objective>
Land the Nyquist Wave-0 scaffold for PRIM-05: the typed error variants, the numpy
oracle fixtures (D-09), and the SVD/eig integration-test files — everything the kernel
plans (03/04) need to exist BEFORE they implement, so each behavior has a failing test
to drive it. This plan has no dependency on the ROCm bring-up (it touches error.rs,
gen_oracle.py, fixtures, and new test files — disjoint from Plan 01's runtime.rs/Cargo),
so it runs in Wave 1 alongside Plan 01.

Purpose: RESEARCH §Validation Architecture lists 14 PRIM-05 test functions, all marked
"Wave 0". This plan creates the fixtures + error surface + test skeletons so plans 03/04
fill in green checks against a fixed reference, not a moving target.
Output: extended PrimError, extended gen_oracle.py, committed .npz fixtures, and two
compiling test scaffolds carrying the named Nyquist test functions.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-svd-eigendecomposition-primitive-hard-gate/03-RESEARCH.md
@.planning/phases/03-svd-eigendecomposition-primitive-hard-gate/03-PATTERNS.md
@.planning/phases/03-svd-eigendecomposition-primitive-hard-gate/03-VALIDATION.md
@crates/mlrs-core/src/error.rs
@scripts/gen_oracle.py
@crates/mlrs-backend/tests/gemm_test.rs
@AGENTS.md
</context>

<artifacts_this_phase_produces>
- PrimError variants NotSquare { operand, rows, cols } and NotConverged { operand, max_sweeps, residual } (crates/mlrs-core/src/error.rs).
- gen_oracle.py functions gen_svd (tall/wide, f32+f64) and gen_eigh (symmetric, f32+f64), with shape constants (e.g. SVD_TALL=(8,4), SVD_WIDE=(4,8), EIG_N=4) and a clustered/rank-deficient case selector.
- Fixtures: svd_tall_f32_seed42.npz, svd_tall_f64_seed42.npz, svd_wide_f32_seed42.npz, eigh_f32_seed42.npz, eigh_f64_seed42.npz (committed blobs in tests/fixtures/).
- Test scaffolds svd_test.rs (fixture() resolver + the Nyquist SVD test fns) and eig_test.rs (the eig test fns) — see VALIDATION.md Per-Task map for the exact fn names.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Extend PrimError with NotSquare + NotConverged and add the SVD/eig fixture generators to gen_oracle.py</name>
  <files>crates/mlrs-core/src/error.rs, scripts/gen_oracle.py</files>
  <read_first>
    - crates/mlrs-core/src/error.rs (the existing PrimError enum, lines ~66-100 — ShapeMismatch + DimMismatch with thiserror #[error(...)] + named fields + per-field doc comments; keep "one variant per violation class")
    - scripts/gen_oracle.py (the existing gen_gemm / gen_covariance functions + the shape-constant block ~47-59 + _FIXTURE_DIR ~36 + np.random.default_rng(seed) idiom)
    - 03-PATTERNS.md sections "crates/mlrs-core/src/error.rs" and "scripts/gen_oracle.py + gen_fixture.rs" (the exact variant shape + the gen_svd/gen_eigh recipe)
    - 03-RESEARCH.md Security §V5 (NotSquare for eig pre-launch validation) + D-12 (NotConverged for the max-sweep cap)
    - 03-CONTEXT.md D-04 (descending order; np.linalg.eigh returns ASCENDING — fixtures store the eigh output as-is and the test reverses, OR store reversed; pick reversed-in-test per RESEARCH Pattern 5 and document) + D-08 (degenerate cases)
  </read_first>
  <action>
    In error.rs add two variants to PrimError following the existing thiserror form (named
    fields + doc comment per variant): NotSquare { operand: &'static str, rows: usize, cols: usize }
    with message "primitive {operand} must be square: rows({rows}) != cols({cols})", and
    NotConverged { operand: &'static str, max_sweeps: u32, residual: f64 } with message
    "primitive {operand} did not converge within {max_sweeps} sweeps (off-diagonal norm
    {residual:e})". Variant naming is D-13 discretion; do not touch existing variants. In
    gen_oracle.py add gen_svd(seed, dtype, shape, kind) and gen_eigh(seed, dtype, n, kind)
    mirroring gen_gemm/gen_covariance: seeded rng = np.random.default_rng(seed); for SVD
    A = rng.standard_normal(shape).astype(dtype), U, S, Vt = np.linalg.svd(A,
    full_matrices=False) (D-02), savez named arrays A, U, S, Vt under
    svd_{kind}_{dtype}_seed{seed}.npz; for eigh build a symmetric A (e.g. AᵀA-style or
    M+Mᵀ) , w, V = np.linalg.eigh(Asym), reverse both to descending (D-04), savez A, w, V
    under eigh_{dtype}_seed{seed}.npz. Add shape constants SVD_TALL/SVD_WIDE/EIG_N and a
    clustered/rank-deficient case (RESEARCH D-08) so plans 03/04 can load a degenerate
    fixture for invariant-only checks. Wire the new generators into the script's main entry
    so a single run regenerates all fixtures. Do NOT run sklearn at test time (D-09 — fixtures
    are committed blobs).
  </action>
  <verify>
    <automated>cargo build -p mlrs-core 2>&1 | tail -3</automated>
    <automated>grep -n "NotSquare\|NotConverged" crates/mlrs-core/src/error.rs</automated>
    <automated>grep -n "np.linalg.svd\|np.linalg.eigh\|def gen_svd\|def gen_eigh" scripts/gen_oracle.py</automated>
  </verify>
  <acceptance_criteria>
    - cargo build -p mlrs-core exits 0; error.rs contains both NotSquare and NotConverged with thiserror #[error(...)] messages and per-field doc comments.
    - gen_oracle.py defines gen_svd and gen_eigh using np.linalg.svd(full_matrices=False) and np.linalg.eigh, with the eigh output reversed to descending (D-04).
    - The script's main wires in the new generators (a single invocation produces all five fixtures).
  </acceptance_criteria>
  <done>PrimError carries the SVD/eig validation+convergence variants and gen_oracle.py can emit the numpy reference fixtures.</done>
</task>

<task type="auto">
  <name>Task 2: Generate + commit the .npz fixtures and create the svd_test.rs / eig_test.rs scaffolds with the named Nyquist test functions</name>
  <files>crates/mlrs-backend/tests/svd_test.rs, crates/mlrs-backend/tests/eig_test.rs, tests/fixtures/svd_tall_f32_seed42.npz, tests/fixtures/svd_tall_f64_seed42.npz, tests/fixtures/svd_wide_f32_seed42.npz, tests/fixtures/eigh_f32_seed42.npz, tests/fixtures/eigh_f64_seed42.npz</files>
  <read_first>
    - crates/mlrs-backend/tests/gemm_test.rs (the full oracle-harness analog: the //! header naming tests + AGENTS.md §2 note; the fixture() resolver lines ~77-84; the per-case device runner ~120-149; the f64 skip pattern ~175-194 using capability::skip_f64_with_log; assert_slice_close_f32_gemm ~46-74; host_gemm_ref ~91-115)
    - 03-VALIDATION.md "Per-Task Verification Map" (the exact test fn names: svd_tall_f32_fixture, svd_tall_f64_fixture, svd_wide_f32_fixture, svd_reconstruction_invariant, svd_orthonormality_invariant, svd_degenerate_invariants, svd_moderate_256x64, eig_symmetric_f32_fixture, eig_symmetric_f64_fixture, eig_residual_invariant, eig_clustered_invariant)
    - 03-VALIDATION.md "Manual-Only Verifications" (the /tmp venv numpy regen command, PEP 668)
    - 03-PATTERNS.md section "svd_test.rs + eig_test.rs" (imports, fixture resolver, f64 split, invariant helpers) + the project memory note that fixtures are committed blobs regenerated via a /tmp numpy venv
  </read_first>
  <action>
    Generate the fixtures by running gen_oracle.py through a /tmp numpy venv (PEP 668):
    python3 -m venv /tmp/oraclevenv && /tmp/oraclevenv/bin/pip install numpy &&
    /tmp/oraclevenv/bin/python scripts/gen_oracle.py , then commit the five .npz blobs under
    tests/fixtures/ . Create svd_test.rs and eig_test.rs copying the gemm_test.rs structure:
    the //! module-doc header naming each test and stating tests live in tests/ (AGENTS.md §2,
    NO in-source mod tests); the fixture(name) PathBuf resolver verbatim; the imports
    (mlrs_backend::{capability, device_array::DeviceArray, pool::BufferPool,
    runtime::{self, ActiveRuntime}}, mlrs_core::{assert_slice_close, load_npz, Tolerance,
    F32_TOL, F64_TOL}, mlrs_core::sign_flip::align_rows). Declare the named Nyquist test
    functions from VALIDATION.md as #[test] stubs that load the fixture and (for now) assert
    only that the fixture loads + mark the not-yet-implemented prim call with #[ignore = "prim
    lands in plan 03/04"] so the scaffold COMPILES on cpu and rocm without svd()/eig() existing
    yet. f64 tests must include the capability::skip_f64_with_log() gate-and-return exactly as
    gemm_test.rs (f64 runs on cpu, SKIPS on rocm). Do NOT implement the prims here — plans
    03/04 remove the #[ignore] and wire the real svd()/eig() calls + invariant assertions.
  </action>
  <verify>
    <automated>ls -la tests/fixtures/svd_tall_f32_seed42.npz tests/fixtures/svd_tall_f64_seed42.npz tests/fixtures/svd_wide_f32_seed42.npz tests/fixtures/eigh_f32_seed42.npz tests/fixtures/eigh_f64_seed42.npz</automated>
    <automated>cargo test -p mlrs-backend --no-default-features --features cpu --test svd_test --test eig_test 2>&1 | tail -20</automated>
    <automated>grep -c "fn svd_tall_f32_fixture\|fn svd_reconstruction_invariant\|fn eig_residual_invariant\|fn svd_moderate_256x64" crates/mlrs-backend/tests/svd_test.rs crates/mlrs-backend/tests/eig_test.rs</automated>
  </verify>
  <acceptance_criteria>
    - All five .npz fixtures exist under tests/fixtures/ with non-zero size.
    - cargo test ... --features cpu --test svd_test --test eig_test COMPILES and runs (the ignored stubs report as ignored, the fixture-load assertions pass) — proves the scaffold + fixtures are wired before any prim exists.
    - svd_test.rs declares svd_tall_f32_fixture, svd_tall_f64_fixture, svd_wide_f32_fixture, svd_reconstruction_invariant, svd_orthonormality_invariant, svd_degenerate_invariants, svd_moderate_256x64; eig_test.rs declares eig_symmetric_f32_fixture, eig_symmetric_f64_fixture, eig_residual_invariant, eig_clustered_invariant (VALIDATION.md names).
    - Every f64 test contains a capability::skip_f64_with_log() gate-and-return; no in-source mod tests (AGENTS.md §2).
  </acceptance_criteria>
  <done>The numpy fixtures are committed and both test files compile with all Nyquist test functions present (ignored where the prim is not yet implemented).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time numpy → committed fixture blob | gen_oracle.py runs offline in a /tmp venv; the committed .npz is the trusted reference. No untrusted data at test time. |
| caller geometry → prim (future) | NotSquare/NotConverged are the typed rejections plans 03/04 will emit pre-launch (ASVS V5). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering | committed .npz fixtures | accept | Fixtures are deterministic (seeded np.random.default_rng), regenerable from gen_oracle.py; the script is the source of truth (D-09). |
| T-03-02-02 | Information Disclosure | error variant messages | mitigate | NotSquare/NotConverged messages report only shapes/sweep counts (no buffer contents); mirrors existing ShapeMismatch (no sensitive data leaked). |
| T-03-02-SC | Tampering | pip install numpy in /tmp venv | accept | numpy is a build-time-only dependency for fixture regen (PEP 668 venv), never linked into the library or test binary; fixtures are committed blobs (RESEARCH Package Legitimacy: no new registry installs in the crate graph). |
</threat_model>

<verification>
- cargo build -p mlrs-core green with the new PrimError variants.
- All five fixtures present under tests/fixtures/.
- cargo test ... --features cpu --test svd_test --test eig_test compiles and runs (ignored stubs + fixture-load assertions).
</verification>

<success_criteria>
- PrimError has NotSquare + NotConverged.
- gen_oracle.py emits the SVD/eig numpy fixtures; the five .npz blobs are committed.
- svd_test.rs and eig_test.rs compile with all VALIDATION.md-named test functions present (ignored until plans 03/04 implement the prims).
</success_criteria>

<output>
Create `.planning/phases/03-svd-eigendecomposition-primitive-hard-gate/03-02-SUMMARY.md` when done.
</output>
