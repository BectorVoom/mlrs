---
phase: 05-distance-based-iterative-solver-estimators
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - crates/mlrs-kernels/src/kmeans.rs
  - crates/mlrs-backend/src/prims/kmeans.rs
  - crates/mlrs-backend/tests/kmeanspp_test.rs
  - crates/mlrs-backend/tests/lloyd_test.rs
autonomous: true
requirements: [CLUSTER-01]
nyquist_compliant: true

must_haves:
  truths:
    - "A Lloyd centroid-update + inertia primitive recomputes per-label centroid means and the Σ-squared-distance inertia, validated against numpy within 1e-5"
    - "A k-means++ D²-weighted sampling primitive (host-seeded RNG over device D² weights) draws valid, seed-reproducible centers (D-09a/c)"
  artifacts:
    - path: "crates/mlrs-kernels/src/kmeans.rs"
      provides: "D² compute + centroid sum-by-label + inertia #[cube] kernels"
      contains: "#[cube"
    - path: "crates/mlrs-backend/src/prims/kmeans.rs"
      provides: "lloyd_update + inertia + kmeanspp_sample host-orchestration wrappers"
      contains: "pub fn"
  key_links:
    - from: "crates/mlrs-backend/src/prims/kmeans.rs"
      to: "crates/mlrs-backend/src/prims/distance.rs"
      via: "distance + argmin_rows reuse for assignment"
      pattern: "distance"
    - from: "crates/mlrs-backend/tests/lloyd_test.rs"
      to: "tests/fixtures/kmeans_f32_seed42.npz"
      via: "load_npz oracle compare"
      pattern: "load_npz"
---

<objective>
Land the NEW KMeans primitives (D-01): (1) a Lloyd centroid-update + inertia `#[cube]` kernel set — per-label centroid sum-by-label / count → mean, and inertia = Σ squared distance to the assigned center — and (2) the k-means++ D²-weighted sampling primitive with HOST-side seeded RNG over device-computed D² weights (D-09c, init only, not the hot loop). Assignment reuses the existing distance + argmin_rows prims (do NOT rebuild). Each lands with its standalone oracle/invariant BEFORE the KMeans estimator (07) consumes it.

The `kmeans.rs` (kernel) and `prims/kmeans.rs` (wrapper) stub files + their `pub mod` registrations were created by 05-01; this plan FILLS those stub files (adding any `pub use` inside them), never touching the shared `lib.rs`/`prims/mod.rs` — staying file-disjoint from the sibling Wave-2 prim plans.

Purpose: CLUSTER-01 Lloyd iteration math + the real k-means++ default init (D-09a). The injected-init oracle (D-09) is driven by the estimator; this plan validates the centroid/inertia math deterministically and the k-means++ sampler's validity + seed-reproducibility.
Output: `kmeans.rs` kernels + `prims/kmeans.rs` (lloyd_update, inertia, kmeanspp_sample) + `lloyd_test.rs` + `kmeanspp_test.rs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-distance-based-iterative-solver-estimators/05-RESEARCH.md
@.planning/phases/05-distance-based-iterative-solver-estimators/05-PATTERNS.md
@AGENTS.md
@crates/mlrs-backend/src/prims/distance.rs
@crates/mlrs-backend/src/prims/reduce.rs
</context>

<artifacts_this_phase_produces>
- Kernel: `mlrs_kernels::kmeans` — D² compute, centroid sum-by-label, inertia `#[cube]` fns (filled into the 05-01 stub)
- Prim: `mlrs_backend::prims::kmeans::{lloyd_update, inertia, kmeanspp_sample}`
- Tests: real assertions in `lloyd_test.rs` + `kmeanspp_test.rs`
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: fill kmeans #[cube] kernels (D² + centroid sum-by-label + inertia) + lloyd_update/inertia wrappers + lloyd_test oracle</name>
  <files>crates/mlrs-kernels/src/kmeans.rs, crates/mlrs-backend/src/prims/kmeans.rs, crates/mlrs-backend/tests/lloyd_test.rs</files>
  <read_first>
    - crates/mlrs-kernels/src/kmeans.rs + crates/mlrs-backend/src/prims/kmeans.rs (the 05-01 stubs to fill; modules already declared)
    - crates/mlrs-kernels/src/reduce.rs (reduce_sumsq_shared ~161-186 — the squared-norm accumulation for D²/inertia)
    - crates/mlrs-kernels/src/elementwise.rs (center_columns ~81-93 — the `c = tid % cols` per-element scatter shape for centroid sum-by-label)
    - crates/mlrs-backend/src/prims/distance.rs + prims/reduce.rs::argmin_rows (assignment = argmin of squared distance — REUSE, do not rebuild)
    - crates/mlrs-backend/tests/cholesky_test.rs (standalone prim oracle scaffold + f64 skip gate)
    - 05-RESEARCH.md §"KMeans Lloyd iteration + convergence" code example (centroid = mean of assigned; empty-cluster relocation = farthest point; inertia = Σ d²(X_i, centers[label_i]); max_iter=300) + §"_k_means_common.pyx" relocation pin
    - 05-PATTERNS.md §"kmeans.rs" + the Wave-0 lloyd_test stub from 05-01
  </read_first>
  <behavior>
    - Given X (n×d), labels (n), k: centroid[c] = mean over rows with label==c
    - Empty cluster c (no assigned points): relocate to the point farthest from its current center (sklearn _k_means_common relocation)
    - inertia = Σ_i ‖X_i − centers[labels_i]‖² (squared, no sqrt)
    - All within 1e-5 of a numpy reference
  </behavior>
  <action>
    Fill `kmeans.rs` with feature-free `#[cube]` kernels generic over `<F: Float + CubeElement>`: a centroid sum-by-label accumulation (scatter X rows into per-label sums + counts, the center_columns `tid % cols` scatter shape) and an inertia accumulation (reduce_sumsq pattern over per-row distance-to-assigned-center). Where the math is a pure host reduction over small k, prefer composing column_reduce/argmin_rows rather than a bespoke kernel (Claude's-discretion granularity per RESEARCH Open Q1 — keep device work on the n-heavy parts). Fill `prims/kmeans.rs` with `lloyd_update(pool, x, labels, k, n, d, ...) -> centers` and `inertia(pool, x, centers, labels, n, d) -> F`, validating geometry before any unsafe launch (ASVS V5), implementing sklearn's empty-cluster relocation (farthest-point by argpartition of point→center d²). Add `pub use` inside the stub files as needed; do NOT edit lib.rs/prims/mod.rs (05-01 owns the `pub mod` lines).
    De-`#[ignore]` `lloyd_test.rs`: from `kmeans_{f32,f64}_seed42.npz` take X + the fixture `labels`, run `lloyd_update` and assert the resulting centers equal the per-label numpy means within 1e-5; run `inertia` and assert it equals the fixture `inertia` within 1e-5. f64 guarded by `skip_f64_with_log`.
  </action>
  <verify>
    <automated>cargo build -p mlrs-kernels 2>&1 | tail -3 && cargo test --features cpu -p mlrs-backend lloyd_test 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p mlrs-kernels` exits 0; kmeans.rs kernels are feature-free `<F: Float + CubeElement>`
    - `cargo test --features cpu -p mlrs-backend lloyd_test` exits 0 (no longer ignored)
    - prims/kmeans.rs validates geometry before unsafe launch (grep PrimError before ArrayArg)
    - lloyd_update result matches per-label numpy means within 1e-5; inertia matches fixture within 1e-5
    - Empty-cluster relocation is implemented (farthest-point), not a divide-by-zero
    - lib.rs/prims/mod.rs NOT modified (05-01 owns the `pub mod` lines)
    - `cargo build -p mlrs-backend --features rocm --tests` exits 0
  </acceptance_criteria>
  <done>Lloyd centroid-update + inertia primitive validated standalone within 1e-5 on cpu(f64); empty-cluster relocation present; no shared-file edit.</done>
</task>

<task type="auto">
  <name>Task 2: fill k-means++ D²-weighted sampling (host-seeded RNG over device D²) + kmeanspp_test invariant</name>
  <files>crates/mlrs-backend/src/prims/kmeans.rs, crates/mlrs-backend/tests/kmeanspp_test.rs</files>
  <read_first>
    - crates/mlrs-backend/src/prims/kmeans.rs (Task 1 — extend the same prim file)
    - crates/mlrs-backend/src/prims/distance.rs (D² to current centers via the distance prim, sqrt=false)
    - 05-RESEARCH.md §"Architectural Responsibility Map" (k-means++ row: host-side seeded RNG reads device D² weights per center, INIT ONLY) + §"Anti-Patterns" (NO device-side RNG — backend-divergent) + D-09a/b/c
    - 05-PATTERNS.md §"prims/kmeans.rs (D²-sample host-RNG)" + the Wave-0 kmeanspp_test stub
    - crates/mlrs-backend/src/prims/reduce.rs (per-row min distance for the D² weight)
  </read_first>
  <action>
    Add `kmeanspp_sample(pool, x, n, d, k, seed) -> Vec<usize>` (or centers DeviceArray) to `prims/kmeans.rs`: pick center 0 uniformly via a HOST-side seeded PRNG (documented seeded RNG, never OsRng — ASVS V6); for each subsequent center compute D² = min squared distance to the chosen centers via the distance prim + per-row min reduce, read the D² weights back to host (init only, NOT the hot loop — D-09c), and draw the next center index with the host PRNG weighted by D² (sklearn's `n_init=1` / 'auto', D-09b). Validate `1 <= k <= n` (PrimError on bad k/shape) before launch.
    De-`#[ignore]` `kmeanspp_test.rs` as an INVARIANT test (no committed sklearn k-means++ value — RNG can't match bit-for-bit, D-09): assert (a) the k sampled indices are distinct and in `0..n`, (b) running `kmeanspp_sample` twice with the SAME seed yields identical indices (seed-reproducibility, D-09c), (c) different seeds can differ. f64 guarded by `skip_f64_with_log`.
  </action>
  <verify>
    <automated>cargo test --features cpu -p mlrs-backend kmeanspp_test 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --features cpu -p mlrs-backend kmeanspp_test` exits 0 (no longer ignored)
    - kmeanspp_sample uses a host-side seeded PRNG (grep: no device RNG, no OsRng)
    - Invariant test asserts distinct indices in 0..n AND same-seed reproducibility
    - D² weights are read back per center (init only); the prim does not RNG on-device
    - `cargo build -p mlrs-backend --features rocm --tests` exits 0
  </acceptance_criteria>
  <done>k-means++ D²-sampling validated: valid distinct centers + seed-reproducible on cpu; no device RNG.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| estimator → lloyd_update/kmeanspp_sample(n, d, k) | k/geometry untrusted; empty clusters and zero-count means are degenerate-input hazards |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Tampering / Info-disclosure | lloyd/kmeanspp geometry & k | mitigate | validate `1 <= k <= n`, `n*d == x.len()` → PrimError before unsafe launch (ASVS V5) |
| T-05-03-02 | DoS | empty-cluster / zero-count centroid mean | mitigate | sklearn empty-cluster relocation (farthest point) before the mean → never a divide-by-zero NaN |
| T-05-03-03 | Tampering | seeded RNG reproducibility | mitigate | host-side documented seeded PRNG only (never OsRng — ASVS V6); same-seed reproducibility pinned by the invariant test |
| T-05-03-SC | Tampering | dependencies | accept | zero new packages (RESEARCH audit: none) |
</threat_model>

<verification>
- `cargo test --features cpu -p mlrs-backend lloyd_test kmeanspp_test` green (centroids/inertia 1e-5; k-means++ valid + seed-reproducible)
- `cargo build -p mlrs-kernels` + `-p mlrs-backend --features rocm --tests` green
</verification>

<success_criteria>
- Lloyd centroid-update + inertia validated within 1e-5 standalone; empty-cluster relocation present
- k-means++ D²-sampling valid + seed-reproducible, host-RNG only — both before the KMeans estimator (D-01)
</success_criteria>

<output>
Create `.planning/phases/05-distance-based-iterative-solver-estimators/05-03-SUMMARY.md` when done
</output>
