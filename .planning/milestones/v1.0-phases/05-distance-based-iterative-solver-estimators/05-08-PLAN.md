---
phase: 05-distance-based-iterative-solver-estimators
plan: 08
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - crates/mlrs-algos/src/neighbors/mod.rs
  - crates/mlrs-algos/src/neighbors/nearest.rs
  - crates/mlrs-algos/src/neighbors/classifier.rs
  - crates/mlrs-algos/src/neighbors/regressor.rs
  - crates/mlrs-algos/tests/nearest_neighbors_test.rs
  - crates/mlrs-algos/tests/knn_classifier_test.rs
  - crates/mlrs-algos/tests/knn_regressor_test.rs
autonomous: true
requirements: [NEIGH-01, NEIGH-02, NEIGH-03]
nyquist_compliant: true

must_haves:
  truths:
    - "NearestNeighbors.kneighbors returns k sqrt-Euclidean distances + i32 indices matching sklearn within 1e-5 (lowest-index tie-break)"
    - "KNeighborsClassifier predict (majority vote, lowest-class-index tie) + predict_proba (per-class neighbor fraction) match sklearn"
    - "KNeighborsRegressor predict (mean of k neighbor targets) matches sklearn within 1e-5"
  artifacts:
    - path: "crates/mlrs-algos/src/neighbors/nearest.rs"
      provides: "NearestNeighbors<F>: Fit + KNeighbors trait"
      contains: "impl"
    - path: "crates/mlrs-algos/src/neighbors/classifier.rs"
      provides: "KNeighborsClassifier<F>: Fit + PredictLabels + PredictProba"
      contains: "impl"
    - path: "crates/mlrs-algos/src/neighbors/regressor.rs"
      provides: "KNeighborsRegressor<F>: Fit + Predict<F>"
      contains: "impl"
  key_links:
    - from: "crates/mlrs-algos/src/neighbors/nearest.rs"
      to: "mlrs_backend::prims::topk::top_k"
      via: "distance + top_k over query rows"
      pattern: "top_k"
    - from: "crates/mlrs-algos/tests/nearest_neighbors_test.rs"
      to: "tests/fixtures/knn_f32_seed42.npz"
      via: "load_npz oracle compare"
      pattern: "load_npz"
---

<objective>
Land the three KNN estimators (NEIGH-01/02/03) on the validated top-k primitive (05-02). `NearestNeighbors<F>` implements `Fit` (store training X) + the `KNeighbors` trait (D-07 — returns `(DeviceArray<F>` sqrt-Euclidean distances, `DeviceArray<i32>` indices`)` via distance+top_k, lowest-index tie-break). `KNeighborsClassifier<F>` implements `Fit` + `PredictLabels` (majority vote, argmax with lowest-class-index tie) + `PredictProba` (per-class neighbor fraction, uniform weights). `KNeighborsRegressor<F>` implements `Fit` + `Predict<F>` (mean of the k neighbor targets). All brute-force Euclidean, `weights='uniform'` (CONTEXT Deferred — no `'distance'`/spatial index in v1).

Purpose: NEIGH-01 + NEIGH-02 + NEIGH-03 — the neighbors family.
Output: `neighbors/{nearest,classifier,regressor}.rs`, `neighbors/mod.rs` (mod lines), 3 oracle tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-distance-based-iterative-solver-estimators/05-RESEARCH.md
@.planning/phases/05-distance-based-iterative-solver-estimators/05-PATTERNS.md
@AGENTS.md
@crates/mlrs-algos/src/linear/ridge.rs
@crates/mlrs-algos/src/traits.rs
@crates/mlrs-algos/tests/ridge_test.rs
</context>

<artifacts_this_phase_produces>
- Estimators: `mlrs_algos::neighbors::nearest::NearestNeighbors<F>`, `::classifier::KNeighborsClassifier<F>`, `::regressor::KNeighborsRegressor<F>`
- `neighbors/mod.rs` `pub mod nearest; pub mod classifier; pub mod regressor;`
- Tests: real assertions in `nearest_neighbors_test.rs`, `knn_classifier_test.rs`, `knn_regressor_test.rs`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: NearestNeighbors<F> (Fit + KNeighbors trait) + nearest_neighbors oracle</name>
  <files>crates/mlrs-algos/src/neighbors/nearest.rs, crates/mlrs-algos/src/neighbors/mod.rs, crates/mlrs-algos/tests/nearest_neighbors_test.rs</files>
  <read_first>
    - crates/mlrs-algos/src/linear/ridge.rs (struct + new + device-resident state + validate-before-launch + host accessors)
    - crates/mlrs-algos/src/traits.rs (the new KNeighbors trait from 05-01 — returns (distances F, indices i32))
    - mlrs_backend::prims::topk::top_k + prims::distance (from 05-02 — top_k on squared distance, sqrt at boundary)
    - 05-RESEARCH.md §"_base.py:743-749" (argpartition(k-1) then argsort the k; return sqrt(d²)) + Pitfall 8 (select on squared, sqrt the returned k; lowest-index tie) + §"phase_requirements" NEIGH-01
    - 05-PATTERNS.md §"neighbors/nearest.rs" + the Wave-0 nearest_neighbors_test stub
  </read_first>
  <action>
    Add `neighbors/nearest.rs` with `NearestNeighbors<F>` mirroring `ridge.rs`: fields `n_neighbors: usize`, device-resident training `x_train_: Option<DeviceArray<..,F>>` + its `(n_train, d)` shape. `Fit::fit` stores X (no y); validate geometry before launch. Implement the `KNeighbors` trait: `kneighbors(pool, xq, shape, k)` validates `1 <= k <= n_train` → `AlgoError::InvalidK` BEFORE launch, computes `distance(xq, x_train_, sqrt=false)` then `top_k(.., k, sqrt=true)` returning the k sqrt-Euclidean distances (F) + the k indices (i32, D-06 host u32→i32). Add `pub mod nearest;` to neighbors/mod.rs.
    De-`#[ignore]` `nearest_neighbors_test.rs`: load `knn_{f32,f64}_seed42.npz`, fit on X, call `kneighbors(Xq, k)`, assert returned distances within 1e-5 of the fixture `distances` AND indices equal the fixture `indices`. Include the constructed-tie invariant (lowest index, Pitfall 8/A5). f64 guarded by `skip_f64_with_log`.
  </action>
  <verify>
    <automated>cargo test --features cpu -p mlrs-algos nearest_neighbors_test 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --features cpu -p mlrs-algos nearest_neighbors_test` exits 0 (no longer ignored)
    - NearestNeighbors implements the KNeighbors trait returning (F distances, i32 indices)
    - kneighbors validates `1 <= k <= n_train` → InvalidK before launch
    - distances within 1e-5 of fixture (sqrt Euclidean); indices equal fixture; tie → lowest index
    - `cargo build -p mlrs-algos --features rocm --tests` exits 0
  </acceptance_criteria>
  <done>NearestNeighbors.kneighbors returns k sqrt-Euclidean distances + i32 indices matching sklearn within 1e-5 (NEIGH-01).</done>
</task>

<task type="auto">
  <name>Task 2: KNeighborsClassifier (PredictLabels + PredictProba) + KNeighborsRegressor (Predict) + oracles</name>
  <files>crates/mlrs-algos/src/neighbors/classifier.rs, crates/mlrs-algos/src/neighbors/regressor.rs, crates/mlrs-algos/src/neighbors/mod.rs, crates/mlrs-algos/tests/knn_classifier_test.rs, crates/mlrs-algos/tests/knn_regressor_test.rs</files>
  <read_first>
    - crates/mlrs-algos/src/neighbors/nearest.rs (Task 1 — reuse its kneighbors path for the vote/mean) + traits.rs (PredictLabels + PredictProba from 05-01; Predict<F>)
    - mlrs_backend::prims::reduce::argmax_rows (the lowest-index tie-break for the proba argmax vote)
    - 05-RESEARCH.md §"_classification.py" (predict = argmax(proba); proba = per-class neighbor fraction, uniform; lowest class index on tie) + §"_regression.py" (predict = mean of k neighbor targets, uniform) + NEIGH-02/03
    - 05-PATTERNS.md §"neighbors/classifier.rs / regressor.rs" + the Wave-0 knn_classifier_test / knn_regressor_test stubs
  </read_first>
  <action>
    Add `neighbors/classifier.rs` with `KNeighborsClassifier<F>` storing training X + i32 class targets `y_class_`. `Fit::fit` stores both. `PredictProba::predict_proba`: get the k neighbor indices (reuse the nearest kneighbors path), gather their classes host-side, compute per-class neighbor FRACTION (uniform weights) → F device array (n_query × n_classes). `PredictLabels::predict_labels`: argmax of the proba with the lowest-class-index tie-break (argmax_rows convention) → i32 labels. Validate `1 <= k <= n_train` → InvalidK before launch.
    Add `neighbors/regressor.rs` with `KNeighborsRegressor<F>` storing training X + F targets `y_reg_`. `Fit::fit` stores both. `Predict<F>::predict`: get the k neighbor indices, gather their targets, return the MEAN (uniform weights) → F device array. Validate k before launch.
    Add `pub mod classifier; pub mod regressor;` to neighbors/mod.rs.
    De-`#[ignore]` `knn_classifier_test.rs` (assert predict matches the fixture classifier `predict` exactly AND predict_proba within 1e-5 of the fixture `predict_proba`) and `knn_regressor_test.rs` (assert predict within 1e-5 of the fixture regressor `predict`), both from `knn_{f32,f64}_seed42.npz` (y_class / y_reg keys). f64 guarded by `skip_f64_with_log`.
  </action>
  <verify>
    <automated>cargo test --features cpu -p mlrs-algos knn_classifier_test knn_regressor_test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --features cpu -p mlrs-algos knn_classifier_test knn_regressor_test` exits 0 (no longer ignored)
    - KNeighborsClassifier implements PredictLabels (argmax, lowest-class-index tie) + PredictProba (per-class fraction); predict matches fixture exactly, proba within 1e-5
    - KNeighborsRegressor implements Predict<F> (mean of k targets); predict within 1e-5 of fixture
    - Both validate `1 <= k <= n_train` → InvalidK before launch
    - `cargo build -p mlrs-algos --features rocm --tests` exits 0
  </acceptance_criteria>
  <done>KNeighborsClassifier predict/predict_proba and KNeighborsRegressor predict match sklearn within tolerance (NEIGH-02/03).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → kneighbors / predict(k) | k + query geometry untrusted at the estimator boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08-01 | Tampering / Info-disclosure | k vs n_train, query geometry | mitigate | validate `1 <= k <= n_train` (InvalidK) + `n_query*d == xq.len()` before any prim launch (ASVS V5); top_k prim re-validates (05-02) |
| T-05-08-02 | Tampering | tie-break determinism (neighbor index + class vote) | mitigate | lowest-index from top_k (05-02); lowest-class-index from argmax_rows; constructed-tie oracle pins it |
| T-05-08-SC | Tampering | dependencies | accept | zero new packages (RESEARCH audit: none) |
</threat_model>

<verification>
- `cargo test --features cpu -p mlrs-algos nearest_neighbors_test knn_classifier_test knn_regressor_test` green
- `cargo build -p mlrs-algos --features rocm --tests` green
</verification>

<success_criteria>
- NearestNeighbors.kneighbors: k sqrt-Euclidean distances + i32 indices within 1e-5 (NEIGH-01)
- KNeighborsClassifier predict/predict_proba match sklearn (NEIGH-02); KNeighborsRegressor predict within 1e-5 (NEIGH-03)
</success_criteria>

<output>
Create `.planning/phases/05-distance-based-iterative-solver-estimators/05-08-SUMMARY.md` when done
</output>
