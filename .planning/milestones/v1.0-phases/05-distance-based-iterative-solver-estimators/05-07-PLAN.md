---
phase: 05-distance-based-iterative-solver-estimators
plan: 07
type: execute
wave: 3
depends_on: ["05-01", "05-03", "05-04"]
files_modified:
  - crates/mlrs-algos/src/cluster/mod.rs
  - crates/mlrs-algos/src/cluster/kmeans.rs
  - crates/mlrs-algos/src/cluster/dbscan.rs
  - crates/mlrs-algos/tests/kmeans_test.rs
  - crates/mlrs-algos/tests/dbscan_test.rs
autonomous: true
requirements: [CLUSTER-01, CLUSTER-02]
nyquist_compliant: true

must_haves:
  truths:
    - "KMeans fits via k-means++ init + the Lloyd loop, exposes cluster_centers_/labels_/inertia_, predicts new points, and matches sklearn up to label permutation within 1e-5 from injected init (D-09)"
    - "DBSCAN fits via the device core-mask + host index-ordered DFS, exposes labels_ (noise=-1) + core_sample_indices_, matching sklearn up to label permutation; it does NOT implement standalone predict (D-08)"
  artifacts:
    - path: "crates/mlrs-algos/src/cluster/kmeans.rs"
      provides: "KMeans<F>: Fit + PredictLabels + fit_predict"
      contains: "impl"
    - path: "crates/mlrs-algos/src/cluster/dbscan.rs"
      provides: "DBSCAN<F>: Fit + fit_predict (host DFS), no standalone predict"
      contains: "impl"
  key_links:
    - from: "crates/mlrs-algos/src/cluster/kmeans.rs"
      to: "mlrs_backend::prims::kmeans"
      via: "kmeanspp_sample + lloyd_update + inertia + distance/argmin_rows"
      pattern: "lloyd"
    - from: "crates/mlrs-algos/src/cluster/dbscan.rs"
      to: "mlrs_backend::prims::dbscan::eps_core_mask"
      via: "device core mask → host DFS"
      pattern: "eps_core_mask"
---

<objective>
Land the clustering estimators (CLUSTER-01/02) on the validated Wave-2 primitives. `KMeans<F>` implements `Fit` (k-means++ init via D-09a sampler, the Lloyd loop reproducing sklearn's strict-OR-tol convergence, max_iter=300, empty-cluster relocation), `PredictLabels` (assign new points to fitted centers, returning i32 labels — D-08: KMeans.predict returns INTEGER labels, so it implements PredictLabels, NOT the F-typed `Predict<F>`), and `fit_predict`, storing `cluster_centers_` (F) device-resident + `labels_` (i32). `DBSCAN<F>` implements `Fit` + `fit_predict` driving the device `eps_core_mask` prim then the HOST index-ordered LIFO DFS (D-04, `_dbscan_inner.pyx`), storing `labels_` (i32, noise=-1) + `core_sample_indices_` (i32) — and does NOT implement standalone `predict` (D-08). Oracle uses INJECTED init (D-09) + `label_perm` up-to-permutation comparison.

Purpose: CLUSTER-01 + CLUSTER-02 — the distance-based clustering family.
Output: `cluster/kmeans.rs`, `cluster/dbscan.rs`, `cluster/mod.rs` (mod lines), `kmeans_test.rs`, `dbscan_test.rs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-distance-based-iterative-solver-estimators/05-RESEARCH.md
@.planning/phases/05-distance-based-iterative-solver-estimators/05-PATTERNS.md
@AGENTS.md
@crates/mlrs-algos/src/linear/ridge.rs
@crates/mlrs-algos/src/traits.rs
@crates/mlrs-algos/tests/ridge_test.rs
</context>

<artifacts_this_phase_produces>
- Estimators: `mlrs_algos::cluster::kmeans::KMeans<F>`, `mlrs_algos::cluster::dbscan::DBSCAN<F>`
- `cluster/mod.rs` `pub mod kmeans; pub mod dbscan;`
- Tests: real assertions in `kmeans_test.rs`, `dbscan_test.rs`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: KMeans<F> (k-means++ init + Lloyd loop) Fit/PredictLabels/fit_predict + kmeans oracle</name>
  <files>crates/mlrs-algos/src/cluster/kmeans.rs, crates/mlrs-algos/src/cluster/mod.rs, crates/mlrs-algos/tests/kmeans_test.rs</files>
  <read_first>
    - crates/mlrs-algos/src/linear/ridge.rs (the estimator that consumes a new prim: struct + new + device-resident fitted state + host accessors `coef(..)` NotFitted, validate-hyperparameter-before-launch, host_to_f64/f64_to_host helpers — copy verbatim)
    - crates/mlrs-algos/src/traits.rs (Fit/Predict + the new PredictLabels from 05-01)
    - mlrs_backend::prims::kmeans (kmeanspp_sample, lloyd_update, inertia — from 05-03) + prims::distance + prims::reduce::argmin_rows (assignment)
    - 05-RESEARCH.md §"KMeans Lloyd iteration + convergence" (strict array_equal(labels,labels_old) BREAK before the tol check; tol = mean(var(X,axis=0))·1e-4; max_iter=300; one final assignment pass if not strict-converged — Pitfall 6) + D-09/D-09a/b/c + §"Architectural Responsibility Map"
    - 05-PATTERNS.md §"cluster/kmeans.rs" + the Wave-0 kmeans_test stub + label_perm note
    - crates/mlrs-core/src/label_perm.rs (best_match_accuracy must be 1.0)
  </read_first>
  <action>
    Add `cluster/kmeans.rs` with `KMeans<F>` mirroring `ridge.rs`: fields `n_clusters: usize`, `max_iter: usize` (=300), `tol: F` (=1e-4), `seed`, an OPTIONAL injected `init` (for the oracle, D-09), device-resident `cluster_centers_: Option<DeviceArray<..,F>>`, `labels_: Option<DeviceArray<..,i32>>`, `inertia_: Option<F>`. Host accessors materialize on demand with `NotFitted` before fit. `Fit::fit`: validate `1 <= n_clusters <= n_samples` → `AlgoError::InvalidK` BEFORE any launch (ASVS V5); init centers from the injected `init` if present else `kmeanspp_sample` (D-09a, n_init=1 D-09b); run the Lloyd loop — assignment via `distance(sqrt=false)` + `argmin_rows`, centers via `lloyd_update` (empty-cluster relocation inside the prim), break on strict `array_equal(labels,labels_old)` THEN on `center_shift_tot <= tol_scaled` where `tol_scaled = mean(var(X,axis=0))·tol` (Pitfall 6), one final assignment pass if not strict-converged; compute `inertia` via the prim. Store labels as i32 (D-06 host u32→i32 idiom). Implement `PredictLabels` for new-point assignment (assign new X to fitted centers via distance+argmin → returns i32 LABELS; do NOT implement `Predict<F>`, which returns a `DeviceArray<..,F>` — D-08/D-05) and a `fit_predict`. Add `pub mod kmeans;` to cluster/mod.rs.
    De-`#[ignore]` `kmeans_test.rs`: load `kmeans_{f32,f64}_seed42.npz`, construct `KMeans` with the fixture's injected `init` (D-09), fit, then assert `cluster_centers_` and `inertia_` within 1e-5 (up to label permutation) and `best_match_accuracy(labels_i64, fixture_labels_i64) == 1.0` (label_perm). f64 guarded by `skip_f64_with_log`.
  </action>
  <verify>
    <automated>cargo test --features cpu -p mlrs-algos kmeans_test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --features cpu -p mlrs-algos kmeans_test` exits 0 (no longer ignored)
    - KMeans validates `1 <= n_clusters <= n_samples` → AlgoError::InvalidK before any launch
    - Fit reproduces sklearn's strict-OR-tol convergence with tol = mean(var(X,axis=0))·1e-4, max_iter=300 (Pitfall 6)
    - centers + inertia within 1e-5; `best_match_accuracy == 1.0` (label_perm, D-09)
    - labels_ stored as i32; KMeans implements PredictLabels (new-point assignment) + fit_predict
    - `cargo build -p mlrs-algos --features rocm --tests` exits 0
  </acceptance_criteria>
  <done>KMeans fits from injected init matching sklearn centers/labels/inertia up to permutation within 1e-5; k-means++ default init; PredictLabels assigns new points.</done>
</task>

<task type="auto">
  <name>Task 2: DBSCAN<F> (device core-mask + host index-ordered DFS) Fit/fit_predict + dbscan oracle</name>
  <files>crates/mlrs-algos/src/cluster/dbscan.rs, crates/mlrs-algos/src/cluster/mod.rs, crates/mlrs-algos/tests/dbscan_test.rs</files>
  <read_first>
    - crates/mlrs-algos/src/linear/ridge.rs (the Fit shell: struct + validate-before-launch + device-resident state + accessors)
    - mlrs_backend::prims::dbscan::eps_core_mask (from 05-04 — returns host is_core mask + adjacency)
    - 05-RESEARCH.md §"DBSCAN core mask + host DFS" code example (the EXACT index-ordered LIFO DFS: labels=-1, for i in 0..n skip if labeled or not core, push neighbors, label_num increment; core = self-inclusive eps-count ≥ min_samples; `<= eps`) + Pitfall 7 (border joins the FIRST core point reaching it in index order — DETERMINISTIC) + D-04
    - 05-PATTERNS.md §"No Analog Found" (cluster/dbscan.rs host DFS — pin the LIFO from `_dbscan_inner.pyx`; Fit shell mirrors ridge.rs) + the Wave-0 dbscan_test stub
    - crates/mlrs-core/src/label_perm.rs
  </read_first>
  <action>
    Add `cluster/dbscan.rs` with `DBSCAN<F>` mirroring the `ridge.rs` Fit shell: fields `eps: F`, `min_samples: usize`, device-resident `labels_: Option<DeviceArray<..,i32>>`, `core_sample_indices_: Option<DeviceArray<..,i32>>`. `Fit::fit`: validate `eps >= 0` → `AlgoError::InvalidEps`, `min_samples >= 1` → `AlgoError::InvalidMinSamples` BEFORE launch (ASVS V5); call `eps_core_mask` (device n² distance + threshold → host is_core + adjacency, D-04); run the HOST index-ordered LIFO DFS EXACTLY per `_dbscan_inner.pyx` (Pitfall 7 — labels init -1, iterate i in 0..n in index order, skip if labeled or not core, push core-point neighbors onto a LIFO stack, increment label_num per seed); set noise = -1, materialize `labels_`/`core_sample_indices_` as i32 (D-06). Implement `fit_predict` returning the i32 labels. Do NOT implement standalone `predict` (D-08 — no transductive predict; document this). Add `pub mod dbscan;` to cluster/mod.rs.
    De-`#[ignore]` `dbscan_test.rs`: load `dbscan_{f32,f64}_seed42.npz`, fit, assert `core_sample_indices_` exactly matches the fixture set and `best_match_accuracy(labels_i64, fixture_labels_i64) == 1.0` (label_perm up to permutation, noise=-1 handled). f64 guarded by `skip_f64_with_log`.
  </action>
  <verify>
    <automated>cargo test --features cpu -p mlrs-algos dbscan_test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --features cpu -p mlrs-algos dbscan_test` exits 0 (no longer ignored)
    - DBSCAN validates `eps >= 0` (InvalidEps) and `min_samples >= 1` (InvalidMinSamples) before launch
    - The host DFS is index-ordered LIFO (grep: stack push/pop, `0..n` index loop) reproducing `_dbscan_inner.pyx` (Pitfall 7)
    - core_sample_indices_ exactly matches the fixture; `best_match_accuracy == 1.0`; noise = -1
    - DBSCAN does NOT implement standalone Predict (D-08, documented)
    - `cargo build -p mlrs-algos --features rocm --tests` exits 0
  </acceptance_criteria>
  <done>DBSCAN matches sklearn labels (noise=-1) + core_sample_indices_ up to permutation via device core-mask + host index-ordered DFS; no transductive predict.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → KMeans/DBSCAN fit | n_clusters/eps/min_samples + geometry untrusted at the estimator boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07-01 | Tampering | KMeans n_clusters / DBSCAN eps,min_samples | mitigate | validate `1<=n_clusters<=n_samples` (InvalidK), `eps>=0` (InvalidEps), `min_samples>=1` (InvalidMinSamples) before any prim launch (ASVS V5) |
| T-05-07-02 | DoS | empty cluster (KMeans) | mitigate | empty-cluster relocation handled in the lloyd_update prim (05-03) — no NaN centroid |
| T-05-07-03 | DoS | DBSCAN n² for large n | mitigate | n² bound + reuse handled in eps_core_mask (05-04) + plan-11 gate; estimator does not re-allocate |
| T-05-07-SC | Tampering | dependencies | accept | zero new packages (RESEARCH audit: none) |
</threat_model>

<verification>
- `cargo test --features cpu -p mlrs-algos kmeans_test dbscan_test` green (centers/inertia + labels up to permutation 1e-5; core_sample_indices_ exact)
- `cargo build -p mlrs-algos --features rocm --tests` green
</verification>

<success_criteria>
- KMeans: k-means++ init + Lloyd loop, cluster_centers_/labels_/inertia_ + PredictLabels, matches sklearn up to permutation from injected init (CLUSTER-01)
- DBSCAN: device core-mask + host index-ordered DFS, labels_(−1) + core_sample_indices_, no standalone predict, matches sklearn up to permutation (CLUSTER-02)
</success_criteria>

<output>
Create `.planning/phases/05-distance-based-iterative-solver-estimators/05-07-SUMMARY.md` when done
</output>
