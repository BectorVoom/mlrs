---
phase: 05-distance-based-iterative-solver-estimators
plan: 06
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - crates/mlrs-kernels/src/lbfgs.rs
  - crates/mlrs-backend/src/prims/lbfgs.rs
  - crates/mlrs-backend/tests/lbfgs_test.rs
autonomous: true
requirements: [LINEAR-05]
nyquist_compliant: true

must_haves:
  truths:
    - "A standalone L-BFGS solver minimizes a convex quadratic ½xᵀAx − bᵀx to its known minimizer x* = A⁻¹b within 1e-5, isolating solver correctness from sklearn path-matching (Pitfall 5)"
    - "A stable softmax loss+gradient kernel reproduces sklearn's symmetric multinomial objective (1/n)Σloss + ½·l2_reg·‖coef‖² (intercept unpenalized) and gradient within 1e-5"
    - "The L-BFGS host loop reads back exactly one scalar (max |proj-grad|) per iteration and reuses gradient + (s,y) history buffers across iterations (D-10)"
  artifacts:
    - path: "crates/mlrs-kernels/src/lbfgs.rs"
      provides: "stable softmax loss + gradient #[cube] kernel"
      contains: "#[cube"
    - path: "crates/mlrs-backend/src/prims/lbfgs.rs"
      provides: "lbfgs_minimize host two-loop recursion + strong-Wolfe line search (m=10)"
      contains: "pub fn"
  key_links:
    - from: "crates/mlrs-backend/src/prims/lbfgs.rs"
      to: "crates/mlrs-kernels/src/lbfgs.rs"
      via: "softmax loss/grad kernel launch in the host loop"
      pattern: "launch::"
    - from: "crates/mlrs-backend/tests/lbfgs_test.rs"
      to: "tests/fixtures/logistic_multi_f32_seed42.npz"
      via: "softmax-grad oracle compare"
      pattern: "load_npz"
---

<objective>
Land the NEW L-BFGS solver primitive (D-03 independent solver for LogisticRegression) — THE highest correctness risk in the project. Two device pieces + a host loop: (1) a stable softmax loss+gradient `#[cube]` kernel reproducing sklearn's symmetric over-parameterized multinomial objective; (2) the host L-BFGS two-loop recursion with `m=10` + a strong-Wolfe line search, reading back exactly one scalar (max |proj-grad|) per iteration and reusing gradient + `(s,y)` history buffers (D-10). Pinned from `_linear_loss.py`/`_loss/loss.py`/`scipy _lbfgsb_py.py`: objective `(1/n)·Σloss + ½·l2_reg·‖coef‖²`, `l2_reg = 1/(C·n)`, intercept unpenalized, L-BFGS-B `m=10`/`maxls=50`/`gtol=tol`/`ftol=64·eps`/`maxiter=100`.

CRITICAL (Pitfall 5): validate the L-BFGS solver STANDALONE on a convex quadratic with a known minimizer BEFORE the softmax/LogReg path — this isolates "is my L-BFGS correct" from "does it match sklearn's path". Lands before the LogReg estimator (10) consumes it (D-01).

Purpose: LINEAR-05 solver core.
Output: `lbfgs.rs` kernel + `prims/lbfgs.rs` (lbfgs_minimize, softmax loss/grad orchestration) + `lbfgs_test.rs` (convex-quadratic invariant + softmax-grad oracle).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-distance-based-iterative-solver-estimators/05-RESEARCH.md
@.planning/phases/05-distance-based-iterative-solver-estimators/05-PATTERNS.md
@AGENTS.md
@crates/mlrs-backend/src/prims/gemm.rs
@crates/mlrs-backend/src/prims/reduce.rs
@crates/mlrs-backend/src/prims/cholesky.rs
</context>

<artifacts_this_phase_produces>
- Kernel: `mlrs_kernels::lbfgs` module — stable softmax loss + gradient `#[cube]` fn(s)
- Prim: `mlrs_backend::prims::lbfgs::{lbfgs_minimize, softmax_loss_grad}`
- Test: real assertions in `lbfgs_test.rs` (convex-quadratic invariant + softmax-grad oracle)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: stable softmax loss+gradient #[cube] kernel (symmetric multinomial, logsumexp)</name>
  <files>crates/mlrs-kernels/src/lbfgs.rs</files>
  <read_first>
    - crates/mlrs-kernels/src/reduce.rs (reduce_max_shared ~304-329 — the max for logsumexp; reduce_sum for the exp-sum)
    - crates/mlrs-kernels/src/elementwise.rs (dist_combine_clamp — the 2D logits map shape; center_columns for the (P−Y) form)
    - 05-RESEARCH.md §"LogReg objective + gradient" code example (raw = X@W+b; lse = logsumexp; loss = (1/n)Σ(lse−raw_y) + ½·l2_reg·‖W‖²; gradW = (1/n)(P−Y)ᵀX + l2_reg·W; gradb = (1/n)Σ(p−Y), NO penalty) + Pitfall 4 (stable `m + log Σ exp(raw−m)`) + Pitfall 3 (intercept unpenalized, l2_reg = 1/(C·n)) + §"HalfMultinomialLoss" (symmetric over-parameterized — n_classes full weight vectors, D-12)
    - 05-PATTERNS.md §"lbfgs.rs" (the two-loop recursion is HOST-side; the kernel emits ONLY loss + grad)
    - AGENTS.md §3 + cubecl manual INDEX.md
  </read_first>
  <behavior>
    - Given X (n×d), W (K×d), b (K), y (n int labels), l2_reg: raw[i,k] = X[i]·W[k] + b[k]
    - lse[i] = max_k raw[i,k] + log Σ_k exp(raw[i,k] − max) (stable, no overflow on well-separated classes)
    - loss = (1/n)Σ_i(lse[i] − raw[i,y_i]) + ½·l2_reg·‖W‖_F² (b UNPENALIZED)
    - p[i,k] = softmax(raw[i,k]); gradW = (1/n)(P−Y_onehot)ᵀX + l2_reg·W; gradb = (1/n)Σ_i(p[i]−Y_onehot[i])
    - loss + grad within 1e-5 of a numpy reference (validated in Task 3)
  </behavior>
  <action>
    Add `lbfgs.rs` with feature-free `#[cube]` kernels generic over `<F: Float + CubeElement>` that emit the multinomial softmax loss + gradient ONLY (the two-loop recursion is host-side, D-10). Compute `raw = X@W + b` (compose the gemm prim host-side or a 2D logits map), the STABLE logsumexp (`m = reduce_max` over the K logits per row, then `m + log Σ exp(raw−m)` — Pitfall 4), the per-row softmax `p`, the loss accumulation `(1/n)Σ(lse − raw_y) + ½·l2_reg·‖W‖²` (intercept unpenalized — Pitfall 3), and the gradient `gradW = (1/n)(P−Y)ᵀX + l2_reg·W`, `gradb = (1/n)Σ(p−Y)` (no penalty). Symmetric over-parameterized form: K full weight vectors (D-12), so binary = 2-class case. Scalar args (`n`, `d`, `k_classes`, `l2_reg: F`) by value; no `continue`; no hardcoded plane width. Fill the 05-01 `lbfgs.rs` stub (module already declared); add any `pub use` INSIDE lbfgs.rs — do NOT edit lib.rs.
  </action>
  <verify>
    <automated>cargo build -p mlrs-kernels 2>&1 | tail -3 && grep -q "#\[cube\]" crates/mlrs-kernels/src/lbfgs.rs && echo LBFGS_KERNEL_OK</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p mlrs-kernels` exits 0; lbfgs.rs is feature-free `<F: Float + CubeElement>`
    - lib.rs is NOT modified (05-01 owns the `pub mod` line)
    - The logsumexp subtracts the row-max before exp (grep: `reduce_max` / `max` before `exp`) — Pitfall 4
    - The loss penalty is on W only, not b (intercept unpenalized) — Pitfall 3
    - Symmetric K-full-weight-vector form (not K−1); no two-loop recursion in the kernel
  </acceptance_criteria>
  <done>Stable symmetric-multinomial softmax loss+grad kernel compiles feature-free; logsumexp stabilized; intercept unpenalized.</done>
</task>

<task type="auto">
  <name>Task 2: lbfgs_minimize host loop (two-loop recursion m=10, strong-Wolfe) + convex-quadratic standalone invariant</name>
  <files>crates/mlrs-backend/src/prims/lbfgs.rs, crates/mlrs-backend/tests/lbfgs_test.rs</files>
  <read_first>
    - crates/mlrs-backend/src/prims/cholesky.rs (the new-primitive wrapper shape: validate→launch→scalar-readback; `to_host` + `release_into`)
    - crates/mlrs-backend/src/prims/gemm.rs + reduce.rs (Aᵀx / Ax for the quadratic gradient; vector dots for the two-loop)
    - 05-RESEARCH.md §"Pattern 2 (host loop)" + Pitfall 5 (CONVEX-QUADRATIC standalone validation FIRST: minimize ½xᵀAx − bᵀx, optimum x* = A⁻¹b, assert within 1e-5 — isolates solver correctness) + §"scipy _lbfgsb_py.py" constants (m=10, maxls=50, gtol=1e-4, ftol=64·eps, maxiter=100) + §"Open Questions" Q2 (host owns history + two-loop; gradient is the only large device buffer, reused)
    - 05-PATTERNS.md §"No Analog Found" (no existing host iterative-optimizer; pin the math from RESEARCH) + the Wave-0 lbfgs_test stub
  </read_first>
  <action>
    Add `prims/lbfgs.rs` with a GENERIC `lbfgs_minimize` host loop parameterized by a closure/trait that returns (loss, gradient) for a given parameter vector — owning the standard two-loop recursion with `m=10` history of `(s, y)` pairs and a strong-Wolfe (Moré-Thuente) line search, the scipy L-BFGS-B constants (`gtol=1e-4` on max |proj-grad|, `ftol=64·eps` relative-f decrease, `maxls=50`, `maxiter=100`). Acquire the gradient + history buffers ONCE and reuse every iteration; read back EXACTLY ONE scalar (max |proj-grad|) per iteration for the convergence test (D-10). On `maxiter` reached return the last iterate (the LogReg estimator surfaces AlgoError::NotConverged if needed). Validate parameter-vector length before any unsafe launch (ASVS V5). Fill the 05-01 `prims/lbfgs.rs` stub; do NOT edit prims/mod.rs (05-01 owns the `pub mod` line).
    De-`#[ignore]` the convex-quadratic invariant in `lbfgs_test.rs` (Pitfall 5): construct a small SPD `A` and vector `b`, minimize `f(x) = ½xᵀAx − bᵀx` (gradient `Ax − b`) with `lbfgs_minimize`, and assert the final iterate equals `x* = A⁻¹b` (solved with the Phase-4 cholesky prim or a host solve) within 1e-5. This is the algebraic-invariant pattern of `cholesky_test.rs` `‖A·x−b‖`. f64 guarded by `skip_f64_with_log`. (The softmax-objective oracle is Task 3.)
  </action>
  <verify>
    <automated>cargo test --features cpu -p mlrs-backend lbfgs_test convex_quadratic 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - The convex-quadratic invariant test passes: final iterate within 1e-5 of `A⁻¹b` (solver correctness isolated, Pitfall 5)
    - lbfgs_minimize uses `m=10`, strong-Wolfe line search, gtol=1e-4 / ftol=64·eps / maxls=50 / maxiter=100 (grep the constants)
    - Exactly one scalar (max |proj-grad|) readback per iteration; gradient + (s,y) history acquired once and reused
    - Parameter-vector length validated before unsafe launch
    - `cargo build -p mlrs-backend --features rocm --tests` exits 0
  </acceptance_criteria>
  <done>L-BFGS solver minimizes the convex quadratic to A⁻¹b within 1e-5 standalone — solver correctness proven before any softmax/LogReg path consumes it (Pitfall 5).</done>
</task>

<task type="auto">
  <name>Task 3: softmax loss/grad oracle (numpy reference, binary + multiclass) + lbfgs softmax minimize smoke</name>
  <files>crates/mlrs-backend/src/prims/lbfgs.rs, crates/mlrs-backend/tests/lbfgs_test.rs</files>
  <read_first>
    - 05-RESEARCH.md §"LogReg objective + gradient" (the exact loss/grad form to match) + Pitfall 3/4 + §"Metadata" (L-BFGS exact path-matching is MEDIUM — gate on the gradient/loss numerically here; the gauge-invariant predict_proba gate is the ESTIMATOR's job, plan 10)
    - crates/mlrs-backend/tests/lbfgs_test.rs (the convex-quadratic test from Task 2 + the Wave-0 softmax-grad stub names)
    - tests/fixtures/logistic_binary_*.npz + logistic_multi_*.npz (committed in 05-01; carry X, y, C, coef, intercept)
  </read_first>
  <action>
    Add a `softmax_loss_grad(pool, x, y, w, b, n, d, k, l2_reg) -> (loss, gradW, gradb)` orchestration entry in `prims/lbfgs.rs` (the host launcher for the Task-1 kernel, validating geometry before launch). De-`#[ignore]` the softmax-grad oracle in `lbfgs_test.rs`: for both `logistic_binary` and `logistic_multi` fixtures, compute the loss + gradient at a fixed test W/b (e.g. zeros and a small random point) and assert they match a HOST numpy-equivalent reference computed in-test (the RESEARCH objective formula with `l2_reg = 1/(C·n)`, intercept unpenalized) within 1e-5 — a finite-difference gradient check is acceptable as the invariant. Add a smoke test that `lbfgs_minimize` driven by `softmax_loss_grad` on the binary fixture converges (max |proj-grad| under gtol) without NaN — full sklearn coef_ agreement is the ESTIMATOR's gate (plan 10, gauge-invariant predict_proba primary). f64 guarded by `skip_f64_with_log`.
  </action>
  <verify>
    <automated>cargo test --features cpu -p mlrs-backend lbfgs_test 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --features cpu -p mlrs-backend lbfgs_test` exits 0 (all lbfgs_test functions, no longer ignored)
    - softmax loss + gradient match the in-test numpy-equivalent reference within 1e-5 for binary AND multiclass
    - The gradient passes a finite-difference check (or matches the closed-form reference)
    - `lbfgs_minimize`+`softmax_loss_grad` converges on the binary fixture without NaN (Pitfall 4 stability holds)
    - softmax_loss_grad validates geometry before unsafe launch
    - `cargo build -p mlrs-backend --features rocm --tests` exits 0
  </acceptance_criteria>
  <done>Softmax loss/grad matches numpy reference within 1e-5 (binary+multiclass); L-BFGS+softmax converges without NaN — L-BFGS primitive complete and validated before LogReg (D-01).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| estimator → lbfgs_minimize / softmax_loss_grad(n, d, k, C) | geometry/C untrusted; large logits → overflow (NaN); non-convergent solver → DoS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06-01 | Tampering / Info-disclosure | softmax/lbfgs geometry | mitigate | validate parameter-vector + `n*d`/`y`/`W`/`b` lengths → PrimError before unsafe launch (ASVS V5) |
| T-05-06-02 | DoS | NaN/Inf from large logits | mitigate | stable logsumexp (subtract row-max before exp, Pitfall 4) → no overflow on well-separated classes; smoke test pins no-NaN convergence |
| T-05-06-03 | DoS | non-convergent L-BFGS | mitigate | `maxiter=100` cap → bounded iterations; estimator surfaces NotConverged, never a silent hang |
| T-05-06-04 | Tampering | per-iteration array readback breaking the memory gate | mitigate | exactly ONE scalar (max\|proj-grad\|) readback per iteration; gradient + (s,y) history reused (D-10); plan 11 gates it |
| T-05-06-SC | Tampering | dependencies | accept | zero new packages (RESEARCH audit: none) |
</threat_model>

<verification>
- `cargo test --features cpu -p mlrs-backend lbfgs_test` green: convex-quadratic invariant (A⁻¹b) + softmax loss/grad oracle (binary+multiclass) + no-NaN convergence smoke
- `cargo build -p mlrs-kernels` + `-p mlrs-backend --features rocm --tests` green
</verification>

<success_criteria>
- Stable symmetric-multinomial softmax loss+grad kernel (intercept unpenalized, l2_reg=1/(C·n))
- L-BFGS solver proven correct standalone on the convex quadratic (x*=A⁻¹b within 1e-5) BEFORE the softmax path (Pitfall 5)
- softmax loss/grad matches numpy within 1e-5; L-BFGS+softmax converges without NaN — all before the LogReg estimator (D-01)
</success_criteria>

<output>
Create `.planning/phases/05-distance-based-iterative-solver-estimators/05-06-SUMMARY.md` when done
</output>
