---
phase: 02-core-compute-primitives
plan: 04
type: execute
wave: 4
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - crates/mlrs-backend/src/prims/mod.rs
  - crates/mlrs-backend/src/prims/covariance.rs
  - tests/fixtures/cov_ddof0_f64_seed42.npz
  - tests/fixtures/cov_ddof1_f64_seed42.npz
  - tests/fixtures/cov_ddof1_f32_seed42.npz
  - crates/mlrs-backend/tests/covariance_test.rs
autonomous: true
requirements: [PRIM-04]
user_setup: []

must_haves:
  truths:
    - "Covariance / XᵀX (Gram) is built on GEMM as Aᵀ·A via the transa transpose flag — no materialized transpose buffer (D-09)"
    - "Both population (ddof=0) and sample (ddof=1) normalization are produced and pinned by np.cov fixtures (D-12)"
    - "Covariance matches the host reference within tolerance for f32 and f64 on cpu and wgpu"
    - "Covariance reuses the GEMM output buffer rather than allocating a parallel one (D-10 gate 3 prerequisite)"
  artifacts:
    - path: "crates/mlrs-backend/src/prims/covariance.rs"
      provides: "covariance host API: column-mean centering + Aᵀ·A via GEMM(transa) + ddof scale, GEMM-buffer reuse"
      contains: "pub fn covariance"
    - path: "crates/mlrs-backend/tests/covariance_test.rs"
      provides: "ddof=0 AND ddof=1 fixtures + host ref, f32/f64, cpu/wgpu"
      contains: "covariance_ddof0_matches"
  key_links:
    - from: "crates/mlrs-backend/src/prims/covariance.rs"
      to: "crates/mlrs-backend/src/prims/gemm.rs"
      via: "gemm(transa=true) for Aᵀ·A"
      pattern: "gemm\\("
    - from: "crates/mlrs-backend/src/prims/covariance.rs"
      to: "crates/mlrs-backend/src/prims/reduce.rs"
      via: "column-mean reduction for centering"
      pattern: "reduce|mean|column"
    - from: "crates/mlrs-backend/src/prims/covariance.rs"
      to: "crates/mlrs-kernels/src/elementwise.rs"
      via: "scale kernel for 1/(n-ddof) normalization"
      pattern: "scale"
---

<objective>
Build the covariance / XᵀX (Gram) primitive (PRIM-04) on top of GEMM, realized as
`Aᵀ·A` via the transpose flag (D-06) with no materialized transpose buffer, column-
mean centering (Plan 02 reduction), and ddof normalization for both population
(ddof=0) and sample (ddof=1) conventions pinned by `np.cov` fixtures. It reuses the
GEMM output buffer (setting up D-10 gate 3 in Plan 05).

Purpose: PRIM-04 — a covariance primitive PCA and the linear closed-form solvers
inherit, with the exact sklearn/numpy normalization convention baked in. Output:
the `covariance` host API, ddof=0/1 npz fixtures, and `covariance_test.rs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-compute-primitives/02-CONTEXT.md
@.planning/phases/02-core-compute-primitives/02-RESEARCH.md
@.planning/phases/02-core-compute-primitives/02-PATTERNS.md
@AGENTS.md
@.planning/phases/02-core-compute-primitives/02-01-SUMMARY.md
@.planning/phases/02-core-compute-primitives/02-02-SUMMARY.md
@.planning/phases/02-core-compute-primitives/02-03-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan (Plan 04) adds:
- **Host API fn** (`crates/mlrs-backend/src/prims/covariance.rs`):
  `covariance(...)` with a `ddof` parameter (0 and 1 both supported).
- **New file**: `crates/mlrs-backend/tests/covariance_test.rs`, ddof=0/1 `.npz`
  fixtures.
No new kernels: covariance composes GEMM (Plan 01) + column-mean reduction
(Plan 02) + the `scale` kernel (Plan 03 `elementwise.rs`).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Covariance host API (column-mean centering + Aᵀ·A + ddof scale, GEMM-buffer reuse)</name>
  <files>crates/mlrs-backend/src/prims/covariance.rs, crates/mlrs-backend/src/prims/mod.rs</files>
  <behavior>
    - covariance centers columns via the column-mean reduction, computes Aᵀ·A via gemm(transa=true) (no transpose buffer), then scales by 1/(n-ddof) via the scale kernel.
    - ddof=0 (population) and ddof=1 (sample) both selectable.
    - The Aᵀ·A output reuses the GEMM output buffer (passed as the scale target / out-buffer) rather than allocating a parallel one (D-10 gate 3).
  </behavior>
  <read_first>
    - crates/mlrs-backend/src/prims/gemm.rs (gemm API — call with transa=true for Aᵀ·A)
    - crates/mlrs-backend/src/prims/reduce.rs (column-mean reduction fn — Plan 02 SUMMARY records the name)
    - crates/mlrs-kernels/src/elementwise.rs (the scale kernel — Plan 03 SUMMARY records its signature)
    - crates/mlrs-backend/src/device_array.rs (DeviceArray in/out)
    - crates/mlrs-backend/src/pool.rs (acquire/release; reuse the GEMM output as the covariance out-buffer, D-11/D-10 gate 3)
    - crates/mlrs-backend/src/prims/mod.rs (add `pub mod covariance;`)
    - .planning/phases/02-core-compute-primitives/02-RESEARCH.md (§Per-Primitive §4 Covariance; D-09; D-10 gate 3 assertion strategy)
    - .planning/phases/02-core-compute-primitives/02-PATTERNS.md (§covariance composition — Aᵀ·A via GEMM(transa), reuse GEMM output as `out`)
    - .planning/phases/02-core-compute-primitives/02-02-SUMMARY.md, 02-03-SUMMARY.md (column-mean + scale signatures)
  </read_first>
  <action>
Add `pub mod covariance;` to `prims/mod.rs` and create
`crates/mlrs-backend/src/prims/covariance.rs`. Implement `covariance(pool, a:
&DeviceArray, (n_samples, n_features), ddof: u32, out: Option<DeviceArray>) ->
DeviceArray`: assert `n_samples*n_features == a.len()` (D-04); center columns by
subtracting the per-column mean (column-mean reduction from Plan 02 — two-pass for
stability); compute the `n_features × n_features` Gram `Aᵀ·A` via `gemm(...,
transa=true)` with NO transpose buffer (D-06); scale the result by `1/(n_samples -
ddof)` via the `scale` kernel from `elementwise.rs`. Drive the GEMM output into a
pool-acquired buffer and pass THAT same buffer as the scale target / out-buffer so
covariance reuses the GEMM output rather than allocating a parallel one (the
mechanism Plan 05's D-10 gate 3 asserts). Support `ddof = 0` and `ddof = 1`. Keep
device-resident (no `to_host` in the API, D-05).
  </action>
  <verify>
    <automated>cargo build -p mlrs-backend --features cpu && cargo build -p mlrs-backend --features wgpu</automated>
  </verify>
  <acceptance_criteria>
    - `crates/mlrs-backend/src/prims/covariance.rs` contains `pub fn covariance` calling `gemm(...)` with the transa flag and the `scale` kernel for `1/(n-ddof)`.
    - The covariance output handle is the reused GEMM output buffer (not a fresh parallel allocation) — verifiable in Plan 05's gate; documented in this SUMMARY.
    - No `to_host` in `covariance.rs` (`grep -c to_host crates/mlrs-backend/src/prims/covariance.rs` is 0).
    - `cargo build -p mlrs-backend --features cpu` and `--features wgpu` exit 0.
  </acceptance_criteria>
  <done>Device-resident covariance composing column-mean centering + Aᵀ·A via GEMM(transa) + ddof scale, reusing the GEMM output buffer.</done>
</task>

<task type="auto">
  <name>Task 2: Validate covariance (ddof=0 AND ddof=1 fixtures + host ref, f32/f64, cpu/wgpu)</name>
  <files>crates/mlrs-backend/tests/covariance_test.rs, tests/fixtures/cov_ddof0_f64_seed42.npz, tests/fixtures/cov_ddof1_f64_seed42.npz, tests/fixtures/cov_ddof1_f32_seed42.npz, scripts/gen_oracle.py</files>
  <read_first>
    - crates/mlrs-backend/tests/pipeline_test.rs (oracle test header/imports, assert_slice_close, f64 gate)
    - crates/mlrs-backend/src/prims/covariance.rs (the API under test)
    - crates/mlrs-core/src/compare.rs (assert_slice_close)
    - crates/mlrs-core/src/oracle.rs (load_npz for the cov fixtures)
    - scripts/gen_oracle.py (extend for np.cov ddof=0 and ddof=1 cases; /tmp numpy venv, PEP 668)
    - .planning/phases/02-core-compute-primitives/02-RESEARCH.md (§Per-Primitive §4 Validate; D-12 cov ddof fixtures — np.cov ddof=1 vs population ddof=0)
    - .planning/phases/02-core-compute-primitives/02-PATTERNS.md (§fixtures — cov_ddofN_dtype_seed naming)
  </read_first>
  <action>
Create `crates/mlrs-backend/tests/covariance_test.rs`. Extend `gen_oracle.py` to
emit `cov_ddof0_f64_seed42.npz` (population, `np.cov(A, rowvar=False, ddof=0)`),
`cov_ddof1_f64_seed42.npz` (sample, `ddof=1`), and `cov_ddof1_f32_seed42.npz`,
storing the input `A` and the numpy covariance reference; regenerate via the /tmp
numpy venv and commit the blobs. Be explicit about the numpy convention
(`rowvar=False` so features are columns) so the fixture pins exactly what estimators
inherit. Write `covariance_ddof0_matches` and `covariance_ddof1_matches`: run
`covariance(ddof=0)` / `covariance(ddof=1)` and `assert_slice_close` against BOTH a
host-reference loop (f64) AND the corresponding npz fixture. f64 arms guarded by
`skip_f64_with_log`; the f32 case uses `cov_ddof1_f32_seed42.npz` + F32_TOL; each
test logs the dtype/backend line; run on cpu and wgpu.
  </action>
  <verify>
    <automated>cargo test -p mlrs-backend --features cpu covariance</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p mlrs-backend --features cpu covariance` and `--features wgpu covariance` exit 0.
    - Both ddof=0 and ddof=1 outputs match the host reference AND the np.cov fixtures within F32_TOL/F64_TOL.
    - `cov_ddof0_f64_seed42.npz`, `cov_ddof1_f64_seed42.npz`, `cov_ddof1_f32_seed42.npz` exist under `tests/fixtures/`.
    - The fixtures encode the `rowvar=False` convention (features as columns) matching the host API's `(n_samples, n_features)` contract.
  </acceptance_criteria>
  <done>PRIM-04 validated: covariance matches np.cov for both ddof=0 and ddof=1 (host ref + fixtures), f32 and f64, green on cpu and wgpu.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host slice → device buffer | A uploaded via `DeviceArray::from_host`; `len` is the validated read-back source of truth (T-04-01) |
| caller `(n_samples, n_features)` → GEMM index math | covariance derives geometry from the caller shape; must satisfy `n_samples*n_features == len` (D-04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-0204-01 | Tampering | `ArrayArg::from_raw_parts(handle, len)` in the Aᵀ·A launch | mitigate | `len` from validated `DeviceArray.len`; GEMM kernel bounds-checks indices (T-04-01 carried forward) |
| T-0204-02 | Tampering | caller `(n_samples, n_features)` vs `len` | mitigate | assert `n_samples*n_features == a.len()` at the `covariance` entry before launch (D-04, ASVS V5) |
| T-0204-SC | Tampering | cargo dependency | mitigate | Zero new dependencies — composition of in-tree GEMM + reduction + scale kernel |

No high-severity threats: numerical compute-kernel phase, no auth/session/network/PII surface.
</threat_model>

<verification>
- `cargo test -p mlrs-backend --features cpu covariance` and `--features wgpu covariance` green.
- ddof=0 and ddof=1 both match np.cov fixtures + host ref.
- Covariance reuses the GEMM output buffer (verified in Plan 05).
</verification>

<success_criteria>
- PRIM-04 / ROADMAP Criterion 4: covariance / XᵀX built on GEMM matches the host reference within tolerance for both dtypes on cpu and wgpu.
- Aᵀ·A via transpose flag, no transpose buffer (D-09); ddof=0 and ddof=1 pinned by fixtures (D-12).
</success_criteria>

<output>
Create `.planning/phases/02-core-compute-primitives/02-04-SUMMARY.md` when done.
Record: how the GEMM output buffer is reused as the covariance output (the exact
mechanism Plan 05's D-10 gate 3 asserts), and any CubeCL build-error resolutions.
</output>
