---
phase: 06-python-surface-pyo3-estimators-per-backend-wheels
plan: 06
type: execute
wave: 5
depends_on: ["06-04"]
files_modified:
  - crates/mlrs-py/python/tests/test_estimator_checks.py
  - crates/mlrs-py/python/tests/test_wheels.py
  - crates/mlrs-py/python/tests/test_import_probe.py
  - crates/mlrs-py/python/tests/checks_triage.md
autonomous: false
requirements: [PY-01, PY-02, PY-04]

must_haves:
  truths:
    - "Each of the 12 estimators passes its RELEVANT subset of sklearn.utils.estimator_checks; intentionally-unsupported checks (sparse/array-api/NaN, DBSCAN/NearestNeighbors predict) are skipped-with-documented-reason (criterion 1, NOT 'all checks pass')"
    - "The relevant-vs-skipped triage is recorded per estimator in a committed triage doc (the empirical Wave-0 result)"
    - "All four backend wheels build via maturin under the distinct distribution names mlrs-cpu / mlrs-wgpu / mlrs-cuda / mlrs-rocm, each importable as `import mlrs`, abi3-py312 (PY-04/D-07/D-09)"
    - "Importing a wheel whose backend driver is absent fails with a clear ImportError, not a crash (PY-04/D-08)"
  artifacts:
    - path: "crates/mlrs-py/python/tests/test_estimator_checks.py"
      provides: "parametrize_with_checks triage over the 12 estimators with skip-with-reason"
      contains: "parametrize_with_checks"
    - path: "crates/mlrs-py/python/tests/checks_triage.md"
      provides: "per-estimator relevant/skipped estimator_checks record"
      contains: "skip"
    - path: "crates/mlrs-py/python/tests/test_wheels.py"
      provides: "the four wheels build with the right dist names + abi3-py312"
      contains: "mlrs_cpu"
  key_links:
    - from: "crates/mlrs-py/python/tests/test_wheels.py"
      to: "crates/mlrs-py/pyproject/{cpu,wgpu,cuda,rocm}.pyproject.toml"
      via: "maturin build -m <pyproject>"
      pattern: "maturin build"
    - from: "crates/mlrs-py/python/tests/test_import_probe.py"
      to: "the D-08 catch_unwind probe"
      via: "absent-driver ImportError assertion"
      pattern: "ImportError"
---

<objective>
Close the phase gate's remaining two criteria: (1) run the RELEVANT subset of
`sklearn.utils.estimator_checks` for each of the 12 estimators, triaging pass/fail
empirically and skipping intentionally-unsupported checks with a documented reason
(criterion 1 — "relevant", not "all"); (2) build all four per-backend wheels under
their distinct distribution names with `abi3-py312`, confirm each imports as `mlrs`,
and assert the driver-absent `ImportError` (criterion 4 / PY-04).

Purpose: criterion 1's checks half and criterion 4 (wheels + probe) are the last
parts of the gate. The cuda wheel is compile-only here; rocm runs f32. A blocking
human-verify covers the build/import smoke for backends that can't be fully exercised
in this environment.
Output: `test_estimator_checks.py`, `checks_triage.md`, `test_wheels.py`,
`test_import_probe.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@AGENTS.md
@.planning/phases/06-python-surface-pyo3-estimators-per-backend-wheels/06-RESEARCH.md
@.planning/phases/06-python-surface-pyo3-estimators-per-backend-wheels/06-PATTERNS.md
@.planning/phases/06-python-surface-pyo3-estimators-per-backend-wheels/06-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: estimator_checks triage over the 12 estimators (criterion 1, relevant subset)</name>
  <read_first>
    - .planning/phases/06-python-surface-pyo3-estimators-per-backend-wheels/06-RESEARCH.md (## sklearn estimator_checks — the per-family "run these / skip these" table; sklearn >=1.6 __sklearn_tags__ note; Open Question 3 DBSCAN/NearestNeighbors predict-less)
    - .planning/phases/06-python-surface-pyo3-estimators-per-backend-wheels/06-PATTERNS.md (test_estimator_checks.py section, lines 244-248)
    - crates/mlrs-py/python/tests/test_estimator_checks.py (the Plan 01 stub)
    - crates/mlrs-py/python/mlrs/base.py (the __sklearn_tags__ override from Plan 04 the checks read)
  </read_first>
  <action>Convert `test_estimator_checks.py` into a real triage using `sklearn.utils.estimator_checks.parametrize_with_checks([...all 12 instances...])`. Run it, then for each check that fails ONLY because mlrs intentionally does not support it (sparse, array-api, NaN/allow_nan, DBSCAN/NearestNeighbors predict-based checks per RESEARCH Open Q3), wrap with an `expected_failures`/skip mechanism (per-(estimator, check) `pytest.mark.xfail(strict=False)` or the `expected_failed_checks` dict that `parametrize_with_checks` accepts in sklearn >=1.6) carrying a documented reason string. Checks that fail for a REAL bug must be fixed in the Plan-04 shim or the Plan-03 wrapper (loop back), NOT skipped. Record the final per-estimator relevant/skipped result in `crates/mlrs-py/python/tests/checks_triage.md` (one row per estimator: which check groups pass, which are skipped + why). Do NOT promise "all checks pass" — criterion 1 says "relevant". Tests stay in the pytest tree (AGENTS.md §2).</action>
  <verify>
    <automated>maturin develop -m crates/mlrs-py/pyproject/cpu.pyproject.toml >/dev/null 2>&1; python3 -m pytest crates/mlrs-py/python/tests/test_estimator_checks.py -q 2>&1 | grep -Eq '[0-9]+ passed' && test -f crates/mlrs-py/python/tests/checks_triage.md && ! python3 -m pytest crates/mlrs-py/python/tests/test_estimator_checks.py -q 2>&1 | grep -qE '[1-9][0-9]* failed'</automated>
  </verify>
  <acceptance_criteria>
    - `pytest test_estimator_checks.py` exits with 0 unexpected failures (relevant checks pass; unsupported checks xfail/skip with reason).
    - `checks_triage.md` records, per estimator, the relevant checks that pass and the skipped checks + reason (empirical Wave-0 triage result).
    - DBSCAN and NearestNeighbors skip predict-based checks with the documented reason (sklearn-faithful — their sklearn counterparts have no predict).
    - No check is skipped for a real bug (any genuine failure was fixed upstream in Plan 03/04, not masked).
  </acceptance_criteria>
  <done>Each estimator passes its relevant estimator_checks subset; intentional gaps are documented in checks_triage.md (criterion 1 satisfied without over-promising).</done>
</task>

<task type="auto">
  <name>Task 2: Build the four wheels + assert dist names/abi3 + driver-absent ImportError (PY-04/D-08)</name>
  <read_first>
    - .planning/phases/06-python-surface-pyo3-estimators-per-backend-wheels/06-RESEARCH.md (### Pattern 1 maturin multi-dist build command; ### Pattern 4 import probe; Environment Availability — cuda compile-only, rocm runs f32)
    - crates/mlrs-py/pyproject/{cpu,wgpu,cuda,rocm}.pyproject.toml (the Plan 01 templates)
    - crates/mlrs-py/src/lib.rs (the catch_unwind probe from Plan 02 — the behavior under test)
    - crates/mlrs-py/python/tests/{test_wheels.py, test_import_probe.py} (Plan 01 stubs)
  </read_first>
  <action>Create `test_wheels.py`: build each backend wheel via `maturin build -m crates/mlrs-py/pyproject/<backend>.pyproject.toml --release` (subprocess) and assert the produced `target/wheels/*.whl` filename matches the distribution name (`mlrs_cpu-*`, `mlrs_wgpu-*`, `mlrs_cuda-*`, `mlrs_rocm-*`) AND contains the `abi3` / `cp312-abi3` tag (D-09). For cpu (and rocm where runnable) also assert the built wheel imports as `import mlrs` in a fresh subprocess. cuda is compile-only: assert the build succeeds and the wheel name/tag are right, but mark the cuda IMPORT smoke as the human-verify task below (no cuda driver here). Create `test_import_probe.py`: assert the D-08 behavior — on a backend whose driver is present, `import mlrs` succeeds; simulate/assert the absent-driver path raises `ImportError` with a message naming the backend (use the rocm wheel's f64 path or a driver-absent harness where feasible; where the driver cannot be removed, assert the probe code path exists and is reached, and defer the live absent-driver confirmation to the human-verify task). Use `mlrs.backend_supports_f64()`-style capability surface to keep assertions backend-aware.</action>
  <verify>
    <automated>cd /home/user/Documents/workspace/mlrs && /tmp/mlrs-venv/bin/maturin build -m crates/mlrs-py/pyproject/cpu.pyproject.toml --release 2>&1 | grep -Eiq 'Built wheel|Finished' && ls target/wheels/ | grep -Eq 'mlrs_cpu-.*(abi3|cp312)'</automated>
  </verify>
  <acceptance_criteria>
    - cpu wheel builds; `ls target/wheels` shows `mlrs_cpu-*` with an `abi3`/`cp312-abi3` tag; a fresh-subprocess `import mlrs` succeeds.
    - wgpu + rocm wheels build with the right dist names + abi3 tag (rocm import smoke runs f32).
    - cuda wheel BUILDS (compile-only) with name `mlrs_cuda-*` + abi3 tag; its import smoke is deferred to the human-verify task.
    - `test_import_probe.py` asserts `import mlrs` succeeds with the driver present and that the absent-driver path raises a clear `ImportError` (grep `ImportError`).
  </acceptance_criteria>
  <done>All four wheels build under distinct dist names with abi3-py312 and a constant `import mlrs`; the driver-absent ImportError path is asserted (PY-04/D-08).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify cuda wheel build + driver-absent import (untestable backends)</name>
  <what-built>The four per-backend wheels (Task 2) and the D-08 import probe. cuda is compile-only in this environment (no GPU/driver) and the live driver-absent ImportError cannot be fully exercised on every backend here.</what-built>
  <how-to-verify>
    1. Confirm `target/wheels/` contains `mlrs_cpu-*`, `mlrs_wgpu-*`, `mlrs_cuda-*`, `mlrs_rocm-*`, each with a `cp312-abi3` (or `abi3`) tag in the filename.
    2. On a machine WITH a CUDA driver (opportunistic): `pip install target/wheels/mlrs_cuda-*.whl && python -c "import mlrs; print(mlrs.__name__)"` prints `mlrs`.
    3. On a machine WITHOUT the matching driver (e.g. install `mlrs-rocm` where no ROCm/HIP runtime is present, or `mlrs-cuda` on a non-CUDA host): `python -c "import mlrs"` raises a clear `ImportError` naming the backend and how to fix it — NOT a segfault/abort (verifies the catch_unwind probe + `panic = "unwind"`).
    4. Confirm installing two backend wheels into one env overwrites the shared `mlrs` namespace (D-07 accepted; documented).
  </how-to-verify>
  <acceptance_criteria>
    - Developer confirms the four wheel names + abi3 tags.
    - Developer confirms (where hardware allows) a present-driver `import mlrs` succeeds and an absent-driver `import mlrs` raises a clean ImportError (no abort).
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe the wheel/import issues observed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| absent driver -> CPython import | the probe must fail closed with ImportError, never abort (re-asserted here) |
| two backend wheels -> shared mlrs namespace | double-install overwrites (D-07 accepted; documented, not a vuln) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-16 | DoS | Driver-absent import aborting the host process instead of raising | mitigate | `test_import_probe.py` asserts the `ImportError` path; the human-verify task confirms a clean traceback (not a segfault) on a driver-absent host (D-08; backstops T-06-05 from Plan 02). |
| T-06-17 | Spoofing/Tampering | A wheel built under the wrong distribution name or non-abi3 tag shipping to the wrong backend | mitigate | `test_wheels.py` asserts each wheel filename matches its backend dist name + carries the abi3/cp312-abi3 tag (PY-04). |
| T-06-SC | Tampering | maturin/sklearn installs in the build/test venv | accept | Dependencies were legitimacy-verified in Plan 01 (Task 1 checkpoint); no new package introduced here. |

The DoS threat (driver-absent abort) is mitigated by the automated probe assertion
plus the blocking human-verify for backends that can't be exercised in-environment.
No unmitigated `high`.
</threat_model>

<verification>
- `pytest test_estimator_checks.py` passes the relevant subset; `checks_triage.md` records the gaps.
- The four wheels build under `mlrs_{cpu,wgpu,cuda,rocm}-*` with abi3; cpu/wgpu/rocm import as `mlrs`.
- `test_import_probe.py` asserts present-driver import + absent-driver ImportError.
- Human-verify confirms cuda build + driver-absent ImportError where hardware allows.
</verification>

<success_criteria>
- Each estimator passes its relevant estimator_checks; intentional gaps documented (criterion 1 / PY-01/PY-02).
- All four backend wheels build under distinct dist names, abi3-py312, constant `import mlrs` (PY-04/D-07/D-09).
- Driver-absent import raises a clear ImportError, not a crash (PY-04/D-08).
</success_criteria>

<output>
Create `.planning/phases/06-python-surface-pyo3-estimators-per-backend-wheels/06-06-SUMMARY.md` when done.
</output>
