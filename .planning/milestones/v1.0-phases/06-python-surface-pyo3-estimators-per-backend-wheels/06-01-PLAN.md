---
phase: 06-python-surface-pyo3-estimators-per-backend-wheels
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - crates/mlrs-py/Cargo.toml
  - crates/mlrs-py/python/mlrs/__init__.py
  - crates/mlrs-py/python/mlrs/base.py
  - crates/mlrs-py/python/mlrs/_io.py
  - crates/mlrs-py/python/mlrs/linear.py
  - crates/mlrs-py/python/mlrs/cluster.py
  - crates/mlrs-py/python/mlrs/decomposition.py
  - crates/mlrs-py/python/mlrs/neighbors.py
  - crates/mlrs-py/pyproject/cpu.pyproject.toml
  - crates/mlrs-py/pyproject/wgpu.pyproject.toml
  - crates/mlrs-py/pyproject/cuda.pyproject.toml
  - crates/mlrs-py/pyproject/rocm.pyproject.toml
  - crates/mlrs-py/python/tests/conftest.py
  - crates/mlrs-py/python/tests/test_oracle_linear.py
  - crates/mlrs-py/python/tests/test_oracle_cluster.py
  - crates/mlrs-py/python/tests/test_oracle_decomposition.py
  - crates/mlrs-py/python/tests/test_oracle_neighbors.py
  - crates/mlrs-py/python/tests/test_estimator_checks.py
  - crates/mlrs-py/python/tests/test_dtype.py
  - crates/mlrs-py/python/tests/test_params.py
  - crates/mlrs-py/src/arrow_symbol_probe.rs
autonomous: false
requirements: [PY-01, PY-02, PY-03, PY-04, PY-05]
user_setup:
  - service: python-build-toolchain
    why: "maturin/pyarrow/scikit-learn/numpy/pytest are not installed; PEP 668 blocks system pip"
    env_vars: []
    dashboard_config:
      - task: "Create a venv and install build/test deps: python3 -m venv /tmp/mlrs-venv && /tmp/mlrs-venv/bin/pip install 'maturin>=1.14,<2' 'pyarrow>=14' 'scikit-learn>=1.6' 'numpy>=1.26' pytest"
        location: "local shell (PEP 668 requires a venv; mirrors the gen_oracle /tmp venv pattern)"

must_haves:
  truths:
    - "The workspace pins pyo3 = 0.28 (NOT 0.29) with an explicit ABI-pin comment (D-09/PY-05)"
    - "cargo tree -i pyo3 resolves exactly ONE pyo3 version (no 0.28+0.29 skew)"
    - "Four per-backend pyproject templates exist, differing only in [project].name and [tool.maturin].features, all with module-name = mlrs._mlrs and python-source pointing at the pure-Python package (D-07/D-09)"
    - "The pure-Python mlrs package skeleton imports and exposes the 12 estimator class names (D-01)"
    - "A pytest tree exists with conftest + per-family stubs that COLLECT (xfail/skip until wrappers land), enforcing the Nyquist scaffold"
    - "The exact arrow-59 FromPyArrow entry point (from_pyarrow_bound vs from_pyarrow) is confirmed and recorded for Plan 02"
  artifacts:
    - path: "Cargo.toml"
      provides: "pyo3 0.28 workspace pin with ABI-pin comment"
      contains: "pyo3"
    - path: "crates/mlrs-py/Cargo.toml"
      provides: "pyo3 abi3-py312 + extension-module, arrow pyarrow, backend feature block"
      contains: "abi3-py312"
    - path: "crates/mlrs-py/pyproject/cpu.pyproject.toml"
      provides: "mlrs-cpu distribution template (module-name = mlrs._mlrs)"
      contains: "mlrs-cpu"
    - path: "crates/mlrs-py/python/mlrs/__init__.py"
      provides: "pure-Python package entry that re-exports the 12 estimators"
      contains: "KMeans"
    - path: "crates/mlrs-py/python/tests/conftest.py"
      provides: "fixture loader + sign-flip/label-perm helpers + capability skip marker"
      min_lines: 30
  key_links:
    - from: "crates/mlrs-py/pyproject/cpu.pyproject.toml"
      to: "crates/mlrs-py/python/mlrs"
      via: "python-source"
      pattern: "python-source"
    - from: "crates/mlrs-py/Cargo.toml"
      to: "Cargo.toml [workspace.dependencies] pyo3"
      via: "workspace = true"
      pattern: "pyo3.*workspace"
---

<objective>
Lay the Wave-0 scaffold for the entire phase: pin the binding toolchain (pyo3 0.28
ABI lock), create the four per-backend maturin pyproject templates, stand up the
pure-Python `mlrs` package skeleton (D-01), and create the pytest test tree with
collecting stubs (the Nyquist scaffold). Also resolve the one open research unknown
that blocks Plan 02 — the exact arrow-59 `FromPyArrow` method name — via a tiny
compile probe.

Purpose: Every later plan (02 plumbing, 03 wrappers, 04 shim, 05/06 tests+wheels)
depends on these files existing with the correct version pins and layout. Getting
the pyo3 0.28 pin wrong (linking two PyO3 ABIs) breaks every downstream wheel.
Output: workspace + crate Cargo manifests, 4 pyproject templates, the `mlrs/`
Python package skeleton, the `python/tests/` pytest scaffold, and a recorded
`FromPyArrow` symbol for Plan 02.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@AGENTS.md
@.planning/phases/06-python-surface-pyo3-estimators-per-backend-wheels/06-CONTEXT.md
@.planning/phases/06-python-surface-pyo3-estimators-per-backend-wheels/06-RESEARCH.md
@.planning/phases/06-python-surface-pyo3-estimators-per-backend-wheels/06-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This phase (across all six plans) creates these NEW symbols/files — the source-grounding
pass must EXCLUDE these from drift verification (they do not exist yet at plan time):

- **Workspace dep:** `pyo3 = { version = "0.28", default-features = false }` in `Cargo.toml [workspace.dependencies]`.
- **`#[pymodule]` name:** `_mlrs` (compiled extension; import path `mlrs._mlrs`).
- **Rust binding modules (Plan 02):** `crates/mlrs-py/src/{ingress.rs, egress.rs, capability.rs, dispatch.rs}` + `_mlrs` in `lib.rs`; the process-global `OnceLock<Mutex<BufferPool<ActiveRuntime>>>`; `mlrs.backend_supports_f64()` capability flag fn.
- **Per-estimator dispatch enums (Plan 02/03):** `AnyLinearRegression`, `AnyRidge`, `AnyLasso`, `AnyElasticNet`, `AnyLogisticRegression`, `AnyKMeans`, `AnyDBSCAN`, `AnyPCA`, `AnyTruncatedSVD`, `AnyNearestNeighbors`, `AnyKNeighborsClassifier`, `AnyKNeighborsRegressor` — each `{ Unfit{..}, F32(Estimator<f32>), F64(Estimator<f64>) }` (D-06), generated by a `macro_rules! any_estimator!`.
- **`#[pyclass]` wrappers (Plan 03):** `crates/mlrs-py/src/estimators/*.rs` — 12 pyclasses (`PyLinearRegression`, `PyRidge`, `PyLasso`, `PyElasticNet`, `PyLogisticRegression`, `PyKMeans`, `PyDBSCAN`, `PyPCA`, `PyTruncatedSVD`, `PyNearestNeighbors`, `PyKNeighborsClassifier`, `PyKNeighborsRegressor`).
- **Pure-Python package (Plan 01 skeleton, Plan 04 logic):** `crates/mlrs-py/python/mlrs/{__init__.py, base.py, _io.py, linear.py, cluster.py, decomposition.py, neighbors.py}` exposing the 12 sklearn-compatible classes (same names, no `Py` prefix).
- **Per-backend pyproject names:** distributions `mlrs-cpu` / `mlrs-wgpu` / `mlrs-cuda` / `mlrs-rocm` (`crates/mlrs-py/pyproject/{cpu,wgpu,cuda,rocm}.pyproject.toml`).
- **Test files:** `crates/mlrs-py/python/tests/{conftest.py, test_oracle_linear.py, test_oracle_cluster.py, test_oracle_decomposition.py, test_oracle_neighbors.py, test_estimator_checks.py, test_dtype.py, test_params.py}` + Rust `crates/mlrs-py/tests/*.rs` (Plan 02+).

**The 12 estimators (named, not abstract):** LinearRegression, Ridge, Lasso, ElasticNet,
LogisticRegression (`crates/mlrs-algos/src/linear/`); KMeans, DBSCAN (`.../cluster/`);
PCA, TruncatedSVD (`.../decomposition/`); NearestNeighbors, KNeighborsClassifier,
KNeighborsRegressor (`.../neighbors/`).
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Confirm Python runtime-dep floor versions (package-legitimacy gate)</name>
  <read_first>
    - .planning/phases/06-python-surface-pyo3-estimators-per-backend-wheels/06-RESEARCH.md (## Package Legitimacy Audit — pyarrow/numpy floors are [ASSUMED])
    - .planning/phases/06-python-surface-pyo3-estimators-per-backend-wheels/06-RESEARCH.md (## Standard Stack — Supporting deps)
  </read_first>
  <decision>Pin the floor versions for the three Python runtime dependencies that the RESEARCH audit marked [ASSUMED]: pyarrow, numpy, scikit-learn (scikit-learn is OK-verified ≥1.6; pyarrow/numpy floors are assumed).</decision>
  <context>The maturin pyproject templates declare `dependencies = ["numpy>=?", "pyarrow>=?", "scikit-learn>=1.6"]`. RESEARCH proposes numpy>=1.26 / pyarrow>=14 (the capsule protocol floor) / scikit-learn>=1.6 (the __sklearn_tags__ floor). The legitimacy gate forbids auto-approving [ASSUMED] floors. Verify each against pypi.org/project/{pyarrow,numpy,scikit-learn} and approve or amend.</context>
  <options>
    <option id="research-defaults">
      <name>Accept RESEARCH floors: numpy>=1.26, pyarrow>=14, scikit-learn>=1.6</name>
      <pros>pyarrow>=14 is the documented PyCapsule (__arrow_c_array__) floor; numpy>=1.26 matches the cuML baseline; sklearn>=1.6 is the __sklearn_tags__ public-API floor.</pros>
      <cons>pyarrow/numpy floors are [ASSUMED] in research — confirm on pypi before locking.</cons>
    </option>
    <option id="custom">
      <name>Amend one or more floors after checking pypi</name>
      <pros>Lets the developer raise a floor if a needed API arrived later.</pros>
      <cons>Requires the developer to state the replacement values.</cons>
    </option>
  </options>
  <acceptance_criteria>
    - The developer states the three approved floor strings (e.g. "numpy>=1.26, pyarrow>=14, scikit-learn>=1.6").
    - These exact strings are used verbatim in all four pyproject templates' `[project].dependencies` in Task 3.
  </acceptance_criteria>
  <resume-signal>State the three approved floor strings (or "approved" to accept the RESEARCH defaults).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Pin pyo3 0.28 (workspace + crate) and wire backend features</name>
  <read_first>
    - Cargo.toml (workspace [workspace.dependencies] — existing arrow="59", cubecl default-features=false comment style at lines 13-39)
    - crates/mlrs-py/Cargo.toml (current: only anyhow + mimalloc; crate-type cdylib+rlib at lines 1-13)
    - .planning/phases/06-python-surface-pyo3-estimators-per-backend-wheels/06-RESEARCH.md (## Standard Stack → Installation block; ## Common Pitfalls → Pitfall 1)
    - .planning/phases/06-python-surface-pyo3-estimators-per-backend-wheels/06-PATTERNS.md (crates/mlrs-py/Cargo.toml section, lines 154-173)
  </read_first>
  <action>Add to the workspace `[workspace.dependencies]` (in `Cargo.toml`): `pyo3 = { version = "0.28", default-features = false }` with an inline comment mirroring the existing cubecl rationale comment style — state that pyo3 is PINNED to 0.28 (NOT the latest 0.29) because arrow-59's `pyarrow` feature transitively pins pyo3 0.28.x and only one pyo3 ABI may link into the cdylib; this overrides the "track latest" workspace policy for this dep only. In `crates/mlrs-py/Cargo.toml` add: `pyo3 = { workspace = true, features = ["abi3-py312", "extension-module"] }`; `arrow = { workspace = true, features = ["pyarrow"] }`; path deps `mlrs-algos = { path = "../mlrs-algos" }` and `mlrs-backend = { path = "../mlrs-backend" }`. Add a `[features]` block with `cpu = ["mlrs-backend/cpu", "mlrs-algos/cpu"]` and identical `wgpu`/`cuda`/`rocm` arms forwarding to the same sub-crate features. Do NOT add the `numpy` Rust crate (it would drag a second pyo3). Keep `crate-type = ["cdylib", "rlib"]` and the existing anyhow/mimalloc deps unchanged. Do NOT add `panic = "abort"` anywhere (the D-08 probe needs unwind).</action>
  <verify>
    <automated>cargo tree -i pyo3 2>/dev/null | grep -c 'pyo3 v0.29' | grep -qx 0 && cargo tree -p mlrs-py --features cpu -i pyo3 2>/dev/null | grep -q 'pyo3 v0.28'</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'pyo3' Cargo.toml` shows `version = "0.28"` plus an ABI-pin comment.
    - `grep 'abi3-py312' crates/mlrs-py/Cargo.toml` matches.
    - `cargo tree -p mlrs-py --features cpu -i pyo3` reports exactly one pyo3 version, 0.28.x (no 0.29).
    - `cargo build -p mlrs-py --features cpu` compiles (the crate still only has lib.rs + allocator.rs at this point).
  </acceptance_criteria>
  <done>pyo3 0.28 is the single linked ABI; crate features cpu/wgpu/cuda/rocm forward to the sub-crates; the workspace comment documents the override.</done>
</task>

<task type="auto">
  <name>Task 3: Create the four per-backend maturin pyproject templates</name>
  <read_first>
    - .planning/phases/06-python-surface-pyo3-estimators-per-backend-wheels/06-RESEARCH.md (### Pattern 1: Maturin Multi-Distribution — the full template + module-name/python-source/[project].name precedence)
    - .planning/phases/06-python-surface-pyo3-estimators-per-backend-wheels/06-CONTEXT.md (D-07 constant import mlrs / distinct dist names; D-09 abi3-py312)
  </read_first>
  <action>Create `crates/mlrs-py/pyproject/{cpu,wgpu,cuda,rocm}.pyproject.toml`. All four are IDENTICAL except `[project].name` (`mlrs-cpu`/`mlrs-wgpu`/`mlrs-cuda`/`mlrs-rocm`) and `[tool.maturin].features` (`["cpu"]`/`["wgpu"]`/`["cuda"]`/`["rocm"]`). Each file: `[build-system]` requires `maturin>=1.14,<2`, build-backend `maturin`; `[project]` with `requires-python = ">=3.12"`, `dynamic = ["version"]`, and `dependencies` = the three floor strings approved in Task 1; `[tool.maturin]` with `manifest-path = "crates/mlrs-py/Cargo.toml"`, `module-name = "mlrs._mlrs"` (constant across all four — this is what makes every wheel `import mlrs`), `python-source = "crates/mlrs-py/python"`, and the per-backend `features`. Path values are relative to the repo root (where maturin is invoked with `-m`).</action>
  <verify>
    <automated>test -f crates/mlrs-py/pyproject/cpu.pyproject.toml && test -f crates/mlrs-py/pyproject/wgpu.pyproject.toml && test -f crates/mlrs-py/pyproject/cuda.pyproject.toml && test -f crates/mlrs-py/pyproject/rocm.pyproject.toml && grep -l 'module-name = "mlrs._mlrs"' crates/mlrs-py/pyproject/*.pyproject.toml | wc -l | grep -qx 4</automated>
  </verify>
  <acceptance_criteria>
    - All four files exist; `grep -c 'mlrs._mlrs' crates/mlrs-py/pyproject/*.pyproject.toml` shows the constant module-name in each.
    - `grep 'name = ' crates/mlrs-py/pyproject/cpu.pyproject.toml` shows `mlrs-cpu`; the other three show their respective dist names.
    - Each `[tool.maturin].features` lists exactly its own backend.
  </acceptance_criteria>
  <done>One templated pyproject per backend; constant `module-name = "mlrs._mlrs"` + `python-source` everywhere; only dist name and feature differ.</done>
</task>

<task type="auto">
  <name>Task 4: Stand up the pure-Python mlrs package skeleton</name>
  <read_first>
    - .planning/phases/06-python-surface-pyo3-estimators-per-backend-wheels/06-RESEARCH.md (### Recommended Project Structure — the python/mlrs/ layout; ## Hyperparameter Mapping — the 12 class names + sklearn ctor params)
    - .planning/phases/06-python-surface-pyo3-estimators-per-backend-wheels/06-PATTERNS.md (python/mlrs/base.py, __init__.py sections — cuML method ref; __init__ purity rule)
    - crates/mlrs-algos/src/lib.rs (re-export structure ref, lines 31-44)
  </read_first>
  <action>Create the `crates/mlrs-py/python/mlrs/` package skeleton (logic is filled in Plan 04; this task is the importable shell). `__init__.py`: at top, `from . import _mlrs` is NOT yet possible (extension built later) — guard the extension import in a try/except that re-raises a clear message, then re-export the 12 estimator classes from the family modules. Create `base.py` with a `MlrsBase(sklearn.base.BaseEstimator)` stub (docstring noting Plan 04 fills output_type routing). Create `_io.py` stub (docstring: numpy/list→pyarrow normalize + output_type, Plan 04). Create `linear.py` (LinearRegression, Ridge, Lasso, ElasticNet, LogisticRegression), `cluster.py` (KMeans, DBSCAN), `decomposition.py` (PCA, TruncatedSVD), `neighbors.py` (NearestNeighbors, KNeighborsClassifier, KNeighborsRegressor) — each estimator a class subclassing `MlrsBase` with a sklearn-faithful `__init__` that stores every ctor arg verbatim under the SAME name (`self.C = C`, `self.n_clusters = n_clusters`, etc., per the RESEARCH Hyperparameter Mapping table) and a `NotImplementedError`-raising `fit` placeholder. NO param transformation in `__init__` (purity rule). These compile/import as pure Python without the extension built (the extension import is the part guarded).</action>
  <verify>
    <automated>test -f crates/mlrs-py/python/mlrs/__init__.py && python3 -c "import ast,glob; [ast.parse(open(f).read()) for f in glob.glob('crates/mlrs-py/python/mlrs/*.py')]; print('parse-ok')" | grep -qx parse-ok</automated>
  </verify>
  <acceptance_criteria>
    - All seven `.py` files exist and parse (`python3 -c "import ast..."` succeeds — no venv needed for AST parse).
    - `grep -c 'class ' crates/mlrs-py/python/mlrs/linear.py` ≥ 5; cluster ≥ 2; decomposition ≥ 2; neighbors ≥ 3.
    - Each estimator `__init__` stores args verbatim (e.g. `grep 'self.C = C' crates/mlrs-py/python/mlrs/linear.py` matches for LogisticRegression).
  </acceptance_criteria>
  <done>The 12 estimator classes exist as importable pure-Python shells with faithful __init__ signatures; the extension import is guarded for the not-yet-built case.</done>
</task>

<task type="auto">
  <name>Task 5: Create the pytest scaffold (conftest + collecting stubs) and confirm the arrow FromPyArrow symbol</name>
  <read_first>
    - .planning/phases/06-python-surface-pyo3-estimators-per-backend-wheels/06-RESEARCH.md (## pytest Oracle Harness — conftest loader + sign-flip/label-perm + capability skip; ## Open Questions Q2 — FromPyArrow method name; ## Validation Architecture → Phase Requirements → Test Map)
    - .planning/phases/06-python-surface-pyo3-estimators-per-backend-wheels/06-VALIDATION.md (Wave 0 Requirements; Per-Task Verification Map)
    - .planning/phases/06-python-surface-pyo3-estimators-per-backend-wheels/06-PATTERNS.md (conftest/test_oracle analogs — gen_oracle.py + kmeans_test.rs compare structure)
    - scripts/gen_oracle.py (fixture format; .npz key names)
    - AGENTS.md (§2 source/test separation — Python tests live OUTSIDE source)
  </read_first>
  <action>Create the `crates/mlrs-py/python/tests/` pytest tree. `conftest.py`: a `numpy.load`-based loader for the committed `tests/fixtures/*.npz` blobs (path is repo-root `tests/fixtures/`), a `sign_flip_allclose(a, b, atol=1e-5)` helper (PCA/SVD components_ column-sign invariance), a `label_perm_allclose(a, b)` helper (KMeans/DBSCAN label-permutation invariance), and a `requires_f64` pytest skip marker keyed on `mlrs.backend_supports_f64()` (import-guarded — skip the whole module if `mlrs` is not importable yet). Create `test_oracle_{linear,cluster,decomposition,neighbors}.py`, `test_estimator_checks.py`, `test_dtype.py`, `test_params.py` as COLLECTING stubs: each maps to its PY-requirement (comment the Req ID), parametrizes over the relevant committed fixtures, and is marked `pytest.mark.xfail(reason="wrappers land in Plan 03/04", strict=False)` or `pytest.importorskip("mlrs")` so the suite COLLECTS green now and converts to real assertions in Plans 04-06. Separately, create `crates/mlrs-py/src/arrow_symbol_probe.rs` as a NON-compiled doc-probe: run `cargo doc -p arrow --features pyarrow --no-deps 2>/dev/null` then grep the generated docs (or `cargo tree`/source) to confirm whether arrow-59 exposes `ArrayData::from_pyarrow_bound` or `from_pyarrow`; record the confirmed symbol name as a top-of-file comment in `arrow_symbol_probe.rs` for Plan 02 to consume (this file is documentation only — add `#![allow(dead_code)]`-style note; do NOT add it to lib.rs `mod` list).</action>
  <verify>
    <automated>test -f crates/mlrs-py/python/tests/conftest.py && python3 -c "import ast,glob; [ast.parse(open(f).read()) for f in glob.glob('crates/mlrs-py/python/tests/*.py')]; print('ok')" | grep -qx ok && grep -Eq 'from_pyarrow(_bound)?' crates/mlrs-py/src/arrow_symbol_probe.rs</automated>
  </verify>
  <acceptance_criteria>
    - `conftest.py` + the 7 test stubs exist and AST-parse.
    - `conftest.py` defines `sign_flip_allclose`, `label_perm_allclose`, and a f64-capability skip marker (grep each).
    - Each test stub names its PY-0x requirement in a comment and either `importorskip("mlrs")` or `xfail`s (collects green pre-wrapper).
    - `crates/mlrs-py/src/arrow_symbol_probe.rs` records the confirmed arrow-59 FromPyArrow method name in a comment (matched by the grep).
    - After `maturin develop` is available, `pytest crates/mlrs-py/python/tests --collect-only` succeeds (deferred to Plan 05 when the venv exists; collection-parse is proven now via AST).
  </acceptance_criteria>
  <done>The Nyquist test scaffold collects green; the arrow FromPyArrow symbol is pinned for Plan 02; no in-source #[cfg(test)] module was used (AGENTS.md §2).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build toolchain → linked cdylib | A wrong pyo3 version pin links two ABIs into one extension → import-time crash |
| pyproject template → installed wheel namespace | Two backend wheels share the `mlrs` namespace (D-07 accepted); double-install overwrites |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | pyo3 ABI skew (0.28 vs 0.29) in the cdylib link | mitigate | Task 2 pins pyo3 0.28 in `[workspace.dependencies]`; the verify asserts `cargo tree -i pyo3` resolves a single 0.28 version (RESEARCH Pitfall 1). |
| T-06-02 | Tampering | [ASSUMED] Python dep floors (pyarrow/numpy) | mitigate | Task 1 blocking-human checkpoint confirms floors against pypi before they land in the templates (package-legitimacy gate). |
| T-06-SC | Tampering | maturin/pyarrow/sklearn installs into the venv | mitigate | Task 1 legitimacy checkpoint (blocking-human, non-auto-approvable); deps verified on pypi.org/crates.io per the RESEARCH audit (all OK-verdict). |

No `high`-severity unmitigated threats. The ABI-skew threat (the only crash-class risk here) is gated by the Task 2 verify.
</threat_model>

<verification>
- `cargo tree -p mlrs-py --features cpu -i pyo3` → single pyo3 0.28.x.
- `cargo build -p mlrs-py --features cpu` compiles.
- All four pyproject templates exist with constant `module-name = "mlrs._mlrs"`.
- The 12 Python estimator shells + 8 test files AST-parse.
- The arrow FromPyArrow symbol is recorded for Plan 02.
</verification>

<success_criteria>
- pyo3 0.28 is the single linked ABI (no 0.29 anywhere in the tree).
- Four per-backend pyproject templates produce a constant `import mlrs`.
- The pure-Python package skeleton exposes all 12 estimators with faithful `__init__`s.
- The pytest scaffold collects green (Nyquist); the arrow symbol unknown is resolved.
</success_criteria>

<output>
Create `.planning/phases/06-python-surface-pyo3-estimators-per-backend-wheels/06-01-SUMMARY.md` when done.
</output>
