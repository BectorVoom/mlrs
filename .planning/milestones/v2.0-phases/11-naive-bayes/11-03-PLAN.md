---
phase: 11-naive-bayes
plan: 03
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - crates/mlrs-algos/src/naive_bayes/multinomial_nb.rs
  - crates/mlrs-algos/src/naive_bayes/bernoulli_nb.rs
  - crates/mlrs-algos/src/naive_bayes/complement_nb.rs
  - crates/mlrs-algos/tests/multinomial_nb_test.rs
  - crates/mlrs-algos/tests/bernoulli_nb_test.rs
  - crates/mlrs-algos/tests/complement_nb_test.rs
autonomous: true
requirements: [NB-02, NB-03, NB-04]
must_haves:
  truths:
    - "A user can fit MultinomialNB, BernoulliNB, ComplementNB with predict/predict_proba/predict_log_proba matching sklearn"
    - "Exact predict labels match sklearn EXACTLY (hard gate) for all three on cpu f64 + f32"
    - "Each variant uses its correct smoothing denominator (MNB alpha·n_features, BNB 2·alpha)"
    - "BernoulliNB includes the (1−x)·log(1−p) non-occurrence term and the binarize Option<f64> path"
    - "ComplementNB uses complement counts + optional L1 norm + argmin decision"
    - "All three stay separate structs (D-03 — no shared base), sharing only the GEMM joint-LL + nb_common helpers"
    - "build() rejects alpha<0; force_alpha=False & alpha<1e-10 clips to 1e-10 + warns (D-06)"
    - "predict_proba rows sum to 1; re-fit leak-free"
  artifacts:
    - path: "crates/mlrs-algos/src/naive_bayes/multinomial_nb.rs"
      provides: "MultinomialNB Fit + Predict surface via GEMM joint-LL"
      contains: "gemm"
    - path: "crates/mlrs-algos/src/naive_bayes/bernoulli_nb.rs"
      provides: "BernoulliNB with non-occurrence term + binarize"
      contains: "binarize"
    - path: "crates/mlrs-algos/src/naive_bayes/complement_nb.rs"
      provides: "ComplementNB complement weights + argmin"
      contains: "argmin_decode"
  key_links:
    - from: "crates/mlrs-algos/src/naive_bayes/multinomial_nb.rs"
      to: "mlrs_backend::prims::gemm"
      via: "X @ feature_log_prob_.T joint-LL matvec (transb=true)"
      pattern: "gemm"
    - from: "crates/mlrs-algos/src/naive_bayes/complement_nb.rs"
      to: "nb_common::argmin_decode"
      via: "CNB argmin decision (D-08)"
      pattern: "argmin_decode"
---

<objective>
Fill the three count-based NB variants (NB-02 MultinomialNB, NB-03 BernoulliNB,
NB-04 ComplementNB). They share the GEMM joint-LL path (`class_log_prior_ +
X @ feature_log_prob_.T`, the mbsgd decision-margin generalization, A1 confirmed:
gemm transb=true) and the host log-sum-exp, but stay THREE SEPARATE structs
(D-03 — no shared base). Each implements its own per-variant smoothing denominator
and decision rule verbatim from FEATURES.md.

Purpose: Deliver the discrete count variants; grouped in one plan because they share
the exact device path, kept structurally independent per D-03.
Output: Three estimators + three green oracles; exact labels the hard gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-naive-bayes/11-RESEARCH.md
@.planning/phases/11-naive-bayes/11-PATTERNS.md
@.planning/research/FEATURES.md
@.planning/phases/11-naive-bayes/11-01-SUMMARY.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MultinomialNB fit + GEMM joint-LL predict + oracle (NB-02)</name>
  <read_first>
    - crates/mlrs-algos/src/naive_bayes/multinomial_nb.rs (Wave-0 stub: builder/build/force_alpha/Fit-guard)
    - crates/mlrs-algos/src/naive_bayes/nb_common.rs (class_grouped_sum, log_sum_exp_normalize, empirical_class_log_prior, argmax_decode)
    - crates/mlrs-algos/src/linear/mbsgd_classifier.rs lines 506-523 (gemm matvec decision_margin — generalize to n_classes columns, transb=true)
    - crates/mlrs-backend/src/prims/gemm.rs lines 54-80 (transb=true reads B as (n_features,n_classes).T)
    - .planning/research/FEATURES.md §MultinomialNB (feature_log_prob_[c,j]=log((count+alpha)/(Σ_j count+alpha·n_features)); joint LL = class_log_prior_ + X@flp.T)
    - 11-RESEARCH.md Pitfall 4 (denominator alpha·n_features, NOT alpha·1), §"Pattern 2"
    - crates/mlrs-algos/tests/multinomial_nb_test.rs (Wave-0 scaffolds)
  </read_first>
  <behavior>
    - fit: feature_count_[c,j] = Σ over class-c rows of x[i,j] (via class_grouped_sum); feature_log_prob_[c,j] = log((feature_count_[c,j]+alpha)/(Σ_j feature_count_[c,j]+alpha·n_features)).
    - predict joint LL[i,c] = class_log_prior_[c] + (X @ feature_log_prob_.T)[i,c] via gemm transb=true.
    - exact predict labels == sklearn; predict_proba rows sum to 1.0±1e-6.
  </behavior>
  <action>
    Implement MultinomialNB::fit: classes_ sort/dedup → feature_count_ via class_grouped_sum →
    feature_log_prob_ with denominator `Σ_j count[c,j] + alpha·n_features` (Pitfall 4) → class_log_prior_
    (empirical when fit_prior=true & class_prior=None, else supplied/uniform). Upload feature_log_prob_
    as an (n_classes, n_features) device buffer for the GEMM. Implement PredictLabels/PredictProba/
    PredictLogProba: gemm(X (n_query,n_features), flp (n_features,n_classes), transa=false, transb=true)
    → raw n_query×n_classes → host-add class_log_prior_[c] bias → nb_common::log_sum_exp_normalize +
    argmax_decode. release_into(pool) the GEMM output + prior fitted on re-fit (WR-07). Un-#[ignore] and
    fill multinomial_nb_test.rs: exact_labels (f64 skip + f32), proba_band (rows sum to 1),
    default_matches_sklearn, build_rejects_bad_alpha, force_alpha_clip (force_alpha=false & alpha=1e-12
    → clipped to 1e-10), refit_releases_buffers.
  </action>
  <verify>
    <automated>cargo test --features cpu -p mlrs-algos --test multinomial_nb_test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "gemm" crates/mlrs-algos/src/naive_bayes/multinomial_nb.rs` returns a match
    - `grep -c "SharedMemory\|F::INFINITY\|Atomic" crates/mlrs-algos/src/naive_bayes/multinomial_nb.rs` (after `grep -v '^//'`) == 0
    - `cargo test --features cpu -p mlrs-algos --test multinomial_nb_test` exits 0
    - exact_labels asserts integer equality with no band; proba rows sum to 1.0±1e-6
  </acceptance_criteria>
  <done>MultinomialNB green; alpha·n_features denominator; GEMM joint-LL; force_alpha clip+warn.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: BernoulliNB fit (binarize + non-occurrence term) + oracle (NB-03)</name>
  <read_first>
    - crates/mlrs-algos/src/naive_bayes/bernoulli_nb.rs (Wave-0 stub incl. binarize: Option<f64>)
    - crates/mlrs-algos/src/density/kernel_density.rs lines 108-119 (Option/enum knob precedent for binarize)
    - .planning/research/FEATURES.md §BernoulliNB (binarize=0.0 → x>binarize→1, None skips; feature_log_prob_[c,j]=log((count+alpha)/(class_count[c]+2·alpha)); LL = class_log_prior + Σ_j[x_j·log p + (1−x_j)·log(1−p)])
    - 11-RESEARCH.md Pitfall 5 (the (1−x)·log(1−p) non-occurrence term; fold into GEMM via flp=log p − log(1−p) + per-class const Σ_j log(1−p_cj))
    - crates/mlrs-algos/src/naive_bayes/multinomial_nb.rs (Task 1 — the GEMM joint-LL path to adapt)
  </read_first>
  <behavior>
    - binarize=Some(t): x>t → 1 else 0 applied at fit AND predict ingress; binarize=None: assume already binary, no thresholding.
    - feature_log_prob_[c,j] = log((count+alpha)/(class_count[c]+2·alpha)) (denominator 2·alpha, Pitfall 4).
    - predict LL folds the non-occurrence term: flp_gemm = log p − log(1−p), plus per-class constant Σ_j log(1−p_cj) + class_log_prior_.
    - exact predict labels == sklearn; proba rows sum to 1.
  </behavior>
  <action>
    Implement BernoulliNB::fit: apply binarize (Option<f64>, D-04: None → assume binary, Some(t) → x>t)
    to a host/device copy of X → feature_count_ via class_grouped_sum over binarized X → feature_log_prob_
    with denominator `class_count[c] + 2·alpha` (Pitfall 4). Implement the predict joint-LL with the
    explicit non-occurrence term (Pitfall 5): set the GEMM operand to `flp = log p − log(1−p)` and add the
    per-class constant `Σ_j log(1−p_cj)` (sklearn neg_prob) plus class_log_prior_ as the bias; binarize the
    query X the same way before the GEMM. nb_common::log_sum_exp_normalize + argmax_decode. Un-#[ignore]
    and fill bernoulli_nb_test.rs: exact_labels (f64 skip + f32), binarize_none (assume-binary path),
    proba_band (rows sum to 1), default_matches_sklearn (binarize=0.0, alpha=1.0), build_rejects_bad_alpha,
    refit_releases_buffers.
  </action>
  <verify>
    <automated>cargo test --features cpu -p mlrs-algos --test bernoulli_nb_test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "binarize" crates/mlrs-algos/src/naive_bayes/bernoulli_nb.rs` returns a match
    - `grep -c "SharedMemory\|F::INFINITY\|Atomic" crates/mlrs-algos/src/naive_bayes/bernoulli_nb.rs` (after `grep -v '^//'`) == 0
    - `cargo test --features cpu -p mlrs-algos --test bernoulli_nb_test` exits 0 (incl. binarize_none)
    - exact_labels integer equality, no band; proba rows sum to 1.0±1e-6
  </acceptance_criteria>
  <done>BernoulliNB green; non-occurrence term + binarize Option path; 2·alpha denominator.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ComplementNB fit (complement weights, norm, argmin) + oracle (NB-04)</name>
  <read_first>
    - crates/mlrs-algos/src/naive_bayes/complement_nb.rs (Wave-0 stub incl. norm: bool)
    - crates/mlrs-algos/src/naive_bayes/nb_common.rs (argmin_decode — D-08)
    - .planning/research/FEATURES.md §ComplementNB (weights = log((complement_count+alpha)/(complement_count.sum()+alpha·n_features)); complement_count[c,j]=Σ_{c'≠c} feature_count[c',j]; norm=False default → if True L1-normalize weights; decision = argmin of X@weights)
    - 11-RESEARCH.md Pitfall 6 (complement counts + optional L1 norm + argmin sign — do NOT copy MNB)
    - crates/mlrs-algos/src/naive_bayes/multinomial_nb.rs (Task 1 — the GEMM joint-LL path; CNB differs in weights + argmin)
  </read_first>
  <behavior>
    - feature_count_ via class_grouped_sum; feature_all_ = feature_count_.sum(axis=0); complement_count[c,j] = feature_all_[j] − feature_count_[c,j].
    - weights[c,j] = log((complement_count[c,j]+alpha)/(Σ_j complement_count[c,j]+alpha·n_features)); if norm=True L1-normalize each class's weight row.
    - decision = argmin of X@weights (note the sign, D-08 internal); predict_proba via log-sum-exp over −(X@weights).
    - exact predict labels == sklearn.
  </behavior>
  <action>
    Implement ComplementNB::fit: feature_count_ via class_grouped_sum → feature_all_ (column totals across
    all classes) → complement_count[c,j] = feature_all_[j] − feature_count_[c,j] → weights with the
    complement denominator (Pitfall 6); if norm=true L1-normalize each weight row. Implement the predict
    path reusing the GEMM (X @ weights.T) but decoding with nb_common::argmin_decode (D-08 — internal to
    CNB's PredictLabels, NOT a trait special-case). predict_proba/predict_log_proba normalize the joint LL
    consistently with sklearn's CNB (use the same sign convention sklearn uses for predict_proba). Un-#[ignore]
    and fill complement_nb_test.rs: exact_labels (f64 skip + f32, argmin), norm_true (L1-normalized weights),
    proba_band (rows sum to 1), default_matches_sklearn (norm=False, alpha=1.0), build_rejects_bad_alpha,
    refit_releases_buffers.
  </action>
  <verify>
    <automated>cargo test --features cpu -p mlrs-algos --test complement_nb_test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "argmin_decode" crates/mlrs-algos/src/naive_bayes/complement_nb.rs` returns a match
    - `grep -c "SharedMemory\|F::INFINITY\|Atomic" crates/mlrs-algos/src/naive_bayes/complement_nb.rs` (after `grep -v '^//'`) == 0
    - `cargo test --features cpu -p mlrs-algos --test complement_nb_test` exits 0 (incl. norm_true)
    - exact_labels integer equality, no band (NOT the complement/sign-flipped labels — the argmin convention is correct)
  </acceptance_criteria>
  <done>ComplementNB green; complement weights + norm + argmin; labels match sklearn (not sign-flipped).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host → discrete NB build() | alpha + force_alpha + class_prior cross here |
| host → gemm prim | X + feature_log_prob_ shapes cross into the matvec launch grid |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03-01 | Tampering | alpha intake | mitigate | build() rejects alpha<0 → BuildError::InvalidAlpha; force_alpha clip+warn (D-06) before fit |
| T-11-03-02 | Tampering | integer-count denominator overflow | mitigate | counts accumulated in f64 host (class_grouped_sum returns f64) — no integer overflow in the denominator |
| T-11-03-03 | Tampering | NaN/Inf from log(0) on zero counts | mitigate | alpha smoothing (≥ default 1.0) keeps numerator/denominator positive; host log-sum-exp single terminal log |
| T-11-03-04 | Denial of Service | gemm geometry mismatch | mitigate | n_features-agreement guard → AlgoError before the gemm launch; gemm prim u32-guards the grid |
| — | Spoofing / Repudiation / Information Disclosure / Elevation | — | N/A | in-process numeric arrays; no auth/network/secret/privilege surface |

No `high`-severity threats. V5 Input Validation covered by D-05 build/fit split.
</threat_model>

<verification>
- `cargo test --features cpu -p mlrs-algos --test multinomial_nb_test --test bernoulli_nb_test --test complement_nb_test` exits 0
- exact labels match sklearn exactly for all three (f64 + f32)
- correct per-variant denominators (MNB alpha·n_features, BNB 2·alpha)
- BernoulliNB non-occurrence term + binarize; ComplementNB complement weights + argmin
- proba rows sum to 1; no new kernel; grep clean; PoolStats no-leak
</verification>

<success_criteria>
MultinomialNB (NB-02), BernoulliNB (NB-03), ComplementNB (NB-04) each fit/predict
matching sklearn within band, exact labels the hard gate, three independent structs
sharing only the GEMM joint-LL + nb_common helpers, correct per-variant smoothing,
build() rejects bad alpha + force_alpha clip+warn.
</success_criteria>

<output>
Create `.planning/phases/11-naive-bayes/11-03-SUMMARY.md` when done.
</output>
