---
phase: 10-sgd-linear-svm
plan: 02
type: execute
wave: 1
depends_on: [10-01]
files_modified:
  - crates/mlrs-kernels/src/sgd.rs
  - crates/mlrs-backend/src/prims/sgd.rs
  - crates/mlrs-backend/tests/sgd_test.rs
  - crates/mlrs-backend/tests/memory_gate_test.rs
autonomous: true
requirements: [PRIM-10]
must_haves:
  truths:
    - "prims/sgd.rs minimizes a convex squared-loss objective to the host closed-form optimum within tolerance (standalone, before any estimator)"
    - "The two sgd_* kernels LAUNCH on --features cpu (not just compile) — the cpu-launch success criterion"
    - "sgd_margin and sgd_weight_update are SharedMemory-free / atomics-free / INFINITY-free by construction (grep gates clean)"
    - "The two-pass GATHER is single-owner: pass 1 owns p[i], pass 2 owns w[j]; no scatter, no cross-unit atomics"
    - "The PoolStats memory gate passes: bounded allocations, peak plateaus, read_backs bounded across repeated solves"
    - "hinge/log/squared/squared-hinge/epsilon-insensitive dloss + optimal(Bottou-t0)/invscaling/constant schedules implemented host-side and isolated-tested"
  artifacts:
    - path: "crates/mlrs-kernels/src/sgd.rs"
      provides: "sgd_margin (pass 1) + sgd_weight_update (pass 2) real GATHER bodies"
      contains: "ABSOLUTE_POS"
    - path: "crates/mlrs-backend/src/prims/sgd.rs"
      provides: "sgd_solve host epoch loop: minibatch slice, dloss table, schedule, penalty, NotConverged-at-cap"
      contains: "fn dloss"
    - path: "crates/mlrs-backend/tests/sgd_test.rs"
      provides: "sgd_convex_objective + sgd_cpu_launch + dloss/schedule unit tests"
      contains: "sgd_convex_objective"
    - path: "crates/mlrs-backend/tests/memory_gate_test.rs"
      provides: "memory_gate_sgd_bounded PoolStats gate"
      contains: "memory_gate_sgd_bounded"
  key_links:
    - from: "crates/mlrs-backend/src/prims/sgd.rs"
      to: "sgd_margin / sgd_weight_update"
      via: "launch::<F, ActiveRuntime> per minibatch"
      pattern: "sgd_margin::launch|sgd_weight_update::launch"
    - from: "crates/mlrs-backend/tests/sgd_test.rs"
      to: "prims::sgd::sgd_solve"
      via: "convex-objective host reference comparison"
      pattern: "sgd_solve"
---

<objective>
Fill the SGD primitive (PRIM-10) — the single genuinely-new device solver of v2 and the highest cpu-MLIR risk — and validate it STANDALONE on a convex objective BEFORE any of the four estimators consume it. This is the core [v2-P4] de-risk and ROADMAP success criterion #1.

Purpose: Prove the two-pass GATHER kernel launches on cpu-MLIR (not just compiles), converges to a known optimum, and conserves memory — so Wave-2 estimators wire onto a validated prim contract.
Output: A working `sgd_solve` host orchestration over two single-owner GATHER kernels, gated by a convex-objective test, the cpu-launch test, the SharedMemory/INFINITY grep gates, and the PoolStats memory gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-sgd-linear-svm/10-RESEARCH.md
@.planning/phases/10-sgd-linear-svm/10-PATTERNS.md
@.planning/phases/10-sgd-linear-svm/10-VALIDATION.md
@.planning/phases/10-sgd-linear-svm/10-01-PLAN.md
</context>

<artifacts_produced>
## Artifacts this plan fills (Wave-0 stubs → real bodies)

**Kernel bodies (mlrs-kernels/src/sgd.rs):**
- `sgd_margin<F>` — pass 1: `let i=ABSOLUTE_POS; if i < b { acc = Σ_j x[i*d+j]*w[j]; p[i] = acc + bias }` (one unit per sample, GATHER over d, bounds-checked).
- `sgd_weight_update<F>` — pass 2: `let j=ABSOLUTE_POS; if j < d { grad = Σ_i g[i]*x[i*d+j]; w[j] = w[j] - eta*grad*inv_b }` (one unit per coordinate, GATHER over batch).

**Prim host orchestration (mlrs-backend/src/prims/sgd.rs):**
- `fn dloss(loss: Loss, p: f64, y: f64, epsilon: f64) -> f64` — the six-loss subgradient table (RESEARCH §SGD-Math).
- `fn optimal_t0(loss: Loss, alpha: f64) -> f64` — Bottou t0 (RESEARCH lines 510-514, A1).
- `fn schedule_eta(lr: LearningRate, t, eta0, alpha, power_t, t0) -> f64` — optimal/invscaling/constant/adaptive.
- `sgd_solve<F>` real body: epoch loop over `max_iter`, minibatch slice (shuffle=false → natural row order), per-batch sgd_margin launch → host g[i]=dloss → eta=schedule(t) → sgd_weight_update launch → host penalty shrink (L2 lazy wscale / L1 cumulative) → intercept `b -= eta*Σg*intercept_decay`; NotConverged at cap via map_sgd_error.

**Tests (mlrs-backend/tests):**
- `sgd_test::sgd_convex_objective` — fit a known-optimum convex squared-loss linear system, assert iterate within tol.
- `sgd_test::sgd_cpu_launch` — both kernels launch on --features cpu.
- `sgd_test::dloss_*` / `schedule_*` — host helper unit tests (isolate t0 via a constant-schedule case first, A1/Pitfall 3).
- `memory_gate_test::memory_gate_sgd_bounded` — PoolStats gate (bounded alloc / peak plateaus / read_backs bounded across N repeated solves).
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fill the two-pass GATHER kernels + cpu-launch gate</name>
  <files>crates/mlrs-kernels/src/sgd.rs, crates/mlrs-backend/tests/sgd_test.rs</files>
  <behavior>
    - sgd_margin: for a B×d minibatch and length-d w with bias, p[i] == Σ_j x[i,j]*w[j] + bias for every i in 0..B (host-reference match, f32+f64).
    - sgd_weight_update: for precomputed g[0..B], w[j]_out == w[j]_in - eta*(Σ_i g[i]*x[i,j])*inv_b for every j in 0..d.
    - Both kernels LAUNCH on --features cpu (cpu-MLIR `run pass` does not panic) — the success criterion, not just compile.
    - Over-provisioned threads beyond B (margin) / d (update) write nothing (bounds-check holds).
  </behavior>
  <read_first>
    - crates/mlrs-kernels/src/coordinate.rs lines 57-78 (col_dot GATHER), 93-107 (residual_axpy bounds-check), 19-34 (safety contract)
    - crates/mlrs-kernels/src/laplacian sources via prims/laplacian.rs (09-02 cpu-launch precedent: one thread per coordinate, ascending scan, F/u32 accumulators)
    - 10-RESEARCH.md §Pattern-1 (lines 199-256 exact kernel bodies), §Pitfall-1 (lines 445-450 launch gate)
    - 10-PATTERNS.md §sgd.rs kernel (lines 42-111)
    - MEMORY [[cubecl-cpu-no-shared-memory]] — SharedMemory-free idiom; cpu-LAUNCH is the authoritative gate, not the recalled list
  </read_first>
  <action>
    Replace the Wave-0 placeholder bodies with the real GATHER bodies (RESEARCH §Pattern-1). sgd_margin: `let i = ABSOLUTE_POS; if i < b as usize { let mut acc = F::from_int(0i64); let mut j = 0u32; let base = (i as u32)*d; while j < d { acc += x[(base+j) as usize]*w[j as usize]; j += 1u32; } p[i] = acc + bias; }`. sgd_weight_update: `let j = ABSOLUTE_POS; if j < d as usize { let mut grad = F::from_int(0i64); let mut i = 0u32; while i < b { grad += g[i as usize]*x[(i*d + j as u32) as usize]; i += 1u32; } w[j] = w[j] - eta*grad*inv_b; }`. Scalars BY VALUE (bias/eta/inv_b: F, b/d: u32). NO SharedMemory/atomics/F::INFINITY/mutable-bool/descending-shift. Penalty shrink is applied HOST-side in the prim (Task 2), NOT inside the kernel, to keep the kernel single-purpose. Un-ignore and fill `sgd_test::sgd_cpu_launch` (launch both kernels on a small batch under ActiveRuntime, assert no panic + result matches a host dot/axpy reference for f32 and f64; f64 case carries skip_f64_with_log on rocm but runs on cpu). Add `sgd_margin_matches_host` / `sgd_weight_update_matches_host` unit tests against a plain host computation.
  </action>
  <verify>
    <automated>cargo build -p mlrs-kernels 2>&1 | tail -3; cargo test -p mlrs-backend --features cpu sgd_test::sgd_cpu_launch -- --nocapture 2>&1 | tail -15; grep -v '^//' crates/mlrs-kernels/src/sgd.rs | grep -c SharedMemory; grep -v '^//' crates/mlrs-kernels/src/sgd.rs | grep -c INFINITY; grep -c "atomic" crates/mlrs-kernels/src/sgd.rs</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p mlrs-kernels` exits 0
    - `cargo test -p mlrs-backend --features cpu sgd_test::sgd_cpu_launch` exits 0 (kernels LAUNCH on cpu, not just compile)
    - `grep -v '^//' crates/mlrs-kernels/src/sgd.rs | grep -c SharedMemory` == 0
    - `grep -v '^//' crates/mlrs-kernels/src/sgd.rs | grep -c INFINITY` == 0
    - `grep -c "atomic" crates/mlrs-kernels/src/sgd.rs` == 0
    - `sgd_test.rs` contains `sgd_cpu_launch` and host-reference match assertions for both kernels (f32 + f64)
  </acceptance_criteria>
  <done>Both GATHER kernels carry real bodies, launch on cpu-MLIR, match a host dot/axpy reference, and pass the SharedMemory/INFINITY/atomic grep gates.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Host epoch loop, dloss/schedule helpers, convex-objective + memory gate</name>
  <files>crates/mlrs-backend/src/prims/sgd.rs, crates/mlrs-backend/tests/sgd_test.rs, crates/mlrs-backend/tests/memory_gate_test.rs</files>
  <behavior>
    - sgd_solve on a convex squared-loss linear system (squared_error loss, l2 penalty alpha~0, constant schedule, fixed max_iter) converges to the host closed-form OLS optimum within tolerance (f64 strict 1e-5; f32 documented band).
    - dloss matches the RESEARCH §SGD-Math table for all six losses at sample points (hinge threshold 1.0; squared_error p-y; epsilon-insensitive subgradient).
    - optimal_t0 reproduces the live-sklearn t0 for the constant-schedule fixture's alpha (validated against the Wave-0 optimal-schedule fixture pin in Wave 2; isolated here by first asserting a CONSTANT-schedule case matches before the optimal case).
    - Repeated sgd_solve calls (N=5, fixed shape) show bounded allocations (delta==0 after warmup), peak plateaus, read_backs bounded per call (PoolStats gate).
  </behavior>
  <read_first>
    - crates/mlrs-backend/src/prims/coordinate_descent.rs lines 137-222 (host loop + per-iter launch), map_cd_error (NotConverged), lines 232-258 (host_to_f64/narrow_to_f helpers)
    - crates/mlrs-backend/src/prims/lbfgs.rs (host orchestration + device-objective scalar readback shape)
    - crates/mlrs-backend/tests/memory_gate_test.rs — memory_gate_iterative_solver_bounded ([05-11] CD/L-BFGS bounded-allocation gate-encoded EXCEPTION; copy the N=5 repeated-call PoolStats shape)
    - 10-RESEARCH.md §SGD-Math (lines 335-394 dloss/schedules/defaults), §Code Examples (dloss lines 490-504, optimal_t0 lines 510-514), §Pattern-2 (lines 269-273), §Validation Architecture Criterion-1 (line 622)
    - 10-PATTERNS.md §prims/sgd.rs (lines 114-163)
    - MEMORY [[backend-test-suite-slow]] — run targeted `cargo test -p mlrs-backend --features cpu sgd`, background the full suite
  </read_first>
  <action>
    Implement the host helpers in prims/sgd.rs: `dloss(loss, p, y, epsilon) -> f64` (the exact six-loss table, RESEARCH lines 490-504), `optimal_t0(loss, alpha) -> f64` (typw=sqrt(1/sqrt(alpha)); initial_eta0=typw/max(1,|dloss(loss,-typw,1)|); t0=1/(initial_eta0*alpha) — RESEARCH lines 510-514, A1), and `schedule_eta(lr, t, eta0, alpha, power_t, t0) -> f64` (optimal=1/(alpha*(t0+t-1)); invscaling=eta0/t^power_t; constant=eta0; adaptive starts eta0, /5 on no-improvement, stop <1e-6). Fill `sgd_solve`: initialize w=0/b=0 device buffers, `let max_iter = if max_iter==0 { SGD_DEFAULT_MAX_ITER } else { max_iter }`, for epoch in 0..max_iter { iterate minibatches in NATURAL ROW ORDER (shuffle=false; if shuffle, permute via prims::rng::SplitMix64 — never OsRng, ASVS-V6) — for each batch: sgd_margin::launch → read p[] → host g[i]=dloss(loss,p_i,y_i,epsilon) clipped to ±1e12 → eta=schedule_eta(t) → upload g[] → sgd_weight_update::launch → host penalty shrink (L2 lazy wscale w*=max(0,1-(1-l1_ratio)*eta*alpha); L1 cumulative u+=l1_ratio*eta*alpha then soft-shrink; ElasticNet both) → if fit_intercept: b -= eta*(Σ_i g_i)*intercept_decay (intercept_decay=1.0 dense, A3) }. Average the minibatch gradient by inv_b=1/B (A2 — a 1-sample batch reduces to plain SGD and disambiguates). Convergence: track max weight change; if < tol*scale stop; else NotConverged at cap via map_sgd_error. Use host_to_f64/f64_to_host for all schedule/loss scalars (copy the trio). Release transient batch buffers to the pool (memory gate). Fill `sgd_test::sgd_convex_objective` (squared_error, constant schedule, near-zero alpha → fit to host OLS optimum within tol; f64 strict 1e-5, f32 band) and the dloss/schedule unit tests (assert a CONSTANT-schedule case FIRST to isolate t0). Fill `memory_gate_test::memory_gate_sgd_bounded` (N=5 repeated sgd_solve at fixed shape → bounded alloc / peak plateaus / read_backs bounded, the [05-11] iterative-solver gate-encoded exception form).
  </action>
  <verify>
    <automated>cargo test -p mlrs-backend --features cpu sgd_test::sgd_convex_objective -- --nocapture 2>&1 | tail -15; cargo test -p mlrs-backend --features cpu sgd_test -- --nocapture 2>&1 | tail -10; cargo test -p mlrs-backend --features cpu memory_gate_sgd_bounded -- --nocapture 2>&1 | tail -10; grep -c "OsRng" crates/mlrs-backend/src/prims/sgd.rs</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p mlrs-backend --features cpu sgd_test::sgd_convex_objective` exits 0 (iterate within tol of host closed-form optimum)
    - `cargo test -p mlrs-backend --features cpu sgd_test` exits 0 (dloss/schedule unit tests green; constant-schedule case isolated)
    - `cargo test -p mlrs-backend --features cpu memory_gate_sgd_bounded` exits 0 (bounded alloc / peak plateaus / read_backs bounded)
    - `prims/sgd.rs` contains `fn dloss`, `fn optimal_t0`, `fn schedule_eta`, and a `sgd_solve` epoch loop that launches both kernels
    - `grep -c "OsRng" crates/mlrs-backend/src/prims/sgd.rs` == 0 (ASVS-V6: SplitMix64 only)
    - f64 oracle case carries skip_f64_with_log; f32 uses a documented band
  </acceptance_criteria>
  <done>sgd_solve converges to the convex optimum standalone, the dloss/schedule helpers match the RESEARCH table (constant case isolated), and the PoolStats memory gate passes — PRIM-10 validated before any estimator wires it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prim host loop → device GATHER kernels | Batch shape (B,d), weight length d, batch indices reach the kernels |
| host schedule/penalty scalars | alpha/eta0/epsilon/max_iter drive the loop arithmetic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02-01 | Tampering / Information Disclosure | GATHER index bounds in sgd_margin/sgd_weight_update | mitigate | Every kernel thread bounds-checks (`if i < b`, `if j < d`); the prim validates x.len()==n*d before launch (carried from Wave-0 geometry guard). Over-provisioned threads write nothing → no out-of-bounds device read/write. |
| T-10-02-02 | Denial of Service | unbounded epoch loop on huge max_iter | mitigate | max_iter is a finite cap; NotConverged surfaced at the cap via map_sgd_error (T-05-10-03 precedent), never an infinite loop. |
| T-10-02-03 | Tampering | non-reproducible RNG if shuffle=true | mitigate | Shuffle uses prims::rng::SplitMix64 (seeded, reproducible), NEVER OsRng/rand (ASVS-V6). grep gate asserts OsRng absent. Pinned oracle uses shuffle=false (identity order). |
| T-10-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs (v2 "Cargo unchanged"; RESEARCH §Package Legitimacy Audit: none). |

No high-severity threats: all device index access is bounds-checked, iteration is capped, and RNG is the seeded SplitMix64. The prim has no network/auth/filesystem surface.
</threat_model>

<verification>
- `cargo test -p mlrs-backend --features cpu sgd` green (convex objective, cpu launch, dloss/schedule, memory gate).
- SharedMemory/INFINITY/atomic/OsRng grep gates clean on the new sources.
- f64 cases skip_f64_with_log; f32 documented band; convex f64 strict 1e-5.
- rocm(f32) test target builds (verified at wave merge per [[rocm-is-runnable-gpu-gate]]).
</verification>

<success_criteria>
- PRIM-10 met: sgd_solve validated STANDALONE on a convex objective before any estimator wires it.
- cpu-launch gate green (kernels launch, not just compile).
- Two-pass GATHER is single-owner (pass 1 owns p[i], pass 2 owns w[j]); no scatter/atomics.
- PoolStats memory gate green.
- All six losses + optimal(Bottou-t0)/invscaling/constant/adaptive schedules implemented and isolated-tested.
</success_criteria>

<output>
Create `.planning/phases/10-sgd-linear-svm/10-02-SUMMARY.md` when done.
</output>
