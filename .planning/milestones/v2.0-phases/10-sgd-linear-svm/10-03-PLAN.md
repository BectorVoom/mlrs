---
phase: 10-sgd-linear-svm
plan: 03
type: execute
wave: 2
depends_on: [10-02]
files_modified:
  - crates/mlrs-algos/src/linear/mbsgd_classifier.rs
  - crates/mlrs-algos/src/linear/mbsgd_regressor.rs
  - crates/mlrs-algos/src/linear/sgd_config.rs
  - crates/mlrs-algos/tests/mbsgd_classifier_test.rs
  - crates/mlrs-algos/tests/mbsgd_regressor_test.rs
  - crates/mlrs-algos/tests/sgd_config_test.rs
autonomous: true
requirements: [SGDSVM-01, SGDSVM-02]
must_haves:
  truths:
    - "MBSGDClassifier.fit + predict reproduce the pinned sklearn SGDClassifier oracle: coef_/intercept_ within tolerance, predict labels EXACT (hard gate)"
    - "MBSGDClassifier(log) predict_proba matches the pinned oracle within tolerance (sigmoid 1/(1+exp(-margin)))"
    - "MBSGDRegressor.fit + predict reproduce the pinned sklearn SGDRegressor oracle (squared_error + invscaling) within tolerance"
    - "builder().build() with no setters reproduces sklearn's default-constructor params for both estimators (D-03 litmus)"
    - "build() validates data-independent params (alpha>=0, l1_ratio in [0,1], eta0>0, valid loss-for-estimator) -> BuildError (D-08); fit() validates geometry/labels -> AlgoError"
    - "TryFrom<&str> accepts sklearn loss spellings + legacy aliases and rejects unknown -> BuildError (D-05)"
  artifacts:
    - path: "crates/mlrs-algos/src/linear/mbsgd_classifier.rs"
      provides: "MBSGDClassifier builder.build() validation + Fit + PredictLabels + PredictProba"
      contains: "impl"
    - path: "crates/mlrs-algos/src/linear/mbsgd_regressor.rs"
      provides: "MBSGDRegressor builder.build() validation + Fit + Predict"
      contains: "impl"
    - path: "crates/mlrs-algos/tests/mbsgd_classifier_test.rs"
      provides: "oracle / exact_labels / proba / default_matches_sklearn / build_rejects_bad_alpha"
      contains: "exact_labels"
    - path: "crates/mlrs-algos/tests/mbsgd_regressor_test.rs"
      provides: "oracle / default_matches_sklearn"
      contains: "oracle"
  key_links:
    - from: "crates/mlrs-algos/src/linear/mbsgd_classifier.rs"
      to: "mlrs_backend::prims::sgd::sgd_solve"
      via: "fit delegates to the validated prim"
      pattern: "sgd_solve"
    - from: "crates/mlrs-algos/src/linear/mbsgd_regressor.rs"
      to: "elastic_net::predict_linear"
      via: "shared X·coef_+intercept_ predict path"
      pattern: "predict_linear"
---

<objective>
Wire the two minibatch-SGD estimators onto the validated PRIM-10 prim: `MBSGDClassifier` (SGDSVM-01) and `MBSGDRegressor` (SGDSVM-02), each gated by the pinned-deterministic sklearn oracle. Move the data-independent validation into `build()` (D-08) and keep the data-dependent geometry/label checks in `fit()`.

Purpose: Deliver the two SGD-prim consumers with exact sklearn parity under the pinned oracle (shuffle=False, tol=0, fixed schedule/iters). Exact predicted labels are the HARD classifier gate.
Output: Fitted MBSGDClassifier/Regressor matching their pinned fixtures, with the D-03 default litmus, D-08 split validation, and D-05 TryFrom all proven.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-sgd-linear-svm/10-RESEARCH.md
@.planning/phases/10-sgd-linear-svm/10-PATTERNS.md
@.planning/phases/10-sgd-linear-svm/10-VALIDATION.md
@.planning/phases/10-sgd-linear-svm/10-02-PLAN.md
</context>

<artifacts_produced>
## Artifacts this plan fills

**MBSGDClassifier (mbsgd_classifier.rs):**
- `MBSGDClassifierBuilder::build()` validation body (D-08 data-independent: alpha>=0, l1_ratio∈[0,1], eta0>0 unless optimal, valid loss-for-classifier {Hinge,Log,SquaredHinge}); InvalidLossForEstimator on EpsilonInsensitive/SquaredEpsilonInsensitive.
- `impl Fit` — classes_ distinct-sorted-label remap (±1 encoding, Pitfall 4), geometry/label checks (AlgoError), delegate to `sgd_solve(config)`.
- `impl PredictLabels` — argmax/sign → classes_ roundtrip (i32 ids).
- `impl PredictProba` — log-loss sigmoid `1/(1+exp(-margin))`.
- host_to_f64/f64_to_host/f_epsilon helper trio (private copy).

**MBSGDRegressor (mbsgd_regressor.rs):**
- `MBSGDRegressorBuilder::build()` validation (alpha>=0, l1_ratio∈[0,1], eta0>0, epsilon>=0, valid loss-for-regressor {SquaredLoss,EpsilonInsensitive}).
- `impl Fit` — geometry check, delegate to `sgd_solve(config)`.
- `impl Predict` — reuse `elastic_net::predict_linear`.

**Tests:** mbsgd_classifier_test (oracle/exact_labels/proba/default_matches_sklearn/build_rejects_bad_alpha), mbsgd_regressor_test (oracle/default_matches_sklearn), sgd_config_test (try_from + build validation, un-ignored).
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MBSGDClassifier — build() validation, Fit, PredictLabels, PredictProba + oracle</name>
  <files>crates/mlrs-algos/src/linear/mbsgd_classifier.rs, crates/mlrs-algos/tests/mbsgd_classifier_test.rs, crates/mlrs-algos/tests/sgd_config_test.rs</files>
  <behavior>
    - MBSGDClassifier::builder().loss(Hinge).penalty(L2).alpha(1e-4)... pinned to the fixture (shuffle false, tol 0, fixed max_iter, explicit eta0/schedule).fit then predict reproduces the fixture's predict labels EXACTLY (hard gate, integer, no band) for f32 + f64.
    - coef_/intercept_ match the fixture within tolerance (f64 strict 1e-5; f32 documented band per recurring gate).
    - MBSGDClassifier(log) predict_proba matches the log-loss fixture within tolerance (sigmoid).
    - builder().build() with NO setters yields config equal to sklearn SGDClassifier defaults (D-03 litmus): loss=Hinge, penalty=L2, alpha=1e-4, lr=Optimal, max_iter=1000, tol=1e-3, eta0=0.01, l1_ratio=0.15.
    - build() rejects alpha<0 -> BuildError::InvalidAlpha; rejects EpsilonInsensitive -> BuildError::InvalidLossForEstimator; TryFrom("bogus") -> BuildError::UnknownLoss.
  </behavior>
  <read_first>
    - crates/mlrs-algos/src/linear/logistic.rs lines 48-61 (imports), 99-125 (struct w/ classes_), 239-282 (classes_ remap Pitfall 4), 586-601 (predict_labels argmax roundtrip), 481-564 (predict_proba), 662-690 (host_to_f64 trio), 314-364/606-660 (prim_err capture + ScratchGuard RAII), 201-268 (fit-time validation that SPLITS per D-08)
    - crates/mlrs-backend/src/prims/sgd.rs (sgd_solve signature from Wave 1)
    - crates/mlrs-algos/tests/logistic_test.rs (oracle test shape: load .npz, fit, compare coef/intercept/predict/proba)
    - 10-PATTERNS.md §mbsgd_classifier.rs (lines 222-281), §Shared Patterns (validate-before-launch split, device-residency, host_to_f64, re-fit buffer reuse)
    - 10-RESEARCH.md §SGD-Math (Log dloss → sigmoid), §Pitfall-4 (±1 encoding), §Pitfall-7 (default vs pinned), §Validation Architecture Criterion-2
    - 10-CONTEXT.md D-03/D-08
  </read_first>
  <action>
    Fill MBSGDClassifierBuilder::build() with the D-08 data-independent validation: `if !(alpha >= 0.0) { return Err(BuildError::InvalidAlpha{..}) }`, l1_ratio∈[0,1] when penalty=ElasticNet, eta0>0 unless learning_rate=Optimal, and a loss-for-classifier gate (Hinge/Log/SquaredHinge valid; EpsilonInsensitive/SquaredEpsilonInsensitive → InvalidLossForEstimator). build() lowers into SgdConfig (D-06). Implement Fit (copy logistic.rs classes_ distinct-sorted-dedup remap, reject <2 classes, build dense ±1 y for the margin loss; geometry x.len()==n*d → AlgoError; re-fit buffer release per WR-07; delegate to sgd_solve(&config) → store coef_/intercept_ device-resident). Implement PredictLabels (margin sign or argmax → classes_[best] as i32). Implement PredictProba (log loss → per-sample sigmoid 1/(1+exp(-margin)); for hinge there is no calibrated proba — match sklearn behavior, expose proba only for log loss or document the decision-function fallback). Copy the host_to_f64/f64_to_host/f_epsilon trio (private). Wrap any per-batch fallible launch in the prim_err/ScratchGuard pattern so a device failure is typed AlgoError::Prim, never a panic. Un-ignore + fill the classifier oracle tests: `oracle` (coef/intercept band), `exact_labels` (HARD gate, exact), `proba` (log-loss sigmoid within tol), `default_matches_sklearn` (D-03 litmus — assert bare build() config equals sklearn defaults), `build_rejects_bad_alpha` (BuildError). f64 cases skip_f64_with_log; f32 band. Un-ignore sgd_config_test try_from + build-validation cases that exercise the now-filled build().
  </action>
  <verify>
    <automated>cargo test -p mlrs-algos --features cpu mbsgd_classifier_test -- --nocapture 2>&1 | tail -20; cargo test -p mlrs-algos --features cpu sgd_config_test -- --nocapture 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p mlrs-algos --features cpu mbsgd_classifier_test::exact_labels` exits 0 (predict labels EXACT, hard gate)
    - `cargo test -p mlrs-algos --features cpu mbsgd_classifier_test::oracle` exits 0 (coef/intercept within band)
    - `cargo test -p mlrs-algos --features cpu mbsgd_classifier_test::proba` exits 0 (log-loss sigmoid within tol)
    - `cargo test -p mlrs-algos --features cpu mbsgd_classifier_test::default_matches_sklearn` exits 0 (D-03 litmus)
    - `cargo test -p mlrs-algos --features cpu mbsgd_classifier_test::build_rejects_bad_alpha` exits 0 (BuildError)
    - `cargo test -p mlrs-algos --features cpu sgd_config_test` exits 0 (TryFrom + build validation)
    - f64 oracle cases reference skip_f64_with_log; f32 uses a documented band
  </acceptance_criteria>
  <done>MBSGDClassifier matches the pinned oracle (exact labels hard gate, coef/proba within tol), the D-03 default litmus holds, and build()/TryFrom validation (D-05/D-08) is proven.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MBSGDRegressor — build() validation, Fit, Predict + oracle</name>
  <files>crates/mlrs-algos/src/linear/mbsgd_regressor.rs, crates/mlrs-algos/tests/mbsgd_regressor_test.rs</files>
  <behavior>
    - MBSGDRegressor::builder().loss(SquaredLoss).learning_rate(InvScaling)... pinned to the fixture.fit then predict matches the fixture's predict within tolerance (f64 strict 1e-5; f32 band).
    - coef_/intercept_ match the fixture within tolerance.
    - epsilon-insensitive loss path matches its fixture (subgradient ±1 / tube).
    - builder().build() with no setters yields sklearn SGDRegressor defaults (D-03: loss=SquaredLoss, lr=InvScaling, power_t=0.25).
    - build() rejects epsilon<0 -> BuildError::InvalidEpsilon; rejects Hinge -> InvalidLossForEstimator.
  </behavior>
  <read_first>
    - crates/mlrs-algos/src/linear/elastic_net.rs lines 57-74 (struct), 117-137 (accessors), 140-178 (Fit unwraps y then solves), 180-268 (Predict via predict_linear, pub(crate) line 207), 272-287 (host_to_f64 trio)
    - crates/mlrs-backend/src/prims/sgd.rs (sgd_solve signature)
    - crates/mlrs-algos/tests/elastic_net_test.rs (regressor oracle test shape)
    - 10-PATTERNS.md §mbsgd_regressor.rs (lines 284-316)
    - 10-RESEARCH.md §SGD-Math (squared_error grad=p-y; epsilon_insensitive subgradient; invscaling schedule), §Validation Architecture Criterion-3
    - 10-CONTEXT.md D-03/D-06/D-08
  </read_first>
  <action>
    Fill MBSGDRegressorBuilder::build() validation (alpha>=0, l1_ratio∈[0,1] when ElasticNet, eta0>0, epsilon>=0, loss-for-regressor gate {SquaredLoss,EpsilonInsensitive} valid; Hinge/Log/SquaredHinge → InvalidLossForEstimator), lowering into SgdConfig. Implement Fit (copy elastic_net: unwrap y with NotFitted-on-missing, geometry check → AlgoError, re-fit buffer release, delegate to sgd_solve(&config) → store coef_/intercept_). Implement Predict by calling the shared `predict_linear(self.coef_.as_ref(), self.intercept_.as_ref(), "mbsgd_regressor", pool, x, shape)` (pub(crate) in elastic_net.rs — the X·coef_+intercept_ GEMM-then-broadcast path is reusable). Copy the host_to_f64 trio (private). Un-ignore + fill `mbsgd_regressor_test::oracle` (squared_error + invscaling fixture, coef/intercept/predict within tol; add the epsilon-insensitive fixture case) and `default_matches_sklearn` (D-03 litmus). f64 skip_f64_with_log; f32 band.
  </action>
  <verify>
    <automated>cargo test -p mlrs-algos --features cpu mbsgd_regressor_test -- --nocapture 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p mlrs-algos --features cpu mbsgd_regressor_test::oracle` exits 0 (predict + coef/intercept within tolerance)
    - `cargo test -p mlrs-algos --features cpu mbsgd_regressor_test::default_matches_sklearn` exits 0 (D-03 litmus)
    - `mbsgd_regressor.rs` Predict delegates to `predict_linear` (no duplicated GEMM path)
    - epsilon-insensitive fixture case passes
    - f64 cases reference skip_f64_with_log; f32 documented band
  </acceptance_criteria>
  <done>MBSGDRegressor matches the pinned oracle (squared_error + epsilon-insensitive), reuses predict_linear, and the D-03 default litmus + D-08 build validation hold.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| builder().build() | Data-independent hyperparameters validated here (D-08) before any device work |
| fit() | Data-dependent geometry/labels (untrusted shape/label values) validated here |
| estimator → sgd_solve | Lowered SgdConfig + device arrays reach the validated prim |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03-01 | Tampering | build() data-independent validation | mitigate | alpha<0 / l1_ratio out of range / eta0<=0 / invalid loss-for-estimator → typed BuildError (D-08), never reaches the prim. |
| T-10-03-02 | Tampering / DoS | fit() geometry + label checks | mitigate | x.len()!=n*d and <2 classes → typed AlgoError BEFORE the device launch (ASVS-V5 validate-before-launch); over-provisioned kernel threads bounds-checked in Wave 1. |
| T-10-03-03 | Denial of Service | device launch failure across estimator boundary | mitigate | per-batch fallible launch wrapped in prim_err/ScratchGuard RAII → typed AlgoError::Prim, never an unwinding panic (logistic.rs precedent). |
| T-10-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs (v2 Cargo-unchanged; RESEARCH §Package Legitimacy Audit: none). |

No high-severity threats: untrusted hyperparameters and data shapes are funneled into typed BuildError/AlgoError before any unsafe device launch; the proba/label math is pure host arithmetic over validated device results.
</threat_model>

<verification>
- `cargo test -p mlrs-algos --features cpu mbsgd_classifier_test mbsgd_regressor_test sgd_config_test` green.
- Classifier exact-labels HARD gate passes; coef/proba within band.
- D-03 default litmus passes for both estimators.
- f64 cases skip_f64_with_log; f32 documented band; rocm(f32) target builds at wave merge.
</verification>

<success_criteria>
- SGDSVM-01 met: MBSGDClassifier predict/predict_proba match the pinned oracle (exact labels hard gate).
- SGDSVM-02 met: MBSGDRegressor predict matches the pinned oracle (squared_error + epsilon-insensitive).
- D-03 (default==sklearn), D-05 (TryFrom), D-06 (SgdConfig lowering), D-08 (split validation) all proven by test.
</success_criteria>

<output>
Create `.planning/phases/10-sgd-linear-svm/10-03-SUMMARY.md` when done.
</output>
