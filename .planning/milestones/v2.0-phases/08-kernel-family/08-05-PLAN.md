---
phase: 08-kernel-family
plan: 05
type: execute
wave: 3
depends_on: ["08-03", "08-04"]
files_modified:
  - crates/mlrs-py/src/estimators/kernel.rs
  - crates/mlrs-py/src/estimators/mod.rs
  - crates/mlrs-py/src/lib.rs
  - crates/mlrs-py/tests/test_kernel.py
autonomous: true
requirements: [KERNEL-01, KERNEL-02]
must_haves:
  truths:
    - "PyKernelRidge exposes fit/predict with f32/f64 runtime dispatch, GIL release, and guard_f64() on the F64 arm"
    - "PyKernelDensity exposes fit/score_samples (the one new exposed method) with the same dispatch contracts"
    - "Both pyclasses use the shipped any_estimator! machinery (zero new binding infra) and dtype-suffixed accessors (dual_coef_f32/_f64, log_density_f32/_f64)"
    - "The kernel name + raw gamma/degree/coef0/bandwidth are stored in Unfit; the typed Kernel<F> is built at fit once n_features is known"
  artifacts:
    - path: "crates/mlrs-py/src/estimators/kernel.rs"
      provides: "PyKernelRidge + PyKernelDensity (any_estimator! + #[pymethods])"
      contains: "score_samples"
    - path: "crates/mlrs-py/tests/test_kernel.py"
      provides: "py smoke test: fit/predict/score_samples, f32/f64 dispatch"
      contains: "score_samples"
  key_links:
    - from: "crates/mlrs-py/src/lib.rs"
      to: "PyKernelRidge / PyKernelDensity"
      via: "m.add_class registration in the pymodule"
      pattern: "add_class::<PyKernel"
    - from: "crates/mlrs-py/src/estimators/kernel.rs"
      to: "mlrs_algos::kernel_ridge::KernelRidge / density::kernel_density::KernelDensity"
      via: "any_estimator! algo binding"
      pattern: "any_estimator!"
---

<objective>
PyO3 wrappers for both Phase-8 estimators (PY-06 incremental share): `PyKernelRidge` (fit/predict)
and `PyKernelDensity` (fit/`score_samples` — the one new exposed method). Reuses the shipped
`any_estimator!` Unfit/F32/F64 machinery + dtype-suffixed accessors + `py.detach` GIL release +
`guard_f64()` — v2 adds ZERO binding infrastructure. PY-06's final cross-cutting sign-off remains
Phase 11; only the incremental wrapper share is in scope here.

Purpose: Expose KernelRidge/KernelDensity to the sklearn-compatible Python surface, completing the
phase's user-facing deliverable. Wave 3 — depends on both estimators (Plans 03 + 04).
Output: kernel.rs PyO3 module + pymodule registration + green py smoke test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-kernel-family/08-CONTEXT.md
@.planning/phases/08-kernel-family/08-RESEARCH.md
@.planning/phases/08-kernel-family/08-PATTERNS.md
@.planning/phases/07-covariance-projection/07-07-PLAN.md
@crates/mlrs-py/src/estimators/covariance.rs
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: PyKernelRidge + PyKernelDensity (any_estimator! + dispatch + GIL + guard_f64)</name>
  <files>crates/mlrs-py/src/estimators/kernel.rs, crates/mlrs-py/src/estimators/mod.rs, crates/mlrs-py/src/lib.rs</files>
  <read_first>
    - crates/mlrs-py/src/estimators/covariance.rs:15-22 (imports), :28-32 (`any_estimator!` invocation), :74-104 (fit body — `py.detach` GIL release + `guard_f64()` BEFORE the F64 upload — the two load-bearing contracts), :106-147 (dtype-suffixed accessors `covariance_f32/_f64`), :148-157 (`is_fitted`/`dtype`). 08-PATTERNS.md §kernel.rs lines 233-276.
    - crates/mlrs-py/src/dispatch.rs:85-108 (the `any_estimator!` macro) + :21-54 (GIL/f64-guard doc).
    - crates/mlrs-py/src/lib.rs:137,162-163 (the `add_class` registration pattern); estimators/mod.rs `pub mod` lines.
    - 08-RESEARCH.md §Code Examples (`any_estimator!` + score_samples shape) + Open Q3 (store kernel NAME/u8 + raw gamma/degree/coef0/bandwidth in Unfit; build typed Kernel<F> at fit). STATE.md [07-07] (the v2 incremental-wrap precedent: zero new infra, dtype-suffixed array accessors only, scalar attrs single-typed, guard_f64 on every F64 arm).
  </read_first>
  <action>
    Create `estimators/kernel.rs` mirroring `covariance.rs`. Two `any_estimator!` invocations:
    `AnyKernelRidge` over `mlrs_algos::kernel_ridge::KernelRidge` and `AnyKernelDensity` over
    `mlrs_algos::density::kernel_density::KernelDensity`. Store in each `Unfit` the kernel NAME
    (u8 tag or string) + raw hyperparameters (KernelRidge: alpha/gamma/degree/coef0;
    KernelDensity: bandwidth spec) — build the typed `Kernel<F>` / resolve bandwidth at `fit` once
    `n_features` is known (Open Q3 / D-05 gamma=None path).

    For each pyclass, hand-write `#[pymethods]` copying the covariance.rs:74-104 fit body VERBATIM
    for the two contracts: wrap the trait call in `py.detach(|| { global_pool().lock()… })` (PY-03
    GIL release), and call `crate::capability::guard_f64()?` BEFORE the F64-arm upload (statically
    grep-verifiable, per STATE.md [07-07] WARNING 2). Dispatch on `float_dtype` → F32/F64 arms.
    - PyKernelRidge: `fit(X, y, rows, cols, …)` + `predict(X_test, …)`; dtype-suffixed accessor
      `dual_coef_f32`/`dual_coef_f64` (mirror `covariance_f32/_f64`).
    - PyKernelDensity: `fit(X, …)` + `score_samples(Q, rows, cols, …)` — the ONE new exposed method
      (RESEARCH §Code Examples); accessor `log_density_f32`/`log_density_f64`.
    Add `is_fitted`/`dtype` helpers (covariance.rs:148-157). Register: `estimators/mod.rs` add
    `pub mod kernel;`; `lib.rs` add `use estimators::kernel::{PyKernelRidge, PyKernelDensity};` +
    two `m.add_class::<…>()?;` (mirror lib.rs:137,162-163). On any cubecl/pyo3 build error consult
    /home/user/Documents/workspace/cintx/docs/cubecl_error_guideline.md (pyo3 stays 0.28 — no bump).
  </action>
  <verify>
    <automated>cargo build -p mlrs-py --features cpu 2>&1 | tail -8; grep -q "any_estimator!" crates/mlrs-py/src/estimators/kernel.rs && grep -q "score_samples" crates/mlrs-py/src/estimators/kernel.rs && grep -q "guard_f64" crates/mlrs-py/src/estimators/kernel.rs && grep -q "add_class::<PyKernel" crates/mlrs-py/src/lib.rs && echo PY_KERNEL_OK</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p mlrs-py --features cpu` succeeds.
    - kernel.rs contains two `any_estimator!` invocations, a `score_samples` method, and `guard_f64` on each F64 arm.
    - lib.rs registers both pyclasses via `m.add_class`.
    - Unfit stores kernel name + raw hyperparameters; the typed Kernel<F> is built at fit.
  </acceptance_criteria>
  <done>PyKernelRidge + PyKernelDensity compile under `--features cpu`, reuse any_estimator!, expose score_samples, and apply GIL release + guard_f64; both registered in the pymodule.</done>
</task>

<task type="auto">
  <name>Task 2: Python smoke test (fit/predict/score_samples, f32/f64 dispatch)</name>
  <files>crates/mlrs-py/tests/test_kernel.py</files>
  <read_first>
    - crates/mlrs-py/tests/ — the existing py test harness (maturin develop / `import mlrs`, the f32/f64 dispatch + f64-on-incapable-backend skip pattern). STATE.md [06-05] (PY-03 GIL-release subprocess pattern; f64-on-incapable-backend skip-gated).
    - 08-PATTERNS.md §kernel.rs (dtype-suffixed accessors); 08-VALIDATION.md row "8-05-xx … PY-06 (share) … existing py test harness".
  </read_first>
  <action>
    Add `test_kernel.py` to the existing py harness: build the extension (maturin develop, the
    shipped flow), then a smoke test that (a) fits `PyKernelRidge` on a tiny array and asserts
    `predict` returns the expected shape and matches a small reference within the documented band;
    (b) fits `PyKernelDensity` and asserts `score_samples(Q)` returns a length-`rows(Q)` vector of
    finite log-densities. Exercise BOTH f32 and f64 dtype dispatch; gate the f64 case behind the
    backend-capability skip (skip on incapable backends, mirror the v1 f64-on-incapable-backend
    skip — STATE.md [06-05]). Keep it a SMOKE test (the ≤1e-5 / documented-tol numerical contract
    is already gated by the Rust oracle in Plans 03/04); this proves the FFI path + dtype dispatch +
    score_samples exposure end to end.
  </action>
  <verify>
    <automated>ls crates/mlrs-py/tests/test_kernel.py && grep -q "score_samples" crates/mlrs-py/tests/test_kernel.py && grep -Eq "float32|float64" crates/mlrs-py/tests/test_kernel.py && echo PY_SMOKE_PRESENT</automated>
  </verify>
  <acceptance_criteria>
    - `crates/mlrs-py/tests/test_kernel.py` exists, exercises `predict` and `score_samples`, and covers both f32 and f64 dispatch (f64 behind the backend-capability skip).
    - The py harness runs the test green after `maturin develop` (run per the shipped py-test flow; record the result in the SUMMARY).
  </acceptance_criteria>
  <done>The Python smoke test fits both estimators, calls predict + score_samples across f32/f64, and passes through the real FFI path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python caller → PyO3 estimator | Caller-supplied numpy/Arrow arrays + hyperparameters cross the FFI boundary; f64 requested on an incapable backend. Pure numeric; no network/auth/secrets/filesystem. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05-01 | Tampering | f64 requested on an f64-incapable backend (e.g. rocm) | mitigate | `guard_f64()?` called BEFORE the F64-arm upload (statically grep-verifiable); raises a clean PyError, never a device abort (STATE.md [07-07]). |
| T-08-05-02 | Tampering | malformed array geometry across FFI | mitigate | Geometry validated at the Rust estimator boundary (Plans 03/04 validate-before-launch); the wrapper forwards typed AlgoError → PyError via `algo_err_to_py`. |
| T-08-05-03 | Denial of Service | GIL held during long compute | mitigate | `py.detach(|| …)` releases the GIL during device compute (PY-03 contract, mirror covariance.rs). |
| T-08-05-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase (pyo3 stays 0.28, no new crate; sklearn/numpy build-time-only oracle deps). Package Legitimacy Gate not applicable. |
</threat_model>

<verification>
- `cargo build -p mlrs-py --features cpu` green; both pyclasses registered.
- grep gates: kernel.rs has `any_estimator!` ×2, `score_samples`, `guard_f64` on F64 arms; lib.rs has `add_class::<PyKernel…>`.
- `test_kernel.py` runs green via the shipped maturin-develop py-test flow (fit/predict/score_samples, f32/f64; f64 behind capability skip).
</verification>

<success_criteria>
- PyKernelRidge (fit/predict) + PyKernelDensity (fit/score_samples) exposed via any_estimator! with zero new binding infra.
- f32/f64 runtime dispatch, GIL release (py.detach), guard_f64() on each F64 arm.
- score_samples is the one new exposed method; dtype-suffixed accessors present.
- Both pyclasses registered in the pymodule; py smoke test green.
</success_criteria>

<output>
Create `.planning/phases/08-kernel-family/08-05-SUMMARY.md` when done.
</output>
