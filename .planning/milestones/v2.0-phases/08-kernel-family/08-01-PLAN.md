---
phase: 08-kernel-family
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - crates/mlrs-algos/src/traits.rs
  - crates/mlrs-algos/src/error.rs
  - crates/mlrs-algos/src/lib.rs
  - crates/mlrs-algos/src/kernel_ridge/mod.rs
  - crates/mlrs-algos/src/density/mod.rs
  - crates/mlrs-backend/src/prims/mod.rs
  - crates/mlrs-backend/src/prims/kernel_matrix.rs
  - crates/mlrs-backend/tests/kernel_matrix_test.rs
  - crates/mlrs-algos/tests/kernel_ridge_test.rs
  - crates/mlrs-algos/tests/kernel_density_test.rs
  - scripts/gen_oracle.py
  - tests/fixtures/kernel_matrix_f32_seed42.npz  # workspace-root tests/fixtures (gen_oracle _FIXTURE_DIR)
autonomous: true
requirements: [PRIM-08, KERNEL-01, KERNEL-02]
must_haves:
  truths:
    - "ScoreSamples<F> trait exists in traits.rs and is re-exported from lib.rs"
    - "AlgoError has InvalidBandwidth/InvalidDegree/InvalidKernel variants"
    - "kernel_ridge/ and density/ module homes exist and compile (empty estimators)"
    - "prims/kernel_matrix.rs exists as a compiling stub with the Kernel<F> enum + kernel_matrix signature"
    - "Three test files compile today with #[ignore] stubs (no reference to unwritten symbols beyond the stub signatures)"
    - "gen_oracle.py produces committed kernel_matrix/kernel_ridge/kernel_density .npz fixtures"
  artifacts:
    - path: "crates/mlrs-algos/src/traits.rs"
      provides: "ScoreSamples<F> trait"
      contains: "pub trait ScoreSamples"
    - path: "crates/mlrs-algos/src/error.rs"
      provides: "Phase-8 hyperparameter guard variants"
      contains: "InvalidBandwidth"
    - path: "crates/mlrs-backend/src/prims/kernel_matrix.rs"
      provides: "Kernel<F> enum + kernel_matrix host fn signature"
      contains: "pub enum Kernel"
    - path: "scripts/gen_oracle.py"
      provides: "gen_kernel_matrix/gen_kernel_ridge/gen_kernel_density generators"
      contains: "def gen_kernel_matrix"
  key_links:
    - from: "crates/mlrs-algos/src/lib.rs"
      to: "traits::ScoreSamples"
      via: "pub use re-export"
      pattern: "ScoreSamples"
---

<objective>
Wave-0 scaffold for Phase 8 (Kernel Family). Lands the cross-cutting surface every
later plan compiles against — the `ScoreSamples<F>` trait (D-12), the three new
`AlgoError` hyperparameter-guard variants, the `Kernel<F>` enum + `kernel_matrix`
host-fn signature (D-01), the two new estimator module homes, and the Nyquist test
scaffold: three `#[ignore]` test files plus three `gen_oracle.py` generators that emit
committed `.npz` fixtures. Mirrors the Phase-7 07-01 scaffold exactly.

Purpose: Make every Wave-1/2/3 plan file-disjoint and parallel-safe by front-loading
all shared-file edits (lib.rs, traits.rs, error.rs, prims/mod.rs) and the test
scaffolding into one wave, so later plans only flesh out their own owned files.
Output: Compiling-but-stubbed surface + committed oracle fixtures.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-kernel-family/08-CONTEXT.md
@.planning/phases/08-kernel-family/08-RESEARCH.md
@.planning/phases/08-kernel-family/08-PATTERNS.md
@.planning/phases/08-kernel-family/08-VALIDATION.md
@.planning/phases/07-covariance-projection/07-01-PLAN.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ScoreSamples trait + AlgoError variants + module/registry scaffold</name>
  <files>crates/mlrs-algos/src/traits.rs, crates/mlrs-algos/src/error.rs, crates/mlrs-algos/src/lib.rs, crates/mlrs-algos/src/kernel_ridge/mod.rs, crates/mlrs-algos/src/density/mod.rs</files>
  <read_first>
    - crates/mlrs-algos/src/traits.rs:86-122 — `PartialFit<F>` (most-recent addition; same `<F: Float + CubeElement + Pod>` bound) and `Predict<F>` (return-shape analog). 08-PATTERNS.md §traits.rs lines 120-147.
    - crates/mlrs-algos/src/error.rs:54-60 (`InvalidAlpha`), :111-117 (`InvalidEps`), :186-191 (`InvalidBatchSize`) — struct-variant `#[error("…")]` style. 08-PATTERNS.md §error.rs lines 214-229 (exact new-variant text).
    - crates/mlrs-algos/src/lib.rs:37-44 (`pub mod` block) and :50-52 (`pub use traits::{…}` list).
    - crates/mlrs-algos/src/covariance/mod.rs — module-index analog (a `mod.rs` index + per-estimator file).
    - .planning/phases/07-covariance-projection/07-01-PLAN.md — the prior scaffold this mirrors.
  </read_first>
  <action>
    Add `pub trait ScoreSamples<F> where F: Float + CubeElement + Pod` to traits.rs with one
    method `fn score_samples(&self, pool: &mut BufferPool<ActiveRuntime>, x:
    &DeviceArray<ActiveRuntime, F>, shape: (usize, usize)) -> Result<DeviceArray<ActiveRuntime,
    F>, AlgoError>` returning length-`n_samples` log-densities (NOT Predict semantics, per D-12).
    Imports already present in traits.rs (Pod, CubeElement/Float, DeviceArray/BufferPool/
    ActiveRuntime, AlgoError). Add `ScoreSamples` to the `pub use traits::{…}` list in lib.rs.

    Extend AlgoError (error.rs) with three struct variants in the existing style (text per
    08-PATTERNS.md §error.rs): `InvalidBandwidth { estimator: &'static str, bandwidth: f64 }`
    (must be > 0), `InvalidDegree { estimator: &'static str, degree: f64 }` (must be >= 1, sklearn
    `Interval(Real, 1, None)`), `InvalidKernel { estimator: &'static str, kernel: String }`
    (unknown kernel name). Reuse existing `InvalidAlpha` for `alpha >= 0`; do NOT add a new alpha
    variant.

    Create the two estimator module homes: `kernel_ridge/mod.rs` and `density/mod.rs` (new
    `density/` home per RESEARCH Open Q2 — KD is not a neighbor estimator in mlrs's trait sense).
    Each is an empty-but-compiling module index (doc comment + `pub use` placeholder for the
    estimator struct the Wave-2 plans add). Register both with `pub mod kernel_ridge;` and
    `pub mod density;` in lib.rs (alphabetical with existing `pub mod` lines). This is the ONLY
    plan that edits lib.rs/traits.rs/error.rs — Wave-2 plans edit only their own module files.
  </action>
  <verify>
    <automated>cargo build -p mlrs-algos --features cpu 2>&1 | tail -5; grep -q "pub trait ScoreSamples" crates/mlrs-algos/src/traits.rs && grep -q "InvalidBandwidth" crates/mlrs-algos/src/error.rs && grep -q "ScoreSamples" crates/mlrs-algos/src/lib.rs && echo SCAFFOLD_OK</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p mlrs-algos --features cpu` succeeds.
    - traits.rs contains `pub trait ScoreSamples`; lib.rs re-exports it.
    - error.rs contains `InvalidBandwidth`, `InvalidDegree`, `InvalidKernel`.
    - lib.rs contains `pub mod kernel_ridge;` and `pub mod density;`.
  </acceptance_criteria>
  <done>ScoreSamples trait, three AlgoError variants, and both module homes exist and compile under `--features cpu`.</done>
</task>

<task type="auto">
  <name>Task 2: kernel_matrix.rs stub (Kernel<F> enum + host signature) + prims registration</name>
  <files>crates/mlrs-backend/src/prims/kernel_matrix.rs, crates/mlrs-backend/src/prims/mod.rs</files>
  <read_first>
    - crates/mlrs-backend/src/prims/covariance.rs:42-52 (imports), :151-204 (base-op → in-place map idiom), :266-273 (`launch_dims_1d`). 08-PATTERNS.md §kernel_matrix.rs lines 34-82 (the keystone analog).
    - crates/mlrs-backend/src/prims/distance.rs:79 — RBF base signature `distance::<F>(pool, x, (rows_x,cols), y, (rows_y,cols), sqrt, out)`.
    - crates/mlrs-backend/src/prims/gemm.rs:54 — linear/poly/sigmoid base signature.
    - crates/mlrs-backend/src/prims/mod.rs — existing `pub mod` registry lines.
  </read_first>
  <action>
    Create `prims/kernel_matrix.rs` as a COMPILING STUB that defines the public surface Wave-1
    fleshes out, so the Wave-0 test file compiles today. Define the typed `pub enum Kernel<F>`
    (D-01): variants `Linear`, `Rbf { gamma: F }`, `Poly { gamma: F, degree: F, coef0: F }`,
    `Sigmoid { gamma: F, coef0: F }` with the same `<F: Float + CubeElement + Pod>`-style bound
    used by the prim functions (match the generic bounds in covariance.rs). Define the host fn
    signature `pub fn kernel_matrix<F>(pool, x, shape_x: (usize,usize), y, shape_y:
    (usize,usize), kernel: Kernel<F>, out: Option<DeviceArray<…>>) -> Result<DeviceArray<…>,
    PrimError>` returning an `rows_x × rows_y` matrix (D-02 general K(X,Y)). The stub body may
    `todo!()`/`unimplemented!()` for the compute path BUT must keep the geometry-validation
    early-return so the signature and error type are real (Wave 1 fills the body). Register
    `pub mod kernel_matrix;` in prims/mod.rs (this is the only plan that edits prims/mod.rs).
    Do NOT write the map kernel here — that is Wave 1 (in mlrs-kernels).
  </action>
  <verify>
    <automated>cargo build -p mlrs-backend --features cpu 2>&1 | tail -5; grep -q "pub enum Kernel" crates/mlrs-backend/src/prims/kernel_matrix.rs && grep -q "pub mod kernel_matrix" crates/mlrs-backend/src/prims/mod.rs && echo KM_STUB_OK</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p mlrs-backend --features cpu` succeeds.
    - kernel_matrix.rs contains `pub enum Kernel` with all four variants and a `pub fn kernel_matrix` signature returning `Result<_, PrimError>`.
    - prims/mod.rs contains `pub mod kernel_matrix;`.
  </acceptance_criteria>
  <done>The Kernel<F> enum and kernel_matrix host signature exist and compile; prims/mod.rs registers the module.</done>
</task>

<task type="auto">
  <name>Task 3: gen_oracle.py generators + committed .npz fixtures + three #[ignore] test scaffolds</name>
  <files>scripts/gen_oracle.py, crates/mlrs-backend/tests/kernel_matrix_test.rs, crates/mlrs-algos/tests/kernel_ridge_test.rs, crates/mlrs-algos/tests/kernel_density_test.rs, tests/fixtures/kernel_*.npz (workspace-root)</files>
  <read_first>
    - scripts/gen_oracle.py:968-1010 (`gen_ridge`) and the `gen_covariance` generator + `main()` registration loop at :1342-1349. 08-PATTERNS.md §gen_oracle.py lines 325-354 (exact generator + registration shape, both dtypes).
    - crates/mlrs-backend/tests/incremental_svd_test.rs:45 (`F32_MERGE_TOL`), :48-64 (fixture loader + `from_f64`), :170-181 (`skip_f64_with_log` gate), :201-269 (PoolStats gate shape). 08-PATTERNS.md §kernel_matrix_test.rs lines 280-301.
    - crates/mlrs-algos/tests/ridge_test.rs:23-126 (harness: `fixture`, `host_to_f64`/`f64_to`, `assert_close` numpy-allclose), :171-179 (`skip_f64_with_log`). 08-PATTERNS.md §kernel_ridge_test lines 304-322.
    - 08-RESEARCH.md §"Oracle cases the planner must pin" (KernelRidge) and §"Oracle (D-10)" (KernelDensity) for the exact case matrix; §"Bandwidth resolution" + §"Per-kernel log-normalization" for the KD fixtures (MAX_DIM=64 → keep KernelRidge n_samples ≤ 64, Pitfall 6 / A2).
  </read_first>
  <action>
    Add three generators to gen_oracle.py mirroring `gen_ridge`, each using
    `numpy.random.default_rng(seed)` (byte-reproducible) and `np.ascontiguousarray(...).astype(dtype)`
    (row-major, the PCA fix). Register all three in `main()` over `(np.float32, np.float64)`:

    (a) `gen_kernel_matrix`: emit X, Y and the host-reference K for each of the 4 kernels
        (linear/rbf/poly/sigmoid) computed with `sklearn.metrics.pairwise.pairwise_kernels`
        (gamma resolved = 1/n_features for the None default, plus one explicit-gamma case;
        degree=3, coef0=1). Small shapes.
    (b) `gen_kernel_ridge`: `from sklearn.kernel_ridge import KernelRidge`; emit X, y, X_test,
        and `reg.predict(X_test)` for: one case per kernel (linear/rbf/poly/sigmoid), one
        2-target case (multi-RHS, D-04), one explicit-gamma + one gamma=None (D-05), and at
        least one `degree=3, coef0=1` default poly/sigmoid case. Keep `n_samples ≤ 64` (A2).
    (c) `gen_kernel_density`: `from sklearn.neighbors import KernelDensity`; fit with
        `atol=0, rtol=0` (D-10 forced-exact) for all 6 kernels (gaussian/tophat/epanechnikov/
        exponential/linear/cosine) + at least one `bandwidth='scott'` and one `'silverman'`
        case; emit X, Q, and `kde.score_samples(Q)`. Small `n` so brute-force matches the
        exact-forced tree.

    Regenerate the committed `.npz` blobs via a `/tmp` venv with numpy+scipy+scikit-learn (PEP 668
    — see [[oracle-fixture-regen-needs-venv]]); CI never runs Python, the blobs are committed.

    Create the three test files as Nyquist `#[ignore]` scaffolds (mirror the Phase-3 03-02
    scaffold pattern and Phase-7 07-01): each loads its fixtures and asserts fixture-load + shape
    well-formedness ONLY — NO reference to the not-yet-written `kernel_matrix` compute body,
    `KernelRidge`, or `KernelDensity` beyond the stub signatures already in scope, so the test
    crates compile today. Include the `skip_f64_with_log` f64 gate verbatim and the PoolStats
    memory-gate stub (ignored) in kernel_matrix_test.rs. Carry every VALIDATION.md test name as
    an `#[ignore]` stub so Wave-1/2 plans just remove `#[ignore]` and wire the real call.
  </action>
  <verify>
    <automated>cargo test --features cpu -p mlrs-backend kernel_matrix -- --list 2>&1 | tail -5; cargo test --features cpu -p mlrs-algos kernel_ridge kernel_density -- --list 2>&1 | tail -5; ls tests/fixtures/kernel_matrix_*.npz tests/fixtures/kernel_ridge_*.npz tests/fixtures/kernel_density_*.npz 2>/dev/null && echo FIXTURES_OK</automated>
  </verify>
  <acceptance_criteria>
    - Three test files compile (the `-- --list` invocations succeed and enumerate the ignored stubs).
    - gen_oracle.py contains `def gen_kernel_matrix`, `def gen_kernel_ridge`, `def gen_kernel_density`, each registered in `main()` for both dtypes.
    - Committed `kernel_matrix_*`, `kernel_ridge_*`, `kernel_density_*` `.npz` fixtures exist under workspace-root `tests/fixtures/`.
    - kernel_matrix_test.rs contains the `skip_f64_with_log` gate and an `#[ignore]` PoolStats gate stub.
  </acceptance_criteria>
  <done>Oracle fixtures are committed and three #[ignore] test scaffolds compile under the targeted commands.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host caller → estimator/prim | Caller-supplied f32/f64 arrays + hyperparameters (bandwidth, degree, gamma, coef0, alpha, kernel name) cross here. No network/auth/secrets/filesystem surface — pure numeric library. |
| build-time gen_oracle.py → committed .npz | Fixtures generated offline in a /tmp venv; CI never runs Python. Trusted, reproducible (seeded numpy RNG, no OsRng). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-01 | Tampering | AlgoError guard surface (this plan defines the variants) | mitigate | Define typed `InvalidBandwidth`/`InvalidDegree`/`InvalidKernel`; Wave-2 estimators raise them at `fit` BEFORE any device launch (ASVS V5 / V5 validate-before-launch). |
| T-08-01-02 | Information Disclosure | committed .npz fixtures | accept | Fixtures are synthetic seeded-RNG numeric data, no PII/secrets. |
| T-08-01-SC | Tampering | npm/pip/cargo installs | mitigate | This phase installs NO packages (workspace deps unchanged, pyo3 0.28; sklearn/scipy/numpy are build-time-only oracle deps in a /tmp venv). Package Legitimacy Gate not applicable (RESEARCH §Package Legitimacy Audit). |
</threat_model>

<verification>
- `cargo build -p mlrs-algos --features cpu` and `cargo build -p mlrs-backend --features cpu` both succeed.
- `cargo test --features cpu -p mlrs-backend kernel_matrix -- --list` and `-p mlrs-algos kernel_ridge kernel_density -- --list` enumerate the ignored stubs (crates compile).
- Committed `.npz` fixtures present for kernel_matrix / kernel_ridge / kernel_density, both dtypes.
- No non-stub symbol referenced before it exists (scaffold compiles independent of Wave 1/2).
</verification>

<success_criteria>
- ScoreSamples<F> trait exists + re-exported (D-12 surface).
- Three AlgoError hyperparameter-guard variants exist.
- Kernel<F> enum + kernel_matrix host signature compile (Wave-1 fills the body).
- kernel_ridge/ and density/ module homes exist.
- Three #[ignore] test scaffolds compile; oracle fixtures committed.
</success_criteria>

<output>
Create `.planning/phases/08-kernel-family/08-01-SUMMARY.md` when done.
</output>
