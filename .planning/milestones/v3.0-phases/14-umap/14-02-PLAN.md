---
phase: 14-umap
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - crates/mlrs-algos/src/manifold/umap_internals.rs
autonomous: true
requirements: [UMAP-02]
must_haves:
  truths:
    - "Per-row smooth-kNN ρ/σ matches umap-learn 0.5.12 sigmas/rhos to ≤1e-5 (f64) for all 5 metrics"
    - "Membership strengths + t-conorm fuzzy union match umap-learn graph COO rows/cols/vals to ≤1e-5 (f64) for all 5 metrics"
    - "All three host stage fns are pure host f64 numerics (no device kernel), order-deterministic"
  artifacts:
    - path: "crates/mlrs-algos/src/manifold/umap_internals.rs"
      provides: "smooth_knn_dist, compute_membership_strengths, fuzzy_union host stage fns"
      contains: "fn smooth_knn_dist"
      min_lines: 80
  key_links:
    - from: "crates/mlrs-algos/src/manifold/umap_internals.rs"
      to: "knn_dist (n,k) host f64"
      via: "per-row binary search on d - rho"
      pattern: "fn smooth_knn_dist"
    - from: "crates/mlrs-algos/tests/umap_test.rs"
      to: "umap_internals::fuzzy_union"
      via: "value-gate vs umap graph COO"
      pattern: "fuzzy_union"
---

<objective>
Land UMAP deterministic stages 1–3 as pure host f64 numerics in the `umap_internals.rs` module (created empty + wired into `mod.rs` by Plan 01): the smooth-kNN ρ/σ per-row binary search, the membership-strength formula, and the t-conorm fuzzy-set union (UMAP's symmetrization, D-04). Each is value-gated ≤1e-5 (f64) against the committed umap-learn 0.5.12 fixtures for ALL 5 metrics (D-02). No device kernel — these are host array math over the directed `(n,k)` KNN result, which is exactly why they value-gate to ≤1e-5 (host f64 matches umap-learn's own numpy/numba f64 without device-reduction-order drift).

Purpose: These three stages are the deterministic core of the fuzzy simplicial set. This plan fills ONLY `umap_internals.rs` (Plan 01 already declared the module in `mod.rs`), so it is file-disjoint from Plan 03's `umap_init.rs` and the two run in parallel in Wave 2. Plan 04 then wires `fit` over this validated, value-gated foundation.
Output: `smooth_knn_dist`, `compute_membership_strengths`, `fuzzy_union` in `umap_internals.rs`; the `smooth_knn_*` and `fuzzy_union_*` test families turn GREEN for all 5 metrics.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-umap/14-RESEARCH.md
@.planning/phases/14-umap/14-PATTERNS.md
@AGENTS.md
@.planning/phases/14-umap/14-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: smooth-kNN ρ/σ per-row binary search (host f64, value-gated × 5 metrics)</name>
  <read_first>
    - crates/mlrs-algos/src/manifold/umap_internals.rs (the empty stub Plan 01 created + wired into mod.rs — fill it; do NOT re-edit mod.rs)
    - crates/mlrs-backend/src/prims/rng.rs (host-glue numeric idiom: pure host fn, one upload, NO device kernel — the shape to mirror)
    - .planning/phases/14-umap/14-RESEARCH.md (§ Pattern 1 smooth-kNN, lines 182-187 — constants `target=log2(n_neighbors)*bandwidth`, `SMOOTH_K_TOLERANCE=1e-5`, `MIN_K_DIST_SCALE=1e-3`, `n_iter=64`, rho/local_connectivity interpolation, sigma floor; ORDER load-bearing: rho first then search on `d−rho`)
    - .planning/phases/14-umap/14-PATTERNS.md (§ Host stage fns lines 171 — `hi = inf` is HOST f64 only, the F::INFINITY ban is DEVICE-kernel-only)
    - crates/mlrs-algos/tests/umap_test.rs (the `smooth_knn_<metric>` RED tests from Plan 01 this turns GREEN)
  </read_first>
  <behavior>
    - For each row i: rho_i = nearest non-zero-distance neighbor when local_connectivity=1.0 (index=floor(1.0)=1, interpolation=0 → non_zero_dists[0]); non-integer local_connectivity interpolates per the formula.
    - Binary search finds sigma_i s.t. Σ_j exp(-(max(0, d_ij − rho_i))/sigma_i) = target=log2(n_neighbors); n_iter=64; tolerance SMOOTH_K_TOLERANCE=1e-5.
    - sigma floor: sigma_i = max(sigma_i, MIN_K_DIST_SCALE*mean(d_i.)); global fallback MIN_K_DIST_SCALE*mean(all d) when rho_i ≤ 0.
    - sigmas/rhos match umap fixture arrays to ≤1e-5 for all 5 metrics.
  </behavior>
  <action>
    In the pre-created `crates/mlrs-algos/src/manifold/umap_internals.rs`, add `smooth_knn_dist(knn_dist: &[f64], n: usize, k: usize, n_neighbors: usize, local_connectivity: f64) -> (Vec<f64> /*sigmas*/, Vec<f64> /*rhos*/)` as a pure HOST f64 routine (mirror the `rng.rs` host-glue shape — no device kernel, no `DeviceArray`). Implement rho FIRST (the local_connectivity interpolation over each row's non-zero distances), THEN the per-row binary search on `d − rho` (ORDER is load-bearing, RESEARCH Pattern 1), with the verified constants `target = log2(n_neighbors as f64)` (bandwidth=1.0), `SMOOTH_K_TOLERANCE = 1e-5`, `MIN_K_DIST_SCALE = 1e-3`, `n_iter = 64`, the per-row and global sigma floors. The search upper bound may use `f64::INFINITY` (HOST-side only — the device `F::INFINITY` ban does not apply here). Do NOT touch `mod.rs` (Plan 01 already declared the module). Tests separated from source (AGENTS.md §2) — no in-source `mod tests`.
  </action>
  <verify>
    <automated>cargo test -p mlrs-algos --features cpu --test umap_test smooth_knn 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p mlrs-algos --features cpu --test umap_test smooth_knn` passes for all 5 metrics (sigmas/rhos ≤1e-5 vs the committed `umap_fuzzy_<metric>_f64.npz` fixtures).
    - `grep -n "fn smooth_knn_dist" crates/mlrs-algos/src/manifold/umap_internals.rs` matches (source assertion).
  </acceptance_criteria>
  <done>`smooth_knn_dist` produces ρ/σ matching umap-learn 0.5.12 to ≤1e-5 (f64) across all 5 metrics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: membership strengths + t-conorm fuzzy union (host, value-gated × 5 metrics)</name>
  <read_first>
    - crates/mlrs-algos/src/manifold/umap_internals.rs (the file from Task 1 — add these two fns alongside `smooth_knn_dist`)
    - .planning/phases/14-umap/14-RESEARCH.md (§ Pattern 2 membership lines 189-190; § Pattern 3 t-conorm union lines 192-193; § Code Examples membership + t-conorm lines 282-292 — VERIFIED umap-learn 0.5.12 formula)
    - crates/mlrs-algos/tests/umap_test.rs (the `fuzzy_union_<metric>` RED tests this turns GREEN)
    - .planning/phases/14-umap/14-CONTEXT.md (D-04 — UMAP owns the t-conorm symmetrization on the directed Phase-13 graph)
  </read_first>
  <behavior>
    - membership val_ij = exp(-(max(0, d_ij − rho_i))/sigma_i); val=1.0 when (d_ij − rho_i ≤ 0) or sigma_i==0; self edges already dropped (include_self=false).
    - COO emit: rows[i*k+j]=i, cols=knn_idx[i,j], vals=val_ij.
    - t-conorm union: prod = A∘Aᵀ; G = set_op_mix_ratio*(A + Aᵀ − prod) + (1−set_op_mix_ratio)*prod; at ratio=1.0 → A + Aᵀ − A∘Aᵀ; G symmetric.
    - G's COO rows/cols/vals match the umap fixture graph to ≤1e-5 for all 5 metrics.
  </behavior>
  <action>
    Add `compute_membership_strengths(knn_idx: &[f64 or usize], knn_dist: &[f64], rhos: &[f64], sigmas: &[f64], n: usize, k: usize) -> (rows, cols, vals) COO` and `fuzzy_union(rows, cols, vals, n, set_op_mix_ratio) -> (rows, cols, vals) symmetric COO` to `umap_internals.rs`, both pure host f64. Membership uses the VERIFIED formula `val = if (d − rho ≤ 0 || sigma == 0) { 1.0 } else { exp(-(d − rho)/sigma) }` (RESEARCH Pattern 2 / Code Examples). The union builds `A` as a host hashmap/COO (n is small at fixture scale), computes the elementwise product `A∘Aᵀ`, and forms `G = mix*(A + Aᵀ − prod) + (1−mix)*prod` (RESEARCH Pattern 3 — this IS UMAP's symmetrization, D-04, over the directed Phase-13 graph). Make the COO traversal/merge order deterministic (sorted by (row,col)) so downstream consumers and the value-gate are stable.
  </action>
  <verify>
    <automated>cargo test -p mlrs-algos --features cpu --test umap_test fuzzy_union 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p mlrs-algos --features cpu --test umap_test fuzzy_union` passes for all 5 metrics (G's COO rows/cols/vals ≤1e-5 vs the committed `umap_fuzzy_<metric>_f64.npz` graph).
    - `grep -n "fn compute_membership_strengths\|fn fuzzy_union" crates/mlrs-algos/src/manifold/umap_internals.rs` matches both (source assertion).
    - The full `cargo test -p mlrs-algos --features cpu --test umap_test smooth_knn` AND `... fuzzy_union` both green (the deterministic-stages-1-3 gate, all 5 metrics).
  </acceptance_criteria>
  <done>Membership + t-conorm union produce a symmetric fuzzy graph matching umap-learn 0.5.12 COO to ≤1e-5 (f64) across all 5 metrics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → host numerics | User array distances + `n_neighbors`/`local_connectivity`/`set_op_mix_ratio` enter pure host f64 routines. No device launch, no network/auth surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03 | DoS | smooth-kNN binary search non-termination | mitigate | Fixed `n_iter = 64` cap (umap-learn constant) — bounded iteration, no unbounded loop on pathological input. |
| T-14-04 | DoS / NaN | divide-by-zero (sigma=0, rho≤0, zero-degree row) | mitigate | umap's typed-zero guards: `val=1.0` when `sigma==0`; per-row + global `MIN_K_DIST_SCALE` sigma floor; rho≤0 global fallback — no NaN propagation. |
| T-14-05 | Tampering | OOB on `knn_idx` column values | mitigate | Membership COO indexes `cols=knn_idx[i,j]` from the Phase-13 prim output (already bounds-validated `< n`); host array indexing panics rather than OOB-reads. No device GATHER here. |

No `high`-severity threats: pure host f64 numerics with bounded iteration and umap's documented zero-guards; no device launch, no untrusted execution path.
</threat_model>

<verification>
- `cargo test -p mlrs-algos --features cpu --test umap_test smooth_knn` green (5 metrics).
- `cargo test -p mlrs-algos --features cpu --test umap_test fuzzy_union` green (5 metrics).
- `cargo build -p mlrs-algos --features cpu` exits 0.
</verification>

<success_criteria>
- smooth-kNN ρ/σ ≤1e-5 vs umap-learn 0.5.12 × 5 metrics (UMAP-02).
- membership + t-conorm union graph COO ≤1e-5 × 5 metrics (UMAP-02, D-04 symmetrization).
- All host f64, no device kernel; deterministic COO ordering; `mod.rs` untouched (Plan 01 owns it).
</success_criteria>

<output>
Create `.planning/phases/14-umap/14-02-SUMMARY.md` when done.
</output>
