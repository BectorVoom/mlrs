---
phase: 14-umap
plan: 05
type: execute
wave: 4
depends_on: ["14-04"]
files_modified:
  - crates/mlrs-algos/src/manifold/umap.rs
  - crates/mlrs-algos/src/manifold/umap_internals.rs
  - crates/mlrs-algos/tests/umap_test.rs
autonomous: true
requirements: [UMAP-04]
must_haves:
  truths:
    - "transform(X_new) embeds new points against the fitted fuzzy graph via KNN(new→train)→membership→init_graph_transform→frozen-subset SGD on the SAME umap_layout_step kernel"
    - "Transformed new points pass the property sub-gate (trustworthiness ≥ umap−ε) vs umap-learn 0.5.12 for all 5 metrics"
    - "transform reproduces byte-identical output for a fixed random_state per (backend,dtype); the stale zeros-embedding shell tests are replaced by real-contract assertions"
  artifacts:
    - path: "crates/mlrs-algos/src/manifold/umap.rs"
      provides: "real transform body (frozen-subset path)"
      contains: "init_graph_transform"
  key_links:
    - from: "crates/mlrs-algos/src/manifold/umap.rs"
      to: "mlrs_kernels::umap_layout_step (move_other=false, owners=new points only)"
      via: "frozen-subset SGD over training coords read-only"
      pattern: "umap_layout_step"
    - from: "crates/mlrs-algos/src/manifold/umap.rs"
      to: "query-vs-train KNN (distance + top_k composed in-estimator)"
      via: "X_new-vs-X_train neighbors (no self-drop, new≠train)"
      pattern: "top_k|distance"
---

<objective>
Land UMAP's `transform(X_new)` via the full umap-learn frozen-subset path (D-03): KNN(new→train) → fuzzy membership of the new points against the fitted graph → `init_graph_transform` neighbor-weighted-average init → reduced-epoch SGD optimizing ONLY the new points with the training embedding frozen (read-only GATHER targets), driving the SAME `umap_layout_step` kernel from Plan 04 (owners = new points only, `move_other=false`). Gate the new points with a property sub-gate (trustworthiness ≥ umap−ε) and byte-identical reproducibility (D-05). Replace the now-stale zeros-embedding shell tests (`fit_roundtrip`, `fit_no_leak`) so they assert the real fit contract rather than the old trivial zeros.

Purpose: `transform` is UMAP-04, the last in-scope behavior, and the reason the layout kernel was built frozen-subset-capable from day one. It depends on Plan 04 (the kernel + the fitted `fit` path it consumes) and touches `umap.rs` after Plan 04, so it is the final wave. The stale shell tests must be updated here — leaving them asserting a zeros embedding would contradict the real fit.
Output: real `transform` body in `umap.rs`; `init_graph_transform` host fn; query-vs-train KNN composition; the `transform_property_*` test GREEN; the replaced shell tests asserting the real contract.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-umap/14-RESEARCH.md
@.planning/phases/14-umap/14-PATTERNS.md
@.planning/phases/14-umap/14-CONTEXT.md
@AGENTS.md
@.planning/phases/14-umap/14-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: query-vs-train KNN composition + init_graph_transform host fn</name>
  <read_first>
    - crates/mlrs-backend/src/prims/knn_graph.rs (the X-vs-X-only prim — transform needs X_new-vs-X_train; RESEARCH Pitfall 5 / Q2/A2: compose `distance` + `top_k` in-estimator, no self-drop since new≠train)
    - crates/mlrs-kernels/src/distance.rs + crates/mlrs-kernels/src/topk.rs (the `distance` + `top_k` building blocks to compose for the query-vs-train neighbors)
    - crates/mlrs-algos/src/manifold/umap_internals.rs (Plan 02: `smooth_knn_dist`, `compute_membership_strengths` — reused for the new points' OWN knn distances)
    - .planning/phases/14-umap/14-RESEARCH.md (§ Pattern 7 transform path lines 219-224 — the 5-step recipe; init_graph_transform row-normalized weighted avg; n_epochs=100; move_other=False; § Q2/A2)
    - .planning/phases/14-umap/14-PATTERNS.md (§ transform frozen-subset pattern lines 84 — keep the `p != n_features_in_` ShapeMismatch guard; replace the zeros body)
  </read_first>
  <action>
    Add `init_graph_transform` to `umap_internals.rs`: `init_graph_transform(transform_graph_rows, cols, vals, embedding_train: &[f64], m, n, n_components) -> Vec<f64>` computing `init[new_i] = Σ_j (graph_ij / rowsum_i) · embedding_train[col_j]` — the row-normalized weighted average of trained neighbor coords (RESEARCH Pattern 7 step 3). Compose the query-vs-train KNN in `umap.rs` (or a small private helper): `distance(X_new, X_train)` (m×n) + `top_k(k)` directly — NO self-drop (new≠train, RESEARCH Q2/A2), the resolution of Pitfall 5. Reuse the Plan-02 `smooth_knn_dist` + `compute_membership_strengths` on the new points' OWN knn distances against the SAME constants to build the transform membership graph. On any cubecl build error consult the error guideline FIRST (AGENTS.md §4).
  </action>
  <verify>
    <automated>cargo build -p mlrs-algos --features cpu 2>&1 | tail -5; grep -n "fn init_graph_transform" crates/mlrs-algos/src/manifold/umap_internals.rs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "fn init_graph_transform" crates/mlrs-algos/src/manifold/umap_internals.rs` matches (source assertion).
    - The query-vs-train KNN composes `distance` + `top_k` (no self-drop): `grep -n "top_k\|distance" crates/mlrs-algos/src/manifold/umap.rs` shows the composition (no `knn_graph(... include_self ...)` self-graph misuse for transform — Pitfall 5).
    - `cargo build -p mlrs-algos --features cpu` exits 0.
  </acceptance_criteria>
  <done>Query-vs-train KNN composes `distance`+`top_k` (no self-drop) and `init_graph_transform` produces the neighbor-weighted-average init for new points.</done>
</task>

<task type="auto">
  <name>Task 2: real transform body (frozen-subset SGD) + property sub-gate, replace stale shell tests</name>
  <read_first>
    - crates/mlrs-algos/src/manifold/umap.rs (lines 444-477 — the current trivial-zeros `transform` to REPLACE; KEEP the `p != self.n_features_in_` ShapeMismatch guard + `validate_geometry`; the `Transform` impl is Fitted-only)
    - crates/mlrs-kernels/src/umap_layout.rs (Plan 04: drive `umap_layout_step` with owners = new points only placed contiguously AFTER the n frozen training rows, `move_other=false`)
    - crates/mlrs-algos/src/manifold/umap.rs host epoch driver (Plan 04: reuse the per-epoch alpha decay + host neg-sample draws; for transform `n_epochs=100` when fit-time was None — RESEARCH Pattern 7 step 4)
    - crates/mlrs-algos/tests/umap_test.rs (the `transform_property_*` RED test; AND the stale `fit_roundtrip`/`fit_no_leak` that assert a ZEROS embedding — these MUST be replaced, per the carry-through reminder)
    - .planning/phases/14-umap/14-RESEARCH.md (§ Pattern 7 steps 4-5; § Validation transform sub-gate line 357; D-05 transform byte-identical)
    - .planning/phases/14-umap/14-CONTEXT.md (carry-through: the shell tests asserting a ZEROS embedding MUST be updated/replaced when real fit lands)
  </read_first>
  <action>
    Replace the trivial-zeros `transform` body in `umap.rs` (keep the Fitted-only `Transform` impl, the `validate_geometry`, and the `p != self.n_features_in_` → `ShapeMismatch` guard): run the Plan-1 query-vs-train KNN + Plan-02 membership → `init_graph_transform` (Task 1) → place the m new-point init coords contiguously AFTER the n frozen training rows in a combined embedding buffer → drive the SAME `umap_layout_step` host epoch driver with `owners = m new points`, `move_other = false`, `n_epochs = 100` (when fit-time `n_epochs` was None; else the reduced count), training coords read-only (D-03). Return the m new-point coordinates `(m, n_components)`. All neg-sample/SGD RNG draws order-deterministic from `random_state` (D-05 — transform byte-identical per backend+dtype). Then REPLACE the stale shell tests: rewrite `fit_roundtrip` to assert the real fit produces a finite, non-zeros `(n, n_components)` embedding with `n_features_in()==p` (NOT the old all-zeros assertion — that contract is gone), and keep `fit_no_leak` as a re-fit memory gate against the REAL fit (still a valid PoolStats no-growth check). Finalize `transform_property_<metric>` to assert the new points' trustworthiness ≥ umap−ε (the calibrated Plan-04 threshold) for all 5 metrics, plus a transform reproducibility assertion (byte-identical across two transform runs, same `random_state`). On any cubecl build error consult the error guideline FIRST (AGENTS.md §4).
  </action>
  <verify>
    <automated>cargo test -p mlrs-algos --features cpu --test umap_test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p mlrs-algos --features cpu --test umap_test transform_property` passes for all 5 metrics (new points' trustworthiness ≥ umap−ε — UMAP-04 property sub-gate).
    - The full `cargo test -p mlrs-algos --features cpu --test umap_test` is GREEN end-to-end (all value-gate + property-gate + reproducibility + transform + the rewritten shell tests).
    - `grep -n "init_graph_transform\|umap_layout_step\|move_other" crates/mlrs-algos/src/manifold/umap.rs` confirms the frozen-subset transform path wired.
    - The stale zeros assertions are GONE: `grep -n "all zeros\|== 0.0" crates/mlrs-algos/tests/umap_test.rs` no longer asserts a zeros embedding in `fit_roundtrip` (the old trivial contract is replaced).
    - `cargo build -p mlrs-algos --features cpu` exits 0.
  </acceptance_criteria>
  <done>`transform(X_new)` runs the frozen-subset SGD on the SAME kernel (move_other=false), passes the property sub-gate for all 5 metrics + transform reproducibility, and the stale zeros shell tests are replaced by real-contract assertions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → transform | User `X_new` (m×d) enters `transform`; column count validated against the fitted `n_features_in_` before any launch. No network/auth surface. |
| host driver → device kernel | Frozen-subset launch geometry (owners=m, training rows read-only) crosses into the device GATHER; the SAME validated kernel as Plan 04. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-15 | Tampering | wrong-shape `X_new` (col count ≠ fit-time) | mitigate | Keep the existing `p != self.n_features_in_` → `PrimError::ShapeMismatch` guard (umap.rs:466) — typed error, never a wrong-shape device read. |
| T-14-16 | Tampering / DoS | OOB GATHER on the frozen training coords | mitigate | Owners placed contiguously AFTER the n training rows; kernel bounds-checks index `< n+m`; query-vs-train neg indices drawn `next_below` in range — the same geometry-validation discipline as Plan 04. |
| T-14-17 | Tampering | device RNG breaking transform D-05 | mitigate | Transform reuses the host `SplitMix64` substream plumbing (no device RNG) → byte-identical transform per (backend,dtype). |
| T-14-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs (reuse in-repo prims/kernels). No [ASSUMED]/[SUS] package → no blocking-human checkpoint. |

No `high`-severity threats: the transform reuses the Plan-04 validated kernel + host RNG, keeps the typed shape guard, and adds no new untrusted-input or install surface.
</threat_model>

<verification>
- `cargo test -p mlrs-algos --features cpu --test umap_test transform_property` green (5 metrics).
- Full `cargo test -p mlrs-algos --features cpu --test umap_test` green end-to-end.
- Stale zeros shell-test assertions replaced by real-contract assertions.
- `cargo build -p mlrs-algos --features cpu` exits 0.
</verification>

<success_criteria>
- `transform(X_new)` embeds new points via the frozen-subset path on the SAME kernel (D-03), property-sub-gated for all 5 metrics (UMAP-04).
- Transform byte-identical reproducibility per (backend,dtype) (D-05).
- The Phase-12 zeros shell tests are updated to the real fit contract (carry-through reminder honored).
</success_criteria>

<output>
Create `.planning/phases/14-umap/14-05-SUMMARY.md` when done.
</output>
