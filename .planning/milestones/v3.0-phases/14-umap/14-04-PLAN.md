---
phase: 14-umap
plan: 04
type: execute
wave: 3
depends_on: ["14-02", "14-03"]
files_modified:
  - crates/mlrs-kernels/src/umap_layout.rs
  - crates/mlrs-kernels/src/lib.rs
  - crates/mlrs-algos/src/manifold/umap.rs
  - crates/mlrs-algos/tests/umap_test.rs
  - .planning/phases/14-umap/14-VALIDATION.md
autonomous: true
requirements: [UMAP-01, UMAP-03]
must_haves:
  truths:
    - "umap_layout_step<F> launches under --features cpu (verified at launch, not just compile) and on rocm f32, with NO SharedMemory/Atomic/F::INFINITY/mutable-bool/shift-loop/cross-sibling accumulator"
    - "Umap::fit/fit_transform produce embedding_ (n, n_components) via KNN→fuzzy→union→init→a/b→SGD layout for all 5 metrics"
    - "The SGD layout passes the property gate vs umap-learn 0.5.12 (trustworthiness/kNN-overlap ≥ umap−ε, downstream-ARI in band) and the same random_state reproduces byte-identical embeddings across runs per (backend,dtype)"
  artifacts:
    - path: "crates/mlrs-kernels/src/umap_layout.rs"
      provides: "umap_layout_step<F> vertex-owner GATHER SGD kernel, frozen-subset-capable"
      contains: "umap_layout_step"
    - path: "crates/mlrs-algos/src/manifold/umap.rs"
      provides: "real fit/fit_transform bodies + host epoch driver + Metric→knn_graph::Metric mapping"
      contains: "knn_graph"
    - path: ".planning/phases/14-umap/14-VALIDATION.md"
      provides: "calibrated ε / ARI-band thresholds recorded from the first fixture run"
      contains: "ε"
  key_links:
    - from: "crates/mlrs-algos/src/manifold/umap.rs"
      to: "mlrs_kernels::umap_layout_step"
      via: "host epoch driver launch per epoch with host-drawn neg_idx buffer"
      pattern: "umap_layout_step"
    - from: "crates/mlrs-algos/src/manifold/umap.rs"
      to: "umap_internals + umap_init host stages"
      via: "fit pipeline orchestration"
      pattern: "smooth_knn_dist|fuzzy_union|spectral_init|fit_ab"
---

<objective>
SPIKE-GATED core of Phase 14: author the ONE genuinely new device kernel `umap_layout_step<F>` (vertex-owner GATHER SGD step, frozen-subset-capable from day one per D-03), confirm it launches under cpu-MLIR (Spike flag item 1), then wire `Umap::fit`/`fit_transform` as a host epoch driver orchestrating the full deterministic pipeline (KNN graph → Plan-02 fuzzy/union → Plan-03 spectral/random init + a/b) into the stochastic SGD layout with order-deterministic host-drawn negative sampling (D-05). Calibrate the property-gate thresholds (ε / ARI band) on the first fixture run (Spike flag item 2) and record them in 14-VALIDATION.md. This plan must come AFTER Plans 02/03 because it consumes their validated host stages.

Purpose: This is the only new compute artifact in the phase and the named cpu-MLIR feasibility unknown. The kernel must obey every spike landmine (no SharedMemory/Atomic/F::INFINITY/mutable-bool/shift-loop; `CUBE_POS_X`/`UNIT_POS_X==0` launch; no cross-sibling-loop accumulator). The frozen-subset mode is required from day one so Plan 05's `transform` drives the SAME kernel.
Output: `umap_layout_step` kernel + re-export; real `fit`/`fit_transform` bodies + host epoch driver in `umap.rs`; calibrated thresholds in 14-VALIDATION.md; the `layout_property_*` and `reproducible_*` tests turn GREEN.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-umap/14-RESEARCH.md
@.planning/phases/14-umap/14-PATTERNS.md
@.planning/phases/14-umap/14-VALIDATION.md
@.planning/phases/14-umap/14-CONTEXT.md
@AGENTS.md
@.claude/skills/spike-findings-mlrs/SKILL.md
@.planning/phases/14-umap/14-02-SUMMARY.md
@.planning/phases/14-umap/14-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: author umap_layout_step&lt;F&gt; vertex-owner GATHER SGD kernel + launch-smoke proof (cpu-MLIR-safe, frozen-subset)</name>
  <read_first>
    - .claude/skills/spike-findings-mlrs/SKILL.md (R-8 launch shape + no-cross-sibling-accumulator; banned ops) AND references/cpu-mlir-kernel-authoring.md (the proven op-set + 002-A launch landmine + 002-B silent-miscompile landmine)
    - crates/mlrs-kernels/src/topk.rs (lines 62-67 — `CUBE_POS_X`/`UNIT_POS_X==0` launch shape COPY-VERBATIM; lines 119-177 — the self-contained-nested accumulate shape that avoids 002-B)
    - crates/mlrs-kernels/src/sgd.rs (lines 61-124 — `F`/`u32`-only forward `while` GATHER math; the two-pass SGD gradient idiom)
    - crates/mlrs-kernels/src/lib.rs (lines 30-47 — the `pub mod` + `pub use` re-export idiom; `MAX_DIM` precedent)
    - crates/mlrs-backend/src/prims/distance.rs (the `F::powf` STATIC form proven under cpu-MLIR — never instance `x.powf()`)
    - .planning/phases/14-umap/14-RESEARCH.md (§ Pattern 5 lines 200-210 — gradient formulas in SQUARED distance, clip ±4 without F::INFINITY, frozen-subset mode; § Code Examples lines 303-317 VERIFIED grads)
    - .planning/phases/14-umap/14-PATTERNS.md (§ umap_layout.rs lines 88-130 — launch-shape COPY, anti-pattern guard, gradient math, pow form, clip idiom, frozen-subset, neg_idx-as-buffer, re-export)
    - /home/user/Documents/workspace/cubecl_manual/manual/Cubecl/INDEX.md (AGENTS.md §3 — READ the manual before writing the kernel; on any build error consult cubecl_error_guideline.md FIRST)
  </read_first>
  <action>
    Create `crates/mlrs-kernels/src/umap_layout.rs` with `#[cube(launch)] pub fn umap_layout_step<F: Float + CubeElement>(...)`: one OWNER row per cube, `let row = CUBE_POS_X; if row < n_owners { if UNIT_POS_X == 0u32 { … } }` (the topk.rs shape — NEVER bare `ABSOLUTE_POS` 1D, 002-A). Inside the single owner-loop body, walk the owner's positive edges (attractive) then its negative samples (repulsive), accumulating the per-dim coordinate delta in an `F` accumulator read WITHIN THE SAME outer iteration (no cross-sibling-loop accumulator — 002-B silent miscompile). Gradients run in SQUARED distance using the VERIFIED formulas (RESEARCH Pattern 5 / Code Examples): attractive `grad = (-2·a·b·F::powf(dist²,b−1))/(a·F::powf(dist²,b)+1)` (0 at dist²≤0); repulsive `grad = (2·gamma·b)/((0.001+dist²)·(a·F::powf(dist²,b)+1))` (skip when neg-index==positive-target, `grad_d=4.0` at dist²==0); per-dim `grad_d = clip(grad·(cur_d−other_d), −4, 4)`; `cur_d += grad_d·alpha`; `if move_other { other_d −= grad_d·alpha }`. Use the STATIC `F::powf`, NEVER instance `x.powf()`. Implement `clip` with statement-form `if` and FINITE literals (hi=4.0, lo=−4.0) — NO `F::INFINITY`, NO `max`/`min` intrinsic (banned constant landmine). Frozen-subset (D-03): the kernel takes `n_owners` and writes only `embedding[owner]`; non-owner coords are read-only GATHER targets; `move_other` toggles two-sided update. Negative-sample indices arrive as a pre-packed host-drawn `neg_idx` device BUFFER the kernel GATHERs — NO in-kernel/device RNG (banned, backend-divergent, breaks D-05). Generic `<F: Float + CubeElement>`, NO backend feature. Add `pub mod umap_layout;` + `pub use self::umap_layout::umap_layout_step;` to `lib.rs` (the sgd/topk re-export idiom). Add a launch-smoke test (separate test file or the existing kernels smoke harness) that LAUNCHES the kernel on a tiny fixed input under `--features cpu` (f32 AND f64) and asserts a non-zero, plausible coordinate change (NOT just non-panic — a happy-path-only check hides the 002-B miscompile, R-9). On ANY cubecl build error, consult `cubecl_error_guideline.md` FIRST (AGENTS.md §4).
  </action>
  <verify>
    <automated>cargo test -p mlrs-kernels --features cpu umap_layout 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p mlrs-kernels --features cpu umap_layout` LAUNCHES the kernel and passes (verified-at-launch under cpu-MLIR, f32 AND f64 — the Spike flag item 1 feasibility gate).
    - `grep -n "CUBE_POS_X\|UNIT_POS_X == 0" crates/mlrs-kernels/src/umap_layout.rs` confirms the safe launch shape (no bare `ABSOLUTE_POS`).
    - `grep -nE "SharedMemory|Atomic|INFINITY|\.powf\(" crates/mlrs-kernels/src/umap_layout.rs` returns NO matches (no banned ops; instance `.powf` absent — static `F::powf` only).
    - `grep -n "umap_layout_step" crates/mlrs-kernels/src/lib.rs` confirms the re-export.
    - The smoke test asserts a VALUE change (coordinates move), not just non-panic (R-9 — catches the 002-B silent miscompile).
  </acceptance_criteria>
  <done>`umap_layout_step<F>` launches under cpu-MLIR (f32+f64) and rocm f32, obeys every landmine, supports frozen-subset + move_other, and a value-asserting smoke test proves it moves coordinates.</done>
</task>

<task type="auto">
  <name>Task 2: host epoch driver + real fit/fit_transform bodies (full pipeline orchestration, order-deterministic RNG)</name>
  <read_first>
    - crates/mlrs-algos/src/manifold/umap.rs (lines 372-421 — the current trivial `fit` body to REPLACE; keep the typestate signature + `validate_geometry`; the `_y` ignore)
    - crates/mlrs-backend/src/prims/sgd.rs (lines 149-427 `sgd_solve` — the host epoch driver precedent: validate→epoch loop→per-step launch→readback→cap; lines 235-258 the cubecl-0.10 `ArrayArg::from_raw_parts` 2-arg launch idiom; lines 216-418 epoch-loop+alpha-decay shell)
    - crates/mlrs-backend/src/prims/knn_graph.rs (lines 127-140 `knn_graph<F>(pool, x, (n,d), k, metric, include_self, p)` — call with `include_self=false`; lines 57-75 the `Metric` shape to map onto)
    - crates/mlrs-backend/src/prims/rng.rs (SplitMix64 `next_below` unbiased neg-sample draws, `permutation` for shuffle — RESEARCH Pattern 6)
    - crates/mlrs-algos/src/manifold/umap_internals.rs (Plan 02: `smooth_knn_dist`, `compute_membership_strengths`, `fuzzy_union`)
    - crates/mlrs-algos/src/manifold/umap_init.rs (Plan 03: `fit_ab`, `spectral_init`, `random_init`, `noisy_scale_coords`)
    - .planning/phases/14-umap/14-RESEARCH.md (§ Architecture diagram lines 113-146 — pipeline order; § Pattern 6 neg-sample plumbing; § Discretion `n_epochs=None`→500 small/200 large @10000 threshold — Assumption A4)
    - .planning/phases/14-umap/14-PATTERNS.md (§ Host epoch driver lines 134-157 — COPY the launch idiom + epoch/alpha/RNG plumbing; § fit signature lines 56-71 + KNN call + Metric mapping)
  </read_first>
  <action>
    Replace the trivial-zeros `fit` body in `umap.rs` with the real pipeline (keep the typestate `fit(self,...) -> Result<Umap<F,Fitted>,AlgoError>` signature + `validate_geometry(x, shape)?` BEFORE any launch): (1) map `self.metric`/`p` → `knn_graph::Metric` (the no-lossy mapping the extended enum enables), call `knn_graph::<F>(pool, x, (n,d), self.n_neighbors, metric, /*include_self=*/false, p)?`; (2) host-readback knn_dist/knn_idx to f64, run `smooth_knn_dist` → `compute_membership_strengths` → `fuzzy_union` (Plan 02); (3) derive `(a,b)` via `fit_ab` (Plan 03) unless `self.a`/`self.b` override; (4) build the init: `spectral_init` (Plan 03, falls back to `random_init` above n=64) when `init=Spectral`, else `random_init`; (5) compute `epochs_per_sample` from the symmetric graph + `make_epochs_per_sample`, resolve `n_epochs=None`→500 (n≤10000) / 200 (else) (Assumption A4); (6) run the host epoch driver mirroring `sgd_solve` (PATTERNS lines 134-157): per epoch `alpha = learning_rate*(1 − epoch/n_epochs)`, draw negative-sample indices HOST-side via a `SplitMix64` substream seeded as a pure function of `(random_state, epoch, edge_id)` using `next_below(n)` (unbiased, NEVER `% n`), pack into a per-epoch `neg_idx` device buffer, launch `umap_layout_step` over owners=all n with `move_other=true`. Implement `fit_transform` as `fit` then return the host embedding (or the standard fit-then-embedding accessor). Store the final `embedding_` device array + `n_features_in_=d`. All RNG draws order-deterministic (D-05). Provide the `Metric→knn_graph::Metric` mapping as a small private fn in `umap.rs`. Apply a finite epoch/iteration discipline (the `sgd` cap precedent — ASVS V5). On any cubecl build error consult the error guideline FIRST (AGENTS.md §4).
  </action>
  <verify>
    <automated>cargo test -p mlrs-algos --features cpu --test umap_test layout_property 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p mlrs-algos --features cpu --test umap_test layout_property` runs the real fit and produces an `embedding_` of shape `(n, n_components)` for all 5 metrics (RED→exercised; the property assertion itself is finalized in Task 3 after calibration).
    - `grep -n "knn_graph\|smooth_knn_dist\|fuzzy_union\|fit_ab\|spectral_init\|umap_layout_step" crates/mlrs-algos/src/manifold/umap.rs` confirms the full pipeline is wired (source assertion).
    - `grep -n "next_below\|SplitMix64" crates/mlrs-algos/src/manifold/umap.rs` confirms HOST neg-sample draws (no device RNG); `grep -n "% n\|% (n" crates/mlrs-algos/src/manifold/umap.rs` returns no biased-modulo draw.
    - `grep -n "include_self" crates/mlrs-algos/src/manifold/umap.rs` shows the KNN call uses `false` (the UMAP self-drop path).
    - `cargo build -p mlrs-algos --features cpu` exits 0.
  </acceptance_criteria>
  <done>`fit`/`fit_transform` run the full KNN→fuzzy→union→init→a/b→SGD pipeline for all 5 metrics with order-deterministic host RNG, producing a real `(n, n_components)` embedding.</done>
</task>

<task type="auto">
  <name>Task 3: calibrate property-gate thresholds + reproducibility, turn layout_property and reproducible GREEN</name>
  <read_first>
    - crates/mlrs-algos/tests/umap_test.rs (the `layout_property_*` and `reproducible_*` tests from Plan 01 with TODO threshold consts to finalize)
    - .planning/phases/14-umap/14-VALIDATION.md (the "Calibrated thresholds (ε, ARI band) recorded here after the first oracle-fixture/spike run" placeholder lines 53-55)
    - .planning/phases/14-umap/14-RESEARCH.md (§ Property-gate metric definitions + Calibration protocol lines 370-374 — set ε = (umap_score − mlrs_score) margin with a small safety buffer, kept tight; § D-04 track umap-learn TIGHTLY; Q4 calibration)
    - .planning/phases/14-umap/14-CONTEXT.md (D-04 relative-to-oracle gate; D-05 byte-identical fit reproducibility per (backend,dtype))
  </read_first>
  <action>
    Run the property-gate metric helpers (Plan 01: trustworthiness / kNN-overlap / downstream-ARI) on mlrs's and umap's embeddings on the committed `umap_layout_<metric>_f64.npz` fixtures' identical seeded data. Compute the per-metric margins `ε = (umap_score − mlrs_score)` with a small tight safety buffer (D-04 — track umap-learn tightly, relative-to-oracle, NOT absolute floors) and the downstream-ARI band. Replace the TODO threshold consts in `umap_test.rs` with the calibrated values and RECORD them (per metric) in 14-VALIDATION.md's calibration section (so the gate is reproducible — Spike flag item 2). Finalize `layout_property_<metric>` to assert `trustworthiness ≥ umap−ε ∧ kNN-overlap ≥ umap−ε ∧ ARI in band` for all 5 metrics. Finalize `reproducible_<dtype>`: two `fit` runs with the same `random_state` produce a BYTE-IDENTICAL embedding (per backend+dtype — f64 on cpu; D-05). If a metric's margin is borderline (RESEARCH acknowledged risk), iterate the threshold within the tight buffer OR flag the specific metric for a follow-up — do NOT loosen the gate to absolute floors (that violates D-04).
  </action>
  <verify>
    <automated>cargo test -p mlrs-algos --features cpu --test umap_test 'layout_property|reproducible' 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p mlrs-algos --features cpu --test umap_test layout_property` passes for all 5 metrics with the calibrated relative-to-umap thresholds (UMAP-03 property gate).
    - `cargo test -p mlrs-algos --features cpu --test umap_test reproducible` passes (byte-identical embedding across two same-`random_state` fit runs, per backend+dtype — D-05).
    - `grep -n "ε\|epsilon\|trustworthiness\|ARI" .planning/phases/14-umap/14-VALIDATION.md` shows the calibrated per-metric thresholds recorded (not the placeholder).
    - The thresholds are RELATIVE (`≥ umap − ε`), not absolute floors (`grep` the test shows `umap_score - ` style comparison — D-04 honored).
  </acceptance_criteria>
  <done>Property-gate thresholds calibrated and recorded in 14-VALIDATION.md; `layout_property` (5 metrics, relative-to-umap) and `reproducible` (byte-identical) GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host driver → device kernel | Host-validated launch geometry (`n_owners`, edge buffers, `neg_idx`) crosses into `unsafe { ArrayArg::from_raw_parts }` and the device GATHER. |
| caller → fit | User `X` + hyperparameters (`n_neighbors`, `metric`, `p`, `n_epochs`, `random_state`) enter `fit`; geometry validated before any launch. No network/auth surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-10 | Tampering / DoS | OOB device GATHER (bad `n_owners` / neg-index ≥ n) | mitigate | Host-validate all launch geometry before `ArrayArg::from_raw_parts` (the `validate_geometry`/`self_drop_gather` precedent); kernel bounds-checks `row < n_owners` and GATHER index `< n_vertices`; `next_below(n)` keeps neg indices in range. |
| T-14-11 | Correctness | biased modulo in negative sampling | mitigate | `SplitMix64::next_below` rejection sampling — NEVER `% n` (the `rng.rs` ASVS-V6 contract); acceptance grep asserts no `% n`. |
| T-14-12 | DoS | unbounded epoch loop | mitigate | `n_epochs` resolved to a finite value (500/200 heuristic or user cap); the `sgd_solve` finite-loop precedent — no unbounded iteration. |
| T-14-13 | Tampering | device RNG divergence breaking D-05 | mitigate | NO in-kernel/device RNG; all draws host-side `SplitMix64` substreams seeded as a pure function of `(random_state, epoch, edge)` → byte-identical per (backend,dtype). |
| T-14-14 | DoS / NaN | F::INFINITY / NaN in the layout kernel | mitigate | Banned constant absent (acceptance grep); finite-literal clip; the `0.001 + dist²` fudge in the repulsive grad prevents divide-by-zero. |
| T-14-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (all reuse in-repo prims/kernels + new in-repo kernel). No [ASSUMED]/[SUS] package → no blocking-human checkpoint needed. |

No `high`-severity threats: every device launch is geometry-validated host-side, RNG is host-drawn and bounds-safe, iteration is finite, and the banned-constant landmines are grep-enforced absent.
</threat_model>

<verification>
- `cargo test -p mlrs-kernels --features cpu umap_layout` green (kernel launches under cpu-MLIR, f32+f64).
- `cargo test -p mlrs-algos --features cpu --test umap_test layout_property` green (5 metrics, relative property gate).
- `cargo test -p mlrs-algos --features cpu --test umap_test reproducible` green (byte-identical).
- Calibrated thresholds recorded in 14-VALIDATION.md.
- `cargo build -p mlrs-algos --features cpu` and `cargo build -p mlrs-kernels --features cpu` exit 0.
</verification>

<success_criteria>
- `umap_layout_step` launches cpu-MLIR (f32+f64) + rocm f32; no banned ops; frozen-subset-capable (UMAP-03, Spike item 1).
- `fit`/`fit_transform` produce `embedding_` for all 5 metrics via the full pipeline (UMAP-01).
- Property gate (relative-to-umap, D-04) + byte-identical reproducibility (D-05) GREEN; thresholds calibrated + recorded (Spike item 2).
</success_criteria>

<output>
Create `.planning/phases/14-umap/14-04-SUMMARY.md` when done.
</output>
