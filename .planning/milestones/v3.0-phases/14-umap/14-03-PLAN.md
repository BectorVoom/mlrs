---
phase: 14-umap
plan: 03
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - crates/mlrs-algos/src/manifold/umap_init.rs
  - crates/mlrs-algos/src/cluster/spectral.rs
autonomous: true
requirements: [UMAP-01, UMAP-02]
must_haves:
  truths:
    - "Host Levenberg–Marquardt a/b curve fit matches umap-learn find_ab_params to ≤1e-5 (f64) across the (min_dist,spread) grid"
    - "Spectral init via laplacian→eig→recover matches umap spectral_layout coords to ≤1e-5 (up-to-sign per column) for n≤64 connected fixtures, all 5 metrics"
    - "Above the Jacobi n=64 cap, init falls back to uniform(-10,10) random init via SplitMix64 (does NOT error)"
  artifacts:
    - path: "crates/mlrs-algos/src/manifold/umap_init.rs"
      provides: "fit_ab (LM), spectral_init, random_init, noisy_scale_coords host fns"
      contains: "fn fit_ab"
      min_lines: 100
  key_links:
    - from: "crates/mlrs-algos/src/manifold/umap_init.rs"
      to: "mlrs_backend::prims::{laplacian, eig}"
      via: "spectral init over the symmetric fuzzy graph"
      pattern: "laplacian"
    - from: "crates/mlrs-algos/src/manifold/umap_init.rs"
      to: "crate::cluster::spectral::recover"
      via: "slice-smallest/dd/sign-flip recovery (drop_first=true)"
      pattern: "recover"
---

<objective>
Land UMAP's two remaining deterministic value-gated stages in `umap_init.rs` (created empty + wired into `mod.rs` by Plan 01), parallel-buildable with Plan 02: (1) the host-side Levenberg–Marquardt a/b curve fit (D-06) value-gated ≤1e-5 vs umap-learn `find_ab_params`, and (2) spectral init (reusing the existing `laplacian` + `eig` + `recover` stack on the symmetric fuzzy graph, with the n≤64 Jacobi cap and the umap-specific RANDOM FALLBACK above it — UMAP falls back, does not error like SpectralEmbedding). Both value-gated ≤1e-5 (f64); spectral compared up-to-sign per column (umap's `spectral_layout` applies no sign flip; `recover` does — RESEARCH Q3/A3).

Purpose: a/b is the fifth deterministic value-gated stage (D-06); spectral/random init is the embedding seed for the SGD layout. Both must be validated before Plan 04 drives them through `fit`. This plan fills ONLY `umap_init.rs` (+ optionally a `sign_flip` flag in the shared `spectral.rs::recover`); it does NOT touch `mod.rs` (Plan 01 owns it) or `umap_internals.rs` (Plan 02 owns it), so it is file-disjoint from Plan 02 in Wave 2.
Output: `fit_ab`, `spectral_init`, `random_init`, `noisy_scale_coords` in `umap_init.rs`; an optional `sign_flip` flag threaded into `recover` if dump-diff shows it needed; the `ab_fit` and `spectral_init_*` test families turn GREEN.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-umap/14-RESEARCH.md
@.planning/phases/14-umap/14-PATTERNS.md
@AGENTS.md
@crates/mlrs-algos/src/cluster/spectral_embedding.rs
@.planning/phases/14-umap/14-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: host Levenberg–Marquardt a/b curve fit (D-06, value-gated ≤1e-5)</name>
  <read_first>
    - crates/mlrs-algos/src/manifold/umap_init.rs (the empty stub Plan 01 created + wired into mod.rs — fill it; do NOT re-edit mod.rs)
    - crates/mlrs-backend/src/prims/sgd.rs (lines 185-189 max_iter cap; lines 481-514 — the `optimal_t0`/`schedule_eta` host-f64 helper precedent the LM routine mirrors; iteration-cap + typed-error discipline)
    - .planning/phases/14-umap/14-RESEARCH.md (§ Code Examples a/b curve fit lines 294-301 — `curve(x,a,b)=1/(1+a·x^(2b))`, `xv=linspace(0, spread*3, 300)`, `yv=where(xv<min_dist, 1, exp(-(xv−min_dist)/spread))`; § D-06; Assumption A5 — LM with analytic Jacobian hits ≤1e-5)
    - .planning/phases/14-umap/14-PATTERNS.md (§ Host stage fns lines 169 — LM is NEW host numeric, NO device kernel; cap iterations + typed non-convergence error, the eig/sgd MAX_SWEEPS precedent)
    - crates/mlrs-algos/tests/umap_test.rs (the `ab_fit` RED test this turns GREEN)
  </read_first>
  <behavior>
    - Build target curve: xv=linspace(0, spread*3, 300); yv = if xv<min_dist {1.0} else {exp(-(xv−min_dist)/spread)}.
    - Fit a,b minimizing Σ(curve(xv,a,b) − yv)² via Gauss-Newton/LM with analytic Jacobian.
    - For each (min_dist,spread) grid point in the fixture, derived (a,b) match umap find_ab_params to ≤1e-5.
    - Non-convergence within the iteration cap returns a typed error, never an unbounded loop.
  </behavior>
  <action>
    In the pre-created `crates/mlrs-algos/src/manifold/umap_init.rs`, add `fit_ab(min_dist: f64, spread: f64) -> Result<(f64 /*a*/, f64 /*b*/), AlgoError>` as a self-contained HOST f64 Levenberg–Marquardt (or Gauss-Newton with LM damping — solver internals are Claude's discretion per D-06, any choice that hits ≤1e-5) fitting `1/(1 + a·x^(2b))` to the smooth target curve over `linspace(0, spread*3, 300)` (RESEARCH Code Examples). Use an analytic Jacobian (∂/∂a, ∂/∂b of the residual). Apply a finite iteration cap with a typed non-convergence error (the `sgd.rs`/`eig` MAX_SWEEPS DoS-guard precedent — ASVS V5). NO device kernel (D-06). Do NOT touch `mod.rs` (Plan 01 declared the module). Tests separated from source (AGENTS.md §2).
  </action>
  <verify>
    <automated>cargo test -p mlrs-algos --features cpu --test umap_test ab_fit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p mlrs-algos --features cpu --test umap_test ab_fit` passes (derived a,b ≤1e-5 vs the committed `umap_ab_f64.npz` grid).
    - `grep -n "fn fit_ab" crates/mlrs-algos/src/manifold/umap_init.rs` matches (source assertion).
    - The LM loop has a finite iteration cap and a typed non-convergence branch (`grep -n "max_iter\|MAX_\|NotConverged\|Err(" crates/mlrs-algos/src/manifold/umap_init.rs` shows a bounded loop + error path).
  </acceptance_criteria>
  <done>`fit_ab` derives (a,b) matching umap-learn 0.5.12 `find_ab_params` to ≤1e-5 (f64) with a capped, typed-error LM.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: spectral init (reuse laplacian+eig+recover) + random fallback + noisy scale (value-gated ≤1e-5 up-to-sign)</name>
  <read_first>
    - crates/mlrs-algos/src/cluster/spectral_embedding.rs (lines 139-143, 239-283 — the laplacian→eig→recover orchestration to mirror; lines 32-45 imports)
    - crates/mlrs-algos/src/cluster/spectral.rs (line 53 `recover` signature — `pub(crate) fn recover<F>(...)`; the slice-smallest→/dd→sign-flip→drop_first→transpose recovery; this task MAY add a `sign_flip: bool` arg if the dump-diff shows umap needs no flip)
    - crates/mlrs-backend/src/prims/laplacian.rs (line 86 `laplacian<F>` signature — `(L, dd)` symmetric-normalized `I−D^-1/2 A D^-1/2`)
    - crates/mlrs-backend/src/prims/eig.rs (line 76 `eig<F>`; lines 208-222 — `MAX_DIM=64` Jacobi cap; DESCENDING order)
    - crates/mlrs-backend/src/prims/rng.rs (lines 58-108 SplitMix64; lines 144-173 Box–Muller — reuse for noisy_scale noise=1e-4 and random init)
    - .planning/phases/14-umap/14-RESEARCH.md (§ Pattern 4 spectral init lines 195-198 — `k=n_components+1`, `argsort(eig)[1:k]` drop-first, `noisy_scale_coords(max=10, noise=1e-4)`, cap+fallback; § Pitfall 3 + Q3/A3 sign convention)
    - .planning/phases/14-umap/14-PATTERNS.md (§ Spectral-init core pattern lines 41-54 — COPY the load-bearing sequence; the up-to-sign NOTE)
    - crates/mlrs-algos/tests/umap_test.rs (the `spectral_init_<metric>` RED tests this turns GREEN, compared up-to-sign per column)
  </read_first>
  <behavior>
    - For n≤64: feed the symmetric fuzzy graph G as affinity → laplacian(G,n) → eig (descending, reverse to ascending) → recover(..., drop_first=true) → spectral coords (n, n_components).
    - Compare to umap spectral_layout coords up-to-sign PER COLUMN (umap applies no deterministic sign flip; recover does — RESEARCH Q3/A3) to ≤1e-5.
    - noisy_scale_coords: expansion=10/max|coords|; coords*=expansion; coords += SplitMix64 normal(scale=1e-4).
    - For n>64: random init uniform(-10,10) via SplitMix64 (FALLBACK, not an error — unlike SpectralEmbedding).
  </behavior>
  <action>
    Add to `umap_init.rs`: `spectral_init<F>(pool, &g_affinity: &DeviceArray, n, n_components, seed) -> Result<Vec<F>, AlgoError>` mirroring `spectral_embedding.rs:239-283` — `laplacian::<F>(pool, &g, n)?` → `eig::<F>(pool, &l, n, ...)?` (DESCENDING; reverse to ascending) → `recover::<F>(&v_host, &dd_host, n, n_components, /*drop_first=*/true)` → `noisy_scale_coords` (max=10, noise=1e-4 via SplitMix64 Box–Muller draws, RESEARCH Pattern 4). Add `random_init<F>(n, n_components, seed) -> Vec<F>` = `uniform(-10,10)` via `SplitMix64::next_f64`. Add `noisy_scale_coords` as a host helper. Implement the cap: when `n > MAX_DIM (==64)`, `spectral_init` returns the `random_init` result (the umap RANDOM FALLBACK above the Jacobi cap — Claude's-discretion v2 convention, but UMAP falls back rather than erroring; PATTERNS lines 43-45). For the up-to-sign value-gate: dump-diff first; if umap's coords differ only by per-column sign, EITHER thread a `sign_flip: bool` arg into `recover` (default true preserves the spectral-family callers) and pass `false` for the UMAP path, OR keep `recover`'s flip and have the test compare up-to-sign — pick whichever makes the ≤1e-5 gate pass and document the choice (RESEARCH A3). All RNG draws order-deterministic from `seed` (D-05 backbone; reused by Plan 04).
  </action>
  <verify>
    <automated>cargo test -p mlrs-algos --features cpu --test umap_test spectral_init 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p mlrs-algos --features cpu --test umap_test spectral_init` passes for all 5 metrics (coords ≤1e-5 up-to-sign per column vs `umap_spectral_<metric>_f64.npz`).
    - `grep -n "fn spectral_init\|fn random_init\|fn noisy_scale_coords" crates/mlrs-algos/src/manifold/umap_init.rs` matches all three (source assertion).
    - `grep -n "laplacian\|eig\|recover" crates/mlrs-algos/src/manifold/umap_init.rs` shows all three reuse-prims invoked (no re-derived Laplacian/eig — Don't-Hand-Roll).
    - A code path for `n > 64` returns `random_init` (no error) — `grep -n "MAX_DIM\|> 64\|random_init" crates/mlrs-algos/src/manifold/umap_init.rs` confirms the fallback branch.
    - If `recover`'s signature changed, the existing spectral-family tests still pass: `cargo test -p mlrs-algos --features cpu spectral 2>&1 | tail -5` (no regression to SpectralEmbedding/SpectralClustering).
  </acceptance_criteria>
  <done>Spectral init reuses laplacian+eig+recover, matches umap-learn coords ≤1e-5 up-to-sign for n≤64 across all 5 metrics, and falls back to random init above the n=64 Jacobi cap without erroring.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → host LM / init | `min_dist`/`spread`/`random_state` and the fuzzy graph enter host numerics; the spectral path makes validated `laplacian`/`eig` device launches. No network/auth surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-06 | DoS | LM a/b non-convergence | mitigate | Finite iteration cap + typed `NotConverged`-style error (the `sgd`/`eig` MAX_SWEEPS precedent, ASVS V5). |
| T-14-07 | DoS / correctness | `eig` on `n > MAX_DIM` (Jacobi cap) | mitigate | `n > 64` takes the random-init FALLBACK before any `eig` launch — never an OOB/over-cap Jacobi call (the cap is the documented fallback boundary). |
| T-14-08 | Correctness | biased random init draw | mitigate | `SplitMix64::next_f64`/`next_below` (rejection sampling) — NEVER `% n`, the `rng.rs` ASVS-V6 contract; seeded non-crypto, never `OsRng`. |
| T-14-09 | DoS / NaN | zero-degree node in Laplacian | accept | `laplacian.rs` already carries the zero-degree guard (`dd=1` for isolated node) — reused as-is, no new surface. Fixtures designed connected (RESEARCH Q1). |

No `high`-severity threats: bounded host LM, pre-launch geometry validation in the reused prims, and the documented n>64 fallback prevent over-cap/unbounded paths.
</threat_model>

<verification>
- `cargo test -p mlrs-algos --features cpu --test umap_test ab_fit` green.
- `cargo test -p mlrs-algos --features cpu --test umap_test spectral_init` green (5 metrics, up-to-sign).
- `cargo test -p mlrs-algos --features cpu spectral` green (no spectral-family regression if `recover` changed).
- `cargo build -p mlrs-algos --features cpu` exits 0.
</verification>

<success_criteria>
- a/b LM fit ≤1e-5 vs umap-learn 0.5.12 (D-06, UMAP-01/02).
- Spectral init ≤1e-5 up-to-sign × 5 metrics for n≤64; random fallback above the cap (UMAP-02).
- All RNG draws order-deterministic from `seed` (D-05 backbone for Plan 04); `mod.rs` untouched (Plan 01 owns it).
</success_criteria>

<output>
Create `.planning/phases/14-umap/14-03-SUMMARY.md` when done.
</output>
