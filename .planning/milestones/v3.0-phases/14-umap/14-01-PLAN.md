---
phase: 14-umap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/gen_oracle.py
  - crates/mlrs-algos/tests/umap_test.rs
  - crates/mlrs-algos/src/manifold/umap.rs
  - crates/mlrs-algos/src/manifold/mod.rs
  - crates/mlrs-algos/src/manifold/umap_internals.rs
  - crates/mlrs-algos/src/manifold/umap_init.rs
  - tests/fixtures/
autonomous: true
requirements: [UMAP-01, UMAP-02, UMAP-03, UMAP-04]
must_haves:
  truths:
    - "Committed umap-learn 0.5.12 oracle fixtures exist for all 5 metrics × every deterministic stage (fuzzy/spectral/ab/layout/transform)"
    - "umap_test.rs compiles and runs the value-gate + property-gate + reproducibility harness (RED-by-design against the not-yet-real fit body)"
    - "Umap::Metric carries all 5 variants mirroring knn_graph::Metric exactly (Minkowski { p: f64 })"
    - "umap_internals and umap_init module stubs exist and are wired into manifold/mod.rs (empty, compiling — Plans 02/03 fill them file-disjointly)"
  artifacts:
    - path: "scripts/gen_oracle.py"
      provides: "gen_umap_fuzzy_*, gen_umap_spectral_*, gen_umap_ab_*, gen_umap_layout_*, gen_umap_transform_* generators"
      contains: "def gen_umap_"
    - path: "crates/mlrs-algos/tests/umap_test.rs"
      provides: "value-gate, property-gate, reproducibility, transform test harness"
      contains: "load_npz"
    - path: "crates/mlrs-algos/src/manifold/umap.rs"
      provides: "Metric enum extended to 5 variants"
      contains: "Minkowski"
  key_links:
    - from: "crates/mlrs-algos/tests/umap_test.rs"
      to: "tests/fixtures/umap_*.npz"
      via: "mlrs_core::load_npz / OracleCase"
      pattern: "load_npz"
    - from: "crates/mlrs-algos/src/manifold/umap.rs"
      to: "mlrs_backend::prims::knn_graph::Metric"
      via: "metric mapping (no lossy conversion)"
      pattern: "Minkowski"
---

<objective>
Nyquist Wave 0 for Phase 14 UMAP: stand up the committed umap-learn 0.5.12 oracle fixtures (all 5 metrics × every deterministic stage), the in-repo property-gate metric helpers, and the RED-by-design value-gate + property-gate test harness in `umap_test.rs`; extend the shell's `Metric` enum to the full 5-metric set mirroring the Phase-13 prim; AND create the two empty `umap_internals`/`umap_init` module stubs wired into `manifold/mod.rs` so the Wave-2 plans (02/03) fill them WITHOUT both editing `mod.rs` (file-disjoint parallel safety). This plan creates the verification substrate every subsequent plan turns GREEN; it must compile and run (tests RED, referencing the trivial-zeros fit body that later plans replace).

Purpose: Without committed oracle intermediates and the test harness, no deterministic stage (Plans 02/03) or stochastic layout (Plan 04) can be value/property-gated. The `Metric` extension is the single enum every downstream stage routes through, and the pre-declared module stubs keep Plans 02 and 03 file-disjoint so they run in parallel in Wave 2.
Output: `gen_umap_*` generators in `scripts/gen_oracle.py`; committed `tests/fixtures/umap_*.npz` blobs; extended value/property/reproducibility/transform tests in `umap_test.rs`; 5-variant `Metric` enum in `umap.rs`; empty `umap_internals.rs`/`umap_init.rs` stubs declared in `mod.rs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-umap/14-RESEARCH.md
@.planning/phases/14-umap/14-PATTERNS.md
@.planning/phases/14-umap/14-VALIDATION.md
@.planning/phases/14-umap/14-CONTEXT.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the Umap Metric enum to 5 variants + create umap_internals/umap_init module stubs</name>
  <read_first>
    - crates/mlrs-algos/src/manifold/umap.rs (lines 41-48 — current `Euclidean`-only `Metric`; the build()/new()/into_builder()/hyperparams_eq sites already reference `Metric` and must keep compiling)
    - crates/mlrs-backend/src/prims/knn_graph.rs (lines 57-75 — the canonical `Metric` shape with `Minkowski { p: f64 }`; mirror it exactly per PATTERNS Pitfall 4)
    - crates/mlrs-algos/src/manifold/mod.rs (the module index — add `pub(crate) mod umap_internals;` and `pub(crate) mod umap_init;` declarations here so Plans 02/03 do NOT both edit mod.rs)
    - .planning/phases/14-umap/14-PATTERNS.md (§ `Metric` enum extension, lines 73-82)
  </read_first>
  <action>
    Replace the single-variant `Metric` enum in `umap.rs` with the 5 variants `Euclidean`, `Manhattan`, `Cosine`, `Chebyshev`, `Minkowski { p: f64 }` — mirroring `knn_graph::Metric` exactly so UMAP's `metric=`/`p` map straight onto the prim with no lossy conversion (D-01, Pitfall 4). Because `Minkowski { p: f64 }` carries a non-`Eq` `f64`, drop `Eq` from the `derive` (keep `Debug, Clone, Copy, PartialEq`) and confirm `hyperparams_eq` (umap.rs:159) still compiles via `PartialEq`. Document each variant with its umap-learn `metric=` string. Create the two EMPTY module stub files `crates/mlrs-algos/src/manifold/umap_internals.rs` (Plan 02's home) and `crates/mlrs-algos/src/manifold/umap_init.rs` (Plan 03's home) — each with only a module doc comment, no fns — and declare both in `manifold/mod.rs` (`pub(crate) mod umap_internals;` / `pub(crate) mod umap_init;`). This pre-creates the file ownership so Plans 02 and 03 fill their own file without both editing `mod.rs` (parallel-safe Wave 2). Do NOT wire any `fit`/`transform` body here, and do NOT add a `Metric → knn_graph::Metric` mapping fn yet (Plan 04 adds it at the KNN call site).
  </action>
  <verify>
    <automated>cargo build -p mlrs-algos --features cpu 2>&1 | tail -5; cargo test -p mlrs-algos --features cpu --test umap_test defaults_equal 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Minkowski { p: f64 }" crates/mlrs-algos/src/manifold/umap.rs` matches (source assertion).
    - `grep -n "umap_internals\|umap_init" crates/mlrs-algos/src/manifold/mod.rs` matches both module declarations.
    - `test -f crates/mlrs-algos/src/manifold/umap_internals.rs && test -f crates/mlrs-algos/src/manifold/umap_init.rs` (both stub files exist).
    - `cargo build -p mlrs-algos --features cpu` exits 0 (extended enum + empty stubs compile; existing builder/new/into_builder/hyperparams_eq sites still compile).
    - The 4 existing shell tests still pass `cargo test -p mlrs-algos --features cpu --test umap_test` (no contract regression from the enum/stub change alone).
  </acceptance_criteria>
  <done>`Metric` has all 5 variants mirroring the Phase-13 prim shape; `umap_internals`/`umap_init` stubs exist and are wired; the crate plus existing shell tests still build/pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add gen_umap_* oracle generators and commit the 5-metric × per-stage .npz fixtures</name>
  <read_first>
    - scripts/gen_oracle.py (lines 814-922 — `gen_knn_metric`: the metric-tag-in-filename + float-encoded-index precedent; lines 264-301 — the `load_npz` 4/8-byte-float-array constraint comment)
    - .planning/phases/14-umap/14-RESEARCH.md (§ Validation Architecture → "Per-stage × per-metric fixture matrix" lines 360-368; § Code Examples membership/ab/spectral formulas lines 282-326; "How to dump" line 368 — call umap-learn internals directly, do NOT recompute)
    - .planning/phases/14-umap/14-PATTERNS.md (§ umap_test.rs, lines 191 — generator naming + /tmp venv landmine)
    - MEMORY: oracle-fixture-regen-needs-venv (PEP 668 /tmp numpy venv)
  </read_first>
  <action>
    Add generators to `scripts/gen_oracle.py`, one family per deterministic stage, dumping umap-learn 0.5.12's OWN internals (import `from umap.umap_ import fuzzy_simplicial_set, find_ab_params, smooth_knn_dist` and `from umap.spectral import spectral_layout`; never recompute in numpy — Pitfall 6): (1) `gen_umap_fuzzy_<metric>_<dtype>` dumping `X`, `knn_idx`, `knn_dist`, `sigmas (n)`, `rhos (n)`, graph COO `rows/cols/vals`, plus scalar params `set_op_mix_ratio`/`local_connectivity`/`n_neighbors`; (2) `gen_umap_spectral_<metric>_<dtype>` dumping the symmetric graph + umap `spectral_layout` coords on an n≤64 CONNECTED design (RESEARCH Q1 — fixtures must be connected so the single-component `laplacian`+`eig` path matches); (3) `gen_umap_ab_<dtype>` — a `(min_dist, spread) → (a, b)` grid from `find_ab_params` (metric-independent, one fixture); (4) `gen_umap_layout_<metric>_<dtype>` dumping `X`, umap `embedding_`, true labels (for ARI), the fixed `random_state`/`n_epochs`; (5) `gen_umap_transform_<metric>_<dtype>` dumping `X_train`, `X_new`, fitted `embedding_`, umap transform output. For each metric ∈ {euclidean, manhattan, cosine, chebyshev, minkowski(p=3)} emit fuzzy/spectral/layout/transform; ab is one fixture. Encode indices as float and put the metric tag in the filename (the `gen_knn_metric` precedent — `load_npz` decodes only 4/8-byte float arrays). Run the generators in a `/tmp` venv with `numpy scipy scikit-learn umap-learn==0.5.12` (PEP 668 landmine) and COMMIT the resulting `.npz` blobs to `tests/fixtures/`. Keep the generators behind the existing `gen_oracle.py` main dispatch so they are reproducible but not run at test time.
  </action>
  <verify>
    <automated>ls tests/fixtures/umap_fuzzy_euclidean_f64.npz tests/fixtures/umap_spectral_euclidean_f64.npz tests/fixtures/umap_ab_f64.npz tests/fixtures/umap_layout_euclidean_f64.npz tests/fixtures/umap_transform_euclidean_f64.npz 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `ls tests/fixtures/umap_fuzzy_{euclidean,manhattan,cosine,chebyshev,minkowski}_f64.npz` lists all 5 metric fuzzy fixtures (file-exists, all metrics).
    - `ls tests/fixtures/umap_spectral_*_f64.npz tests/fixtures/umap_layout_*_f64.npz tests/fixtures/umap_transform_*_f64.npz` each list 5 metric blobs; `umap_ab_f64.npz` exists (single metric-independent fixture).
    - `grep -v '^#' scripts/gen_oracle.py | grep -c 'def gen_umap_'` ≥ 5.
    - Each committed `.npz` loads via `python3 -c "import numpy as np; d=np.load('tests/fixtures/umap_fuzzy_euclidean_f64.npz'); print(sorted(d.files))"` and lists `rows`, `cols`, `vals`, `sigmas`, `rhos` (dump-shape assertion).
  </acceptance_criteria>
  <done>All 5-metric × per-stage `.npz` fixtures are generated from umap-learn 0.5.12 internals and committed as blobs; generators reproduce them from the `/tmp` venv.</done>
</task>

<task type="auto">
  <name>Task 3: Build the RED value-gate + property-gate + reproducibility test harness in umap_test.rs</name>
  <read_first>
    - crates/mlrs-algos/tests/umap_test.rs (lines 1-141 — existing shell tests + the `skip_f64_with_log` gate + pool/device setup idiom to reuse)
    - .planning/phases/14-umap/14-RESEARCH.md (§ Phase Requirements → Test Map lines 348-358; § Property-gate metric definitions lines 370-374 — trustworthiness/kNN-overlap/downstream-ARI host formulas; § Validation Architecture)
    - .planning/phases/14-umap/14-PATTERNS.md (§ umap_test.rs lines 179-194 — f64 capability-gate COPY-VERBATIM, oracle-fixture value-gate, property-gate not-element-wise)
    - .planning/phases/14-umap/14-VALIDATION.md (Per-Task Verification Map; Wave 0 Requirements)
  </read_first>
  <action>
    Extend `umap_test.rs` with the RED-by-design harness referencing the (not-yet-real) fit/transform bodies, one test family per VALIDATION map row: `smooth_knn_<metric>` and `fuzzy_union_<metric>` (value ≤1e-5 f64 vs the dumped `sigmas`/`rhos`/graph COO `rows/cols/vals`), `spectral_init_<metric>` (value ≤1e-5 up-to-sign per column vs umap `spectral_layout` coords — RESEARCH Q3/A3), `ab_fit` (value ≤1e-5 vs umap `a`/`b`), `layout_property_<metric>` (trustworthiness/kNN-overlap ≥ umap−ε and downstream-ARI within band — NOT coordinate match), `reproducible_<dtype>` (byte-identical embedding across two `fit` runs for a fixed `random_state`, per backend+dtype — D-05), and `transform_property_<metric>` (trustworthiness of new points ≥ umap−ε). Add in-repo HOST property-gate helpers (no sklearn at test time): `trustworthiness(x, embedding, k)` (the sklearn `T = 1 − (2/(nk(2n−3k−1)))·Σ…` formula), `knn_overlap(high_d, low_d, k)`, and `downstream_ari` reusing any existing kmeans+ARI helper if present (else a small host ARI). COPY the `skip_f64_with_log` capability-gate block VERBATIM (umap_test.rs:64-71) into every f64 device test. Load fixtures via `mlrs_core::load_npz`/`OracleCase`. Leave the calibrated ε/ARI-band as named consts seeded with placeholder TODO values that Plan 04's calibration run overwrites and records in 14-VALIDATION.md (do NOT invent thresholds — RESEARCH Q4 / Spike flag item 2). These tests are EXPECTED RED until Plans 02-05 land the real bodies; they MUST compile and run.
  </action>
  <verify>
    <automated>cargo test -p mlrs-algos --features cpu --test umap_test --no-run 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p mlrs-algos --features cpu --test umap_test --no-run` exits 0 (the harness COMPILES against the current shell — RED at runtime is expected, build is not).
    - `grep -E "fn (smooth_knn|fuzzy_union|spectral_init|ab_fit|layout_property|reproducible|transform_property)" crates/mlrs-algos/tests/umap_test.rs` matches all 7 test families (source assertion, per-metric coverage where applicable).
    - `grep -n "fn trustworthiness\|fn knn_overlap\|fn downstream_ari" crates/mlrs-algos/tests/umap_test.rs` matches all 3 host property-gate helpers.
    - `grep -n "skip_f64_with_log" crates/mlrs-algos/tests/umap_test.rs` appears in every new device-touching test (no un-gated f64 path).
  </acceptance_criteria>
  <done>The full value/property/reproducibility/transform harness compiles, loads the committed fixtures, gates f64 with the capability skip, and is RED-by-design pending Plans 02-05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → estimator/test | User-supplied numeric arrays + hyperparameters (`n_neighbors`, `metric`, `p`) cross into host validation. No network/auth/session surface (numeric Rust library). |
| build-time → repo | `gen_oracle.py` runs in a `/tmp` venv; only committed `.npz` blobs (4/8-byte float arrays) enter the tree — never a runtime dependency. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | `.npz` fixture decode (`load_npz`) | mitigate | `load_npz` decodes only 4/8-byte float arrays; indices are float-encoded, metric tag in filename (the `gen_knn_metric` precedent) — no arbitrary-object deserialization path. |
| T-14-02 | Tampering | `umap-learn==0.5.12` in the `/tmp` oracle venv | accept | Build-time-only, pinned, approved in RESEARCH Package Legitimacy Audit; never committed to any manifest, never a runtime/CI dependency. |
| T-14-SC | Tampering | npm/pip/cargo installs | mitigate | No runtime package installs in this plan; the single build-time pip pin (umap-learn 0.5.12) is OK-verdict in the RESEARCH audit. No [ASSUMED]/[SUS] runtime package → no blocking-human checkpoint required. |

No `high`-severity threats: this plan adds committed float blobs, a 5-variant enum, empty module stubs, and RED test scaffolding — no device launch, no untrusted-input execution path.
</threat_model>

<verification>
- `cargo build -p mlrs-algos --features cpu` exits 0 (enum + stubs).
- `cargo test -p mlrs-algos --features cpu --test umap_test --no-run` exits 0 (harness compiles).
- All 5-metric × per-stage `umap_*.npz` fixtures present in `tests/fixtures/`.
- The 4 pre-existing shell tests still pass.
</verification>

<success_criteria>
- `Metric` extended to 5 variants mirroring `knn_graph::Metric` (D-01).
- `umap_internals`/`umap_init` stubs declared in `mod.rs` (Plans 02/03 file-disjoint).
- Committed umap-learn 0.5.12 oracle fixtures for all 5 metrics × every deterministic stage (D-02).
- RED-by-design value/property/reproducibility/transform harness compiles and runs.
- ε/ARI-band left as TODO consts for Plan 04 calibration (no invented thresholds).
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces (Phase 14, all 5 plans)

Every symbol Phase 14 creates, by plan:

**Plan 01 (this plan):**
- `Metric` enum extended to 5 variants in `crates/mlrs-algos/src/manifold/umap.rs`: `Euclidean`, `Manhattan`, `Cosine`, `Chebyshev`, `Minkowski { p: f64 }` (mirrors `knn_graph::Metric`).
- Empty module stubs `crates/mlrs-algos/src/manifold/umap_internals.rs` + `crates/mlrs-algos/src/manifold/umap_init.rs`, declared in `crates/mlrs-algos/src/manifold/mod.rs`.
- `gen_oracle.py` generators: `gen_umap_fuzzy_<metric>_<dtype>`, `gen_umap_spectral_<metric>_<dtype>`, `gen_umap_ab_<dtype>`, `gen_umap_layout_<metric>_<dtype>`, `gen_umap_transform_<metric>_<dtype>`.
- Committed fixtures `tests/fixtures/umap_{fuzzy,spectral,layout,transform}_{euclidean,manhattan,cosine,chebyshev,minkowski}_f64.npz` + `tests/fixtures/umap_ab_f64.npz`.
- Test harness + helpers in `crates/mlrs-algos/tests/umap_test.rs`: test fns `smooth_knn_<metric>`, `fuzzy_union_<metric>`, `spectral_init_<metric>`, `ab_fit`, `layout_property_<metric>`, `reproducible_<dtype>`, `transform_property_<metric>`; host helpers `trustworthiness`, `knn_overlap`, `downstream_ari`; placeholder ε/ARI-band consts.

**Plan 02:** host fns in `umap_internals.rs` — `smooth_knn_dist`, `compute_membership_strengths`, `fuzzy_union`.

**Plan 03:** host fns in `umap_init.rs` — `fit_ab` (LM), `spectral_init`, `random_init`, `noisy_scale_coords`; optional `sign_flip: bool` arg added to `crate::cluster::spectral::recover`.

**Plan 04:** NEW device kernel `umap_layout_step<F>` in `crates/mlrs-kernels/src/umap_layout.rs` (+ `pub mod umap_layout;` / `pub use ... umap_layout_step` in `crates/mlrs-kernels/src/lib.rs`); real `Umap::fit`/`fit_transform` bodies + host epoch driver + private `Metric → knn_graph::Metric` mapping fn in `umap.rs`; calibrated ε/ARI-band thresholds recorded in `14-VALIDATION.md`.

**Plan 05:** `init_graph_transform` host fn in `umap_internals.rs`; real `Umap::transform` body (frozen-subset path) + query-vs-train KNN composition (`distance`+`top_k`) in `umap.rs`; rewritten shell tests (`fit_roundtrip`, `fit_no_leak`) asserting the real fit contract.

New prim/module paths: `crates/mlrs-algos/src/manifold/umap_internals.rs`, `crates/mlrs-algos/src/manifold/umap_init.rs`, `crates/mlrs-kernels/src/umap_layout.rs`.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/14-umap/14-01-SUMMARY.md` when done.
</output>
