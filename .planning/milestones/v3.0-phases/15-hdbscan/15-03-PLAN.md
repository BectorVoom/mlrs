---
phase: 15-hdbscan
plan: 03
type: execute
wave: 3
depends_on: ["15-01", "15-02"]
files_modified:
  - crates/mlrs-algos/src/cluster/hdbscan.rs
  - crates/mlrs-algos/src/cluster/hdbscan/mst.rs
  - crates/mlrs-algos/src/cluster/hdbscan/single_linkage.rs
  - crates/mlrs-algos/src/error.rs
autonomous: true
requirements: [HDBS-01, HDBS-02]
must_haves:
  truths:
    - "The Metric enum exposes Euclidean, Manhattan, Cosine, Chebyshev, Minkowski{p}, Precomputed; minkowski p>=1 and alpha>0 are validated host-side"
    - "build() rejects min_samples<1 (when Some) and max_cluster_size that is neither 0 nor >= min_cluster_size, with typed errors, before any device launch"
    - "mst.rs implements BOTH oracle MST variants: dense argmin-first-min (Variant A, cosine/precomputed) and source-tracking strict-< lowest-j (Variant B, euclidean/l1/l2/chebyshev/minkowski), each with its per-path alpha placement"
    - "MST edges are ordered by the oracle's argsort-by-weight rule and fed through a make_single_linkage UnionFind that relabels N+i per merge — and tie_break_exact passes (D-04/D-05 TRUE GATE)"
  artifacts:
    - path: "crates/mlrs-algos/src/cluster/hdbscan/mst.rs"
      provides: "both Prim MST variants + argsort-by-weight"
      contains: "mst_from_mutual_reachability"
      min_lines: 60
    - path: "crates/mlrs-algos/src/cluster/hdbscan/single_linkage.rs"
      provides: "UnionFind + make_single_linkage"
      contains: "make_single_linkage"
    - path: "crates/mlrs-algos/src/cluster/hdbscan.rs"
      provides: "extended Metric enum, build validation, precomputed fit short-circuit through MST+single-linkage"
      contains: "Precomputed"
  key_links:
    - from: "crates/mlrs-algos/src/cluster/hdbscan.rs"
      to: "crates/mlrs-algos/src/cluster/hdbscan/mst.rs"
      via: "precomputed path feeds the dense distance matrix to mst_from_mutual_reachability"
      pattern: "mst_from_mutual_reachability"
    - from: "crates/mlrs-algos/src/cluster/hdbscan/mst.rs"
      to: "crates/mlrs-algos/src/cluster/hdbscan/single_linkage.rs"
      via: "argsorted MST edges feed make_single_linkage"
      pattern: "make_single_linkage"
---

<objective>
Land the HDBSCAN exactness core (HDBS-02 D-04/D-05 TRUE GATE): extend the `Metric`
enum, resolve the deferred build validation, and port the two oracle MST variants +
single-linkage so the precomputed path (the cleanest exact anchor) produces an MST →
single-linkage dendrogram that matches sklearn bit-for-bit on the tie-heavy +
duplicate-point fixture. This plan is sequenced in Wave 2 (after Wave-1 scaffolding)
and BEFORE the device front-end (Wave 4) commits, so the oracle-matched MST tie-break
is locked first — per the ROADMAP SPIKE-BEFORE-PLANNING flag and D-05.

Purpose: D-05 holds the exact line non-negotiably. If a tie genuinely flips a label
even with the oracle-matched tie-break and the distinct-weight gate design, that is a
phase BLOCKER to surface, not a silently-widened band.
Output: extended Metric, build validation, mst.rs, single_linkage.rs, precomputed
MST path through `fit`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-hdbscan/15-RESEARCH.md
@.planning/phases/15-hdbscan/15-PATTERNS.md
@crates/mlrs-algos/src/cluster/hdbscan.rs
@crates/mlrs-algos/src/cluster/dbscan.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend Metric enum, resolve build validation, add typed errors</name>
  <files>crates/mlrs-algos/src/cluster/hdbscan.rs, crates/mlrs-algos/src/error.rs</files>
  <read_first>
    - crates/mlrs-algos/src/cluster/hdbscan.rs (the Metric enum lines ~48-52, the deferred validation TODO lines ~260-264 in `build`, `Hdbscan::new` lines ~118-134, `hyperparams_eq`/`into_builder`)
    - crates/mlrs-backend/src/prims/knn_graph.rs (the `Metric` enum lines ~60-75 to mirror, and the minkowski `p>=1` validation lines ~444-453)
    - crates/mlrs-algos/src/error.rs (the `AlgoError::InvalidMinSamples` precedent ~line 126 and `BuildError::InvalidMinClusterSize` ~lines 605-610 — template a new `InvalidMaxClusterSize`)
    - crates/mlrs-algos/src/cluster/dbscan.rs (the `min_samples >= 1` guard shape lines ~166-171)
    - .planning/phases/15-hdbscan/15-PATTERNS.md hdbscan.rs section (the exact enum + validation spec)
  </read_first>
  <behavior>
    - `Metric::Minkowski { p: 0.5 }` (p<1) is rejected at build with a typed error
    - `min_samples = Some(0)` is rejected at build (InvalidMinSamples), `Some(1)` accepted
    - `max_cluster_size = 3` with `min_cluster_size = 5` is rejected (neither 0 nor >= mcs); `0` accepted (unbounded); `5` accepted
    - `alpha = 0.0` (and negative) is rejected; `alpha = 1.0` accepted
    - `Metric::Precomputed` constructs and round-trips through builder/into_builder
  </behavior>
  <action>
    Extend the `Metric` enum in `crates/mlrs-algos/src/cluster/hdbscan.rs` to
    `Euclidean, Manhattan, Cosine, Chebyshev, Minkowski { p: f64 }, Precomputed` mirroring
    `knn_graph.rs::Metric` plus `Precomputed`; drop `Eq` (the `Minkowski{p:f64}` variant makes
    the enum non-`Eq` — use `PartialEq` and update `hyperparams_eq` accordingly). Resolve the
    deferred TODO in `HdbscanBuilder::build`: reject `min_samples < 1` when `Some` via
    `AlgoError::InvalidMinSamples` (or a `BuildError` mirror — keep the build-time errors as
    `BuildError`, matching `InvalidMinClusterSize`); add a new
    `BuildError::InvalidMaxClusterSize { estimator, max_cluster_size, min_cluster_size }`
    variant in `error.rs` and reject `max_cluster_size` that is neither `0` nor
    `>= min_cluster_size`; reject `alpha <= 0.0`; for `Metric::Minkowski { p }` reject `p < 1.0`
    (knn_graph precedent). Keep the `min_samples=None -> min_cluster_size` resolution. Add a
    `store_centers: Option<StoreCenters>` hyperparameter (enum `Centroid|Medoid|Both`) to the
    struct + builder + `new()` default `None` + `hyperparams_eq` + `into_builder` (its compute
    lands in 15-06; wire the field now so the surface is complete).
  </action>
  <verify>
    <automated>cargo test --features cpu --test hdbscan_test build_validation</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --features cpu --test hdbscan_test build_validation` passes
    - `grep -c 'Precomputed' crates/mlrs-algos/src/cluster/hdbscan.rs` >= 1 and the enum has all 6 variants
    - `grep -n 'InvalidMaxClusterSize' crates/mlrs-algos/src/error.rs` shows the new variant
    - Source assertion: build rejects min_samples<1, bad max_cluster_size, alpha<=0, minkowski p<1 BEFORE constructing the estimator
  </acceptance_criteria>
  <done>The Metric enum has all six variants, the build validation rejects every invalid hyperparameter with typed errors, and build_validation is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Port both MST variants + single-linkage; wire the precomputed MST path</name>
  <files>crates/mlrs-algos/src/cluster/hdbscan/mst.rs, crates/mlrs-algos/src/cluster/hdbscan/single_linkage.rs, crates/mlrs-algos/src/cluster/hdbscan.rs</files>
  <read_first>
    - .planning/phases/15-hdbscan/15-RESEARCH.md Pattern 2 (mutual reachability + alpha), Pattern 3 (BOTH MST variants — Variant A dense argmin-first-min, Variant B source-tracking strict-< lowest-j), Pattern 4 (the unstable argsort-by-weight + make_single_linkage UnionFind), Pitfall 1 (argsort tie-order, distinct-weight gate design), Pitfall 2 (two alpha placements)
    - .planning/phases/15-hdbscan/15-PATTERNS.md mst.rs section (the D-04 unstable-argsort seam; ORACLE rule, NOT the mlrs lowest-index convention) + the host-scalar f64 bridging idiom (`mlrs_core::{host_to_f64, f64_to_host}`)
    - crates/mlrs-algos/src/cluster/spectral_embedding.rs (host f64 bridging lines ~241, 271-284)
    - crates/mlrs-algos/src/cluster/dbscan.rs (the device-stage -> to_host -> sequential host walk -> from_host shape lines ~147-246; precomputed-style host validation gate lines ~160-179)
    - crates/mlrs-algos/src/cluster/hdbscan.rs (the trivial `fit` body lines ~304-321 to replace for the precomputed branch)
  </read_first>
  <behavior>
    - On the precomputed distinct-weight fixture, the MST + single-linkage dendrogram drives labels that are a -1-pinned permutation of sklearn's (validated downstream once 15-04 lands labelling; here assert the MST edge set + sorted order against a committed reference if available, else assert single-linkage is deterministic and tie-free on distinct weights)
    - tie_break_exact: on the tie-heavy + duplicate-point fixture the MST tie-break matches the oracle rule (Variant A argmin-first-min / Variant B strict-< lowest-j) — the D-04/D-05 TRUE GATE
    - Variant B uses raw core distances with `pair_distance /= alpha`; Variant A divides the whole matrix by alpha before core distances (the two alpha placements differ)
  </behavior>
  <action>
    Create `crates/mlrs-algos/src/cluster/hdbscan/mst.rs` with both oracle Prim variants ported
    line-for-line from RESEARCH Pattern 3: `mst_from_mutual_reachability` (Variant A — dense,
    Prim from node 0, next node via FIRST-min `argmin`, shrinking `current_labels` remap; used
    for cosine + precomputed) and `mst_from_data_matrix` (Variant B — source-tracking
    `current_sources[]`, strict `<` comparisons so ties resolve to lowest `j` as `j` scans
    `0..n`; used for euclidean/l1/l2/chebyshev/minkowski). Replicate the per-variant alpha
    exactly (Variant A: divide the whole matrix by alpha BEFORE core distances; Variant B:
    `pair_distance /= alpha` with RAW core distances). Add an `argsort_by_weight` that orders
    the n-1 MST edges by weight per the oracle's `np.argsort` semantics — for the GATED
    fixtures the weights are DISTINCT so any deterministic order is oracle-equal (document this;
    the tie-heavy fixture is the characterization gate, not a band). Do all scalar math in f64
    via `mlrs_core::{host_to_f64, f64_to_host}`. Create
    `crates/mlrs-algos/src/cluster/hdbscan/single_linkage.rs` with the `UnionFind`
    (`parent=full(2N-1,-1)`, `next_label=N`, `size=[1]*N+[0]*(N-1)`, `union` creates a new
    label `N+i`, `fast_find` with path-compression) and `make_single_linkage` exactly per
    Pattern 4. Declare the `mod hdbscan { mod mst; mod single_linkage; }` submodule tree from
    `hdbscan.rs`. Wire the `Metric::Precomputed` branch of `fit`: validate `x` is square `(n,n)`
    (typed error; document symmetry expectation), `x.to_host(pool)`, divide by alpha, compute
    core distances host-side as the `(min_samples-1)`-th smallest per row (`np.partition`
    equivalent over the row including self-zero), build the dense mutual-reachability, run
    Variant A MST -> argsort -> single_linkage, and store the single-linkage hierarchy on the
    estimator for the Wave-3 condense/select stage (labels stay all-`-1` until 15-04 wires the
    tree — keep `labels_` populated from the existing path so the file still fits/compiles).
    Invoke `Skill("spike-findings-mlrs")` before touching any device path (none here — pure
    host) and to honor the R-9 duplicate-point assertion convention in the tie gate.
  </action>
  <verify>
    <automated>cargo test --features cpu --test hdbscan_test tie_break_exact</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --features cpu --test hdbscan_test tie_break_exact` passes (D-04/D-05 TRUE GATE) — OR, if a metric proves un-exactable even with the oracle-matched tie-break, the SUMMARY surfaces it as a phase BLOCKER per D-05 (do NOT demote to a band)
    - `grep -c 'mst_from_data_matrix' crates/mlrs-algos/src/cluster/hdbscan/mst.rs` >= 1 and `grep -c 'mst_from_mutual_reachability' ...` >= 1 (both variants present)
    - Source assertion: Variant B uses strict `<` (lowest-j ties) and raw core dist + `pair_distance/alpha`; Variant A divides the matrix by alpha before core distances
  </acceptance_criteria>
  <done>Both oracle MST variants + single-linkage exist with correct per-path alpha, and tie_break_exact passes (or an un-exactable metric is surfaced as a D-05 blocker).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller X -> precomputed validation | When `metric=Precomputed`, `X` is interpreted as an n x n distance matrix; squareness must be validated host-side before the back-end consumes it. |
| host build-time params -> back-end | min_samples/max_cluster_size/alpha/minkowski-p validated before any compute. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03-V5a | Tampering | Non-square precomputed matrix | mitigate | Validate `X` is square `(n,n)` with a typed error before the back-end runs; document the symmetry expectation (sklearn does `allclose(X,X.T)`) — D-02. Do not silently proceed. |
| T-15-03-V5b | Tampering | Invalid hyperparameters (min_samples, max_cluster_size, alpha, minkowski p) | mitigate | Build-time typed `BuildError` rejection BEFORE any device launch (ASVS V5); resolves the shell's deferred validation TODO (D-09). |
| T-15-03-OVF | DoS | n x n host allocation for the dense precomputed/cosine matrix | mitigate | `checked_mul` on `n*n` before allocating the host buffer (knn_graph overflow-guard precedent). |
| T-15-03-MIS | Tampering | cpu-MLIR silent miscompile | accept | This plan is pure host scalar Rust (no kernel) — the cross-loop-accumulator MLIR hazard does not apply here; the MR kernel lands in 15-05 under its own threat row. |
| T-15-03-SC | Tampering | npm/pip/cargo installs | mitigate | No new crate dependency added (host port reuses `mlrs_core` bridging); nothing to audit. |
</threat_model>

<verification>
- `cargo test --features cpu --test hdbscan_test build_validation tie_break_exact` green.
- Both MST variants present with correct per-path alpha; single-linkage relabels N+i.
- D-05: any un-exactable metric is surfaced as a blocker, never band-demoted.
</verification>

<success_criteria>
The Metric enum + build validation are complete, both oracle MST variants + single-linkage
are ported with correct alpha placement, and the D-04/D-05 exact tie-break gate passes (or a
blocker is surfaced).
</success_criteria>

## Artifacts this phase produces (cumulative — this plan's slice)

- **Extended symbol:** `hdbscan::Metric` now `{Euclidean, Manhattan, Cosine, Chebyshev, Minkowski{p}, Precomputed}`; new `hdbscan::StoreCenters {Centroid, Medoid, Both}` enum + `store_centers` builder field/accessor.
- **New error:** `BuildError::InvalidMaxClusterSize`.
- **New modules:** `crates/mlrs-algos/src/cluster/hdbscan/mst.rs` (`mst_from_mutual_reachability`, `mst_from_data_matrix`, `argsort_by_weight`), `.../single_linkage.rs` (`UnionFind`, `make_single_linkage`).
- **New behavior:** `Metric::Precomputed` `fit` short-circuit (square validation -> core dist -> dense MR -> Variant A MST -> single-linkage hierarchy).

<output>
Create `.planning/phases/15-hdbscan/15-03-SUMMARY.md` when done.
</output>
