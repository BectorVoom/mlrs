---
phase: 16-builder-retrofit-sweep-shim-coverage
plan: 12
type: execute
wave: 12
depends_on: ["16-11"]
files_modified:
  - crates/mlrs-algos/src/traits.rs
  - crates/mlrs-algos/src/lib.rs
  - crates/mlrs-algos/tests/compile_fail.rs
autonomous: true
requirements: [BLDR-03]
must_haves:
  truths:
    - "crates/mlrs-algos/src/traits.rs is hard-deleted and its pub mod traits; declaration is removed from lib.rs (D-01)"
    - "No source file in either crate references crate::traits or mlrs_algos::traits (single trait surface)"
    - "predict-before-fit is a compile error for the migrated estimators (trybuild compile_fail gate)"
    - "The targeted oracle suite + the Python static shim suite are green after the deletion"
  artifacts:
    - path: "crates/mlrs-algos/src/lib.rs"
      provides: "lib.rs with pub mod traits; removed; typestate is the single trait surface"
      contains: "pub mod typestate"
    - path: "crates/mlrs-algos/tests/compile_fail.rs"
      provides: "trybuild compile-fail proof that predict-before-fit does not compile"
      contains: "compile_fail"
  key_links:
    - from: "crates/mlrs-algos/src/lib.rs"
      to: "crates/mlrs-algos/src/traits.rs"
      via: "the pub mod traits; line is removed (the module file is deleted)"
      pattern: "pub mod traits"
---

<objective>
FINAL plan — the convergence commit. Hard-delete `crates/mlrs-algos/src/traits.rs` and its `pub mod traits;` in lib.rs (D-01), gated by an empty grep across BOTH crates (the deletion only succeeds once every estimator AND every mlrs-py PyO3 importer is off the old surface — Pitfall 3). Then run the phase-end gate: the trybuild `compile_fail` predict-before-fit proof, the targeted oracle suite, and the full Python static shim suite.

This is the single-trait-surface end-state the entire phase converges to (D-01). After this commit, `mlrs_algos::typestate` is the ONLY trait surface.

Purpose: Remove the two-surface debt; prove the convergence holds (grep empty + everything green).
Output: traits.rs gone; one trait surface; phase-end gate green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-builder-retrofit-sweep-shim-coverage/16-CONTEXT.md
@.planning/phases/16-builder-retrofit-sweep-shim-coverage/16-RESEARCH.md
@.planning/phases/16-builder-retrofit-sweep-shim-coverage/16-PATTERNS.md
@.planning/phases/16-builder-retrofit-sweep-shim-coverage/16-09-SUMMARY.md
</context>

<artifacts_this_phase_produces>
- DELETED: `crates/mlrs-algos/src/traits.rs` (the 9 old traits)
- `lib.rs` with `pub mod traits;` removed (typestate is the single surface)
- Confirmed/extended `compile_fail.rs` trybuild fixtures proving predict-before-fit does not compile
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Pre-deletion grep gate then delete traits.rs + remove pub mod traits;</name>
  <files>crates/mlrs-algos/src/traits.rs, crates/mlrs-algos/src/lib.rs</files>
  <read_first>
    - crates/mlrs-algos/src/lib.rs (the `pub mod traits;` declaration + any `pub use crate::traits::*` re-export)
    - crates/mlrs-algos/src/traits.rs (the file to delete — confirm nothing re-exports its types elsewhere)
    - .planning/phases/16-builder-retrofit-sweep-shim-coverage/16-RESEARCH.md (Pitfall 3 — the hidden cross-crate break; deletion must not precede an empty grep)
    - .planning/phases/16-builder-retrofit-sweep-shim-coverage/16-09-SUMMARY.md (confirms 29/29 migrated)
  </read_first>
  <action>
    First run the convergence grep across BOTH crates to confirm every estimator and every PyO3 importer is off the old surface (Pitfall 3): search for `mlrs_algos::traits` and `crate::traits` under `crates/mlrs-algos/src` and `crates/mlrs-py/src`, excluding `traits.rs` itself. It must return EMPTY. If ANY reference remains, STOP and report which file/estimator was missed — do NOT delete traits.rs while a reference exists (it would break both crates). Also confirm no `pub use crate::traits::*` re-export exists in lib.rs or any mod.rs that would orphan a name on deletion. THEN: delete `crates/mlrs-algos/src/traits.rs` and remove the `pub mod traits;` line from lib.rs. Rebuild both crates to confirm the deletion is clean. Commit the deletion as a single atomic commit (the convergence commit).
  </action>
  <verify>
    <automated>bash -c 'set -e; m=$(grep -rln "mlrs_algos::traits\|crate::traits" crates/mlrs-algos/src crates/mlrs-py/src --include="*.rs" | grep -v "src/traits.rs" || true); if [ -n "$m" ]; then echo "RESIDUAL REFS: $m"; exit 1; fi; test ! -f crates/mlrs-algos/src/traits.rs; cargo build -p mlrs-algos --features cpu && cargo build -p mlrs-py --features cpu'</automated>
  </verify>
  <acceptance_criteria>
    - The grep for `mlrs_algos::traits` / `crate::traits` across both crates' src (excluding traits.rs) returns EMPTY before deletion.
    - `crates/mlrs-algos/src/traits.rs` no longer exists; `pub mod traits;` is removed from lib.rs.
    - `cargo build -p mlrs-algos --features cpu` and `cargo build -p mlrs-py --features cpu` both succeed after deletion.
  </acceptance_criteria>
  <done>traits.rs hard-deleted; pub mod traits; removed; both crates build; single trait surface achieved.</done>
</task>

<task type="auto">
  <name>Task 2: Phase-end gate — compile_fail (trybuild) + targeted oracle suite + Python static shim suite</name>
  <files>crates/mlrs-algos/tests/compile_fail.rs</files>
  <read_first>
    - crates/mlrs-algos/tests/compile_fail.rs (the trybuild harness; confirm it has at least the UMAP predict-before-fit fixture, and that it still asserts compile failure after traits.rs is gone)
    - crates/mlrs-algos/tests/ui/ (trybuild .rs + .stderr fixtures — confirm the predict-before-fit-does-not-compile fixture exists; the UMAP fixture is the representative typestate proof per VALIDATION Wave-0 note)
    - .planning/phases/16-builder-retrofit-sweep-shim-coverage/16-VALIDATION.md (the Sampling Rate "Before /gsd-verify-work" row — the exact phase-gate command set)
    - .planning/phases/16-builder-retrofit-sweep-shim-coverage/16-RESEARCH.md (Environment Availability — full `cargo test` is slow/disk-exhausting; run TARGETED)
  </read_first>
  <action>
    Run the phase-end gate. (1) trybuild compile-fail: `cargo test --features cpu --test compile_fail` — confirm predict-before-fit (calling a Fitted-only accessor on an Unfit estimator) does NOT compile. If the harness only has the UMAP fixture, that is the accepted representative typestate proof (VALIDATION Wave-0 note); if a migrated-estimator fixture is trivially addable, add one for a pilot (e.g. Ridge) to strengthen the gate — but do not block on broad per-estimator fixtures. (2) Targeted oracle suite: run the migrated estimators' suites (do NOT run the full `cargo test --features cpu` blind — per RESEARCH it is slow and can exhaust disk; run a targeted set covering the pilots + one estimator per shape/module, or run modules sequentially with `cargo clean` between if disk pressure appears). (3) Python static shim suite: `pytest crates/mlrs-py/python/tests/test_shims.py test_params.py test_estimator_checks.py` green without the compiled extension. Record any residual UAT item (the live check_estimator run stays deferred — SHIM-03). This task gates the phase as a whole before /gsd-verify-work.
  </action>
  <verify>
    <automated>cargo test --features cpu --test compile_fail && cd crates/mlrs-py/python && python -m pytest tests/test_shims.py tests/test_params.py tests/test_estimator_checks.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --features cpu --test compile_fail` passes (predict-before-fit does NOT compile — the trybuild gate is green).
    - The targeted oracle suite for the migrated estimators is green (run targeted, not blind-full, per RESEARCH disk/time constraints).
    - `pytest tests/test_shims.py tests/test_params.py tests/test_estimator_checks.py` is green without the compiled `_mlrs` extension.
    - A UAT note is recorded for the deferred live `check_estimator` run (SHIM-03 by-design).
  </acceptance_criteria>
  <done>Phase-end gate green: compile_fail trybuild + targeted oracle suite + Python static shim suite all pass; the convergence is verified and the only deferred item is the live check_estimator UAT.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| compile-time lifecycle | predict-before-fit must be a type error, not a runtime path |
| cross-crate trait surface | mlrs-py must not reference a deleted trait module |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-PITFALL3 | Tampering / DoS | traits.rs deletion vs mlrs-py importers | mitigate | The empty-grep gate across BOTH crates is the precondition for deletion; the verify fails (and blocks the commit) if any residual reference exists |
| T-16-SEAL | Tampering | single trait surface | accept | After deletion `typestate` is the only surface; `State` stays sealed; no third lifecycle state introduced |
| T-16-FFI-DEFER | — | live check_estimator | accept | Deferred to UAT (no maturin+pyarrow host); the static + trybuild gates are the maximum verifiable here |
</threat_model>

<verification>
- Grep for `crate::traits` / `mlrs_algos::traits` across both crates (excluding the deleted file) returns empty.
- traits.rs deleted; `pub mod traits;` removed; both crates build.
- `cargo test --features cpu --test compile_fail` green; Python static shim suite green.
</verification>

<success_criteria>
- traits.rs hard-deleted; single trait surface (`mlrs_algos::typestate`); predict-before-fit is a compile error; phase-end gate (compile_fail + targeted oracle + Python static) green; live check_estimator deferred to UAT.
</success_criteria>

<output>
Create `.planning/phases/16-builder-retrofit-sweep-shim-coverage/16-12-SUMMARY.md` when done. Record the convergence-commit hash, the final grep-empty proof, and the deferred-UAT note for the live check_estimator run.
</output>
