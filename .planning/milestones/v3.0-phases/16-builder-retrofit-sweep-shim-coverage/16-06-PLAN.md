---
phase: 16-builder-retrofit-sweep-shim-coverage
plan: 06
type: execute
wave: 6
depends_on: ["16-05"]
files_modified:
  - crates/mlrs-algos/src/cluster/kmeans.rs
  - crates/mlrs-algos/src/covariance/empirical_covariance.rs
  - crates/mlrs-algos/src/covariance/ledoit_wolf.rs
  - crates/mlrs-algos/tests/kmeans_test.rs
  - crates/mlrs-algos/tests/empirical_covariance_test.rs
  - crates/mlrs-algos/tests/ledoit_wolf_test.rs
  - crates/mlrs-py/src/estimators/cluster.rs
  - crates/mlrs-py/src/estimators/covariance.rs
autonomous: true
requirements: [BLDR-03]
must_haves:
  truths:
    - "KMeans construct only via zero-arg new() + builder; its three constructors (new, with_init, with_opts) all fold into the builder, including an init(Option<Vec<F>>) setter"
    - "EmpiricalCovariance and LedoitWolf construct only via zero-arg new() + builder"
    - "Each estimator's oracle suite passes unchanged (exact-label / 1e-5, D-03)"
    - "cargo build -p mlrs-py --features cpu succeeds after each PyO3 arm migrates"
  artifacts:
    - path: "crates/mlrs-algos/src/cluster/kmeans.rs"
      provides: "KMeans<F, S=Unfit> + builder subsuming new/with_init/with_opts incl. .init(Option<Vec<F>>)"
      contains: "KMeans<F, S"
    - path: "crates/mlrs-algos/src/covariance/ledoit_wolf.rs"
      provides: "LedoitWolf<F, S=Unfit> + builder + Fitted accessors"
      contains: "LedoitWolf<F, S"
  key_links:
    - from: "crates/mlrs-py/src/estimators/cluster.rs"
      to: "crates/mlrs-algos/src/cluster/kmeans.rs"
      via: "PyKMeans fit builds via KMeans::builder()...build()?; AnyKMeans arms are KMeans<f*, Fitted>"
      pattern: "KMeans::<f(32|64)>::builder"
---

<objective>
Bulk sweep — KMeans (the LATE multi-constructor stress case, D-06) + covariance/. KMeans is handled last among the migrations because it has THREE constructors: `new(n_clusters, seed)`, `with_init(n_clusters, init: Vec<F>)` (injects a caller-supplied init matrix — the fixture tests use this), and `with_opts(n_clusters, seed, max_iter, tol)`. Its builder must be the widest, including an `init(Option<Vec<F>>)` setter. The recipe is now proven on every simpler shape, so KMeans is mechanical.

Covariance (EmpiricalCovariance, LedoitWolf) are straightforward shape-A `Fit`-only estimators.

Purpose: Clear the hardest constructor case + the covariance module.
Output: KMeans, EmpiricalCovariance, LedoitWolf on the typestate surface.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-builder-retrofit-sweep-shim-coverage/16-CONTEXT.md
@.planning/phases/16-builder-retrofit-sweep-shim-coverage/16-RESEARCH.md
@.planning/phases/16-builder-retrofit-sweep-shim-coverage/16-PATTERNS.md
@.planning/phases/16-builder-retrofit-sweep-shim-coverage/16-05-SUMMARY.md
</context>

<artifacts_this_phase_produces>
- `KMeans<F, S=Unfit>` + `KMeansBuilder` (`.n_clusters(usize).seed(u64).max_iter(usize).tol(f64).init(Option<Vec<F>>)`) + Fitted Fit/PredictLabels/accessors
- `EmpiricalCovariance<F, S=Unfit>` + builder (`.assume_centered(bool).store_precision(bool)`) + Fitted Fit/accessors
- `LedoitWolf<F, S=Unfit>` + builder (`.assume_centered(bool)`) + Fitted Fit/accessors
- `AnyKMeans`/`AnyEmpiricalCovariance`/`AnyLedoitWolf` → `any_estimator_typestate!`
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate KMeans (LATE multi-constructor; new + with_init + with_opts → builder)</name>
  <files>crates/mlrs-algos/src/cluster/kmeans.rs, crates/mlrs-algos/tests/kmeans_test.rs, crates/mlrs-py/src/estimators/cluster.rs</files>
  <read_first>
    - crates/mlrs-algos/src/cluster/kmeans.rs (full — struct, new(n_clusters, seed), with_init(n_clusters, init: Vec<F>), with_opts(n_clusters, seed, max_iter, tol), Fit + PredictLabels, fit body + geometry guard + the empty-cluster relocation logic)
    - crates/mlrs-algos/src/cluster/dbscan.rs + crates/mlrs-algos/src/cluster/spectral_clustering.rs (migrated wide-builder siblings — Option/Vec setter pattern)
    - crates/mlrs-algos/src/typestate.rs (Fit/PredictLabels/Fitted/Unfit/validate_geometry)
    - crates/mlrs-algos/tests/kmeans_test.rs (~4 new + 3 with_init + 2 with_opts; note kmeans_test.rs:104 KMeans::<F>::with_init(KM_K, init_host) — the injected-init fixture path)
    - crates/mlrs-py/src/estimators/cluster.rs (PyKMeans — sklearn random_state→Rust seed mapping; AnyKMeans + import)
    - .planning/phases/16-builder-retrofit-sweep-shim-coverage/16-PATTERNS.md (§1 + the "KMeans multi-constructor, builder gains init(Option<Vec<F>>) setter" note in RESEARCH §Call-Site)
  </read_first>
  <behavior>
    - KMeans::<F, Unfit>::new() yields sklearn defaults (n_clusters=8, max_iter=300, tol=1e-4, seed default — match current new()/with_opts exactly); builder exposes n_clusters/seed/max_iter/tol AND init(Option<Vec<F>>) so the injected-init fixture path is preserved.
    - On Fitted: predict_labels + cluster_centers_/inertia_ accessors; exact-label + 1e-5 oracle passes unchanged (including the empty-cluster relocation path).
  </behavior>
  <action>
    Apply Shape-A recipe with the WIDEST builder (subsumes all three constructors). KMeansBuilder setters: `.n_clusters(usize).seed(u64).max_iter(usize).tol(f64).init(Option<Vec<F>>)` — the `init` setter is the with_init replacement (note: init carries `F` data, so the builder may need to be generic over F OR store the init as a typed payload threaded into build::<F>(); follow whichever the spectral_clustering wide-builder established, keeping the injected-init fixture at kmeans_test.rs:104 working). Defaults from current new()/with_opts. Swap `use crate::traits::{Fit, PredictLabels}` → typestate. Consuming-self `Fit::fit` — copy EVERY compute line byte-identical INCLUDING the empty-cluster relocation logic (exact-label gate is sensitive); validate_geometry at top. Move PredictLabels + cluster_centers_/inertia_/labels_ accessors onto `impl<F> KMeans<F, Fitted>`. Migrate ALL call sites (new, with_init, with_opts forms → builder; with_init → `.init(Some(init_host))`) + PyKMeans arm (keep random_state→seed mapping); `AnyKMeans` → `any_estimator_typestate!`. hyperparams_eq case. Commit.
  </action>
  <verify>
    <automated>cargo test --features cpu --test kmeans_test && cargo build -p mlrs-py --features cpu</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --features cpu --test kmeans_test` passes (EXACT-label + 1e-5 inertia/centers, incl. injected-init fixture; zero fit-body compute deltas — empty-cluster relocation preserved).
    - `cargo build -p mlrs-py --features cpu` succeeds.
    - `! grep -q 'crate::traits' crates/mlrs-algos/src/cluster/kmeans.rs`, `! grep -q 'fn with_init' crates/mlrs-algos/src/cluster/kmeans.rs`, `! grep -q 'fn with_opts' crates/mlrs-algos/src/cluster/kmeans.rs` (all three folded into builder).
    - KMeansBuilder exposes an `init` setter; the kmeans_test injected-init path uses it.
  </acceptance_criteria>
  <done>KMeans migrated with all three constructors folded into one builder, exact-label oracle green (incl. injected init), mlrs-py builds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate EmpiricalCovariance (shape A; Fit-only)</name>
  <files>crates/mlrs-algos/src/covariance/empirical_covariance.rs, crates/mlrs-algos/tests/empirical_covariance_test.rs, crates/mlrs-py/src/estimators/covariance.rs</files>
  <read_first>
    - crates/mlrs-algos/src/covariance/empirical_covariance.rs (full — struct, new(assume_centered: bool, store_precision: bool), Fit, accessors, fit body)
    - crates/mlrs-algos/src/linear/ridge.rs (migrated Shape-A reference)
    - crates/mlrs-algos/src/typestate.rs (Fit/Fitted/Unfit/validate_geometry)
    - crates/mlrs-algos/tests/empirical_covariance_test.rs (~3 call sites)
    - crates/mlrs-py/src/estimators/covariance.rs (PyEmpiricalCovariance :35 + Any* + import)
    - .planning/phases/16-builder-retrofit-sweep-shim-coverage/16-PATTERNS.md (§1)
  </read_first>
  <behavior>
    - EmpiricalCovariance::<F, Unfit>::new() yields sklearn defaults (assume_centered=false, store_precision=true — match current); builder `.assume_centered(bool).store_precision(bool)`; covariance_/precision_ accessors on Fitted; oracle unchanged.
  </behavior>
  <action>
    Apply Shape-A recipe. EmpiricalCovarianceBuilder `.assume_centered(bool).store_precision(bool)`. Swap `use crate::traits::{Fit}` → typestate; consuming-self Fit (validate_geometry, BYTE-IDENTICAL); covariance_/precision_/location_ accessors onto Fitted. Migrate ~3 test call sites + PyEmpiricalCovariance arm; `AnyEmpiricalCovariance` → `any_estimator_typestate!`. hyperparams_eq case. Commit.
  </action>
  <verify>
    <automated>cargo test --features cpu --test empirical_covariance_test && cargo build -p mlrs-py --features cpu</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --features cpu --test empirical_covariance_test` passes (1e-5, zero fit-body deltas).
    - `cargo build -p mlrs-py --features cpu` succeeds.
    - `! grep -q 'crate::traits' crates/mlrs-algos/src/covariance/empirical_covariance.rs`.
  </acceptance_criteria>
  <done>EmpiricalCovariance migrated, oracle green, mlrs-py builds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Migrate LedoitWolf (shape A; Fit-only)</name>
  <files>crates/mlrs-algos/src/covariance/ledoit_wolf.rs, crates/mlrs-algos/tests/ledoit_wolf_test.rs, crates/mlrs-py/src/estimators/covariance.rs</files>
  <read_first>
    - crates/mlrs-algos/src/covariance/ledoit_wolf.rs (full — struct, new(assume_centered: bool), Fit, accessors, fit body)
    - crates/mlrs-algos/src/covariance/empirical_covariance.rs (the JUST-migrated sibling)
    - crates/mlrs-algos/tests/ledoit_wolf_test.rs (~3 call sites)
    - crates/mlrs-py/src/estimators/covariance.rs (PyLedoitWolf :171 + Any* + import)
    - .planning/phases/16-builder-retrofit-sweep-shim-coverage/16-PATTERNS.md (§1)
  </read_first>
  <behavior>
    - LedoitWolf::<F, Unfit>::new() yields sklearn default (assume_centered=false); builder `.assume_centered(bool)`; covariance_/shrinkage_ accessors on Fitted; oracle unchanged.
  </behavior>
  <action>
    Apply Shape-A recipe, sibling of EmpiricalCovariance. LedoitWolfBuilder `.assume_centered(bool)`. Swap to typestate; consuming-self Fit (BYTE-IDENTICAL — the shrinkage estimate math is 1e-5-sensitive); accessors onto Fitted. Migrate ~3 test call sites + PyLedoitWolf arm; `AnyLedoitWolf` → `any_estimator_typestate!`. hyperparams_eq case. Commit.
  </action>
  <verify>
    <automated>cargo test --features cpu --test ledoit_wolf_test && cargo build -p mlrs-py --features cpu</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --features cpu --test ledoit_wolf_test` passes (1e-5, zero fit-body deltas).
    - `cargo build -p mlrs-py --features cpu` succeeds.
    - `! grep -q 'crate::traits' crates/mlrs-algos/src/covariance/ledoit_wolf.rs`.
  </acceptance_criteria>
  <done>LedoitWolf migrated, oracle green, mlrs-py builds; cluster + covariance modules complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → build() | n_clusters/assume_centered hyperparameter + injected init matrix (data-independent shape) |
| host x geometry → fit | shapes reach validate_geometry before device launch |
| caller-supplied init (KMeans) → fit | injected init Vec must not bypass geometry checks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-V5 | Tampering / DoS | KMeans/covariance fit geometry guard | mitigate | `validate_geometry` stays atop each ported fit; KMeans injected-init dimensions validated against n_clusters/n_features as in the current code (do not drop that check) |
| T-16-GUARDF64 | DoS | PyKMeans/PyEmpiricalCovariance/PyLedoitWolf F64 arms | mitigate | `guard_f64()` + `lock_pool()` preserved in migrated PyO3 fits |
| T-16-ARM | Tampering | Any* Fitted arms | mitigate | `any_estimator_typestate!` arms are `T<f*, Fitted>` |
</threat_model>

<verification>
- All three oracle suites green (exact-label / 1e-5; KMeans injected-init covered).
- `cargo build -p mlrs-py --features cpu` green.
- No KMeans/covariance file references `crate::traits`; KMeans with_init/with_opts removed.
</verification>

<success_criteria>
- KMeans (multi-constructor), EmpiricalCovariance, LedoitWolf migrated; cluster + covariance modules complete.
</success_criteria>

<output>
Create `.planning/phases/16-builder-retrofit-sweep-shim-coverage/16-06-SUMMARY.md` when done. Record the KMeans init(Option<Vec<F>>) builder pattern.
</output>
