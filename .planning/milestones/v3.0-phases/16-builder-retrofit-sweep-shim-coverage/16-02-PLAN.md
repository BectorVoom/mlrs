---
phase: 16-builder-retrofit-sweep-shim-coverage
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - crates/mlrs-algos/src/linear/linear_regression.rs
  - crates/mlrs-algos/src/linear/lasso.rs
  - crates/mlrs-algos/src/linear/elastic_net.rs
  - crates/mlrs-algos/tests/linear_regression_test.rs
  - crates/mlrs-algos/tests/lasso_test.rs
  - crates/mlrs-algos/tests/elastic_net_test.rs
  - crates/mlrs-py/src/estimators/linear.rs
autonomous: true
requirements: [BLDR-03]
must_haves:
  truths:
    - "LinearRegression, Lasso, ElasticNet construct only via zero-arg new() + builder; arg-new and with_opts are gone"
    - "Each estimator's oracle suite passes unchanged at 1e-5 (fit-body byte-identical, D-03)"
    - "predict-before-fit is a compile error for all three (Fitted-gated Predict)"
    - "cargo build -p mlrs-py --features cpu succeeds after each estimator's PyO3 arm migrates"
  artifacts:
    - path: "crates/mlrs-algos/src/linear/lasso.rs"
      provides: "Lasso<F, S=Unfit> + LassoBuilder (subsumes with_opts params: alpha, fit_intercept, max_iter, tol)"
      contains: "struct Lasso<F, S"
    - path: "crates/mlrs-algos/src/linear/elastic_net.rs"
      provides: "ElasticNet<F, S=Unfit> + ElasticNetBuilder (alpha, l1_ratio, fit_intercept, max_iter, tol)"
      contains: "struct ElasticNet<F, S"
  key_links:
    - from: "crates/mlrs-py/src/estimators/linear.rs"
      to: "crates/mlrs-algos/src/linear/lasso.rs"
      via: "PyLasso fit builds via Lasso::builder()...build()?; AnyLasso arms are Lasso<f*, Fitted>"
      pattern: "Lasso::<f(32|64)>::builder"
---

<objective>
Bulk sweep — linear/ remainder, part 1 (D-06 module order). Three shape-A estimators that follow the proven Pilot-A recipe. Lasso and ElasticNet additionally fold their `with_opts(...)` multi-arg constructor (max_iter, tol) into builder setters — the builder subsumes both `new` and `with_opts`.

Each estimator is one commit gated by its oracle suite + `cargo build -p mlrs-py`. Fit-body math byte-identical (D-03).

Purpose: Advance the linear module. These are the first multi-arg `with_opts` cases — proving the builder absorbs secondary constructors before the harder cluster/projection cases.
Output: LinearRegression, Lasso, ElasticNet on the typestate surface; call sites + PyO3 arms migrated.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-builder-retrofit-sweep-shim-coverage/16-CONTEXT.md
@.planning/phases/16-builder-retrofit-sweep-shim-coverage/16-RESEARCH.md
@.planning/phases/16-builder-retrofit-sweep-shim-coverage/16-PATTERNS.md
@.planning/phases/16-builder-retrofit-sweep-shim-coverage/16-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
- `LinearRegression<F, S=Unfit>` + `LinearRegressionBuilder` (`.fit_intercept(bool)`) + zero-arg new() + Fitted Predict/accessors
- `Lasso<F, S=Unfit>` + `LassoBuilder` (`.alpha(f64).fit_intercept(bool).max_iter(usize).tol(f64)`) + zero-arg new() + Fitted Predict/accessors
- `ElasticNet<F, S=Unfit>` + `ElasticNetBuilder` (`.alpha(f64).l1_ratio(f64).fit_intercept(bool).max_iter(usize).tol(f64)`) + zero-arg new() + Fitted Predict/accessors
- `AnyLinearRegression`/`AnyLasso`/`AnyElasticNet` switched to `any_estimator_typestate!`
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate LinearRegression (shape A, single-arg new)</name>
  <files>crates/mlrs-algos/src/linear/linear_regression.rs, crates/mlrs-algos/tests/linear_regression_test.rs, crates/mlrs-py/src/estimators/linear.rs</files>
  <read_first>
    - crates/mlrs-algos/src/linear/linear_regression.rs (full — struct, new(fit_intercept), Fit/Predict, accessors, fit body + geometry guard)
    - crates/mlrs-algos/src/linear/ridge.rs (the JUST-migrated Pilot-A reference — copy its exact shape)
    - crates/mlrs-algos/src/typestate.rs (Fit/Predict/Fitted/Unfit/validate_geometry)
    - crates/mlrs-algos/tests/linear_regression_test.rs (call sites — ~4)
    - crates/mlrs-py/src/estimators/linear.rs (PyLinearRegression fit arms + AnyLinearRegression enum + import)
    - .planning/phases/16-builder-retrofit-sweep-shim-coverage/16-PATTERNS.md (§1 Shape-A, §4 PyO3 migration)
  </read_first>
  <behavior>
    - LinearRegression::<F, Unfit>::new() yields fit_intercept=true (sklearn default); equals builder().build()?.
    - fit consumes self → Fitted; predict only on Fitted; oracle passes unchanged at 1e-5.
  </behavior>
  <action>
    Apply the proven Pilot-A recipe (now embodied in the migrated ridge.rs) to linear_regression.rs: add `<F, S = Unfit>` + `PhantomData`; zero-arg `new()` (fit_intercept = true, sklearn default); `LinearRegressionBuilder` with `.fit_intercept(bool)`, `Default` from `new().into_builder()`, `build<F>() -> Result<_, BuildError>` (no data-independent hyperparam to validate beyond construction — keep build infallible-but-typed for uniformity); swap `use crate::traits` → typestate; consuming-self `Fit::fit` with `validate_geometry` and BYTE-IDENTICAL math; move Predict + accessors to `impl ...<F, Fitted>`. Migrate the ~4 test call sites and the PyLinearRegression arm in linear.rs (builder form, drop any `as f32`); switch `AnyLinearRegression` to `any_estimator_typestate!`. Add the `hyperparams_eq` defaults case. Commit estimator + tests + PyO3 arm together.
  </action>
  <verify>
    <automated>cargo test --features cpu --test linear_regression_test && cargo build -p mlrs-py --features cpu</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --features cpu --test linear_regression_test` passes (zero compute-line deltas in fit).
    - `cargo build -p mlrs-py --features cpu` succeeds.
    - `! grep -q 'crate::traits' crates/mlrs-algos/src/linear/linear_regression.rs`.
    - `AnyLinearRegression` uses `any_estimator_typestate!`.
  </acceptance_criteria>
  <done>LinearRegression migrated, oracle green, mlrs-py builds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate Lasso (shape A, new + with_opts → builder)</name>
  <files>crates/mlrs-algos/src/linear/lasso.rs, crates/mlrs-algos/tests/lasso_test.rs, crates/mlrs-py/src/estimators/linear.rs</files>
  <read_first>
    - crates/mlrs-algos/src/linear/lasso.rs (full — struct, new(alpha, fit_intercept), with_opts(alpha, fit_intercept, max_iter, tol), Fit/Predict, fit body + any alpha<0 guard)
    - crates/mlrs-algos/src/linear/ridge.rs (Pilot-A reference)
    - crates/mlrs-algos/src/linear/coordinate_descent.rs (shared CD solver — confirm fit-body math stays byte-identical; do not alter)
    - crates/mlrs-algos/tests/lasso_test.rs (call sites incl. with_opts — ~3)
    - crates/mlrs-py/src/estimators/linear.rs (PyLasso fit arms + AnyLasso + import)
    - .planning/phases/16-builder-retrofit-sweep-shim-coverage/16-PATTERNS.md (§1 + the "with_opts folds into builder" note at :109)
  </read_first>
  <behavior>
    - Lasso::<F, Unfit>::new() yields sklearn defaults (alpha=1.0, fit_intercept=true, max_iter=1000, tol=1e-4 — match the current new()/with_opts defaults exactly).
    - The builder exposes alpha/fit_intercept/max_iter/tol; new() == builder().build()?.
    - Data-independent alpha>=0 check (if present in fit) moves to build() → BuildError; oracle passes unchanged.
  </behavior>
  <action>
    Apply Shape-A recipe; the LassoBuilder subsumes BOTH `new(alpha, fit_intercept)` AND `with_opts(alpha, fit_intercept, max_iter, tol)` — expose all four as f64/usize setters with defaults derived from the current constructor defaults (read them from lasso.rs, do not invent). zero-arg `new()` sets all four to current defaults. Relocate any data-independent hyperparam validation (alpha>=0) from the fit body to `build()` → `BuildError`. Consuming-self `Fit::fit` with `validate_geometry`, BYTE-IDENTICAL CD-solver math. Move Predict + accessors to Fitted. Migrate the ~3 test call sites (both `new` and `with_opts` forms → builder) and the PyLasso arm; switch `AnyLasso` to `any_estimator_typestate!`. Add hyperparams_eq defaults case. Commit together.
  </action>
  <verify>
    <automated>cargo test --features cpu --test lasso_test && cargo build -p mlrs-py --features cpu</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --features cpu --test lasso_test` passes (1e-5, zero fit-body compute deltas).
    - `cargo build -p mlrs-py --features cpu` succeeds.
    - `! grep -q 'crate::traits' crates/mlrs-algos/src/linear/lasso.rs` and `! grep -q 'fn with_opts' crates/mlrs-algos/src/linear/lasso.rs` (with_opts removed, folded into builder).
    - LassoBuilder exposes alpha, fit_intercept, max_iter, tol setters.
  </acceptance_criteria>
  <done>Lasso migrated with with_opts folded into the builder, oracle green, mlrs-py builds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Migrate ElasticNet (shape A, new + with_opts → builder)</name>
  <files>crates/mlrs-algos/src/linear/elastic_net.rs, crates/mlrs-algos/tests/elastic_net_test.rs, crates/mlrs-py/src/estimators/linear.rs</files>
  <read_first>
    - crates/mlrs-algos/src/linear/elastic_net.rs (full — struct, new(alpha, l1_ratio, fit_intercept), with_opts(...), Fit/Predict, fit body)
    - crates/mlrs-algos/src/linear/lasso.rs (the JUST-migrated sibling — same CD-solver family, copy its builder shape)
    - crates/mlrs-algos/tests/elastic_net_test.rs (call sites — ~3)
    - crates/mlrs-py/src/estimators/linear.rs (PyElasticNet fit arms + AnyElasticNet + import)
    - .planning/phases/16-builder-retrofit-sweep-shim-coverage/16-PATTERNS.md (§1)
  </read_first>
  <behavior>
    - ElasticNet::<F, Unfit>::new() yields sklearn defaults (alpha=1.0, l1_ratio=0.5, fit_intercept=true, max_iter/tol matching current); builder exposes all; new() == builder().build()?.
    - Oracle passes unchanged.
  </behavior>
  <action>
    Same recipe as Lasso (ElasticNet is its sibling). ElasticNetBuilder subsumes `new` + `with_opts` with setters `.alpha(f64).l1_ratio(f64).fit_intercept(bool).max_iter(usize).tol(f64)`; defaults from the current constructors. Relocate data-independent validation (alpha>=0, l1_ratio in [0,1] if present) to `build()` → BuildError. Consuming-self Fit, BYTE-IDENTICAL math, Fitted accessors/Predict. Migrate ~3 test call sites + PyElasticNet arm; `AnyElasticNet` → `any_estimator_typestate!`. hyperparams_eq case. Commit together.
  </action>
  <verify>
    <automated>cargo test --features cpu --test elastic_net_test && cargo build -p mlrs-py --features cpu</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --features cpu --test elastic_net_test` passes (1e-5, zero fit-body deltas).
    - `cargo build -p mlrs-py --features cpu` succeeds.
    - `! grep -q 'crate::traits' crates/mlrs-algos/src/linear/elastic_net.rs` and `! grep -q 'fn with_opts' crates/mlrs-algos/src/linear/elastic_net.rs`.
    - ElasticNetBuilder exposes all 5 setters.
  </acceptance_criteria>
  <done>ElasticNet migrated, oracle green, mlrs-py builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → build() | alpha/l1_ratio hyperparameter validation (data-independent) |
| host x/y geometry → fit | shapes reach validate_geometry before device launch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-V5 | Tampering / DoS | linear-family fit geometry guard | mitigate | `validate_geometry` stays at the top of each ported fit; data-independent alpha/l1_ratio validation relocates to build() → BuildError, not dropped |
| T-16-GUARDF64 | DoS | PyLasso/PyElasticNet/PyLinearRegression F64 arms | mitigate | `guard_f64()` + `lock_pool()` preserved verbatim in migrated PyO3 fits |
| T-16-ARM | Tampering | Any* Fitted arm types | mitigate | `any_estimator_typestate!` ensures fit arms are `T<f*, Fitted>` |
</threat_model>

<verification>
- All three oracle suites green (1e-5, f64+f32).
- `cargo build -p mlrs-py --features cpu` green after each migration.
- None of the three files reference `crate::traits`; `with_opts` removed from Lasso/ElasticNet.
</verification>

<success_criteria>
- LinearRegression, Lasso, ElasticNet on the typestate surface; with_opts folded into builders; oracles green; mlrs-py builds.
</success_criteria>

<output>
Create `.planning/phases/16-builder-retrofit-sweep-shim-coverage/16-02-SUMMARY.md` when done.
</output>
