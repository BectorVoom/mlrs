---
phase: 16-builder-retrofit-sweep-shim-coverage
plan: 04
type: execute
wave: 4
depends_on: ["16-03"]
files_modified:
  - crates/mlrs-algos/src/decomposition/pca.rs
  - crates/mlrs-algos/src/decomposition/truncated_svd.rs
  - crates/mlrs-algos/src/decomposition/incremental_pca.rs
  - crates/mlrs-algos/tests/pca_test.rs
  - crates/mlrs-algos/tests/truncated_svd_test.rs
  - crates/mlrs-algos/tests/incremental_pca_test.rs
  - crates/mlrs-py/src/estimators/decomposition.rs
autonomous: true
requirements: [BLDR-03]
must_haves:
  truths:
    - "PCA, TruncatedSvd, IncrementalPCA construct only via zero-arg new() + builder; arg-new removed"
    - "PCA's inverse_transform (reconstruction) still works via the Plan-00 typestate Transform default override"
    - "IncrementalPCA's PartialFit is impl'd on BOTH Unfit (first batch) and Fitted (subsequent batches) for Fitted→Fitted streaming"
    - "Each estimator's oracle suite passes unchanged at 1e-5 (D-03)"
    - "cargo build -p mlrs-py --features cpu succeeds after each PyO3 arm migrates"
  artifacts:
    - path: "crates/mlrs-algos/src/decomposition/incremental_pca.rs"
      provides: "IncrementalPCA<F, S=Unfit> with typestate PartialFit on Unfit AND Fitted"
      contains: "IncrementalPCA<F, S"
    - path: "crates/mlrs-algos/src/decomposition/pca.rs"
      provides: "PCA<F, S=Unfit> + Transform (incl. inverse_transform override) on Fitted"
      contains: "struct Pca<F, S"
  key_links:
    - from: "crates/mlrs-algos/src/decomposition/incremental_pca.rs"
      to: "crates/mlrs-algos/src/typestate.rs"
      via: "impl PartialFit for IncrementalPCA<F, Unfit> (type Fitted = Fitted) AND for IncrementalPCA<F, Fitted> (type Fitted = Self)"
      pattern: "PartialFit.*IncrementalPCA<F, (Unfit|Fitted)>"
---

<objective>
Bulk sweep — decomposition/ (D-06). PCA + TruncatedSvd are shape-A transformers; PCA exercises the `Transform::inverse_transform` default added in Plan 00. IncrementalPCA is the ONLY `PartialFit` consumer — the multi-transition typestate case (Pitfall 5): `PartialFit` must be impl'd on BOTH `Unfit` (first batch) and `Fitted` (subsequent batches) for `Fitted → Fitted` streaming.

Done AFTER the simpler linear cases (D-06) because IncrementalPCA's multi-state lifecycle is the hardest transition shape.

Purpose: Migrate the decomposition module and prove the PartialFit multi-transition + Transform inverse_transform paths.
Output: PCA, TruncatedSvd, IncrementalPCA on the typestate surface.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-builder-retrofit-sweep-shim-coverage/16-CONTEXT.md
@.planning/phases/16-builder-retrofit-sweep-shim-coverage/16-RESEARCH.md
@.planning/phases/16-builder-retrofit-sweep-shim-coverage/16-PATTERNS.md
@.planning/phases/16-builder-retrofit-sweep-shim-coverage/16-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
- `Pca<F, S=Unfit>` + `PcaBuilder` (`.n_components(usize)`) + Fitted Transform (incl. inverse_transform override) + accessors
- `TruncatedSvd<F, S=Unfit>` + `TruncatedSvdBuilder` (`.n_components(usize)`) + Fitted Transform + accessors
- `IncrementalPCA<F, S=Unfit>` + `IncrementalPcaBuilder` (`.n_components(usize).whiten(bool).batch_size(Option<usize>)`) + Fit + PartialFit (on Unfit AND Fitted) + Transform on Fitted
- `AnyPca`/`AnyTruncatedSvd`/`AnyIncrementalPCA` → `any_estimator_typestate!`
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate PCA (shape A, transformer; inverse_transform override)</name>
  <files>crates/mlrs-algos/src/decomposition/pca.rs, crates/mlrs-algos/tests/pca_test.rs, crates/mlrs-py/src/estimators/decomposition.rs</files>
  <read_first>
    - crates/mlrs-algos/src/decomposition/pca.rs (full — struct Pca<F>, new(n_components), Fit + Transform (incl. inverse_transform), accessors, fit body + geometry guard)
    - crates/mlrs-algos/src/manifold/umap.rs (Transform-on-Fitted exemplar) + crates/mlrs-algos/src/linear/ridge.rs (migrated Shape-A reference)
    - crates/mlrs-algos/src/typestate.rs (Transform with the Plan-00 inverse_transform default; Fit/Fitted/Unfit/validate_geometry)
    - crates/mlrs-algos/src/traits.rs:127-155 (old Transform + inverse_transform default — confirm PCA's current override body to port verbatim)
    - crates/mlrs-algos/tests/pca_test.rs (~3 call sites; confirm inverse_transform coverage)
    - crates/mlrs-py/src/estimators/decomposition.rs (PyPCA fit + transform/inverse_transform arms + AnyPca + import)
    - .planning/phases/16-builder-retrofit-sweep-shim-coverage/16-PATTERNS.md (§1, §3 inverse_transform default, §4)
  </read_first>
  <behavior>
    - Pca::<F, Unfit>::new() yields the sklearn default (n_components handling matching current new(); note: PCA currently takes a required n_components — keep the builder setter, default per current new()).
    - On Fitted: transform + inverse_transform (reconstruction) work; oracle (incl. inverse_transform if covered) passes unchanged at 1e-5.
  </behavior>
  <action>
    Apply Shape-A recipe to pca.rs. PcaBuilder `.n_components(usize)`; zero-arg `new()` with the current default (read pca.rs — if n_components has no natural default, mirror sklearn's None/all-components behavior the current code uses; do not invent). Swap `use crate::traits::{Fit, Transform}` → typestate. Consuming-self `Fit::fit` (validate_geometry, BYTE-IDENTICAL math). Move `impl Transform` onto `impl<F> Pca<F, Fitted>` — OVERRIDE `inverse_transform` with PCA's current reconstruction body ported VERBATIM (the typestate Transform default returns Unsupported; PCA overrides it — this is why the Plan-00 default port matters). Move accessors (components_, explained_variance_, etc.) to Fitted. Migrate ~3 test call sites + PyPCA arm (fit + transform + inverse_transform forwarders); `AnyPca` → `any_estimator_typestate!`. hyperparams_eq case. Commit.
  </action>
  <verify>
    <automated>cargo test --features cpu --test pca_test && cargo build -p mlrs-py --features cpu</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --features cpu --test pca_test` passes (1e-5, incl. inverse_transform path if covered; zero fit-body deltas).
    - `cargo build -p mlrs-py --features cpu` succeeds.
    - `! grep -q 'crate::traits' crates/mlrs-algos/src/decomposition/pca.rs`.
    - `grep -c 'fn inverse_transform' crates/mlrs-algos/src/decomposition/pca.rs` > 0 (override present on Fitted).
  </acceptance_criteria>
  <done>PCA migrated with inverse_transform override on Fitted, oracle green, mlrs-py builds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate TruncatedSvd (shape A, transformer)</name>
  <files>crates/mlrs-algos/src/decomposition/truncated_svd.rs, crates/mlrs-algos/tests/truncated_svd_test.rs, crates/mlrs-py/src/estimators/decomposition.rs</files>
  <read_first>
    - crates/mlrs-algos/src/decomposition/truncated_svd.rs (full — struct, new(n_components), Fit + Transform, accessors, fit body)
    - crates/mlrs-algos/src/decomposition/pca.rs (the JUST-migrated transformer sibling)
    - crates/mlrs-algos/tests/truncated_svd_test.rs (~3 call sites)
    - crates/mlrs-py/src/estimators/decomposition.rs (PyTruncatedSVD + AnyTruncatedSvd + import)
    - .planning/phases/16-builder-retrofit-sweep-shim-coverage/16-PATTERNS.md (§1)
  </read_first>
  <behavior>
    - TruncatedSvd::<F, Unfit>::new() default per current new(); builder `.n_components(usize)`; transform on Fitted; oracle unchanged.
  </behavior>
  <action>
    Same Shape-A transformer recipe as PCA (TruncatedSvd does NOT need an inverse_transform override unless its current code has one — check; if absent, the typestate default Unsupported is correct). TruncatedSvdBuilder `.n_components(usize)`; consuming-self Fit (BYTE-IDENTICAL); Transform + accessors on Fitted. Migrate ~3 test call sites + PyTruncatedSVD arm; `AnyTruncatedSvd` → `any_estimator_typestate!`. hyperparams_eq case. Commit.
  </action>
  <verify>
    <automated>cargo test --features cpu --test truncated_svd_test && cargo build -p mlrs-py --features cpu</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --features cpu --test truncated_svd_test` passes (1e-5, zero fit-body deltas).
    - `cargo build -p mlrs-py --features cpu` succeeds.
    - `! grep -q 'crate::traits' crates/mlrs-algos/src/decomposition/truncated_svd.rs`.
  </acceptance_criteria>
  <done>TruncatedSvd migrated, oracle green, mlrs-py builds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Migrate IncrementalPCA (shape A; PartialFit multi-transition — Pitfall 5)</name>
  <files>crates/mlrs-algos/src/decomposition/incremental_pca.rs, crates/mlrs-algos/tests/incremental_pca_test.rs, crates/mlrs-py/src/estimators/decomposition.rs</files>
  <read_first>
    - crates/mlrs-algos/src/decomposition/incremental_pca.rs (full — struct, new(n_components, whiten, batch_size: Option<usize>), Fit + PartialFit + Transform, partial_fit body, fit body, accessors)
    - crates/mlrs-algos/src/typestate.rs (PartialFit at :195-215 — read the doc note about Fitted→Fitted streaming; Fit/Transform/Fitted/Unfit)
    - crates/mlrs-algos/src/decomposition/pca.rs (the migrated transformer sibling)
    - crates/mlrs-algos/tests/incremental_pca_test.rs (~7 call sites — the heaviest; confirm multi-batch partial_fit coverage)
    - crates/mlrs-py/src/estimators/decomposition.rs (PyIncrementalPCA fit + partial_fit arms + AnyIncrementalPCA + import)
    - .planning/phases/16-builder-retrofit-sweep-shim-coverage/16-PATTERNS.md (§3 PartialFit note + Pitfall 5 in RESEARCH §Common Pitfalls)
  </read_first>
  <behavior>
    - IncrementalPCA::<F, Unfit>::new() default per current new(); builder `.n_components(usize).whiten(bool).batch_size(Option<usize>)`.
    - partial_fit on Unfit (first batch) → Fitted; partial_fit on Fitted (subsequent batch) → Fitted (Self). Multi-batch streaming oracle passes unchanged.
    - transform on Fitted.
  </behavior>
  <action>
    Apply Shape-A recipe PLUS the PartialFit multi-transition (Pitfall 5 — the critical IncrementalPCA-specific step): add `<F, S = Unfit>` + PhantomData; IncrementalPcaBuilder with `.n_components(usize).whiten(bool).batch_size(Option<usize>)`; zero-arg `new()` defaults per current new(). Swap `use crate::traits::{Fit, PartialFit, Transform}` → typestate. Implement `typestate::PartialFit for IncrementalPCA<F, Unfit>` (`type Fitted = IncrementalPCA<F, Fitted>`, first-batch path) AND `typestate::PartialFit for IncrementalPCA<F, Fitted>` (`type Fitted = Self`, subsequent-batch path) so `Fitted → Fitted` streaming works — a naive single-Unfit impl breaks multi-batch partial_fit. Consuming-self `Fit::fit` (validate_geometry, BYTE-IDENTICAL math for both the one-shot fit and the partial_fit accumulation logic — copy every compute line). Move Transform + accessors onto Fitted. Migrate the ~7 test call sites (incl. the multi-batch partial_fit sequence — each partial_fit now consumes-and-returns the next state) + PyIncrementalPCA arms (fit AND partial_fit; the partial_fit PyO3 arm must thread the state transition); `AnyIncrementalPCA` → `any_estimator_typestate!`. hyperparams_eq case. Commit.
  </action>
  <verify>
    <automated>cargo test --features cpu --test incremental_pca_test && cargo build -p mlrs-py --features cpu</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --features cpu --test incremental_pca_test` passes (1e-5, multi-batch partial_fit covered; zero fit/partial_fit-body compute deltas).
    - `cargo build -p mlrs-py --features cpu` succeeds.
    - `! grep -q 'crate::traits' crates/mlrs-algos/src/decomposition/incremental_pca.rs`.
    - `grep -cE 'PartialFit.*IncrementalPCA<F, Unfit>|PartialFit.*IncrementalPCA<F, Fitted>' crates/mlrs-algos/src/decomposition/incremental_pca.rs` returns 2 (impl'd on BOTH states — Pitfall 5).
  </acceptance_criteria>
  <done>IncrementalPCA migrated with PartialFit on both Unfit and Fitted, multi-batch oracle green, mlrs-py builds; decomposition module complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → build() | n_components / batch_size hyperparameter (data-independent) |
| host x geometry → fit/partial_fit | shapes reach validate_geometry before device launch |
| streaming batches → partial_fit | each batch's geometry must be guarded on every transition |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-V5 | Tampering / DoS | decomposition fit + partial_fit geometry guard | mitigate | `validate_geometry` stays atop fit AND every partial_fit transition (Unfit and Fitted), before any device launch |
| T-16-GUARDF64 | DoS | PyPCA/PyTruncatedSVD/PyIncrementalPCA F64 arms | mitigate | `guard_f64()` + `lock_pool()` preserved in migrated PyO3 fit/partial_fit |
| T-16-ARM | Tampering | Any* Fitted arms + IncrementalPCA Fitted→Fitted | mitigate | `any_estimator_typestate!` arms are `T<f*, Fitted>`; partial_fit on Fitted returns Self (not a fresh Unfit) |
</threat_model>

<verification>
- All three oracle suites green (1e-5; IncrementalPCA multi-batch covered).
- `cargo build -p mlrs-py --features cpu` green.
- No decomposition/ file references `crate::traits`; PartialFit on both IncrementalPCA states; PCA inverse_transform override present.
</verification>

<success_criteria>
- PCA, TruncatedSvd, IncrementalPCA migrated; inverse_transform + PartialFit multi-transition proven; decomposition module complete.
</success_criteria>

<output>
Create `.planning/phases/16-builder-retrofit-sweep-shim-coverage/16-04-SUMMARY.md` when done. Record the PartialFit-on-both-states pattern for any future streaming estimators.
</output>
