---
phase: 16-builder-retrofit-sweep-shim-coverage
plan: 07
type: execute
wave: 7
depends_on: ["16-06"]
files_modified:
  - crates/mlrs-algos/src/projection/gaussian.rs
  - crates/mlrs-algos/src/projection/sparse.rs
  - crates/mlrs-algos/src/density/kernel_density.rs
  - crates/mlrs-algos/tests/gaussian_projection_test.rs
  - crates/mlrs-algos/tests/sparse_projection_test.rs
  - crates/mlrs-algos/tests/kernel_density_test.rs
  - crates/mlrs-py/src/estimators/projection.rs
  - crates/mlrs-py/src/estimators/kernel.rs
autonomous: true
requirements: [BLDR-03]
must_haves:
  truths:
    - "GaussianRandomProjection and SparseRandomProjection construct only via zero-arg new() + builder; the NComponents enum is a builder setter"
    - "KernelDensity (shape A', inherent fit + ScoreSamples) ADOPTS typestate Fit and gates ScoreSamples on Fitted"
    - "Each estimator's oracle suite passes unchanged at 1e-5 (D-03)"
    - "cargo build -p mlrs-py --features cpu succeeds after each PyO3 arm migrates"
  artifacts:
    - path: "crates/mlrs-algos/src/projection/gaussian.rs"
      provides: "GaussianRandomProjection<F, S=Unfit> + builder (.n_components(NComponents).seed(u64).eps(f64)) + Fitted Transform"
      contains: "GaussianRandomProjection<F, S"
    - path: "crates/mlrs-algos/src/density/kernel_density.rs"
      provides: "KernelDensity<F, S=Unfit> adopting typestate Fit + ScoreSamples on Fitted"
      contains: "KernelDensity<F, S"
  key_links:
    - from: "crates/mlrs-algos/src/density/kernel_density.rs"
      to: "crates/mlrs-algos/src/typestate.rs"
      via: "newly ADOPTS impl typestate::Fit for KernelDensity<F, Unfit>; ScoreSamples moves to typestate and is gated on Fitted"
      pattern: "typestate::(Fit|ScoreSamples)"
---

<objective>
Bulk sweep — projection/ + density/ (D-06). GaussianRandomProjection + SparseRandomProjection are shape-A transformers whose `new` takes an `NComponents` enum (the builder setter must accept the enum). KernelDensity is shape A' — it has an INHERENT `fit` plus an OLD `crate::traits::ScoreSamples` impl (no `Fit` trait); the retrofit ADOPTS `typestate::Fit` and moves `ScoreSamples` to the typestate version, gated on Fitted (RESEARCH Open Q3 + verified A3).

Purpose: Migrate projection + density; prove the enum-typed builder setter and the ScoreSamples-only lifecycle adoption.
Output: GaussianRandomProjection, SparseRandomProjection, KernelDensity on the typestate surface.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-builder-retrofit-sweep-shim-coverage/16-CONTEXT.md
@.planning/phases/16-builder-retrofit-sweep-shim-coverage/16-RESEARCH.md
@.planning/phases/16-builder-retrofit-sweep-shim-coverage/16-PATTERNS.md
@.planning/phases/16-builder-retrofit-sweep-shim-coverage/16-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
- `GaussianRandomProjection<F, S=Unfit>` + builder (`.n_components(NComponents).seed(u64).eps(f64)`) + Fitted Fit/Transform
- `SparseRandomProjection<F, S=Unfit>` + builder + Fitted Fit/Transform
- `KernelDensity<F, S=Unfit>` + builder (`.kernel(KdKernel).bandwidth(BandwidthSpec)`) + newly-ADOPTED typestate Fit + ScoreSamples on Fitted
- `AnyGaussianRandomProjection`/`AnySparseRandomProjection`/`AnyKernelDensity` → `any_estimator_typestate!`
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate GaussianRandomProjection (shape A; NComponents enum setter)</name>
  <files>crates/mlrs-algos/src/projection/gaussian.rs, crates/mlrs-algos/tests/gaussian_projection_test.rs, crates/mlrs-py/src/estimators/projection.rs</files>
  <read_first>
    - crates/mlrs-algos/src/projection/gaussian.rs (full — struct, new(n_components: NComponents, seed: u64, eps: f64), Fit + Transform, accessors, fit body; the NComponents enum definition)
    - crates/mlrs-algos/src/linear/ridge.rs + crates/mlrs-algos/src/decomposition/pca.rs (migrated Shape-A transformer references)
    - crates/mlrs-algos/src/typestate.rs (Fit/Transform/Fitted/Unfit/validate_geometry)
    - crates/mlrs-algos/tests/gaussian_projection_test.rs (~7 call sites)
    - crates/mlrs-py/src/estimators/projection.rs (PyGaussianRandomProjection + Any* + import)
    - .planning/phases/16-builder-retrofit-sweep-shim-coverage/16-PATTERNS.md (§1 + the "NComponents enum builder setter" note in RESEARCH §Call-Site)
  </read_first>
  <behavior>
    - GaussianRandomProjection::<F, Unfit>::new() yields sklearn defaults (n_components=NComponents::Auto, eps=0.1, seed default — match current); builder `.n_components(NComponents).seed(u64).eps(f64)`; new() == builder().build()?.
    - Transform on Fitted; oracle passes unchanged.
  </behavior>
  <action>
    Apply Shape-A recipe. The builder's `n_components` setter takes the `NComponents` enum directly (not f64); `eps` is f64 (A5), `seed` is u64. Defaults from current new(). Swap `use crate::traits::{Fit, Transform}` → typestate; consuming-self Fit (validate_geometry, BYTE-IDENTICAL); Transform + components_ accessor on Fitted. Migrate ~7 test call sites + PyGaussianRandomProjection arm; `AnyGaussianRandomProjection` → `any_estimator_typestate!`. hyperparams_eq case. Commit.
  </action>
  <verify>
    <automated>cargo test --features cpu --test gaussian_projection_test && cargo build -p mlrs-py --features cpu</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --features cpu --test gaussian_projection_test` passes (1e-5, zero fit-body deltas).
    - `cargo build -p mlrs-py --features cpu` succeeds.
    - `! grep -q 'crate::traits' crates/mlrs-algos/src/projection/gaussian.rs`.
    - The builder's n_components setter accepts the NComponents enum.
  </acceptance_criteria>
  <done>GaussianRandomProjection migrated, oracle green, mlrs-py builds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate SparseRandomProjection (shape A; multi-arg new)</name>
  <files>crates/mlrs-algos/src/projection/sparse.rs, crates/mlrs-algos/tests/sparse_projection_test.rs, crates/mlrs-py/src/estimators/projection.rs</files>
  <read_first>
    - crates/mlrs-algos/src/projection/sparse.rs (full — struct, multi-arg new, Fit + Transform, accessors, fit body)
    - crates/mlrs-algos/src/projection/gaussian.rs (the JUST-migrated sibling)
    - crates/mlrs-algos/tests/sparse_projection_test.rs (~5 call sites)
    - crates/mlrs-py/src/estimators/projection.rs (PySparseRandomProjection + Any* + import)
    - .planning/phases/16-builder-retrofit-sweep-shim-coverage/16-PATTERNS.md (§1)
  </read_first>
  <behavior>
    - SparseRandomProjection::<F, Unfit>::new() yields sklearn defaults (match current multi-arg new); builder exposes its params (incl. NComponents and density if present); Transform on Fitted; oracle unchanged.
  </behavior>
  <action>
    Apply Shape-A recipe, sibling of GaussianRandomProjection. Builder exposes all `new` args (NComponents enum, density/eps as f64, seed as u64). Swap to typestate; consuming-self Fit (BYTE-IDENTICAL); Transform + accessors on Fitted. Migrate ~5 test call sites + PySparseRandomProjection arm; `AnySparseRandomProjection` → `any_estimator_typestate!`. hyperparams_eq case. Commit.
  </action>
  <verify>
    <automated>cargo test --features cpu --test sparse_projection_test && cargo build -p mlrs-py --features cpu</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --features cpu --test sparse_projection_test` passes (1e-5, zero fit-body deltas).
    - `cargo build -p mlrs-py --features cpu` succeeds.
    - `! grep -q 'crate::traits' crates/mlrs-algos/src/projection/sparse.rs`.
  </acceptance_criteria>
  <done>SparseRandomProjection migrated, oracle green, mlrs-py builds; projection module complete.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Migrate KernelDensity (shape A'; adopt typestate Fit, gate ScoreSamples on Fitted)</name>
  <files>crates/mlrs-algos/src/density/kernel_density.rs, crates/mlrs-algos/tests/kernel_density_test.rs, crates/mlrs-py/src/estimators/kernel.rs</files>
  <read_first>
    - crates/mlrs-algos/src/density/kernel_density.rs (full — struct KernelDensity<F>, new(kernel: KdKernel, bandwidth: BandwidthSpec), INHERENT fit at :178, impl ScoreSamples at :252 using crate::traits::ScoreSamples (:67); accessors; the KdKernel/BandwidthSpec types)
    - crates/mlrs-algos/src/cluster/spectral_embedding.rs (the migrated shape-A' sibling — adopt-a-trait reference)
    - crates/mlrs-algos/src/typestate.rs (Fit/ScoreSamples — ScoreSamples added in Plan 00; Fitted/Unfit/validate_geometry)
    - crates/mlrs-algos/tests/kernel_density_test.rs (~4 call sites)
    - crates/mlrs-py/src/estimators/kernel.rs (PyKernelDensity + Any* + import — note this file also wraps KernelRidge)
    - .planning/phases/16-builder-retrofit-sweep-shim-coverage/16-PATTERNS.md (§1 shape-A' note; RESEARCH A3 — ScoreSamples-only lifecycle, inherent fit)
  </read_first>
  <behavior>
    - KernelDensity::<F, Unfit>::new() yields sklearn defaults (kernel=Gaussian, bandwidth per current new()); builder `.kernel(KdKernel).bandwidth(BandwidthSpec)`; new() == builder().build()?.
    - It NEWLY adopts typestate Fit (consuming-self, its inherent fit becomes the trait impl); score_samples is gated on Fitted via the typestate ScoreSamples trait. Oracle passes unchanged.
  </behavior>
  <action>
    Apply the Shape-A' recipe: add `<F, S = Unfit>` + PhantomData + builder + zero-arg new(). Convert the INHERENT `fit` (:178) into `impl typestate::Fit for KernelDensity<F, Unfit>` (consuming-self, type Fitted = Fitted variant, validate_geometry, BYTE-IDENTICAL math). Change `use crate::traits::ScoreSamples` (:67) → `use crate::typestate::ScoreSamples` and move `impl ScoreSamples` (:252) onto `impl<F> KernelDensity<F, Fitted>` (gated on Fitted, dropping any NotFitted branch). This brings KernelDensity fully onto the single trait surface (it adopts Fit it never had + the typestate ScoreSamples). Migrate ~4 test call sites + PyKernelDensity arm; `AnyKernelDensity` → `any_estimator_typestate!`. hyperparams_eq case. Commit.
  </action>
  <verify>
    <automated>cargo test --features cpu --test kernel_density_test && cargo build -p mlrs-py --features cpu</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --features cpu --test kernel_density_test` passes (1e-5, zero fit-body deltas).
    - `cargo build -p mlrs-py --features cpu` succeeds.
    - `! grep -q 'crate::traits' crates/mlrs-algos/src/density/kernel_density.rs` (off the old surface; now uses typestate::ScoreSamples + typestate::Fit).
    - `grep -cE 'typestate::(Fit|ScoreSamples)' crates/mlrs-algos/src/density/kernel_density.rs` > 0.
  </acceptance_criteria>
  <done>KernelDensity migrated, adopting typestate Fit + ScoreSamples on Fitted, oracle green, mlrs-py builds; density module complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → build() | n_components/eps/kernel/bandwidth hyperparameter (data-independent) |
| host x geometry → fit/score_samples | shapes reach validate_geometry before device launch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-V5 | Tampering / DoS | projection/density fit + score_samples geometry guard | mitigate | `validate_geometry` stays atop ported fit AND score_samples; data-independent validation relocates to build() → BuildError, not dropped |
| T-16-GUARDF64 | DoS | PyGaussian/PySparse/PyKernelDensity F64 arms | mitigate | `guard_f64()` + `lock_pool()` preserved in migrated PyO3 fits |
| T-16-ARM | Tampering | Any* Fitted arms | mitigate | `any_estimator_typestate!` arms are `T<f*, Fitted>` |
</threat_model>

<verification>
- All three oracle suites green (1e-5).
- `cargo build -p mlrs-py --features cpu` green.
- No projection/density file references `crate::traits`; KernelDensity now on the typestate surface.
</verification>

<success_criteria>
- GaussianRandomProjection, SparseRandomProjection, KernelDensity migrated; enum-setter + shape-A' ScoreSamples adoption proven; projection + density modules complete.
</success_criteria>

<output>
Create `.planning/phases/16-builder-retrofit-sweep-shim-coverage/16-07-SUMMARY.md` when done.
</output>
