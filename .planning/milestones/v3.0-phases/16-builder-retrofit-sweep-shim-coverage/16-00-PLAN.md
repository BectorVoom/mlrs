---
phase: 16-builder-retrofit-sweep-shim-coverage
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - crates/mlrs-algos/src/typestate.rs
  - crates/mlrs-algos/tests/typestate_test.rs
  - crates/mlrs-py/python/tests/test_params.py
autonomous: true
requirements: [BLDR-03, SHIM-01, SHIM-03]
must_haves:
  truths:
    - "typestate.rs defines all 9 lifecycle/accessor traits an estimator can implement (4 existing + 5 added)"
    - "Transform in typestate.rs carries the defaulted inverse_transform method (PCA reconstruction path) so PCA does not regress"
    - "A static AST-based __init__-purity test exists in test_params.py and runs without the compiled _mlrs extension"
    - "The builder-setter type convention (f64 setters; build::<F>() narrows) is locked and documented for the sweep"
  artifacts:
    - path: "crates/mlrs-algos/src/typestate.rs"
      provides: "5 added &self accessor traits (PredictLabels, KNeighbors, ScoreSamples, PredictProba, PredictLogProba) + Transform::inverse_transform default"
      contains: "pub trait PredictLabels"
    - path: "crates/mlrs-algos/tests/typestate_test.rs"
      provides: "Compile/build proof that the 5 new traits are well-formed and the inverse_transform default exists"
    - path: "crates/mlrs-py/python/tests/test_params.py"
      provides: "AST __init__-purity test (import ast, inspect.getsource)"
      contains: "import ast"
  key_links:
    - from: "crates/mlrs-algos/src/typestate.rs"
      to: "crates/mlrs-algos/src/traits.rs"
      via: "the 5 new traits mirror the old traits.rs signatures verbatim (host geometry args, AlgoError return)"
      pattern: "predict_labels|kneighbors|score_samples|predict_proba|predict_log_proba"
---

<objective>
Wave 0 — the BLOCKING prerequisite for every estimator migration. Grow `mlrs_algos::typestate` to mirror ALL 9 old `crate::traits` traits (it currently has 4 of 9), add the `Transform::inverse_transform` default that PCA needs, lock the builder-setter type convention (`f64`), and add the NEW AST-based `__init__`-purity test to the Python static shim gate.

Per D-01: typestate becomes the single trait surface; per D-04/A5: builder setters are `f64`, `build::<F>()` narrows. No estimator can migrate until the 5 missing accessor traits exist here.

Purpose: Unblock the sweep (Plans 01-08) and the shim work (Plan 10). The 5 accessor traits are consumed by classifiers/neighbors/density/NB estimators that cannot migrate to typestate until the target trait exists.
Output: Expanded `typestate.rs`, a `typestate_test.rs` build/compile proof, and a new `test_init_purity_ast` test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-builder-retrofit-sweep-shim-coverage/16-CONTEXT.md
@.planning/phases/16-builder-retrofit-sweep-shim-coverage/16-RESEARCH.md
@.planning/phases/16-builder-retrofit-sweep-shim-coverage/16-PATTERNS.md
</context>

<artifacts_this_phase_produces>
NEW symbols created in this plan (the source-grounding pass must treat these as net-new, not drift):
- `typestate::PredictLabels<F>` trait (added)
- `typestate::KNeighbors<F>` trait (added)
- `typestate::ScoreSamples<F>` trait (added)
- `typestate::PredictProba<F>` trait (added)
- `typestate::PredictLogProba<F>` trait (added)
- `typestate::Transform::inverse_transform` default method (added to existing `Transform`)
- `test_init_purity_ast` test function in `test_params.py` (NEW — no `import ast` exists in the test tree today)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Port the 5 missing accessor traits + Transform::inverse_transform default into typestate.rs</name>
  <files>crates/mlrs-algos/src/typestate.rs</files>
  <read_first>
    - crates/mlrs-algos/src/typestate.rs (full — current 4 traits Fit/Predict/Transform/PartialFit at :127-215, sealed State machinery at :82-115, validate_geometry at :59-76, module doc at :9-18)
    - crates/mlrs-algos/src/traits.rs (full — the 9 old trait signatures; the 5 to port: PredictLabels :168-181, KNeighbors :191-213, ScoreSamples :231-245, PredictProba :256-270, PredictLogProba :288-303; Transform::inverse_transform default at :145-155)
    - .planning/phases/16-builder-retrofit-sweep-shim-coverage/16-PATTERNS.md (§3 — trait-addition template, copy the Predict shape at typestate.rs:152-164)
  </read_first>
  <action>
    Add 5 new `&self` accessor traits to typestate.rs, each generic `<F> where F: Float + CubeElement + Pod`, mirroring the corresponding traits.rs signatures VERBATIM (same method name, same host-geometry args `(pool: &mut BufferPool<ActiveRuntime>, x: &DeviceArray<ActiveRuntime, F>, shape: (usize, usize))`, same `Result<_, AlgoError>` return): `PredictLabels` (returns `DeviceArray<ActiveRuntime, i32>`), `KNeighbors` (takes `k: usize`, returns the `(DeviceArray<F>, DeviceArray<i32>)` tuple per traits.rs:191-213), `ScoreSamples` (returns `DeviceArray<ActiveRuntime, F>`), `PredictProba` (returns `DeviceArray<ActiveRuntime, F>`), `PredictLogProba` (returns `DeviceArray<ActiveRuntime, F>`). These traits have NO associated `type Fitted` (they read fitted state, they do not transition lifecycle) — they will be impl'd ONLY on the `Fitted`-tagged estimator (mirroring how `Transform` at typestate.rs:169-181 is impl'd only on `Umap<F, Fitted>`). Then add the defaulted `inverse_transform` method to the EXISTING `Transform` trait (port traits.rs:145-155 verbatim — returns `Err(AlgoError::Unsupported { estimator: "transform", operation: "inverse_transform" })` as the default body). Update the typestate.rs module doc header (typestate.rs:9-18 says "30 estimators continue to compile against [traits.rs]... FROZEN") to note that traits.rs is being hard-deleted this phase (D-01) and typestate is now the single surface. Do NOT remove or alter the existing Fit/Predict/Transform/PartialFit traits or the sealed State machinery. Builder-setter convention lock (A5): add a doc comment near the top of typestate.rs stating the phase-16 convention — "Builder setters are `f64`-typed for uniformity with the shipped mbsgd/umap builders; `build::<F>()` narrows via cast" — so every downstream retrofit follows one convention.
  </action>
  <verify>
    <automated>cargo build -p mlrs-algos --features cpu</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p mlrs-algos --features cpu` succeeds.
    - `grep -cE 'pub trait (PredictLabels|KNeighbors|ScoreSamples|PredictProba|PredictLogProba)' crates/mlrs-algos/src/typestate.rs` returns 5.
    - `grep -c 'fn inverse_transform' crates/mlrs-algos/src/typestate.rs` returns at least 1 (the default in Transform).
    - Each new trait's method signature matches the corresponding traits.rs trait (same method name, same arg arity) — verifiable by reading both files side by side.
  </acceptance_criteria>
  <done>typestate.rs defines all 9 traits (4 pre-existing + 5 added) and Transform carries the inverse_transform default; mlrs-algos builds under cpu.</done>
</task>

<task type="auto">
  <name>Task 2: Add typestate_test.rs build/compile proof for the new trait surface</name>
  <files>crates/mlrs-algos/tests/typestate_test.rs</files>
  <read_first>
    - crates/mlrs-algos/src/typestate.rs (the surface just expanded in Task 1)
    - crates/mlrs-algos/tests/ (list — confirm whether typestate_test.rs already exists; AGENTS.md §2: tests live in crates/*/tests/, NOT in-source)
    - crates/mlrs-algos/src/manifold/umap.rs (umap_test reference for how a Fitted-only trait impl is exercised)
  </read_first>
  <action>
    Create or extend `crates/mlrs-algos/tests/typestate_test.rs` with a minimal compile/build-level proof that the 5 new accessor traits are well-formed and `Transform::inverse_transform` has a callable default. Define a tiny test-only marker struct with `Unfit`/`Fitted` states (or reuse an existing exemplar if the file already has one) and a trivial `impl` of one of the new traits on its `Fitted` variant, asserting the trait is object-usable / the method resolves. Also assert the `inverse_transform` default returns the `Unsupported` error variant when not overridden (call it on a type that impls `Transform` without overriding). Keep this test free of device kernels (no CubeCL launch needed — it is a type/trait-surface proof). This is the Wave 0 gate that the trait additions are syntactically and coherence-correct before any estimator depends on them.
  </action>
  <verify>
    <automated>cargo test --features cpu --test typestate_test</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --features cpu --test typestate_test` compiles and passes.
    - The test references at least 2 of the 5 newly-added traits by name (proving they are importable from `mlrs_algos::typestate`).
    - The inverse_transform-default assertion confirms the `Unsupported` error path.
  </acceptance_criteria>
  <done>typestate_test.rs green; the new trait surface is proven coherent before estimator migrations begin.</done>
</task>

<task type="auto">
  <name>Task 3: Add the AST-based __init__-purity test to test_params.py</name>
  <files>crates/mlrs-py/python/tests/test_params.py</files>
  <read_first>
    - crates/mlrs-py/python/tests/test_params.py (full — runtime purity test at :117-127, EXPECTED_PARAMS/SET_PARAM/ALL_12 at :25-83, the PCA _construct special-case at :86-91)
    - crates/mlrs-py/python/mlrs/linear.py (Ridge __init__ at :60-63 — the pure shape: self.<arg> = <arg>, no compute)
    - crates/mlrs-py/python/mlrs/base.py (MlrsBase — confirm tests import the package without the compiled _mlrs extension)
    - .planning/phases/16-builder-retrofit-sweep-shim-coverage/16-PATTERNS.md (§7b — AST purity sketch)
  </read_first>
  <action>
    Add a NEW test function `test_init_purity_ast` to test_params.py (this is genuinely new — `grep 'import ast'` returns nothing in the test tree today). At module top add `import ast` and `import inspect`. The test parametrizes over the current shim matrix (`ALL_12` for now; Plan 10 expands the matrix to the full set — keep the parametrization source as the shared list so it grows automatically). For each shim class: parse `ast.parse(inspect.getsource(cls.__init__).strip())`, take the function def, and assert every statement in its body is an `ast.Assign` whose single target is `self.<name>` (`ast.Attribute` with `ast.Name` value `id == "self"`) and whose value is a bare `ast.Name` with the SAME identifier as the attribute (`self.x = x` — store verbatim, same name). REJECT any `ast.Call`, `ast.BinOp`, `ast.Compare`, or other computation/validation node in the body. Allow the trailing `self.output_type = output_type` assignment in the same bare-Name shape. This is the strongest SHIM-01 guarantee achievable without FFI (D-07 step 3). Do NOT modify the existing runtime purity test (test_params.py:117-127) — the AST test is additive and stricter. Do NOT yet expand the matrix beyond ALL_12 here (Plan 10 owns the matrix expansion to avoid touching the same lists in two waves).
  </action>
  <verify>
    <automated>cd crates/mlrs-py/python && python -m pytest tests/test_params.py -k init_purity -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'import ast' crates/mlrs-py/python/tests/test_params.py` returns at least 1.
    - `pytest tests/test_params.py -k init_purity` passes for every class in the current `ALL_12` matrix WITHOUT importing the compiled `_mlrs` extension.
    - The test asserts (not just inspects) and FAILS if a `self.x = validate(x)`-style impure body is introduced — verifiable by a temporary local mutation that the executor reverts.
  </acceptance_criteria>
  <done>test_init_purity_ast exists, imports ast, and green over the current matrix without the compiled extension.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python ctor → shim state | user-supplied kwargs stored into the pure-Python shim (must be stored verbatim, no validation in __init__) |
| host geometry → typestate trait method | shapes/lengths passed into accessor traits cross into device reads downstream |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-V5 | Tampering | the 5 new typestate accessor traits' method signatures | mitigate | Port traits.rs signatures verbatim — same host-geometry args reach the same downstream device guard; no validation is added OR dropped at the trait boundary (the guard stays in each estimator's fit/accessor, ported in later plans) |
| T-16-00-PURITY | Tampering | shim `__init__` purity | mitigate | The AST test makes any computation/validation injected into `__init__` a hard test failure (SHIM-01 invariant enforced statically) |
| T-16-SEAL | Tampering | `State` seal | accept | `State: sealed::Sealed` is a closed set; this plan adds traits, not states — no third lifecycle state introduced; no action beyond "do not break the seal" |
</threat_model>

<verification>
- `cargo build -p mlrs-algos --features cpu` succeeds (trait additions coherent).
- `cargo test --features cpu --test typestate_test` green.
- `pytest crates/mlrs-py/python/tests/test_params.py -k init_purity` green (no compiled extension).
- The 5 new traits + inverse_transform default are present (greps above).
</verification>

<success_criteria>
- All 9 traits exist in typestate.rs; Transform carries inverse_transform default.
- The builder-setter convention (`f64`) is documented in typestate.rs for the sweep.
- The AST purity test exists and is green over the current matrix.
- Nothing in traits.rs is deleted yet (deletion is Plan 11); the existing 4 typestate traits are unchanged.
</success_criteria>

<output>
Create `.planning/phases/16-builder-retrofit-sweep-shim-coverage/16-00-SUMMARY.md` when done. Record the exact new trait names/signatures and the AST-test location so downstream plans reference them.
</output>
