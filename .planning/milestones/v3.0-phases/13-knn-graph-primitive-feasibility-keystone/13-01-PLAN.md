---
phase: 13-knn-graph-primitive-feasibility-keystone
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/gen_oracle.py
  - crates/mlrs-backend/tests/fixtures/  # committed .npz blobs (per-metric, f32+f64)
  - crates/mlrs-backend/tests/knn_graph_test.rs
  - crates/mlrs-kernels/src/distance.rs
  - crates/mlrs-kernels/src/lib.rs
  - crates/mlrs-backend/src/prims/mod.rs
autonomous: true
requirements: [PRIM-11]
user_setup: []

must_haves:
  truths:
    - "A per-metric KNN oracle fixture (Euclidean, Manhattan, Cosine, Chebyshev, Minkowski-p), f32 + f64, exists as committed .npz blobs, each carrying a duplicate-point train row"
    - "knn_graph_test.rs compiles and its per-metric oracle bodies fail (RED) only because knn_graph does not yet exist — the harness, fixtures, set-equal-index assert, duplicate-point VALUE assert, and memory-gate assert are all present"
    - "crates/mlrs-kernels/src/distance.rs module is registered in lib.rs and crates/mlrs-backend/src/prims/mod.rs declares pub mod knn_graph; (compiling scaffold)"
  artifacts:
    - path: "crates/mlrs-backend/tests/knn_graph_test.rs"
      provides: "per-metric oracle harness + duplicate-point VALUE assert + geometry-rejection test + query-axis memory gate (Wave 0 Nyquist harness for PRIM-11)"
      min_lines: 120
    - path: "scripts/gen_oracle.py"
      provides: "gen_knn extended with metric/p params + duplicate-point design"
      contains: "metric="
    - path: "crates/mlrs-kernels/src/distance.rs"
      provides: "empty-but-registered kernel module home for the new distance + self_drop kernels"
  key_links:
    - from: "crates/mlrs-backend/tests/knn_graph_test.rs"
      to: "crates/mlrs-backend/tests/fixtures/knn_*.npz"
      via: "mlrs_core::load_npz(fixture(name))"
      pattern: "load_npz\\(fixture"
    - from: "crates/mlrs-kernels/src/lib.rs"
      to: "crates/mlrs-kernels/src/distance.rs"
      via: "pub mod distance;"
      pattern: "pub mod distance"
---

<objective>
Lay the Nyquist Wave 0 foundation for PRIM-11: the per-metric sklearn oracle fixtures, the
oracle test harness (set-equal index compare + duplicate-point VALUE assert + geometry-rejection
+ query-axis memory gate), and the compiling module scaffolds (new `mlrs-kernels` distance module
+ `prims/mod.rs` `knn_graph` registration). Everything downstream verifies against this harness.

Purpose: The phase's only catch for the cpu-MLIR SILENT self-drop miscompile (spike FINDING 002-B)
is a duplicate-point VALUE-asserting oracle. It must exist BEFORE any kernel or prim is written so
Plans 02/03 land RED→GREEN against a real gate, not a happy-path stub.
Output: committed `.npz` fixtures (5 metrics × {f32,f64}, each with a duplicate-point row),
`knn_graph_test.rs` (compiles; oracle bodies RED pending `knn_graph`), `mlrs-kernels/src/distance.rs`
(registered, empty), `prims/mod.rs` declaring `pub mod knn_graph;`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-knn-graph-primitive-feasibility-keystone/13-CONTEXT.md
@.planning/phases/13-knn-graph-primitive-feasibility-keystone/13-RESEARCH.md
@.planning/phases/13-knn-graph-primitive-feasibility-keystone/13-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend gen_oracle.py with per-metric gen_knn + duplicate-point design, regen committed .npz fixtures</name>
  <files>scripts/gen_oracle.py, crates/mlrs-backend/tests/fixtures/</files>
  <read_first>
    - scripts/gen_oracle.py lines 436 (KNN_K/KNN_N_CLASSES consts) and 728-791 (existing Euclidean-only `gen_knn`, the `c()` dtype-cast closure, `dtype_tag` naming, `np.savez(... X=, Xq=, distances=, indices=)`)
    - scripts/gen_oracle.py line 707 (`gen_dbscan` `metric=` precedent)
    - 13-PATTERNS.md lines 204-216 ("scripts/gen_oracle.py — per-metric gen_knn (MODIFY)" — exact extend pattern, fixture naming `knn_{metric}_{dtype_tag}_seed{seed}.npz`, duplicate-point design)
    - 13-RESEARCH.md lines 331-341 (fixture generation; sklearn NearestNeighbors metric/p; duplicate-point R-9)
    - MEMORY.md note oracle-fixture-regen-needs-venv (regen needs /tmp venv with numpy; PEP 668; fixtures are committed blobs)
  </read_first>
  <action>
    Generalize `gen_knn(seed, dtype)` in scripts/gen_oracle.py to `gen_knn(seed, dtype, metric, p=None)` per D-05. Build `NearestNeighbors(n_neighbors=K_query, algorithm="brute", metric=metric, p=p)` where `metric ∈ {"euclidean","manhattan","cosine","chebyshev","minkowski"}` and `p` is passed only for minkowski (use a fixed test p such as 3.0). Keep the existing `c()` dtype-cast closure, `dtype_tag`, and `np.savez(out_path, X=, Xq=, distances=, indices=, k=)` structure; extend the filename to `knn_{metric}_{dtype_tag}_seed{seed}.npz`. Generate fixtures for BOTH X-vs-X self-inclusion modes the prim needs — emit enough neighbors that the test can request `include_self=false` (request `k+1` so column 0 = self at distance 0 is droppable) AND `include_self=true`. Construct the design matrix X to contain a DUPLICATE-POINT row (two identical train rows, e.g. rows 0 and 4 equal, per spike 001 kernels_and_harness.rs:143-149) so the silent-miscompile gate (R-9) has data. For cosine, ensure no zero-norm rows (A4). Regenerate the committed `.npz` blobs into crates/mlrs-backend/tests/fixtures/ for all 5 metrics × {f32,f64} using the /tmp numpy venv workaround (PEP 668). Do NOT change the existing Euclidean fixture consumed by topk_test/neighbors tests — add new metric-tagged files.
  </action>
  <verify>
    <automated>python3 -c "import ast,sys; ast.parse(open('scripts/gen_oracle.py').read()); print('parse-ok')" && ls crates/mlrs-backend/tests/fixtures/ | grep -E 'knn_(euclidean|manhattan|cosine|chebyshev|minkowski)_' | grep -c -v '^#' | awk '$1>=10{print "fixtures-ok"; exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/gen_oracle.py` parses (`python3 -c "import ast; ast.parse(...)"` exits 0) and `gen_knn` accepts a `metric` (and `p`) parameter (grep `metric=` inside the gen_knn body).
    - At least 10 committed `.npz` files matching `knn_{euclidean|manhattan|cosine|chebyshev|minkowski}_*` exist in `crates/mlrs-backend/tests/fixtures/` (5 metrics × f32+f64).
    - Each fixture's X contains at least one duplicate train row (two identical rows) — documented in the generator (the R-9 design).
    - The existing Euclidean topk/neighbors fixtures are unchanged (no regression to other suites).
  </acceptance_criteria>
  <done>Per-metric KNN oracle fixtures (5 metrics, f32+f64, each duplicate-point-bearing) are committed; gen_oracle.py regenerates them with metric/p params.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold mlrs-kernels distance module + prims/mod.rs knn_graph registration</name>
  <files>crates/mlrs-kernels/src/distance.rs, crates/mlrs-kernels/src/lib.rs, crates/mlrs-backend/src/prims/mod.rs</files>
  <read_first>
    - crates/mlrs-kernels/src/lib.rs lines 20-40 (existing `pub mod` + `pub use elementwise::{...}` / `pub use topk` block — the registration idiom)
    - crates/mlrs-backend/src/prims/mod.rs lines 36-50 (existing `pub mod distance;` / `pub mod topk;` and the Phase-8/9 stub-comment precedent for a Wave-0 registration whose body lands in a later plan)
    - 13-PATTERNS.md lines 147-161 (mod.rs + lib.rs MODIFY analogs; exact lines to add)
    - crates/mlrs-kernels/src/elementwise.rs lines 18-25 (module-doc cpu-MLIR safety rules — replicate the doc tone in the new distance.rs header, header prose must not self-invalidate grep gates: keep it free of bare metric identifiers)
  </read_first>
  <action>
    Create `crates/mlrs-kernels/src/distance.rs` as a registered module with a module doc-comment stating the cpu-MLIR authoring contract (STATIC `F::powf`, STATEMENT-form running-max, no SharedMemory/Atomic/F::INFINITY/mutable-bool/shift-loop, `CUBE_POS_X`/`UNIT_POS_X==0` for the per-row self-drop kernel). Leave the kernel bodies to Plan 02 — this file is the empty-but-compiling home (mirror the Phase-8/9 Wave-0 stub-registration precedent). Add `pub mod distance;` to crates/mlrs-kernels/src/lib.rs (the new kernel symbols' `pub use` line is added by Plan 02 inside that plan's edit, to keep file ownership clean — but it is acceptable to add the `pub use distance::{...}` line now ONLY if the symbols exist; since they do not yet, add only `pub mod distance;`). Add `pub mod knn_graph;` to crates/mlrs-backend/src/prims/mod.rs with a stub comment noting Plan 03 fills the body; do NOT create knn_graph.rs body content beyond what compiles (Plan 03 owns knn_graph.rs). To keep mod.rs compiling with an empty module declaration, create a minimal `crates/mlrs-backend/src/prims/knn_graph.rs` containing only the module doc + the `Metric` enum placeholder is NOT created here (Plan 03 owns the enum + signature) — instead make the Wave-1 scaffold an empty `knn_graph.rs` with a doc comment so `pub mod knn_graph;` compiles. (File ownership: Plan 03 fully rewrites knn_graph.rs; this plan only creates the empty compiling shell + the mod.rs line.)
  </action>
  <verify>
    <automated>cargo build -p mlrs-kernels --features cpu 2>&1 | tail -3 && cargo build -p mlrs-backend --features cpu 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p mlrs-kernels --features cpu` exits 0 with `pub mod distance;` present in lib.rs (grep `grep -v '^//' crates/mlrs-kernels/src/lib.rs | grep -c 'pub mod distance'` >= 1).
    - `cargo build -p mlrs-backend --features cpu` exits 0 with `pub mod knn_graph;` present in prims/mod.rs.
    - `crates/mlrs-kernels/src/distance.rs` exists with a cpu-MLIR-safety module doc-comment; no kernel bodies yet (Plan 02 owns them).
    - `crates/mlrs-backend/src/prims/knn_graph.rs` exists as an empty compiling shell (Plan 03 owns its body).
  </acceptance_criteria>
  <done>The new distance kernel module and the knn_graph prim module both compile as registered scaffolds under --features cpu; bodies deferred to Plans 02/03.</done>
</task>

<task type="auto">
  <name>Task 3: Write knn_graph_test.rs oracle harness (RED until knn_graph exists)</name>
  <files>crates/mlrs-backend/tests/knn_graph_test.rs</files>
  <read_first>
    - crates/mlrs-backend/tests/topk_test.rs lines 19-31 (module doc, capability gate intent), 43 (DIST_TOL=1e-5), 45-67 (host_to_f64/from_f64/fixture_vec helpers), 73-141 (check_topk oracle body + relative-tol assert at 126-131), 143-151 (fixture() workspace-root resolver), 177-197 (f32+f64 capability-gated test pair, skip_f64_with_log usage), 248-279 (rejects_bad_geometry pattern)
    - crates/mlrs-backend/tests/memory_gate_test.rs lines 72-201 (memory_gate_reuse_bounded: live_bytes conserves, peak_bytes plateaus, reuses grow — the PoolStats assertion idiom)
    - 13-PATTERNS.md lines 165-200 (knn_graph_test.rs analog mapping: set-equal index compare, duplicate-point VALUE assert R-9, include_self=true self-at-col-0, geometry-rejection, query-axis memory gate)
    - 13-RESEARCH.md lines 315-329, 411-426 (per-metric oracle shape; PRIM-11→test map; sampling rate)
    - .claude/skills/spike-findings-mlrs sources/002 host_knn brute-force reference + dup-row assertions (the VALUE-assert model)
  </read_first>
  <action>
    Create crates/mlrs-backend/tests/knn_graph_test.rs modeled on topk_test.rs. Reuse the `host_to_f64`/`from_f64`/`fixture_vec::<F>` byte-cast helpers, `fixture(name)` resolver, `DIST_TOL = 1e-5`, and `capability::skip_f64_with_log()` early-return idiom verbatim in spirit. Reference `Metric` and `knn_graph` from mlrs_backend (these do not exist yet — Plan 03 lands them; the test is RED-by-design until then; gate the test bodies behind the real call so it fails on missing symbol, not on a stub). Implement a generic `check_knn_metric<F>(fixture_name, metric, include_self)` that: loads the per-metric `.npz`, calls `knn_graph(pool, x, (n,d), k, metric, include_self, p)`, reads back indices+distances, asserts (1) per-row index SET-equality vs sklearn `indices` up to tie-ordering (sorted-vec/HashSet per row — NOT exact-index, since cross-metric tie-ordering differs), and (2) distances within DIST_TOL relative tol. Add the five per-metric f64 tests (cpu f64 gate) + an f32 companion that runs on cpu and rocm. Add `knn_rejects_bad_geometry` asserting `k > n-1` (include_self=false), `p < 1` (Minkowski), and `n*d != len` each return `Err(PrimError::ShapeMismatch{operand, ..})` with operands `"k"`,`"p"`,`"x"` BEFORE launch. Add `knn_self_drop_duplicate_point_value` (R-9): on the duplicate-point fixture with include_self=false, assert neighbor 0 of the duplicate's query row is the GENUINE duplicate index (not the self index) AND the returned distance VALUES match the brute-force reference — this is the only catch for the 002-B silent miscompile. Add `knn_include_self_returns_self_at_col0` (HDBSCAN): with include_self=true assert self index present in each row at col 0. Add `knn_memory_gate_query_axis_tiled`: thread ONE BufferPool, run knn_graph at a fixture size, assert `peak_bytes`/`live_bytes` SUB-QUADRATIC in n (never full n×n resident), `live_bytes` conserves after warmup, and scratch `reuses > 0` (hard build-failing assert!; drive f32; threshold tuning deferred to Plan 03). Do NOT put fenced kernel code in the test — only host-side calls + asserts.
  </action>
  <verify>
    <automated>test -f crates/mlrs-backend/tests/knn_graph_test.rs && grep -v '^//' crates/mlrs-backend/tests/knn_graph_test.rs | grep -c -E 'set|HashSet|sort' | awk '$1>=1{print "set-equal-present"; exit 0} {exit 1}' && grep -v '^//' crates/mlrs-backend/tests/knn_graph_test.rs | grep -c 'skip_f64_with_log' | awk '$1>=1{exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `crates/mlrs-backend/tests/knn_graph_test.rs` exists (>=120 lines) and references `Metric` + `knn_graph` from mlrs_backend.
    - Contains a per-row SET-equality index compare (grep for `HashSet`/`sort`), the `DIST_TOL = 1e-5` relative-tol distance assert, the `skip_f64_with_log` capability gate, a `knn_rejects_bad_geometry` test, a `knn_self_drop_duplicate_point_value` VALUE assert, a `knn_include_self_returns_self_at_col0` test, and a `knn_memory_gate_query_axis_tiled` PoolStats test.
    - Test compiles RED only due to missing `Metric`/`knn_graph` (i.e. `cargo test -p mlrs-backend --features cpu --test knn_graph_test` fails with unresolved-symbol, NOT a stubbed pass) — confirming the gate is real before Plan 03. Document this expected-RED state in the SUMMARY.
  </acceptance_criteria>
  <done>The per-metric oracle harness, duplicate-point VALUE assert, geometry-rejection test, and query-axis memory gate all exist and are RED-by-design pending the Plan 03 prim — the Nyquist Wave 0 gate for PRIM-11 is in place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → committed fixtures | `.npz` blobs are trusted build inputs (committed, regen via offline /tmp venv); no untrusted runtime input |
| oracle generator (offline) → repo | `gen_oracle.py` runs offline; output is the trusted committed blob |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-01 | Repudiation (data integrity) | per-metric oracle gate | high | mitigate | Duplicate-point VALUE assert (R-9) in `knn_self_drop_duplicate_point_value` — the only catch for the cpu-MLIR SILENT self-drop miscompile (FINDING 002-B). Asserts VALUES, not non-panic. |
| T-13-02 | Tampering | fixture .npz authenticity | low | accept | Fixtures are committed, regenerated offline from sklearn; no runtime/network ingress. Rationale: low-value internal test asset. |
| T-13-SC | Tampering | npm/pip/cargo installs | n/a | accept | Zero new packages this phase (RESEARCH Package Legitimacy Audit: N/A). No install tasks. |
</threat_model>

<verification>
- `python3 -c "import ast; ast.parse(open('scripts/gen_oracle.py').read())"` exits 0.
- >=10 `knn_{metric}_*` `.npz` fixtures committed under `crates/mlrs-backend/tests/fixtures/`.
- `cargo build -p mlrs-kernels --features cpu` and `cargo build -p mlrs-backend --features cpu` exit 0 (scaffolds compile).
- `crates/mlrs-backend/tests/knn_graph_test.rs` exists with the full gate set, RED-by-design pending Plan 03.
</verification>

<success_criteria>
The Nyquist Wave 0 harness for PRIM-11 is in place: per-metric duplicate-point-bearing oracle
fixtures committed, oracle/geometry/memory-gate test bodies written (RED until knn_graph lands),
and the new kernel + prim module scaffolds compile under --features cpu.
</success_criteria>

<output>
Create `.planning/phases/13-knn-graph-primitive-feasibility-keystone/13-01-SUMMARY.md` when done.
Record the expected-RED state of knn_graph_test.rs (missing `Metric`/`knn_graph`) so Plans 02/03
land GREEN against a real gate.
</output>
