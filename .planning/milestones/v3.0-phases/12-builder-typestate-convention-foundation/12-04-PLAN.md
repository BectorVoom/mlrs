---
phase: 12-builder-typestate-convention-foundation
plan: 04
type: execute
wave: 3
depends_on: ["12-01", "12-02"]
files_modified:
  - crates/mlrs-py/src/dispatch.rs
  - crates/mlrs-py/src/estimators/manifold.rs
  - crates/mlrs-py/src/estimators/cluster.rs
  - crates/mlrs-py/src/estimators/mod.rs
  - crates/mlrs-py/src/lib.rs
  - crates/mlrs-py/tests/manifold_test.rs
autonomous: true
requirements: [BLDR-04]
must_haves:
  truths:
    - "A SECOND macro `any_estimator_typestate!` exists that spells the fitted arms `<f32, Fitted>` / `<f64, Fitted>`; the existing `any_estimator!` is byte-for-byte unchanged (protecting the 35 existing call sites, Success Criterion 3)"
    - "A `#[pyclass(name=\"UMAP\")]` PyO3 shell wraps `Umap` via `any_estimator_typestate!` with a consuming `fit` (`py.detach` + `lock_pool` + `guard_f64` on F64), an `embedding_` accessor, and a runtime `not_fitted` analog on the `Unfit` arm"
    - "A `#[pyclass(name=\"HDBSCAN\")]` PyO3 shell wraps `Hdbscan` with a `labels_` accessor (labels-only, no predict) and the same `fit`/`not_fitted` contract"
    - "Both pyclasses are registered in `mlrs-py/src/lib.rs`; every EXISTING `any_estimator!` call site still compiles and its suite passes (BLDR-04)"
    - "`PyUMAP::unfit_default().is_unfit()` is true (cross-crate smoke without an interpreter); an accessor on the `Unfit` arm returns `not_fitted()` `PyValueError`"
  artifacts:
    - path: "crates/mlrs-py/src/dispatch.rs"
      provides: "Second additive macro any_estimator_typestate! spelling fitted arms <F, Fitted>"
      contains: "any_estimator_typestate"
    - path: "crates/mlrs-py/src/estimators/manifold.rs"
      provides: "PyUMAP via any_estimator_typestate! — consuming fit, embedding_ accessor, not_fitted analog, unfit_default/is_unfit smoke seam"
      contains: "PyUMAP"
      min_lines: 90
    - path: "crates/mlrs-py/src/estimators/cluster.rs"
      provides: "PyHDBSCAN via any_estimator_typestate! — labels_ accessor, not_fitted analog (extends the cluster family file)"
      contains: "PyHDBSCAN"
    - path: "crates/mlrs-py/tests/manifold_test.rs"
      provides: "unfit_default smoke + not_fitted runtime analog (no interpreter)"
  key_links:
    - from: "crates/mlrs-py/src/estimators/manifold.rs"
      to: "any_estimator_typestate!"
      via: "macro invocation producing AnyUmap with F32(Umap<f32, Fitted>)"
      pattern: "any_estimator_typestate!"
    - from: "crates/mlrs-py/src/estimators/manifold.rs"
      to: "crate::errors::not_fitted"
      via: "Unfit-arm accessor → not_fitted(\"umap\", \"embedding_\")"
      pattern: "not_fitted"
    - from: "crates/mlrs-py/src/lib.rs"
      to: "PyUMAP, PyHDBSCAN"
      via: "m.add_class"
      pattern: "add_class::<PyUMAP>"
---

<objective>
Collapse the Rust typestate behind the existing `any_estimator!` machinery so the Python surface is unchanged (BLDR-04). Add a SECOND additive macro `any_estimator_typestate!` (the existing macro cloned with fitted arms spelling `<f32, Fitted>`/`<f64, Fitted>` — D-04) to avoid editing the shared 35-call-site macro. Hand-write two `#[pyclass]` shells — `PyUMAP` (new `estimators/manifold.rs`) and `PyHDBSCAN` (extending `estimators/cluster.rs`) — each following `PyLinearRegression` line-for-line: a consuming `fit` (`py.detach` + `lock_pool` + `guard_f64` on the F64 arm + `build_err_to_py`/`algo_err_to_py`), a fitted accessor (`embedding_` for UMAP, `labels_` for HDBSCAN), and a runtime `NotFittedError` analog (`not_fitted()`) on the `Unfit` arm. Register both in `lib.rs`. Verify every EXISTING call site still compiles + passes (Success Criterion 3) and add the cross-crate smoke + not-fitted tests.

Resolves Open Question 2 (a SECOND macro, NOT an edit to the shared arm — lowest risk to Success Criterion 3) and Open Question 3 (HDBSCAN PyO3 extends `estimators/cluster.rs`, the existing cluster-family file, keeping `estimators/mod.rs` edits minimal). File-disjoint from the compile-fail plan (Plan 03), so the two Wave-3 plans run in parallel.

Implements D-04 (macro fitted-arm change, additive), D-13 (typestate collapses behind `any_estimator!`; reuse single-site error mappers unchanged). Closes BLDR-04.

Purpose: prove the convention is invisible to Python — the same `Unfit/F32/F64` enum, the same error mappers, the same dispatch contracts — and that the 30-estimator suite stays green.
Output: the second macro, two PyO3 shells, `lib.rs` registration, and the `mlrs-py` smoke/not-fitted tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-builder-typestate-convention-foundation/12-CONTEXT.md
@.planning/phases/12-builder-typestate-convention-foundation/12-RESEARCH.md
@.planning/phases/12-builder-typestate-convention-foundation/12-PATTERNS.md
@.planning/phases/12-builder-typestate-convention-foundation/12-01-SUMMARY.md
@.planning/phases/12-builder-typestate-convention-foundation/12-02-SUMMARY.md
@AGENTS.md
</context>

<artifacts_this_phase_produces>
This plan introduces:
- `any_estimator_typestate!` macro (NEW, in `crates/mlrs-py/src/dispatch.rs`) — emits the `Unfit/F32/F64` enum with fitted arms `F32($algo<f32, mlrs_algos::typestate::Fitted>)` / `F64($algo<f64, mlrs_algos::typestate::Fitted>)`
- `AnyUmap` enum + `#[pyclass(name="UMAP")] pub struct PyUMAP` (new `crates/mlrs-py/src/estimators/manifold.rs`) with `unfit_default()`/`is_unfit()` smoke seam, consuming `fit`, `embedding_` accessor(s), `is_fitted`/`dtype`
- `AnyHdbscan` enum + `#[pyclass(name="HDBSCAN")] pub struct PyHDBSCAN` (in `crates/mlrs-py/src/estimators/cluster.rs`) with `labels_` accessor (labels-only), same `fit`/`not_fitted` contract
- `pub mod manifold;` in `estimators/mod.rs`; `m.add_class::<PyUMAP>()?` + `m.add_class::<PyHDBSCAN>()?` + the matching `use` imports in `lib.rs`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add the second additive macro any_estimator_typestate! (fitted arms <F, Fitted>)</name>
  <read_first>
    - crates/mlrs-py/src/dispatch.rs (lines 85-115 — the existing `any_estimator!`; the fitted arms `F32($algo<f32>)`/`F64($algo<f64>)` at :104-106, and the `Unfit { $field : $ty }` arm at :103. CLONE this macro under the new name with ONLY the two fitted arms changed; leave `any_estimator!` byte-for-byte unchanged.)
    - .planning/phases/12-builder-typestate-convention-foundation/12-RESEARCH.md § "Pitfall 2" (lines 419-423 — why `S=Unfit` default makes a naive shared-arm edit store the WRONG arm) and § "Pattern 5" (lines 376-385)
    - .planning/phases/12-builder-typestate-convention-foundation/12-PATTERNS.md § "crates/mlrs-py/src/dispatch.rs" (lines 183-196)
    - crates/mlrs-algos/src/typestate.rs (from Plan 01 — the `Fitted` marker path the macro arms reference: `mlrs_algos::typestate::Fitted`)
  </read_first>
  <action>
    Edit `crates/mlrs-py/src/dispatch.rs`. Add a SECOND `#[macro_export] macro_rules! any_estimator_typestate!` immediately after the existing `any_estimator!`. It is the existing macro cloned verbatim — same `any: $any:ident, algo: $algo:ident $(::$algo_rest:ident)*, unfit: { $($field:ident : $ty:ty),* $(,)? }` matcher, same `Unfit { $($field : $ty),* }` arm — with ONLY the two fitted arms changed to spell the state marker explicitly (D-04):
    - `F32($algo $( :: $algo_rest )* <f32, mlrs_algos::typestate::Fitted>),`
    - `F64($algo $( :: $algo_rest )* <f64, mlrs_algos::typestate::Fitted>),`
    Reason: because `S = Unfit` is the default, `$algo<f32>` would resolve to the WRONG `Unfit` monomorphization in the `F32` arm (Pitfall 2). Do NOT edit the existing `any_estimator!` arm — the 35 existing (no-`S`) call sites depend on it being unchanged (Success Criterion 3). Carry over the doc comment + the `// NOTE (Plan 03)` `#[pymethods]`-hand-written note. This change is additive and file-local.
  </action>
  <verify>
    <automated>cargo build -p mlrs-py --features cpu 2>&1 | tail -5 && grep -c "macro_rules! any_estimator" crates/mlrs-py/src/dispatch.rs</automated>
  </verify>
  <done>`cargo build -p mlrs-py --features cpu` succeeds; `dispatch.rs` defines BOTH `any_estimator!` and `any_estimator_typestate!` (grep count == 2); the existing `any_estimator!` body is unchanged (`git diff` shows only an addition, no edit to the original arm).</done>
</task>

<task type="auto">
  <name>Task 2: Author the PyUMAP shell (estimators/manifold.rs) + lib.rs + mod.rs wiring</name>
  <read_first>
    - crates/mlrs-py/src/estimators/linear.rs (lines 1-183 — the WORKED `PyLinearRegression` shell: imports + macro invocation :11-35, `unfit_default`/`is_unfit` smoke seam :44-56, `#[new]` constructor, consuming-friendly `fit` with `py.detach`+`lock_pool`+`guard_f64` :86-110, accessor `predict_f32` :142-148, `is_fitted`/`dtype` :172-182. Copy line-for-line; the consuming `fit` form is STRICTLY simpler — no `mut`, no `&mut self`.)
    - crates/mlrs-py/src/errors.rs (lines 56-88 — `algo_err_to_py`/`build_err_to_py`/`not_fitted`; REUSE all three unchanged, D-13. `build_err_to_py` already maps the new `BuildError::InvalidMinDist` variant from Plan 02 — no mapper change.)
    - crates/mlrs-py/src/estimators/mod.rs (lines 31-39 — add `pub mod manifold;` alphabetically)
    - crates/mlrs-py/src/lib.rs (lines 199-258 — add `use estimators::manifold::PyUMAP;` alongside :199-216 and `m.add_class::<PyUMAP>()?;` in the registration block near the v3 grouping)
    - crates/mlrs-algos/src/manifold/umap.rs (from Plan 02 — the `Umap` builder setter names + `new()` defaults the `Unfit { … }` arm and the `fit` call must match; `use mlrs_algos::typestate::{Fit, Transform};`)
    - .planning/phases/12-builder-typestate-convention-foundation/12-PATTERNS.md § "estimators/manifold.rs" (lines 199-257) and § "Module wiring" (lines 263-273)
    - .planning/phases/12-builder-typestate-convention-foundation/12-02-SUMMARY.md (the exact builder setter names + default values + public path)
  </read_first>
  <action>
    Create `crates/mlrs-py/src/estimators/manifold.rs`:
    - Imports: `use pyo3::prelude::*; use mlrs_algos::manifold::umap::Umap; use mlrs_algos::typestate::{Fit, Transform};` (NEW surface, NOT `traits::`), `use crate::errors::{algo_err_to_py, build_err_to_py, not_fitted};`, the ingress helpers (`as_f32, as_f64, capsule_to_array, float_dtype, validated_f32, validated_f64, FloatDtype`).
    - `crate::any_estimator_typestate! { any: AnyUmap, algo: mlrs_algos::manifold::umap::Umap, unfit: { <the UMAP-01 hyperparameter fields with their PyO3 types> } }` — the `unfit` field list mirrors the builder setters from Plan 02's SUMMARY (`n_neighbors: usize, n_components: usize, min_dist: f64, spread: f64, …`). Map `Option`/enum params to the PyO3-friendly type the shim passes (e.g. `metric: String`, `random_state: u64` with a `None`→default sentinel like `DBSCAN`'s `DEFAULT_SEED` pattern at `cluster.rs:21`).
    - `#[pyclass(name = "UMAP")] pub struct PyUMAP { inner: AnyUmap }`.
    - Inherent `impl PyUMAP { pub fn unfit_default() -> Self { Self { inner: AnyUmap::Unfit { <sklearn defaults> } } } pub fn is_unfit(&self) -> bool { matches!(self.inner, AnyUmap::Unfit { .. }) } }` (smoke seam, mirror `linear.rs:44-56`).
    - `#[pymethods] impl PyUMAP`: a `#[new]` constructor storing `AnyUmap::Unfit { … }`; a `fit` doing the consuming form — `let fitted = py.detach(|| -> PyResult<AnyUmap> { let mut pool = crate::lock_pool(); match dt { FloatDtype::F32 => { let xd = validated_f32(as_f32(&xa)?, &mut pool)?; let est = Umap::<f32>::builder().<setters from the Unfit params>.build::<f32>().map_err(build_err_to_py)?; let fitted = est.fit(&mut pool, &xd, None, (rows, cols)).map_err(algo_err_to_py)?; Ok(AnyUmap::F32(fitted)) } FloatDtype::F64 => { crate::capability::guard_f64()?; /* BEFORE upload */ … Umap::<f64>::builder()….build::<f64>()….fit(..)…; Ok(AnyUmap::F64(fitted)) } } })?; self.inner = fitted; Ok(())`. Use `crate::lock_pool()` (poison-recovering), NEVER `global_pool().lock().expect()`.
    - `embedding_` accessor(s) (`embedding_f32`/`embedding_f64`, mirror `linear.rs:142-148`): match the fitted arm, call the UMAP `Transform`/`embedding` surface, `to_host_metered(&mut pool)`; on the `Unfit` arm (or wrong-dtype arm) return `not_fitted("umap", "embedding_")` → `PyValueError` (D-13 runtime analog; `not_fitted` already exists — reuse, do NOT add a mapper).
    - `is_fitted`/`dtype` exactly as `linear.rs:172-182`.

    Wire: edit `crates/mlrs-py/src/estimators/mod.rs` add `pub mod manifold;`. Edit `crates/mlrs-py/src/lib.rs` add `use estimators::manifold::PyUMAP;` and `m.add_class::<PyUMAP>()?;` in the registration block (group with the v3 estimators).
  </action>
  <verify>
    <automated>cargo build -p mlrs-py --features cpu 2>&1 | tail -8 && grep -c "PyUMAP" crates/mlrs-py/src/lib.rs</automated>
  </verify>
  <done>`cargo build -p mlrs-py --features cpu` succeeds. `estimators/manifold.rs` invokes `any_estimator_typestate!` and stores a `Umap<f32, Fitted>` in the `F32` arm; `embedding_` accessor returns `not_fitted` on the `Unfit` arm. `lib.rs` registers `PyUMAP`. `estimators/mod.rs` has `pub mod manifold;`.</done>
</task>

<task type="auto">
  <name>Task 3: Author the PyHDBSCAN shell (extend estimators/cluster.rs) + lib.rs wiring</name>
  <read_first>
    - crates/mlrs-py/src/estimators/cluster.rs (lines 1-40 + the full `PyDBSCAN` shell — the labels-only analog: `Fit` + `labels_` accessor, NO standalone predict; `DEFAULT_SEED` sentinel pattern at :21 for `random_state=None`. EXTEND this file with `PyHDBSCAN` following `PyDBSCAN` line-for-line.)
    - crates/mlrs-py/src/estimators/manifold.rs (from Task 2 — the `any_estimator_typestate!` + consuming-`fit` + `not_fitted` template; HDBSCAN differs only in the accessor: `labels_` returning `Vec<i32>` instead of `embedding_`)
    - crates/mlrs-algos/src/cluster/hdbscan.rs (from Plan 02 — the `Hdbscan` builder setters + `new()` defaults + the `labels(&self, pool) -> Vec<i32>` Fitted accessor; `use mlrs_algos::typestate::Fit;`)
    - crates/mlrs-py/src/lib.rs (lines 199, 222-223 — add `PyHDBSCAN` to the `use estimators::cluster::{…}` import and `m.add_class::<PyHDBSCAN>()?;` near `PyDBSCAN`)
    - .planning/phases/12-builder-typestate-convention-foundation/12-PATTERNS.md § "HDBSCAN PyO3" (line 259)
    - .planning/phases/12-builder-typestate-convention-foundation/12-02-SUMMARY.md (the Hdbscan builder setter names + defaults)
  </read_first>
  <action>
    Extend `crates/mlrs-py/src/estimators/cluster.rs` (Open Question 3 resolved: HDBSCAN lives with the cluster family here, not a new file — keeps `estimators/mod.rs` untouched). Add, mirroring `PyDBSCAN` + the Task-2 `PyUMAP` template:
    - Add `use mlrs_algos::cluster::hdbscan::Hdbscan;` and `use mlrs_algos::typestate::Fit;` to the imports (alongside the existing `mlrs_algos::traits::{Fit, PredictLabels}` — NOTE the name collision: the existing `PyKMeans`/`PyDBSCAN` use `traits::Fit`; `PyHDBSCAN` needs `typestate::Fit`. Import the typestate `Fit` under an alias, e.g. `use mlrs_algos::typestate::Fit as TypestateFit;`, OR scope the import inside a submodule, to avoid the two `Fit` names colliding in one file — Pitfall 1 at the call-site level. Pick the alias; it is the minimal change.)
    - `crate::any_estimator_typestate! { any: AnyHdbscan, algo: mlrs_algos::cluster::hdbscan::Hdbscan, unfit: { min_cluster_size: usize, min_samples: usize, cluster_selection_epsilon: f64, cluster_selection_method: String, metric: String, alpha: f64, max_cluster_size: usize } }` (map `min_samples=None` via a sentinel like `0`→resolve-to-min_cluster_size in the builder call, matching the algos default resolution).
    - `#[pyclass(name = "HDBSCAN")] pub struct PyHDBSCAN { inner: AnyHdbscan }` + `unfit_default()`/`is_unfit()` smoke seam.
    - `#[pymethods]`: `#[new]`, a consuming `fit` (same `py.detach`+`lock_pool`+`guard_f64`-on-F64 contract; build via `Hdbscan::<F>::builder()….build::<F>().map_err(build_err_to_py)?.fit(&mut pool, &xd, None, (rows, cols)).map_err(algo_err_to_py)?`; HDBSCAN takes no `y` → pass `None`), a `labels_` accessor (`labels_f32`/`labels_f64` returning `Vec<i32>` via the Fitted `labels()` + `to_host_metered`; mirror `PyDBSCAN`'s i32 labels accessor), `not_fitted("hdbscan", "labels_")` on the `Unfit` arm, and `is_fitted`/`dtype`. HDBSCAN has NO standalone predict (labels-only).
    - Add `use crate::errors::build_err_to_py;` if not already imported in `cluster.rs` (it currently imports only `algo_err_to_py, not_fitted` — add `build_err_to_py` for the new build path).

    Wire: edit `crates/mlrs-py/src/lib.rs` — add `PyHDBSCAN` to the `use estimators::cluster::{PyDBSCAN, PyKMeans};` import and add `m.add_class::<PyHDBSCAN>()?;` near the `PyDBSCAN` registration.
  </action>
  <verify>
    <automated>cargo build -p mlrs-py --features cpu 2>&1 | tail -8 && grep -c "PyHDBSCAN" crates/mlrs-py/src/lib.rs</automated>
  </verify>
  <done>`cargo build -p mlrs-py --features cpu` succeeds. `cluster.rs` defines `PyHDBSCAN` via `any_estimator_typestate!` storing `Hdbscan<f32, Fitted>` in the `F32` arm, with a `labels_` accessor and `not_fitted` on `Unfit`; the two `Fit` names do not collide (aliased). `lib.rs` registers `PyHDBSCAN`.</done>
</task>

<task type="auto">
  <name>Task 4: Cross-crate smoke + not-fitted tests; confirm the 35 existing call sites green</name>
  <read_first>
    - crates/mlrs-py/src/estimators/linear.rs (lines 44-56 — the `unfit_default`/`is_unfit` cross-crate seam the test exercises WITHOUT a Python interpreter)
    - crates/mlrs-py/tests/ (the existing `mlrs-py` integration-test convention; tests live here per AGENTS.md §2)
    - .planning/phases/12-builder-typestate-convention-foundation/12-VALIDATION.md § "Per-Task Verification Map" (TS-pyo3-smoke, TS-not-fitted, TS-existing-green rows)
    - the project MEMORY note "Python wheel untestable in env" — live pytest is OUT; these are Rust-side cross-crate unit tests that run without maturin/pyarrow
  </read_first>
  <action>
    Create `crates/mlrs-py/tests/manifold_test.rs` (and HDBSCAN cases — either here or extend an existing cluster test file; keep both in `manifold_test.rs` for cohesion):
    - `manifold::unfit_default`: `assert!(PyUMAP::unfit_default().is_unfit())` and `assert!(PyHDBSCAN::unfit_default().is_unfit())` — proves the macro-expanded wrapper instantiates in the `Unfit` arm without an interpreter (BLDR-04 smoke).
    - `manifold::not_fitted_before_fit`: call the `embedding_`/`labels_` accessor on an `unfit_default()` instance and assert it returns a `PyValueError` (the `not_fitted` runtime analog, D-13). Use `Python::with_gil` if the accessor signature needs a `Python<'_>` token; assert `result.is_err()` and, if cheaply inspectable, that the error is a `PyValueError`. (Per MEMORY, no live interpreter is needed for these Rust-side `#[pyclass]` calls.)
    These compensate for the deferred live `estimator_checks` (SHIM-03 / MEMORY) — route the live-FFI surface to UAT.

    Then confirm Success Criterion 3 (BLDR-04 regression gate): run the FULL `mlrs-py` suite and confirm every existing `any_estimator!` call site still compiles and passes. The second macro + the two new shells are additive; nothing existing should break.
  </action>
  <verify>
    <automated>cargo test -p mlrs-py --features cpu 2>&1 | tail -25</automated>
  </verify>
  <done>`cargo test -p mlrs-py --features cpu` passes — including the new `manifold_test` cases (`unfit_default`, `not_fitted_before_fit`) AND every pre-existing `mlrs-py` test (Success Criterion 3 / BLDR-04). No existing `any_estimator!` call site regressed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python → PyO3 `fit`/accessor | Untrusted Python args (array capsule, dtype, shape, hyperparameters) cross into the Rust shell. |
| F64 request → backend | An f64 fit request may target an f64-incapable backend (rocm). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02 | Tampering / DoS | Out-of-range hyperparameter from Python reaching the fit allocation | mitigate | The shell builds via `builder().build::<F>().map_err(build_err_to_py)?` BEFORE the fit upload — the data-independent `BuildError` validation (Plan 02) fires first; `build_err_to_py` maps it to a `PyValueError`. |
| T-12-07 | DoS (panic) | f64 fit on an f64-incapable backend (rocm) | mitigate | `crate::capability::guard_f64()?` on the F64 arm BEFORE upload (existing contract, `linear.rs:97`). Algos-side f64 round-trip SKIPS-with-log on rocm. |
| T-12-08 | DoS | Panic in `fit` poisoning the global pool mutex | mitigate | Use `crate::lock_pool()` (poison-recovering, WR-04), NEVER `global_pool().lock().expect()` (`dispatch.rs:30-33`). The trivial fit cannot panic (no compute), but follow the sanctioned path regardless. |
| T-12-09 | Spoofing/Information | predict-before-fit at the Python boundary (no compile guarantee Python-side) | mitigate | Runtime `not_fitted("umap"|"hdbscan", op)` analog on the `Unfit` arm → `PyValueError` the shim re-raises as `NotFittedError` (D-13). Verified by `manifold::not_fitted_before_fit`. |
| T-12-SC | Tampering | (npm/pip/cargo installs) | accept | No new package installs in this plan — `pyo3`/`trybuild` already in-tree (trybuild added in Plan 01, audited OK). |

No `high`-severity threats. V5 Input Validation is satisfied by the `build()`/`guard_f64()`/geometry-guard chain reused unchanged.
</threat_model>

<verification>
- `cargo build -p mlrs-py --features cpu` succeeds (both shells + second macro compile).
- `cargo test -p mlrs-py --features cpu` passes — new smoke/not-fitted tests + every existing suite (Success Criterion 3 / BLDR-04).
- `git diff` on `dispatch.rs` shows only an ADDITION (`any_estimator_typestate!`), the existing `any_estimator!` arm unchanged.
- Phase gate (background per MEMORY): full `cargo test --features cpu` across `mlrs-algos` + `mlrs-py` green.
</verification>

<success_criteria>
- The Python surface is unchanged: the typestate collapses behind the `Unfit/F32/F64` enum via the additive `any_estimator_typestate!`; runtime `NotFittedError` analog at the boundary.
- Every existing `any_estimator!` call site still compiles and passes its suite (BLDR-04).
- The cross-crate smoke (`unfit_default`) and the not-fitted runtime analog pass without a Python interpreter.
</success_criteria>

<output>
Create `.planning/phases/12-builder-typestate-convention-foundation/12-04-SUMMARY.md` when done. Record: the second-macro decision (Open Question 2 resolution), the HDBSCAN-in-cluster.rs decision (Open Question 3 resolution), the `Fit`-alias workaround in `cluster.rs`, and confirmation the 35 existing call sites stayed green. Note the deferred live `estimator_checks` for UAT (SHIM-03 / MEMORY).
</output>
