---
phase: 12-builder-typestate-convention-foundation
plan: 03
type: execute
wave: 3
depends_on: ["12-01", "12-02"]
files_modified:
  - crates/mlrs-algos/tests/compile_fail.rs
  - crates/mlrs-algos/tests/ui/predict_before_fit.rs
  - crates/mlrs-algos/tests/ui/predict_before_fit.stderr
  - crates/mlrs-algos/tests/ui/transform_before_fit.rs
  - crates/mlrs-algos/tests/ui/transform_before_fit.stderr
autonomous: true
requirements: [BLDR-02]
must_haves:
  truths:
    - "A `trybuild` compile-fail test proves that calling `transform`/`predict` on an `Unfit` shell does NOT compile (the structural proof of BLDR-02)"
    - "The compile-fail gate runs under `--features cpu` and fails to compile, referencing the `Unfit` state in a committed golden `.stderr`; the value gate is non-compilation that mentions `Unfit` (accepting E0599 'no method' OR an E0277 trait-bound diagnostic), NOT a specific error code"
  artifacts:
    - path: "crates/mlrs-algos/tests/compile_fail.rs"
      provides: "trybuild::TestCases::new().compile_fail(\"tests/ui/*.rs\") harness"
      contains: "trybuild"
    - path: "crates/mlrs-algos/tests/ui/transform_before_fit.rs"
      provides: "Minimal ui fixture: transform on Umap<f32, Unfit> (must NOT compile)"
    - path: "crates/mlrs-algos/tests/ui/transform_before_fit.stderr"
      provides: "Committed golden expected error"
    - path: "crates/mlrs-algos/tests/ui/predict_before_fit.rs"
      provides: "Minimal ui fixture: a fitted-only accessor on an Unfit shell (must NOT compile)"
    - path: "crates/mlrs-algos/tests/ui/predict_before_fit.stderr"
      provides: "Committed golden expected error"
  key_links:
    - from: "crates/mlrs-algos/tests/compile_fail.rs"
      to: "crates/mlrs-algos/tests/ui/*.rs"
      via: "trybuild glob"
      pattern: "compile_fail"
    - from: "crates/mlrs-algos/tests/ui/transform_before_fit.rs"
      to: "mlrs_algos::manifold::umap::Umap"
      via: "Umap::<f32>::new().transform(..)"
      pattern: "Umap::<f32>"
---

<objective>
Author the `trybuild` compile-fail gate — the MANDATORY structural proof (D-11) that `predict`/`transform`/fitted-attr accessors do NOT exist on an `Unfit` shell, so predict-before-fit is a COMPILE error (BLDR-02). One minimal `tests/ui/*.rs` fixture per before-fit method, each with a committed golden `.stderr`, driven by a `tests/compile_fail.rs` harness.

Depends on the typestate module (Plan 01) and BOTH shells (Plan 02) existing — the ui fixtures import `Umap`/`Hdbscan` and call a fitted-only method on the `Unfit` monomorphization. File-disjoint from the PyO3 plan (Plan 04), so the two Wave-3 plans run in parallel.

Implements D-11 (compile-fail gate mandatory). Closes the structural half of BLDR-02 (the runtime half — fitted accessors + round-trip — is Plan 02).

Purpose: turn the "accessors only on `T<Fitted>`" design into a machine-checked, regression-proof gate so future edits cannot silently re-expose predict-before-fit.
Output: `compile_fail.rs` harness + `ui/{predict,transform}_before_fit.rs` fixtures + committed `.stderr` goldens.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-builder-typestate-convention-foundation/12-CONTEXT.md
@.planning/phases/12-builder-typestate-convention-foundation/12-RESEARCH.md
@.planning/phases/12-builder-typestate-convention-foundation/12-PATTERNS.md
@.planning/phases/12-builder-typestate-convention-foundation/12-01-SUMMARY.md
@.planning/phases/12-builder-typestate-convention-foundation/12-02-SUMMARY.md
@AGENTS.md
</context>

<artifacts_this_phase_produces>
This plan introduces:
- `crates/mlrs-algos/tests/compile_fail.rs` — the `#[test] fn ui()` trybuild harness
- `crates/mlrs-algos/tests/ui/transform_before_fit.rs` + `.stderr` — UMAP `Transform`-on-`Unfit` proof
- `crates/mlrs-algos/tests/ui/predict_before_fit.rs` + `.stderr` — fitted-accessor-on-`Unfit` proof (UMAP `embedding`/`n_features_in` or HDBSCAN `labels`)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Author the trybuild harness + two minimal ui fixtures, generate + commit the goldens</name>
  <read_first>
    - crates/mlrs-algos/src/manifold/umap.rs (from Plan 02 — the `Umap<F, Unfit>` shell; `transform`/`embedding` exist ONLY on `Umap<F, Fitted>`. This is the symbol the ui fixture calls on `Unfit` to trigger the error.)
    - crates/mlrs-algos/src/typestate.rs (from Plan 01 — `use mlrs_algos::typestate::{Fit, Transform};` brings the trait into scope so the diagnostic is "no method", not "unresolved import")
    - .planning/phases/12-builder-typestate-convention-foundation/12-RESEARCH.md § "Pattern 4" (lines 346-374) and § "Pitfall 5" (lines 437-441 — `.stderr` brittleness; keep ui files to ONE method call; feature resolution; regenerate with TRYBUILD=overwrite)
    - .planning/phases/12-builder-typestate-convention-foundation/12-PATTERNS.md § "tests/compile_fail.rs + tests/ui" (lines 310-321)
    - .planning/phases/12-builder-typestate-convention-foundation/12-02-SUMMARY.md (the exact fitted-only method names + public paths to import)
    - AGENTS.md §2 (ui files are `tests/` fixtures — compliant with tests-separated-from-source)
  </read_first>
  <action>
    Create `crates/mlrs-algos/tests/compile_fail.rs`:
    - A single `#[test] fn ui() { let t = trybuild::TestCases::new(); t.compile_fail("tests/ui/*.rs"); }`.
    - Add a header comment documenting the pinned toolchain (read it from `rust-toolchain.toml`/`rustc --version` and record it) and noting that the goldens were generated with `TRYBUILD=overwrite` — `.stderr` is toolchain-sensitive (Pitfall 5).

    Create `crates/mlrs-algos/tests/ui/transform_before_fit.rs` — MINIMAL (one method call, to keep the `.stderr` surface small and stable, Pitfall 5):
    - `use mlrs_algos::manifold::umap::Umap;` + `use mlrs_algos::typestate::Transform;`
    - `fn main() { let est = Umap::<f32>::new(); let _ = est.transform(); }` — the `transform` method does not exist on `Umap<f32, Unfit>` → `E0599` "no method named `transform` found for struct `Umap<f32, Unfit>`". The method-not-found error fires at name resolution BEFORE argument type-checking, so calling `transform()` with no/placeholder args still produces the diagnostic — keep the arg list empty or minimal to avoid argument-count noise in the golden. Pick whichever (no-args vs unit-placeholder) yields the cleanest single-line diagnostic when you generate the golden.

    Create `crates/mlrs-algos/tests/ui/predict_before_fit.rs` — same shape, but exercise a DIFFERENT fitted-only surface so the gate covers accessors too (not just `transform`): call a Fitted-only accessor (e.g. `est.embedding(..)` or `est.n_features_in()` on UMAP, or `est.labels(..)` on `Hdbscan::<f32>::new()`) on an `Unfit` shell → the same `E0599` "no method" on the `Unfit` state. Keep it to one method call. (The file is named `predict_before_fit` per the Wave-0 plan; its body proves a fitted-accessor-before-fit failure — name retained for the Wave-0 contract.)

    Generate the goldens: run `TRYBUILD=overwrite cargo test -p mlrs-algos --features cpu --test compile_fail` on the pinned toolchain. This writes `tests/ui/transform_before_fit.stderr` and `tests/ui/predict_before_fit.stderr`. INSPECT each generated `.stderr`: the value gate is NON-COMPILATION that references the `Unfit` state — accept ANY compile-fail diagnostic whose `.stderr` mentions the `Unfit` marker/state. This is typically `E0599` ("no method named `predict`/`transform`/`embedding`/`labels`… found … in the `Unfit` state"), but a trait-bound `E0277` (an unsatisfied bound, e.g. if the Fitted-only `impl` surfaces as an unmet `where`-clause) is EQUALLY acceptable — do NOT hard-require `E0599` specifically. The only hard requirements: (1) compilation fails, and (2) the diagnostic mentions `Unfit`. The one error to reject is an `E0432`/`E0463` unresolved-crate error — that would mean a missing backend feature (Pitfall 5); if seen, ensure the test ran under `--features cpu`. Commit the committed golden `.stderr` files (generated via `TRYBUILD=overwrite` on the pinned toolchain).

    Re-run WITHOUT `TRYBUILD=overwrite` (`cargo test -p mlrs-algos --features cpu --test compile_fail`) to confirm the gate PASSES against the committed goldens (a passing trybuild compile-fail test means the ui files correctly do NOT compile and match the golden).
  </action>
  <verify>
    <automated>cargo test -p mlrs-algos --features cpu --test compile_fail 2>&1 | tail -25</automated>
  </verify>
  <done>`cargo test -p mlrs-algos --features cpu --test compile_fail` passes (the ui files fail to compile and match the committed goldens). Both `tests/ui/transform_before_fit.stderr` and `tests/ui/predict_before_fit.stderr` are committed and each references the `Unfit` state — the diagnostic is any compile-fail message mentioning `Unfit` (`E0599` "no method" OR a trait-bound `E0277`), NOT a specifically pinned error code. The ui `.rs` files are each a single method call (minimal stderr surface).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan is a compile-time test gate. No runtime input, no allocation, no device kernel, no Python ingress. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05 | Tampering | A future edit silently re-exposing `predict`/`transform` on the `Unfit` state, defeating BLDR-02 | mitigate | The trybuild gate fails (ui file would start compiling, breaking the `compile_fail` assertion) if a fitted-only method ever becomes callable on `Unfit`. Run on every wave merge. |
| T-12-06 | Repudiation | Golden `.stderr` drift across rustc versions masking a real regression | accept | Pitfall 5: keep each ui file to ONE method call (minimal stderr surface) and document the pinned toolchain in `compile_fail.rs`. If stderr proves unstable on this repo's toolchain, the fallback is trybuild's `compile_fail` without exact-stderr matching (still asserts non-compilation) — but the value gate is non-compilation, which holds regardless of exact message. Low risk (Assumption A2). |

No `high`-severity threats. This plan exercises no V5 input surface.
</threat_model>

<verification>
- `cargo test -p mlrs-algos --features cpu --test compile_fail` passes (ui files do not compile, match goldens).
- The goldens reference `Unfit` (any compile-fail diagnostic mentioning `Unfit`: `E0599` or a trait-bound `E0277`, NOT a specifically pinned error code), confirmed by manual inspection during golden generation.
- Per-wave regression: `cargo test -p mlrs-algos --features cpu` still green (the new test target is additive; no source change).
</verification>

<success_criteria>
- A machine-checked structural proof exists that `transform` (and a fitted accessor) cannot be called on an `Unfit` shell — predict-before-fit fails to compile (BLDR-02).
- The gate is regression-proof: any future re-exposure of a fitted method on `Unfit` breaks the `compile_fail` test.
</success_criteria>

<output>
Create `.planning/phases/12-builder-typestate-convention-foundation/12-03-SUMMARY.md` when done. Record: the pinned toolchain version the goldens were generated against, which fitted-only methods each ui fixture exercises, and any `.stderr` stability notes for the verifier.
</output>
